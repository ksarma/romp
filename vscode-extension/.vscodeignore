src/**
.vscode/**
node_modules/**
esbuild.js
install.sh
tsconfig.json
.gitignore
**/*.map
**/*.ts
out-tests/**
# the served labs' build lock and marker (tests/lab_dist.py): vsce reads dist whole, and .gitignore does not govern a VSIX
dist/.lab-build.lock
dist/.lab-built
# the browser-legs CI step (.github/workflows/ci.yml): the roster, the exclusions and the scripts it runs (the step's script, the census, the reporter) are CI-only, not extension code; tools/ci-browser-legs.test.mjs pins these lines
ci-browser-legs.txt
ci-browser-legs-excluded.txt
scripts/**
