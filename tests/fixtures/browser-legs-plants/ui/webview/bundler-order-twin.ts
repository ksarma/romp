// a plant's companion, not a test module: the .ts beside bundler-order-twin.tsx, the file the census read for a specifier spelled
// ./bundler-order-twin before it read the bundler's order from the build (p386); it reaches no browser
export async function go(): Promise<void> {}
