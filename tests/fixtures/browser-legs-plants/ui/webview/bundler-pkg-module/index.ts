// a plant's companion, not a test module: the directory's index, the file the census read for an import spelled
// ./bundler-pkg-module before it refused a directory holding a package.json (p395); it reaches no browser
export async function go(): Promise<void> {}
