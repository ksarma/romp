// a plant's companion, not a test module: the directory's index.ts beside its index.tsx, the file the census read for a specifier
// spelled ./bundler-order-dir before it read the bundler's order from the build (p390); it reaches no browser
export async function go(): Promise<void> {}
