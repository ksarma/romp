// a plant's companion, not a test module: the index.tsx beside index.ts, the file the census read at the order's build for a
// specifier spelled ./bundler-pkg-main-js, the directory's first index in the test build's order (p394); it carries JSX and
// reaches no browser
export const view = <div />;
export async function go(): Promise<void> {}
