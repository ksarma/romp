// a plant's companion, not a test module: the .ts a specifier spelled ./bundler-order-dotjs.js is rewritten to, the file the census
// read for it before it read the bundler's order from the build (p391); it reaches no browser
export async function go(): Promise<void> {}
