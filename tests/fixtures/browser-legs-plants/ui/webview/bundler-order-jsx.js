// a plant's companion, not a test module: the .js beside bundler-order-jsx.jsx, the file the census read for a specifier spelled
// ./bundler-order-jsx before it read the bundler's order from the build (p388); it reaches no browser
exports.go = async function () {};
