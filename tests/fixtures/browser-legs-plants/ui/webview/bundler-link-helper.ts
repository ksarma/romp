// a plant's companion, not a test module: the file ../bundler-link-helper names beside the link's path, ui/webview/bundler-link-dir,
// which the census read for the load in ../bundler-link-target/pkg/index.ts before it refused a load through a symbolic link
// (p398); it reaches no browser
export async function go(): Promise<void> {}
