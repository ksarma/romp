// a plant's companion, not a test module: the .ts beside bundler-order-node.js, the file the census reads, in the bundler's order, for
// the specifier ./bundler-order-node a createRequire-bound loader passes (p392); it reaches no browser
export async function go(): Promise<void> {}
