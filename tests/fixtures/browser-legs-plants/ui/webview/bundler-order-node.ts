// a plant's companion, not a test module: the .ts beside bundler-order-node.js, the file the census reads, under its vscode-extension/
// base and in the bundler's order, for the specifier ../ui/webview/bundler-order-node a loader bound to the working directory's
// package.json passes (p392); it reaches no browser
export async function go(): Promise<void> {}
