// a plant's companion, not a test module: the directory's index, the file the census read for a specifier spelled
// ./bundler-pkg-main, and for ../ui/webview/bundler-pkg-main under its vscode-extension/ base, before it refused a directory
// holding a package.json (p393, p399); it reaches no browser
export async function go(): Promise<void> {}
