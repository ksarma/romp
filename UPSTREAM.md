# Upstream candidates

Things on this fork we may want to offer to the upstream romp project. This is a QUEUE for
decisions, not a promise: per `CLAUDE.md`'s fork rule, nothing goes upstream — no PR, no issue, no
advisory — until the user says so, per change. Add a row when you land something upstream-worthy;
the user prunes or promotes.

A good candidate fixes or improves something upstream has too. Fork-specific infrastructure (the
private-strings hooks, fork-remotes guard, this file) never qualifies.

Status meanings: **candidate** (worth offering, decision not yet made) · **waiting** (candidate,
but gated on something first) · **offered** (a PR is open upstream) · **merged** (landed upstream via
our PR) · **landed** (upstream took it, possibly rebuilt clean) · **resolved upstream** (they fixed
it independently) · **declined** · **keep-private** · **divergence** (the fork deliberately
differs from upstream here; not for offering as-is) · **follow-up** (a fold left work pending).

| What | Where it lives here | Status | Notes |
|---|---|---|---|
| The 2026-08 security audit, shipping upstream as PER-CHAIN PRs (cookie/Origin gate on `/ws`, relay Content-Type, forged postal origin, summarizer `--safe-mode` + private cwd, judge scratch off `/tmp`, serve token out of argv, VS Code exec path, postal marker forgery, judge prompt marks) | PR #1 (`f7761f97`); fork commits `da467ecb` / `b393aaa8` / `c823e9ed` / `3e5d33f2` / `7e6742f2` / `a0e95cd7` | ✅ **CLOSED — all 8 MERGED upstream** (2026-08-15); came home in the 2026-08-15 tip fold (`upfold0815`), which converged the fork's pre-PR copies to the upstream-reviewed final text | DECISION 2026-08-14: fixes go upstream as PRs, not a private advisory — the fork is public (findings already in its history), upstream has no private-reporting intake, and several PRs make the code match SECURITY.md's own claims. **RE-VERIFIED AGAINST v0.11.0 (2026-08-14)** after the v0.9/0.10/0.11 fold-in, with every still-vulnerable verdict attacked by two adversarial refuters. Nothing was fixed by the three releases; the sweeps found **zero** new HTTP routes and no new exec/`/tmp` primitives since v0.8.0. **Chain 1** → MERGED, their #337 (+ follow-up #362). **Chain 2** → MERGED, their #352 (maintainer rebuilt after our #340 leaked a home path). **Chains 3–5 + follow-ons — ALL MERGED 2026-08-14/15, no changes requested:** #378 forwarded-origin trust cap (the one High); #379 judge+announcer off `/tmp`; #380 serve token out of curl argv; #381 postal marker form (reframed as CORRECTNESS — the security framing was refuted); #382 judge prompt marks + N1 (opened draft; the maintainer un-drafted it); #387 uninstall guard canonicalisation; #390 `from_id` header injection; #391 `_hydrate_postal` recipient check. Each was pinned by tests that fail on unfixed upstream, verified against a green 4551-test baseline. **Rebase note:** #380/#382/#387 all needed a rebase onto `c3f9ecc5` before merging, and two of those rebases changed what shipped — #380 gained a FIFTH token-in-argv site (`romp new -m`'s `/send`) that upstream added after the branch was cut, without which the PR's own claim would have been false on merge; #382 had to move the `UNTRUSTED_SYS` trust boundary below upstream's new `_with_user_notes()` so a user style-note could not nominally outrank it. A mechanical rebase would have shipped both wrong. Also corrected: #382's "14 content sections" was always wrong (it is 33 `_sec()` sites across 13 builders; 14 was the test-case count). **Closed out, not offered:** the summarizer prompt-marking (D6) as accepted risk (deprecated, default-OFF, tmux-only, ≤100-char sink); the browser-argv token (`?c=`) as a fork-only DESIGN call, flagged in #380's body; `/recall` session impersonation as unpatchable without a per-session postal credential, flagged in #390. Several audit impact claims did NOT survive refutation and must not be repeated — see the close-out doc referenced in `~/romp-handoffs/security-session.md`. |
| Uninstaller scratch-path guard hardening (canonicalise before the checks; property tests over the spelling class) | PR #2 (`180e2bea` etc.) | ✅ **MERGED — their #387** (2026-08-15) | **The 2026-08-13 "mostly fork-only, low-severity, not an attacker vector" assessment on this row was WRONG, and was corrected by direct probe on 2026-08-14.** Upstream IS affected, and worse than the fork was: `ROMP_JUDGE_SCRATCH="$STATE"` with a plain `--yes` and no `--purge` deletes `$STATE` outright — no path mangling needed at all, because `$STATE` contains `romp` so it clears the name check and nothing refuses the state root. `"$HOME/"` deletes `$HOME` where the home path contains `romp`. Also newly found, in neither the fork commits nor the audit: `rm -rf "$link/"` cannot unlink a symlink so it recurses and EMPTIES the target directory. Two claims that did NOT hold and are not in the PR: the trailing-newline variant is inert upstream (the path does not exist, so `rm` no-ops), and `$HOME/.` aborts mid-run rather than deleting. Preconditions are honest in the PR body: `ROMP_JUDGE_SCRATCH` is an uninstaller-only override nothing in romp sets. **Ordering:** we advised landing #387 before #379; it went the other way, so between the two merges `main` briefly carried #379's `realpath` slug with no symlink guard — the validated string and the deleted target were different paths. #387 closed it on 2026-08-15. Re-proved against post-#379 `main` before merging, not against the original base. |
| Seventeen mid-test `! grep` bats assertions that asserted nothing, armed — grown to NINETEEN at offer time | PR #3 (`b35a55a3`); offer branch `bats-arm` (`5250464f`, single commit off upstream `d0f7c423`) | **merged** — their PR #383, merged as-is 2026-08-14 (merge `7e37f993`; our head `5250464f` is an ancestor of their main, verified 2026-08-15) | Offer branch cleaned up post-merge; CAME HOME in the 2026-08-15 tip fold (`upfold0815`). One residual: upstream's #380 test (`tests/romp-wake-hook.bats`) later added a token-absence check as a bare MID-test `! grep` — the exact inert class this offer armed — which the fold kept in the fork's armed `run`+status form; see the wake-hook row below. Pure test-quality fix, no security angle. Re-verified 2026-08-14 against upstream tip `d0f7c423`: all 17 sites still inert, plus TWO newer inert sites at their `tests/romp.bats` old-claude launch test (arrived via the fork-authored inbound-accept commit upstream merged pre-v0.8.0; the fork re-armed its own copies in `2680e758`), so the offer arms 19. Re-derived, not cherry-picked (two hunks had drifted context at tip); the ~36 final-command negations are deliberately untouched (`!` genuinely works there). Adversarial review corrected the census (55 negated checks at base, not 56) and a flip-verification overclaim in the commit message before offering; all 159 tests across the six files green locally and on CI (the fork's push CI is main-only — a since-closed fork PR #62 provided the pull_request run). |
| The #380 token-absence check in `tests/romp-wake-hook.bats` sits MID-test as a bare `! grep` — inert under bats' errexit, so the serve-token-never-in-argv property it guards is unasserted upstream | the fold kept the fork's armed `run`+status form (fork commit `b393aaa8`; resolution in `upfold0815`) | ✅ **resolved upstream — their #403** (2026-08-15, the security session's follow-up) | Found resolving the 2026-08-15 tip fold: upstream's merged #380 carried the check in the exact class #383 armed (a `!` pipeline is exempt from errexit unless it is the test's final command; this one had two commands after it). Routed to the security session same day; they confirmed, fixed it upstream as #403, and swept every upstream .bats for the pattern — that line was the ONLY remaining instance (expected: #383 had armed the rest). Audit lesson from their repro, for any future absence-check re-audit: an inert check is MASKED whenever the probe's leak also breaks something adjacent (their first probe dropped `--config` with `-H`, so a LATER armed line failed and the test went red anyway) — prove inertness with a leak that keeps everything else well-formed, or the audit passes checks that assert nothing. |
| Judge scratch-refusal blanking card summaries (`""` counted as a model failure) | PR #4 (`a0e95cd7`) | ✅ **resolved upstream** — rode the fork’s security PR #379 | CLOSED 2026-08-23 without an offer: re-verification found the fix ALREADY at their tip byte-identical (the paused-flag "SKIPPED, not failed" block + give-up counters + the pinning test in their test_judge_scratch_private.py, zero diff vs the fork) — it travelled inside the scratch-dirs security chain. User-visible bug upstream too, independent of the security work. |
| Rewind goal cleanup: goals minted from rewound-away turns survive the one-shot `t>=cut_t` sweep by five independent paths (judge parse rewind-blind in the armed window + pinned pass frames; save-rebase resurrection of swept nodes; rewinds romp never sees — native Esc-Esc, `forkAt`, silent `cut_t=None`; sweep at ARM archives goals for rewinds that then fail; task-store mirrors), and the auto-nudge then quotes the orphan into a conversation with no memory of it | fork branch `goalrewind`, four commits: exported chain predicate (`em.chain_membership`) + judge `leaf_override` + write-moment mint guards / `rewindSwept` rebase tombstones / two-phase hide-at-gesture, archive-at-branch-take, loud `cut_t=None` / event-keyed dead-branch reconciliation + one-time boot migration | ✅ **merged** — their PR #544, 2026-08-22 (merge `782c617a`); came home in the 2026-08-22 tip fold (`upfold0823`) | MERGED 2026-08-22: the maintainer REBASED the three commits (landed as `c4dd1f69`/`56536976`/`27351adf`) and dropped the branch’s LRU-lock base commit as already merged via their #538 — rewind content verified byte-identical to the offer. Branch retired; the fold converged the fork to the merged shape (a re-audit reads the merged commits, not fork history). OFFERED 2026-08-20 (branch `rewind-cleanup`, `c6a1ff63` off tip `f3ec1297`): build_session hunks re-derived at tip; interaction with upstream’s 2026-08-19 nudge-gates rework checked (a held card cannot be nudge-quoted); adversarial review clean, scaffold CI green (since-closed fork #96). Upstream ships the same one-shot sweep (`drop_goals_after`, one commit, never revised) and all five paths verbatim; dual-resident live+archive node ids are the smoking gun in any store that has seen rewinds. Every mechanism pinned red-first by tests that fail on unfixed code (tests/test_judge_rewind_cleanup.py + extensions to the two existing rewind suites). |
| The /file relay 404s every remote TEXT file (its view gate predates the text half), so the viewer fails on a federated session's .py/.md with no Download offer | fork PR #61 (`main`) | ✅ **merged — rode their PR #385** (2026-08-18) | Pure bug fix; upstream ships the same preview-only gate. The fix keeps the lying-remote defense exactly (Content-Type still derived locally — text/plain + nosniff is inert) and is pinned by a new relay test. ALREADY FOLDED into the file-links offer: #385's commit A carries the `_remote_file` text-relay widening and its test (added at port time, 2026-08-14 — without it the viewer 404s every federated text file, contradicting the PR's own federation claim), and it survived the 2026-08-15 rebase intact (interdiff-verified). Resolved with #385's merge; came home in the v0.12.0 fold (`upfold0819`), which layered upstream's resumable-206 relaying and tunnel redial onto the same `_remote_file` without disturbing the fork's mirrored save-conflict headers. |
| The dashboard file browser: browse a session's tree (listDir WS op + breadcrumb overlay), open files in the viewer, honest download-only marking, federation for free via the sid-routed splice | fork PR #67 (+ plan in plans/file-browser.md) | ✅ **merged** — their PR #542, 2026-08-22 (merge `9aad69f1`); came home in the 2026-08-22 tip fold (`upfold0823`) | MERGED 2026-08-22: the maintainer rebased our two commits (content-intact, landing as `484f3426`+`0e42b09b`) and added HIS OWN third commit (`614ea7d9`) gating raw-mode saves behind an explicit machine-wide opt-in (the viewer's popup posts a kernel-side setting, broadcast to every attached kernel, and the save route refuses without it) that also tells the owning session — his answer to the write-widening tradeoff the PR body flagged. Branch retired; the fold adopted the gate whole and re-grafted the fork extras (the fileLinkPane relay algebra; the injected-voice roster now covers their edit trace). OFFERED 2026-08-20 (branch `file-browser`, two commits `ac431950`+`35498dbf` off tip `f3ec1297`: browse/open + separately-droppable raw edits): re-derived onto tip’s re-signed (sid,text)→boolean comment sink with a NEW test for the uncovered !landed-while-editing cell; the __rompFeedWasOff pin flipped to browser ownership; droppability proven — full suites green at the commit-1 state alone; review clean, scaffold CI green (since-closed fork #97). Pure feature, no fork-specific content; review-hardened (eight defects found+fixed+pinned before landing). Its gate cleared 2026-08-18: #385 merged upstream, so the viewer surface exists there. Any offer must be RE-DERIVED against the v0.12.0 viewer, not cherry-picked from fork history — upstream evolved the viewer into a pane-agnostic chat modal with a review-comment layer, and the fold (`upfold0819`) reworked the browser's seams to match (the kernel shell relays only `browseFiles` now; feed.css mirrors the modal + comment selectors). Slice 2 (raw-mode edits with mtime conflict refusal) has been live on the fork since PR #67-era work; the offer could carry either slice alone or both. |
| The viewer's GitHub link: a lazy sid-routed fileGitLink op (git on the owning machine answers toplevel/tracked/origin), an anchor that appears only on a real URL | fork PR (fbgh branch) | ✅ **merged** — their PR #543, 2026-08-22 (merge `01ee7f5d`); came home in the 2026-08-22 tip fold (`upfold0823`) | MERGED 2026-08-22: rebased onto the just-landed file browser (landing as `a04be8aa`), plus the maintainer’s follow-on commit (`fbc0a1b5`) generalizing the anchor into a viewer ACTION REGISTRY (the GitHub link is its first entry), which the fold adopted as the shared shape. Branch retired. OFFERED 2026-08-20 (branch `git-link`, `d4129996` off tip `f3ec1297`): anchor re-derived between Wrap and Download (upstream never took raw edits) with self-contained WS poster plumbing; the body offers to rebase around the sibling file-browser PR; review clean, scaffold CI green (since-closed fork #93). Follows the file-links/viewer offer like the rest of the viewer family; pure feature, no fork-specific content. Its gate cleared 2026-08-18 (#385 merged upstream). Like the browser row: re-derive against the v0.12.0 viewer (the fold placed the anchor in the acts bar between Edit/Save and Download), don't cherry-pick fork history. |
| ROMP_EXPECTED_AUTH: a box declares its intended auth side, so apiKeyHelper boxes stop ringing the per-init "launched for the login" false alarm (the never-silent mismatch check inverts to flag the CONTRADICTING landing), and the all-keyed telemetry silence becomes honest surfaces (once-per-episode refresh_usage line, telemetryUnavailable + rail hover, authLive Billing row, persisted apiKeyAuth) | fork branch `apikeyauth` (feature `b5669326` + review fixes) | ✅ **merged** — their PR #577, merged as-is 2026-08-23 (merge `22343b2d`) | MERGED as-is (head is an ancestor of their main, verified same day). Branch retired. OFFERED 2026-08-23 (branch `expected-auth`, `87fca5a4` off tip `147678b1`): Status-interface line inserted surgically without fork churn; the #563 usage-poll interaction explained in the body; review clean, scaffold CI green (since-closed fork #107). Upstream ships the same `_note_auth_source` comparison and the same `api_key_auth` gates, so the permanent false alarm and the silent all-keyed dead-end exist there verbatim. Undeclared behavior is byte-identical; validated on the fork first. |
| Extension self-update dead end: offer the copy-the-command action there too, with ONE shared wording between the toast and the palette error (`CANT_REBUILD`/`MANUAL_REMEDY` already exported by update-target.ts) | the fork's pre-convergence extension.ts — parent of the `extconverge` commit; recoverable from history | ✅ **merged** — their PR #890, merged as-is 2026-09-03 — ✅ came home in upfold0905 (2026-09-05) | MERGED as-is (head is an ancestor). Branch retired. OFFERED 2026-09-02 (branch `ext-self-update`, `020bd1db` off tip `31d8731b`, extension.ts + test only): recovered from the fork’s pre-convergence shape and re-derived onto upstream’s current file; review clean, scaffold CI green (since-closed fork #164). UX-only. The fork carried this shape until 2026-08-14, when the user ruled extension.ts converges to upstream's text and anything worth keeping is OFFERED instead (this row). Upstream's dead-end toast today names the remedy but hands over no action; their update-target.ts already exports the constants, so the offer is a small extension.ts-only diff. |
| Timeline drop-downs clip below the short bottom band (flip/clamp, then host-document render) | PRs #7 + #8 | landed | Confirmed live, then offered as romp-on/romp#221 (2026-08-07, the user's call): both halves squashed to one commit on `timeline-menu-clip`, cut off upstream/main. MERGED upstream (their #221 → `72f5dfe1`, back on the fork via the v0.6.0 merge, PR #35); the backing branch is cleaned up. |
| Fast mode for SDK sessions (the `fastMode` flag-settings opt-in, a chat chip, the timeline star) | PRs #12 (`900a37ce`) + #14 (`26921e7a`, `08d80126`), on fork main | **landed** — their PR #255 MERGED 2026-08-10, in v0.7.0 | Upstream shipped their OWN fast-mode toggle on 2026-08-07 (`258d9719`, their PR #222) hours after ours, and **it does not work**: they send the literal `/fast on` without ever supplying the flag-settings key, so the CLI refuses every SDK session (`fast_mode_state=off reason=sdk_opt_in_required`). The re-check ran 2026-08-09 against their v0.6.0 tip: nothing changed — `git log -S` for `fastMode`/`fast_mode`/`fastReason` over their 42 new commits is empty. CORRECTION to this row's earlier text: their badge is NOT dead-clickable — their kernel blanks it server-side whenever `fastReason` is set (their `kernel.py`, the `"fast": "" if tm.get("fastReason")` gate), so their real failure mode is an **inert feature**: no control ever appears on an SDK session and fast mode can never be enabled; any offer must say that, not "your badge is dead". Verified in CLI 2.1.224: the flag is both the unlock and the starting state, and once a session is CONNECTED with it, literal `/fast on\|off` works live in both directions — so the minimal offer hews to their implementation: persist the ask as a reg bool, mirror it into a per-session settings file at every connect (per-session because `options.settings` is one path and ultracode shares it), keep their literal-send body when the connection carried the flag, reconnect only for the first opt-in. That fix is `sdk_backend.py`-only (~+70/−30); their kernel, UI, badge, parking, and `fast-toggle.test.ts` survive as-is, and it also cures their second latent bug (fast state silently lost on any reconnect). Adopted on the fork 2026-08-09 (PRs #35/#37/#38: option A, full convergence — the fork and the offer are the same implementation) and live-tested on the fork's own kernel: first opt-in reconnects once, later toggles are live, state persists across restarts. Offered upstream as their PR #255 (branch `fast-opt-in-offer`) and MERGED 2026-08-10 into v0.7.0. Upstream then built PAST it — persisted `liveFast`/`liveFastReason` seeding across kernel restarts, a shared `_adopt_fast_state` connect+init path, the curable `sdk_opt_in_required` handling, humanized refusal toasts, and the one-word Fast/Slow badge — all adopted back on the fork in the v0.7.0 merge (2026-08-11), which took their newer generation wholesale. The offer branch can be cleaned up. |
| Cost view cannot see fast mode (footnote + price-table comment) | PR #18 (`890be196`) | keep-private | The user does not want to invest here (2026-08-08). The gap is documented, not fixed; the rail's spend figure was never affected. Multiplier is known exactly if it ever matters: the CLI states `$10/$50 per Mtok` against Opus 5's standard `$5/$25`, so 2x — but applying it needs per-turn fast recording that does not exist. |
| The dashboard as an installable home-screen app + Web Push for the bell events (manifest/icons/Apple metas, iOS-standalone safe-area, `/sw.js` + `/push/*` + VAPID on stdlib+`cryptography`, tap-to-open aimed by wid, `setAppBadge` needs-you count) | PRs #9, #15, #20, #21; `plans/ios-app.md` | landed | All kernel-side code upstream ships too; live-verified end to end on an iPhone 2026-08-08 (install, lock-screen push, tap lands on the firing session, badge). Self-contained: no new hard dependency (`cryptography` is a soft dep behind a loud 500) and no bundle changes. Includes the raw-run test-state hardening (#20) and the SW `skipWaiting` lesson (#21). LANDED upstream 2026-08-08 as `b8b127e3` (the squashed offer), and they built on it (Android cookie persistence `7cfd44f6`, one-device subscription relay `fb1c8972`) — all back on the fork via the v0.6.0 merge (PR #35). |
| Bundle staleness check never watched `ui/webview`, so dashboard edits silently never shipped | PR #24 | resolved upstream | Upstream ships the same `_ensure_bundles` and the same bug: it scans only `vscode-extension/src`, while the browser UI is BUILT from `ui/webview` (render.ts, styles.css) — so an edit there leaves `dist/render.js` stale and the kernel serves the old dashboard with the source correct and pushed. It hides behind luck: any change that also brushes `src/` rebuilds everything. Found live (the user 2026-08-08) when a fast-mode badge stayed blue in the chat while the timeline's star went orange — the timeline pane is served verbatim from source, the chat from dist. Pure bug fix, no fork-specific content. RESOLVED upstream independently in v0.6.0 (`276571f8`, "the bundle watch sees its sources"); the fork kept its own `_bundle_inputs` in the merge as a superset — check the residual delta before ever offering it, likely not worth a PR. |
| The picker's Browse… (and the chat's 📎) do nothing on any non-macOS kernel | PR #25 | **landed** — their PR #258 MERGED 2026-08-10, in v0.7.0 | Upstream ships the same `_pick_folder`/`_pick_file`: an `osascript` call whose `OSError` is caught and returned as `None`, so off macOS the click sends `browseDir`, gets no reply at all, and the button looks alive while doing nothing. Found on a headless Linux kernel (the user 2026-08-08). Fix adds the Linux pickers (zenity/qarma/yad/kdialog, gated on `DISPLAY`/`WAYLAND_DISPLAY` — an installed picker with no screen hangs, which is the same silent nothing), advertises `nativeDialogs` on `sessionList` + `/defaults` so the UI drops a button that cannot work, and warns instead of swallowing a click it can't serve. Pure bug fix, no fork-specific content; anyone running the kernel on Linux hits it. |
| The chat's 📎 opens the picker on the wrong machine from a remote browser; oversize attachments vanish silently; a remote session's attachment bytes land on the wrong kernel | PR #29 | **landed** — their PR #258 MERGED 2026-08-10, in v0.7.0. Upstream then added the pending-attachment chip (up at pick time, retired by the `droppedPath` ack / new `dropSaveFailed` nack), adopted back on the fork in the v0.7.0 merge | Follows #25 one layer up, the same split #26 made for file links: upstream's desktop 📎 posts `pickFile`, so a native dialog opens on the KERNEL's screen — the wrong machine whenever the dashboard is read from another device, and on a headless kernel only a warning. Fix routes by host: the web dashboard opens the BROWSER's own picker (the hidden `<input type=file>` flow touch already used, unscoped + multi-select on desktop, still photo-scoped on phones) and ships bytes over the existing `dropFile` path; the VS Code webview keeps the host dialog, which is correct there. Also: the 50 MB `shipFileToHost` cap used to `return` silently (a big drop just vanished) — now a toast names the file, its size and the cap; and `dropFile` now carries the session id so federation routes the bytes to the kernel that OWNS the session (the saved `drops/` path rides the prompt and is read by the agent on that machine — before this, a remote session's attachment saved onto the viewer's local kernel and handed the agent a nonexistent path). Pure bug fixes, no fork-specific content. |
| Clicking a file link does nothing when the dashboard is read remotely (and on any non-macOS kernel) | PR #26 (+ #30 viewer formatting, #32 downloads, the error-body slice of #28, and the 2026-08-14 text-relay widening); offer branch `file-links` (commits `5b029f03` + `3b2da9b7` off upstream `d0f7c423`) | ✅ **merged** — their PR #385, merged as-is 2026-08-15 (merge `3ff48756`, in v0.12.0; our head `7c0cb37e` is an ancestor of their main, verified 2026-08-20) | MERGED hours after the 2026-08-15 rebase that adapted to their feed-artifact removal (`c1f0dc03`); both commits taken verbatim. Came home in the v0.12.0 fold (`upfold0819`), where upstream had already evolved the viewer (pane-agnostic chat modal, review-comment layer) — the fork adopted their newer generation. Offer branch retired post-merge. Was OFFERED as TWO commits in one PR — viewer/routing/text-serving first, the whole download layer second — so the maintainer can drop or reshape the downloads scope independently; the body's tradeoff paragraph flags the read-widening (view: any text-extension path; download: anything on disk) as deliberate and theirs to scope. Re-verified 2026-08-14: upstream tip still has the swallowed macOS `open`, `/file` still images+PDF only, no viewer, no downloads; their #257/#258 stopped one layer below. Port re-derived from the fork-vs-upstream diff (fork commits had three-release-stale bases); the three view-side relay security tests (lying-remote Content-Type, nosniff, extension-decline) were deliberately left OUT — they belong to the security session's per-chain PRs. The adversarial review found a REAL bug in `_relay_download` (a remote truncating cleanly short of Content-Length left the browser hanging on a keep-alive connection): fixed in the offer's commit B with a red-first direct-call test, and the identical fork-main copy fixed via fork PR #65 (merged 2026-08-14). Fork CI green via a since-closed scaffold PR (#64; push CI is main-only). The sibling of #25, one layer up. Upstream's `_open_file` is the same swallowed macOS `open`, so off macOS a path click is silently inert — but the deeper problem survives fixing that: `openFile` opens on the KERNEL's screen, which is the wrong machine whenever the dashboard is being viewed from another device, and upstream ships the same remote/`tailscale serve` story. Fix widens `/file` (already serving images + PDF to the browser) to source/text with its own 2 MB cap and a NUL sniff, and routes clicks by host — VS Code editor, or a viewer filling the feed pane on the web. Reuses `fileUrl`, so federated sessions work unchanged; the viewer has since gained its formatting layer (the hljs palette on the feed sheet, sanitized rendered-markdown as the `.md` default, a word-wrap toggle with counter-based numbering). Pure bug fix plus the viewer it needs; no fork-specific content. Note it widens what an authenticated dashboard can read to any text-extension path on the box, which is the tradeoff to flag if this is ever offered. The viewer has since also gained browser DOWNLOADS (`filedl`): `?download=1` serves ANY existing file as a streamed `application/octet-stream` attachment (fixed-chunk writes — `_send` slurps, and a multi-GB file would OOM the kernel), the exists-but-unviewable view case is its own 415 so the client offers Download where a true 404 offers nothing, and the `/remote/<host>/file` relay passes `download=1` through with locally-derived attachment headers. Scope is deliberate (the user 2026-08-09): anything on disk is downloadable — the view allowlist is a rendering choice, not a security boundary, since the dashboard's owner can already read any file through an agent — which extends the same tradeoff above from text-extension paths to everything, the headline caveat if this is ever offered. |
| Chat file links are shape-guessed, so a bare `render.js` becomes a link that 404s on click | PR #28 (`linkresolve`) | **landed** — their PR #257 MERGED 2026-08-10, in v0.7.0 | Upstream ships the same shape-only linkifier (`CLICKABLE_PATH_RE` + the looksLike gates) with nothing checking existence. Fix resolves every path-shaped token kernel-side at message-build time (exact stat → unique `git ls-files -co` suffix → unique basename; ambiguity never guessed) and ships `{token: real target}` as `pathLinks` on the chat event; the client keeps all its shape gates and adds map membership, so shortened mentions are FIXED to their real file and phantom paths stay prose. Includes the `/file` error bodies naming the resolved path and the 2.0 MB text cap. Old events without the key keep today's behavior, so it degrades cleanly. Pure fix, no fork-specific content. |
| Dashboard status pips: an UNREADABLE session state renders as nothing, same as a healthy idle one | `pips` branch PR | **landed — merged upstream as their #336 (2026-08-14)** | Upstream ships the same seam: `feed.ts`'s `dotFor` falls to `""` for any session not in the payload's `working`/`awaiting` name lists, so a session whose state the kernel knows (e.g. a latest `waiting` record in states/) draws NO pip — indistinguishable from a rendering hole or an unreadable state — and the fleet pane's dot is working-only (it doesn't even show the straw awaiting dot the feed shows for the same session). Fix: build_feed emits the other two quarters of a TOTAL partition (`ready` = alive and quiet, `stateUnknown` = listed but live state unreadable); feed + fleet render all four explicitly (hollow steel ready ring, gray unknown ring, self-explaining tooltips), with a bare name reserved for payloads that predate the lists — so an old REMOTE kernel in a federated merge keeps the legacy look instead of reading falsely as "unknown". Pure fix, no fork-specific content; tests use the synthetic `web`/`api`/`tests` demo world. REWORKED 2026-08-14 to the maintainer's design call: drop the "ready" ring (a blank keeps meaning "alive and quiet"), keep only the gray unreadable-state ring; rebuilt as a single commit off current `main`. The FORK was converged onto the same 3-state design (fork PR #54, merged) before the offer landed; post-merge the two copies were verified code-identical (residual deltas are comment wording and the fork's OPENING-state strip ladder, both deliberate). Offer branch cleaned up. |
| The auto-nudge status-checks goals mid-oscillation — a blocked-on-the-user card flipped to 'working' by an unblock gets nudged in the gap before the closer's next word | fork PR #48 | **landed — merged upstream as their #333 (2026-08-14, as-is)** | Upstream ships the same `_nudge_fire_list` and judges, so the same flap produces the same false status checks: a goal blocked on the USER's decision is unblocked by "new work filed" / "answered in passing" rulings that don't answer the block, the nudge reads the flip as a stall, and the closer re-files the same block minutes later. Five live fires in one weekend on the fork. Fix holds a goal whose newest judge row is an unblock WITH an earlier judge unblock on record (a goal's FIRST unblock stays nudgeable — that doctrine's own incident was a wrongly-gagged nudge, pinned in tests) until the closer's next word, loudly via the deferral record + backstop; `held` rows carry per-goal whys. Pure fix, no fork-specific content; tests rebuild the incident shapes synthetically. Review arc worth remembering: the maintainer first closed it for a writer-side fix (floor a re-block on the lift's evidence time), his own suite refuted that (`test_interrupt_lift_evidence_time` — an interrupt stamps the lift and the legitimate follow-up block at the SAME evidence time, so there is no writer-side signal), he reopened, then also retracted his adjacent-cycle discriminator tweak after checking it against the first-unblock doctrine test, and merged UNCHANGED. Fork copy verified code-identical (comment-attribution deltas only). Offer branch cleaned up. |
| Kernel-built event timestamps are naive box-local — every rail stamp renders at the KERNEL's wall time on any viewer in a different timezone | `tzstamp` branch PR | **MERGED — upstream PR #331** | Upstream ships the identical `iso()`: `datetime.fromtimestamp(t).strftime(...)` with no offset suffix, shipped in chat-event payloads and recovered client-side with `Date.parse`, which reads an offset-less string in the BROWSER's timezone. Net effect: the rail shows the kernel box's wall-clock digits on every viewer — invisible while box and viewer share a timezone, wrong by the full UTC offset the moment they don't (found reading a UTC devbox's dashboard from a Mac, 2026-08-10). The CLI's own transcript records were never affected (they carry '…Z' and both ends honor it); only kernel-BUILT events (orphan replies, retry notes, effort notes, clear boundaries, atom stamps) took the naive path. One-line fix: `fromtimestamp(t, timezone.utc)` + a 'Z' suffix — the same wire form the transcript already wears. Test round-trips under non-UTC process timezones. Pure fix, no fork-specific content. |
| The durable bg-task scan doesn't know the Monitor tool — a monitor-held session reads idle to the lift/nudge/awaiting machinery | `monscan` branch PR | **MERGED — upstream PR #335** | Upstream ships the identical two-shape `_scan_bg_tasks` (backgrounded Bash + async Agent): a session idle behind a Monitor watch scans as "nothing dispatched", so its awaiting stamp can lift only by the 6h backstop and every consumer of the pairing is blind to the wait. Their live SDK lifecycle path handles monitors fine (probe-verified here — same stream contract), so the gap is transcript-durability only, but that is the path the judge's settled gate and the stamp lift ride. Fix registers the Monitor launch (persistent ones excluded — a session-length subscription is furniture, not awaited work), guards the parser's missing-status→"completed" default so a wrapped monitor EVENT can never end a live watch, fails phantom launches whose ack errored, and emits the launch-recorded lifetime ceiling (`timeout_ms`) as a deadline consumers expire on — which also closes the dead-CLI staleness hole bash tasks can't close (they record no bound). Shapes corpus-derived from 44 real monitor lifecycles; tests rebuild them synthetically. Pure fix, no fork-specific content. |
| The kernel's WebSocket receiver drops fragmented client messages — any large browser→kernel send silently evaporates | `wsfrag` branch PR | resolved upstream; **residual MERGED — upstream PR #332** | Upstream fixed this themselves in v0.7.0 (`afc30e35`, same week we did): FIN-aware reassembly as `_ws_recv` per-frame + `_ws_recv_message`, message cap, ping-between-fragments answered. The fork's v0.7.0 merge adopted their two-function shape and grafted in the two pieces theirs lacks, which are the RESIDUAL candidate: EOF-mid-frame returns a clean close instead of crashing the reader thread with a struct.error on a truncated length-extension/payload, and the big-int XOR unmask replaces a per-byte Python loop that blocks the reader thread for seconds on a multi-MB attachment frame. Both live in the fork's `_ws_recv`; tests/test_ws_fragmented_frames.py pins them (their tests/test_ws_fragmentation.py covers the rest). Small, clean offer. |
| Bundle staleness scan misses `ui/webview` *.js — a `gear.js`-only edit never rebuilds the chat bundle | PR #42 (`efcea880`); offer branch `bundle-js` (`87595102`, single commit off upstream `d0f7c423`) | **merged** — their PR #384, merged as-is 2026-08-14 (merge `1f292bdf`; our head `87595102` is an ancestor of their main, verified 2026-08-15) | Offer branch cleaned up post-merge; CAME HOME in the 2026-08-15 tip fold (`upfold0815`) — the keep-the-helper conflict resolved as predicted (fork keeps `_bundle_inputs`, widened with `src/*.js` for parity; their test's inline-form pin re-aimed at the helper). Upstream's inline scan still misses `ui/romp-timeline-view.js`, flagged unanswered in the PR body. The `romp-timeline-view.js` adjacent gap stays flagged in the PR body, unanswered so far. The third incarnation of the staleness seam, found live 2026-08-09: the scan globs `*.ts`/`*.css` under `ui/webview`, but `gear.js` is a plain-JS module `feed.ts` `require()`s into the chat bundle — a gear-only change ships dark through kernel restarts (`/version` reports the new field while the served bundle predates it). Upstream's bundle-watch fix (`276571f8`, in v0.7.0) has the identical hole. Re-verified and offered 2026-08-14: the fix was HAND-RE-DERIVED against upstream's inline scan (their `_ensure_bundles` never grew the fork's `_bundle_inputs` helper), widening `*.js` over both watch roots (safe: `vscode-extension/src` has no `.js`, dist sits outside both); the test was re-expressed in their `tests/test_bundle_build_mode.py`'s source-level style, deriving the watched-module set from `require()` scans with `gear.js` as a sentinel (the fork's `test_kernel_bundle_staleness.py` imports a fork-only helper and cannot port). The offer's PR body also flags — but does not fix — a SECOND unwatched module the re-verify found: `timeline-main.ts` requires `../romp-timeline-view.js`, which sits OUTSIDE both watch roots (a candidate follow-up if the maintainer wants it; the fork's own `_bundle_inputs` already watches it explicitly). Fork CI green via a since-closed scaffold PR (#63; push CI is main-only). |
| A safeguards refusal classifies as transient — the auto-retry re-sends the refused prompt forever (one refused prompt drew 12 injected retries in ~6 minutes) | `refusalretry` branch | ✅ **merged** — their PR #540, merged as-is 2026-08-22 (merge `c0c82f1f`); came home in the 2026-08-22 tip fold (`upfold0823`) | MERGED as-is 2026-08-22 (head is an ancestor of their main, verified in the fold). Branch retired. OFFERED 2026-08-20 (branch `refusal-retry`, two commits `f460dfe5`+`b8679d2b` off tip `f3ec1297`: kernel class + separately-droppable dashboard mirror): red-first 18 pytest + 16 node failures on the unfixed base, full suites green after; review clean, scaffold CI green (since-closed fork #94). Upstream ships the same `_api_error` / `_fire_api_retry` / `_auto_retry_tick`, so the same storm: a classifier refusal writes an assistant error record (`invalid_request`, "safeguards flagged…") that falls through as transient, and each refusal's NEW record uuid mints a new retry episode — the once-per-episode gate cannot terminate a deterministic error, and in fallback configurations each retry manufactures another model downgrade. Fix adds a fifth on-you class. Detection is event-based with the CLI's wording as a CO-EQUAL signature, both required by the evidence: the system `model_refusal_no_fallback`/`_fallback` record links to its episode by parentUuid (both it and the error carry the refused user message's uuid; `refusedUserMessageUuid` observed diverging from the episode's parent in 2 of 13 records, so it is NOT the link), and the CLI omits the record for some refusal errors (observed in the same storm that produced linked ones), so the text signature is not a legacy fallback. Never auto-retried (manual Retry keeps the override contract); the card floors to needs-you; every retry/countdown surface skips it and names the real fix (rewrite the prompt or drop the thread). Pure fix, no fork-specific content; fixtures synthetic. |
| Per-session env vars at spawn (`romp new --env NAME=VALUE`, repeatable, riding POST /new into the per-sid flag-settings payload) | fork branch `sessenv` | ✅ **merged** — their PR #889, 2026-09-03 (our head an ancestor + his `01b0baa7` fold: the settings file is 0600 and the parked chip names vars only) — ✅ came home in upfold0905 (2026-09-05) | MERGED with a privacy tightening folded on: per-session env VALUES stay private (0600 settings file; the parked chip names variables, not values). FOLD FLAG RESOLVED at upfold0905 (2026-09-05): the `os.chmod(p, 0o600)` after the settings write and the names-only parked `/env` chip (`/env (cleared)` for an empty set) adopted; `tests/test_session_env.py`'s chip pins follow. Branch retired. OFFERED 2026-09-02 (branch `sessenv`, `b3d1b23c` off tip `31d8731b`; the feature itself is already on fork main — this row’s "fork branch" pointer was stale): _apply_new_session_prefs edited minimally to stay clear of open #882’s docstring hunk; review caught one fork-provenance comment, reworded; scaffold CI green (since-closed fork #168). Upstream ships the same flag-settings layer and the same directory-scoped-only env: a `.claude/settings*.json` `env` block reaches every session in the repo and outlives the session, so two SDK sessions in one directory cannot run with different environments. Spawn-time slice only (no live-mutation op, no UI). Names validated at both doors (CLI usage error, /new 400), reg persistence beside model/effort, fork inheritance like model/auth, and reconnect re-assertion by construction (the settings file is rewritten from the reg at every connect). Pinned red-first: bats for the CLI parsing, python for /new validation, the flag-settings merge and its ""-when-empty contract, set_env, and fork inheritance. Pure feature, no fork-specific content. |
| Self-scheduled work can strand in an idle SDK session — in a stuck regime the CLI only ENQUEUES Monitor/cron/task notifications (queue-operation records, no delivery), and nothing drives the turn that would deliver them | fork branch `idledrain` | ✅ **merged** — their PR #541, merged as-is 2026-08-22 (merge `3d930ff3`); came home in the 2026-08-22 tip fold (`upfold0823`) | MERGED as-is 2026-08-22 (head is an ancestor of their main, verified in the fold). Branch retired. OFFERED 2026-08-20 (branch `queue-strand`, `97d7b7ba` off tip `f3ec1297`): drive gate widened to (working, retrying, compacting) to match upstream 8cd065d9; synthetic evidence only in the body; review clean, scaffold CI green (since-closed fork #95). Upstream ships the same SDK turn loop (turns start only from the input generator's fed messages) and no queue watcher. Live-transcript evidence, TWO regimes: in most sessions the CLI delivers this class itself straight from idle (602 of 1311 enqueues across 25 sessions, median 46ms, p90 126ms; the delivery writes NO dequeue record — the user record carrying the text is the evidence), but a session can fall into a stuck regime where nothing ever starts the turn: an overnight 15-min Monitor stacked 33 undelivered enqueues while the session ran zero turns for 8+ hours (that transcript: 123 enqueues, 0 delivered, 66 removes = discards). Background-agent completion notices are delivered in EVERY regime, the stuck one included, and are excluded by id class. Fix = a pusher-cycle tick (`_idle_queue_drive_tick`) reading the transcript's still-pending enqueues PER ENTRY (a content-addressed remove resolves its own entry; a user record carrying the text is the delivery evidence, one entry per carried occurrence, so a varianceless monitor's identical firings resolve one at a time; the same queue-operation records `_pending_queued` folds, no new polling), with a ~60s age floor PER ENTRY — only the aged subset drives, so the delivering regime is never raced and a sub-floor-cadence stream cannot starve candidacy (the CLI's decision NOT to deliver writes no record, so only its window passing proves the stuck regime — the documented exception to the no-time-windows rule), + `SdkBackend.drive_idle_queue`: per-watermark latch (in memory at acceptance; persisted to the reg only after the enqueue lands, so a kernel death in between re-drives instead of discarding), one worker per sid (overlapping drives let a stand-down's mark restore clobber the newer worker's watermark and double-deliver), gates re-checked at the send moment against a re-resolved session, dormant spawns drawing on boot-reconcile's own stagger budget (one shared semaphore), delivery via enqueue() of the queued texts verbatim (no synthetic prompt), problem-ring-loud failures, and the queued-bubble/_backend_queued genuine-filter so a parked wrapper never renders as the user's input. Red-first tests: both regimes, the floor, per-entry resolution, every gate at acceptance and delivery, boot parity. Pure bug fix, no fork-specific content. |
| The transcript-cache LRU races its cross-thread callers: hits MUTATE (pop + reinsert) and eviction is `pop(next(iter()))`, so with more distinct files than slots one thread's eviction pops the entry another just matched (KeyError from the unconditional pop; RuntimeError from `next(iter())` over a resizing dict) — and callers catch-and-degrade, so the raise surfaces as wrong behavior, not a crash | `kernel/event_model.py` `_read_jsonl_incremental`; fixed in the v0.12.0 fold (`upfold0819`) | ✅ **merged** — their PR #538, merged as-is 2026-08-22 (merge `6aea062a`); came home in the 2026-08-22 tip fold (`upfold0823`) | MERGED as-is 2026-08-22 (head is an ancestor of their main, verified in the fold). Branch retired; the fork had carried the fix since `upfold0819`, so the fold converges the two copies. OFFERED 2026-08-20 (branch `lru-race`, `51b18028` off tip `f3ec1297`): two-file hunks re-derived with upstream-true rationale (fork-only vocabulary dropped); hammer test red within ~1s on unfixed code; review clean, scaffold CI green (since-closed fork #90); the port survived a mid-run process restart via the workflow cache. Upstream ships the same mutate-on-hit LRU with the same cross-thread callers (their judge tiers + pusher share the cache), so the race exists there verbatim. Fix is diff-minimal against their shape: a module-level lock held for the cheap dict ops only (lookup+move, insert, evict — the parse stays outside it) plus guarded pops, so a lost race degrades to a re-parse, never a raise. Pinned red-first by a multi-thread hammer in `tests/test_kernel_jsonl_cache.py` (unfixed, it raises within ~1s). |
| AskUserQuestion "write your own answer" replies unreliably vanish from the chat's answered box — the kernel joins answers by regex-scraping the tool_result's flat output string while the SAME user record carries the structured `toolUseResult = {questions, answers}` map, and the event model drops top-level `toolUseResult` from atoms entirely, so the attach loop's `tur` is always None (which also leaves Edit's structuredPatch `diffRows` dead through the parse path); any double-quote inside a question's text breaks the scrape and the typed answer disappears (13 of 95 real answers dropped, 1 garbled, all quote-correlated) | fork branch `askanswer`: `kernel/event_model.py` carries dict `toolUseResult` on tool_result atoms; `kernel/kernel.py` `_ask_fill_answers` fills `chosen` by exact question-text key (string → one value, LIST → the multiSelect picks), regex kept only for old records without the dict; `multiSelect` copied onto askAnswer blocks | ✅ **merged** — their PR #576, merged as-is 2026-08-23 (merge `65ab2c7f`) | MERGED as-is (head is an ancestor of their main, verified same day). Branch retired. OFFERED 2026-08-23 (branch `ask-answers`, `830bba16` off tip `147678b1`, two commits per the coupled halves): structured answers join + toolUseResult carried onto atoms; only 10e3b55d’s test hunks travelled; review clean, scaffold CI green (since-closed fork #105). Upstream ships the whole chain verbatim (their atom builder, their `_ask_fill_chosen` + the always-None `tur`, and the missing `multiSelect` on blocks — their ", " split is dead code, so multiSelect picks render as ONE joined quoted "Other" row instead of highlighted options). Render side needs no change (chosen is already per-value matched). Pure bug fix, no fork-specific content; pinned red-first in tests/test_kernel_askanswer.py (10 new tests red on unfixed code, incl. the authoritative-skips-regex overwrite case and the old-record fallback). |
| A live manual `/compact` produces no "Context compacted" card in the chat — the CLI writes the boundary+summary as a DETACHED side branch (parentUuid null + logicalParentUuid:<pre-compact leaf>; the conversation chains through the /compact command wrappers instead), so the leaf→root walk never visits the pair and the compact atom is never emitted; auto-compactions, which chain THROUGH their boundary, keep theirs | fork branch `compactcard`: `kernel/event_model.py` `FileAdapter._adopt_detached_compactions` | ✅ **merged** — their PR #539, merged as-is 2026-08-22 (merge `9fd6dcf7`); came home in the 2026-08-22 tip fold (`upfold0823`) | MERGED as-is 2026-08-22 (head is an ancestor of their main, verified in the fold). Branch retired. OFFERED 2026-08-20 (branch `compact-card`, `fcc89f58` off tip `f3ec1297`): the no-PLACEMENTS_V-bump judgment call is flagged to the maintainer in the body with an offer to add one — it shipped as offered, no bump; review clean, scaffold CI green (since-closed fork #92). Upstream ships the same walk verbatim, so their manual compacts lose the card too. Fix is a self-contained event-model adoption repair: splice the detached boundary(+summary) back in AFTER its own /compact invocation episode's stdout record (episode found via the summary record's promptId — the CLI stamps it with the invoking /compact's — with file-order fallback), gated on the EPISODE being on the active path, never on trigger or the bare anchor — attached boundaries (auto, and the resume re-splices manual ones arrive as) no-op, a rewound-away /compact stays hidden with its history, and an adopted boundary never arms the post-compaction replay dedup (a live manual compact replays no tail; armed, it silently ate the user's next typed prompt whenever its text repeated an earlier message). Shape verified against the live corpus (10/13 manual boundaries detached — the other 3 are resume re-splices that arrive attached and no-op — and 13/13 manual summaries carry the episode promptId); pinned red-first (golden scenario + replay-dedup class + unit class + warm/cold incremental-cache equivalence + placements canary). Placements: unit sets identical pre/post on the golden scenario and 11/12 boundary-bearing corpus transcripts; the residue is one continuation-work unit re-attributed from the /compact command segment to the boundary turn when queued work continued straight through the compact (the attached-compact shape). |
| User todos slice 1 — the object and its loop: a sid-keyed stamp-not-delete kernel store nothing inference-driven may write (grep-pinned), the two postal tools (`add_user_todo`/`withdraw_user_todo`), build_session's `userTodos` seam + the split composer card with modal Reply / Dismiss, and the delivery-keyed answer stamp whose recall/loss REOPENS the ask (the id rides the queue entry; a boot pass re-offers stranded losses) | fork branch `usertodos` (commits `afb38983` / `ac8340da` / `d554c2f9` / `ca82e738`); plan `plans/user-todos.md`, ADR `docs/adr/0001` | candidate — **major-feature (tier 3)**, discuss with the maintainer first | (the user 2026-09-06: one of two example major features to offer in future) Pure feature, no fork-specific content. Upstream ships every system this builds on — the same kernel, the postal bus (`postal/postal_service.py` verified present on their main), the webview, the voice test — and none of the feature (`add_user_todo` greps empty there). Their judges/unblocker erase blocked-on-you asks the same way (`WHY_UNBLOCK_UNSETTLED` is their own note), so the ADR's authority-tier rationale — the agent's declaration outranks inference, and nothing that reasons by inference may clear the object — applies verbatim; the ADR itself is fork-new and travels with the offer. |
| User todos slice 2 — ambient visibility and the endgame: build_feed's sid-keyed open-count map + quiet card marker, the ⚑ tab glyph, the badge widened to "things only the user can move" (with per-item decision classes: quarantine and parked handoffs count per card), the idle-escalation floor + goal-less placeholder (peer-wait gate, confirming skip, working-top fallback, floor-push latch), and the status-nudge stand-down scoped so the awaiting wake and debt machinery flow past | fork branch `usertodos` (commits `5bee4179` / `13d74533` / `c0282461`) | candidate — **major-feature (tier 3)**, discuss with the maintainer first | (the user 2026-09-06: one of two example major features to offer in future) Same systems upstream: `_auto_nudge_tick`, `_needs_you_count`, `_wait_for_graph`, build_feed's perm-floor family, the feed/tab webview. Depends on slice 1; the floor/badge/nudge pieces are meaningless without the store. |
| User todos slice 3 — memory across context loss: a SessionStart hook (resume + compact sources) hands a resumed/compacted session its open todos as passive additionalContext, rendered kernel-side over a read-only `POST /usertodo/context` so the voice test scans exactly what a session receives | fork branch `usertodos` (commit `57b085bc`; `hooks/romp-usertodo-context.sh`) | candidate — **major-feature (tier 3)**, discuss with the maintainer first | (the user 2026-09-06: one of two example major features to offer in future) Upstream ships the same SessionStart hook family (`hooks/romp-postal-context.sh`, install.sh registration) and the same SDK/tmux split the hook straddles. Depends on slice 1. |
| The "Waiting on you" pane: every session's open user todos across attached machines in one dashboard column, oldest first, with Reply / Dismiss / open-session per row: build_feed ships `userTodoRows` + `userTodosOn` beside the count map (same read, same gates, byte-stable), federation prefixes + merges the rows, app=`waiting` rides the feed frame with the feed pane's caps, `ui/webview/waiting.ts` reuses `userTodoAnswer` / `userTodoDismiss` unchanged and never reads `asks` | fork branch `waitingpane` (stacked on the todoswitch / todofix PRs #183 / #184) | **waiting**: gated on the user-todos slices above (and the switch + log PRs) being offered first; the pane has no meaning upstream without them | BROWSER SHELL ONLY: the VS Code extension's panel mirror (the `KernelPipe` app union, `openPaneByKey` / `updateStrips`, a panel + HTML builder, `page-skeleton.ts`, `view-routing.ts`, the `package.json` command, `romp-menu.ts`) is a deliberate FOLLOW-UP, not in this branch (the esbuild entry ships, so the extension builds unchanged). Pure feature, no fork-specific content. |
| The dead-wait sweep (their #550) files its irreversible "session ended while still waiting" block off ONE raw liveness-listing miss — `_dead_wait_sweep`'s transition diff and `_wake_goal`'s dormant branch both convert a stamped Working card on bare absence from the merged map (a transient tmux list collapse or a swallowed SDK live-merge exception both empty it for a cycle), and nothing lifts the false block when the listing returns; on a tmux-less box the dormant branch takes every file-derived session, alive included | fold-review fix in `upfold0823`: `_dead_wait_corroborated` (SDK reg alive-bit / standing death record / owner scan; unconfirmable → stand down with the transition kept armed, retried next tick) threaded through both writers | ✅ **merged** — their PR #574, merged as-is 2026-08-23 (merge `36c41c95`) | MERGED as-is (head is an ancestor of their main, verified same day). Branch retired. OFFERED 2026-08-23 (branch `deadwait-probe`, `01d0ec06` off tip `147678b1`, one commit — the finished two-round mechanism): docstrings anchor to their own _death_sweep_tick doctrine; _end_on_idle_sweep flagged in the body as follow-up, not patched; red-first 11/12 new tests fail on base; review clean, scaffold CI green (since-closed fork #108). Their sweep has the same hole verbatim, and the fix is their own corroboration doctrine threaded through — their `_death_sweep_tick` already asks `TmuxBackend.alive_sids` and stands down on a failed probe before stamping; the dead-wait writers just never did. The tri-state helper here (`_dead_wait_corroborated`, modeled on the fork's `_confirmed_ended`) is small and would travel with an offer; note their `_end_on_idle_sweep` also spends requests on the same raw `tmux.get() is None` read (a second beneficiary if offered). Pinned red-first in tests/test_dead_wait_block.py. |
| Their edit trace (`_edit_trace_body`, #542's consent-gate commit) embeds the request-supplied file PATH with no marker hygiene — a marker-shaped filename becomes a live `romp-msg-id`/`romp-tag`/`romp-injected` match in the injected body | fold-review fix in `upfold0823`: the path runs through `_neutralize_romp_markers`, like every untrusted half of an injected body | ✅ **merged** — their PR #575, merged as-is 2026-08-23 (merge `7ab7941f`) | MERGED as-is (head is an ancestor of their main, verified 2026-08-24). Branch retired. OFFERED 2026-08-23 (branch `edit-trace-markers`, `dbd6a44a` off tip `147678b1`): carved to ONLY the neutralizer + _edit_trace_body application, tests re-homed off test_user_todos.py (user-todos stays unoffered; its future offer must REUSE the upstream helper); review clean, scaffold CI green (since-closed fork #106). NOTE for the next fold: helper placement diverges (fork keeps it in the todos neighborhood) — dedup deliberately. The neutralizer is fork-side (the user-todos answer-body work) and would travel with an offer. Pinned in tests/test_user_todos.py's MarkerNeutralizerVariants against the verbatim downstream regexes. |
| The file viewer opens images and PDFs as line-numbered mojibake — its fetch pipeline calls `r.text()` on ANY 200, so a clicked .png renders as garbage; fix branches on the kernel's own Content-Type verdict (image/* → the fetched bytes as ONE `<img>` at an object URL revoked on every teardown, SVG via `<img>` only; application/pdf → the lightbox's iframe treatment), hides the text-only affordances (Wrap, comments) over media, and pins the kernel's image-200 contract (image mime + no X-Romp-Text-Utf8) from both sides | fork branch `imageview` (ui/webview/file-view.ts + both sheets; pins in ui/webview/file-view.test.ts + tests/test_kernel_preview.py) | ✅ **merged** — their PR #733, merged as-is 2026-08-27 (merge `478b0c1c`) | MERGED as-is (head is an ancestor of their main, verified same night). Branch retired. OFFERED 2026-08-27 (branch `view-binary`, `2407d8c5` off tip `14a4bd70`): re-derived from origin/main’s post-fold viewer; review’s one mustFix was real — upstream’s 2026-08-26 css-vocab token sweep retires the fold-era raw box-shadow literal, fixed to var(--shadow-modal) in both sheets; red-first 10 media tests fail on base; scaffold CI green (since-closed fork #119). Upstream ships the identical `r.text()` pipeline in the same shared file, so their viewer has the same mojibake on every image click. Self-contained: the kernel needs nothing — it already serves images correctly (locally-derived mime, nosniff, the 50 MB/413 cap, 206 ranges, an image-faithful relay). |
| build_feed's per-session `live` (the backend-alive bit every card and placeholder wears) is clobbered inside the goal loop by the origin badge's local reuse of the same name — after one delegation-origin badge, every later card of that session (and the goal-less user-todo/blocked/awaiting placeholders after the loop) wears the badge's bool: a live session's cards dress .dead and route to revive; a dead session's offer Continue | fold-review fix in `upfold0825`: the badge-local renamed `origin_live` in `kernel/kernel.py` build_feed; pinned red-first in `tests/test_feed_live_clobber.py` (both directions + the placeholder) | ✅ **merged** — their PR #734, merged as-is 2026-08-27 (merge `5e76112e`) | MERGED as-is (head is an ancestor of their main, verified same night). Branch retired. OFFERED 2026-08-27 (branch `feed-livebit`, `3eb2091b` off tip `14a4bd70`): the two-line rename + placeholder-ordering test; review clean, scaffold CI green (since-closed fork #117). Pre-existing in BOTH parents (verified against the merge), made routine by the tags era — origin badges persist for the card's life (their 2026-08-16 rule), so one absorbed badge poisons the session's whole card tail. Two-line fix, no fork-specific content; the test's placeholder leg needs the fork's user-todo store, so an offer would carry the two card-direction tests and re-home the placeholder pin. |
| The timeline's `_viewsKey` drifted from its webview twin: upstream's 2026-08-25 lens fix taught `viewsKey` (session-views.ts) to compare per-surface `actives` and drop the retired `hidden`, but the timeline's hand-copy (romp-timeline-view.js — it can't import TS) was left behind, so a lens-only edit's optimistic pending copy is cleared by the first stale views-bearing frame and the tag filter visibly flaps (revert-then-jump-back) | fold-review fix in `upfold0825`: `_viewsKey` brought to the twin's exact shape; a source-parity pin in `ui/timeline-views-panel.test.ts` compares the two serializations so the copies cannot drift again | ✅ **merged** — their PR #735, merged as-is 2026-08-27 (merge `dd844a6d`) | MERGED as-is (head is an ancestor of their main, verified same night). Branch retired. OFFERED 2026-08-27 (branch `viewskey-drift`, `b2e7d652` off tip `14a4bd70`): mirrors the current twin + a self-checking parity pin in their house style; review clean, scaffold CI green (since-closed fork #116). Upstream ships the same hand-copy in the same file and fixed only the twin, so their timeline flaps identically. Pure bug fix + a drift pin in their own house style (regex/source pins over the .js); no fork-specific content. |
| `_patch_rows` raises TypeError on a structuredPatch hunk whose lines array carries a non-string (`ln[:1]` on int/None), and the chat-build consumer has no guard — the escape reaches the pusher's outer try and aborts EVERY push cycle permanently, since the bad record is permanent; every other odd shape already degrades to no rows | fold-review fix in `upfold0825`: a non-string line entry degrades its hunk inside `_patch_rows`' existing bad-shape idiom; pinned red-first in `tests/test_kernel_patch_rows.py` | ✅ **merged** — their PR #736, merged as-is 2026-08-27 (merge `ae718e50`) | MERGED as-is (head is an ancestor of their main, verified same night). Branch retired. OFFERED 2026-08-27 (branch `patchrows-guard`, `dd9d4383` off tip `14a4bd70`): all three raise sites guarded, transplanted clean of the mixed fork commit’s other concerns; review clean, scaffold CI green (since-closed fork #115). Upstream ships `_patch_rows` and the guardless consumer verbatim (the structuredPatch chain their #576 wired live). Three-line fix, no fork-specific content. |
| Three queue-delivery fixes around the lost-send redeliver arm and the rename ping (2026-08-26 review of merge `c88d8049`): a send recovered while its session is LIVE (a fresh spawn) now enters the session's own `_pending` instead of a reg write the next queue mirror overwrites — and (round 2, same day) the RESUMABLE-reconnect caller is flag-only again per `_reconcile_stranded`'s documented policy (`refeed=False`: a missed `_text_landed` tail scan must surface the loss, never land the message twice; the re-feed lives only on the non-resumable boot/dead paths); the ping's empty-queue gate checks and enqueues under one lock hold (`enqueue_if_empty`); `queue_recallable` treats the ping feed-hold as a romp-side hold, so the queued bubble keeps its ✕ while a recall would still win | fold-review fix in `upfold0826`: `kernel/sdk_backend.py` (`_mark_dropped_echoes` redeliver arm + `refeed` flag, `_reconcile_stranded` resumable branch, `SdkSession.enqueue_if_empty` + `_deliver_rename_ping`, `queue_recallable`); pinned red-first in `tests/test_sdk_echo_durability.py` (RedeliveryFeedsTheAuthoritativeQueue; ReconnectStrandIsFlagOnly), `tests/test_sdk_rename_ping.py` (the racing-send interleave), `tests/test_sdk_backend.py` (PendingQueue) | ✅ **merged** — their PR #887, merged as-is 2026-09-03 — ✅ came home in upfold0905 (2026-09-05) | MERGED as-is (head is an ancestor). Branch retired. OFFERED 2026-09-02 (branch `queue-delivery`, `6d52536d` off tip `31d8731b`, three commits — one per fix, each red-first): no function-level overlap with the open sdk_backend offers (#874/#875/#878); one pre-existing xdist-only failure in test_postal_relay_honesty noted as unrelated (reproduced on pristine base); review clean, scaffold CI green (since-closed fork #165). Upstream ships all three (verified by `git grep` against `upstream/main`): the reg-only redeliver write with the same two live-session callers (their ~4416 / 1752 / 2018), the split-lock ping gate, and the recall gate without the `_ping_feeding` arm. Effects there: a recovered send sits in limbo until a future kernel boot; a send racing the gate queues ahead of the ping and folds into its pre-turn record (the fold their 2026-08-25 settle-delivery redesign exists to prevent); a winnable recall is withheld. A FOURTH fix in the same change is fork-only and must not ride the offer: the redeliver rewrite's strings-only filter erased a persisted user-todo answer's `{"text","todo"}` queue entry, but upstream's queue is strings-only by design (no user todos), so its filter is correct there. An offer branch would re-derive the three fixes without the `_TodoText`/`_queue_wire` idiom. |
| T101 ask-unit fold, three inherited holes (2026-08-26 review of merge `c88d8049`; all three byte-identical in `upstream/main` at T105): (1) a fully-fanned dictated ask has NO card anywhere: the kernel's `_pure_delegation_top` suppresses any top whose every leaf is a handoff dict, the shape T101's own link path builds (their rule: one ask fanned to two workers = ONE card with two handoff children); (2) T101's fallback recipient mint is dead code: `mint_recipient = rooted and not link_id` is unsatisfiable because the trace returns None without a link, so `apply_courier`'s courier call site and T105's `userAsk` enrichment riding it never execute (their test passes only via a stubbed trace); (3) the umbrella-dissolution sweep eats an UndoClear-restored pre-T101 container on the next rollup, so the card the user just restored vanishes into promoted children. ROUND 2 (verified 2026-08-26) tightened all the round-1 mechanisms: the ask-unit exemption's discriminator is REAL dictation evidence, never bare promptUuid truthiness — promptUuid is the g200 landable-anchor field stamped on essentially every minted top (`_seg_anchor` anchors autonomous/peer segments; `apply_courier` stamps the delegate mail's anchor), so keying on it re-showed every courier/planner coordination top; `carded` is card VISIBILITY (`_node_carded`: live AND uncleared/unsealed AND reachable from a live top — a cleared-live or dangling ask mints exactly like an archived one, no compaction-boundary flip); and the fallback mint dedupes by ask identity (`askRef`) per recipient store, with an intermediate userAsk-bearing top standing hop re-mints down | fold-review fix in `upfold0826` + round 2: `kernel/kernel.py` (`_pure_delegation_top` exempts only the chain-proven `userAsk` root, or a promptUuid that PROVABLY resolves to a human-dictated record via `_dictated_prompt_uuid` + the shared `jd._human_prompt_record` rule — courier-planted `origin` tops never exempt on their mail anchor, trackers never at all; unresolvable fails open to the card), `kernel/judge.py` (`_node_carded` visibility; the trace's `carded`/`askRef`/hop stand-down; `apply_courier`'s ask-identity dedupe + `askRef` stamp; the dissolution spares a container whose latest clear-family diary row is the user's reopen/undo, and flag-cleared ones for the compactor); red-first in `tests/test_kernel_runspans.py` (PureDelegationTop incl. the courier/segment-anchor/mail-record shapes + fail-open pin), `tests/test_ask_unit_cards.py` (CourierWorld fanned-ask through completion; UmbrellaDissolution undo-restore + re-clear), `tests/test_chain_rooted_minting.py` (TraceRule `carded` incl. cleared/sealed/dangling + the boundary pin; CourierMintMatrix satisfiable fallback, dangling + cleared-live end-to-end mints; FanOutDedupe same-recipient + hop stand-down) | ✅ **merged** — their PR #891, merged as-is 2026-09-03 — ✅ came home in upfold0905 (2026-09-05) | MERGED as-is (head is an ancestor). Branch retired. OFFERED 2026-09-02 (branch `ask-unit-holes`, `219eef63` off tip `31d8731b`, three commits — one per hole): the sdk_backend hunks that shared the fork commits stayed with the queue-delivery offer; review clean, scaffold CI green (since-closed fork #170). The fallback decision follows THEIR design (the T101 message, its fallback test, and T105's `userAsk` investment all treat the mint as live): it fires on proof without a VISIBLE ask card (archive-only chain evidence, or a live card that renders nowhere); a linkless dispatch has no chain to trace and stays quiet per their 2026-08-25 verdict. Their `test_no_link_quiets` pin holds unchanged, and their stub-True suites pass as-is (a non-dict truthy record falls back to the link, the shape `apply_courier`'s T105 docstring already tolerates; a stub-True trace carries no `askRef`, so the dedupe never fires there). |
| Upstream's own test suite restarts a LIVE deployment: their `tests/conftest.py` has no `ROMP_MANAGER_PORT` floor, so `ServeSecurity.test_restart_endpoint_acks_post` (pop/restore around a real `POST /restart`) races the ack against the restored live port and wins — three real kernel restarts fired on this box 2026-08-26/27 from full suite runs in upstream-tree worktrees | fork `tests/conftest.py:25-40` (the two-part floor from fork PR #87: import-time poison + autouse re-assert, because module-level pops during collection erase import-time floors) | ✅ **merged** — their PR #737 (commit 2 of the pair), merged as-is 2026-08-27 (merge `b264635b`) | MERGED as-is (head is an ancestor of their main, verified same night). Branch retired. OFFERED 2026-08-27 as the suite-side half of the restart-race pair (branch `restart-race`, `a133665b`): the two-part floor + the test_restart_audit pop→write conversion + the lab.sh rider. Full suite 5756 green with the floor; scaffold CI green (since-closed fork #123). Found by the general session's 2026-08-27 restart forensics after this session's offer-worktree pytest runs kept restarting the live kernel. The offer is the fork's guard shape re-derived for their conftest. Same-class latent: `tools/romp-lab/lab.sh` never poisons the port (worth a sentence in the PR body). |
| `/restart` acks before resolving the manager port — the deterministic kernel-side fix: resolve the port BEFORE sending the ack, closing the race for every caller, guarded conftest or not | not yet built (design from the 2026-08-27 forensics); pairs with the conftest-floor row | ✅ **merged** — their PR #737 (commit 1 of the pair), merged as-is 2026-08-27 (merge `b264635b`) | MERGED as-is (head is an ancestor of their main, verified same night). Branch retired. OFFERED 2026-08-27 (branch `restart-race`, `a133665b` off tip `14a4bd70`): handlers resolve pre-ack and thread the value down (/restart local+remote branches, /update daemon); non-HTTP callers keep env-at-use via a sentinel default. Deterministic ordering test (restore rides the ack itself — no sleeps) red on base; three source-pin test files updated in-commit with the justification in the body; ServeSecurity needed NO change and is now deterministic. Review clean, scaffold CI green. The conftest floor protects test runs; this closes the underlying race in the kernel itself. Small, and the incident narrative (a test suite restarting a production deployment through an env-restore race) makes the case for both halves. |
| `save_goals`' pid-keyed tmp name is thread-unsafe — two threads saving one sid collide (`FileNotFoundError` at their `judge.py` save path; observed once live on a post-merge boot) | not yet built; thread-key the tmp at all three call sites | ✅ **merged** — their PR #738, merged as-is 2026-08-27 (merge `ae09d878`) | MERGED as-is (head is an ancestor of their main, verified same night). Branch retired. OFFERED 2026-08-27 (branch `savegoals-tmp`, `3948f561` off tip `14a4bd70`): thread-keyed tmp at all mint sites + the two-thread hammer (red within ~1s on base). Review clean, scaffold CI green (since-closed fork #122). Co-found in the 2026-08-27 forensics, unrelated to the restart race. Synthetic reproduction needed (two-thread hammer on one sid). |
| The tmux paste sequence reports success on a dead server: `_Tmux._run`/`_fire` swallow every subprocess error and ignore nonzero exits, so when the tmux server or session dies AFTER the clear-guard passes, set-buffer / paste-buffer / the submitting Enter all silently no-op and the message vanishes with no trace — their #741 clear-guard refusal covers only the pre-clear death (empty capture → no box region), and the never-delivered settle catches the loss only when a LATER send happens to overtake the echo | fold-hardening fix in `upfold0827b` (`b8aef6ba`): checked variants (`set_buffer_checked`/`paste_buffer_checked`/`send_keys_checked`) read the returncode, used only inside `paste()`; any failure aborts the paste with a loud stderr line naming the failed step (returncode contract verified empirically on tmux 3.4: dead server / missing session / missing buffer all exit 1, success 0; set-buffer does not auto-start a server); pinned in `tests/test_user_todos.py` (dead-server-at-set-buffer / at-paste-buffer / failed-Enter tests) | ✅ **merged** — their PR #876, merged as-is 2026-09-02 — ✅ came home in upfold0905 (2026-09-05) | MERGED as-is (head is an ancestor). Branch retired. OFFERED 2026-09-02 (branch `tmux-paste-checked`, `30825998` off tip `70a36077`): only the three checked TmuxBackend methods + the paste/Enter checks + the pane-clear fake adaptation; fork refusal hooks/marks/nonces excluded; red-first 8/9 new tests fail on base; full suite 6502 green (xdist); review clean, scaffold CI green (since-closed fork #151). Upstream ships the same swallowing primitives and the same unchecked paste sequence verbatim (their #741). An offer carries ONLY the checked variants + paste() wiring + the loud abort — the fork's refusal hook, pending-paste marks, nonces and user-todo stand-down machinery are fork-only and must not ride. The offer's failure behavior matches their own clear-guard refusal idiom (stderr, no paste); tests would re-home off test_user_todos.py onto a plain paste-path suite. |
| The federated dashboard's outbound sender loses messages across socket churn: both remote send sites in `outbound()` drop any message routed to a host whose socket isn't OPEN — a warn toast, nothing in the client-diag journal, no second chance. During a kernel restart every socket churns for seconds, so a user's explicit settings gesture vanishes: an Edit-consent yes OK'd mid-churn lost its `setFileEditing` broadcast, and the save was later refused by a kernel that never heard it, with refusal copy pointing back at the popup already answered. Every KERNEL_SETTING broadcast (setAutoNudge, setJudgeModel, setFileEditing, …) loses the same way — a reconnect leaves the mesh silently disagreeing until the next manual gear touch. And the queue's dual: a delayed replay is as dangerous as a drop, because the kernel applies any arriving setting unconditionally and the judge tiers RE-PROPAGATE what they apply — a dashboard tab frozen for hours (iOS always, Chrome tab-freeze) re-dials and flushes a pick the user superseded from another device, and the whole mesh silently walks back to the hours-old value with the gear's disagreement marks showing nothing (every kernel now agrees); same race with two dashboards flushing one setting on a host's recovery, where socket order — not gesture order — used to decide | fix in `settingsdrop` (two halves that only make sense together): (1) `ui/webview/federation.ts` — one queue-aware send path (`sendRemote`) for both sites; KERNEL_SETTING messages queue per connection while the socket is down, LATEST per type only, flushed on the socket's open event before any post-reconnect traffic — extending file-view's flag-lands-first ordering across a down-socket window; non-setting drops keep the toast and gain a `senddrop` client-diag breadcrumb (deliberate non-goal: replaying arbitrary actions after a long disconnect); (2) gesture-time ordering at the authoritative store: every KERNEL_SETTING emitter (all nine of gear.js's change handlers — a fixer round stamped the three that first shipped without it (setJudgeEffort, setIndexEffort, setUpdateMode), whose unstamped flushes took the compat path and got forged-fresh arrival stamps the judge tiers then fanned mesh-wide — plus file-view.ts's two consent posts and feed.ts's judge-limit switch) stamps `gt` = epoch ms at the CLICK, delivered unchanged by the queue; the kernel persists the last-APPLIED stamp beside each value (`auto-nudge.json`/`file-editing.json`/`update-mode.json` gain a `gt` field, the bare judge-tier stores a `<name>.gt` sidecar, absent field reads 0) and an arriving older-or-equal stamp STANDS DOWN — no apply, no propagation, one loud stderr line, plus a `settingStale` frame answered on the DELIVERING socket (the saveFile-ack `_reply` idiom) that the gear toasts in plain words and re-fills on if open, so the losing dashboard is never left displaying its refused pick as applied; `_propagate_judge_settings` bodies and the `/judge-settings` forward carry the origin's `gt` so a stale value can't win via a second hop; unstamped messages (older dashboards, direct HTTP) apply as before with gt = arrival time; equal stamps keep stored (NTP-scale skew immaterial at human gesture timescales). Store hardening from the same round: every gt-gated read-check-write span holds one settings lock (`_SETTINGS_LOCK`; auto-nudge keeps its ledger's `_NUDGE_LOCK`) so racing flushes land in gesture order, not socket order; the judge value+sidecar pair publishes via `_atomic_write`, SIDECAR FIRST, so a crash between the two errs toward one extra stand-down instead of a stale inversion; and the setters' OSError paths are loud on stderr (naming any half-applied state) with the no-propagation-on-refusal behavior kept and documented. Pinned red-first: `ui/webview/federation-send-queue.test.ts` (queue, latest-wins, per-host isolation, flush ordering, drop diagnostics, gt survives the flush un-restamped), the gear's COMPLETENESS pin (gear.test.ts reads federation's actual KERNEL_SETTING set and requires a stamped emitter per member — a tenth member without one goes red), file-view/feed-limit-banner emitter pins, `ui/webview/setting-stale.test.ts` (the frame/toast/refill wiring), and `tests/test_setting_gesture_order.py` (the incident replay through the real WS handler, reverse-arrival race, no-gt compat, propagation carries the stamp end to end, pre-gt file formats read as 0, update-mode gating, the deterministic two-flush lock race, sidecar-crash safety, OSError loudness, the settingStale reply) | ✅ **merged** — their PR #879, 2026-09-03 (our head an ancestor + his merge-of-main and fold `d7729f9b`) — ✅ came home in upfold0905 (2026-09-05) | MERGED with a maintainer fold: a judge pick reaches every remote kernel TWICE with one gt (dashboard broadcast + the origin kernel’s /judge-settings fan-out), so the second copy was classed stale and toasted "changed somewhere else" on every ordinary pick in a mesh — the five gt-gated setters now consult `_gesture_echo` first (equal stamp + stored value = apply nothing, say nothing; equal stamp + different value stays loud). His merge-of-main also reconciled with #882 (judge-tier setters validate against `_judge_model_values()` AND carry the stamp). ⚠ FOLD FLAG: take his `_gesture_echo` + the #882 reconciliation at the fold. His five "your call" follow-ups are queued as candidate rows below. Branch retired. REWORKED 2026-09-02: upstream’s #883 (watchdog abandons a silent socket and redials at once) landed on the same surface mid-offer; analysis showed it shortens but does not close the drop window (every redial passes a CONNECTING window where a send drops, and the watchdog creates more of them) and covers none of this PR’s 26 tests — the queue now composes with their redial (flush on the fresh socket’s open), two red-first composition tests added, kernel half untouched; three-commit structure kept; scaffold CI green (since-closed fork #162). OFFERED 2026-09-02 (branch `fed-outbound-queue`, `8eef361c` off tip `70a36077`, three commits): sender half verbatim, kernel half re-derived at tip (verify verdict was PARTIAL); this PR is the DEPENDENCY for the thinking-display and compact-latch offers (they build on its gt/_setting_stale/STALE_LABELS plumbing) — wave 2 waits for its merge. Review clean, scaffold CI green (since-closed fork #156). Upstream ships the identical sender AND the identical apply side (verified 2026-08-29 at their tip `9b05c51d`: the same two drop-if-not-open sites at their federation.ts :758/:771, the same KERNEL_SETTING set; kernel.py applies every arriving setting unconditionally in `_dispatch_ws` and re-propagates judge tiers via `_propagate_judge_settings`, so a queue added there without gesture ordering would hand them the stale-flush mesh-revert too — the offer must carry both halves together). An offer would carry the queue + open-event flush + `senddrop` breadcrumb, the `gt` stamps at every emitter, the kernel stand-down + stamped propagation, the store hardening (settings lock, sidecar-first atomic publish, loud OSError paths) and the `settingStale` reply + gear toast/refill, all three test files (federation-send-queue, setting-stale, test_setting_gesture_order), and the source-pin updates (flicker-diag's hostconn shapes, warn-toast's senddrop pin, judge-settings-sync's propagation-arg pins, gear-judge-versions' distill setter pin, gear.test.ts's completeness pin); no fork-specific content. |
| SDK sessions never request a thinking display, so every thinking block comes back signature-only: CLI 2.1.257 forces display "omitted" for non-interactive sessions unless `--thinking-display` is passed explicitly (its settings.json `showThinkingSummaries` is consulted in interactive mode only) — and the feed marks every SIGNED block opaque (`"encrypted": bool(signature)` → "Thinking…"), so even a block that did carry a summary would hide behind the placeholder | branch `guidefixes`: `kernel/sdk_backend.py` `_options` passes the SDK's TYPED `thinking={"type": "adaptive", "display": "summarized"}` (claude-agent-sdk 0.2.132 `types.py` ThinkingConfigAdaptive; the transport emits `--thinking adaptive --thinking-display summarized`) behind a per-install, default-OFF, gt-gated kernel setting (`STATE/thinking-summaries.json`; gear checkbox → `setThinkingSummaries`; deliberately NOT a KERNEL_SETTING, so it never propagates); `kernel/kernel.py` `build_session` and `ui/webview/render.ts` mark a block opaque only when signed AND textless. Pinned red-first in `tests/test_setting_gesture_order.py` (ThinkingSummariesSetting), `tests/test_sdk_backend.py` (OptionsAssembly), `tests/test_thinking_summary_events.py`, `ui/webview/thinking-summaries.test.ts` | ✅ **merged** — their PR #896, merged as-is 2026-09-03 (merge `77132226`) — ✅ came home in upfold0905 (2026-09-05) | MERGED as-is "on the owner’s standing word", reviewed inline after: connect-time read mirrors effort, the setter carries the echo check, per-install kept out of the federation set and the /version settings sub-dict, `_options` builds env before the thinking block reads it, the render re-check keeps an older kernel’s flag honest; he called the gear copy (summaries turn adaptive thinking on where a cap had it off) "the right kind of honesty". Branch retired. OFFERED 2026-09-03 (branch `thinking-display`, `239089ab` off tip `7faf24bc`, THREE commits: the SDK typed thinking field on connect; the opaque-only-when-signed-and-textless render rule; the per-install "Thinking summaries" gear setting with gesture ordering + the maintainer’s `_gesture_echo` check composed in). Waited for #879 so it could ride the merged gt plumbing as-is; kept OUT of federation’s KERNEL_SETTING by design (per-install, non-propagating). Body states CLI 2.1.257/2.1.258 + claude-agent-sdk 0.2.132 verification and a counts-only transcript census. Review clean, full suite 6876 green, node 2735, tsc clean; scaffold CI green (since-closed fork #179). Upstream ships the identical omission (verified 2026-09-01 at upstream/main `5c1e0cac`: `kernel/sdk_backend.py:4845` passes `effort=` with no `thinking`; `kernel/kernel.py:19600` `"encrypted": bool(b.get("signature"))`; `ui/webview/render.ts:2569` `ev.encrypted ? "Thinking…"`). Judges stay blind to thinking text (their readers key on type == "text"; pinned). Running sessions are NOT switched live — the python SDK wraps no runtime display control (the CLI's `set_max_thinking_tokens` control request does take a `thinking_display`, unwrapped in 0.2.132); a session picks the setting up at its next reconnect — an effort, billing, per-session-env or bypass-mode switch, the first fast-mode opt-in, a file rewind, or a kernel restart; a MODEL switch applies live over the SDK control channel and does NOT reconnect (round 2 corrected the copy, which had named it as a trigger) — and the gear copy names the real ones. Also inherent: the CLI resolves `--thinking adaptive` ahead of `MAX_THINKING_TOKENS` in its environment and of `alwaysThinkingEnabled: false` in settings (verified in the 2.1.257 binary), so on an install with thinking OFF the toggle turns adaptive thinking on too (the SDK has no display-only field); the gear copy says so and `_options` logs one line when a cap is in the CLI's environment (`thinking_override_note`; the decision helpers `thinking_kw` / `thinking_override_note` are unit-tested WITHOUT the SDK, since OptionsAssembly skips on a runner that lacks it). Upstream may prefer it default-ON; the fork keeps OFF at the maintainer's request. |
| The index judge tier (captioner + archiver) sends `thinking: disabled` (`MAX_THINKING_TOKENS=0`) for EVERY model, though the lever was tuned on Haiku — on Fable, CLI 2.1.257 DROPS the thinking parameter for a model carrying its `rejects_disabled_thinking` capability (claude-fable-5 / 5-1; the API refuses thinking:disabled there), so the var was a silent no-op and every "thinking-off" index call ran FULL-COST adaptive thinking; the gear's Indexing effort sub-copy described the same behavior | branch `guidefixes`: `kernel/judge.py` `_judge_env(tier, auth, model)` + `_judge_run` resolve the tier's model first and key the lever on the CLI's GENERATIONAL adaptive-thinking rule (`_EFFORT_FLOOR` / `_ALIAS_HEAD` / `_adaptive_thinking`: family + major.minor parsed from the id or alias; a bare alias is the version the catalog resolves it to): Fable and Mythos (every version), Opus ≥ 4.6 and Sonnet ≥ 4.6 run adaptive thinking and take `--effort <index-effort setting, default low>` (`INDEX_EFFORT_DEFAULT`), never the env var; Haiku 4.5, Sonnet 4.5, Opus 4.5 and older — the CLI's hardcoded denylist — keep `MAX_THINKING_TOKENS=0` and the no-flag default; an id the rule cannot place gets what the CLI gives an unlisted first-party model (adaptive thinking, `--effort` forwarded, thinking:disabled dropped), so the effort lever, plus one loud stderr line. One stderr line per model names the lever that applied. (Round 1 keyed on family alone — "not Haiku" — so an index tier pinned to Sonnet 4.5, a version the gear's submenu offers, got an ignored `--effort low` and full extended thinking: 3.5× the cost and 2.3× the latency of the env var, measured; round 2 fixed it. Round 3, 2026-09-02, re-derived the rule from the binary's decision procedure rather than its catalog arrays: Mythos placed (claude-mythos-5-1 carries `effort` + `adaptive_thinking` + `rejects_disabled_thinking`, and the CLI hardcodes mythos-5 the same way); a stranger's lever flipped from the env var to effort, because the CLI's first-party default treats an unlisted model as adaptive AND drops thinking:disabled for it — round 2's "cost-safe" env var was the one lever guaranteed not to work there; a bare alias is placed by the catalog's alias head (`haiku` → 4.5, below the line) rather than a blanket yes; and Opus 4.5 corrected — the CLI forwards `--effort` for it (only Haiku and Sonnet 4.5 have the flag deleted), it just has no adaptive thinking, so the env var stays its lever.) The existing `setIndexEffort` gear pick is the configurability (nothing new to propagate); its sub-copy in `ui/webview/gear.js` rewritten (both rows). Pinned red-first in `tests/test_judge.py` (JudgeEnv, EffortCapability, IndexTierLever) and `tests/test_judge_auth_billing.py` | ✅ **merged** — their PR #880, 2026-09-03 (with his `6499afd9` fold) — ✅ came home in upfold0905 (2026-09-05) | MERGED. MAINTAINER REVERSED the lever’s core choice before merging: withholding MAX_THINKING_TOKENS=0 from adaptive-thinking models traded a measured thinking-off path for unmeasured adaptive-low on Sonnet/Opus 4.6+ (which honor the variable) — the variable now rides the index tier UNCONDITIONALLY (a no-op where the CLI drops it; --effort lands beside it). FOLD FLAG RESOLVED at upfold0905 (2026-09-05): his unconditional variable adopted and the fork's withhold shape is gone; `JudgeEnv`, `IndexTierLever` and `JudgeEnvBilling` assert `"0"` on every model. Upstream's own prose in `_judge_env`'s docstring and two stderr comments still describes the withhold — left as his; a doc nit for a later offer. His two no-gate notes are queued as candidate rows below. OFFERED 2026-09-02 (branch `judge-thinking-lever`, `f13ff7b9` off tip `70a36077`): review added privacy-grade test hygiene (assertNotIn over an os.environ copy would print a developer’s credentials on failure → assertIsNone on the single key); scaffold CI green (since-closed fork #154). Upstream ships the same unconditional env var (verified 2026-09-01 at `5c1e0cac`: `kernel/judge.py:1032`) and the same gear copy. The two former pins asserting `"0"` for the bare index tier were rewritten to the model-keyed contract. Round 1's "Sonnet/Opus degrade under thinking-off" had no source in the guide audit and is withdrawn; the capability boundary was read from the 2.1.257 binary, not from the guide — round 2 from its baked-in catalog arrays, round 3 from its adaptive-thinking predicate (a hardcoded denylist of claude-3-*, Opus 4.0/4.1/4.5, Sonnet 4.0/4.5 and Haiku 4.5, then the catalog, then a provider default that is YES on first-party). First-party only: on Bedrock/Vertex the CLI's default flips to NO and a bare `sonnet` resolves to Sonnet 4.5; the fork's judges run on a login token or an Anthropic API key, so neither is modeled, and an offer should say so. |
| The postal SessionStart hook and the romp-postal skill still teach a `DELEGATE:/COORDINATE:/QUESTION:` body prefix, contradicting `send_message`'s REQUIRED `kind` parameter (delegate\|coordinate\|question) that the MCP server's own instructions already mandate — two instructions for one fact, one of them retired | branch `guidefixes`: `hooks/romp-postal-context.sh` (the norm bullet now says set `kind`), `claude/skills/romp-postal/SKILL.md` (tool signature `send_message(to, body, kind)`, shell form `romp mail send --kind …`, the norms bullet); `tests/romp-postal-context.bats` asserts the `kind` wording and that the prefix is gone | **candidate** | Upstream ships the identical texts (verified 2026-09-01 at `5c1e0cac`: `hooks/romp-postal-context.sh:13`, `claude/skills/romp-postal/SKILL.md:32`). Text-only, no behavior change. The shell CLI itself treats `--kind` as optional (`postal/postal_service.py` usage brackets it); the skill teaches the stricter norm on purpose, matching the kernel's own reply hint. |
| The auth-exempt `/busy?drain=1` route has a WRITE side effect: upstream's T121 deploy-drain made the parked-poll refresh call `be.refresh_drain_hold()` unconditionally from the PRE-`_authorize` exempt block, arming/extending a 12s lease (`DRAIN_HOLD_TTL=12.0`) that holds NEW turn starts for EVERY session, refreshable forever. Any drive-by loopback page's no-cors GET (no preflight, no token) or any tailnet client (when the dashboard is tailscale-served) can loop `http://127.0.0.1:<port>/busy?drain=1` every ~10s and freeze all turn starts indefinitely; the only tell is a stderr line — the exact drive-by-loopback adversary `_authorize`'s own docstring names. The fork parent served `/busy` as a pure side-effect-free read. | fold branch `upfold0830`: `kernel/kernel.py` — new `Handler._write_token_ok` (explicit `?token`/`X-Romp-Token`, NOT the ambient cookie) gates the `drain=1` arm, while the `/busy` READ stays auth-exempt; `bin/romp-manager` — the parked poll (`fetchBusy`/`kernelToken`) now reads the 0600 serve-token and sends `X-Romp-Token` so the legit deploy-drain caller keeps arming; pinned red-first in `tests/test_kernel_auth_hardening.py::BusyDrainWriteGate`. Round 3 (same branch) made the manager half fail LOUDLY instead of differently: a token-read failure and a kernel `draining:false` answer to a drain=1 poll each log one clear line per park (a refused drain used to leave the manager believing the hold was armed until the 15-min backstop cut turns mid-flight with no cause on record), the park records the refusal so the backstop apply line names the never-armed hold, and `fetchBusy`'s callback carries the kernel's `draining` answer; behavior unchanged (count-only polling keeps working); pinned in `tests/manager-drain.test.js`. Round 4 (same branch) made that recorded cause ACCURATE: the park COUNTS refusals and arms instead of latching one bit (kernelToken reads fresh per poll, so a refusal can be transient — a token restored mid-park arms every later poll, and the old line still swore the hold never armed), evidence is stamped only on the park that issued the poll (an in-flight probe from a just-cleared park cannot mark its successor), the backstop line distinguishes never-armed from refused-N-times-before-arming, and an armed answer re-arms the once-per-episode refusal notice so a hold lost AGAIN logs again; driven by real parks in the same test file | ✅ **merged** — their PR #868, merged as-is 2026-09-02 (merge `b62fd765`; head an ancestor, verified same day) — ✅ came home in upfold0905 (2026-09-05) | MERGED with the maintainer’s own adversarial pass on the gate recorded in his merge comment (header case/duplicates fail closed, query edges fail closed, HEAD/OPTIONS/POST behind auth, no other exempt route carries a pre-auth side effect, kernel/manager token parity). Branch retired. OFFERED 2026-09-02 (branch `busydrain-gate`, `7009f2c9`, ONE commit off tip `b878b1ad`): kernel gate + manager token as one behavior change (the gate alone would break their own deploy-drain flow); fork-only handoff machinery and five security-chain-adjacent tests deliberately excluded; fork-voice comments re-voiced; red-first both halves; review clean, scaffold CI green (since-closed fork #141). Disclosure lives in the PR body only. Upstream ships the ungated version VERBATIM at `44239c05` (the arm runs in the exempt block with no auth). An offer carries only the gate + the manager's token-on-drain-poll (with the round-3/4 refusal-and-recovery notices and per-park evidence counters, which only make sense alongside it) + the `BusyDrainWriteGate` and `manager-drain` tests; the READ must stay exempt (a bare count leaks nothing and healthz-style probes rely on it). The gate uses an EXPLICIT token, not full `_authorize`, because a state-changing GET is reachable by cookie-carrying subresource loads (`<img>`/`<script>`, no Origin) that `_authorize` accepts for reads — only an explicit token clears both the cross-origin-fetch and the cookie-subresource vectors. A refused unauth drain returns the same read response without arming (expected traffic, not a fail-loud case). |
| The api_error-flush orphaning eats persisted replies: when the CLI recovers from an API-error storm internally (its own backoff retries), it streams AND persists the reply — then flushes its buffered `api_error` system records at the NEXT turn's start, parent-chained from the leaf as it stood at ERROR time (before the reply), writes the stop_hook_summary on the last api_error and the next user record on the stop hook. The persisted reply branch is bypassed on disk; the parse classifies it "rewind" (the one verdict that drops content) and the ghost-reply gates (`landed_text_uuids`, parse- and kernel-side) rightly refuse the orphanReply salvage because the text landed on SOME branch — so the reply vanishes from the chat, the judges read the turn as reply-less, and the model's own memory of it dies at the next transcript reload; the rewind goal-sweeps (`reconcile_rewound_goals` / `_rewound_away`) also read the branch as user-abandoned and can archive goals anchored there. Two incidents verified at transcript level (8 episodes); the shape reaches back to at least mid-Aug 2026 | fix in `orphanflush` (`kernel/event_model.py` `_reattach_flush_orphans` + a fifth `chain_verdicts` verdict `reattached`, kept, never sweepable; `PLACEMENTS_V` 10): a rewound branch whose ACTIVE-SPINE bypass at the fork STARTS with a `system/api_error` record is machine-orphaned by construction — a real user rewind forks at a USER gesture, never through an error chain — and one of its chains re-attaches, selected by PROPERTY, never by the CLI's byte order of writes: the max-seq sibling chain (leaf walked back to the fork) whose head is assistant-typed and which carries reply text, the fork standing down only when no chain qualifies — so a late-written stub pair cannot steal the splice and a late-written second error burst cannot veto it; parallel tool stubs inside the branch stay dropped like their on-spine twins, a later genuine rollback past the fork un-matches the signature and the branch drops again, and existing orphanReply markers stand down (the landed gate keeps suppressing them while the real atom renders). Corpus scan (4,678 transcripts): 49 bypasses match — all assistant-headed reply branches — vs 2,809 rewind forks that don't (2,035 parallel tool stubs, 700 superseded retries, 42 genuine user-gesture rollbacks, 32 other), zero overlap. Pinned red-first in `tests/test_event_model_golden.py` (OrphanFlushReattach: single/consecutive/mid-turn-storm episodes, user-gesture contrast + ghost gate intact, marker stand-down, stub isolation, stop-hook-only bypass stays dropped, user-headed belt, pending-cut precedence, and the sibling-chain write-order defense: late-written stub pair, sibling error burst, order-independent membership, two-reply-chains tiebreak), `tests/test_event_model_incremental.py` (warm/cold equivalence through the flush append, including the late-stub append order), `tests/test_judge_rewind_cleanup.py` (membership sets, sweep predicates spare the branch, kept-parity), `tests/test_placements_canary.py` (v10 seal) | ✅ **merged** — their PR #847, 2026-09-01 (merge `47cc4255`; our head `2d191838` an ancestor, PLUS his follow-on `030953cd` on the PR branch) | MERGED with the maintainer’s own upgrade riding the merge: he adversarially broke our leaf-chain selection three ways (junk-tail laundering, twin-sub-branch disenfranchisement, failure-echo replies), rejected his own first repair by the same method, and redesigned the unit to the BRANCH (sub-tree under a fork-side head, ranked by latest reply text; stand-down narrowed to user-headed branches via a new _is_tool_result discriminator; error-echo records excluded from the reply witness). Branch retired. REWORKED: the maintainer independently built the healing half mid-offer (their 1fa891a8, the eclipsed verdict + their own PLACEMENTS_V 10) — his probe is STRONGER than our original signature (reads through leading system records, refuses re-replied spines our old code would have doubled), but keeps the whole fork component. The offer now builds ON eclipsed: `_select_eclipsed_chains` narrows the keep to one property-selected chain (6 of 14 scenarios fail on unnarrowed T209: stub pairs render, doubled replies, a mid-storm-deleted prompt can re-show), `reattached` verdict dropped, PLACEMENTS_V 11. Re-rebased once more across their event_model churn (textual), full suite 6325 green, scaffold CI green (since-closed fork #133/#134). FOLD FLAG RESOLVED (2026-09-01, `upfold0901`): the fold took the composed upstream design wholesale — `_reattach_flush_orphans`, the `reattached` verdict and the fork's v10 ladder entry retired with their test classes (upstream's EclipsedBranch/EclipsedChainSelection plus the renamed equivalence/rewind/canary suites replace them and cover every fork incident shape, with the one deliberate semantic evolution that a stub INSIDE the winning branch now rides the keep — sibling isolation moved to the chain-selection layer); merged `PLACEMENTS_V` = 11, upstream's ladder entries verbatim, so a fork store recorded under reattached-v10 reseals exactly once at the version step. OFFERED 2026-09-01 (branch `orphanflush-offer`, `d04754ef` off tip `de3147f0`, ONE squashed commit — the fork’s second commit corrected the first’s selection rule, so only the finished mechanism ships): re-verified at tip (all four test files blob-identical; PLACEMENTS_V number-space matches, 9→10 transfers verbatim); red-first 13/13 OrphanFlushReattach fail on base; review clean, scaffold CI green (since-closed fork #131). The PR body carries the corpus counts and the anthropics/claude-code#91113 companion reference. Upstream ships the identical parse and gates verbatim (verified 2026-09-01 at their tip `0170bc80`: `chain_verdicts`/`kept_uuids`/`landed_text_uuids`/`active_path` byte-identical, whole `event_model.py` delta vs our base is one unrelated 3-line hunk; same `landedTextUuids` gate in kernel.py, same `chain_membership`/`_per_file_rewound`/`_rewound_away` in judge.py) — the bug and the fix apply as-is. Root cause is a CLI bug (the buffered flush hijacks the leaf instead of chaining from the current one) — the companion Claude Code bug report, filed as anthropics/claude-code#91113, is the real fix; the parse re-attach defends regardless and is needed anyway for the existing transcripts already carrying the shape. |
| Model pickers pin the user to an exact id on a FAMILY click, and the composer's typed `/model` bypasses the registry | fork branch `modelalias`, landed as fork PR #140 (2026-09-02) and RECONCILED onto upstream's T222 catalog in the `upfold0902` fold (kernel `_route_meta_command`, `_learned_versions`, `_versions_catalog`, `_model_alias_boot_pass`; `sdk_backend._learn_model` `liveModelId`) | ✅ **merged** — their PR #882, 2026-09-03 (with his `2d207d76` fold) — ✅ came home in upfold0905 (2026-09-05) | MERGED. All three decision points the body raised were accepted as shipped; his commit widens the one-time migration’s seed set to include fable’s 5.1 head (the very artefact the boot pass targets — our migration test had enshrined the miss). ⚠ FOLD FLAG: take his seed set at the fold. His two no-gate notes are queued as candidate rows below. OFFERED 2026-09-02 (branch `model-alias`, `a8abcc5b` off tip `eff7b191`, SEVEN separable commits; the body offers a pickers/backend split up front): re-derived onto T222’s catalog and T226’s menu tokens; the catalog-push sibling row rides in as commit 3 (the models frame); the LP-key credential policy excluded; docs/reference.md hunk skipped (upstream has no `romp new --model` row). Full suite 6563 green (xdist), node 2676, bats 100; review clean, scaffold CI green (since-closed fork #160). RECONCILED with T222 (fold `upfold0902`, 2026-09-02) under one rule — upstream's catalog owns the version LIST, the fork's alias owns the DEFAULT and the gestures: `/models` builds each family's `versions` from `_versions_catalog` (the seed table as the Models API fetch and its cache grew it in place, ∪ `_learned_versions` — the ids running CLIs report that the catalog lacks yet, marked — deduped by id, newest-first by the catalog's `_version_key`, every row through `_with_cli_block`) and its `default` from `picks.get(fam) or fam`; a catalog id is a pin (`_version_family` reads the in-place-rebuilt `_VERSION_FAMILY` at call time, under the catalog lock) and a refused one is forgotten like any other; a CLI-reported id the catalog lacks fires T222's staleness refresh (`_note_unknown_model`) so the mark drops once the catalog catches up; the fold's two `_MODEL_ID_RE` grammars collapsed to the catalog's (the fork's `_model_id_parts` reads it and drops a snapshot date — the auto-merge had shadowed one with the other and every learned-id parse raised). Their `ModelsRoute` pin asserting `default` = family-newest was rewritten to the alias; both sides' source pins in `test_kernel_judge_model.py` hold against the one merged line. The catalog's credential path is FORK POLICY (not for the offer): `ANTHROPIC_LP_API_KEY`, else the manager-env work key the SDK backend claims out of `ANTHROPIC_API_KEY` at its construction (`work_api_key`, wired as `jd._WORK_KEY_FN` before the boot refresh — the judges' stash, so the fetch bills where the judges bill), else a bearer; the kernel cannot tell a work key from a session-auth key by value, so the rule is operational — the MANAGER's environment carries the LP/work key, never the interactive session-auth key (fold fixer 2026-09-02, correcting the reconcile commit's claim that an ambient `ANTHROPIC_API_KEY` was never read: the claimer reads exactly that variable, and the first `CredentialPolicy` tests only exercised the unwired path; the wired path is pinned now). DELIBERATE DIVERGENCE from upstream's T223 wake remap (fold fixer 2026-09-02): their `_ensure` consults a kernel-installed `thread_wake_model` hook that re-points a dormant comment thread registered on a superseded full id to its family's newest at its next explicit wake — built when a full id in the reg was the ACCIDENTAL residue of a family click writing the head id. After #140 a full id in `reg.model` is a deliberate pin (the version submenu writes it verbatim, the create dialog sends a pinned family's id, the marker-gated boot pass treats every post-migration head as the user's), so the remap would override only deliberate pins; the fork leaves the hook UNWIRED (`_sdk_locked`; `_family_newest_model` stays, uncalled, as their merge surface) and pins it in `tests/test_thread_rows.py` (`ThreadWakePinsStand`: a thread pinned to a legacy version wakes ON it; an alias floats). An offer of #140 must say so: on their tree the remap and the alias default coexist only while heads are accidental. An offer now re-derives from this merged shape. VERIFIED on upstream `main` (`c7deb529`, 2026-09-01): both bugs are theirs too. `GET /models` computes `default = picks.get(fam) or MODEL_VERSIONS[fam][0].value` (kernel.py ~30636) with `MODEL_VERSIONS["fable"] = [claude-fable-5]` only (~829), so clicking "Fable" pins `--model claude-fable-5` while the CLI's `fable` alias moved to Fable 5.1 (2.1.257); and their `sendMessage` (~8944) hands a typed `/model X` to `_send_or_park` as literal text — the CLI executes it, but the reg / sdk-defaults / reconnect argv keep the old model and the switch reverts on reconnect (only their `sendCommand` ~8980 routes `/model`). Their `sdk_backend.set_model` docstring (~5757) and the "fable resolved to Opus 5" rationale (kernel.py ~7038, bin/romp ~1689) carry the same stale claims. An offer would carry: family default = alias (`picks.get(fam) or fam`); the seed table gaining `claude-fable-5-1` plus the learned-versions union (`liveModelId` persisted by `_learn_model`, `learned` rows marked "new" in both pickers); the composer `/model`/`/effort`/`/fast` routing through the setters (vouched values only — a typo stays the CLI's); the ONE-TIME boot migration of seed-head pins to aliases (mirrors the CLI's own `migration_fable5_to_fable_alias`; marker-gated by `STATE/model-alias-migration.done`, since three of the four heads are current releases a user legitimately pins after the fix); `set_model`'s revert-on-refusal (reg, sdk-defaults and chosen_model go back to what they were when the CLI rejects the value — the mode path's T139 rule, which their set_model lacks too: it persists before the CLI accepts and only logs a refusal) — reverting ONLY on a verdict: the installed SDK (0.2.132 `_internal/query.py`) raises the same bare `Exception` for a CLI error response and for a request that never got an answer (`Control request timeout`, chained from `TimeoutError`; `Query.close()` cancels the reader without resolving pending requests, so a request stranded by a reconnect teardown ends that way — reproduced: a model and an effort parked mid-turn and replayed back-to-back, the effort's reconnect tore the client down with set_model's answer unread, and the 60s timeout reverted every layer while the new connection ran the pick), so `_cli_refusal` tells them apart by construction (exact `Exception`, no `__cause__`, no timeout prefix), a lost answer leaves the optimistic state and logs one line, a refusal landing after a newer pick stands down entirely, and `_revert_model` compare-and-swaps each layer (sdk-defaults is SHARED across sessions — a stale revert rolled another session's accepted pick back; the store gains a lock) — and reverts the session's OWN layers (`chosen_model`, the reg) to its last ACCEPTED model (`_model_accepted`, moved only by the CLI accepting a pick, a connect launching with it, or a lost answer), never to the values the refused write happened to replace: with two picks in flight the second's snapshot held the first, so two refusals in a row wrote the first REFUSED id back into every layer; the SHARED `sdk-defaults.json` `model` is ruled per WRITE, by identity (round 6, 2026-09-02): every writer of the key stamps a fresh `modelTok` beside the value (`swap_sdk_default` / `write_sdk_default`), a live `set_model`'s write is remembered in the backend by token with the `(value, token)` it replaced until the CLI rules (`_seed_write_pending` — the write and its node landing in ONE hold of the store's lock, since a refusal of the write it replaced could otherwise run between them and re-point only the nodes that existed), acceptance drops the node (`_seed_write_settled`), and a refusal — a superseded one included — unwinds exactly its own write (`_seed_write_refused`: back to the replaced pair while the write is still the store's head, and a pending write that landed on it is re-pointed past it so ITS unwind never lands on a refused id; not the head and under no pending write → stand down; an untokened file → never reverted) — because a by-VALUE picture of that layer per session was wrong three ways: picking the value the store already held (another session's accepted pick, the common case) could not adopt it as a baseline so a refusal restored a stale one over that pick; an id this session once refused stayed unadoptable after another session accepted it; and another session's PENDING write was adopted as a baseline, so two cross-session refusals seeded the refused id; each optimistic write is a read-modify-write in one lock hold (`swap_sdk_default`, `_update_reg`, the session lock around `chosen_model`'s compare-and-assign), since a snapshot read outside the hold could describe a value the write never replaced; a refused version leaves the kernel's pick memory too (`on_model_refused` class hook → `_model_pick_refused`, dropping the family's pin only while it still holds the refused id — without it the family row kept sending the refused id and re-ringing the problem on every click; fired for a SUPERSEDED refusal too since round 6, the CAS making it safe for the newer pick's pin — and never for a lost answer, superseded or not); the boot migration tells PERMANENT garbage (a store json.loads cannot parse, or cannot decode — read as bytes so a non-UTF-8 file's `UnicodeDecodeError` lands in the same arm instead of aborting the pass — named by path, nothing to migrate, still stamped) from a TRANSIENT read failure (withholds the marker) — counting garbage a failure withheld the marker forever and re-floated deliberate pins at every boot; a `{type:"models", rev}` frame to the chat, timeline AND feed clients (the settings gear lives in the feed bundle) whenever model-picks.json changes, on which every picker (chat/comment, timeline lanes, gear) re-fetches `/models` — the cached list's `default` is what a family click sends, so after Latest un-pinned a family the same tab's next family click silently RE-PINNED it (event-keyed, no poll) — with the same `rev` stamped on the `/models` payload (clock-seeded so a kernel restart never hands a page a lower one) and every consumer keeping the highest rev it applied, so two overlapping fetches resolving out of order cannot let the stale list win (the gear paints its eight selects from whichever list won and gives each back the value it held — re-injected as a marked off-list option when the new list lacks it, since a select assigned an unlisted value goes blank — so a re-read overtaking the page-load fill never leaves the pickers empty — round 6); the "Latest" row atop every version submenu (chat/comment pickers, timeline lanes, gear judge tiers) — the one picker gesture back to floating once a family is pinned, carried as `floating` on the set op — and a "<Family> · Latest" row in the flat create dialog for a pinned family (it had no floating exit at all); the ack chip on a dormant session (their set_model/set_effort synthesize it only for a live thread, so a composer `/model X` on a dormant session leaves the optimistic bubble unconfirmed); the `[1m]` context tag read through in the pending-switch check; the create dialog sending the family default like the other two pickers; the two comment corrections; and the red-first tests (`tests/test_model_versions.py`, `test_sdk_kernel.py`, `test_sdk_backend.py`, `test_kernel_cmd_gesture.py`, the UI pin files). Re-derive the `/models` hunk on their tree: upstream's row also carries `tone=_model_tone(...)`, which the fork takes in fold #137. |
| `list_regs`'s whole-listing last-good serve resurrects OTHER state roots' sessions, by two arms: (1) it misreads a MISSING `sdk/` dir as a transient scan fault — its own comment claims a missing dir "enumerates FINE", but `os.scandir` RAISES `FileNotFoundError` there, so a state root whose first write hasn't happened yet takes the fault arm; and (2) the fault arm itself serves the ENTIRE module-level `_REG_CACHE` (path-keyed, shared by every backend in the process), so any REAL enumeration fault on one root — PermissionError, EMFILE, transient I/O — serves rows cached from every other root too. Cross-root session resurrection in any process holding backends over several state roots — their own test suite's shape; found at the 2026-09-01 fold when a foreign echo reseeded into a fresh backend and the fork's todo-echo suite went red, only under full-suite ordering | the fold merge (`upfold0901`), both arms: a `FileNotFoundError` arm ahead of the `OSError` arm in `kernel/sdk_backend.py` `list_regs` returns `[]` honestly (a dir that is gone took its regs with it — the listing contract's own unlink rule), and the surviving `OSError` arm's serve is ROOT-SCOPED — the cache filters to reg paths under THIS root's `sdk/` dir (reg paths are absolute, so the root is derivable), with a per-root incident sentinel; the last-good purpose stays for the faulting root's own rows. Pinned by `tests/test_sdk_live_rows.py` `test_a_missing_dir_is_empty_truth_never_another_roots_rows` + `FaultServeRootScope::test_a_fault_on_one_root_serves_only_that_roots_rows` | ✅ **merged** — their PR #874, merged as-is 2026-09-02 — ✅ came home in upfold0905 (2026-09-05) | MERGED as-is (head is an ancestor). Branch retired. OFFERED 2026-09-02 (branch `listregs-roots`, `8a78d7a1` off tip `70a36077`): both arms; review clean, scaffold CI green (since-closed fork #149). Upstream ships the identical arm (their 2026-09-01 listing-completeness review round, `17f77df1`'s file), unfiltered serve included. Production mostly dodges it — one backend per process and `sdk/` exists once anything wrote — but a fresh install's first scan before the first write takes the fault arm too, and any future multi-root embedding inherits the resurrection. Small fix + two tests, cut clean off their tip; the offer carries BOTH arms (missing-dir honest `[]`, fault-arm root-scoped). |
| The /compact suggestion can DOUBLE-SEND under concurrent ticks: `_compact_suggest_tick` checks the per-session `compactSuggested` latch under `_NUDGE_LOCK`, releases the lock, runs its fire-time gates, sends the injected suggestion, and latches only AFTER the send — so two concurrent entries both pass the same unlatched check and inject the suggestion twice into one session. The tick rides `_auto_nudge_tick`'s alive walk, which has concurrent entry points by design: the pusher's periodic pass racing the setCompactSuggest WS handler's act-now re-tick (upstream's shape at their tip `c0443a58` — their setAutoNudge handler no longer re-ticks there; the fork's own setAutoNudge re-tick, restored with the gt-gating, adds a third) | the fold branch (`upfold0901`): `kernel/kernel.py` `_compact_suggest_tick` claims the latch under the lock BEFORE the send — a fresh re-read re-derives what is still due, so the losing entry stands down without sending — and a send failure rolls the claim back under the lock, so the next tick retries ("never latch a fire that never went out" holds durably — durable against the ledger's OTHER writers too, which required closing a side door: the blob's remaining unlocked writers (the debt reminder, outcome and backstop sweeps, the deferral mint + sweep, the interrupt-block marker) ran whole-blob read→write spans with no lock, and a debt reminder's pre-send snapshot written back after the claim erased the latch — or resurrected a rolled-back one past its failure rollback — re-enabling the double send; every writer now holds `_NUDGE_LOCK`, and the ones with IO mid-span re-read fresh under it and mutate only the keys they own, so both writers' outputs survive any interleaving). Pinned red-first by a deterministic two-thread race (events gating the entries between check and send, no sleeps — the TwoFlushRace idiom) + a send-failure-retries pin in `tests/test_compact_suggest.py` (`ConcurrentSendRace`), and the side door by debt-writer straddles of a successful claim (erasure) and of a failed send's rollback (resurrection), each also pinning that the debt keys still land (`DebtWriterRace`, same file) | ✅ **merged** — their PR #897, merged as-is 2026-09-03 (merge `0f86364d`) — ✅ came home in upfold0905 (2026-09-05) | MERGED as-is, reviewed inline after: claim-before-send with failed-send rollback under the lock "exactly right"; the ledger-writer migration (pure dict spans under the lock, IO outside, fresh re-read mutating only the writer’s keys) is what makes the claim durable — the debt writers’ pre-send whole-blob write "was a real side door". One red check on the head was a bats runner hitting Address already in use (fixed-port collision class; he reran) — queued as a candidate row below. Branch retired. OFFERED 2026-09-03 (branch `compact-latch`, `196483e7` off tip `7faf24bc`, two commits): latch claimed under _NUDGE_LOCK before the send + the unlocked ledger writers; the fork’s _set_auto_nudge/_set_compact_suggest bodies deliberately NOT carried (upstream’s merged versions carry `_gesture_echo`). Review clean, scaffold CI green (since-closed fork #178). Upstream ships the same check-release-send-latch shape verbatim at their tip, with the same pusher-vs-WS concurrency — and the same unlocked ledger writers (their `_fire_debt_reminder` is byte-for-byte the erasable snapshot shape), so the offer must carry the writer migration with the claim or the fix stays erasable there. Small: latch-before-send + failure unlatch + the lock-covered key-scoped writers + the tests, cut clean off their tip. |
| A hermeticity floor for the T222 model-catalog refresh: constructing the SDK backend fires `_refresh_model_catalog("boot")` at every build — an async GET to the Models API on any credential the process carries (the manager-env key `work_api_key` claimed, else a bare `ANTHROPIC_API_KEY`, else an `ANTHROPIC_AUTH_TOKEN` bearer) — and upstream floors only its two kernel-SPAWNING tests inline (`test_gear_select_matrix.py`, `test_ship_reship.py`). In a shell carrying none of those the refresh finds no credential and stops, so no test in either suite reaches the network today (checked, not assumed); but every in-process backend construction (thread rows, user todos, judge billing, any future test) is one exported key away from a real request no test asserts on, on a key the test never chose | the fold merge (`upfold0902`): a suite-wide `ROMP_MODEL_CATALOG=off` floor in `tests/conftest.py` — import-time set plus a per-test autouse re-assert, so the catalog suite's own setUp/tearDown pops cannot erase it for the tests that follow (the manager-port poison's reasoning) — pinned by `tests/test_model_catalog_floor.py` (a test pops the switch; the next proves it is back and the refresh is inert) | ✅ **merged** — their PR #877, merged as-is 2026-09-02 — ✅ came home in upfold0905 (2026-09-05) | MERGED as-is (head is an ancestor). Branch retired. OFFERED 2026-09-02 (branch `catalog-floor`, `612b80b4` off tip `70a36077`, test-only): review caught an inaccurate pytest-internals claim in the comment/body — corrected to the verified fixture-fill-before-TestCase.run mechanism before posting; scaffold CI green (since-closed fork #152). Defensive and test-only, no fork-specific content; the catalog suite's fake-server tests keep working because they pop the var in setUp (the fixture runs first). Cut clean off their tip: the conftest block + the pin test. |
| `tests/test_model_fallback_card.py` `FallbackCard` mints its card under the SHARED placeholder sid, so another module's leftover override journal (replayed by `load_goals` on every load; node ids collide at `<sid>:g1`) can reopen the minted card mid-test — green alone, red only under whole-suite ordering; a parallel (xdist) run surfaced it | the fold merge (`upfold0902`): the class takes a private synthetic sid and unlinks its journal in tearDown, exactly the shape the file's own `DedupeBackstop` already uses (reproduced red-first with a planted journaled follow-up, green after) | ✅ **merged** — their PR #881, merged as-is 2026-09-02 — ✅ came home in upfold0905 (2026-09-05) | MERGED as-is (head is an ancestor). Branch retired. OFFERED 2026-09-02 (branch `fallback-sid`, `ea4cc4b4` off tip `70a36077`, test-only): the review REFUTED the port’s "why CI is green today" story (earlier tearDowns do NOT wipe the shared journal; the collision is live in serial order and the assertions held only because none of the leftover ops touches g1) — docstring, commit message and PR body rewritten to the measured facts before posting; scaffold CI green (since-closed fork #153). The file is byte-identical upstream, so the flake exists there too. Test-only, two-line fix plus a docstring. |
| T223's dormant-thread boot skip (`_boot_reconcile`: `if r.get("threadOf") and not queued: continue`) runs BEFORE the arm that reports and clears a dead life's `pendingAsk` / `bgTasks`, and an explicit thread wake (`_ensure` → `SdkSession`) never reads or clears either flag — so a dormant thread whose CLI died holding an ask or background tasks carries the stale flags across every boot and wake until a later boot finds it WITH a queue, and then prepends both old-life notices (`ASK_DIED_NOTICE`, `task_death_notice`) ahead of the user's freshly typed reply | inherited verbatim from upstream `b878b1ad` (`kernel/sdk_backend.py` `_boot_reconcile`: the T223 guard above the T214 ask/bg-task arm); not fixed on the fork | ✅ **merged** — their PR #878 (commit B), merged as-is 2026-09-02 — ✅ came home in upfold0905 (2026-09-05) | MERGED as-is. Branch retired. OFFERED 2026-09-02 combined with the boot-regs-reread row into ONE two-commit PR (branch `boot-reconcile`, `03ffc92b` off tip `70a36077`) — both edit the same _boot_reconcile pass; review clean, scaffold CI green (since-closed fork #155). Found by the fold's adversarial review; not a fork regression — the fork's `_queue_texts` composition only decides what `queued` is, the ordering is upstream's. Fix shape: for a skipped thread, report-or-clear the two flags the way the pending-flag heal already runs above the skip, or clear them at the explicit wake. Red-first test before offering. |
| `SdkBackend.__init__` snapshots `regs = list_regs(...)`, then `_reseed_echoes(regs)` may REFEED a proven-lost human send into a reg's persisted queue via `write_reg` (`_mark_dropped_echoes`, refeed path), then `_boot_reconcile(regs)` walks the PRE-refeed snapshot — so a session whose state tail is not machine-active (no cut turn, no dead tasks, no dead ask) reads an empty `queued` off the stale dict and is not resumed that boot despite the send now sitting in its queue on disk; it delivers only at the next spawn or boot | inherited from upstream `b878b1ad` and present in BOTH merge parents (`kernel/sdk_backend.py`: `__init__`'s regs snapshot, `_reseed_echoes`, `_boot_reconcile`'s `r.get("queue")`) | ✅ **merged** — their PR #878 (commit A), merged as-is 2026-09-02 — ✅ came home in upfold0905 (2026-09-05) | MERGED as-is. Branch retired. OFFERED 2026-09-02 as commit A of the combined boot-reconcile PR (branch `boot-reconcile`, `03ffc92b`); review clean, scaffold CI green (since-closed fork #155). Fix shape: re-read the reg inside the reconcile loop (`read_reg` per sid — the RMW arm already does) or re-list after the reseed. Test: an alive reg with a human echo absent from its queue and a `waiting` tail; assert the boot resumes it with the refed text queued. |
| `_refresh_model_catalog` called `_push_soon()` after a fetch added version ids, its comment claiming the pickers re-read `/models` on the next frame — but no client re-read the choice lists after page load: `render.ts` filled `MODEL_CHOICES` / `EFFORT_CHOICES` from one page-load fetch, `gear.js` wrote its options exactly once per page (`choicesP`), and the timeline's lane list loaded once — so an open dashboard kept the page-load list until a reload, and the push was a no-op for the thing it named | FIXED on the fork in the `upfold0902` reconciliation (2026-09-02): the fetch that adds ids emits the models frame (`_models_changed` — the `{type:"models", rev}` frame fork PR #140 gave every picker a listener for: chat/comment `loadModelChoices`, the timeline lanes' `refreshModels`, the gear's `adoptChoices`/`paintChoices`), so every open picker re-reads `/models` on the event; the comment names the real mechanism. Pinned in `tests/test_model_catalog.py` (`FetchAndFallback::test_a_fetch_that_adds_ids_tells_every_open_picker_to_re_read_models`, silent when a fetch adds nothing) | ✅ **merged** — their PR #882 (commit 3, the models frame), merged 2026-09-02 (merge `ab176ed7`); the code is in HEAD — ✅ came home in upfold0905 (2026-09-05) | MERGED as-is inside their PR #882 (the model-alias offer's third commit; merge `ab176ed7`, 2026-09-02) and in HEAD after upfold0905. What merged: `_refresh_model_catalog` calls `_models_changed()` when a fetch adds version ids, and `_models_changed` sends the `{type:"models", rev}` frame to the chat, timeline and feed clients whenever the pick memory moves or the catalog grows (both in `kernel/kernel.py`; the comment names this mechanism). Every open picker re-reads `/models` on the frame, highest rev wins: the chat and comment pickers' `loadModelChoices` (`ui/webview/render.ts`), the timeline lanes' `refreshModels` (`ui/webview/timeline-boot.ts`), the gear's `adoptChoices`/`paintChoices` (`ui/webview/gear.js`). Pinned by `tests/test_model_catalog.py` (`FetchAndFallback::test_a_fetch_that_adds_ids_tells_every_open_picker_to_re_read_models`; silent when a fetch adds nothing). It was offered inside the alias PR rather than alone because on upstream's tree without #140 the frame had no listener. Branch retired. |
| An answer that arrives between an ask's PRESENTATION and the ARMING of its future is dropped. T214 (`8ab85123`) made `SdkSession.resolve_ask` read `_cur_ask_fut` synchronously on the caller's thread and report False when it is None/done (the truthful delivery outcome — right), but all three ask coroutines present FIRST and arm SECOND: `_can_use_tool`'s permission branch, `_approve_plan`, and `_ask_one`'s `emit()` each call `_emit_ask` and only then `await _next_ask_action()`, which is where the future was created. An answer landing in that gap is reported lost and the coroutine waits forever. Live effect is a rare false `askLost` toast (only the kernel's click handlers answer, from another thread, so the gap is microseconds); the SDK-gated round-trip tests answer synchronously INSIDE the presentation callback and hit it every time — `CustomAnswerRoundTrip` and `PermissionAndPlanRoundTrip` hang, `AskRoundTrip`'s three ask-driven tests fail at their first wait | branch `askarm`: `kernel/sdk_backend.py` `_arm_ask()` creates (or keeps) the future and is called immediately before `_emit_ask` at the three sites; `_next_ask_action` awaits the armed future and clears in `finally` as before. Pinned SDK-free by `tests/test_sdk_backend.py` `AskArmedBeforePresent::test_an_answer_delivered_inside_the_presentation_callback_is_not_lost` — `_ask_one` needs no SDK, so the standard runner and CI exercise the invariant (red at the fold tip, green with the fix) | ✅ **merged** — their PR #875, 2026-09-02 (our head an ancestor + the maintainer’s `d765be70` folded on) — ✅ came home in upfold0905 (2026-09-05) | MERGED with a maintainer correction on top: arming the future at the ask sites let two OVERLAPPING asks (each SDK control request is its own detached task) share one live future — one click answered both, turning the visible hang into a silently wrong permission; his commit holds a per-session asyncio.Lock from present to resolve so asks answer in turn, and narrows _arm_ask’s keep branch to _next_ask_action. ⚠ FOLD FLAG: fork main carries our original shape — take his serialization at the fold. Branch retired. OFFERED 2026-09-02 (branch `ask-arm`, `4be7b0f6` off tip `70a36077`): _arm_ask at the three sites; the body notes upstream’s own SDK-gated round-trip tests hang on the bug; review clean, scaffold CI green (since-closed fork #150). Upstream/main (`b878b1ad`) is red identically: same `resolve_ask`, same present-then-arm order at the same three sites, and their own SDK-gated `AskRoundTrip` / `CustomAnswerRoundTrip` / `PermissionAndPlanRoundTrip` hang and fail the same way. Neither CI installs `claude_agent_sdk`, so both projects SKIP those classes and never saw it — worth raising as a companion suggestion (install the SDK in the test job, or carry an SDK-free pin like ours). About 13 changed lines + one test, cut clean off their tip. Landed on the fork as PR #139 (2026-09-02); the `upfold0902` fold verified `_arm_ask` still precedes all three `_emit_ask` sites in the merged `sdk_backend.py`. |
| Working-note expiry keeps the note of a session parked on the user: `_clear_done_working_notes` keys on a new `_open_top_goal` (rolled-up status `working` OR `blocked`) instead of `_working_top_goal`, so only a session whose top goals are all done or cleared has its `set_working` claim lifted | branch `notekeep`: `kernel/kernel.py` (`_open_top_goal`, `_clear_done_working_notes`), `tests/test_kernel.py` | ✅ **merged** — their PR #893, merged as-is 2026-09-03 — ✅ came home in upfold0905 (2026-09-05) | MERGED as-is (head is an ancestor). The fold flag above (fork main still carries the two-helper shape) stands. Branch retired. OFFERED 2026-09-02 (branch `notekeep-offer` — `origin/notekeep` is the fork’s own merged branch — `bdfa6136` off tip `31d8731b`): review caught a FALSE call-graph claim (Auto Nudge dropped its `_working_top_goal` call in c48dd598; the expiry was the sole caller), so the offer renames/widens the helper IN PLACE to `_open_top_goal` — no second helper, no dead code; the expiry test now drives the real predicate and asserts the lift on completion. ⚠ FOLD FLAG: fork main (af6d5676) carries the OLD two-helper shape — take upstream’s rename-in-place at the next fold. Scaffold CI green (since-closed fork #166). Upstream ships the same expiry and the same postal contract ("a peer with no note holds nothing"), so the same defect: a blocked-on-you session is idle without being done, its worktree/branch/files are still its own, and lifting its note told peers the surface was free (a 2026-09-02 census across this box's sessions found parked sessions whose claims had vanished from `list_agents` while they still held them). Reverses the first cut's deliberate "blocked-on-you also lifts" choice; the reason is recorded in the docstring. Two lines of code plus two tests; ports as-is. |
| Moving a live session's working directory — a thin wrapper over the CLI's own `set_cwd` control request (CLI 2.1.257, the headless twin of `/cd`; sent through the SDK's private `_send_control_request`, which has no public wrapper): `SdkBackend.move` (canonicalise → revive if dormant, in the OLD cwd → `busy` check → two-phase `cwdPending` reg write → request, `needs_trust` answered as the user's own folder pick → on `ok` the live cwd, reg + `movedFrom`, names/ file and the PRIOR-EPISODE transcripts the CLI does not move all follow, by rename, never copy), a boot-reconcile heal for a kernel death between the CLI's `ok` and the reg write, a guard that keeps the move's turn-less `ResultMessage(num_turns=0)` from running the turn-settle side effects, the drive-op FIFO's new `("cwd", path, retries)` kind (`_moving` holds the queue mid-move; bounded retry on a CLI-side `busy`), `moveSession` WS op + `POST /move` + `romp move`, a "Move to folder…" tab-menu row with its dialog, and the system card reading `_cwd_of` first so it does not lag the move | fork branch `sessmove` | ✅ **merged** — their PR #888, 2026-09-03 (our head an ancestor + his merge-of-main `1e27c135`) — ✅ came home in upfold0905 (2026-09-05) | MERGED: the maintainer merged current main into the branch himself — six ADDITIVE unions with the per-session env change (#889): parked-op kinds gain both env and cwd, the env chip and the move chip render side by side, the learned-version _learn_model(raw=) call sits beside the move-settle branch, the two new backend attributes coexist. ⚠ FOLD FLAG: the fork’s sessmove (fork #146) predates those unions — take upstream’s resolution at the fold. Branch retired. OFFERED 2026-09-02 (origin branch `sessmove-offer` — `origin/sessmove` is the fork’s own merged feature branch, so the agent correctly refused to overwrite it — `33c0e09a` off tip `31d8731b`, six commits): the body flags the SDK private-sender dependency and the CLI version verified against; the ABC default re-derived backend-neutral (upstream’s pending Codex backend would inherit it); adjacent-line overlaps with open #878/#882 expected to rebase mechanically; review clean, scaffold CI green (since-closed fork #167). Upstream ships the same "fixed at creation" model (their `_cwd_of` docstring says there is no SDK call to relocate a session — there is now). Pure feature; every piece is code upstream runs too, no fork-specific content. The private-sender call is isolated in ONE helper (`SdkBackend._set_cwd_request`) with a note to swap it for the public wrapper once the Python SDK grows one — the TypeScript SDK already ships `setCwd(path, {trustAccepted, trustedDirectory})`; an offer should say so. Verified facts worth carrying into any offer body: the CLI MOVES the current `<lastSid>.jsonl` + `<lastSid>/` folder (older fsids of the sid stay behind — romp moves those), sets a pre-existing destination file aside as `.superseded-<ts>`, fires `CwdChanged` (not SessionStart), and a same-fsid transcript under two slugs breaks every future `--resume`; a `set_cwd` sent right after a `ResultMessage` was accepted in the re-run (the "~1s busy window" noted during the first investigation did not reproduce), so the bounded retry is a backstop, not the main path; per-record `cwd` stamps are the CLI's tracked cwd (a Bash `cd` moves them too), hence the registry-first system card. Tests: backend arms with a scripted fake control channel, kernel park/fire/HTTP, event-model handling of the `relocated` + isMeta records, bats argv plumbing, UI source pins, plus one live integration test (opt-in, skips without the SDK + CLI + auth). |
| The API-health signal: `GET /api-health` (authed). Per-(auth label, model family) attempt / response / give-up counts over 60/300/900 s windows; `ok`s deduped (one per `AssistantMessage.message_id`); main thread only, with the exclusion declared in the payload; `rate429` named as an ATTEMPT share beside distinct sessions/turns retrying; give-ups counted ungated from `ResultMessage.api_error_status` (paired with the error-stamped assistant frame that precedes it); a thrash/degraded/recovering/unknown state derived AT READ TIME from the ring and the last persisted `(state, stateSince)` (no tick; hysteresis = enter/exit thresholds plus a 120 s hold on two windows, backtested against the 2026-09-01 shape; every bucket comes back `unknown` at a restart) with the per-bucket state and the transitions tail persisted atomically in a bounded `STATE/api-health.json`; `romp api-health`; two one-shot log diagnostics (the first `api_retry` frame's keys, the first sighting of each unhandled `SystemMessage` subtype) | branch `apihealth`: `kernel/sdk_backend.py` (`ApiHealth`, `api_health_state`, `api_health_counts`, `api_health_auth_label`, the `_ah_note_*` hooks in `_on_message`), `kernel/kernel.py` (`/api-health` after the gate, boot identity from `/version`'s globals), `bin/romp` `api-health`; tests `tests/test_api_health.py`, `tests/test_kernel_auth_hardening.py::ApiHealthRouteGate`, `tests/romp-api-health.bats`; `docs/reference.md` "The API-health signal" | candidate | Every symbol it hooks exists upstream (`SdkSession._on_message`'s api_retry / Assistant / Result branches, `_note_auth_source`, `work_api_key`, `acct_digest`, `_authorize`, `_BOOT_ID`/`_STARTED`), so the change ports as-is; no in-kernel consumer, no UI. Fork-specific, to flag in the body: the auth label is a per-install SALTED digest (`STATE/api-health-salt`; an empty file means unsalted). Upstream may prefer plain digests like its account digest, and the salt file is the one switch. Vocabulary: the JSON's top-level roll-up is `overall`, not the design note's `fleet`, to honour the fork's word rule. The note's `derivedAt`/`tickS`/`retryPause`/`tokens` fields are deliberately not shipped (state is read-time, the retry-pause consumer is a follow-up, token sums were not in the reviewed scope). Thresholds come from one incident on one box; re-check against the next before trusting them. |
| `tests/test_kernel_trust.py::PairsSnapshot` leaks `km._remotes` across modules under pytest-xdist — three tests fail on a parallel run, pass serially and alone | fork commit `dd0166e4` (fork-side fix, already on fork main) | ✅ **merged** — their PR #892, merged as-is 2026-09-03 — ✅ came home in upfold0905 (2026-09-05) | MERGED as-is (head is an ancestor). Branch retired. OFFERED 2026-09-02 (branch `trust-xdist`, `dd1d8850` off tip `31d8731b`, test-only): review refuted two file-name claims in the body’s "Not in this PR" paragraph (one file only mentions `_remotes` inside an assertIn string; the other loads the kernel under a separate module name and restores the map) — replaced with two surveyed planters that do share the module; scaffold CI green (since-closed fork #169). Found 2026-09-02 while validating the fallback-sid offer with `pytest -n 8` at upstream tip `70a36077` (3 failed on run 1, 0 on run 2 — worker-assignment dependent). Pre-existing at upstream tip, not a fork regression; upstream CI runs serially so it never sees it. Test-only. |
| Feed deltas, loud socket drops, event-keyed stale banner. Measured 2026-09-02 on a live kernel: every feed push was the whole frame (5.76 MB on a board of about 660 cards; asks 4.8 MB, ledgers 0.95 MB, all else 13 KB), re-sent every few seconds during activity when 0 to 21 cards and 0 to 1 ledgers differed, and the per-card `trgb` colour ticked with the clock so a colour step re-sent everything. Three frames (`WS_QUEUE_BYTES`) got a client dropped by `_mk_ws_send`, silently (`_send_client` swallowed it; the shim logged nothing on close), and each reconnect's 1 s `armStale` timer fired before the resync frame: about 300 stale-banner raises a day, almost all on the feed pane. Three layers. (1) `{type:"feedDelta"}` to a caught-up client that announced `?caps=feedDelta` (upserts by `itemId`/`sid`, removals, `top` whole when changed); every pane page announces `readyGate` on its socket and the kernel holds every push to that socket until the bundle's `ready` (the shim opens the socket before the bundle has loaded; a frame that landed first was lost, and the `ready` frame was then deduped against it); the cached full frame is served on that `ready`, stamped with the time of the serve rather than of the build (the pusher builds only while a client is connected, so a frame cached overnight carried a clock a night old), and the shim re-sends `ready` on a reconnect once the bundle has sent its own (a reconnecting pane waited 7 to 24 s; a redial that completes before the bundle has loaded sends nothing, and a second `ready` on one socket re-serves the frame); the pane anchors its live clock on the frame's `now` paired with its wire arrival (`nowAt`, stamped by federation), so a re-emitted merged frame never moves an age; `needFullFeed` re-base; `trgb` out of deltas and out of the dedup signature (full frames keep it for older bundles), the client computes the tint from `t` on a live clock. (2) One `ws drop:` stderr line and a bell row per drop, logged at the raise site (`_mk_ws_send` → `_note_ws_drop`, so every direct `client["send"]` caller is covered) and naming the push slot whose frame tipped the budget when a push did (the bell shows at most 5 drop rows, none older than an hour), a `wsclose` clientDiag with code, reason and socket age for every socket that opened, and one coalesced `wsconnfail` row for the redials an outage refused. (3) The stale banner raises on the SECOND keepalive arriving before the resync (one heartbeat period with no resync) or on the reconnected socket closing, never on a timer; a reconnect re-sends `ready` once the bundle has: a feed pane is served its cached frame, every other pane its connect push. | branch `feeddelta`: `kernel/kernel.py` (`_strip_trgb`, `_dedup_sig`, `_feed_parts`, `_feed_delta`, `_feed_body`, `_feed_ms`, `_send_feed`, `_feed_wire_now`, `_send_feed_now`, `READY_GATE_CAP`, `_client_ready`, the `ready` handler, `_note_ws_drop`, `_client_send`, `_WS_DROPS`, `_sdk_problem_rows`, `_shim` caps, stale rule and close breadcrumbs), `ui/webview/feed-delta.ts`, `federation.ts` inbound, `feed.ts`, `feed-age.ts` and `age-color.ts` (`ageRgb`, `cardTint`, `nowSec`); tests `tests/test_feed_delta.py`, `tests/test_ws_drop_loud.py`, `ui/webview/feed-delta.test.ts`, `feed-age.test.ts`, `pane-shim-stale.test.ts`, updated shim pins; `docs/read-side.md` | **offered** — their PR #952 (2026-09-06), label `fix` (the fork's own delta WIRE PROTOCOL is moot: upstream #914 + the #928 fold shipped one on 2026-09-04; only the remainder rode) | Upstream ships the same pusher, shim and feed, so it ports as-is. Wire contract additions only: full `feed` frames still go to every client that does not announce (the VS Code pipes, the Outline pane, remote federation sockets, older bundles) and still carry `trgb`, so an older bundle that destructures it (`const [r,g,b] = it.trgb`) keeps working — a stale tab, or a federated dashboard on a host running the previous build; deltas omit it. The hold is announced too (`readyGate` on the shim's socket URL): a socket that does not announce it is pushed to from accept as before, so federation's remote relays and an older dashboard's sockets are unchanged. Reviewed adversarially 2026-09-03, two rounds. Round one filed 22 items: 14 distinct defects after duplicates, two measurements, one vocabulary flag. Round two confirmed seven distinct items: two majors (the first frame still lost, now to the pusher's own cycle, after the accept-time push was removed; every age anchored on the served frame's hours-old `now`) and five minor ones (an unstamped age, two test gaps, the drop line's lost slot, this row's overstatement). All fixed on the branch; the pusher race was re-run in headless Chromium after the fix (40 loopback loads and 30 with the bundle 350 ms slower, none stuck). Round three confirmed four: two majors (the shim re-sent `ready` on a redial that completed before the bundle had loaded, so the kernel served the frame to a page with no listener and deduped the bundle's own `ready` — 3 of 3 headless loads stuck with the first socket dropped mid-load; the pane re-anchored its live clock on the emit time of every re-emitted merged frame, so a tab drag or an attached kernel's repost moved every age back by the quiet period) and two minor (three ready gates unpinned; a stale tuple comment). All fixed; the reconnect harness re-run after the fix paints 3 of 3, and 20 loopback loads plus 10 with the bundle 350 ms slower stay unstuck. By design, remote federation sockets keep full frames, and the legacy 60 s repost stays for clients that do not announce deltas; the Outline pane kept full frames here too, until the `outlinedelta` row below moved it to deltas. |
| Bare `haiku` alias pinned to 4.5 in `_ALIAS_HEAD` with no drift signal if the catalog moves it; and `test_index_tier_with_no_model_resolves_the_tier_pick` passes against main too, so it does not bind what its name says | maintainer’s no-gate notes on their #880 (2026-09-02); not yet built anywhere | **offered** — their PR #948 (2026-09-06), label `fix` | Two small follow-ups the maintainer named while merging the judge thinking lever. A drift pin for the alias head, and a test that actually binds the tier-pick resolution. |
| `_learned_versions` re-reads every reg on hot paths (the /models read, each pick, the SDK loop) | maintainer’s no-gate note on their #882 (2026-09-02); not yet built anywhere | **offered** — their PR #949 (2026-09-06), label `fix` | Cache or memoize the learned-version scan per boot/rev; he called it fine but worth a line — a perf follow-up once the model-alias stack lands. |
| The composer’s `/effort` reconnects an SDK session while tmux `/model` auto-confirms — asymmetric gesture behavior worth documenting or aligning | maintainer’s no-gate note on their #882 (2026-09-02) | **offered** — their PR #950 (2026-09-06), label `tests-only` (docs + pins, no behavior change) | Documentation-or-alignment follow-up; decide which after the model-alias stack merges. |
| Gesture stamps order by device WALL CLOCK: a laptop ten minutes ahead picks X and every pick from a correctly-clocked phone is refused for ten minutes with copy asserting another device changed it — no gesture can win | maintainer’s follow-up on their #879 (2026-09-03); not yet built | **offered** — their PR #945 (2026-09-06), label `fix`; combined with the other #879 follow-up row into one PR | His proposed shape keeps the design: stamp gestures as `max(Date.now(), lastSeenGt + 1)` (the gear already fetches /version on open and could learn each store’s current gt), add an explicit "apply anyway" action on the settingStale toast (a new user gesture is legitimate new information), and toast copy that does not claim another device acted when the kernel cannot know that. Event-over-clock doctrine. |
| Federation queue edges: a setting queued for a DOWN host is silent at queue time (no client-diag breadcrumb); `flushPending` clears the map only after the whole loop, so a send that throws mid-flush replays delivered entries on the next open; N kernels refusing one stale flush yield N identical toasts naming no host | maintainer’s follow-ups on their #879 (2026-09-03); not yet built | **offered** — their PR #945 (2026-09-06), label `fix`; combined with the other #879 follow-up row into one PR | Three small hardening items on the merged queue-aware sender: queue-time breadcrumb, per-entry clear during flush, host-attributed dedup of stale toasts. |
| `setCompactSuggest` is absent from federation’s `KERNEL_SETTING` set — the maintainer read it as an omission in his #879 follow-ups | design fact, not a change (the per-install, non-propagating settings idiom, 2026-09-01) | **keep as-is — deliberate; explained to the maintainer** | NOT a bug: `setCompactSuggest` and `thinkingSummaries` are per-install settings by design — each kernel keeps its own answer and the gear shows the local kernel’s value as authoritative, so broadcasting them to every remote host would be the defect; `gear.test.ts` pins the KERNEL_SETTING membership by source-parsing federation.ts, so adding it goes red there too. Explained on their #879 (2026-09-03). Any thinking-summaries offer must keep the same non-propagating shape. |
| Upstream bats suites collide on FIXED ports on shared CI runners (`Address already in use` — the red check the maintainer reran past on his #897 merge); the fork already moved one such port (the romp-manager-ensure.bats hunk in fork commit `2b9c5e48`, deliberately left out of the busy-drain offer) | fork commit `2b9c5e48` (partial: one file); a general free-port helper is not yet built | candidate | Flake class, not a logic bug: bats files bind fixed ports, and two suites (or two runners on one box) collide. Offer shape: a shared free-port helper in the bats support lib + migrate the fixed binds; the fork’s single-port move is the precedent. Test-only. |
| To-do card false alarm: `_fold_tasks` folded a BACKGROUND-AGENT `TaskCreate` (the Task tool's `{agent_hint, prompt}` shape, no `subject`, result is an agent id not "Task #N") into the checklist as a phantom pending task, so a session that only launched background agents tripped the card's "can't read the task store" error whenever the store was unresolvable | branch `todofix`: `kernel/kernel.py` (`_fold_tasks` guard), `tests/test_kernel.py` | **offered** — their PR #942 (2026-09-06), label `fix` | OFFERED 2026-09-06 (branch `todofix-offer`, `764089c8` off tip `2b9db2be`): the review caught a REAL recorded background-task id in the test fixture and this row/kernel comment named another project’s session — both redacted here and in the offer (the strings are now in the private-strings scanner); the card-level assertion was made non-vacuous. Scaffold CI green (since-closed fork #205). Upstream ships the same `_fold_tasks` and the same error card, so the same false alarm. One guard: a create with no `subject` and a `prompt`/`agent_hint` is a background task, not a to-do — it does not fold. Repro: a background-agent-only session (another project)'s overnight pipeline (the user 2026-09-03). ~3 lines + a test; ports as-is. |
| The web file viewer's title bar names the session a file was opened from — an identity-coloured chip (the waiting pane's .wt-sess idiom), "host:" quiet for a remote session; the viewer carried the sid for federation routing but never showed whose file was up | fork commit `dfaf23e8` (ui/webview/file-view.ts resolver export + chip; resolvers registered beside initFileView in render.ts and feed.ts; both sheets + fileview-parity.test.ts; pins and an executed ladder replica in ui/webview/file-view.test.ts) | candidate | UI-only, no kernel change. Upstream ships the same shared viewer with the same sid-in/no-label-out, and its shell-relay and Reload openers know only the sid too, so the resolve-from-sid shape carries over unchanged. Leaves the file browser's own bar unlabeled (optional follow-up on the same resolver). |
| A "Files" pane: the file viewer as its own dashboard column (`/files`, `app=files`, `ui/webview/files.ts` + `files-pane.css`, sixth in `_PANE_ORDER`, off by default), a third value of the fork's `fileLinkPane` setting ("pane") routing a chat file-link click into it with the session's identity for the chip, a recent-files empty state, and the viewer's quote-to-composer seed forwarded through the shell from any pane without a composer | fork branch `filespane` (stacked on `dfaf23e8`): `kernel/kernel.py` (`_files_page`, `/files`, every landing pane list, the relay's `pane` branch, the `editorSelection` forward, the shim's `NO_STALE_CAP` — a page with no kernel-pushed view opts out of the stale-banner arm), `ui/webview/files.ts`, `files-recent.ts`, `files-pane.css`, `file-view.ts` (`composerWindow`, `initFileView` `onRelay`, the editor-chunk derivation naming `files.js`), `render.ts` `openPath`, `settings.ts`, `gear.js`; tests in `tests/test_files_pane.py`, `ui/webview/files.test.ts` and the pane pins | candidate (partial) | Stacks on FORK-ONLY infrastructure: upstream removed the `viewFile` shell relay and has no `fileLinkPane` setting, so the routing half does not port as-is, and the pane itself needs upstream to want a sixth column. Portable on its own: the cross-document quote seed (`composerWindow` + the shell's `editorSelection` forward, the branch's first commit), which also fixes the feed-hosted viewer's dead quote gesture that upstream ships. Browser shell only; the VS Code extension mirror is deferred like the waiting pane's. |
| Tab groups on tags: a spawned session inherits its parent's tag memberships (a fork, a promoted comment thread, and `romp new` run inside a session, which sends `ROMP_SID` as `parent` marked `parentAuto`, and inside a comment thread resolves to the thread's session; POST /new and the WS createSession op take `parent` + `tags` and refuse an unknown explicit parent — an unknown AUTO parent creates the session untagged with `parentIgnored` echoed; /new echoes `tags` plus the positional `tagsRequested`/`tagsApplied` pair so the CLI tells a normalized name from a refused one; the two doors differ on a name that already runs: `/new` re-asserts an explicit `--in`, the picker's op warns instead, since its Tags row is a prefill), `romp new --in <tag>` / `--no-inherit`, and the chat tab strip sectioned by home tag on the desktop layout only (headers in `tagOrder`, per-browser fold state with `archived` folded by default, header drag reorders `tagOrder` through the views path the timeline's pill drag writes, one-click "Move to <tag>" rows in the Tags flyout, the picker's prefilled Tags row for SDK sessions, a "Group tabs by tag" switch in the tag-lens menu); every views-blob writer under `_views_lock` | fork branch `tabgroups`: `kernel/kernel.py` (`_inherit_tag_membership`, `_tag_ack` / `_tag_new_session`, `_resolve_parent_sid` / `_parent_from_request`, the `/new` + `createSession` params, the locked `setTimelineViews` write), `bin/romp`, `ui/webview/tab-groups.ts` + `render.ts` / `tag-menu.ts` / `styles.css`, `docs/guide.md` / `reference.md` / `read-side.md`; tests in `tests/test_timeline_views.py` (TagInheritance), `test_new_route_prefs.py` (NewRouteTags), `test_new_session_dir.py` (CreateSessionTags), `test_kernel_fork.py`, `test_comment_threads.py`, `tests/romp.bats`, `ui/webview/tab-groups.test.ts` | candidate | Upstream ships the same tag store, `_heal_timeline_views`, `romp new`, and Tags flyout, so both halves port as-is. Two offerable units: the kernel + CLI inheritance (one commit each), and the sectioned strip. No fork-only infrastructure involved. Known v1 gaps to state in any offer: inheritance is local-tags-only (a remote-homed parent tag is not copied), a tab dropped into another section does not change membership (Move to is the path), and the phone page has no strip to section. |
| The live session-move test's hermetic auth borrows the user's own `apiKeyHelper` command (from `$CLAUDE_CONFIG_DIR/settings.json`, default `~/.claude/settings.json`) instead of reading a key from a `~/.config` cache file; `_user_api_key_helper(config_dir=None)` with its own non-live tests; the skip reason names both accepted auth sources | fork branch `livetest-auth` (PR #190): `tests/test_session_move_live.py` | **offered** — their PR #946 (2026-09-06), label `tests-only`; re-verified at tip: upstream has no cache-file fallback (the row's premise), so the offer is the borrow plus a slug assertion fix | Upstream carries the cache-reading fallback via their #888 (the sessmove fold): a machine-specific path no other box has, and one that now violates the no-key-on-disk rule here. The helper borrow copies a COMMAND, never a key, and works wherever Claude Code itself is configured. Live run verified on our box 2026-09-05 (17 s). |
| Session work no longer dies with a service restart on Linux: the systemd unit relies on systemd's default `KillMode=control-group` (it sets no KillMode line; a wedged kernel or bus holding its port must still die), but every session's CLI is spawned through `bin/romp-cli-scope`, an exec-in-place `systemd-run --user --scope` wrapper (pid, parent and argv preserved, so `find_session_cli` / `find_orphan_clis` still match), and the manager starts its default tmux server in a `romp-tmux-*` scope — so tmux servers, `setsid` children and other work a session started live outside the service cgroup and survive `systemctl --user restart romp-manager`; decided once per backend by `cli_scope_supported` (on under `ROMP_SUPERVISED` on Linux, `ROMP_CLI_SCOPE=0`/`1` overrides, a one-shot probe gates it), and the manager's `tmuxStartArgv` reads the same switch | fork branch `scopes`: `bin/romp-cli-scope`, `kernel/sdk_backend.py` (`cli_scope_supported`, `cli_scope_wrapper`, `_options`, the `ROMP_CLI_REAL` export at construction), `bin/romp-manager` (`tmuxStartArgv`, `startTmuxServer`), `tests/conftest.py` (the `ROMP_CLI_SCOPE=0` floor), `docs/reference.md`, `bin/README.md`; tests in `tests/test_cli_scope.py`, `tests/romp-cli-scope.bats`, `tests/manager-tmux-scope.test.js` | **offered** — their PR #953 (2026-09-06), label `feature`; stacked on #941 and #951 (their commits show in its diff until they merge); the /api-health `cliScope` counters stayed home with the `apihealth` row | Upstream ships the same unit, the same manager `startTmuxServer` and the same SDK spawn, so it ports as-is. Linux/systemd only by construction; macOS/launchd is unchanged (the kernel answers "not Linux" before looking for systemd-run), and a terminal-run manager (`romp up`, no `ROMP_SUPERVISED`) scopes nothing — neither session CLIs nor the tmux server — unless `ROMP_CLI_SCOPE=1`. Found when a service restart on 2026-09-05 killed sessions' tmux servers and setsid jobs along with the kernel; only kernel-only restarts (`romp refresh`) had left them running. Offer WITH the reaper row below (or after it): the scopes remove the cgroup kill that had been killing what the ppid-1 reaper missed. The wrapper runs probes (no `ROMP_SID` — the SDK's per-connect `claude -v` check, which inherits the kernel's environment) unscoped, and falls back to a direct exec with one stderr line when a pre-flight scope fails; unit names carry pid and start time. Known follow-up, not built: a postal bus autostarted from a session's MCP server (`postal/postal_service.py`, `ensure()` from `mcp()`) now starts inside that session's scope. It outlives a service restart, and it keeps the scope loaded after the session ends: a scope stays loaded while any process in it lives, and nothing stops a scope, so the dead session's `romp-session-*` unit stays listed until the bus itself exits. |
| The boot orphan reaper (`find_orphan_clis`) matched nothing under systemd: it called a CLI orphaned only at ppid 1, but `systemd --user` is a child subreaper, so an orphaned SDK CLI re-parents to the user manager's pid — the reap matched nothing on Linux under the service, and only the unit's default KillMode=control-group had been killing those orphans on service restarts; now orphaned = an SDK-driven CLI holding one of our sids whose PARENT is not a live romp kernel (`_is_kernel_cmd` on the parent's `ps` text) | fork branch `scopes`: `kernel/sdk_backend.py` (`find_orphan_clis`, `_is_kernel_cmd`); tests in `tests/test_sdk_lifecycle_hardening.py` (FindOrphanClis) | **offered** — their PR #941 (2026-09-06), label `fix` | OFFERED 2026-09-06 (branch `orphan-reaper`, `509c3f11` off tip `2b9db2be`, two commits carved from the fork’s scopes branch); scaffold CI green after one flake rerun (since-closed fork #204). Upstream ships the same ppid==1 matcher and the same Linux systemd install (`romp-service`), so the same miss: a hard-killed kernel's CLI keeps writing the transcript until the next service restart. Independent of the scopes work (worth offering on its own, and it is a prerequisite for offering the scopes, which remove the cgroup-kill cover). Verified on a Linux box 2026-09-05: an orphan from the service cgroup and from a transient scope both get ppid = the `systemd --user` pid. Pure on the `ps -axwwo pid=,ppid=,command=` text (`-ww`, or procps truncates each line to an exported `$COLUMNS` and the sid, 2 KB into the argv, is cut off); the fixtures that encoded ppid 1 gained the kernel's own line. |
| The manager's unit tests (`tests/manager-*.test.js`: drain, registry, restart, tmux scope) ran nowhere in CI — pytest, `bats tests/*.bats` and the extension's `npm test` covered everything else | fork branch `scopes`: `.github/workflows/ci.yml` (a `node --test tests/manager-*.test.js` step in the vscode-extension job, which already sets up Node) | ✅ **resolved upstream** — their T224 (2026-09-02) | CLOSED 2026-09-06 without an offer: upstream’s ci.yml already runs `node --test tests/manager-*.test.js` (their T224 follow-up to our #868); the gap was fork-side only. Upstream's `ci.yml` has the same three suites and the same gap for its three manager suites. One step, no new pin: the job's Node 22 runs them from the repo root. |
| The Outline pane takes feed deltas: its page announces the feed pane's caps (`_shim("fleet", v, caps=FEED_DELTA_CAP + "," + READY_GATE_CAP)`), so an `app=fleet` socket streams `{type:"feedDelta"}` after its first full frame instead of a multi-megabyte full frame per push. The page loads `federation.js` before `fleet.js`, and federation applies each delta and re-emits a whole `feed` frame, which is all `fleet.ts` reads. Two pane changes ride along: `fleet.ts` keeps the live clock the feed and Waiting on you panes keep (`feed-age.ts` `liveNow`: the frame's `now` plus the local time since its `nowAt` arrival) and re-renders every 15 s while the pane is visible, since a quiet board sends a delta client nothing and its ages, the current goal's elapsed time and the recency cutoff had frozen as of the last change; and `fleet.ts` and `waiting.ts` carry the feed pane's guard for a delta that reaches them unapplied (loud, then a `needFullFeed` re-base) | fork branch `outlinedelta`: `kernel/kernel.py` (`_fleet_page`, the delta-path and `_shim` comments), `tests/test_feed_delta.py` (`OutlineDeltaStream`: a real `app=fleet` socket hears deltas after its first full frame; the shim-cap pin), `tests/test_kernel.py` (`test_fleet_page_served`), `tests/test_kernel_fleet_ledgers.py` (the `_shim` caps pin), `ui/webview/feed-delta.test.ts` (the cap pin; two harness tests on what the pane sees), `ui/webview/fleet.ts` and `waiting.ts`, `ui/webview/fleet-live-clock.test.ts` (fleet.ts run for real under mock timers against a small DOM stand-in: the ages and the cutoff move between frames; a frame without `nowAt` anchors at its arrival; a hidden pane skips the refresh and catches up once when shown; a raw delta is loud), `ui/webview/fleet.test.ts`, `ui/webview/waiting.test.ts`, `docs/read-side.md` | **offered** — their PR #952 (2026-09-06), label `fix` (the fork's own delta WIRE PROTOCOL is moot: upstream #914 + the #928 fold shipped one on 2026-09-04; only the remainder rode) | Follows the feed-deltas row above, which left the Outline pane on full frames. Verified on the live kernel 2026-09-05: seven `ws drop` lines for `app=fleet` sockets in one morning, each about 12.7 MB queued behind a 6.3 MB feed frame, every frame a multi-megabyte `json.dumps` on the GIL. A one-line shim change, the pane's clock and guard, plus tests; re-applies trivially on any upstream fold. Reviewed adversarially 2026-09-05 (two refuters): one medium (the pane had no timer, so on the delta path it froze on a quiet board) and five minor (a silently dropped raw delta, a barrier the test over-described, a 2 s wall-clock dependence in the test, two stale comments, this row's wording), all fixed on the branch. A second round confirmed four low: the refresh rebuilt the list while the pane was hidden (a background tab, or the shell's display:none iframe), the unstamped-frame anchor was untested, and this row's file list omitted the ledgers pin, all fixed on the branch; the fourth is pre-existing and left as a follow-up: `federation.ts` `mergeHostFeeds` concatenates remote hosts' ledgers without rebasing their times onto the local clock (`hostOffsets` / `rebaseHostTimes` serve only the timeline and bars merges), so a remote row's age is off by that host's clock skew in every consumer of the merged frame. |
| `romp keyswap <name>` is refused on this fork. Upstream's swap (their `b4ca13e7` / `d177a8a8` / `60051794`, 2026-09-04) rewrites the `ANTHROPIC_API_KEY=` line of `service.env` from a sibling `service.env.<name>`, which presumes API keys in files; this fork does not write API keys to files — keys reach the sessions through Claude Code's `apiKeyHelper` or the manager's environment. The named form exits 2 with one fixed message, reads and writes nothing, and has no flag that lets it through. The bare report and `--cycle` / `--cycle-all` stay, and the cycle also reconnects a session the kernel handed no key but whose CLI reported one at init (`cycle_key` → `"helper"`): upstream read such a session as login-billed and skipped it, which made `--cycle-all` a no-op on a box whose every session bills through the helper. At backend construction a credential-shaped line in the env file (`ANTHROPIC_API_KEY`, `*_API_KEY`, `*_TOKEN`, non-empty) under a declared `ROMP_EXPECTED_AUTH` is logged once as a problem, naming the file and the variable name only — the billing consequence for `ANTHROPIC_API_KEY` (the variable the launch injects), a plain contradiction line for any other credential-shaped name. The cycle report's re-run hint names only the rows skipped for in-flight work and says a helper-billed session reconnects again on every run that names it (there is no fingerprint to converge on), and a second positional argument is counted, never echoed (round 2, 2026-09-05) | `cli/keyswap.py` (`REFUSAL`, `main`, `_candidates`, `_explain`), `bin/romp` (help row), `kernel/sdk_backend.py` (`cycle_key`'s helper arm, `_credential_names_in_env_file`, `_warn_credential_lines_in_env_file`), `docs/reference.md` (the keyswap section); tests in `tests/test_keyswap_refusal.py`, the adapted `tests/test_keyswap.py` and `tests/romp.bats` | divergence | Fold upfold0905 (2026-09-05). Not for offering as-is: the refusal encodes the fork's policy. Two pieces upstream might take on their own: the helper arm of the cycle (a session on an `apiKeyHelper` cannot pick a rotated key up without a new process, and upstream's cycle never gives it one), and the boot warning, which fires only under a declaration and so stays silent on upstream's ordinary file-key box. `kernel/keysource.py` is carried unchanged (its `write_key` is no longer called by anything here); the kernel's live read of a `service.env` key line stays for installations that keep one there. The runtime auth strings now point at the manager's environment first and name `service.env` as the file-key installation's case. |
| The `setAutoNudge` WS branch re-ticks `_auto_nudge_tick(..., run_dead_wait=False)` on a real apply, so turning the nudge on acts now instead of waiting for the pusher's next tick | `kernel/kernel.py` (the `setAutoNudge` arm of `_dispatch_ws`); fork `5be9181f`, kept through `643a16cb` and upfold0905; pinned by `tests/test_setting_gesture_order.py` (WiringPins) and `tests/test_dead_wait_block.py` | **offered** — their PR #943 (2026-09-06), label `fix` | OFFERED 2026-09-06 (branch `autonudge-retick`, `ba56632f` off tip `2b9db2be`); scaffold CI green (since-closed fork #206). Fork-only, never offered: v0.15.0's branch was a bare `_set_auto_nudge(...)`, and upstream's tip only tells a stale gesture — its turn-on tick sits under `setCompactSuggest` (T208). Kept at the fold (2026-09-05); `_compact_suggest_tick`'s comment counts three concurrent entry points for that reason. A small offer: one call plus the two pins. |
| `romp keyswap <name>` is refused on this fork in FILE mode only. Upstream's swap (their `b4ca13e7` / `d177a8a8` / `60051794`, 2026-09-04) rewrites the `ANTHROPIC_API_KEY=` line of `service.env` from a sibling `service.env.<name>`, which presumes API keys in files; this fork does not write API keys to files, so with no `ROMP_CREDENTIAL_COMMAND` the named form exits 2 with one fixed message, reads and writes nothing, and has no flag that lets it through. In command mode the same argument writes the selector file (a name, never a key): see the key-free row below. The boot warning for a credential-shaped line in the env file under a declared `ROMP_EXPECTED_AUTH` (`_warn_credential_lines_in_env_file`) stays on this row and speaks in file mode; in command mode the key-free row's `key_source_verdict` covers the same file (the line is ignored there, the command wins) and this warning is skipped, so one line is said about that file, never two that disagree | `cli/keyswap.py` (`REFUSAL`, the file-mode branch of `main`, `_candidates`, and in `_cycle` the second and third lines of the no-kernel and no-route blocks, which upstream's end with `The file is already swapped`), `bin/romp` (help row), `docs/reference.md` (the keyswap section's file-mode paragraph); tests in `tests/test_keyswap_refusal.py` (`NamedSwapRefused`), the adapted `tests/test_keyswap.py` and `tests/romp.bats` | divergence | Fold upfold0905 (2026-09-05); narrowed to file mode 2026-09-05 when the key-free mode landed. Not for offering: the refusal encodes the fork's policy. `kernel/keysource.py` is carried unchanged (its `write_key` is no longer called by anything here); the kernel's live read of a `service.env` key line stays for installations that keep one there. |
| Key-free installation mode: a second key source beside upstream's `service.env` file. With `ROMP_CREDENTIAL_COMMAND` set (non-secret; in `service.env` or a systemd drop-in: a line added to the unit or the plist by hand does not survive `romp-service install`, which rewrites both) the kernel runs that command with a one-token selector file as `$1` and merges the `NAME=VALUE` set it prints into every session CLI's launch environment (the key only where the session's Billing pick is the key), every judge call's (minus the key, re-added for key-billed calls) and the catalog fetch's `ANTHROPIC_LP_API_KEY`, never into its own environment or a file. Event-cached (a refresh, a cycle or an authentication failure invalidates; a failed run keeps the previous set, loudly, and never refuses a launch), coalesced, value-free everywhere but one accessor; failure reasons carry counts and exit codes only. Sessions are stamped at connect with the fingerprint they launched under (the set's key, or the configured `apiKeyHelper`'s output hashed inside the module), and `cycle_key` converges on it, which replaces the fold's always-reconnect helper outcome. The mode is pinned at backend construction (`envsource.pin_mode`): a `service.env` edit reaches the kernel at its next start (`romp refresh`), never a running one. The CLI's own auth and endpoint names (`ANTHROPIC_AUTH_TOKEN`, `CLAUDE_CODE_OAUTH_TOKEN`, `ANTHROPIC_BASE_URL`, `ANTHROPIC_CUSTOM_HEADERS`) are dropped like `ROMP_*`; an authentication failure invalidates once per credential; a failed run is re-run by the next caller; the selector is rendered by name only when `ROMP_CREDENTIAL_NAMES` declares it (else `(undeclared, N chars)`); a set with no key and no `apiKeyHelper` is the login, a state not an error. A pure `key_source_verdict` at boot rings on credential-shaped lines in the env file, unit, drop-ins or plist, a startup key, a shell-wrapped ExecStart, `login` declared while the command prints a key, `key` declared with nothing to inject and no helper, dropped names, a bad timeout; `/api-health` gains `keySource` (with `lastRun.stale`, `failures`, `lastOkAt`), `/keycycle` gains `keySource`, `keyKind`, `keyErr`, `setFp`, `selector`, `launched`, `refreshed` and per-row `from`. A failed credential-command run is one problem line per failure episode on every path that can run the command — the connect, the boot, the refresh, the judges' and the catalog's `credential_set`, a judge's `work_api_key`, the status report, `/api-health`, the key cycle — through one noter (`_noted_take`), the boot included: the boot verdict's lines are the boot's one report of the first run and it hands the record to the noter as already said, a boot on a failing command runs the command once (the verdict reads whether a key is injected from the record, not from a second live read), and every record carries the ordinal of the run that produced it, which the noter keeps as a watermark so an older record is never noted after a newer one; a backend's own readers (the status report, `/api-health`, the key cycle) note on that backend, and the module-level noter is a weak reference, so a test process's dropped backend receives nothing. Before, only the connect, boot and refresh paths said anything, a failure first met on a judge call was silent until the next connect, and the boot said a failure twice. The boot verdict on an explicit environ is pure on its inputs (its mode from the environ's own line, else the `service_env_text` handed in), never the env file this process names. Surfaces: `romp keyswap` reports both sides by fingerprint with MISMATCH lines, `<name>` writes the selector (declared names only, undone when nothing moved), `--refresh`, a converging `--cycle`; `romp-service status` prints the mode, the ExecStart shape and credential-shaped names; docs split into "API keys on disk: the file mode" and "Installing without keys on disk" | Branch `keyfree`: `kernel/envsource.py` (new), `kernel/sdk_backend.py` (`key_source_mode`, `credential_set`, `credential_invalidate`, `key_source_verdict`, `_unit_texts`, `_exec_start_shell`, `SdkBackend.credential_fingerprint` / `role_fingerprint` / `key_source_status` / `refresh_key_source`, `cycle_key`, `_options` stamps, `api_health_snapshot`), `kernel/judge.py` (`_ENV_SET_FN`, `_ENV_INVALIDATE_FN`), `kernel/kernel.py` (`/keycycle`, `_models_api_credential`), `cli/keyswap.py`, `bin/romp`, `bin/romp-service`, `docs/reference.md`, `docs/install.md`, `docs/judges.md`, the three READMEs; tests `tests/test_envsource.py`, `tests/test_key_source_verdict.py`, `tests/test_env_value_redaction.py`, extended `test_keyswap.py`, `test_keyswap_refusal.py`, `test_session_auth.py`, `test_judge_auth_billing.py`, `romp.bats`, `romp-service.bats`, `romp-node-launch.bats`; `tests/conftest.py` pops `ROMP_CREDENTIAL_COMMAND` / `NAMES` / `TIMEOUT_S`, floors `ROMP_CREDENTIAL_SELECTOR_FILE` to a path that does not exist and `ROMP_SYSTEMD_DIR` / `ROMP_LAUNCHD_DIR` / `CLAUDE_CONFIG_DIR` to empty dirs, and redacts process-environment values out of failed test reports | candidate | Additive in the kernel: with the command unset AND no `ROMP_EXPECTED_AUTH` declared, upstream's launch paths, boot log and wire shapes are byte-identical, except that the `/keycycle` answer gains `keySource`, `keyKind`, `keyErr`, `setFp`, `selector`, `launched` and `refreshed`, and each cycle row gains `from`. The file-mode CLI is not byte-identical. Measured against the fork's main (`origin/main`, which already carries the row above's refusal, `candidates  none` line and `rotate:` block), the branch changes these lines of it, listed so an offer can name them: (1) `parse_args` takes `--refresh` (main answers `unknown option --refresh`, exit 2); with it the read posts `{"sessions": [], "refresh": true}` and the kernel line gains `(re-read now: was sha256:…)`, `(re-read now: unchanged)` or `(the refresh failed — …)`; without it the POST body and the line are main's; (2) the report and the cycle read the kernel's `keySource`: when it answers `command`, the kernel line reads `kernel      reads sha256:… in COMMAND mode` and the mode `MISMATCH` block follows, and the cycle adds `cycle       NOT DONE — fix the mismatch above first, then cycle.` and stops (upstream's kernel never sends the field, so against it these lines never print); (3) a `cycling` row carries ` (from sha256:…)` when the kernel's row has a non-empty `from`; (4) the hint after rows skipped for in-flight work is one line, `re-run --cycle <names> once quiet; sessions already on this key read "current"`, where main prints two (the second on helper-billed sessions reconnecting on every run that names them) and upstream one that names no session (`re-run the same --cycle once those are quiet; sessions already moved read "current"`); (5) `_explain` has no `helper` entry, so a `helper` row prints the bare word, as upstream's does (main added a two-sentence explanation; the branch's kernel no longer sends that status); (6) when a file-mode cycle skips a `login` row, two hint lines say a session billed through the `apiKeyHelper` reads as the login there and is skipped, and where it cycles; (7) the bare report's closing `rotate:` block is five lines (helper-billed sessions are skipped by that cycle and reach a new key in command mode or through `romp refresh --quiet`) where main's is three and upstream's is `swap with:  romp keyswap <name> [--cycle <session,…> | --cycle-all]`; (8) the fingerprint `MISMATCH` hint lists its three causes in the other order and explains the other-file one in full (the kernel and this shell each resolve `ROMP_SERVICE_ENV_FILE` in their own environment, or its alias `ROMP_SERVICE_ENV`, which the hint names when it produced this shell's path and which `bin/romp-service` now resolves the same way, writing the primary name into the unit or the plist; the places the kernel's can come from, this shell's path, and the remedy in each direction: found, run with the same value or change it there and restart; not found, the kernel reads the default path, so unset it in this shell or install from this shell), in ten lines shared word for word with the two mode `MISMATCH`es, then the nine-line restart block the three share too (the restart per platform, `daemon-reload` before it for a unit or drop-in line, `romp-service install` in place of `launchctl kickstart -k` for a plist line, the Linux install's missing restart, and the cost of either install: it rewrites the unit or the plist, so a hand-added line is gone while a drop-in stays), every prose line at or under 100 columns (a `service.env` path too long for its sentence goes whole on a line of its own); the command-mode fingerprint `MISMATCH`, which names the install as the reload a plist line takes, states the same cost in that sentence; main's and upstream's list the same three causes in four lines, give the other-file one in one direction (the service installed with a path the kernel's environment does not carry) with `romp service install` as its only remedy (upstream's command name; on Linux an install restarts nothing, and the kernel's path may come from a drop-in, a profile or the `romp up` shell, which no install touches); (9) the `/keycycle` wait is 10 s plus twice `ROMP_CREDENTIAL_TIMEOUT_S` (40 s with the default) where main's and upstream's is 30 s; (10) the `REFUSAL` is nine lines where main's is six: it says the manager's key was rotated, names the bare `romp keyswap --cycle-all` without `(or: romp refresh --quiet)`, and adds four lines on helper-billed sessions (skipped by the file-mode cycle; `romp refresh --quiet`, or the cycle in command mode). Byte-identical to upstream and to main: the bare report's four failure blocks (an unusable port, no kernel, no `/keycycle` route, a refused read), the cycle's unusable-port line, both `cycle       FAILED` lines, the two-line `cycle       NOT DONE — the kernel is not on this file's key` block, `cycle       no sessions matched`, and the row format for every row but a `cycling` one with `from`. Not upstream's: the cycle's no-kernel and no-route blocks keep main's second and third lines (upstream's end `The file is already swapped`, never true here; the row above names them). Under a declared `ROMP_EXPECTED_AUTH` the fork's own lines speak in file mode as before: the env-file warning of the row above, plus one line from the verdict for a credential-shaped `Environment=` line in the unit, drop-in or plist. Of the file-mode lines above, (6), (7) and (10) are fork policy (a rotated helper key reaches such sessions through `romp refresh --quiet`, or the cycle in command mode) and stay out of an offer with the refusal; `romp-node-launch` stripping one layer of quotes from a `service.env` value as systemd does rides along as a fix upstream would want. Keep the file-mode REFUSAL out of the offer (the row above). The fork's own box switches to this mode after part B lands; hermetic tests only until then. |
| `_send_or_park` returns three outcomes — `"parked"`, else the backend send's own result (truthy sent; a tmux send returns its nonce), falsy refused — and `_tmux_send`'s inner `paste()` returns False on every abort and True after the Enter (`go()` assigns `delivered = paste()`, fires `on_refused` / `on_delivered` in a `finally`, and returns None). Upstream's `_send_or_park` is True/False (`ae98b1dc`, POST /send's `queued = bool(...)`) | `kernel/kernel.py` (`_send_or_park`, `_tmux_send`; POST /send computes `queued = (_send_or_park(...) == "parked")`); fork `ac8340da`; pinned by `tests/test_kernel_parked_ops_liveness.py` (`== "parked"` / `!= "parked"`) and `tests/test_user_todos.py` (the delivery stamp keyed on the outcome; `_tmux_send`'s `on_refused` / `on_delivered`) | divergence | The user-todo delivery stamp (`on_refused` / `on_delivered`, the slice-1 row) keys on sent-vs-refused-vs-parked, which upstream's bool cannot express. Kept at upfold0905 (2026-09-05); upstream's one consumer repointed to `== "parked"` and its one test adjusted (`assertFalse` → `assertNotEqual(..., "parked")`). Every fold will touch this until one side converges; an offer of the three-outcome contract would ride the user-todos slices. |
| Two WebSocket delta protocols coexist after upfold0905: the fork's feed deltas (a client announces `?caps=feedDelta`; `_send_feed` sends `{type:"feedDelta"}` frames behind the `READY_GATE_CAP` hold) and upstream's per-slot view deltas (`?delta=1&iid=…`; `_send_slot` / `_send_slot_delta` under the client's slot lock; `{type:"delta"}` frames; `needSlot`). The kernel-served shim announces both; the pusher sends the feed slot with the fork's protocol when the client announced `feedDelta`, else upstream's; timeline bars always take upstream's. At `ready`, `_send_feed_now` serves a `?delta=1` client that did not announce `feedDelta` (the Outline page) through `_send_slot`, so the served frame is the slot's base and the connect push that follows sends a delta rather than a second full frame; the feed's per-client state (`efeed`, the dedup slot, `dstate["feed"]`) is read and written under the client's slot lock on both paths, and `_client_reset_feed_base` is the one forgetter (round 2, 2026-09-05) | `kernel/kernel.py` (the pusher's feed send site, `_shim`'s connect URL and reassembler, the WS `ready` handler); `ui/webview/*` (both receivers) | follow-up | Convergence pending: one protocol should carry the feed. The fork's brings the ready-gate hold and the loud-drop accounting (`_note_ws_drop`, `_WS_DROPS`); upstream's brings the per-page instance id supersession and the slot lock. Decide which survives and port the other's properties onto it. The one full frame per `ready` for a `?delta=1`-only client holds while the connect push's delta stays under `_DELTA_MAX_FRACTION` of the full frame; a larger delta pops the slot's base and re-sends a keyed full frame (the size fallback, unchanged). Until then a client that announces neither gets whole frames, and nothing is lost. |
| Three fork-only behaviours kept through upfold0905 where upstream's evolved code sits beside them: fast judging (`_judge_fast`, `_FAST_MODELS`, the `--settings` fast opt-in in `_judge_cmd`, the gear's `setJudgeFast` toggle, `/version.judgeFast`); the user-todo delivery seam (`SdkBackend.send(sid, text, user_todo=None)`, `enqueue(todo=)`, the dict-aware `_queue_texts` / `_queue_wire`, `_mark_dropped_echoes` carrying the todo, `todo_lost=` on the backend); and parent/tags carried into upstream's new Codex arm in both doors — `_create_codex_session(parent=, tags=)` applies them through `_tag_ack` before its push and returns `(sid, echo)` like the SDK create; POST /new's Codex arm echoes `tags` / `tagsRequested` / `tagsApplied` / `parentIgnored` and refuses `env` loudly (the backend has no `set_env`); the WS op's Codex arm warns a `tagError`; the tmux refusal stays after it — so a create asked into a tag group is never spawned untagged (round 2, 2026-09-05) | `kernel/judge.py`, `kernel/sdk_backend.py`, `kernel/kernel.py` (`_dispatch_ws`'s create arms); fork `4c3b62bf` (fast judging), `d554c2f9` (the seam), the `tabgroups` branch | divergence | Fast judging was never offered (a per-install cost toggle). The seam rides the user-todos slices (candidate rows above). The Codex tagging follows from the tab-groups row and would ride that offer. None of the three changes upstream's behaviour while the fork-only feature is off. |
| The viewer's GitHub link says why when there is none: the `fileGitLink` reply carries `reason` beside `url` ("not in a git repository" / "not committed (untracked file)" / "not committed (no commits yet)" / "no origin remote" / "the origin remote is not on GitHub"), and a branch absent from origin keeps its url with a "branch X is not on origin" note. The local tracking ref answers first; only its absence pays one `ls-remote` (3 s timeout, no terminal prompt, in its own process group so the ssh it spawned dies with it), and that answer is memoized per (repo, branch) until the tracking ref changes; "could not check" when origin does not answer, asked again next time. The check trusts the local tracking ref: a branch deleted on GitHub reads as present until a `fetch --prune`. The branch name comes from `branch --show-current`, so a tag of the same name does not bend the ref to `heads/<branch>`. The viewer renders the action as one unit: no url is a real disabled button with the reason as a caption beside it and in its tooltip; a link with a note stays a dashed anchor with the note as its caption | branch `ghreason`: `kernel/kernel.py` (`_file_github_link`, `_origin_has_branch` and its `_ORIGIN_MEMO` / `_ORIGIN_INFLIGHT`, `_git_net_out`, the `GH_*` phrases, the `fileGitLink` op; `_file_github_url` kept as the url-only wrapper), `ui/webview/file-view.ts` (`githubLinkAction`), both sheets, `docs/guide.md`; tests `tests/test_file_github.py` (a stand-in ssh serves origin from a local bare repo; the kernel's git environment pinned hermetic), `ui/webview/github-link.test.ts` (the three states run against a DOM stand-in), `fileview-parity.test.ts` | **offered** — their PR #947 (2026-09-06), label `feature` | Upstream ships the same silent verdict (their #543 plus the registry follow-on `fbc0a1b5`): an empty url hides the anchor, so a user cannot tell an uncommitted file from a broken feature; that confusion happened here on 2026-09-05. Wire contract is additive: `reason` rides beside `url`; an older client ignores it, and an older kernel's reasonless reply renders disabled with a caption saying the kernel predates link reasons. Reviewed adversarially 2026-09-05 (two refuters): two medium (the origin-backed tests ran the kernel's git under the developer's global config, so an insteadOf rewrite reached real github.com; the reason was reachable only by mouse hover) and nine low (`heads/<branch>` from a same-named tag, the ssh orphaned by the timeout, no memo across opens, no phrase for a missing origin, the stale-tracking-ref limit undocumented, aria on an hrefless anchor, kernel phrase literals pinned in the UI test, the guide's wording, the reasonless fallback text), all fixed on the branch. |
| bats isolation of the tmux socket directory: every suite that starts a real `bin/romp-manager`, or whose subject shells out to tmux, loads `tests/tmux-private.bash` (`TMUX_TMPDIR` under the test dir, created first because tmux 3.4 silently falls back to the default socket directory when it names a missing one; the private server killed in teardown through the real tmux, never a PATH mock). `romp-manager-ensure.bats` keeps its recording fake tmux on PATH for the whole file, and two tests pin the isolation: the `ensure`-spawned manager's tmux is handed the private directory and it already exists; with the real tmux, the server socket lands inside the test dir and the teardown kill reaches it. | branch `batsisolate` (`tests/tmux-private.bash`, `tests/tmux-private.bats`, `tests/romp-manager-ensure.bats`, `tests/romp-manager-origin.bats`, `tests/romp.bats`, `tests/tmux-status-hook.bats`, `tests/README.md`) | **offered** — their PR #951 (2026-09-06), label `tests-only` | Upstream's suite has the same hole: `romp-manager-ensure.bats` and `romp-manager-origin.bats` start a real manager with no tmux mock, so its startup `tmux start-server` runs on the developer's default socket. On 2026-09-06 a sweep did that ninety seconds after a stray kill-server had removed the default server, and for the rest of the day the machine's tmux server carried the sweep's environment inside the service's cgroup. Test-only; no product code. |
| Kernel performance counters and `romp perf`: an always-on collector (`_PerfStats`, one lock, dict increments on the hot paths, no formatting) counts pusher cycles and wakes (event vs backstop), keeps a 256-entry ring of cycle durations for p50/p90 at read time, splits cycle time into stages (tick jobs, `_push_all`, and inside `_push` the chat block, the feed, the timeline, the serialize-and-send loop), counts chat/feed/timeline builds served from cache vs rebuilt, bytes sent per slot as full/delta/deduped, `load_goals`/`save_goals` calls and writes (`judge.goal_io_stats`), producer passes with durations, HTTP requests and ms per method and normalized path (static files and remote host names collapsed; capped at 64 keys, the rest under "other"; WebSocket upgrades counted at arrival, not timed), the pusher thread's and the judge threads' and pool workers' CPU beside the process total, and rss/threads. `GET /perf` serves the snapshot; `POST /perf {"log": bool}` flips the `romp-perf` stderr log at runtime (`_set_perf_log`), which until now needed `ROMP_PERF` at process start and so a restart. `romp perf` prints rates from two snapshots `--interval` seconds apart; `--json` one snapshot; `log on\|off` the toggle | branch `romp-perf`: `kernel/kernel.py` (`_PerfStats`, `_PERF_STATS`, `_CountedEvent` for `_pusher_wake`, `_perf_http_key` + `_perf_http_timed` on every `do_*`, the stage timers in `_push`/`_pusher_cycle`/`_pusher_cycle_jobs`/`_producer`, the two routes), `kernel/judge.py` (`_GOAL_IO`, `goal_io_stats`, `_TimedPool` / `judge_worker_cpu_ms`), `bin/romp` (`perf`), `docs/reference.md`; tests `tests/test_perf_stats.py`, `tests/romp-perf.bats` | candidate | Upstream has the same `ROMP_PERF` import-time switch and no runtime numbers; its pusher CPU fixes (2026-08-10, three rounds) were each verified with a hand-run profiler. Additive: no existing route or payload changes; the per-push cost is a few lock acquisitions. Not yet offered. |
| Browser-side performance telemetry and `romp perf client`: the feed, Outline, Waiting on you and chat bundles wrap their window `message` handler through `ui/webview/perf-telemetry.ts`, federation times its merge and dispatch as `fed:<type>` nested outside (each level records its own time, so the figures add up), and the timeline's listener is wrapped on both hosts (the kernel page's inline boot through the `window.__rompPerf` that federation.js publishes before it); per frame type a count, summed and max handler time, exact counts over 16.7 ms and at or over 100 ms, and a 14-bucket log2 histogram additive across minutes; the main-thread-free gap two animation frames after a handler; the browser's `long-animation-frame` reports (`longtask` fallback) keyed `<file>:<function>@<position>` (`page:` for inline page scripts). ONE `clientDiag` row per pane per minute (surface `perf`, what `minute`) only when something happened, plus at most five immediate `slowframe` rows a minute (the rest counted). The kernel rotates `client-diag.jsonl` at 8 MB to one `.1` file, under one lock across the handler threads. `romp perf client [--minutes <n>] [--json]` folds both files per dashboard and pane (handler ms/min by type with window p50/p90/p99, the worst minute, attribution with invokers, the slowest frames) with no kernel round trip and is loud when there are none. Rows carry numbers and code identifiers only (ids, element sources and URLs are stripped from invokers). | `ui/webview/perf-telemetry.ts`, the handler wrap in `feed.ts` / `fleet.ts` / `waiting.ts` / `render.ts`, `federation.ts` (`start()`, `inbound()`), `timeline-main.ts`, `kernel/kernel.py` (`_client_diag_append`, `_TIMELINE_BOOT`), `vscode-extension/esbuild.js` (webview `minifyIdentifiers: false`), `bin/romp` (`perf client`), `docs/reference.md`; tests `ui/webview/perf-telemetry.test.ts`, `tests/test_client_diag_rotation.py`, `tests/romp-perf-client.bats` | candidate | Two changes beyond the bundles, both stated in the docs: the kernel rotates `client-diag.jsonl` past 8 MB (the rows are about 1 KB per pane per active minute and nothing pruned the file), and the webview RELEASE build keeps identifiers (whitespace and syntax still minified; measured +27% raw and +13.5% gzipped over all webview bundles, `feed.js` 119 KB to 134 KB gzipped) because the browser's long-frame attribution names callbacks by their compile-time name, so a mangled build reported `feed.js:Qe` and a different letter after every rebuild. The browser side goes live on a reload through the per-request dist token; the rotation takes effect at the next kernel restart. A page without `performance.now` gets no telemetry and an unwrapped handler, so the node stand-in tests are untouched. Pairs with the kernel counters row above: that one says what the kernel spent, this one what the browser spent on the frames it received. Not yet offered. |
| File editor: a future COMMENT and TRACK-CHANGES layer on the dashboard file editor (raw-mode edits shipped in their #542) | not built yet | candidate — **major-feature (tier 3)**, discuss with the maintainer before building | Named by the user 2026-09-06 as the second example major feature to offer in future (with user todos). Nothing exists yet on either tree; this row reserves the discussion. |
| The SDK live tail has a lock, and a handler failure on one streamed SDK message no longer ends the session's CLI. **The lock:** `SdkBackend._live` (sid → {message uuid → atom}) is shared by the kernel thread (send, recall, dismiss, the pusher's `live_atoms` / `prune_live`) and each session's loop thread (`_forward`, the settle's `retire_live_work`, `_mark_dropped_echoes` at spawn and reconnect) and had no lock: `KeyError: '<message uuid>'` from a sweep reading `d[k]` off a stale key list closed a session's CLI mid-work (killing a Workflow run and a background agent, 2026-09-06); `_persist_echoes` raised `RuntimeError: dictionary changed size during iteration` out of the pusher's whole chat build (every client missed the cycle, nothing reached the error center); `_mark_dropped_echoes` raised it out of the session thread at reconnect, outside the connect's try (the thread died with no reconnect); and the two-step `if not d: _live.pop(sid)` orphaned an atom stashed between the steps (an input echo gone from the chat and from `reg['echoes']`). One `RLock` (`_live_lock`) now guards it: every stash (`_stash_live`, `_forward`), every pop, the sid-level pop and every iterating read take it; sweeps walk a snapshot taken under it; the emptiness check and the sid-level pop are one step; it is never held across I/O or a callback (`retire_live_work` snapshots, salvages outside, pops from the same dict object back under it). Cost on the pusher path: +0.5 µs per `live_atoms`+`prune_live` on a 100-atom tail (26.6 → 27.0). **The containment:** the receive loop (`SdkSession._drain`) runs each message through `_handle_stream_message`, which logs the exception type, the message type and subtype, the exception's text (uuid-shaped ids shortened, 160-char clip), what that message lost, by its shape (a transcript record is kept — a compact boundary is one, a control request's command-output line is not; a ResultMessage's turn still settled, or did not, read from the settle's own marker rather than assumed; a stream-only frame's content is gone) and a frame chain bounded from the OUTER end (at most the innermost 8 frames and 600 chars, dropping outer frames first, so the failing frame is always kept; a RecursionError's ran to 18 KB) and continues; the reporter is guarded (a failing `__str__` or log callback cannot end the stream); the ResultMessage branch is ONE try whose `finally` is the whole settle (the state resets, 'waiting', the turn-end count, the queue wake, the poke, the rename ping, the deferred reconnect), so a raise anywhere in the result's bookkeeping — the spend fold on a NaN usage field, a failing spend write — still closes the turn, and the settle's own failure reports are guarded, so a raising log callback cannot replace the bookkeeping's exception in the containment's report; repeats of one (session, exception type, failing frame as file/line/function) count on one error-center entry with one feed-cache bust (`_log`'s `key=`) and re-enter with the full line once the ring has evicted the entry; a fault of the stream itself still ends the loop, logged with the failing task and its frame chain. **The reconnect hold** (pre-existing on main, found by the round-3 review of the settle): `inputs()` holds the queue while a reconnect is armed (`_reconnect`). The settle woke the feeder and then armed the deferred reconnect, both wakeups FIFO on the loop, so the feeder fed a message gate-held behind an interrupted turn to the client about to be torn down; the teardown stranded it in flight and `_reconcile_stranded`, on a resumable conversation, flagged it 'never delivered', so the user had to resend. Now the new client's feeder takes the head; the same hold covers the idle arm in `_do_request_reconnect` racing a send. It keys on the armed flag only, so a pending `/effort` change does not stop mid-turn forwards | `kernel/sdk_backend.py` (`_live_lock`, `_stash_live`, the sweeps and echo walks, `_note_live_tail_race`, `_drain`, `_handle_stream_message`, `_note_message_failure`, `_failure_consequence`, `_compact_tb` / `_mask_ids`, `_log`, the `_reconnect` hold in `_amain`'s `inputs()`); `tests/test_sdk_stream_survives_handler_errors.py` (real-thread hammers, each run until both threads meet an iteration floor so the overlap is by construction; the lock-free figures were measured with the round-1 fixed-count hammers — `prune_live` raised on 677 of 1500 calls and 11–17 of 20000 stashes were orphaned; the test docstring quotes the reviewers' own probes' figures — and under the lock the current hammers see zero of each; the settle under a raising log callback; the reconnect hold run through the real `_amain` against a stand-in SDK module); fork PR #223 | candidate | Upstream ships the same loop, the same lock-free tail and the same walks (verified against `upstream/main` at `2b9db2be`: the `drain` closure in `_amain` calls `_on_message` bare; `prune_live` / `retire_live_work` / `_evict_live_overflow` walk `list(d.keys())` then `d[k]`; `_persist_echoes` and `_mark_dropped_echoes` iterate `d.values()` unsnapshotted; the sid-level pop is two steps; the settle wakes the feeder before it arms the deferred reconnect and `inputs()` has no hold for an armed one). Offer as one PR: the lock, the containment, the reconnect hold and their tests. NOT done, to disclose in the offer: six `SystemMessage` subtypes a newer CLI streams still reach the unhandled catch-all (`hook_started`, `hook_response`, `thinking_tokens`, `background_tasks_changed`, `vcs_state_changed`, `permission_denied` — logged once each, never handled; the last two carry data the kernel could use); `tests/test_kernel_deferral_sweep.py` still mints goals under the shared placeholder sid. |
| Test fixtures leaked under /tmp: the suite’s `mkdtemp` state roots and per-test scratch were never removed (about 1.9 million directories accumulated on one dev box from repeated sweeps; a shell glob over them OOM-killed the kernel) | fork PR #201 (`romp-tmpclean`, merged 2026-09-06); upstream’s `tests/conftest.py` leaks the same `mkdtemp` | **offered** — their PR #944 (2026-09-06), label `tests-only` | OFFERED 2026-09-06 (branch `tmpclean`, `e95ce457` off tip `2b9db2be`): cleanup ported onto upstream’s conftest and tests/__init__.py; proven by two suite runs under a private TMPDIR with no fixture growth; scaffold CI green (since-closed fork #207). |
| Temp-directory hygiene, the child-process half, plus git hermeticity. `tests/__init__.py`'s mkdtemp hook (fork PR #201) records and removes what the test process itself mints, but not what its children make — kernels, git, a shell's `mktemp -d` — nor mkstemp files or `os.mkdir` paths (~5,600 such entries per full run before). `tests/conftest.py` redirects the process temp dir (`tempfile.tempdir` + `TMPDIR`, inherited by every child) into one private `romp-tests-*` root at import time and removes it whole in `pytest_unconfigure`, after the hook's `pytest_sessionfinish` sweep of the inside (controller and every xdist worker each own one); the package's state dir, minted before the redirect, goes with it; a root that survives removal is named on stderr. The same conftest floors git to no global/system config with a synthetic identity (`GIT_CONFIG_GLOBAL`, `GIT_CONFIG_NOSYSTEM`, `GIT_AUTHOR_*`/`GIT_COMMITTER_*`); the bats suites that build fixture repos `load git-hermetic` — four exist upstream (bootstrap-sh, docs-serve, install-sh, release-sh), three are fork-only (fork-remotes, gitleaks-config, pre-push-hook). The socket tests' inbox dir (`tests/test_kernel_socket_deliver.py`, upstream too) had pinned `dir="/tmp"` past any redirect and never removed it; it now follows the root, falls back to the system temp dir conftest recorded once per run (`ROMP_TESTS_SYSTEM_TMPDIR`, inherited by xdist workers) only when the socket path would not fit `sun_path`, and is removed at close. `test_kernel_worktree_display.TreeOf`'s class-level seed repo gets a tearDownClass. `tests/romp.bats` (upstream too) exports a no-op `romp-postal-service` stand-in as `ROMP_POSTAL_BIN` in `setup`, and `bin/romp`'s resume picker-check (one line) goes through that seam like `mail` and `refresh` — the script's own directory leads PATH, so nothing else can stand in: the real service, detached, minted a serve-token under the test's `$HOME` after teardown had removed it, re-creating four to six test dirs per run of that file. Pinned by `tests/test_tempdir_hygiene.py` (beside #201's hook checks: the in-run floors and child inheritance, no literal `dir=` in any tempfile call, the survivor notice, global and system `core.hooksPath` probes, and a nested pytest that must leave a fresh system temp dir untouched, serial and under xdist) and `tests/git-hermetic.bats` (the same two hooksPath probes; both proofs skip on git < 2.32 with one message). | branch `tmpleak` (`tests/conftest.py`, `tests/__init__.py`, `tests/git-hermetic.bash`, `tests/git-hermetic.bats`, `tests/test_tempdir_hygiene.py`, `tests/test_kernel_socket_deliver.py`, `tests/test_kernel_worktree_display.py`, `tests/test_reg_field_gutting.py`, `tests/test_kernel_provisional_command.py`, seven `tests/*.bats` (four upstream, three fork-only), `tests/README.md`) | candidate | Upstream's suite has the same preambles, the same fixtures and the same socket pin, and neither half of the hygiene (#201's hook has no row of its own; an offer of temp hygiene carries both halves). On a box with a global pre-commit hook the git floor cuts the four upstream git-fixture bats suites from 64 s to 26 s (measured 2026-09-06; the fork-only pair went from 31 s to 7 s). Test-only but for that one line in `bin/romp`. |
| The tags dialog's, the lane gear menu's and the tab menu's tag edits are targeted WS ops (`tagEdit` `{writeId, edit: {op, tid, …}}`: create / rename / recolor / addMember / removeMember / delete / move, by the tag's stored id — the kernel mints a create's id and default name; a move is one write, both halves or neither), and every views write is answered on the posting socket (`tagEditAck` / `viewsAck` with `writeId`, `ok`, the post-write client blob, its `seq`, a create's `tid` and `name`, and the stale-writer guard's kept tags with name-free reasons; a whole-blob write carries `edited`, the tag ids it changed, and is `ok` when every kept tag is one it did not edit). The views store stamps a write sequence (`seq`) on every accepted write and every frame carries it; clients adopt a blob only when its `seq` is at least the held one, settle the optimistic copy on the ack, and keep the exact echo and three-frame yield for seq-less blobs only. The kernel announces `tagEdit` in a `caps` frame at every `ready` (and on `/version`), answers a message no handler takes with `unknownOp`, and clients fall back to the whole blob without the cap. The Outline's lens write posts with a `writeId` and `edited: []` and ignores acks. Round 3 (the verification round's ten findings): one create in flight at a time per surface; a refusal reverts only its own write (in-flight records carry their op or blob; the copy re-derives from the store plus the rest); the legacy create's client-minted id, named as edited; the join menu's new-tag draft; a create ack leaves an open editor alone; the reader stamps a seq-less store once, re-stamps a file written outside the kernel whose seq fell behind, and the setter refreshes the read cache (the hidden-to-archived migration's 321-deep recursion fixed with it); the guard's notice follows the ack's `edited` rule and a stale copy cannot re-create a deleted tag. Round 4 (six verified findings): the hidden-to-archived migration works on a copy, so an existing archived tag is stamped; the reader's re-stamp write is guarded (an unwritable store is served, logged once per error kind, and not retried per read); a file written outside the kernel whose seq fell behind is judged against the last served blob (resurrections refused by the mtime only a kernel stamps, newer members kept, the notice names each tag); lens and order writes are built from the store's blob plus their fields in both clients, the whole-blob door refuses a name collision, and the normalizer drops a second entry under one id; a row whose create is in flight takes no gesture in either client; the join menu's draft is keyed by the open [+] and dies with its surface. Round 5 (seven verified findings, one high): `edited` bounds a whole-blob write — an empty list changes no tag (the store's tags stand; only the lens, order and active land; a lens write built from a copy taken in the same second as a targeted edit used to revert it), a list of ids changes those tags only with a differing copy of any other kept quietly, no `edited` keeps the legacy reading; the name-collision pass runs to a fixpoint so a refused rename keeps its stored name from a new taker; the judgment is a step apart from the write (`_judge_timeline_views` / `_write_timeline_views`) and an unwritable store serves and caches the judged blob rather than the refused foreign file; the first-read stamp and the migration give every tag an mtime so a deleted legacy tag cannot come back through a foreign file; the seq floor outlives the read cache's entry (`_VIEWS_SEQ_FLOOR`) so a store restored from an older copy is re-stamped past it; tag names are clamped and stripped in the normalizer and `_edit_tag`'s lookup, so a padded spelling is the same name; the Outline pane's lens write carries a writeId and `edited: []`. Round 6 (nine verified findings, one medium): the cap never drops a kept store tag — a write's creates are refused with a reason naming the cap, last first, and a posted 33rd tag reaches that pass instead of the normalizer's slice; the write door judges and writes under the file lock, so the reader's re-stamp cannot land between them; `active` is validated against the tags that stand, and the lens, order and active fields' last-writer-wins is stated in the docs; an order write orders the stored tags array too (the pill drag's contract, the clients' comments corrected); the legacy stamp's one-time cost (a copy older than the file's `at` is refused once on legacy tags it edits) is documented; the seq floor is keyed by store path; the clients keep the blob their seq gate last turned away and adopt it on the `caps` frame, so a store the restarted kernel serves under an older seq is adopted on the caps frame itself and the gate is never left open; lens and order entries read on the stored name basis (cap, then strip). Round 7: the caps frame carries `viewsSeq`, the seq of the views blob the `ready` handler's own connect push served (null when none), and a client adopts its kept blob only when its seq matches, so a pusher frame enqueued between the push and the caps frame is never adopted; a posted tag past the door's 64-tag read bound gets a refusal row (not created, or the store's copy kept) instead of a silent drop, and the loud notice names the bound as a fourth cause; the reader's re-stamp normalizes under the disk cap before the judge and names what the cap dropped as a file fact with no reload clause (`writer`, apart from `foreign`); a remote tag's rendered name and a pending edit's are on the stored name basis, so a lens can select a padded remote name. Round 8 (six verified findings): the caps frame's `viewsSeq` is the store's current seq when the connect push served no views blob (a chat page on a sentinel cycle), null only with no store, and a client that kept nothing the frame names remembers it as the announced seq (one slot per store, cleared on the next adoption) and adopts the later blob carrying it below the held one, so a restart over a restored older store met on a sentinel cycle is adopted on the pusher's next frame; the re-stamp's file-fact notice and the judge's loud notice put the cause and the count first and bound their lists to the 240 characters the bell shows (`_notice_list`, "and N more"), the verb agreeing with the count; the file-fact notice fires on the failed-stamp path too, saying the tags are not served and are lost at the next write; past-the-bound refusal rows are bounded to 64 plus one summary row (`more`, `moreEdited`, read by the ok rule) and the ack's `error` to 1000 characters; the migration finds a padded archived tag on the stored basis. Round 9 (five verified findings): the announced slot is cleared only by an adoption that changes the held blob, and a re-arrival of the blob already held leaves it standing (`announcedAfter`, mirrored as `viewsAnnouncedAfter` in the timeline and applied in the router's two stores), so a pane behind the federation router, which replays its stored blob on every merged re-emit, keeps the slot through the window between the caps frame and the pusher's frame and adopts the restored store when the router does; the timeline dialog's rendering of a nameless summary row is exercised by a test on the real panel; the ack's one-line `error` leads with the poster's own refusals (the rows `edited` names, or the summary row counting one), so the bound cannot cut the row that made `ok` false; the quiet stderr label for a kept differing copy carries its cause; the file-fact notice fires once when two readers that start with the cache empty race to the lock on a full disk (the waiter re-checks the read cache under the lock) | fork branch `tagack`: `kernel/kernel.py` (`_apply_tag_edit`, `_move_tag_member`, `_default_tag_name`, `_ack_views_write`, `KERNEL_WS_CAPS` / `_send_caps` / `_note_unknown_op`, the `ready`, `tagEdit` and `setTimelineViews` arms of `_dispatch_ws` and its terminal else, `_edit_tag(tid=, exists=)`, `_set_timeline_views` stamping `seq` and returning the refused rows, `_norm_timeline_views` preserving `seq` and deduping ids, `/version` `caps`, the inline boot block, `_views_restamp` / `_VIEWS_RESTAMP_ERR` / the `foreign` guard and name-collision pass in `_set_timeline_views`, `_judge_timeline_views` / `_write_timeline_views`, `_views_stamp_legacy_tags`, `_views_cache_put` / `_VIEWS_SEQ_FLOOR` / `_views_seq_floor`, `_tag_name_basis`, the cap pass in `_judge_timeline_views`, `_norm_timeline_views(tag_cap=)`, `_views_unread`, `_judge_timeline_views(writer=)`, `_send_caps(views_seq=)` / `_views_seq_of` / `_VIEWS_SERVED`, `_notice_list` / `SYNC_NOTICE_FIT` / `_views_lost_notice` / `_ACK_ERROR_CAP`, the summary row in `_judge_timeline_views`, `_refusal_is_the_posters` / `_ack_views_write(edited=)`, the `(differing copy)` label, the reader's under-lock cache re-check), `ui/romp-timeline-view.js` (`_takeViews`, `_postTagEdit`, `_setLens` / `lensBlob` / `applyLensFields`, `viewsAck`, `setCaps` / `viewsCapsAdopts` / `viewsAnnouncedSeq` / `viewsAnnouncedAfter`, `unknownOp`, `_reconcileViews`, the rename and join drafts, the lane menu notice, the pending union), `ui/webview/render.ts` (`postLens`, `postTagOrder`, `onKernelCaps`, the flyout's pending rows) + `ui/webview/views-writes.ts` (`lensBlob`, `applyLensFields`, `isPlaceholderId`, `capsAdopts`, `announcedSeq`, `announcedAfter`, `InflightWrite.lens`) + `ui/webview/session-views.ts` (`TagUnion.pending`), `ui/webview/federation.ts` (`tagEdit` / `setTimelineViews` routed local; the two views stores seq-gated, keeping the last blob turned away and adopting it on the local `caps` when `viewsSeq` names it, re-emitting first, remembering an unmatched `viewsSeq` as the announced seq, and re-deriving the slot on adoption; a remote's `caps` dropped), `ui/webview/timeline-boot.ts`, `ui/webview/fleet.ts` (`postOutlineLens`), `vscode-extension/src/pipe-intent.ts`; tests `tests/test_tag_edit_ack.py`, `tests/test_views_stale_writer.py`, `tests/test_timeline_views.py`, `ui/webview/fleet.test.ts`, `ui/timeline-views-ack.test.ts`, `ui/webview/views-writes.test.ts`, `ui/webview/federation-views-seq.test.ts` | candidate | Upstream carries the same stale-writer guard (their copy of fork `805f8406`) and the same dialog and flyout, so the same loss: a create followed by typing the tag's name posts the whole blob from the un-echoed copy, the guard refuses the rename against the dialog's own create, the refusal reaches stderr and the sync notices only, and the dialog drops its copy after three frames (default names, no members). The user lost a batch of renames and assignments this way on 2026-09-05. Round 2 of the fork's review (the same day) added the write sequence, id addressing, the atomic move, the capability handshake and the whole-blob `edited` semantics. The `/tag` route's `_edit_tag` is unchanged for its callers (the new keyword arguments default to the old behaviour). The `caps` frame is one extra frame per `ready` for every pane. |
| A folded tab-strip section shows its members' user-todo flag: the header derives a ⚑ from the same session field the tab's glyph reads (`userTodos`, refreshed by every chat delta), with a count when more than one member has an open todo and a tooltip naming those sessions; the flag is a focusable button that opens the section (`open-group` on the `#tabs` delegate; its own dragstart guard, so a press never starts the header's group drag); open headers carry none (every member tab wears its own); the phone layout's flat strip is unchanged. Two companions on the same branch: the tab menu's Tags flyout gains **Show when folded** (a member pinned to its section keeps its tab on the strip under the folded header; a per-browser view preference stored with the fold state under `romp:tabgroups`, ONE entry per tab and section — the sid, the section's displayed name at pin time, and the local tag's id when the section has one, never a remote host's tag id (`PinnedRef`) — matched when the entry's name is the home section's name OR its id is the home's local id, so the pin follows the section through a host attaching or detaching, a same-named tag appearing or disappearing on the other side, and either side's rename; on adds or replaces the home section's entry and off removes only that section's, other sections' entries untouched, so the row's per-section copy is the whole truth; renames follow the tag: on adopting a views blob in which a tag keeps its id under a new name (compared with the previously adopted blob), entries carrying the tag's id, or the old name where the renamed tag holds the tab (any such tag for an entry with no id, a remote one for an entry with a local id), follow EVERY matching rename — one entry per new name, with the id for a local tag — and where a same-named tag on the other side still holds the tab the half the tab did not move to keeps its entry, whichever side renamed, so the tab is pinned in whichever half tagOrder makes its home; the follow is ONCE PER BROWSER: the store remembers, by tag id, the name each renamed tag's pins were last carried to (`followed`), so a pane adopting the frame late — after the user turned a pin off — or a later frame from a stale base, or a same-seq frame carrying a remote rename (remote tags ride the blob with no write-seq change), writes nothing (`tagRenames` / `followTagRenames`, from render.ts's one base-assignment site `adoptBase`), and that memory is checked against every adopted blob, renames or none, per host — an entry stands while the blob names its tag by the remembered name, or while it is a remote host's and the blob carries none of that host's tags (detached, or a reattach ahead of its cached read), the blob not saying whether the tag is gone or the host is; a local id the store lacks and a remote id its host lists without lose their entries, and a tag the blob names by another name (renamed on while no client watched) has the pins under the remembered name carried to the blob's name — the rename the browser owes, the tag's own members deciding as for a watched one (`TagUnion.locals` / `remotes`) — and its memory re-stamped; each memory entry is stamped with the write seq of its tag's STORE as the adopted blob carries it (`followedSeq`: the blob's own `seq` for a local tag; for a remote host's tag the host's own, which the kernel puts on every `remoteTags` row of that host), and a blob whose seq for that store is older than the stamp stands down on the tag — the entry kept, no rename owed or followed for it, nothing re-stamped — while a blob at the stamp or past it proceeds; and a blob that names every tag as the held blob does (the same ids under the same names) is no news about names and moves nothing — unless a stamped memory entry disagrees with the blob's name for its tag and the blob's seq for that store is past the stamp (a tag renamed back to the held name after another pane carried its pin from a blob this pane never saw), when the check runs (`followAdoption`, the adoption's one entry point); the pin row's write prunes per entry, judging only an entry whose session's fate the page can know — a known tab, a local sid, or a sid on a host attached with its tunnel up whose tab list has reached the pane (render.ts `reachableHosts` through tab-groups.ts `reachableFrom`: the federation router's attached hosts less its down set and less its pending set, the hosts the pane has no tabs from yet) — and a judged entry stands while its session is a known tab and some union of its name or its local id holds it, while a detached, down or still-arriving host's entries stand untouched until its tabs are here; the store's earlier `{tag, sid}` shape migrates on read against the current unions; LIMITS: a remote-only pin has no id, so a remote tag's rename that no client of the browser watched is followed only through the memory (a rename of the tag followed before ties the pin's name to the tag's id); with no memory of the tag the entry stays under the old name until the user pins the tab again, after which the tag's renames are followed, watched or not; and a LOCAL tab pinned under a section only a remote host's tag made loses the pin on that host's detach (the sid is local and the entry stores no host), to be set again after the reattach; and a reattached host's tabs and tags reach the pane by two roads — the tabs over the relay within a /tunnels poll (4 s) and a connect; the tags on the local kernel's views blob, cached by the supervisor pass that marks the host up and shipped by the pusher's next tabOrder frame (the kernel wakes the pusher when a host's cached /views reading changes, `_cache_remote_views`, so that frame is the next cycle: sub-second on an idle box, the end of the cycle in flight on a loaded one, 20-40 s) — so the tabs can lead the tags by up to a pusher cycle, and by a supervisor pass (15 s) more when the kernel's first /views read fails while its /sessions probe answers; a pin write in that window sees the host's sessions as known tabs no union of the pinned name holds and drops its remote-only pins, to be set again; a memory entry without a stamp (a store from before it, or a kernel or host that stamps no seq) stands no blob down, so a pane adopting an intermediate frame late can still move such an entry's pin once until a followed rename stamps it, the re-adoption of the held blob being caught for every tag by the name check; and a store restored to an older copy is stood down for its tags until its next write, whose time-ordered seq passes the stamp; and two name states can share one seq when the tags are edited from the Obsidian-hosted timeline (its Electron branch writes the views file under the seq it holds, and the kernel's reader leaves an equal seq alone; the kernel's own tag door bumps the seq on every write), so a pane processing the first of two such renames late, after another pane followed the second, moves the pin once and its next frame carries it back — one round, a remote-only pin folded away between the two adoptions; the header's count and flag read the hidden members only, and a section whose every member is pinned stays folded with the total for a count and a title saying none is hidden), and the section header reads as a label rather than a session (a disclosure chevron that flips with the fold, the tag's color as a short bar, a small letter-spaced name, the member count — the folded-away count while folded — and no state class, close or pip of its own; folded, the member-state pip stays after the count, small, over the hidden members, its tooltip naming the sessions (`sectionPipMembers`, `sectionPipTitle`); focusable, Enter or Space fold, focus kept across re-renders — on the flag when the flag held it, and the header's key handler stands down for the flag so Enter runs the flag's open; hover and focus color the chevron in the accent with no row wash; to assistive tech the chevron, bar and pip are hidden, the header's name is an aria-label in words with the pip's phrase and the flag's (the flag is a button nested in a role=button header, whose children a conforming tool may prune), and the active section's header is a group, not a button; the pip's tooltip counts several sessions). Stacks on the `tagack` branch. | fork branch `tagflags` (on `tagack`): `kernel/kernel.py` (`_cache_remote_views`, the one store of a host's /views reading — the tunnel supervisor's pass, `_apply_pending_tag_edits`' fresh read, `_forward_tag_edit`'s refresh; `_NOT_SAVED` gains `_views_at`, and `_remotes_load` drops every `_NOT_SAVED` key an older file carries; `_views_client` puts the host's own `seq` on each `remoteTags` row), `ui/webview/session-views.ts` (`TagUnion.locals`, `SessionTag.seq`), `ui/webview/tab-state.ts` (`sectionTodoFlag`, `sectionTodoTitle`, `SECTION_PIP_TITLE_MANY`), `ui/webview/tab-groups.ts` (`sectionRef` / `SectionRef`, `PinnedRef`, `TabGroupsState.followed` / `followedSeq`, `isPinned` / `setPinned` / `togglePinned` / `prunePinned`, `reachableFrom` / `RouterHosts`, `tagRenames` / `followTagRenames` / `followAdoption`, `tagNames` / `sameTagNames`, `parseTabGroups`' shape migration, `headWords`, `StripItem.hidden`), `ui/webview/render.ts` (`makeGroupHead`, the `open-group` handler, the flyout's pin row, `adoptBase`, `tabGroups`, `knownTabIds`, `reachableHosts`, `renderTabs`' header refocus), `ui/webview/styles.css` (`.tab-group-head` family, `.tab-group-caret`, `.tab-group-swatch`, `.tab-group-flag`), `ui/webview/federation.ts` (the `pending` list on `__rompFed`), `docs/guide.md`; tests `ui/webview/tab-group-flags.test.ts`, `ui/webview/tab-groups.test.ts`, `ui/webview/tab-state.test.ts`, `tests/test_tag_federation_v2.py` (`SupervisorViewsCache`, `LateApply`, `HostSeqRidesTheRows`), `tests/test_remotes_stale_and_persist.py` (`SupervisorStatePersists`) | candidate | Rides the tab-groups offer (the row above's branch and the `tabgroups` rows): a fold hid the one tab mark that means "needs you", and a folded `archived` group is the default. The branch's adversarial review (2026-09-06, eight verified findings, one medium: the pin API and menu row had lost their tests) is folded in, and its second round (one medium: a remote member's pin under a mixed section's local id died with the local tag's delete or rename), third (two low: a member both tags hold lost its pin when the renamed local tag was its home) and fourth (two low: every key derived from the holders at click time lost the pin when a same-named tag appeared or disappeared on the other side AFTER the pin — pinned before the second holder, then the local tag deleted or renamed; a remote pin, then a local same-name tag, then its rename — and on replaced other sections' entries; the model was replaced with the per-section name-and-id entry, renames followed on adoption), and fifth (two medium: a pin click while a host was detached dropped every pin on its sessions, which folded away on the reattach; a pane adopting the rename frame late re-applied a pin the user had turned off — and three low: a remote rename of a mixed section left the pin in the local half only, only the first of two same-named renames in one frame was followed, and the follow's no-change check compared by index; the prune now judges only entries whose host is reachable, the follow remembers per browser the name each renamed tag's pins went to, follows every match on either side, and that memory replaces the index comparison), and sixth (two low: the follow's memory was pruned to the blob's tags, so a rename followed while a host was detached erased that host's memory and a stale pane re-applied a pin the user had turned off after the reattach; a host attached and up whose tabs had not reached the pane counted as reachable, so a pin click in that window dropped every pin on its sessions — the memory is pruned per host now, and the prune subtracts the router's pending hosts), and seventh (two low: the memory kept through a detach was never checked against the blob, so a remote tag renamed back while its host was detached and forward again after the user re-pinned the tab read as already followed, and the tab folded away with no gesture on it — the memory is checked against every adopted blob now, and an entry the blob names otherwise goes; and the stated one-pass window for a reattached host's tags named a narrower trigger than the mechanism, the supervisor caching its /views reading without waking the pusher — the kernel now marks the views dirty when a host's cached reading changes, and the limit names the pusher cycle), and eighth (three low: a pane whose held blob predated two renames of a remote tag computed the coalesced one, carried nothing — the watching pane had left the pin under the middle name — and stamped the last, so the watching pane's frame for the second rename read as already followed and the tab folded away; a memory the blob contradicts is now the rename the browser owes, followed on that adoption over the tag's own members; the pending-edit apply stored a host's /views reading bare, so a change seen only there marked nothing and woke no one — every store goes through `_cache_remote_views` now, and the test scans the whole module; and the /views poll's stamp was persisted and counted, rewriting the 0600 remotes file about once a minute per up host — `_views_at` is out of the saved row and its signature, `views` staying so a down host's tags survive a restart; this row), and ninth (one medium: the round-8 carry let a pane whose local socket was dead, re-adopting its stale blob on every router re-emit, read the fresher pane's memory as the rename it owed in reverse and carry the pin back, the fresher pane carrying it forward on its next frame — a move per trigger on no new information, and a bounded flap when a pane adopted an intermediate frame late; a blob naming every tag as the held one does is no news now and moves nothing, and the memory is stamped with the write seq of each tag's store, a blob older than the stamp standing down on that tag — and two low: a remotes file the previous build wrote restored the /views poll stamp on load, gating the first boot's read for up to a minute — every unsaved key goes on load now; and the owed rename's mixed-pin off case, unpinned-half case and the `!x.local` clause had no test), and tenth (one medium: the round-9 shortcut answered before the seqs were read, so a tag renamed back to the name a stale pane held — after another pane had carried the pin from a blob that pane never saw — left the pin under the stale name and the tab folded away until the tag's next rename, or for good once a pin write judged the orphan; the shortcut now yields to the check when a stamped memory entry disagrees with the blob's name for its tag and the blob's seq for that store is past the stamp — and one low: two name states sharing one seq on the Obsidian timeline's write path admit one flap round, stated as a limit rather than re-stamped in the reader, whose equal-seq rule that surface's held seq depends on; this row). Kernel changes: `_cache_remote_views`, the one store of a host's /views reading, marking the views dirty when it changes; `_views_at` left out of the persisted row, and every unsaved key dropped on load; and each `remoteTags` row carrying its host's own views `seq`. |
| The pre-push hook scanned every commit in a ref's update range by TREE, so once a denylisted string had landed on main (and been redacted there), every branch cut from main in between was refused: its commits inherit the string in their trees, and a branch that merged main also carried main's own commits in `remote_sha..local_sha` (2026-09-06: two identifiers added to the fork's list, redacted forward in fork PR #209; 22 branches on the fork's remote had inherited them) | branch `privfix`: `.githooks/pre-push` (`rev_range`, `added_lines`, `scan_identifiers`), `tests/pre-push-hook.bats` | candidate | Upstream ships the same hook with the same per-commit tree scan over `remote_sha..local_sha` (theirs is the identifier-only variant: no gitleaks pass, no symlink scan). Two changes: (1) per pushed ref, the TIP tree must be clean (the exposure a push creates) and each new commit must ADD no banned line in its first-parent diff, so an introduction inside an intermediate commit is still caught and named while a commit that only inherits an older leak is not; (2) the range excludes commits reachable from the pushed-to remote's refs (`<local> --not <remote_sha> --remotes=<remote>`), scoped to that remote, falling back to the old range when the clone has no remote-tracking refs for it. About 40 lines; the bats cases port only if they take the test file too (upstream has no `tests/pre-push-hook.bats`). This file's header lists the private-strings hooks as fork-only infrastructure; the hook itself is tracked upstream, so the row stands and the header is the user's call. |
| Opt-in per-session memory limits on the transient scopes: `ROMP_CLI_SCOPE_MEMORY_MAX` / `_MEMORY_HIGH` / `_MEMORY_SWAP_MAX` (systemd sizes, validated; `MemoryMax=`, `MemoryHigh=`, `MemorySwapMax=` on the session's scope, with `OOMPolicy=continue` so one OOM kill takes the one process and not the whole scope) and `ROMP_CLI_SCOPE_OOM_SCORE_ADJ` (written to the session tree's `oom_score_adj` by the exec-in-place wrapper, so machine-wide killers prefer a session over the kernel). A value that does not parse is refused at the kernel's start and never reaches systemd-run; the wrapper's own check reports one with a third stderr form, `romp-cli-scope: ignored: …`, logged at arrival; the pre-flight carries the properties so an old systemd still launches the CLI, in its scope, without them. The kernel runs the wrapper's own steps once at its start (a probe scope with the properties, a memory-controller check inside it, the adjustment write in a throwaway child), so a value the machine refuses lands in `rejected` once instead of one `ignored:` line per launch, and a user manager without the memory controller (systemd accepts the properties and applies nothing) is a problem line. `/api-health` `cliScope` reports the values in force, `rejected`, `memoryControllerDelegated`, `unsettled` (the boot checks that did not answer) and `limitsIgnored`. Verified on systemd 255: a scope's default `OOMPolicy=stop` SIGTERMs the whole scope on one kill; `MemoryMax` alone lets the process swap out instead of dying on a box with swap; a user unit's `OOMScoreAdjust=` cannot go below the user manager's own value and the sessions inherit the kernel's anyway, so that lever does not separate them. | branch `scopemem`: `bin/romp-cli-scope` (`preflight`, `size_ok`, `adj_ok`, `apply_adj`, `ignored`), `kernel/sdk_backend.py` (`CLI_SCOPE_LIMITS`, `cli_scope_limits`, `CLI_SCOPE_IGNORED_PREFIX`, `_note_cli_scope_ignored`, the `_options` overlay, `api_health_snapshot`), `docs/reference.md`; tests `tests/test_cli_scope.py`, `tests/romp-cli-scope.bats`, `tests/test_sdk_launch_error.py` | candidate | Motivated by 2026-09-06: a session's shell globbed a bloated `/tmp`, grew past 30 GB, and earlyoom killed the kernel as the largest process. Nothing is on by default; the docs carry a suggested setting for a shared machine. Depends on the per-session scopes (#194 on the fork). |
| `tests/test_postal_relay_honesty.py::PresenceBlinkHonesty::test_disk_twin_carries_the_cache_across_a_bus_restart` depends on module order: red under xdist (`-n 8`) and when selected alone with `-k`, green with its module run in order — the one recurring full-sweep failure on every batch-12 port | upstream code (the fork carries the same module); surfaced by the batch-12 sweeps (2026-09-06); not yet built | candidate | `tests-only`: make the test seed its own state (or have the fixture reset it) so order stops mattering |
| `tests/romp.bats` leaks 6 temp dirs per run: `bin/romp`'s detached picker-check on the resume path double-forks the REAL postal service, which mints a `serve-token` under the fixture home after teardown's `rm -rf`; the suite's curl mock also flakes on SIGPIPE | leak fix: fork branch `tmpleak`, commit `e98c8c79` (the detached call honours `ROMP_POSTAL_BIN` like the mail and refresh paths, and romp.bats stands in for the service; fork PR pending, 2026-09-06). Their #944's exit-time sweep cannot catch it (the write happens in a detached process after cleanup). SIGPIPE flake: fork main `835a0a4a` (2026-09-04); still open upstream | candidate | `tests-only`; its own port once the fork PR lands — not a fold into #944 |

When offering: work from a branch cut off the upstream default, carrying only that change —
never a fork branch with fork-only commits tangled in.

Security items: the plan was `SECURITY.md`'s private-advisory process, but the user CHANGED this
(2026-08-14) to shipping fixes as ordinary per-chain PRs — the fork is public (so the findings are
already discoverable in its history) and upstream has no private-reporting intake. See the security
bundle row above and `~/romp-handoffs/security-session.md`. Still do NOT enumerate an upstream-unfixed
hole anywhere public beyond the PR that fixes it.
