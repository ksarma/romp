# Refusals

An aligned paragraph before the code.

```python
def handler(request):
	return respond(request)
```

    indented code line

| Route | p95 |
|-------|-----|
| GET /notes | 120 ms |

<div class="callout">
An HTML block the viewer keeps.
</div>

Fast &amp; simple prose with an entity.

A [label with \] bracket](https://example.com/x) refuses.

- Item one
-	Tab after the marker

> 	quoted after a tab

An aligned paragraph after everything.
