# Handler notes

Before paragraph with several words in it.

```python
# a comment
def f(x):
    return x + 1  # trailing

value = f(2)
```

After paragraph with several words in it.

```
plain fence, no language
second line of it
```

Last paragraph.
