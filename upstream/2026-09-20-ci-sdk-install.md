---
title: CI installs the Claude Agent SDK at the session host's tested version in every Python cell
status: candidate
where: .github/workflows/ci.yml (the Install the Claude Agent SDK step; the Run pytest step's env and plugin block; the served-page step's plugin flag; the job cap's comment; the cryptography step's comment); scripts/release.sh (the uv road's comment and notice say what that road installs and lacks; the uvx command unchanged); tests/README.md (the SDK install, the pin module, the plugin flag and what the two checks leave unread); tests/__init__.py (registers sdk_blocker); tests/conftest.py (the never-skips guard and the one flip both belts share); tests/release-sh.bats (the resolver comment says what the uv road installs); tests/sdk_blocker.py (new); tests/test_bats_bare_negation.py (its pytest launcher passes -p no:anyio); tests/test_ci_sdk_pin.py (new); tests/test_claude_config_floor.py (its pytest launchers pass -p no:anyio); tests/test_env_value_redaction.py (its pytest launcher passes -p no:anyio); tests/test_host_transport.py (its docstring and the comment on the venv path); tests/test_key_source_floor.py (its pytest launcher passes -p no:anyio); tests/test_mode_truth.py (the fake gate); tests/test_sdk_backend.py (docstrings; FastModeReportedState's drain); tests/test_served_tests_require.py (its pytest launcher passes -p no:anyio; the belt tuple's existence check and the served switch's xfail-alone cases, reasoned and bare); tests/test_session_host.py (the SDK-transport gate and its probe under the spawn's own environment, the drift case comparing the two environments both ways, the pipe-transport control and the switch case); tests/test_session_host_sdk_pin.py (the no-SDK control); tests/test_supervised_floor.py (its pytest launcher passes -p no:anyio); tests/test_tempdir_hygiene.py (its pytest launchers pass -p no:anyio); upstream/2026-09-20-ci-sdk-install.md (this entry)
added: 2026-09-20
pr: 872
tier: docs
offered:
closed:
---
CI's Python cells install pytest and cryptography and nothing else, so the 48 tests gated on claude_agent_sdk importing (43 in tests/test_sdk_backend.py, 5 in tests/test_host_transport.py) skip in every cell and read green having checked nothing, the two SDK-transport host tests in tests/test_session_host.py skip with them, every session host a test spawns takes the pipe transport, and 98 other tests never meet the real SDK: 25 spawned-host cases in tests/test_session_host.py whose host runs the pipe transport, and 73 in-process cases across eight other modules whose fixtures install a fake SDK only when the real module is absent (148 tests in three disjoint sets); upstream's workflow has the same shape and the same gated module. The change is one step, Install the Claude Agent SDK, between the cryptography step and pytest, reading the version from SDK_TESTED_VERSION in kernel/session_host.py with the sed bin/romp-sdk-setup runs over the same line, so the number has one declaration and the installer, the host's own version check and CI follow it; on 2026-09-20 that was 0.2.156, the version the box's full suite passed under that day (16875 passed, 566 skipped, 1 xfailed, 0 failed), while PyPI's latest was 0.2.157, released 2026-09-18 as the fifth version in four days and left alone on purpose. The step refuses an empty, malformed or duplicated constant and carries no continue-on-error: the SDK requires cryptography through mcp's pyjwt[crypto], and a lagging wheel must red the cell, not skip 48 tests. tests/test_ci_sdk_pin.py never skips (tests/conftest.py reports any skip outcome in that file, at collection or at run time and whatever its spelling, as a failure carrying the skip's reason, and the module's NeverSkips class proves that belt by running pytest in a child, pins that the test the belt exists for is collected by asking pytest's own collector in a child, since the belt reads reports and a test never collected files none, and asserts this file's name is in the belt's tuple as written, with tests/test_served_tests_require.py asserting from outside the module that every entry names a file under tests/; the residual of that collection pin is that it lives in the module it guards, so a road that changes what a run collects without touching the file files no report and takes the pin with it, or acts on the run where the pin's child may not see it; the road is a class that no list closes, anything that changes what the run collects, among them a collect_ignore or collect_ignore_glob, a collection hook in a conftest or plugin (pytest_ignore_collect, pytest_collection_modifyitems), an ini file's test-file pattern, testpaths or addopts, PYTEST_ADDOPTS, --ignore or --ignore-glob, -k, -m or --deselect, and a module-level __test__ = False; as read on 2026-09-23, none of these is on the workflow's Run pytest line or in its env, no conftest in the tree sets collect_ignore or collect_ignore_glob or defines a collection hook (tests/conftest.py, the only one, implements two reporting hooks, pytest_make_collect_report and pytest_collectreport, which drop nothing), and the repo has no pytest.ini, .pytest.ini, pytest.toml, .pytest.toml, pyproject.toml, setup.cfg or tox.ini; nothing pins that): it executes the step's own read of the constant and, wherever the SDK imports, asserts the installed version equals it, so a box whose venv moved and a CI cell whose install disagreed both go red; on a venv without the SDK it checks the pin's presence and form and warns, in pytest's warnings summary, that the equality was not checked there; under ROMP_SDK_REQUIRE=1, which the Run pytest step sets, that road fails, naming the interpreter, and the install step's own import of the package in the same python reds first when the install missed the interpreter on PATH. The bump is the constant's own act (install the new version, run the host tests, move the number); the residual is that nothing polls PyPI, a person notices a release. A second residual: the step pins the SDK alone and its dependency closure resolves fresh on every run (26 packages on 2026-09-20, the 3.12 cell of run 35518107329), so a red that no commit explains is a transitive release the pin test does not catch; one of those packages, anyio, registers a pytest plugin that every pytest process a Python cell runs would auto-load, and both pytest lines in the workflow (the Run pytest step and the extension job's served-page step) and every pytest the suite starts from a test, as a child or in process, in a form the launcher census reads pass -p no:anyio, so no pytest process the two checks read loads a plugin the box's default run does not, unless something loads anyio's plugin again by its entry-point name or its module name (-p anyio or -p anyio.pytest_plugin after the flag, PYTEST_PLUGINS, plugins= on pytest.main), which neither check reads; the two plugin sets are not equal, since the box's default run loads pytest-xdist's two plugins, which no cell installs (the step's comment states the decision and the measurement: the two gated modules read the same counts with and without the plugin; before the children gained the flag, a synthetic broken anyio pytest plugin in a CI-shaped venv red tests in each of the six modules whose children lacked it, none of which imports the SDK). tests/test_ci_sdk_pin.py holds the flag on both populations, and holds that every pytest invocation in the workflow sets ROMP_SDK_REQUIRE=1 or is listed with a reason whose premises it checks (the served-page step is the one listing: its job installs no SDK). The switch is read as exactly 1 from the env GitHub Actions merges for the command, each scope an env: key line as the file writes it, a quoted value as written; the check does not run the shell, so a step whose run text spells the switch other than as a prefix on its pytest command (an unset, an export, an assignment), or whose job's other run texts spell it (a write to $GITHUB_ENV reaches the steps after), is red until read, and so is any other line of the file that spells the switch outside an env: key line the check reads (an aliased or flow env:, a quoted or spaced key, a value continued on the next line or written as a block scalar, a key-shaped line inside another key's block scalar, a step's shell: or a job's defaults; inside another key's quoted scalar over several lines, it is red at that scalar's first line); any write of the switch that does not spell its name is outside the read, among them one in a step's own run text (env -i, an indirect unset such as unset "${!ROMP_@}", a loop over the environment), one in a step's shell: or a job's defaults, one by a script or action a step calls, and one to $GITHUB_ENV. Two residuals of those checks: the workflow check reads ci.yml's text, so a line there that spells pytest in a form the check does not read as a command, in any layout, is red until read (a line census holds every such line, outside a comment or a name: key's own line and other than a pip install, to a row the parser read, so a name continued onto a line that spells pytest is red there too and is reworded, and a job key the parser does not read is red), the check does not read YAML anchors, aliases or merge keys, a step in flow style, a quoted scalar or flow collection continued past its line, or a double-quoted scalar holding a backslash escape, and reds each at its line, and a run line that never spells pytest, a pytest run by a script or action a step calls, and the other workflow files are outside it; the launcher census reads argv literals under tests/, the positional arguments of asyncio.create_subprocess_exec and of the os.exec and os.spawn l forms, and an in-process pytest.main, pytest.console_main or _pytest.config.main by name, alias or import (whose own argv must carry the flag, since plugin autoload runs again inside it), so a pytest command in a constant string or f-string written at the call and handed to subprocess, os.system or os.popen, asyncio.create_subprocess_shell, shlex.split or a shell's -c is red until spelled as an argv, and so is a call through a name the census cannot resolve (in a comprehension in a class body that binds the name, or, beside a star import from a module it does not read, one the module binds or no scope binds); a string anywhere else, or one held in a variable or built with %, + or .format, is outside the read, as is an argv assembled one element at a time and any other call, string or in process, reached through a name the census does not resolve (tuple unpacking, a conditional expression, a parameter default, a class or instance attribute, getattr, importlib or runpy, a builtin or a module name such a star import rebinds, a class body's call before the class binds that name, among others). The 3.14t cell installs it too: on a free-threaded 3.14.6 with the GIL off the install used wheels only (cryptography, cffi, pydantic-core and rpds-py publish cp314t wheels), the import left the GIL off, and on 2026-09-20 the gated pair passed (653 passed, 1 skipped, an unrelated runner-floor pin) and the host pair (tests/test_session_host.py and tests/test_session_host_sdk_pin.py) passed in CI's shape with the switch on, where the pair's three MachineVenv tests skip (below) and the rest run; the SDK's classifiers stop at 3.13, so a release that breaks there shows as a red cell. Two re-gates: the two SDK-transport host tests gate on what the host's interpreter imports (a probe of sys.executable) or on the box venv, not on the venv directory, and the no-SDK control in tests/test_session_host_sdk_pin.py hides an installed SDK from its child with a sitecustomize.py blocker (under an interpreter that has the SDK it read CLINotFoundError where FileNotFoundError was expected, on 3.12 and 3.14t alike); one new control in tests/test_session_host.py hides the SDK from its host the same way and asserts the pipe transport, which otherwise has no test once the interpreter has the SDK. The Linux job cap stays at 25 minutes; its comment, which priced the cap from pytest phases of 160 to 300 s, now states the measured phases (866 to 1184 s on the run this branch was cut from, main's run 35503189425, read from the Actions API) and the headroom (the Linux jobs took 16.5 to 19.9 minutes whole on main's run 35513531701 and 17.3 to 20.9 on this branch's first CI run, 35516364698, whose SDK step took 5.5 to 7.7 s per cell, step start to pip's Successfully installed line; the rest of the worst cell's extra minute is the tests that moved from skipping to running, on top of main's own per-cell spread of 147 to 235 s over eleven runs through 35518469298), so the cap pin in tests/test_ci_bats_bound.py is untouched; the cryptography step's comment no longer promises the 3.14t cell can continue without cryptography, which the SDK step requires. The step carries a 5 minute timeout, the file's convention for a download. Still skipping in CI: three of the four MachineVenv tests in tests/test_session_host_sdk_pin.py, which test the venv the installer builds (the fourth builds its own temp layout and runs; under CI's shape the class reads 1 passed, 3 skipped), and the two ServedRestart tests in tests/test_session_host_restart.py, which gate on the same venv directory and also need the extension bundles, so a re-gate alone would not run them in the Python job; on a plain venv without the SDK the 48 and the two host tests skip as before. Failing alone, not skipping: on an interpreter with the SDK that evaluates annotations at class definition (3.10 to 3.13: the four Linux cells 3.10 through 3.13 and the two macOS cells, not 3.14t) tests/test_session_host.py run ALONE fails 8 SpawnSecrets tests, its Popen patch live when the module first imports the SDK and mcp's win32 utilities evaluate subprocess.Popen[bytes] at class definition; green when an SDK-importing module is collected first, as the suite and CI's one pytest invocation do; pre-existing at the base on such an interpreter, recorded in the class docstring and the SDK step's comment, a follow-up. The statements in the tree that say CI installs no SDK were found by command, not by reading (the PR body carries the census, a multi-line perl match over git ls-files, and its output; 13 hits at the round-1 head): the six in tests/ are rewritten in this PR to say every Python matrix cell installs it (the vscode-extension job's served-page pytest step still installs none); four sites in product code are outside this PR's tier, stay as they are, and are the residual: kernel/host_transport.py's ImportError fallback comment (the hermetic tests, CI), kernel/sdk_backend.py's Pure translation logic banner (in CI without the dep) and its _overlay_text docstring, the one that names split_spawn_secrets (the no-SDK road of the hermetic tests and CI), and kernel/session_host.py's PipeCliTransport docstring (CI, which install no SDK); bin/romp_sdk_backend.py is a symlink to kernel/sdk_backend.py, so the census lists those two twice; the one hit in ui/webview/md-config-block-boxes.test.ts is about a playwright browser and stays true. Cost per Linux cell about 106.5 MB of download after the cryptography step (the SDK wheel is 102 MB there and 92 MB on macOS arm64; it bundles the Claude Code binary) and 5.5 to 7.7 s of install per cell on the branch's first CI run, 35516364698 (5.4 to 6.8 s on this box). Travels with 2026-09-18-sdk-pin-for-private-imports (#777), which introduced SDK_TESTED_VERSION; upstream's installer installs the SDK unpinned and its tree has no such constant, so an offer there carries the step with a literal version until it does.
