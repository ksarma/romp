---
title: The SDK the host imports private internals from is pinned, and a mismatch fails loudly
status: candidate
where: bin/README.md, bin/romp-sdk-setup, docs/architecture.md, docs/reference.md, kernel/host_transport.py (host_log_mark, host_log_rows, host_exit_reason and its spawn-watermark bound; the public-package import of Transport, CLIConnectionError and ProcessError), kernel/sdk_backend.py (the watermark, the launch-error carry and the host.exited-before-socket and host.never-served-socket rows in _host_transport_for, _file_refused_launch_context, _record_refused_launch_position and HOST_LOG_POS_REFUSED, the served road's honouring of that position in _file_host_log_rows, _file_sdk_untested_row and its once-per-kernel-life memo _sdk_untested_reported, the tail exclusion in _record_launch_error), kernel/session_host.py (SDK_TESTED_VERSION, installed_sdk_version, error_chain, sdk_internals, sdk_mismatch_text, sdk_broken_install_text, SdkInternalsMismatch, the _process guard in _spawn, the causes field on the cli-spawn-failed row in run, main), tests/install-optional-deps.bats, tests/test_session_host_sdk_pin.py, tests/test_host_transport.py (the hostLogPos drop on every road that clears the host directory)
added: 2026-09-18
pr: 777
tier: fix
offered:
closed:
---
kernel/session_host.py drives claude_agent_sdk._internal.transport.subprocess_cli.SubprocessCLITransport and its _process attribute, neither public, with no fallback, while bin/romp-sdk-setup upgraded the package unpinned: a release that moved one would have failed every hosted session launch with the install step none the wiser (the box admin hazard review of the merged pull-in, 2026-09-16). One constant, SDK_TESTED_VERSION in kernel/session_host.py (0.2.156, the version the host tests run against on the box), now declares the version the imports were verified against; the setup script reads that line and installs claude-agent-sdk==<it>, refusing to run with no line to read and refusing a venv that holds another version after the install; the host compares the version it imports (the package __version__, falling back to importlib.metadata) to the pin before the import, imports directly on a match, and on another version resolves each name inside a try and fails through the host-crashed record with both versions and the repin command when one is gone (the kernel launch error now carries that record, host_transport.host_exit_reason), or runs with one host.log row saying newer or older, filed as a problem row once per kernel life per version pair. Review round 1 added the launch-error tail exclusion for a host refusal, the host.exited-before-socket row, the ImportError narrowing and the split broken-install text; round 2 put the chain of types behind a spawn failure on its row and made the untested-version report once per kernel life; the closing check bounded the reason read on a spawn-time watermark of host.log (not the last host-started row, which a host that died writing nothing never wrote), filed the drift fact as its own host.sdk-untested row on both refused roads with the failure keeping its bare type and the version stated beside it without a remedy (the type-name allowlist is gone), added the host.never-served-socket row on the deadline road, and made the pin read the imported module first. Review round 3 (2026-09-19) pinned the watermark by mutation (each of its four reads dropped to the file's start reds a case: the drift row in the next kernel life over a surviving log, the deadline road's reason for a host that wedges after a failing row and for one that wrote nothing over a previous host's rows) and pinned the memo's cross-session key and the row reader's non-object guard the same way; it aligned the installer with the host (the verify step loads kernel/session_host.py by path under the venv's python and calls installed_sdk_version(), so both read the imported module's __version__ first and the metadata second, each under its own interpreter (the second residual below), with the module-and-metadata disagreement refused in its own words naming both versions and the module's file, the metadata kept as a second check, and a non-importing SDK refused with its error); and it closed one refused launch reported twice (the refused roads record host.log's line count at the refusal, hostLogPos under HOST_LOG_POS_REFUSED, and the served road starts the next host past it). Review round 4 (2026-09-19; eighteen findings, five defects, none refuted) corrected the statement of that position's reach in six sites (it is the whole file's line count, the residual below), pinned the dropped row as the head's behaviour, rendered the installer's ready line per branch (the module's __version__ from its file, or the package metadata under the venv's site for a module with no usable __version__) with a bats leg executing the metadata branch, added a leg running the script through a symlink from outside the checkout with the verify step executed, corrected the row-reader case's comment to the reverse scan the code has, and filled this entry's pr field. RESIDUAL, by the reviewer's ruling and not folded in here: the spawn watermark reaches the two refused-road reads only. The served road (_file_host_log_rows at the hello and the exit) reads from an identity-keyed line position and starts an unseen identity at line zero, so over a host.log that survived a previous launch (a stale kernel-held lease keeps the directory) a fresh host that serves files a previous host's rows as its own problem rows; and after a refused launch it starts past the position that launch recorded, which is host.log's WHOLE line count at the refusal, so a previous host's row that no road had filed (a reader-behind, an end-forced) is filed by no road and VANISHES (round 4, 2026-09-19: on the round-3 base those rows were filed, misattributed; a single prefix position cannot keep the rows before the watermark and skip the rows after it, both spellings executed by the refuters). Bounding that road on the byte watermark is the queued served-road change, where this is fixed; the drop is pinned as the head's behaviour until then. SECOND RESIDUAL (extra7-2, round 4): the installer's verify step runs under the venv's python while a host runs under the kernel's interpreter, and bin/romp-session-host adds the venv's site only when claude_agent_sdk is not already importable, so a claude_agent_sdk on the kernel interpreter's own site is what every host launch reads and the installer never sees; the module-versus-metadata divergence is closed, and the class of an installer that says ready where every launch disagrees is narrowed, not closed. Tests: thirteen bats cases (44 in tests/install-optional-deps.bats at the round-4 head, 31 at the base) over a logging pip stub (red on the base, which passed an open upgrade; the pinned install, the missing-line and missing-file refusals, the symlink and readable-file guards, the executed verify step at a wrong version and at the pin, and from round 3 the module-versus-metadata split refused with both readers agreeing, the second check's own words, the non-importing SDK, the ready line's source, and from round 4 the metadata branch of that line and the out-of-checkout run) and tests/test_session_host_sdk_pin.py, 53 cases (the helper with fake modules, a real host over a fake SDK site, the launch error over a stale stderr tail, the public-import fallback, the watermark through the real spawn road over a stale lease, the separate drift row and the deadline row, both relation guards, the module-first pin, and from round 3 the next-kernel-life drift row, the two deadline-road reads, the memo across two sessions, the non-object lines, the refused launch's rows not filed again by a serving host, the machine venv read as the host and the installer read it, and from round 4 the pinned residual: a previous host's unfiled row vanishing after a refused launch). The kernel side goes live at the next kernel restart; the setup script side at the next run of bin/romp-sdk-setup. An offer waits on the user word.
