---
title: The SDK the host imports private internals from is pinned, and a mismatch fails loudly
status: candidate
where: bin/README.md, bin/romp-sdk-setup, docs/architecture.md, docs/reference.md, kernel/host_transport.py (host_log_mark, host_log_rows, host_exit_reason and its spawn-watermark bound; the public-package import of Transport, CLIConnectionError and ProcessError), kernel/sdk_backend.py (the watermark, the launch-error carry and the host.exited-before-socket and host.never-served-socket rows in _host_transport_for, _file_refused_launch_context, _file_sdk_untested_row and its once-per-kernel-life memo _sdk_untested_reported, the tail exclusion in _record_launch_error), kernel/session_host.py (SDK_TESTED_VERSION, installed_sdk_version, error_chain, sdk_internals, sdk_mismatch_text, sdk_broken_install_text, SdkInternalsMismatch, the _process guard in _spawn, the causes field on the cli-spawn-failed row in run, main), tests/install-optional-deps.bats, tests/test_session_host_sdk_pin.py
added: 2026-09-18
pr:
tier: fix
offered:
closed:
---
kernel/session_host.py drives claude_agent_sdk._internal.transport.subprocess_cli.SubprocessCLITransport and its _process attribute, neither public, with no fallback, while bin/romp-sdk-setup upgraded the package unpinned: a release that moved one would have failed every hosted session launch with the install step none the wiser (the box admin hazard review of the merged pull-in, 2026-09-16). One constant, SDK_TESTED_VERSION in kernel/session_host.py (0.2.156, the version the host tests run against on the box), now declares the version the imports were verified against; the setup script reads that line and installs claude-agent-sdk==<it>, refusing to run with no line to read and refusing a venv that holds another version after the install; the host compares the version it imports (the package __version__, falling back to importlib.metadata) to the pin before the import, imports directly on a match, and on another version resolves each name inside a try and fails through the host-crashed record with both versions and the repin command when one is gone (the kernel launch error now carries that record, host_transport.host_exit_reason), or runs with one host.log row saying newer or older, filed as a problem row once per kernel life per version pair. Review round 1 added the launch-error tail exclusion for a host refusal, the host.exited-before-socket row, the ImportError narrowing and the split broken-install text; round 2 put the chain of types behind a spawn failure on its row and made the untested-version report once per kernel life; the closing check bounded the reason read on a spawn-time watermark of host.log (not the last host-started row, which a host that died writing nothing never wrote), filed the drift fact as its own host.sdk-untested row on both refused roads with the failure keeping its bare type and the version stated beside it without a remedy (the type-name allowlist is gone), added the host.never-served-socket row on the deadline road, and made the pin read the imported module first. Tests: six bats cases over a logging pip stub (red on the base, which passed an open upgrade; the pinned install, the missing-line and missing-file refusals, the symlink and readable-file guards, the executed verify step) and tests/test_session_host_sdk_pin.py, 41 cases (the helper with fake modules, a real host over a fake SDK site, the launch error over a stale stderr tail, the public-import fallback, the watermark through the real spawn road over a stale lease, the separate drift row and the deadline row, both relation guards, the module-first pin). The kernel side goes live at the next kernel restart; the setup script side at the next run of bin/romp-sdk-setup. An offer waits on the user word.
