---
title: The ten upstream lines the lazy-panes branch touched, eight modified at the head and two restored byte-identical with a fork line after each: the reveal column count, the deferred gear open, the layout-gated feed tab swap
status: candidate
where: kernel/kernel.py: _reveal_request (the signature gains cols=None, kept on the park; the `[reveal]` print, two lines, gains the cols field; upstream-authored 2026-09-08; compound provenance: the function is the fork's, 2026-08-08, two arguments, given boot= by the project on 2026-09-06 and its current four-argument shape on 2026-09-08), the /reveal route in Handler (the call passes cols=cols; upstream-authored 2026-09-08), _LANDING_REVEAL_JS (the land() reveal post's body line `var body={sid:sid,wid:wid(),via:via};if(boot)body.boot=true;`, upstream-authored 2026-09-08: joined with the fork's cols statement in PR 821's pass 5 (the author's label) and restored byte-identical by that pass's verify fixer, with the fork's `var cc=cols();if(cc>0)body.cols=cc;` line on its own line after it, the shape the sPend line below has), _LANDING_SETTINGS_JS (the gear opener's load listener pair: armed once for the element's life behind `sArmed` and posting the ask the tap that fetched recorded in `sOpen` instead of the first tap's open; upstream-authored 2026-09-11; the `var sPend=false;` line itself stands unedited since PR 821's pass 6 (the author's build label of 2026-09-20, not a reviewer's round), with the fork's declarations on the line after it), the browseFiles feed arm (the `__rompMobileTab('feed')` call gated on `window.__rompMobileOn()`; fork-origin, taken by the project: the command's tail names the viewFile twin's commit of 2026-08-14, the line's first appearance, which the project removed on 2026-08-15; the copy at the line's site, the browseFiles arm's, is the fork commit of 2026-08-20, the date filed here), _landing (the `f-files` iframe markup token, `src` to `data-src`; fork-origin 2026-09-10, taken by the project)
added: 2026-09-20
pr:
tier: fix
offered:
closed:
---
Ten lines of kernel/kernel.py that exist byte-identically in upstream/main were touched by fork PR 821 (the lazy panes): eight are modified at the head, and two were modified at one pass and restored byte-identical at a later one with a fork line inserted after each, so they stand unedited at the head and this offer carries them with their fork lines: the gear opener's `var sPend=false;` (restored in pass 6, the fork's `var sArmed=false,sOpen=null;` after it) and the reveal script's `var body={sid:sid,wid:wid(),via:via};if(boot)body.boot=true;` (joined with the fork's cols statement in pass 5, restored by that pass's verify fixer, the fork's `var cc=cols();if(cc>0)body.cols=cc;` after it); every pass label here is the author's build label, not a reviewer's round. THE METHOD (the reviewer's round-5 ruling): the population is derived by per-commit removal, never by the head's diff, since a line restored before the head leaves no trace there (the head-diff derivation this entry first stated found nine and could not see the reveal body line). Over every non-merge branch commit touching kernel/kernel.py since the merge base with origin/main, take the commit's removed lines against its parent, drop blank and brace-only lines, and test each byte-identical (the whole line, indentation included) against upstream/main's kernel.py; a line no branch commit ever removed is excluded (`userSwitch`, which the fork declares again after the project's line, is the control). The disambiguation against a coincidental duplicate is the same previous and next non-blank non-brace line in the parent and in upstream: nine of the ten match, the exception the f-files markup line, whose neighbours differ on both sides (a fork div before it; in upstream the `_data_pane_markup` splice after it), each of the ten occurring once in upstream. Re-run with git alone from a checkout with the fork as origin and the project as upstream (the script that also reads neighbours, provenance and language is outside the repo; this pipeline reproduces the population): `base=$(git merge-base HEAD origin/main); for c in $(git log --no-merges --format=%h $base..HEAD -- kernel/kernel.py); do git diff -U0 --output-indicator-old='~' "$c^" "$c" -- kernel/kernel.py | grep '^~' | cut -c2-; done | grep -vE '^[[:space:]{}()\[\];,]*$' | sort -u > removed.txt; git show upstream/main:kernel/kernel.py | grep -xF -f removed.txt | sort -u` (at 3ca8ecaef, the head the reviewer's round 5 ruled, 41 commits and 206 distinct removed lines; at 2828c25de, the pass-7 head, 46 and 245 (45 and 244 at 034c88ef9, an earlier pass-7 commit); ten byte-present at each, the same ten: every later kernel.py commit on the branch moves the first two figures and leaves the ten, so each figure here names the head it was measured at). The introducing commit, `git log upstream/main -S'<line>' --format='%h %an %ad' --date=short -- kernel/kernel.py | tail -1`, decides upstream-authored against fork-origin by its author field: a project maintainer's commit is upstream-authored, a fork commit the project took is fork-origin. The split from that one derivation: eight upstream-authored, five by the project's commit of 2026-09-08 (`_reveal_request`'s signature, its `[reveal]` print, two lines, the /reveal route's call, and the reveal body line) and three by its commit of 2026-09-11 (`var sPend=false;` and the gear opener's load listener pair); two fork-origin lines the project took, the browse arm's `__rompMobileTab('feed')` (the browseFiles arm's copy of 2026-08-20, the date filed; the command's tail is the viewFile twin's commit of 2026-08-14, the line's first appearance, which the project removed on 2026-08-15, so three commits name the line and the tail is the twin's) and the f-files markup line (2026-09-10). By language: four Python lines (the signature, the print's two lines, the route's call), five JavaScript lines in the embedded scripts (four in `_LANDING_SETTINGS_JS`: the sPend declaration, the listener pair, the browse arm's tab swap; one in `_LANDING_REVEAL_JS`: the reveal body line), and one markup line in a Python string literal inside `def _landing()` (the f-files iframe). The fold cost, the upstream-authored lines modified at the head, is six: the listener pair, the signature, the print's two lines, the route's call; the two restored lines cost nothing, and the two fork-origin lines are outside it. The four Python lines' provenance is compound: `_reveal_request` was introduced by a fork commit of 2026-08-08 in a two-argument form and given its current shape by the project (2026-09-06, 2026-09-08), so the lines as they stand are the project's and the function the fork's. Each is a plain fix a maintainer would take: the reveal request carries the chat column count the shell posts so a parked reveal can name the one full for a one-column window; the gear opener arms its load listener once and opens at the LATEST tap's ask (before, every re-fetch after a dead document opened at the first tap's tab and section, and a listener per fetch was armed); the feed browse arm swaps tabs on the phone layout alone (on the desktop grid the swap persisted the remembered phone tab from a desktop gesture); the Files pane is served with data-src so it loads on its first show. Filed so the divergence is temporary: once offered and taken, the fold meets these lines as the project's own and the fork stops carrying them. Tier fix: each has a test that is red before it (the reveal body pins in `tests/test_kernel_webpush.py`, the two-line pin on the converted body line included, RelayArms in `tests/test_pane_state_broadcast.py`, the gated-switch pins in `tests/test_files_pane.py` and `tests/test_error_center.py`, the `f-files` pins in `tests/test_files_pane.py` and `tests/test_kernel_pane_rail.py`). The offer waits on the user's word; it goes with or after PR 821 lands on the fork.
