---
title: The nine upstream lines the lazy-panes branch modifies: the reveal column count, the deferred gear open, the layout-gated feed tab swap
status: candidate
where: kernel/kernel.py: _reveal_request (the signature gains cols=None, kept on the park; the `[reveal]` print, two lines, gains the cols field; upstream-authored 2026-09-08; compound provenance: the function is the fork's, 2026-08-08, two arguments, given boot= by the project on 2026-09-06 and its current four-argument shape on 2026-09-08), the /reveal route in Handler (the call passes cols=cols; upstream-authored 2026-09-08), _LANDING_SETTINGS_JS (the gear opener's load listener pair: armed once for the element's life behind `sArmed` and posting the ask the tap that fetched recorded in `sOpen` instead of the first tap's open; upstream-authored 2026-09-11; the `var sPend=false;` line itself stands unedited since PR 821's pass 6 (the author's build label of 2026-09-20, not a reviewer's round), with the fork's declarations on the line after it), the browseFiles feed arm (the `__rompMobileTab('feed')` call gated on `window.__rompMobileOn()`; fork-origin 2026-08-20, the browseFiles arm's copy (a viewFile twin of 2026-08-14 was removed on 2026-08-15), taken by the project), _landing (the `f-files` iframe markup token, `src` to `data-src`; fork-origin 2026-09-10, taken by the project)
added: 2026-09-20
pr:
tier: fix
offered:
closed:
---
Nine lines of kernel/kernel.py that exist byte-identically in upstream/main are touched by fork PR 821 (the lazy panes): eight modified, and the gear opener's `var sPend=false;` declaration, restored byte-identical in PR 821's pass 6 (the author's label) with the fork's two names declared on the line after it, a line this offer carries with the listener pair. Derived by content at the post-merge head (every removed line of the kernel.py diff against the merge base, tested for a byte-identical line in upstream/main with the same neighbours; the introducing commit in upstream history names the author): four Python lines and five JavaScript lines, seven upstream-authored and two the project took from the fork. The four Python lines' provenance is compound: `_reveal_request` was introduced by a fork commit of 2026-08-08 in a two-argument form and given its current shape by the project (2026-09-06, 2026-09-08), so the lines as they stand are the project's and the function the fork's. Each is a plain fix a maintainer would take: the reveal request carries the chat column count the shell posts so a parked reveal can name the one full for a one-column window; the gear opener arms its load listener once and opens at the LATEST tap's ask (before, every re-fetch after a dead document opened at the first tap's tab and section, and a listener per fetch was armed); the feed browse arm swaps tabs on the phone layout alone (on the desktop grid the swap persisted the remembered phone tab from a desktop gesture); the Files pane is served with data-src so it loads on its first show. Filed so the divergence is temporary: once offered and taken, the fold meets these lines as the project's own and the fork stops carrying them. Tier fix: each has a test that is red before it (the reveal body pins in `tests/test_kernel_webpush.py`, RelayArms in `tests/test_pane_state_broadcast.py`, the gated-switch pins in `tests/test_files_pane.py` and `tests/test_error_center.py`, the `f-files` pins in `tests/test_files_pane.py` and `tests/test_kernel_pane_rail.py`). The offer waits on the user's word; it goes with or after PR 821 lands on the fork.
