---
title: Markdown viewer Slice 7: every failure says what happened (a render that throws shows the file's text as written under a line naming the error, with Rendered still chosen and the seam's mode() answering raw; a figure that fails to load wears a label naming its source; every failure pane fires the paint hooks and the seam exposes error(), so the Comments panel's wait for a reload ends at the paint that failed instead of at a 15 s deadline; both save doors put a BOM file's EF BB BF back, keyed on the disk bytes each reads, and the change marks and the editor's seed map its offsets so the marks paint; a Latin-1 file's note bar says why Edit is off; an empty file says so in place of its text; Raw rows split on CR, CRLF and LF, the panel's row counter and the viewer's scroll agreeing on a CR-only file and the editor's refusal over pending changes covering it)
status: candidate
where: fork branch `mdviewer-s7`, cut from e6aeb1138, the head of `mdviewer-s6` (Slice 6, fork PR 753) when the build began, every commit self-contained so that the branch could rebase onto the fork's main once that PR landed, and rebased there after the build's consolidation, onto 462ad3ccf, the merge of that PR, its base since (e6aeb1138 and 462ad3ccf differ, by Slice 6's manager review rounds 2 and 3, in nine of the files the branch changes, ui/webview/file-view.ts, ui/webview/real-viewer-leg.ts, ui/webview/file-view.test.ts, ui/webview/file-view-links.test.ts, ui/webview/file-view-outline.test.ts, ui/webview/file-view-reload.test.ts, kernel/kernel.py, tests/test_kernel_preview.py and plans/markdown-viewer.md, so a run a commit message records over e6aeb1138 ran over a tree without those rounds' hunks in them; the rebase re-minted every commit, so a sha a commit message names for a fails-before tree other than e6aeb1138 is the pre-rebase lineage's, and the plan's Slice 7 note lists each such sha with its build unit, the unit the branch commit's subject carries; `git range-diff` over the two lineages pairs the commits one for one, eighteen pairs the same patch, A3's differing in ui/webview/file-view-reload.test.ts's hunk headers alone and A5's re-resolved over those rounds' hunks in ui/webview/file-view-links.test.ts, ui/webview/file-view-outline.test.ts, ui/webview/file-view.test.ts and ui/webview/real-viewer-leg.ts, its ui/webview/file-view.ts hunks differing in context alone): ui/webview/file-view.ts (mdBlock without its catch and its rendered flag, both link passes on every render; both viewers' renderBody wrapping build and swap in one try with renderFell, renderFellLine and the exported RENDER_FELL, mode() answering raw while renderFell is set; error() a required member of FileViewActionCtx with viewError set at the fetch catch and imgFailed and cleared at every content paint, the fetch catch firing fireRendered after syncOutline and before rearmDiskBar; armFigureLabels with its capture-phase error and load listeners, figureAnchor, figureLabelAfter, figureLabelText, FIGERR_MARK and the exported FIGURE_FAILED, headingWords skipping the mark; the Verdict's notUtf8 and the text landing's noteBar(LATIN1_NOTICE) before landTarget; EMPTY_FILE, emptyFileLine and the prepend in both viewers' text paints; RAW_ROW_SPLIT in wrapNumberedHtml and codeBlock's gutter count, scrollToOffset through anchor-map's rawRowForOffset with the exact fallback count, trackedRefusal keyed on any CR with the exported CR_REFUSAL in place of the closure const CRLF_REFUSAL; the review's round 1: spendHeading and the URL viewer's landFragment returning while renderFell is set, scrollToSourceOffset reading mode() for its branch, dropLatin1Line after settleDiskBar at every landing whose header does not say 0, failedSource, shownSource, absUrl and linkAround with FV_SRCSET kept by rewriteFigureSrcs beside a rewritten srcset, figureLabelText's formula over them and figureAnchor climbing a link holding the figure alone, the landing hold's comment re-aimed; the review's round 2: fellMessage and MARKED_REPORT_TAIL, both viewers' catches recording renderFell after the fallback swap through them, FIGURE_NO_SOURCE in figureLabelText's formula for a figure that names no source, dropLatin1Line called in the fetch chain's catch after closeOutline, the landing hold's comment naming the passes that throw through and not the hooks, failedSource's header on a URL document's resolved candidates; the review's round 3: pickFormat, the one helper behind the bar's Rendered and Raw clicks and the seam's setMode, raising LATIN1_NOTICE again when its paint put the text back over a failure pane, and viewError cleared after the text paint's swap stands, before the folds' restore, rather than before the pass), ui/webview/md-sanitize.ts (setMdSanitizer, a node-only seam read through purifier() by sanitizeMd and the hooks install), ui/webview/real-viewer-leg.ts (openViewer's serve, before, utf8 and waitFor options, the per-path header table, the bundle exporting the heal's three functions and anchor-map's rawRows and rawRowForOffset), ui/webview/styles.css and ui/webview/feed.css (the `.fileview-md .fv-figerr` rule in the gate's shape with the error dress's ink and one print line beside the gate's, byte-equal; the review's round 4: `overflow-wrap: anywhere` on the `.fileview-err` rule, byte-equal, so a failure message's long unbroken token breaks inside the line's box instead of widening the body at phone width), ui/webview/file-comments.ts (bytesLanded clearing the bytes row at the landing that shows the status's text, the consolidation pass, over a content paint alone since the review's round 1, which added bytesPaneGone for a format click's repaint of the earlier text under a standing failure row, superseded in the review's round 2 by syncFailedRow over the failedRow record bytesFailed writes, the failure row read against every later paint, the editing early return included, and mapped the panel's three remaining reads of the host's offsets on a BOM file by the status's bom, reveal's scroll and cue through one from, renderChangeCard's line suffix and changeView's groups over the host's text with the U+FEFF put back; paintAll reading ctx.error() after the editing early-return and clearLanding, bytesFailed with the exported BYTES_FAILED and its tail, the deadline's comment re-aimed to an older kernel alone; paintChanges handing the painters curFrom - off and curTo - off with off = s.bom ? 1 : 0; begin() handing the editor viewRecords(seed.records, off) while editSeed.records keeps the host's; the review's round 4: reloadOut and reloadView, the one door for every re-fetch the panel asks (fcreload, askReload, the re-read after an edit over moved bytes), paintAll's head standing down while such a fetch is out and the onRendered hook's paint branch clearing the record, refresh's end leaving the "bytes" busy mark to an armed wait; its round 5: bytesLate's doc comment naming two pre-existing edges, the deadline armed afresh by every status that lands while the wait is up and the row's tail not re-read against a pane landing after it, recorded in the plan's round 3 and round 5 paragraphs and routed, no behaviour change), ui/webview/anchor-map.ts (fv-figerr in CONTROL_CLASSES; rawOffsetToLine counting CRLF, a lone CR and LF, its doc and the Raw section's split comment re-aimed, its body two native searches since the review's round 1 with the same row at every offset; the failed figure's label left out of the Rendered pairing's top-level nodes, isFigureLabel in analyzeRendered's holdsContent, the consolidation pass), ui/webview/reader-place.ts (fv-figerr in CONTROL_CLASSES), ui/webview/file-comments-model.ts (lineStartOffset counting CRLF, a lone CR and LF as rawOffsetToLine does, the review's round 1), tools/file-comments-host.mjs (doSave computing content and suggestions once, after checkIsText and before checkContentText, when file.text begins with U+FEFF and a.content does not, every later read using them; the text contract above readFile and doSave's header restated; readFile's header recording, in the review's round 1, that the host's U+FFFD decode of a non-UTF-8 file and the kernel's latin-1 fallback differ at every non-ASCII byte, pre-existing), kernel/kernel.py (_save_file re-prepending EF BB BF when cur begins with it and content does not, the three bytes counted against _TEXT_MAX_BYTES in a second comparison, prior gaining "written"; _edit_log_after diffing prior["bytes"] against prior["written"]: Python the kernel RUNS, so the live dashboard's kernel picks the change up only at the live kernel's restart, the manager's step after the landing; the HEAD route, the frame handler, the save verb's door and the landing shell are untouched; the review's round 4: a comment at the BOM rule naming the double-BOM shape, pre-existing, recorded in the plan's item 4); tests ui/webview/file-view-seam.test.ts (the sanitizer stand-in with sanitizeFails and sanitizeAnswers, the disk entry's utf8, layRows on the three-ending split; ten new cases, three more in the review's round 1, three more in its round 2, two more in its round 3), ui/webview/file-view.test.ts (four new source pins, one more in the review's round 1, one more in its round 2, pins re-aimed, five re-aimed and four added in its round 3), ui/webview/file-view-links.test.ts, ui/webview/file-view-place.test.ts (two render catch pins re-aimed again in the review's round 2 and one in its round 3), ui/webview/md-url-view.test.ts (its URL renderBody tail pin re-aimed again in the review's round 2), ui/webview/styles-fileview-err-sizes.test.ts (the pane's chain pin allowing the Latin-1 line's drop, the review's round 2) and ui/webview/md-sanitize.test.ts (pins re-aimed; one new in md-sanitize, its seam pin sweeping the production sources since the review's round 1), ui/webview/file-view-landing-throw-browser.test.ts (both scenes re-aimed), ui/webview/file-view-place-memory.test.ts and ui/webview/file-view-outline.test.ts (the stand-in installed, the textContent intercepts retired, the marked import dropped), ui/webview/file-view-text-size.test.ts (the hook's one-line pin re-aimed in the review's round 4), ui/webview/file-view-edit-events.test.ts, ui/webview/file-view-edit-races.test.ts, ui/webview/file-view-undo-landed.test.ts, ui/webview/file-view-undo-landed-ack.test.ts, ui/webview/file-view-pdf-chunk-latch.test.ts, ui/webview/file-view-pdf-frame.test.ts, ui/webview/file-view-pdf-lifecycle.test.ts and ui/webview/file-comments-save-busy-viewer.test.ts (the stand-in installed), ui/webview/file-view-tracked-edit.test.ts (three new), ui/webview/file-view-reload.test.ts (five scenes extended; the review's round 2 adding the failed Reload's 413 scene, the stub's GET answering a status with a body), ui/webview/file-view-figure-error.test.ts (the formula pin re-aimed in the review's round 2), ui/webview/file-view-figure-empty-source.test.ts (new in the review's round 2), ui/webview/file-view-figure-error-browser.test.ts, ui/webview/file-view-failures-browser.test.ts (its panel scene the consolidation pass's) and ui/webview/file-view-raw-rows-browser.test.ts (new; the review's round 1 adding two node cases and a picture-and-srcset browser scene to the figure-error suites and the URL viewer's fallen-render fragment scene to the failures leg), ui/webview/file-view-fell-wrap-browser.test.ts (new in the review's round 4: the RENDER_FELL line with a 120-character token fits at 380 px on the chat modal, the feed modal and the Files pane), ui/webview/fileview-parity.test.ts (the new head; the `.fileview-err {` head in the review's round 4), ui/webview/anchor-map.test.ts (three new, two more in the review's round 1 over the viewer's grid; the RAW_ROW_SPLIT pin, the replica of wrapNumberedHtml on the three endings with the older Raw grid cases keeping the LF-only grid through wrapNumberedHtmlLf), ui/webview/anchor-map-cells-browser.test.ts (the cells' text read with the failed picture's label aside, its presence pinned), ui/webview/render-sanitize.test.ts (the sanitizer pin re-aimed to purifier().sanitize(), ui/webview/anchor-map-fallback-markup.test.ts (the mirror list), ui/webview/md-config-figure-gate-place.test.ts (two scenes), ui/webview/file-comments.test.ts (the member list, the swap-line pin re-aimed to the try, again in the review's round 2 to the three-line catch and again in its round 3 to the clear after the empty-file line, the setMode pin re-aimed to pickFormat in round 3, mode()'s closure; the scrollToOffset pin re-aimed to rawRowForOffset and the RAW_ROW_SPLIT fallback), ui/webview/file-comments-changes-review2.test.ts (the failing-reload scene, in the browser's order: the status answered before the fetch lands; two scenes in the review's round 1, the decode pane's row and the repaint's tail; four in its round 2, the failure row against a landing of newer text, a pane back over the flipped row, a pane with other words and Edit clicked over the pane; two in its round 4, the row's Reload over a pane whose fetch never answers reaching the deadline row and the poll's fetch on a new mtime over a standing pane, three scenes re-aimed to reloadOut), ui/webview/file-comments-changes-review.test.ts and ui/webview/file-comments-about-fixes.test.ts (the BOM cases flipped), ui/webview/file-comments-editing-races.test.ts (one new), ui/webview/file-comments-reveal-landing.test.ts (the stand-in on the three-ending split, one new; the scrollToOffset pin re-aimed to rawRowForOffset and the RAW_ROW_SPLIT fallback; three BOM cases in the review's round 1; the pass's first-line pin re-aimed in its round 2), ui/webview/file-comments-model-line-start.test.ts and ui/webview/anchor-map-raw-offset-to-line.test.ts (new in the review's round 1), and the 112 other files of the stub sweep, `error: () => null,` beside the mtimeNs stub of every hand-written seam ctx, one line each: ui/webview/feed-css-margin-fit.test.ts, ui/webview/feed-css-margin-footer-rule.test.ts, ui/webview/feed-css-margin-footers.test.ts, ui/webview/feed-css-margin-leader.test.ts, ui/webview/feed-css-saved-line-head-dress.test.ts, ui/webview/file-comments-about-review2.test.ts, ui/webview/file-comments-about.test.ts, ui/webview/file-comments-anchors-unsure-rendered.test.ts, ui/webview/file-comments-anchors-unsure.test.ts, ui/webview/file-comments-anchors.test.ts, ui/webview/file-comments-arrivals-about.test.ts, ui/webview/file-comments-arrivals-browser.test.ts, ui/webview/file-comments-arrivals-fixes-browser.test.ts, ui/webview/file-comments-arrivals-fixes.test.ts, ui/webview/file-comments-arrivals-review2-browser.test.ts, ui/webview/file-comments-arrivals-review2.test.ts, ui/webview/file-comments-arrivals.test.ts, ui/webview/file-comments-behavior.test.ts (the pass's first-line pin re-aimed in the review's round 2, the hook's one-line pin in its round 4), ui/webview/file-comments-block-paint.test.ts, ui/webview/file-comments-busy-retry-fence.test.ts, ui/webview/file-comments-changes-review3.test.ts, ui/webview/file-comments-changes.test.ts (the reveal order pin re-aimed in the review's round 1), ui/webview/file-comments-composer-chat-nav-chord.test.ts, ui/webview/file-comments-composer-fixes-browser.test.ts, ui/webview/file-comments-composer-fixes.test.ts, ui/webview/file-comments-composer-review.test.ts, ui/webview/file-comments-composer-shell-chord.test.ts, ui/webview/file-comments-composer.test.ts, ui/webview/file-comments-crop-keep.test.ts, ui/webview/file-comments-crop-wait.test.ts, ui/webview/file-comments-editing-cards.test.ts, ui/webview/file-comments-editing-landed.test.ts, ui/webview/file-comments-editing-moved-latch.test.ts (the edit-end re-read's pin re-aimed in the review's round 4), ui/webview/file-comments-editing-round3.test.ts (the changeGroups call pin re-aimed in the review's round 1), ui/webview/file-comments-figure-page.test.ts, ui/webview/file-comments-filter-fixes.test.ts, ui/webview/file-comments-filter-review.test.ts, ui/webview/file-comments-filter-saved-line.test.ts, ui/webview/file-comments-filter.test.ts, ui/webview/file-comments-focus-audit.test.ts, ui/webview/file-comments-focus-browser.test.ts, ui/webview/file-comments-focus-review.test.ts, ui/webview/file-comments-focus-verify-2.test.ts, ui/webview/file-comments-focus-verify.test.ts, ui/webview/file-comments-focus.test.ts, ui/webview/file-comments-follow.test.ts, ui/webview/file-comments-hint-order.test.ts, ui/webview/file-comments-host-tiebreak-review-3-panel.test.ts, ui/webview/file-comments-inline-review.test.ts, ui/webview/file-comments-inline-toggle.test.ts, ui/webview/file-comments-keyboard-offer.test.ts, ui/webview/file-comments-margin-browser.test.ts, ui/webview/file-comments-margin-fixes-browser.test.ts, ui/webview/file-comments-margin-fixes.test.ts, ui/webview/file-comments-margin-image-browser.test.ts, ui/webview/file-comments-margin-image-pad.test.ts, ui/webview/file-comments-margin-review-browser.test.ts, ui/webview/file-comments-margin-review.test.ts, ui/webview/file-comments-margin.test.ts, ui/webview/file-comments-markclick-controls.test.ts, ui/webview/file-comments-markclick.test.ts, ui/webview/file-comments-overlap.test.ts, ui/webview/file-comments-page-states.test.ts, ui/webview/file-comments-pages.test.ts, ui/webview/file-comments-paint-offer.test.ts, ui/webview/file-comments-panel.test.ts, ui/webview/file-comments-pdf-pictures.test.ts, ui/webview/file-comments-region-tied.test.ts, ui/webview/file-comments-regions-review-2.test.ts, ui/webview/file-comments-regions-review-3.test.ts, ui/webview/file-comments-regions-review-4.test.ts, ui/webview/file-comments-regions-review.test.ts, ui/webview/file-comments-regions.test.ts, ui/webview/file-comments-reply-keep-browser.test.ts, ui/webview/file-comments-reply-keep.test.ts, ui/webview/file-comments-reply-move.test.ts, ui/webview/file-comments-reply-place.test.ts, ui/webview/file-comments-reply-review2-browser.test.ts, ui/webview/file-comments-reply-review2.test.ts, ui/webview/file-comments-resolve-answered-fixes.test.ts, ui/webview/file-comments-resolve-answered-review2.test.ts, ui/webview/file-comments-resolve-answered.test.ts, ui/webview/file-comments-reveal-arms-focus.test.ts, ui/webview/file-comments-reveal-one-pass.test.ts, ui/webview/file-comments-reveal-title.test.ts (a BOM case in the review's round 1), ui/webview/file-comments-review-fixes-3.test.ts, ui/webview/file-comments-review-fixes.test.ts, ui/webview/file-comments-save-busy.test.ts, ui/webview/file-comments-seen-fixes.test.ts, ui/webview/file-comments-seen-review2-browser.test.ts, ui/webview/file-comments-seen-review2.test.ts, ui/webview/file-comments-seen-review3-browser.test.ts, ui/webview/file-comments-seen-review3.test.ts, ui/webview/file-comments-send-note.test.ts, ui/webview/file-comments-send-resolves.test.ts, ui/webview/file-comments-send-seen-browser.test.ts, ui/webview/file-comments-send-seen.test.ts, ui/webview/file-comments-tiebreak-recourse.test.ts, ui/webview/file-comments-tiebreak-region.test.ts, ui/webview/file-comments-tiebreak-shown.test.ts, ui/webview/file-comments-tiebreak-touch.test.ts, ui/webview/file-comments-tiebreak.test.ts, ui/webview/file-comments-todo-choices-review.test.ts, ui/webview/file-comments-todo-choices.test.ts, ui/webview/file-comments-todopick-focus.test.ts, ui/webview/file-comments-unpaint-normalize.test.ts, ui/webview/github-link.test.ts, ui/webview/md-config-fold-paint-browser.test.ts, ui/webview/md-config-goto-closed-details-browser.test.ts, ui/webview/styles-fc-arrivals-line-fit.test.ts, ui/webview/styles-fc-margin-fit.test.ts, ui/webview/styles-fc-margin-footer.test.ts; tools/file-comments-host-save.test.mjs (three new), tools/file-comments-host-untouched.test.mjs (one pin re-aimed), tools/file-review-plan-anchors-states.test.mjs (the hook's one-line pin re-aimed in the review's round 4); tests/test_savefile.py (six new), tests/test_kernel_file_comments_save.py (two new e2e cases), tests/test_kernel_file_comments_hardening.py (one pin re-aimed), tests/test_kernel_preview.py and tests/test_file_view.py (record pins), tests/test_guide_files_failures.py (new; the label's text pin re-aimed in the review's round 1, the catch's and the label's pins in its round 2, the Latin-1 raise count in its round 3); docs/guide.md (the Files section's How a markdown file reads, Your place in the file, Figures and Edit paragraphs, each by appended sentences, the Edit paragraph's CRLF sentence widened to CR or CRLF), plans/markdown-viewer.md (the Slice 7 build note), tests/test_markdown_viewer_plan_note_slice7.py (new in the review's round 3, the note held to four of its own rules; its stage rule reading a round 3 count since its round 4) and this entry
added: 2026-09-14
pr:
tier: feature
offered:
closed:
---
Upstream ships the same viewer, the same Comments panel, the same file-comments host and the same kernel save door, so every item applies there once the slices before it have landed: a markdown file whose render throws shows its text as written, the way Raw shows it, under a line that says so and names the error, with Rendered still chosen and the viewer's seam answering raw for that body, where before the source was written into the Rendered box as one unannounced paragraph; a figure the browser cannot fetch or decode wears a label naming the fact and the figure's source where it drew a wordless broken-image glyph; a fetch that is refused or fails fires the paint hooks like any other paint and the seam's new error() names the pane, so the Comments panel's wait for a reload it asked for ends at that paint, in the seam's words with a Reload, where before its loader stood a 15 s deadline out; a file whose bytes begin EF BB BF keeps them through a save from the editor and from the panel, the change marks paint on it and the editor's seed sits on the right characters, where before a save dropped the first three bytes and the marks stayed card-only; a file the kernel decoded as Latin-1 says in the note bar why Edit is off; an empty file says so in place of its text; and a file with CR or CRLF line endings gives one Raw row per line, with the viewer's scroll, the panel's landing cue and the row map agreeing, and the editor refusing pending changes on it in words that name both endings. Built as Slice 7 of plans/markdown-viewer.md on Slice 6's head (fork PR 753, 2026-09-13-markdown-viewer-slice6), so the offer stacks on the seven slices before it, each with its own entry (Slice 1 #390, Slice 2 #396, Slice 3 #407, Slice 4 #707, Slice 5 #749, Slice 8 #752, Slice 6 #753). The store format is untouched: the host keeps a BOM file's U+FEFF in its text as the vendored CLIs do and the status's bom bit stays the offset reconciler, so a BOM file's sidecar keeps its coordinates and its fingerprint, and the plan's older clause that the host strips the BOM is superseded by that mechanism (the plan's Slice 7 note records the reading). The kernel's Python changes in _save_file and the edit log (the BOM put back, keyed on the bytes the save read, and the entry diffed over the text as written), so a deployment reads the change only after its kernel restarts; the HEAD route, the frame handler and the landing shell are untouched. The seam FileViewActionCtx gains a REQUIRED member error(): string | null, so a host that builds a ctx by hand adds it (this repo's 118 hand-written test stubs were swept in one commit); mdBlock no longer catches, so a throw from marked, the sanitizer or a DOM pass reaches renderBody's catch, which paints the line and the Raw rows, and from a button's click no longer surfaces as an uncaught exception with the old body standing; md-sanitize.ts gains a node-only setMdSanitizer seam, since DOMPurify without a window.document is the bare factory and every node suite that drives the real viewer over a markdown file had lived on the catch. The kernel's X-Romp-Text-Utf8 header and its Latin-1 re-encoding are unchanged and pinned; no request carries a bom argument.
