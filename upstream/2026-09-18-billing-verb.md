---
title: romp billing: a session's billing from the shell
status: candidate
where: bin/romp, cli/perf_public.py (HTTP_ROUTES), docs/reference.md, kernel/credentials.py (_read_settings), kernel/kernel.py (_BILLING_CHOICES, _BILLING_NOT_SDK, _BILLING_PARK_WORDS, _BILLING_WORDS, _PERF_HTTP_ROUTES, _apply_pending_ops, _auth_both, _auth_refusal, _billing_default, _billing_far_answer, _billing_read, _billing_refusal, _billing_request, _drive, _drop_parked_auth, _park_op_locked, _park_reason, _save_pending_ops, _sdk_locked, _set_auth_or_park, _set_auth_or_park_verdict, do_GET, do_POST), kernel/sdk_backend.py (RegUnreadable, RenameRefused, STEP_LIVE_FIELDS, StepWrite, __init__, _ack_cmd_chip, _ask_parked_pick, _auth_live_fields, _clear_served_auth_pending, _connect_landed, _default_label, _deliver_rename_ping, _describe_moved, _do_set_mode, _finish_move, _follow_default, _follow_default_guarded, _follow_default_unlanded, _hold_write, _log_rename_compensation, _mirror_auth, _mirror_auth_pending, _mirror_pick, _note_auth_source, _options, _publish_name, _recover_picked_pending_at_init, _refuse_default_clear, _refuse_pick_write, _refuse_rename, _restore_step_writes, _revert_mode_record, _revert_rename_record, _sched_tool_hook, _served_by_connect, _stamp_compose, _step_fields_view, _step_write, _stop_hook, _update_reg, _update_reg_derived, _update_reg_dropping, _update_reg_if_holds, auth_apply_outlook, auth_relaunch_staggered, billing_view, deliver_lost_wakeups, explicit_default_pick, follow_default_auth, fork, note_live, pop_auth_refusal, promote_thread, record_reads, rename, set_auth, set_auth_followers, set_auth_guarded, spawn, turn_open, write_name), tests/fixtures/fake_claude.py, tests/romp-headless.bats, tests/test_billing_route.py, tests/test_cap_switch_offer.py, tests/test_cli_billing_kernel.py, tests/test_comment_threads.py, tests/test_credentials.py, tests/test_headless_verbs_help.py, tests/test_kernel_cmd_gesture.py, tests/test_kernel_effort_reconnect.py, tests/test_kernel_headless_ops.py, tests/test_scheduled_sessions.py, tests/test_sdk_backend.py, tests/test_sdk_rename_ping.py, tests/test_session_auth.py, tests/test_session_emoji.py, upstream/2026-09-18-billing-verb.md
added: 2026-09-18
pr: 813
tier: feature
offered:
closed:
---
Until this only the dashboard could change which account a session bills. `romp billing <session> <pick>` posts the same setAuth op the Billing menu posts (a name, a sid or an attached host's session, forwarded as /end forwards), so the session reconnects at its next quiet moment; `--now` cuts the turn in flight after the pick is recorded, so the cut turn's settle arms the reconnect (no new arm path; a compaction is refused, not cut); `default` clears the session's own pick, a value the dashboard has no op for, through the follower walk's per-session move; `--all-following <pick>` writes a pick on every live follower of the machine default through that walk, with no /auth chip; the bare verb prints the launched side, the pick and the machine default, for a dormant session from its reg. The CLI and the kernel routes are upstream's code; the lab test runs the real bin/romp against a hermetic kernel with the fake CLI and skips without the SDK venv. The `where:` line is derived from the whole PR diff and regenerated at the head of each round that changes the diff; regenerated at round 6's sixteenth commit (2026-09-21; the names file's publications under the record lock and the names lock add `_finish_move`, `_publish_name`, `_update_reg_dropping`, `fork`, `promote_thread`, `spawn` and `write_name` under kernel/sdk_backend.py and `_sdk_locked` under kernel/kernel.py, and tests/test_kernel_headless_ops.py and tests/test_session_emoji.py join the files); before that at round 6's fifteenth commit (2026-09-21; the rename road's one hold and the armed set's derived writes add `_log_rename_compensation`, `_update_reg_derived`, `deliver_lost_wakeups`, `_sched_tool_hook` and `_stop_hook` under kernel/sdk_backend.py, and tests/test_scheduled_sessions.py joins the files); before that at round 6's fourteenth commit (2026-09-21; the rename record write's compare-and-swap adds `RenameRefused` (a module-level class), `_deliver_rename_ping`, `_refuse_rename` and `_update_reg_if_holds` under kernel/sdk_backend.py; tests/test_sdk_rename_ping.py and tests/test_kernel_effort_reconnect.py were already files); before that re-derived at round 6's thirteenth commit (2026-09-21; the rename door's read order and the compensation's residual list touch rename and _revert_rename_record, both already named, and tests/test_sdk_rename_ping.py, already a file: the line is unchanged) and at its twelfth (the rename compensation's stand-down: the same two names and the same file, the line unchanged); last regenerated at round 6's tenth commit (2026-09-21; the round's own verifiers' pass found rename's compensation, the one restore site the ninth commit's population missed, and the fix adds _revert_rename_record and rename under kernel/sdk_backend.py and tests/test_sdk_rename_ping.py to the files); before that at round 6's ninth commit (2026-09-21; the reviewer's ruling on the guard's restore adds STEP_LIVE_FIELDS, _describe_moved, _do_set_mode, _restore_step_writes, _revert_mode_record, _step_fields_view, _step_write and note_live under kernel/sdk_backend.py, and a blank or comment-only touched class-body line names no class); before that at round 6's eighth commit, the ledger line (2026-09-21; the seventh commit, the record-write pin, had added tests/test_kernel_effort_reconnect.py to the diff and the line, set at round 6's cluster A commit on 2026-09-20, lacked that one file; the round-5 rules-1 and extra5-1 had found the line stale before cluster A by the three names round 4 had added and never re-derived). The method: the merge-base with main after round 6's merge of main, `git merge-base origin/main HEAD`, is the base; `git diff --name-only <base>..HEAD` gives the files, and for each Python file outside tests/ the top-level definitions whose lines the hunks of `git diff -U0 <base>..HEAD -- <file>` touch (a class's method by its name, the class itself when the touched line is a class-body statement outside every method, a module-level assignment by its target), read on the head's blob for added lines and on the base's blob for deleted ones. Round 6's additions to the line: `RegUnreadable` and `StepWrite` (two module-level classes), `_mirror_pick` and `_update_reg` in kernel/sdk_backend.py, and tests/test_kernel_cmd_gesture.py; the eighth commit's addition: tests/test_kernel_effort_reconnect.py, the seventh commit's one file; the ninth commit's additions: the eight kernel/sdk_backend.py names the regeneration sentence lists; the tenth commit's additions: `_revert_rename_record` and `rename` in kernel/sdk_backend.py, and tests/test_sdk_rename_ping.py. Two names it carries are fork-only infrastructure, not part of what ships upstream: `cli/perf_public.py`'s HTTP_ROUTES allowlist (the fork's public perf surface) and `tests/test_headless_verbs_help.py` (the fork's headless-verbs help census); the line names them because the branch changed those files, and the marking lives here in the body. Rebased onto the reviewer's round 1 of the auth-default fix (2026-09-18): the verb's pending writes carry the stored login beside the side, its slot flags are written in the pending's hold, its never-landed branch and `default` dispatch key on the cannot-tell class (a report, or a live host lease with none; a parked pick then waits for the CLI's first init, which decides it), the walk survives one follower's fault, and its words say when a relaunch waits for its spawn slot ("staggered") or for the CLI's first report ("report").
