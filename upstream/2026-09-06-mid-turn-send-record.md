---
title: A send fed into a running turn lost its only visible record: `prune_live`'s stale-echo rule retired an input echo carrying ANY absolute path once a later human record landed (a rule for image drops, whose path the CLI rewrites to `[Image #N]`), so a staged comment's quote-chip `path:line` label got the second of two back-to-back comments retired with the message still queued in the CLI, and nothing showed it for the 30 s until the splice (2026-09-06 audit). Fix, as it ships after the 2026-09-06 review: on the SDK route NO echo is floored. An echo retires only when its text lands in a record stamped at or after the send (a user record or the `queued_command` attachment, via `_atom_user_texts`) or when the CLI dies holding it (`dropped`). The CLI extracts no image paths on the stream-json route (its only image-path test, `/\.(png|jpe?g|gif|webp)$/i`, belongs to the interactive composer's paste handler), so the floor had no true positives there; `_path_bearing` and that set (pinned equal between kernel and backend) remain only for the tmux settle path (`_tmux_echo_settle`), where the paste hook runs and rewrites the path to `[Image #N]`. The chat's bare-path image preview (`_user_images`) reads its own set, `_PREVIEW_IMG_RE`, built from the served MIME table (`_IMG_MIME`, svg and bmp included). `SdkSession.fed_texts` stays as `inflight`'s observable twin; it gates nothing. The by-text comparison has ONE key, `session_backend.echo_text_key` (outer whitespace stripped, nothing else), shared by the kernel's `_atom_user_text(s)`, `prune_live`'s retire and the boot guard's scan (`_landed_texts`, `_text_landed`): the scan used to collapse inner whitespace while the prune compared raw text, so a send with a trailing newline (`romp send` passes its argument verbatim) was found, hence neither re-fed nor flagged, and yet never pruned or dismissable (2026-09-06 review, round 3)
status: approved
where: fork PR #240 (branch `senddur`, merged 2026-09-06), commits `6fc38149` (the image-only narrowing and the fed-texts guard, since superseded), `45eeaa30` (`kernel/sdk_backend.py` `_IMG_PATH_RE` / `_path_bearing` on the CLI's set, `prune_live` floors no echo; `kernel/kernel.py` `_IMG_PATH_RE` twin), `efdfc3ef` (`tests/test_sdk_backend.py` follows the floor's removal), `3c878dbd` (`kernel/kernel.py` `_PREVIEW_IMG_RE` beside `_IMG_MIME`) and `86d50a18` (`kernel/session_backend.py` `echo_text_key`, adopted by `kernel/sdk_backend.py` and `kernel/kernel.py`; OneTextRuleAcrossKernelAndBackend); red-first in `tests/test_sdk_echo_durability.py` (NoFloorRetiresAnEcho, FedEchoesOutliveTheFloor, the quote-chip label) and `tests/test_kernel_fed_echo_absorbed.py` (a real SdkBackend through `em.parse_session` + `_merge_live_atoms`; ImagePathPredicateTwins pins both twins to the CLI's set; PreviewSetIsTheHydrationRoutes)
added: 2026-09-06
pr: 240
tier: fix
offered:
closed:
---
Upstream ships the same rule and the same gap (verified by `git grep` against `upstream/main` `2b9db2be`, 2026-09-06): `_PATHY_RE` any-path at their sdk_backend ~4636, `_inflight_texts` at ~2044 with no reader in `prune_live` ~7920. Pure bug fix, no fork-only idiom. An offer branch carries the four commits together; their tmux route keeps `_path_bearing` and is unchanged.

Status detail (migrated from the table): candidate

APPROVED 2026-09-07: the fork owner said offer it (batch 13, tier fix — the base for the mid-turn send story and the boot duplicate guard entries).
