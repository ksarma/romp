---
title: Served labs build vscode-extension/dist once, under a file lock, and copy it under the same lock (setup ERRORs under pytest -n 8)
status: candidate
where: fork PR #440 (`lab-dist-copy-race`): `tests/lab_dist.py` new (DistBuild: flock on dist/.lab-build.lock, a marker dist/.lab-built naming the build key, computed before the build, and the output state, written atomically after it; inputs derived from esbuild.js's exported configs (node requires the module, which builds only as a script, and writes module.exports as JSON to a file; every exported string value naming a path on disk inside the checkout, relative or absolute, contributes its top-level tree; no text scan; a relative value reaching an existing path outside the checkout raises, and so does an absolute one naming an existing source file outside it that is not under node_modules' realpath, while an absolute outside directory, a non-source file and a dependency's realpath stay silent), the config's own tree seeded, then followed imports; a bare package the config requires that node cannot find skips the served labs as a failed build does, every other derivation failure raises; the command line and the lock files content in the key; BUILD_TIMEOUT, the kernel bound; copytree ignore for esbuild staging names); `tests/__init__.py` registers the bare names `lab_dist` and `lab_dist_stub` beside romp_load; `tests/lab_dist_stub.py` new (a node preload, `--require` on NODE_OPTIONS, that stands in for a bare package the config itself requires and node cannot find, on a checkout with no node_modules beside the config, and loads the real module wherever one resolves, so the two real-tree pins run on a checkout without the extension's node_modules; a require from inside an installed package and a subpath into an installed package stay node's error, a node_modules that exists and lacks the package makes the preload throw naming itself, every read of the stand-in throws, and a skip inside its block is re-raised as a failure naming the file, so the pins go red, never skip, where the stand-in does not apply); every served-lab module (`tests/test_awaiting_box_sync.py`, `tests/test_dashboard_reload_served.py`, `tests/test_feed_hover_border_static.py`, `tests/test_feed_thread_fold_columns.py`, `tests/test_gear_select_matrix.py`, `tests/test_notification_tap_resume_browser.py`, `tests/test_paste_to_focus_browser.py`, `tests/test_pending_at_tail.py`, `tests/test_pending_bubble_stable.py`, `tests/test_queued_copy_held.py`, `tests/test_refusal_notice_served.py`, `tests/test_scroll_marks_click.py`, `tests/test_scroll_marks_growth.py`, `tests/test_ship_reship.py`, `tests/test_tab_groups_rows.py`) calls `lab_dist.copy_dist(dist)` in place of its own esbuild run and copytree; `tests/test_lab_dist.py` new; two kernel pins in fork-only modules (`tests/test_kernel_bundle_staleness.py`: input parity and the cache-bust token; `tests/test_kernel_bundle_vendor_inputs.py`: BUILD_TIMEOUT against all three of the kernel's esbuild calls), which upstream lacks, so an offer hosts the three pins in a module of its own that loads the kernel under isolation or leaves them on the fork; `vscode-extension/.vscodeignore` two rules; `tests/README.md` the served-lab paragraph: copy_dist, the skip rule, the stub and the ratchet. Upstream carries the exports and the `require.main` guard the derivation relies on since romp-on/romp#1213 (merged 2026-09-09), so an offer against upstream main finds both in place. The upstream half of this subject, romp-on/romp#1238 (the served pages' copy of dist skips a concurrent build's staging files, and esbuild.js's failure line survives an empty errors list; the fork's entry for it is `upstream/2026-09-10-dist-copy-staging-race.md`), merged 2026-09-10 as acc9aa41: upstream main carries `tests/dist_copy.py`, whose `copy_dist(src, dst)` is a lock-free twin of the copy step here (a copytree that ignores the staging names; no shared build, no lock, no marker), imported by 17 served modules (the fifteen above plus `tests/test_log_opener_moved.py` and `tests/test_remote_comment_name_served.py`, new upstream and not yet on fork main) and driven by `tests/test_dist_copy_staging.py`, each served module still running `node esbuild.js` itself. An offer of this entry replaces `tests/dist_copy.py`, the two-argument `copy_dist` call in those 17 modules and the raw-copytree guard in `tests/test_dist_copy_staging.py`. Convergence on the fork: when the inbound fold brings `tests/dist_copy.py`, `tests/lab_dist.py` adopts its `copy_dist` for the copy step and keeps the shared build, the lock, the reader and the stand-in as the fork's post-offer delta.
added: 2026-09-09
pr: 440
tier: docs
offered: their PR #1238, the copy step only (merged 2026-09-10 as acc9aa41: tests/dist_copy.py, a lock-free copy_dist(src, dst)); the shared build, the lock, the reader and the stand-in are unoffered
closed:
---
Under pytest -n 8 two served-lab classes on two workers each ran node esbuild.js into the one dist and copied it; a copy that listed dist during the peer staging phase failed on `.pdf-worker.js.tmp-<pid>-<n>` names renamed away before the copy reached them (shutil.Error at setUpClass; green alone, 11 of 11 iterations red under -n 2 on the two scroll-marks modules). Upstream (67773fb2) carries fifteen served modules in the same build-block shape: the thirteen here (four changed upstream outside the build block: test_gear_select_matrix, test_queued_copy_held, test_ship_reship, test_tab_groups_rows) plus tests/test_notification_tap_resume_browser.py and tests/test_refusal_notice_served.py, which reach the fork through the fold (fork PR #451). An offer converts all fifteen, or its own ratchet fails upstream on the two it left. Upstream also lacks tests/romp_load.py, so an offer carries the tests/__init__.py registration too. Fork-side follow-up, done: #440 merged fork main after #451 landed (merge commit c51146da) and converted the two fold modules in the commit on top of that merge, so the ratchet guards them from the first main that holds both.

2026-09-10: romp-on/romp#1238 merged as acc9aa41. Upstream main (9ac871fc) carries `tests/dist_copy.py`, whose `copy_dist(src, dst)` is a lock-free twin of this harness's copy step (a copytree ignoring the staging names, no shared build), imported by 17 served modules, the fifteen plus `tests/test_log_opener_moved.py` and `tests/test_remote_comment_name_served.py`, and driven by `tests/test_dist_copy_staging.py`; every served module there still runs `node esbuild.js` itself, so the race this entry retires is closed upstream on the copy side only. The two new served modules reach the fork through the fold (fork PR #598 carries them with the old build block; the slice after it brings `tests/dist_copy.py`), so whichever of #440 and that fold lands second converts them, as #451's two were. Convergence: when the fold brings `tests/dist_copy.py`, `tests/lab_dist.py` adopts its `copy_dist` for the copy step and keeps the shared build, the lock, the reader and the stand-in as the fork's post-offer delta. An offer of this entry converts all 17 modules and replaces the twin and its test, or its own ratchet fails upstream on every one it left.
