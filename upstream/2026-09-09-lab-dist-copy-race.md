---
title: Served labs build vscode-extension/dist once, under a file lock, and copy it under the same lock (setup ERRORs under pytest -n 8)
status: candidate
where: fork PR #440 (`lab-dist-copy-race`): `tests/lab_dist.py` new (DistBuild: flock on dist/.lab-build.lock, a marker dist/.lab-built naming the build key, computed before the build, and the output state, written atomically after it; inputs derived from esbuild.js's exported configs (node requires the module, which builds only as a script, and writes module.exports as JSON to a file; every exported string value naming a path on disk inside the checkout, relative or absolute, contributes its top-level tree; no text scan; a relative value reaching an existing path outside the checkout raises, and so does an absolute one naming an existing source file outside it that is not under node_modules' realpath, while an absolute outside directory, a non-source file and a dependency's realpath stay silent), the config's own tree seeded, then followed imports; a bare package the config requires that node cannot find skips the served labs as a failed build does, every other derivation failure raises; the command line and the lock files content in the key; BUILD_TIMEOUT, the kernel bound; copytree ignore for esbuild staging names); `tests/__init__.py` registers the bare names `lab_dist` and `lab_dist_stub` beside romp_load; `tests/lab_dist_stub.py` new (a node preload, `--require` on NODE_OPTIONS, that stands in for a bare package the config itself requires and node cannot find, on a checkout with no node_modules beside the config, and loads the real module wherever one resolves, so the two real-tree pins run on a checkout without the extension's node_modules; a require from inside an installed package and a subpath into an installed package stay node's error, a node_modules that exists and lacks the package makes the preload throw naming itself, every read of the stand-in throws, and a skip inside its block is re-raised as a failure naming the file, so the pins go red, never skip, where the stand-in does not apply); every served-lab module (`tests/test_awaiting_box_sync.py`, `tests/test_dashboard_reload_served.py`, `tests/test_feed_hover_border_static.py`, `tests/test_feed_thread_fold_columns.py`, `tests/test_gear_select_matrix.py`, `tests/test_notification_tap_resume_browser.py`, `tests/test_paste_to_focus_browser.py`, `tests/test_pending_at_tail.py`, `tests/test_pending_bubble_stable.py`, `tests/test_queued_copy_held.py`, `tests/test_refusal_notice_served.py`, `tests/test_scroll_marks_click.py`, `tests/test_scroll_marks_growth.py`, `tests/test_ship_reship.py`, `tests/test_tab_groups_rows.py`) calls `lab_dist.copy_dist(dist)` in place of its own esbuild run and copytree; `tests/test_lab_dist.py` new; two kernel pins in fork-only modules (`tests/test_kernel_bundle_staleness.py`: input parity and the cache-bust token; `tests/test_kernel_bundle_vendor_inputs.py`: BUILD_TIMEOUT against all three of the kernel's esbuild calls), which upstream lacks, so an offer hosts the three pins in a module of its own that loads the kernel under isolation or leaves them on the fork; `vscode-extension/.vscodeignore` two rules; `tests/README.md` the served-lab paragraph: copy_dist, the skip rule, the stub and the ratchet. Upstream carries the exports and the `require.main` guard the derivation relies on since romp-on/romp#1213 (merged 2026-09-09), so an offer against upstream main finds both in place. The upstream half of this subject, romp-on/romp#1238 (the served pages' copy of dist skips a concurrent build's staging files, and esbuild.js's failure line survives an empty errors list; the fork's entry for it is `upstream/2026-09-10-dist-copy-staging-race.md`), merged 2026-09-10 as acc9aa41: upstream main carries `tests/dist_copy.py`, whose `copy_dist(src, dst)` is a lock-free twin of the copy step here (a copytree that ignores the staging names; no shared build, no lock, no marker), imported by 17 served modules (the fifteen above plus `tests/test_log_opener_moved.py` and `tests/test_remote_comment_name_served.py`, new upstream and not yet on fork main) and driven by `tests/test_dist_copy_staging.py`, each served module still running `node esbuild.js` itself. An offer of this entry replaces `tests/dist_copy.py`, the two-argument `copy_dist` call in those 17 modules and the raw-copytree guard in `tests/test_dist_copy_staging.py`. Convergence on the fork (round 15): `tests/lab_dist.py` keeps its own copy step, `shutil.copytree` with `copy_ignore`, which leaves out the lock and the marker as well as the staging names (`tests/test_lab_dist.py` pins both out of every lab copy), and does not call upstream's `copy_dist(src, dst)`, which takes no ignore and would copy both into every lab (its `STAGING_GLOB`, `.*.tmp-*`, also demands the leading dot where lab_dist's `*.tmp-*` does not); a later offer may add an ignore parameter to upstream's `copy_dist`, after which lab_dist can call it and the twin stops being a twin. This PR's text ratchet (`test_no_test_module_builds_or_copies_dist_on_its_own`) refuses three files as upstream carries them, none a served module the conversion rule covers: `tests/dist_copy.py` and `tests/test_dist_copy_staging.py` (each names esbuild.js and calls copytree) and `tests/test_perf_bench.py` (romp-on/romp#1057, 929655af, which copies kernel/ into a scratch checkout and reaches the fork as an add/add conflict on that file; it precedes 1238 in upstream's first-parent history, so a slice cut between them meets that red one slice before the twin). The plan: `test_perf_bench.py` is in `_TREE_COPIERS` already (the fork's copy calls no copytree, so the entry is a no-op until the fold); the slice that brings the twin pair allowlists both in `_ESBUILD_TEXT_READERS` and `_TREE_COPIERS` with the reason (upstream's copy primitive and its guard, called by no fork class, kept side by side so later folds of both merge clean; the guard's copytree runs over a scratch tree, never dist); the alternative, dropping both in favour of lab_dist and the fork's staging tests, carries a delete/modify conflict on every later fold of the twin (1266 alone rewrote 128 lines of its test), so allowlisting is the cheaper default while the offer is pending. The collision is one-way: upstream's guard globs test_*.py and passes over the fork tree. That slice also conflicts on the copy line of every converted served module (`lab_dist.copy_dist(dist)` against upstream's `copy_dist(os.path.join(EXT, "dist"), dist)`, one hunk each, 17 modules under the expected order and 15 if #440 lands first or in the same batch; the esbuild-run lines merge away and upstream's `from tests.dist_copy import copy_dist` merges clean as an unused import): this PR's line wins, the fold re-applies upstream's textual changes around it and removes the import by hand (no lint job or test reads an unused import), and `test_every_served_module_calls_copy_dist` is what goes red on a copy line resolved to the twin, since the text ratchet does not read `copy_dist(`. romp-on/romp#1266 (merged 2026-09-10 as 15ad1d26; the fork's record is `upstream/2026-09-10-dist-copy-readme-and-guard-depth.md`) deepened that raw-copytree guard to an ast walk and added a `tests/README.md` paragraph naming `tests.dist_copy.copy_dist` as the served pages' one door. Against upstream main holding romp-on/romp#1278 and #1283 (verified at f0597caa with a three-way merge of the file), this PR's served-lab paragraph sits inside the README's one conflict hunk, the `romp_load` bullet, which both sides rewrote and to which the fork appended the fifteen served-lab lines with no unchanged line between; 1266's paragraph itself lands clean further down. The resolution keeps both paragraphs, this PR's first (the served-lab sentences beneath upstream's bullet wording) and 1266's where it lands, with two doors named: `lab_dist.copy_dist` for the fork's served classes, `tests.dist_copy.copy_dist` as upstream's, called by no fork class once converted (the guard sentence stays true, scanning test_*.py only); an offer rewrites that README paragraph too.
added: 2026-09-09
pr: 440
tier: docs
offered: their PR #1238, the copy step only (merged 2026-09-10 as acc9aa41: tests/dist_copy.py, a lock-free copy_dist(src, dst)); the shared build, the lock, the reader and the stand-in are unoffered
closed:
---
Under pytest -n 8 two served-lab classes on two workers each ran node esbuild.js into the one dist and copied it; a copy that listed dist during the peer staging phase failed on `.pdf-worker.js.tmp-<pid>-<n>` names renamed away before the copy reached them (shutil.Error at setUpClass; green alone, 11 of 11 iterations red under -n 2 on the two scroll-marks modules). Upstream (67773fb2) carries fifteen served modules in the same build-block shape: the thirteen here (four changed upstream outside the build block: test_gear_select_matrix, test_queued_copy_held, test_ship_reship, test_tab_groups_rows) plus tests/test_notification_tap_resume_browser.py and tests/test_refusal_notice_served.py, which reach the fork through the fold (fork PR #451). An offer converts all fifteen, or its own ratchet fails upstream on the two it left. Upstream lacked tests/romp_load.py then, so an offer would have carried the tests/__init__.py registration too (true until romp-on/romp#1278 on 2026-09-10; the round-15 paragraph below has the current state). Fork-side follow-up, done: #440 merged fork main after #451 landed (merge commit c51146da) and converted the two fold modules in the commit on top of that merge, so the ratchet guards them from the first main that holds both.

2026-09-10: romp-on/romp#1238 merged as acc9aa41. Upstream main (9ac871fc) carries `tests/dist_copy.py`, whose `copy_dist(src, dst)` is a lock-free twin of this harness's copy step (a copytree ignoring the staging names, no shared build), imported by 17 served modules, the fifteen plus `tests/test_log_opener_moved.py` and `tests/test_remote_comment_name_served.py`, and driven by `tests/test_dist_copy_staging.py`; every served module there still runs `node esbuild.js` itself, so the race this entry retires is closed upstream on the copy side only. The two new served modules reach the fork through the fold (fork PR #598 carries them with the old build block; the slice after it brings `tests/dist_copy.py`), so whichever of #440 and that fold lands second converts them, as #451's two were. Convergence, as first planned here and corrected in round 15: `tests/lab_dist.py` keeps its own copy step and does not call the twin (upstream's `copy_dist(src, dst)` takes no ignore, so a lab_dist that called it would copy the lock and the marker into every lab); the twin and its test are allowlisted in the ratchet by the fold slice that brings them (the round-15 paragraph below). An offer of this entry converts all 17 modules and replaces the twin and its test, or its own ratchet fails upstream on every one it left.

2026-09-10, round 14: fold PR #598 (upfold 4) carries `tests/test_log_opener_moved.py` and `tests/test_remote_comment_name_served.py` with the old build block, and the expected order is #598 first: this branch then merges fork main, converts the two through `lab_dist.copy_dist` in a commit on top of the merge, raises the served floor in `tests/test_lab_dist.py` from 15 to 17 and adds both paths to the where line above, as for #451. If #440 lands first, or both land in one batch, the fold session converts them in #598 or its next slice, and the reviewing session sends it the note. Either way the text ratchet is what goes red, naming the two, on the batch head's sweep and the batch PR's CI before main. The slice that brings `tests/dist_copy.py` and `tests/test_dist_copy_staging.py` meets the same ratchet on both and allowlists them with the reason (upstream's copy primitive and its guard, called by no fork class, kept side by side; `tests/lab_dist.py` keeps its own copy step and does not call the twin, since upstream's `copy_dist(src, dst)` takes no ignore), or drops both for lab_dist and carries the delete/modify conflict on later folds of the twin. The same window brings a third ratchet hit, upstream's `tests/test_perf_bench.py` (romp-on/romp#1057, 929655af, a copytree of kernel/ into a scratch checkout; it precedes 1238 in upstream's first-parent history, so a slice cut between them meets it one slice before the twin), pre-allowlisted in `_TREE_COPIERS` by round 15, and a modify/modify conflict on the copy line of every converted served module (17 under the expected order, 15 if #440 lands first or in the same batch): this PR's `lab_dist.copy_dist(dist)` wins, upstream's textual changes are re-applied around it and its unused `from tests.dist_copy import copy_dist` is removed by hand; `test_every_served_module_calls_copy_dist` goes red on a copy line resolved to the twin. A later slice brings romp-on/romp#1266's `tests/README.md` paragraph (15ad1d26; the fork's record is `upstream/2026-09-10-dist-copy-readme-and-guard-depth.md`); against upstream main holding romp-on/romp#1278 and #1283, this PR's served-lab paragraph sits inside the README's one conflict hunk, the `romp_load` bullet both sides rewrote, while 1266's paragraph lands clean further down, so the resolution keeps both paragraphs, this PR's first beneath upstream's bullet wording and 1266's where it lands, two doors named (`lab_dist.copy_dist` for the fork's served classes, `tests.dist_copy.copy_dist` as upstream's, called by no fork class).

2026-09-10, round 15: the reader and the preload judge a request by its NAME segments alone (the first, two for a scoped request): `pkg/`, `pkg/./index`, `pkg/../pkg` and `@scope/q/`, which node resolves to the installed package, are that package's requests, so a config requiring one where no such package resolves skips as the environment like `pkg` (round 14 refused every empty, `.` or `..` segment, and the served modules' call raised there); `@/x`, `@scope//pkg`, `@scope/./x` and `@scope/../x` stay node's error alone; a name's characters are not read, so a name npm would refuse for its spelling passes as a package. Upstream carries `tests/romp_load.py` and the `tests/__init__.py` lines registering it since romp-on/romp#1278 (9ac871fc, merged 2026-09-10), so an offer adds only the two registration blocks for `lab_dist` and `lab_dist_stub` beneath that registration (this PR's ten-line change to `tests/__init__.py`), never the fork's `tests/__init__.py` or `tests/romp_load.py` wholesale (their comments and docstring differ textually from upstream's); the two kernel-pin modules (`tests/test_kernel_bundle_staleness.py`, `tests/test_kernel_bundle_vendor_inputs.py`) stay absent upstream. The convergence plan, the three ratchet files, the copy-line conflicts and the README hunk are stated once, in the where line above.
