---
title: The todo Reply sheet holds three rows with the phone keyboard up; the long detail scrolls within a cap
status: candidate
where: ui/webview/styles.css (#ut-reply-prompt .ut-reply-input, #ut-reply-prompt .ut-detail.open, #ut-reply-prompt.kb-tight, #ut-reply-prompt .picker-box), ui/webview/waiting.ts (showReply: kbFit, grow), ui/webview/render.ts (showUserTodoReply: kbFit, grow; renderTodo), ui/webview/reply-sheet-keyboard.test.ts, ui/webview/waiting-reply-sheet-browser.test.ts, ui/webview/render-reply-sheet-browser.test.ts, ui/webview/waiting-reply-focus.test.ts, ui/webview/render-todo-file-chip.test.ts, tests/test_reply_sheet_served.py, tests/reply_sheet_browser.mjs
added: 2026-09-19
pr: 859
tier: fix
offered:
closed:
---
On a phone with the keyboard up a todo's long detail filled the Reply sheet and the answer box was one squeezed line above Cancel and Send (2026-09-19, screenshot). The sheet is a column flex box capped at the window with overflow hidden; a wrapped text block's automatic minimum refuses to shrink and a textarea's resolves to zero, so the box took the whole deficit and the cap clipped. Four rules scoped to the dialog (the answer box a fixed item with a three-row floor; the detail the item that gives way, a scroll container capped at 12em and scrolling within itself, never under two of its lines, wrapping an unbreakable token; the short-window frame on the overlay's own selector; the box scrolling at every height as the backstop) and, in both builders, the picker's kb-tight fold on the window's own resize plus a grow handler capped at the room the box has left, read from the box, re-run on the resize, and standing down for a height the person dragged (file-comments.ts autosize's guard). The maintainer's round 1 found the first cut's three rules failing in composition at 390x508 (the answer box grown to a share of the window laid Send below the box's clip, and a tap there closed the sheet with the answer): the room cap, the floor, the every-height scroll, the drag guard and the served leg are that round's fixes. Fake-DOM legs execute the fold and the grow out of each builder's source; rule pins read the sheet with comments stripped; browser legs in Chromium, Firefox and WebKit measure the assembled sheet with a tap on Send at 390x508, 420, 490, 900, 300 and 230 against the served /waiting page and the chat's real builder; a served pytest leg drives both served pages in CI's browser step.
