---
title: Analytics cost view: `_session_tok_rows` counts each API response once (the CLI writes a multi-block response as several assistant records sharing one `message.id`, 2.3 to 3.0 per response measured; a main transcript's records repeat one usage block, a subagent transcript's carry the stream-start snapshot until the last record's final tally, so the fold keeps the largest output count) and walks `<sid>/subagents/` recursively (Task agents at the top, Workflow agents at `workflows/wf_<id>/agent-<id>.jsonl` on Claude Code 2.1.261; the flat listing missed a quarter of the sessions' tokens); the price table gains `claude-fable-5-1` (cache read 0.25 $/Mtok per the CLI 2.1.261 model catalog; the family fallback had priced it at Fable 5's 1) and `_price_for` matches a (family, major, minor) signature before falling to the family; `/analytics` reads the period at the rail's bucket edges (`_analytics_edges`: whole hours, or whole local dates beyond the hour ledger; `from` and `buckets` in the payload) and cuts the ledger sum, the transcript rows and the judge rows at the same start, so the modal's `judges = N%` line is like-for-like and its ledger figure equals the rail cell's; `sessions.ledger` is the CLI's own per-turn cost summed from `spend.json` (the keyed split where a key runs beside a login, the rail's own arm, flagged `keyed`) plus `estBefore`, the token-price estimate for the part of the period before the ledger's first bucket (`sinceT`), and the modal shows the two added and labelled, with tokens × prices alone only where the ledger has nothing; the hour keys are distinct (the DST fall-back day's repeated hour was summed twice) and `_bucket_start` names a bucket's first instant explicitly (mktime's isdst=-1 answer for the repeated hour depends on prior calls), and `_analytics_edges` takes the period's start from it in both branches (hour arithmetic from the current hour put the start on the repeated hour's second 01:00 while the ledger summed the whole bucket, so the transcript and judge figures ran an hour short of the session dollars; the same for a repeated midnight, and half an hour off in a 30-minute-shift zone); the subagents walk skips symlinked files and a symlinked subagents directory; the keyed split arms under the rail's guard (recorded key turns), not on a key merely configured; day-bucket edges travel as the kernel's date strings (`fromDate`, `sinceDate`) so a browser in another zone prints the kernel's date; the footnote states that the ledger's first bucket is partial
status: offered
where: fork main `ff8b42db` (2026-09-06) plus two 2026-09-06 fixer rounds: `kernel/kernel.py` (`_subagent_transcripts`, `_session_tok_rows`, `_session_usage`, `_session_tokens`, `_session_cost`, `DEFAULT_MODEL_PRICES`, `_price_for`, `_bucket_start`, `_analytics_edges`, `_spend_ledger_window`, `_token_analytics`), `ui/webview/gear.js` (`raRender`, `raWhen`, `raDate`); tests in `tests/test_token_usage.py`, `ui/webview/rail-spend.test.ts`; `docs/reference.md`, `docs/guide.md`
added: 2026-09-06
pr:
tier: fix
offered: their PR #969
closed:
---
Fork `ff8b42db` (2026-09-05) plus two 2026-09-06 fixer rounds after its adversarial reviews (the recursive walk, one cut for every figure, the keyed split, the labelled estimate; then the DST keys, the symlink bound, the keyed guard, the day-date edges, the partial-bucket footnote; then the period start taken from the oldest bucket itself). The modal and `_token_analytics` are upstream's (their `6efdcb14` lineage); here the view read 1.55x the ledger over 24 hours. The pre-fix ledger flag landed beside it (`SPEND_PRE_FIX_DATE`, `_spend_pre_fix`, the hover's words) is fork-only: its cutoff is this ledger's own history.

Status detail (migrated from the table): candidate

APPROVED 2026-09-07: the fork owner said offer it (batch 13, tier fix — their open #956 ships `romp spend-rebuild`, which recounts the LEDGER's token columns from the transcripts under the same two rules (streaming splits counted once by message id; subagent calls included); check whether it supersedes the per-`message.id` and subagent parts of this view-side fix (ideally share its counting code), and offer the `claude-fable-5-1` price row and the period-edge alignment as the non-overlapping remainder. `SPEND_PRE_FIX_DATE` stays home).

OFFERED 2026-09-07: offered upstream as their PR #969 (2026-09-07, label fix, head cb14b937). Part A only: the once-per-`message.id` count, the recursive `subagents/` walk (Task and `workflows/wf_<id>/` agents), the `claude-fable-5-1` price row and `_price_for`'s signature match; the PR body states it shares no hunks with their #956. The ledger-figure half (`_analytics_edges`, `_spend_ledger_window`, the keyed split and `estBefore`, the DST bucket keys, the `gear.js` period edges) stays held for upstream #956, which recounts the ledger's own token columns under the same rules.
