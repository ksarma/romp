---
title: Compact mode streams its tail by unit, and the head gap's per-turn estimate is the median over complete turns, measured off the unit observer
status: candidate
where: ui/webview/render.ts (syncView/syncViewInner's anchored flag and its applyMeasure take, appendActive's flag (its follow or the anchor it holds) and its raw road's untake, the compact seam and its spacer branch's footer patch, normal mode's browse branch's footer patch and the fast path's, patchWorkedFooters (the list required at every site, the no-list arm gone), normal mode's exact tail by the list's units (dayWalkBeforeEvent removed), ensureView's unit ResizeObserver and view ResizeObserver (its tailLabel predicate), renderWindowItems and its anchored gate, railSeed/railExit/railChainBefore, unitOfNode/isSpacerNode/trimUnitsFrom, normal mode's tail walk (trimUnitsFrom) and the host-scoped ring clear both tail paths call (clearRailRings, the band module's remover with an optional host), evictCompactTop/reseedWindowHead, gapUnitsOf, sizeSpacers/hiddenHeight, queueSpacerRow's per-row frame and its shown-live-view predicate, dropSpacerRowsOnVisibility and its visibilitychange listener, takeMeasureAtBottom, measureUnits/applyMeasure/redrawGapUnits/forgetAverage/entryBoxHeight, figuresBefore/untakeMeasure (the take given back at every road that ends unanchored), toggleToolGroup and its flag (whether a row was captured) and its raw road's untake, landActive's take before its attempt, its saved-place fallback's restore of the row captured at the saved place (captureScrollAnchor's `at`) and the untake before its raw write, its saved place read from the scroller on a view the scroller already holds (the record synced to it before the capture and the raw write), its miss with a fetch armed restoring nothing (the fetch read from this attempt; the take kept under this attempt's pre-jump, anchorPreJumped, cleared on scrollToAnchor's entry and set by preJumpIntoGap), showActive's scrollerHolds (decided before the display flip, handed to the synchronous land alone), keepPlaceAcrossWindow's take, its double-miss restore and its untake, scrollToAnchor's untake on its two miss roads, landNearestMoment's on its miss, virtualizeToViewport's when the focus unit is gone, the two followRebuilt clears (syncViewInner's paint and the #content scroll listener's gesture), the four forgetAverage resets in showActive/runPrebuild/rerenderAll/chatHead, runPrebuild's flagless syncView, scrollToAnchor's, landNearestMoment's and fillInPlace's window builds, fillInPlace's flagged no-unit fallback and the untake on its two raw roads, virtualizeToViewport's tail re-window), ui/webview/chat-compact-tail.ts (new), ui/webview/turn-estimate.ts (new; completeTurnHeights' gap break), ui/webview/chat-regions.ts (MAX_TURN_PX, gapHeight's cap), ui/webview/scroll-keep.ts (followRebuiltTail), ui/webview/scroll-write.ts (spacerRow and its inactive marker, restored on the owner's approval of 2026-09-21, tailLabel's unit-aware tail with the spacer rule standing in for a view with no unit-carrying child, unitChanges' unit-aware tail filing nothing with no tail and nothing for a foreign child), ui/webview/landing-settle.ts (the writer census's comment), kernel/kernel.py (CLIENT_DIAG_KEYS['chat'] gains 'view', on the owner's approval of 2026-09-21; CLIENT_DIAG_VALUES and _client_diag_value_admitted bound its value to the one word, refused in _client_diag_admit), docs/reference.md (the clientDiag paragraph's sentence on that bound: an admitted key whose value is outside the set the kernel states is refused, said once by key and never by value); tests, new: ui/webview/chat-compact-tail.test.ts, compact-tail-differential.test.ts (with a normal-mode leg, a head-gap and a mid-gap leg in normal mode, and the hover held through a frame on both sides), compact-seam-exec.test.ts (with the normal-mode browsed world, the hover-class ownership census, SEAM_PRELUDE, the seam's stubbed collaborators, and the pin of its stubbed names against render.ts's module-level declarations), rail-chain.test.ts, spacer-measure.test.ts (the syntax-tree censuses; the view-key minter census over the modules the page bundles load, derived from esbuild's metafile by bundledModules and walked by censusViewWrites, the bundles' inputs partitioned into modules and styles and the directory's listing into the classes LISTING_CLASSES tries in order (the anchor map's fixtures first, then tests and types, modules, styles), the two tied by equality both ways, with the fourth-class witness through the same walker, the kind cell over kindOf's suffix-to-kind mapping and the element shape under its kind, the partition case running synthetic names of every listing class through the census's own partition and LISTING_CLASSES, and the CLIENT_DIAG_VALUES table's balanced parse, parseValuesTable, beside its regex; the untake mechanism, the unit-less views through the observer, the section-view and hidden-page frames), turn-estimate.test.ts, toolgroup-toggle-keep.test.ts, land-active-keep.test.ts (with the missed land on a view the scroller holds: a link with a time and one without, the raw write, and the source pin on showActive's decision; the deep pre-jump standing through its armed fetch, for a reader scrolled up and for one following the tail, and the source pin on where the fetch and pre-jump marks are set and read), scroll-to-anchor-roads.test.ts, append-active-keep.test.ts, fill-in-place.test.ts; re-anchored: ui/webview/chat-exact-tail.test.ts (the block re-recorded), chat-exact-tail-exec.test.ts, chat-windowing.test.ts, compact-cache.test.ts, host-offline-ux.test.ts, chat-regions.test.ts, tail-shrink.test.ts, unit-change-row.test.ts (the unit-less worlds and the band as the entry), tail-mutation.test.ts (the kind union's pin, re-anchored on spacer-dropped), at-bottom.test.ts, follow-tail.test.ts, landing-settle.test.ts, scroll-keep-on-show.test.ts, scroll-movers.test.ts, comments.test.ts, tail-change-row.test.ts (the placeholder and loader labels), reload-restore.test.ts, render-scroll.test.ts, keep-across-window.test.ts, tab-switch-defer.test.ts (landActive's signature and showActive's synchronous land), day-divider.test.ts (its pin over normal mode's trim, the shared walk); kernel: tests/test_client_diag_allowlist.py (the inactive spacer row's dispatch pin and marked fixture row: the key in the chat entry on the owner's approval of 2026-09-21); served: tests/test_landing_notice_browser.py (road 16: a deep link with no time whose land misses inside the frame after a re-window's write; road 2: the pre-jump standing while its ask is held), tests/test_history_regions_browser.py (ServedCompactStream, the launched engine; the WebKit run opt-in), tests/test_live_paused_window_browser.py (ROMP_LAB_ENGINE's exit codes and legal set, UnknownEngineFailsLoudly's misspelled and non-browser cases); upstream/2026-09-19-chat-tail-compact-incremental.md (this entry)
added: 2026-09-19
pr: 861
tier: fix
offered:
closed:
---
Two defects the user sees on both platforms, visible on the phone: typing and streaming stutter (in compact mode, the default, every streamed frame that changed or appended an event rebuilt the whole rendered window, at least 80 units, once per animation frame) and the head spacer jumping 24k to 1.43M px in one second (the per-turn estimate was the window's whole height over its user-row count, measured once for the view's life, so a tail window with one user row read about 7,150 px per turn). Fix 1: the compact tail trims by data-unit from the first unit the plan names (chat-compact-tail.ts) and re-appends with appendItem; the rebuild stays for a stale view, a change above the window, a gap past the start or a bottom spacer; the folded-reply footer is patched by position instead of marking the view stale. Fix 2: the median over the turns the window holds whole, at least two, re-measured on every window build, capped at 20 default turns under every reader. Fix 3: no layout read in the render task: heights come from the unit ResizeObserver's border-box map at frame end, the figures reach the DOM inside the next paint, and the spacer diag row reads the scroller a frame later. One change lies outside the three, forced by the second: a follow-mode reader's re-window at the tail (virtualizeToViewport) writes the bottom and arms the view observer to follow the rebuilt rows' first settling (scroll-keep.ts followRebuiltTail), because under the median estimate the rebuilt view came out longer and the go-to-the-live-tail jump ended above the bottom in both engines. The author's pass 1, applying the maintainer's round 1 ruling, added: the trim and the measure key on what is a unit (a hover's rail band, appended as the thread's last child, neither ends the trim nor counts as a row); a tool-run toggle keeps the reader's place as appendActive does (the anchor row put back over the re-sized head spacer); an eviction re-seeds the unit it promotes to the window's first with the build's rail reference; the per-turn median at an even count is the lower middle turn; and a node-level differential compares the incremental tail's DOM with a rebuild of the same state, driven per seam outcome as that pass drove it: the append by unit kind and fold state, the status-only fast path over working and idle, and the rebuild by the shrink (the rebuild's other whys share one executor with the rebuild leg and have no frame). The author's pass 3, applying the maintainer's round 2 ruling, added: landActive takes before its attempt and its saved-place fallback restores the row captured at the saved place instead of writing the pre-resize scrollTop raw; the kernel's chat allowlist admitted the inactive spacer row's marker (view: inactive), pinned by a dispatch assertion, and a new field a page posts to the kernel is the owner's to approve, field by field, so the field was taken out of the tree restorably on 2026-09-21 and restored the same day on the owner's approval, on his word and not on that ruling (the row posts view: inactive for a switched-away view, the chat allowlist names the key, then the branch's one kernel/kernel.py change (the value bound below, CLIENT_DIAG_VALUES, came with the pass after the maintainer's round 5), and the pins are of the presence); normal mode's tail trims through the shared walk; the hover's rings and glow came off with the band in both tail paths (narrowed below); tailLabel and unitChanges read the tail by the unit predicate when one is given; the build and sync censuses were keyed on the caller and the road, and the fill's no-unit fallback passes its own predicate; keepPlaceAcrossWindow restores the row under the viewport top when both its restores miss; and the differential drives the spacer outcome in both directions below a browsed window and the compacting state, reads the working state the seam stored on the view (v.working) instead of restating the state test, and mints its notice run through production's foldable predicate. The author's pass after the maintainer's round 3 ruling added: the unit-aware tail scan files nothing for a view with no unit-carrying child and nothing for a foreign child (the pass-3 delta threw where the parent returned an empty list), and tailLabel names the placeholder and the loader through the spacer rule; every reader of the take state is keyed on the outcome (a take no restore covered is given back before the raw write, figuresBefore and untakeMeasure, at appendActive, the toggle, the fill, landActive, keepPlaceAcrossWindow, scrollToAnchor, landNearestMoment and virtualizeToViewport; the reload restore's raw write keeps its take by measurement), with appendActive and fillInPlace lifted into harnesses; normal mode's browse branch patches the footers, and the differential runs on both sides of the compact switch; the tail paint takes only the band's rings off, through the band module's remover on the view's host, and leaves the glow to applyGlow; the take rule's censuses read the compiler's syntax tree and key on the property each guards; every round mention names the maintainer's round by number or the author's pass by label. The author's fixer pass over that pass added: the hover-class ownership census keyed on the class a mutation resolves to over render.ts and every module it reaches (a toggle, an alias or another module's remover reds), a census of the scroll writes inside the take state's readers by owner and writer, the takers' and resets' counts on the syntax tree, the ruler instrument read through the lift's own bindings, the loader driven as a view's only child, and the property asserted before the mechanism in the raw-road harness cases. The closing pass over that fixer pass added: the writes census attributes a write to the reader on its lexical chain and reads the write family's registered wrappers, the average's writers are counted under every assignment operator, the ownership census resolves a regular-expression literal and a className rewrite through .replace, the differential's stand-in element takes className writes, and the wrong-kind miss road has its own test. The author's pass after the maintainer's round 4 ruling added: the reload restore's raw write is pinned by ORDERING, both halves keyed on the property (the executed half's model observes the geometry and fails closed on what it lacks; the tree half derives the take state as a field set, with the takers half derived and half listed, and anchors the window on the record's binding with the record's admissions counted), where the clause above says only that the write keeps its take by measurement; and, after the field's restoration on the owner's approval of 2026-09-21, the marker's pins: the builder's mint scoped to the one word, the active rows carrying no key by execution, the allowlist comment, and a mixed frame filing each row with its own view's figures. The author's pass after the maintainer's round 5 ruling added: the kernel bounds the marker's value to the one approved word (CLIENT_DIAG_VALUES) and refuses any other with the unknown key's line; a census over every ui/webview module names the one minter of the key by the property; the spacer row of a live view hidden by the section-at-a-glance view is filed with nulls and the marker, the predicate being the view element's display; the page's visibility edges drop the spacer rows whose frame never came and file one row saying so (spacer-dropped); normal mode's footer patch and exact tail use the list's units, so a head gap no longer moves the footer onto the wrong row; and the served lab's engine guard is membership of the three browser types. The author's pass after the maintainer's round 6 ruling states the ruling on the branch's kernel/kernel.py change: the CLIENT_DIAG_VALUES hunk is the code matching the owner's approval of 2026-09-21 (the maintainer's round 5 ruling, before the hunk grew; restated by the maintainer's round 6 ruling over the grown hunk), adds no key, no second word and no host name, and reaches him as no new question; a growth that added a key, a word or a host name would reach him, a growth that bounds the value to the word he approved does not. The pass also: derives the minter census's population from esbuild's metafile of the shipped page bundles built in memory (the modules the pages load, tied to the directory's listing by equality both ways) and witnesses through the same walker the fourth class the tree cannot name, a view key built from a string value; reads the CLIENT_DIAG_VALUES table by a balanced parse (parseValuesTable) beside the regex, the two agreeing entry for entry; executes two witnesses, the divider's unit tag under a head gap and the visibility drop's count over two views with a third live; stubs gapElement in the seam harness's prelude and pins every stubbed name against render.ts's module-level declarations; hands normal mode's footer patch v.rendered in render.ts, the pass's one production change; and derives the refusal cell's second word from the kernel's word. The author's pass after the maintainer's round 7 ruling added: the census partitions the bundles' inputs and the directory's files instead of filtering them (.tsx and .jsx admitted under their own script kinds; the inputs into modules and styles, the directory's files into four classes, tests and types, modules, styles and the anchor map's fixtures, the remainder of each asserted empty naming the file and its suffix), bundledModules reds naming the build on an empty entry list, an empty metafile or a module class empty outside node_modules, the last with labelled counts, and its docstring names the census cell as its one caller. The follow-up to the landing lab's road 10 flake in CI: the missed-land fallback captured the saved place's row at the view's record, which lags the scroller by one frame after the re-window's write, so a deep link inside that frame had its anchor restore write the reader back a frame (8 to 0 in the lab); showActive now tells the synchronous land whether the scroller holds the view's reader, landActive syncs the record to the scroller there before it captures, the raw land-saved write stops reading the lag (a read older than this entry), a pre-jump in the same pass still stands, and the lab's road 16 enters that frame on every run. The same follow-up restores the behaviour before this entry for a deep link whose land misses with a fetch armed: the restore of the captured row applies only to a miss with no fetch armed, so a pre-jump into the gap stands until the reply lands it instead of returning the reader to where they started in the same pass, and the take stays under the pre-jump that was measured over it. The lab's road 2 checks that the pre-jump stands while its ask is held, and road 16's second link carries no time, so that road still goes red without the read from the scroller.
