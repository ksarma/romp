---
title: A settings pick waits for the session's live subagents and background tasks before it reconnects the CLI; the launched value picked again reconnects nothing
status: candidate
where: kernel/sdk_backend.py: SdkSession._hold_lock (the ONE lock on the hold state since round 5: the surface set, the held flag, the pending-reconnect flag and the fast/fast-reset swap, taken for every read-modify sequence on either thread) and _hold_gen with _hold_write (round 6: the hold state's generation counter, moved by every writer as it releases the lock, so a routine that computed the launch shape with the lock released re-checks it before it clears names or stamps _launching; _arm_now and _served_by_connect compute the shape outside the lock, recompute once on a move, and the lock is never held across the settings-file reads), _arm_reconnect (the one arm routine, run under that lock: the settle's arm, the idle pick's immediate arm, a pending rewind's request and the immediate-only form all schedule the reconnect through it; it opens the spawn window by stamping _launching and flips the surfaces of the picks riding it, fast to on and auth_live cleared), _arm_reconnect_if_quiet (called at the request and the ResultMessage settle; a held pick arms only at a settle that finds the live sets empty, no removal from the sets arms at its own frame; the arm's read and the discard of exactly the names it read are one hold of the lock), _served_by_connect (round 6: the one served check at every arm point, the immediate arm, the deferral behind a queued text or an open turn, and the settle: a request the composed connect fully serves, the flag included, arms no second reconnect and is withdrawn with one line, the names decided from a snapshot taken before the shape compute; round 7: with no connect in progress, a request the LANDED process already serves, the landing's in-flight switch target counting as the running mode, is withdrawn the same way, so a connect-time mode pick's trailing request after the landing applied it relaunches nothing), _note_work_ended (the removal sites' one log line), _withdraw_held_pick and _settle_withdrawal (the one withdraw routine in two halves: the kernel thread forgets the surface at once and the loop decides the hold's fate from one snapshot of the set under the lock; the hold ends whenever no surface remains, rewind pending or not; a live mode pick retires a pending mode pick, and set_effort, set_auth, set_fast and set_env withdraw a pending pick reverted to the running value; since round 5 the settle disarms a reconnect armed during the composed half of the spawn window (_connecting) that stood for the withdrawn, re-picked or applied pick alone, and since round 6 the disarm retires the deferred form too), _launched_shape, _reconnect_riding (round 7: the surfaces the reconnect in flight carries, from the arm to the landing; the arm, the loop top and the served check move the names they clear into it, _connect_landed clears it, and snapshot's fastPending and modePending read it beside the surface set and _mode_switching), _reconnect_riding_next (round 9: the names of an arm made while a connect is in progress, moved into the riding set at the landing that does not serve them and cleared by the disarm that retires the arm; snapshot reads both sets), _disarm_locked and _retire_arm_for_live_pick (round 7: the landing's repicked branch retires the reconnect a superseded mode pick armed in the spawn window when the newer pick is a live one and the running shape with that mode is what the session asks for; a newer pick into bypass stamps the connect it will run and the arm stands, re-shaped), _reset_reconnect_state at the loop top (its ride line agrees in number; its read and clear one hold of the lock), _note_reconnect_ask, _live_work_counts, _pick_held and snapshot's pickHeld (the surfaces, the counts, whether a turn is open and the picked value of each held kind; None once every surface is withdrawn) with the running effort and mode while a pick is held, _connect_landed (the _launched_effort, _launched_mode, _launched_auth and _launched_env stamps and the end of the spawn window, _launching and _connecting cleared, all under one _hold_write since round 7, which also takes the landing's mode decision there: the reg's pick, the launched mode and whether a form is pending, returned to the loop and stamped as _mode_switching) and the landing's async half, _do_set_mode with landing=True (the landing rule since round 6: the switch is keyed on the PICK the reg carries, never on perm_mode, which follows the CLI's own report when no pick is pending, and applies only while a mode pick is pending in one of its forms; a mode pick the landed process does not run, other than one into bypass, is applied live over the control channel before the feeder is created, so no queued turn runs on the mode the user withdrew; a refusal keeps the queue held, records the pick's surface again and arms the reconnect; while the switch is in flight _mode_switching names its target as the running intent, so a bypass re-pick during the round trip arms the reconnect that applies it and a re-pick of the target is already applying; round 7: the non-landing path reverts only on the CLI's verdict, _cli_refusal, never on a lost answer such as a request a relaunch's teardown stranded, and a pick a newer one superseded stands down), SdkBackend._launch_shape (the one computation of the shape a connect hands the CLI, stamped as _launching at the arm and again by _options as it composes, which also marks _connecting and, since round 6, stamps _fast_unlocked from the one read of the fast ask it composed the flag-settings file with; round 7: every stamp lands at the READ, under one hold at the top of the compose, from one read of the billing pick that the shape, the helper suppression, the env branch and the unkeyed-pick stamp all derive from, and the loop clears _fast_expect before the compose), _do_set_mode stamps a confirmed live switch, the init handler keeping the declared mode while a mode pick is pending and stamping the reported mode as the running mode in both branches (round 6), SdkBackend.session_meta (round 6: the running effort, effortPending and pickHeld for the thread frame; fastPending and modePending since round 7), _adopt_fast_state's answer to the fast ask and its surface swap under the lock, the unchanged and already-applying guards in set_effort, set_auth and set_env (compared against the pending shape during a spawn; the already-applying branches in the spawn window route through the one withdraw routine), set_mode's already-applying guard and its spawn-window route (a pick made while a connect is in progress is a connect-time pick, never a live switch sent to no client; a bypass re-pick over a pending pick out of bypass on a process already in bypass only withdraws it; round 7: one hold reads the connect in progress, writes the pick and records a connect-time pick's surface, so the landing sees the pick whole or not at all), set_fast's off branch in the spawn window and the pending relaunch's surface following the fast ask (fast and fast-reset; the swap re-reads the ask under the lock and an ask the refusal answered flips nothing; round 7: the connection's flag and the connect in progress are read in the hold that writes the ask), snapshot's fastPending and modePending (round 7: a fast or mode pick pending on a reconnect in any of its forms, or the landing's switch in flight; what effortPending is for effort), one log line per setter, _picks_phrase (an empty list phrases as no pick), _can_use_tool's held-bypass consult, _RECONNECT_CAUSE; kernel/session_backend.py the abstract set_effort docstring (pickHeld in the backend contract); kernel/kernel.py: _park_op_locked logs each park, pickHeld through Sessions.live and build_session's status, fastPending and modePending through the same (round 7), _reconnect_pending (round 7: the one gate for the chat's reconnecting element, any pending flag or the hold, in build_session and _comments_frame), _reconnecting_event for every held kind (round 9: its picks list, the pending kinds the reloading line's title names), _pick_held_sig (the picked values included) and the fast and mode flags in the lane signature (round 7), _comments_frame (round 6: the thread's effort is the running value from session_meta on a live thread, pickHeld and effortPending ride, and the thread's events end on the reconnecting element under the live row's gate); ui/webview/pick-held.ts (the held copy, keyed on the counts and the turn phase; reloadingTitle, the reloading line's title naming the change the reload applies, round 9; heldUntil, the one clause every surface uses for when a held pick applies; heldRowValue, the tab tooltip's held row naming the picked value; heldMenuMarks and RUNNING_TAG, the one menu convention while a pick is held: the check on the picked value and a running tag on the value the session runs), comments.ts CommentThread's effortPending and pickHeld (round 6) and fastPending and modePending (round 7), render.ts renderReconnecting's held branch and its title from reloadingTitle (round 9), Status's fastPending and modePending and syncMetaControls' pending expression reading them for the mode and fast badges' pulse (round 7), threadMetaStatus carrying pickHeld and effortPending (and the fast and mode flags since round 7) into the comment popover's statusline (round 6: the popover surface marks a held pick as the chat's badges and menus do), armMetaPending and settleMetaHold (round 6: a click on the running row arms no local loader, and the hold-cleared frame retires one armed during the hold), the Billing flyout's fallback to st.auth when a held payload names no picked value (round 6), syncMetaControls' pending mark (aria-hidden), showTabTip's Mode and Effort rows while a pick is held, matchesMeta, metaRowMarks and runningTag for the statusline menus' rows and the tab menu's Billing flyout's marks, styles.css .meta-held-mark (the badge's own size, raised with align-self and a translate) and .running-tag, billing-label.ts the held billing row and sub-line through heldUntil and the same-side held row; tests/test_sdk_backend.py SettingsPickWaitsForLiveWork and SettingsPickThroughTheLoop (the stand-in client records set_permission_mode and can refuse it; since round 6 it yields like the real control request, logs writes and switches in order, and can hold a switch open; since round 7 it refuses in the SDK's shape, strands a request its teardown left pending, and takes per-call gates) and the LiveSubagentsRetire cause pin, tests/test_mode_truth.py the refusal fake in the SDK's shape, a bare Exception with no cause, and its request_reconnect stub taking the setter's keyword (round 7), tests/test_comment_threads.py the thread frame's hold case and its fast and mode flags, tests/test_kernel_parked_ops_lock.py ParkIsLogged, tests/test_session_auth.py the launching and landing stamps tests, tests/test_session_env.py SetEnv's request_reconnect stub taking the setter's keyword (round 7), tests/test_kernel_effort_reconnect.py the event and loop pins, the _reconnect_pending gate and the behaviour test of a held row and of the fast and mode flags through Sessions.live and build_session, tests/test_kernel_timeline_split.py the pickHeld sig cases and the fast and mode flags, tests/test_injected_voice.py the cause assertion and the key cycle word, tests/test_chat_build_sig_inputs.py the _reconnecting_event and _reconnect_pending census entries, ui/webview/pick-held.test.ts, effort-switch-pending.test.ts (the menu marks pinned and metaRowMarks executed; the running-row cancel walk executed since round 6; the mode and fast pulse from the server's flags since round 7) and billing-label.test.ts the held cases, auth-selector.test.ts the flyout's marks and its fallback executed, comment-popover-parity.test.ts the popover's hold and the fast and mode flags (threadMetaStatus executed), tab-backend-tooltip.test.ts the tooltip row pins, meta-held-mark-browser.test.ts the measured mark, tip.test.ts the setTip pin re-aimed to metaTip, codex-meta-choices.test.ts the lifted metaTip, matchesMeta, metaRowMarks and runningTag and model-switch-dots.test.ts the held gate, the settle line and the mode and fast flags in the dots pin, re-aimed in round 7 with the local loader's arm and retirement pinned beside it; docs/reference.md one paragraph
added: 2026-09-09
pr:
tier: fix
offered:
closed:
---
Fix tier. Picking effort, permission mode (bypass), fast mode or billing on a session with live subagents, harness background tasks or Workflow runs killed all of them: the reconnect arm read only the open turn (inflight and the untaken hold), never the live sets, and a session idle between its own turns reconnected at once. 26 subagents and 10 background tasks were lost across four sessions on 2026-09-09 to picks that changed nothing. Now a settings-driven reconnect arms only at a turn's settle that finds the live sets empty; a task's end never arms it at its own frame, since the CLI starts a turn of its own to deliver the result, and the removal only logs that the pick waits; a rewind keeps its old behaviour. Tests fail before the fix.
