---
title: A settings pick waits for the session's live subagents and background tasks before it reconnects the CLI; the launched value picked again reconnects nothing
status: candidate
where: kernel/sdk_backend.py: SdkSession._arm_reconnect_if_quiet (called at the request and the ResultMessage settle; a held pick arms only at a settle that finds the live sets empty, and no removal from the sets arms at its own frame), _note_work_ended (the removal sites' one log line), _withdraw_held_pick (a live mode pick retires a held bypass pick), _reset_reconnect_state at the loop top, _note_reconnect_ask, _live_work_counts, _pick_held and snapshot's pickHeld with the running effort and mode while a pick is held, _connect_landed (the _launched_effort/_launched_mode/_launched_auth stamps; _options records _launching; _do_set_mode stamps a confirmed live switch), the unchanged and already-applying guards in set_effort and set_auth (compared against the pending shape during a spawn), the already-applying guard in set_mode, one log line per setter, _can_use_tool's held-bypass consult, _RECONNECT_CAUSE; kernel/kernel.py: _park_op_locked logs each park, pickHeld through Sessions.live and build_session's status, _reconnecting_event for every held kind, _pick_held_sig; ui/webview/pick-held.ts (the held copy), render.ts renderReconnecting's held branch and syncMetaControls' pending mark, billing-label.ts the held billing row; tests/test_sdk_backend.py SettingsPickWaitsForLiveWork and SettingsPickThroughTheLoop and the LiveSubagentsRetire cause pin, tests/test_kernel_parked_ops_lock.py ParkIsLogged, tests/test_session_auth.py the launching and landing stamps tests, tests/test_kernel_effort_reconnect.py the event and loop pins, tests/test_kernel_timeline_split.py the pickHeld sig cases, tests/test_injected_voice.py the cause assertion and the key cycle word, ui/webview/pick-held.test.ts, effort-switch-pending.test.ts and billing-label.test.ts the held cases; docs/reference.md one paragraph
added: 2026-09-09
pr:
tier: fix
offered:
closed:
---
Fix tier. Picking effort, permission mode (bypass), fast mode or billing on a session with live subagents, harness background tasks or Workflow runs killed all of them: the reconnect arm read only the open turn (inflight and the untaken hold), never the live sets, and a session idle between its own turns reconnected at once. 26 subagents and 10 background tasks were lost across four sessions on 2026-09-09 to picks that changed nothing. Now a settings-driven reconnect arms only at a turn's settle that finds the live sets empty; a task's end never arms it at its own frame, since the CLI starts a turn of its own to deliver the result, and the removal only logs that the pick waits; a rewind keeps its old behaviour. Tests fail before the fix.
