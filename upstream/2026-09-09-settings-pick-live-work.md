---
title: A settings pick waits for the session's live subagents and background tasks before it reconnects the CLI; the launched value picked again reconnects nothing
status: candidate
where: kernel/sdk_backend.py: SdkSession._arm_reconnect (the one arm routine: the settle's arm, the idle pick's immediate arm, a pending rewind's request and the immediate-only form all schedule the reconnect through it; it opens the spawn window by stamping _launching and flips the surfaces of the picks riding it, fast to on and auth_live cleared), _arm_reconnect_if_quiet (called at the request and the ResultMessage settle; a held pick arms only at a settle that finds the live sets empty, no removal from the sets arms at its own frame), _note_work_ended (the removal sites' one log line), _withdraw_held_pick and _settle_withdrawal (the one withdraw routine in two halves: the kernel thread forgets the surface at once and the loop decides the hold's fate from the set it finds; the hold ends whenever no surface remains, rewind pending or not; a live mode pick retires a pending mode pick, and set_effort, set_auth, set_fast and set_env withdraw a pending pick reverted to the running value), _reset_reconnect_state at the loop top (its ride line agrees in number), _note_reconnect_ask, _live_work_counts, _pick_held and snapshot's pickHeld (the surfaces, the counts and whether a turn is open; None once every surface is withdrawn) with the running effort and mode while a pick is held, _connect_landed (the _launched_effort, _launched_mode, _launched_auth and _launched_env stamps and the end of the spawn window, _launching cleared), SdkBackend._launch_shape (the one computation of the shape a connect hands the CLI, stamped as _launching at the arm and again by _options as it composes), _do_set_mode stamps a confirmed live switch, the init handler keeping the declared mode while a mode pick is pending, the unchanged and already-applying guards in set_effort, set_auth and set_env (compared against the pending shape during a spawn), set_mode's already-applying guard and its spawn-window route (a pick made while a connect is in progress is a connect-time pick, never a live switch sent to no client), set_fast's off branch in the spawn window and the pending relaunch's surface following the fast ask (fast and fast-reset), one log line per setter, _can_use_tool's held-bypass consult, _RECONNECT_CAUSE; kernel/session_backend.py the abstract set_effort docstring (pickHeld in the backend contract); kernel/kernel.py: _park_op_locked logs each park, pickHeld through Sessions.live and build_session's status, _reconnecting_event for every held kind, _pick_held_sig; ui/webview/pick-held.ts (the held copy, keyed on the counts and the turn phase; heldUntil, the one clause every surface uses for when a held pick applies, and heldRowValue, the tab tooltip's held row), render.ts renderReconnecting's held branch, syncMetaControls' pending mark (aria-hidden) and showTabTip's Mode and Effort rows while a pick is held, styles.css .meta-held-mark (the badge's own size, raised with align-self and a translate), billing-label.ts the held billing row and sub-line through heldUntil; tests/test_sdk_backend.py SettingsPickWaitsForLiveWork and SettingsPickThroughTheLoop and the LiveSubagentsRetire cause pin, tests/test_kernel_parked_ops_lock.py ParkIsLogged, tests/test_session_auth.py the launching and landing stamps tests, tests/test_kernel_effort_reconnect.py the event and loop pins, tests/test_kernel_timeline_split.py the pickHeld sig cases, tests/test_injected_voice.py the cause assertion and the key cycle word, tests/test_chat_build_sig_inputs.py the _reconnecting_event census entry, ui/webview/pick-held.test.ts, effort-switch-pending.test.ts and billing-label.test.ts the held cases, tab-backend-tooltip.test.ts the tooltip row pins, meta-held-mark-browser.test.ts the measured mark, tip.test.ts the setTip pin re-aimed to metaTip, codex-meta-choices.test.ts the lifted metaTip and model-switch-dots.test.ts the held gate in the dots pin; docs/reference.md one paragraph
added: 2026-09-09
pr:
tier: fix
offered:
closed:
---
Fix tier. Picking effort, permission mode (bypass), fast mode or billing on a session with live subagents, harness background tasks or Workflow runs killed all of them: the reconnect arm read only the open turn (inflight and the untaken hold), never the live sets, and a session idle between its own turns reconnected at once. 26 subagents and 10 background tasks were lost across four sessions on 2026-09-09 to picks that changed nothing. Now a settings-driven reconnect arms only at a turn's settle that finds the live sets empty; a task's end never arms it at its own frame, since the CLI starts a turn of its own to deliver the result, and the removal only logs that the pick waits; a rewind keeps its old behaviour. Tests fail before the fix.
