---
title: The file viewer keeps a trail of the files reached through its links, with Back and Forward, and an Open the picture control on its figures
status: candidate
where: fork branch `filereview-linknav`, cut from the fork's main 34142c262: ui/webview/file-trail.ts (new: the trail's state and pure functions, trailRoot, trailPush, trailBack, trailForward, trailSetView, trailEnd, trailBackTarget, trailForwardTarget, fileNameOf, navTitle and navChord, and the live instance, liveTrail and setTrail), ui/webview/file-view.ts (openFromViewer and the trailNext tag; moveTrail after runLeave's write in the replace path; the bar's fileview-nav group with its two glyph buttons, hidden when neither direction has a target (T367; the PR's file review, round 2); onNavKey, one capture-phase document listener removed through the close hooks; closeFileView and openUrlView end the trail and the conflict Reload tags itself reload; the body delegate's path-link open through openFromViewer; the figure half: FIGURE_OPEN_TITLE, figureTarget (refusing on figureHasPicture, one rule over FigureState with figureWantsControl, a target or a control only for a state with a picture to name, so a fetching or a failed figure opens nothing on any gesture and a state added later is refused with them; the PR's file review, round 2, the rule in place of a list of the two before its round 3) and chosenSource (the candidate the browser chose, as the author wrote it, which failedSource delegates to; the review's round 2), figureControlOf, figureOfControl, bareFigureOf, shownAddress (the one stripper of a picture's address: parsed against the document, its sign-in part, query and fragment emptied, so a title shows origin plus path, and one the parser refuses cut as text through its last @ and then from its first ? or #; since the fixes for the file review's round 14, correctness-1) and shownSource (the failed figure's label: a data: source cut to its head, a source with a scheme or a leading // through shownAddress, a workspace path as written; the same fixes, extra5-3), linkAbove (a link of the click's own set above figureAnchor's climb: figureLinkOf over FIGURE_LINK_SET, an anchor with an href, the viewer's two link classes and the openpath control, the one exported predicate the click listener, dressFigureTitle and linkAbove read, so a dead link or an author's named anchor withholds nothing; since the fixes for the file review's round 12, correctness-1 with ui-1, which had read any anchor here), figureState with figureHasPicture, figureBox (the laid-out box of a figure in the document as it is, 0 by 0 included, so a loaded figure the author gave no box is under the floor; the picture's own size only for a figure not yet in the document; the file review's round 3, correctness-1) and the FIGOPEN_MIN_PX floor with figureTooSmall, the one decision decideFigureControl over the verdict figureWantsControl, with the dress it applies at every decision, dressFigureControl (the control's words, glyph and class keyed on the target), dressFigureTitle (the picture's own title line carrying the address, origin plus path through shownAddress) and dressFigureMark with FIGWEB_MARK (data-fv-figweb, the outbound mark on a web picture that wears no control, set from the verdict right after the title; since the fixes for the file review's round 12, fresh-1), figureFoldOf (a fold's own first summary, a sibling of FIGURE_LINK_SET that the click listener, dressFigureTitle and dressFigureMark read and linkAbove does not, so a picture there yields its click, plain or modified, to the fold, wears no title line and no mark and keeps its control, which opens it; since the fixes for the file review's round 14, fresh-1), addFigureControls in mdBlock's file arm, armFigureControls (the load and the error), watchFigureBoxes (one ResizeObserver per open over the figures of the Rendered box, armed at each text paint and not before the first, in place of a re-read from the width watch's repaint, which missed a text-size step, the PR's file review, round 2; a 0 by 0 report runs no decision, a rule over the report whatever produced it since the file review's round 4 (regression-3: before its round 3 the reason named the viewer's hide and a gated placeholder's img as the roads to such a report, reporting the real box at the show or the restore; narrowed in its round 3 and stated with its residual by the author's closing pass after the file review's round 3 (behaviour-4), the list still closed, and a loaded figure the author gave no box outside it), the residual a figure hidden after its load by any other road keeping a standing control until its next report with a box, which the product reaches by a `<details>` folded by the reader (an expanded callout renders as one), harmless there since the fold reports nothing and folds the control with the figure and the reopen's first report with a box decides the figure again (the file review's round 4, ui-1; the line before it said no road in the product reached it), and the arm before the first paint removed in the file review's round 3) and removeFigureControl with the keyboardTakers register (the keyboard handed to the viewer's body before a control's removal), the body's second click listener with openFigure and the figure's own door openFigureInViewer, the body's mousedown listener cancelling a press of any mouse button on the web control, so no press focuses it, and keyOnHiddenWebControl with controlInView, Enter or Space opening the web control only while its box meets the viewport and the scrollport of every ancestor that clips on that axis, read at each key (since the fixes for the file review's round 14, ui-1 with extra9-1), and figureControlAfter with figureLabelAfter's step past the control; since the author's closing pass after the file review's landing round mdBlock's chain block comment states the census's reach, the reached locals' imported callees' modules and their imports under every import form, and its judged sites with their reasons, in place of a no-write claim the widened census contradicted), ui/webview/icons.ts (ICON_BACK, ICON_FORWARD, ICON_EXPAND, and ICON_OUTBOUND since the fixes for the file review's round 11, ui-1 with extra8-1: a web picture's control wears the outbound glyph, names the host in its words and carries the class fv-figopen-web, and the picture's own title carries the address after an author's), ui/webview/styles.css and ui/webview/feed.css (the `.fileview-md .fv-figopen` rules, the same lines in both, the dashed `.fv-figopen-web` rule among them since those fixes, and the two `.fileview-md img[data-fv-figweb]` rules under screen at-rules, the mark's outline on hover and at rest where hover is none or any pointer is coarse, since the fixes for the file review's round 12, fresh-1; both dresses in the outbound dress's own token, `--outbound-line`, declared in both theme blocks of both sheets and clearing 3 to 1 over the page in each theme, the control's rest colour a rule of its own, `.fv-figopen-web:not(:hover)`, and both at-rest at-rules keyed on hover none or any pointer coarse with screen on both queries, since the fixes for the file review's round 13, ui-1 with extra6-1 and extra7-2; and four more rules under screen at-rules since the painted-contrast ask of 2026-09-23, the web control resting at full opacity where the at-rest rule stands, a local one at 0.8, its focus ring standing 2px off its border, the button family's press cue off on it, since the release that opens the tab comes while it is held, and a dead link holding either dress dimming by colour and not by opacity, so every state a gesture opens the tab from, inside an href-less dead link too and the press held, paints the dress at 3:1 or better, the token's line at the token's own ratio (4.83:1 dark, 4.90:1 light), the mark's outline in each of its states and the web control's border at rest, revealed by the pointer over its picture, under any focus and under a Space press from it, and the family's accent border at 7.87:1 dark and 3.79:1 light against its hover wash, the web control's under the pointer and in a pointer press, as the file review's round 14, extra7-1, found this line had to split them; and since the fixes for the file review's round 14 both mark rules stand on a ring of var(--bg) 3px wide under the outline, with a margin rule at rest under screen, 1px up and down and 7px across the line, each axis the least that keeps the ring off the neighbours' ink, what still reaches it stated in the comment, correctness-2 with extra5-1 and extra5-2 and the margin ruling of 2026-09-24, and the reveal rule carries `.fileview-md .fv-figopen-web:focus`, so any focus on the web control is painted, ui-1 with extra9-1), ui/webview/real-viewer-leg.ts (inBrowser takes optional launch options handed to playwright's launch, so the open leg's hybrid twin runs Chromium under Blink's own device settings, every existing caller unchanged, in the file review's round 13, extra7-2), docs/guide.md (the two closing sentences of the Links in a file paragraph; since the author's closing pass after the file review's round 3 (behaviour-2, records-1, coverage-1) the trail sentence names the pictures opened from figures among what the trail keeps and states that the two arrow buttons appear once there is a file to step back or forward to, after a link is followed or a picture opened, and not before; its round 3 had conditioned them on a link followed, which is false on the picture road; and, since the fixes for the file review's round 14, the figure sentence's clauses on a web picture's button opening from Enter or Space only while it is on the screen, ui-1 with extra9-1, on the picture's tooltip showing the address without its sign-in part, query and fragment, correctness-1, and on a picture in a fold's title line, whose click opens or closes the fold, fresh-1, and the Figures paragraph's failed-figure sentence, where a web address shows without its sign-in part, query and fragment, extra5-3); tests: ui/webview/file-trail.test.ts, ui/webview/file-trail-browser.test.ts, ui/webview/file-figure-open.test.ts and ui/webview/file-figure-open-browser.test.ts (new; since the author's closing pass after the file review's round 8 the browser leg drives the reader's place on a top-level html-block figure wearing the production control, the Raw switch landing on the img's own row, the layout the node scene's fixture box stands in for; since the fixes for the file review's round 14 the browser leg reads the mark by its worst dash-to-ring ratio on the highlight, table header, even-row and note-callout grounds and holds that the ring covers no neighbouring ink and that the dash beside a neighbour reads 3:1 on both sides, runs of f glued to the picture among the neighbours and, in a case of its own on the Files pane, a left float in a note callout, correctness-2 with extra5-1 and extra5-2 and the margin ruling of 2026-09-24, drives the web control's focus pins (a) to (f), pins (d) and (f) by Space and by Enter, with a key-release pin, an other-button pin and four keep checks, ui-1 with extra9-1, and the fold cases with their two controls, fresh-1, and the node module holds the closed set with the margin rule, the fold's source pins and the Mouse census read by the compiler with its armed plants, controls and bound, tests-1 with extra6-3), ui/webview/file-view-figure-shapes.test.ts and ui/webview/file-view-figure-shapes-browser.test.ts (new in the review's round 1: figureTarget reads the web address before the model's join, no control stands inside a link that holds more than the figure, the bare figure's click yields to an enclosing link, and the 48px floor, pinned at source and driven in Chromium; since the file review's round 3 the source pins derive the refused states and hold their literals absent from every reader, since its round 4 read as the compiler reads them through ui/webview/source-units.ts, the reader the attribution pin shares, with the pin's boundary (whole literal values and regular expressions; not an identifier key, an assembled word, a prefix or a case-folded test) stated in its message and pinned by execution since the author's closing pass after the file review's round 4 (guards-1 with records-11), and hold the arm before the first paint absent), ui/webview/file-view-figure-chosen.test.ts and ui/webview/file-view-figure-chosen-browser.test.ts (new in the review's round 2: figureTarget reads chosenSource, pinned at source and driven in Chromium over a picture element, a srcset img and a gated remote picture; the browser leg observes the remote figures' requests at a context-level route with the real window.open since the PR's file review, and since its round 2 from before the open, with a never-drained list of foreign window.fetch calls, the Cookie header of each request read under three seated classes, the loaded picture's Ctrl-click and the failed remote figure's clicks), ui/webview/file-view-figure-floor-browser.test.ts, ui/webview/file-view-figure-state-browser.test.ts and ui/webview/file-view-figure-recent-browser.test.ts (new in the PR's file review, 2026-09-20: the floor re-read at each reflow of the figure, the body's width and a text-size step alike, the keyboard handed on at a control's removal, the hidden viewer's 0 by 0 report adding no control while hidden, and, its round 3, a loaded `<img hidden>` and `<img width="0">` beside prose getting no control with the click on the words before them opening nothing, the click point derived from the words themselves since the author's closing pass after the file review's round 3 (behaviour-3), and a picture re-fetched while the viewer is hidden losing its control at that load and regaining it at the show; the control decided from the figure's current state, a failed figure with a box opening nothing; and no Recent row for a picture opened from a figure while a Forward step onto it mints one, each driven in Chromium), and re-aimed ui/webview/file-view.test.ts, ui/webview/file-view-links.test.ts (its listener pin follows the mousedown listener's cancel of a press on the web control since the fixes for the file review's round 14, ui-1 with extra9-1), ui/webview/file-view-text-size.test.ts (its bar case reads the group hidden on a fresh open), ui/webview/fileview-parity.test.ts (and, in the file review's round 10, correctness-5, its Outline button census on parsed rules over every sheet a page of either host loads, and its heads the mark's margin rule and the reveal's `:focus` member since the fixes for the file review's round 14), ui/webview/file-view-outline.test.ts (the same census, and since the fixes for the file review's round 14, correctness-1, the title cases beside its credential case, a query token, a signature pair, a fragment token, a sign-in part with a query and addresses the parser refuses, each title origin plus path), ui/webview/theme-parity.test.ts (the outbound line's pair, --outbound-line over --bg at 3 to 1 in both themes of both sheets, and feed's evaluated count, in the file review's round 13, ui-1 with extra6-1; and the painted test beside it since the painted-contrast ask of 2026-09-23, every state a gesture opens the tab from composed from the declared opacities over the worst picture and on the VS Code editor grounds, whose executed twin is the open leg's paintedRatio; since the fixes for the file review's round 14 the mark's ring read off both sheets, correctness-2, and the reveal's `:focus` member among the painted states, ui-1), ui/webview/file-view-notice.test.ts (its Escape cases run every keydown handler an open registers in the document's order, the capture phase first, and its shim removes a listener only under the capture flag its add carried) and ui/webview/md-url-view.test.ts (the local-file mode's sibling link opens through openFromViewer with a push, and the door's shape: the tag set, the host's opener called, the tag cleared in a finally) and ui/webview/pdf-new-tab.test.ts (the sweep repair: its count of the gesture reads inside openFileView counts the links' two and the figure's four apart and the rest of the function as none, so openFileView's own open still reads none), and ui/test-code-only.ts (the comment stripper the source pins read through: its header's reader roster names ui/webview/file-figure-open.test.ts as its sixth reader since the file review's round 13, extra7-1 with extra6-2, whose count of figureLinkOf's calls and census of the arguments of the Playwright Mouse calls its compiler walk reads read the code alone, the walk a callee that is a member of a member named mouse, through parentheses, a non-null assertion and the type forms, each member a property access or a string-literal element access, a computed key, an alias and a destructured binding outside it, each witnessed by a plant, since the fixes for the file review's round 14, tests-1 with extra6-3); the trail half (file-trail.ts, the bar's group, the chords, the two arrows) reads nothing upstream lacks, and the figure half reads fork-only modules (figurePath from file-comments-model.ts, panelMark from file-comments.ts, GATE_ACT from figure-gate.ts, the regions layer's wrap) and its browser legs boot the fork's real-viewer-leg.ts, files.ts page and md-config.ts wikilinks, so an offer adapts them rather than copies; FORK-ONLY: ui/webview/anchor-map.ts and ui/webview/reader-place.ts (CONTROL_CLASSES gains fv-figopen; anchor-map's isFigureCompanion; since the file review's round 8, correctness-1, reader-place's structural read leaves a figure's two companions out of the element lists it walks by the same pair predicate, blockElementsOf with isFigureCompanion, so a top-level figure wearing the control reads as the figure and not as the paragraph after it) with their suites ui/webview/anchor-map-fallback-markup.test.ts, ui/webview/file-view-figure-error.test.ts and ui/webview/file-view-figure-error-browser.test.ts (re-aimed: the label readers step past the control; since the fixes for the file review's round 14, extra5-3, the error suite's node unit, which CI runs, calls shownSource directly and through the error listener, and the error leg's Chromium label cases relay the synthetic hosts from a second local server, no label printing a sign-in part, a query or a fragment) and tests/test_guide_files_failures.py (re-aimed: first to hold both walks' control lists' seventh entry, fv-figopen, then, the file review's round 8, correctness-1, to name the executed cases of the control's exclusion in that pin's place, a pin on a list's membership having stayed green while the structural read ignored both lists; its failed-figure sentence gains the web-address clause since the fixes for the file review's round 14, extra5-3), ui/webview/file-view-place-blocks.test.ts (re-aimed in the file review's round 8, correctness-1: readPlace over a top-level figure wearing the control, at the root's level and nested in a wrapper, and over a right-floated figure wearing the failed figure's label at its top, reads the figure; red before the fix at the paragraph after it), ui/webview/file-view-seam.test.ts (re-aimed at the merge of the fork's main and in the file review's landing round: its re-parse census's callee list gained addFigureControls, this follow-on's pass, with the local functions it reaches, and the walk follows a reached local's imported callees and every import form transitively, judging the sites it finds per module, with the whole file's count in its one home; since the author's closing pass after the file review's landing round its RE_PARSE matches a template argument under any spelling, the form its derivation command and the plan's paragraph spell, the same lines at that head, and its test's name counts the third judged site on the road, the glyph's holder after the adoption; since the author's closing pass after the file review's round 8 its method-call guard reads the compiler's tree under any access spelling, its binding reader follows a require through a cast, parentheses, a non-null mark or a member, and its three compiler readers refuse a module the parser reports a diagnostic on; REACHED_LOCALS names figureFoldOf since the fixes for the file review's round 14, fresh-1), ui/webview/anchor-map.test.ts (re-aimed in the file review's landing round: the control at the box's top level is no block's node, executed beside the failed figure's label's case; and, extra9-1, the caption beside a control carrying a text node of its own maps, the executed case of anchor-map.ts's entry) and ui/webview/md-config-figure-gate-place.test.ts (re-aimed there too: readPlace over a loaded figure wearing the control, and, extra9-1, over one whose control carries a text node of its own, the executed case of reader-place.ts's entry), ui/webview/compact-seam-exec.test.ts and ui/webview/spacer-measure.test.ts (two of main's censuses, which the merge of the fork's main at fa3ef54b5 brought, re-aimed after it: the closed multiset of class mutators whose class is no constant of the source names decideFigureControl's classList.add, the control's float side read off the figure's align, and dressFigureControl's classList.toggle, the web class declared by a concatenation the census does not fold; the roster of the modules under ui/webview no page bundle loads names ui/webview/css-rules.mjs, ui/webview/host-sheets.mjs and ui/webview/source-units.ts, the readers this follow-on's tests import; and the closed multiset of the writes of a view property names the trail's two, trailSetView's and moveTrail's, kept in the page's trail state and never posted), and the fork's records with their pin: plans/markdown-viewer.md (the section Follow-on: Link navigation, L1 to L6, and Slice 7's failed-figure label formula with its stripping rule since the fixes for the file review's round 14, extra5-3), plans/file-browser.md (the pointer sentence in the navigation-stack section, the glyphs' condition added in the file review's round 3), tools/markdown-viewer-plan-linknav.test.mjs (new; since the fixes for the file review's round 14 its reveal constant carries the web control's `:focus`, ui-1, its helperStyle pins refuse any str.<method>( before the run and a constant-held or split opening tag, with pins recording the over-refusal and the bound, extra6-1 and extra6-2, and its copy of the guide's figure sentence moves with the guide), ui/webview/css-rules.mjs with ui/webview/css-rules.d.mts (new in the file review's round 9, correctness-1 with tests-1 and ui-1: the sheet's rules read as rules, each with the at-rules enclosing it, the one reader the closed set over the control's sheet rules stands on in ui/webview/file-figure-open.test.ts and in tools/markdown-viewer-plan-linknav.test.mjs, where the set had been keyed on lines at column zero; under tools/ at that round's fixes, and under ui/, the tree the bundles read, since the author's closing pass after the file review's round 10, because a ui test may not import from tools/, hooks/ or tests/, trees outside the kernel's bundle-staleness inputs, which tests/test_lab_dist.py's derivation pin holds), ui/webview/host-sheets.mjs with ui/webview/host-sheets.d.mts (new in the file review's round 10, correctness-1 with regression-5 and correctness-5: every sheet a page of either host loads, derived from the page assembly, the kernel's page functions and the extension's webview builders, the population the closed set and the Outline census read through the reader; under ui/ for the reader's reason; since the fixes for the file review's round 11, extra6-1 with extra7-1, kernel-1 and tests-1, the style blocks the helpers a page calls write into its HTML at serve time are followed one level, and the page read fails by name on a def in a shape it has no rule for and reads a body through a triple-quoted literal, the file review's round 11, correctness-1 with regression-1 and extra7-2; since the fixes for the file review's round 14 helperStyle refuses any str.<method>( standing before the run, extra6-1, and detects a helper by either tag, so a closing tag with no opening literal it finds fails by name, extra6-2), tools/markdown-viewer-plan-gate-adopt.test.mjs (main's pin of the gate-before-adoption section, re-aimed at the merge: its held copy of that section's re-parse paragraph, re-derived in the file review's landing round with the derived figures filled from the seam test's pins, and its held copy of the chain block's sentence and of that paragraph following the author's closing pass after the file review's landing round; since the author's closing pass after the file review's round 8 its no-typed-addend guard over that paragraph is keyed on the property, a number word within forty characters of a module token in either order), tools/markdown-viewer-plan-linknav-review.test.mjs (new in the review's round 2: the record's corrections held to the source; its condition pin follows L6's and open point 11's words since the fixes for the file review's round 14, extra8-1), ui/webview/linknav-records-attribution.test.ts with ui/webview/source-units.ts (new in the file review's round 4, rules-1 with tests-2: every round a record of the follow-on names belongs to the review named nearest before it and is one the plan's convention enumerates for that review, a pass of the author's has no rounds and an id of its family stands only where the review named nearest before it is the author's pass, and since the fixes for the file review's round 11, fresh-1, a finding cited under a round of the file review is one that round's fixlist filed, held to the roster the plan's convention carries beside its enumeration, read in every checkout as a rule over the tree, every file git lists at the repo root, tracked or untracked and not ignored, whose text names the file review or carries an id of the author's family, the files the branch created (an existence roster, judged in full) and the plan's section, the guide's paragraph and the browser plan's pointer, a phrase outside those judged only where the review named nearest before it is the file review or the author's pass (the file review's round 5, extra5-1 with tests-1, regression-2, correctness-5 with tests-6 and correctness-6 with tests-5 and extra8-4: the first road had read a roster of eighteen files, checked against the diff only by a second road that runs in no CI job; a new uncommitted file is in the population, which no diff lists; file-view.ts's count of this follow-on's units is held equal to the derived count; the reader refuses a suffix it has no rule for naming the file, correctness-7 with tests-4 and extra7-2), and, kept and disclosed as running in no CI job and in none after the merge, from every unit the branch added or touched since the merge-base behind a two-part gate, the base not origin/main and the diff adding the module, the gate a pure function pinned in all four cells and run against a temp repo shaped as the open PR branch, the same round's tests-7 (the file review's round 4, extra8-1, so main, every later branch after the landing and a batch head cut from main's tip stand the road down; a batch head main has moved under runs it over the batch's whole delta, disclosed and not closed by the author's closing pass after the file review's round 4, attribution-and-gates-2 with records-2, which also had the road diff the working tree against the merge-base, guards-3, read the plural and range forms of a round and state the forms outside the reader, guards-2 with attribution-and-gates-3, charge a fault in the plan's section to its plan line, attribution-and-gates-4, and read file-view.ts's units carrying an id of the family in every checkout, records-1), TS and JS read through the compiler's parser and Markdown and Python as paragraphs; the rule stood in the review module since the file review's round 3, keyed on one string and one apostrophe, and the author's closing pass after the file review's round 3, behaviour-6 with records-2 and coverage-2, gave it its two roads; since the author's closing pass after the file review's round 8 its exported count of checks keyed on the delta is held to its own call sites, read with the comments blanked; since the fixes for the file review's round 14 the plan's convention enumerates that round with the ids its fixlist filed, and the count of file-view.ts's units counts the units those fixes add), tests/test_guide_trail_chords_and_figure_button.py (new in the review's round 2: the guide's two sentences held to the shell script's lines in kernel.py and to the viewer's; re-aimed in the PR's file review with a census of figureWantsControl's refusal arms, the failed picture among the four kinds without the button, in its round 3 with the refused states' literals held absent by the property, that copy removed in its round 4, regression-2 with extra6-1, for the one pin in ui/webview/file-view-figure-shapes.test.ts, and since its round 4, extra8-3, asserting the hide line, the guide's condition clause and the browser plan's clause together in one test; its copy of the guide's figure sentence moves with the guide since the fixes for the file review's round 14) and this entry; 53 files in all by `git diff --name-only $(git merge-base origin/main HEAD) HEAD`, run 2026-09-23 at the head that carries the fixes ruled in the file review's round 14, where the merge-base is fa3ef54b5, the fork's main the branch merged after the painted-contrast ask of 2026-09-23, its tip the merge of the fork's batch PR 907, every one of them named in this line (the run at the head that carries the entries two of main's censuses owe this follow-on's modules, after the merge of the fork's main, from the merge-base fa3ef54b5, listed the same 53; the run at the head that carries that ask and the merge of the fork's main after it, from the merge-base fa3ef54b5, listed 51, before ui/webview/compact-seam-exec.test.ts and ui/webview/spacer-measure.test.ts joined the delta in those entries; the run at the head that carries the fixes ruled in the file review's round 13 and the merge of the fork's main after them, from the merge-base 6cf6839ba, the fork's main the branch merged after those fixes, its tip the merge of the fork's batch PR 906, listed the same 51; the run at the head that carries the dress fixes alone among them, from the merge-base de06b8718, the fork's main the branch merged before the file review's round 13, listed 50, before ui/test-code-only.ts joined the delta in the case fixes; the run at the head that carries the fixes ruled in the file review's round 12, from the merge-base de06b8718, the fork's main the branch merged before the file review's round 13, listed 48, before ui/webview/theme-parity.test.ts and ui/webview/real-viewer-leg.ts joined the delta in those dress fixes; the run at the head that carries the fixes ruled in the file review's round 11, from the merge-base 4ae3b6659, the fork's main the branch merged for the file review's round 12, listed the same 48; the run at the head that carries the fixes ruled in the file review's round 10, from the merge-base 53f9f4aa3, the fork's main the branch merged for the file review's round 11, listed the same 48; the run at the head that carries the author's closing pass after the file review's round 10, from the merge-base 65f1895f6, the fork's main the branch merged for the file review's round 10, listed 45, before ui/webview/host-sheets.mjs, ui/webview/host-sheets.d.mts and ui/webview/file-view-outline.test.ts joined the delta in those fixes; the run at the head that carries the fixes for the file review's round 8, where the merge-base was 5e5ee79ed, the fork's main merged into the branch before landing, listed 43, before ui/webview/css-rules.mjs and ui/webview/css-rules.d.mts (under tools/ until the author's closing pass after the file review's round 10) joined the delta in the fixes for the file review's round 9; the run at the head that carries the landing round's first fixes, where the merge-base was 01434a45b, the fork's main the branch had merged for the file review's round 5, listed 42, before ui/webview/file-view-place-blocks.test.ts joined the delta in the fixes for the file review's round 8; the run at the head after that merge, for the file review's round 5, listed 40, before the landing round's executed test re-aimed two more standing suites, and this line then named 39 of them, file-view-seam.test.ts unnamed; the two earlier runs, at the commit that built the one reader for the file review's round 4 and at the head that closed the author's closing pass after the file review's round 4, listed 38 from the older merge-base they shared, before the merge made main's tools/markdown-viewer-plan-gate-adopt.test.mjs a file the branch edits; the branch point became an ancestor of main when main was merged in, so a diff from it no longer names the branch's files), this entry among them
added: 2026-09-19
pr: 862
tier: feature
offered:
closed:
---
A link followed inside a shown file replaced the card with no way back but the Files pane's Recent list. The viewer now keeps a trail of the files reached through its own links (a plain state with pure functions, file-trail.ts; the viewer's own opens take one door that tags the open, so an open from outside is one by construction) and shows Back and Forward as two glyph buttons at the left of its bar, with Cmd+[ and Cmd+] on a Mac, and Alt+Left and Alt+Right on a Files or chat page open in a browser tab of its own, as chords while the file is not being edited and no text box holds the keyboard (in the dashboard the shell takes Alt+Left and Alt+Right to move the keyboard between the panes; the file review's round 14, fresh-2). Back re-opens the previous file with no target, so the remembered place re-seats it, in the view it was left in, without touching the saved preference; an open from outside the viewer starts the trail over, and closing the viewer ends it, as does, under the chat's document-level opener, a plain click on a same-origin absolute .md or .markdown link inside the shown file (the chat's link rule, md-links.ts's isMarkdownUrl, case-insensitive), which replaces the card with the URL view (openUrlView) and ends the trail the same way (the file review's round 9, ui-2: this entry and the guide had named the close alone, the plan's L1 both; on the Files page, whose document has no such opener, the link opens a tab and the trail stands, driven in Chromium by file-trail-browser.test.ts; the replace is held in the tree by file-trail.test.ts's wiring pin). A picture a rendered file embeds from a file or a web address wears an Open the picture glyph button after it (a sibling, never a wrapper; decided from the figure's current state at the paint, the load, the error and each reflow of the figure's own box, the body's width and a text-size step alike: none while the picture is fetching or after a failed load, on a data: picture, on one under 48 px on either side, or inside a link a click can follow that holds more than the picture; a dead link, or an anchor that only marks a place, is not one, so a picture inside it keeps its control and its title, the file review's round 12, correctness-1 with ui-1), revealed by the pointer or a keyboard focus, the web control by any focus, and shown at rest where hover is none or any pointer is coarse, that opens the picture in the viewer through the same push, so Back returns to the file at the figure's place; the picture opened is the candidate the browser chose (a picture element's source, a srcset candidate), never the fallback src; a plain click on the picture does the same while the Comments panel is closed, a Cmd/Ctrl-click on the picture opens the /file URL in a tab while the panel is closed or the pointer is coarse (with the panel open on a fine pointer the layer's overlay takes the press, modified or not, and offers a comment; the control opens at any time), and neither click opens a picture inside a fold's own summary, whose click toggles the fold, while its control stays and opens it (the file review's round 14, fresh-1); a loaded remote picture opens its own address in a top-level tab (the control by a click at any time and by Enter or Space while it is in view, the plain click on the picture with the Comments panel closed, and the Cmd/Ctrl-click where the press reaches it, the panel closed or the pointer coarse, neither click on a picture inside a fold's own summary, whose click toggles the fold; no mouse press leaves the keyboard on the web control, the file review's round 14, extra8-1 with ui-1 and extra9-1; a second, differently kinded, credentialed request: a document request to a host the page had requested the image from, carrying cookie classes the image request did not, the Lax class in Chromium's measurement, where before only an author's link opened one: the PR is a privacy surface and lands on the owner's word; the gate is not bypassed), a fetching or a failed figure opens nothing on any gesture, and a gated placeholder gets its control once loaded; the outbound case is shown before the click: a web picture's control names the host in its words and wears the outbound glyph and a dashed border, the picture's own title carries the address, origin plus path with no sign-in part, query or fragment (the file review's round 14, correctness-1), after an author's, and a web picture that wears no control, one under the floor, wears the dashed outline itself, on hover on a fine pointer and at rest where hover is none or any pointer is coarse, both dresses in a token of the outbound dress's own that clears 3 to 1 over the page in both themes (the file review's round 11, ui-1 with extra8-1, its round 12, fresh-1, and its round 13, ui-1 with extra6-1 and extra7-2); the picture opened from a figure takes no Recent row, and a Forward step onto it does. The Back and Forward group is hidden when neither direction has a target (T367). Browser history is not integrated (recorded as a follow-on with the trade-off). No kernel change, no new route. The trail half reads nothing upstream lacks; the figure half reads the fork's comments model, gate and panel, so an offer adapts it (the where line). Records in plans/markdown-viewer.md, Follow-on: Link navigation.
