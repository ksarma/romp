---
title: The file viewer keeps a trail of the files reached through its links, with Back and Forward, and an Open the picture control on its figures
status: candidate
where: fork branch `filereview-linknav`, cut from the fork's main 34142c262: ui/webview/file-trail.ts (new: the trail's state and pure functions, trailRoot, trailPush, trailBack, trailForward, trailSetView, trailEnd, trailBackTarget, trailForwardTarget, fileNameOf, navTitle and navChord, and the live instance, liveTrail and setTrail), ui/webview/file-view.ts (openFromViewer and the trailNext tag; moveTrail after runLeave's write in the replace path; the bar's fileview-nav group with its two glyph buttons, hidden when neither direction has a target (T367; the PR's file review, round 2); onNavKey, one capture-phase document listener removed through the close hooks; closeFileView and openUrlView end the trail and the conflict Reload tags itself reload; the body delegate's path-link open through openFromViewer; the figure half: FIGURE_OPEN_TITLE, figureTarget (refusing on figureHasPicture, one rule over FigureState with figureWantsControl, a target or a control only for a state with a picture to name, so a fetching or a failed figure opens nothing on any gesture and a state added later is refused with them; the PR's file review, round 2, the rule in place of a list of the two before its round 3) and chosenSource (the candidate the browser chose, as the author wrote it, which failedSource delegates to, reading the chat page's heal record of a parked img when the candidate names nothing and the img has no src, since the fixes for the file review's round 15, fresh-1, and leaving a record equal to the page's own base, absUrl(""), unread, so an empty destination on the chat modal says the source is empty, since the fixes for the file review's round 16, correctness-1; the review's round 2), figureControlOf, figureOfControl, bareFigureOf, figureSourceCredentialed (the one sign-in rule every visible word of a picture's address reads, the picture's title, the web control's title and aria-label and the failed label: an at sign, ASCII or a lookalike, found after the text's percent-escapes are decoded until it stops changing, by decodeEscapes over decodeEscapesOnce, at most DECODE_PASSES, eight, passes since the fixes for the file review's round 16, regression-2, a text still changing at the bound read as carrying a sign-in at both of the rule's reads and the passes counted for the pin by decodePasses, anywhere after a scheme other than data: or after a leading run of two or more slashes or backslashes, in the head a data: source's label prints, or after a colon in a source with neither, withholds the whole address behind FIGURE_ADDRESS_WITHHELD, and the control's words are then FIGURE_OPEN_WEB_WITHHELD; since the fixes for the file review's round 15, correctness-1 with extra5-1, extra6-2, tests-1 and extra9-3, in place of the disclosed residual of two credential spellings, which the coordinator withdrew), shownAddress (a picture's address as its origin alone, parsed against the document, and one the parser refuses cut at its authority by authorityCut, which targetHost's fallback shares; since the fixes for the file review's round 14, correctness-1, which emptied the sign-in part, the query and the fragment, and for the file review's round 15, extra9-2, which took the path off too; its docstring, figureOpenWebTitle's and targetHost's state the sign-in rule's cost since the author's closing pass over those fixes, and controlInView's the frame padding the walk does not read) and shownSource (the failed figure's label: a source that appears to carry a sign-in withheld first, a data: source cut to its head, a source with a scheme or a leading // through shownAddress, a workspace path as written; since the fixes for the file review's round 14, extra5-3), linkAbove (a link of the click's own set above figureAnchor's climb: figureLinkOf over FIGURE_LINK_SET, an anchor with an href, the viewer's two link classes and the openpath control, the one exported predicate the click listener, dressFigureTitle and linkAbove read, so a dead link or an author's named anchor withholds nothing; since the fixes for the file review's round 12, correctness-1 with ui-1, which had read any anchor here), figureState with figureHasPicture, figureBox (the laid-out box of a figure in the document as it is, 0 by 0 included, so a loaded figure the author gave no box is under the floor; the picture's own size only for a figure not yet in the document; the file review's round 3, correctness-1; the box in the picture's own CSS pixels, the rect divided by currentCSSZoom and rounded to a sixteenth of a pixel, the rounding the browser's own zoom on the web dashboard needs, a transform on the figure not taken off, since the fixes for the file review's round 16, fresh-1) and the FIGOPEN_MIN_PX floor with figureTooSmall, the one decision decideFigureControl over the verdict figureWantsControl, with the dress it applies at every decision, dressFigureControl (the control's words, glyph and class keyed on the target), dressFigureTitle (the picture's own title line carrying the address's origin through shownAddress, or the withheld address when the source the target carries appears to carry a sign-in; figureTarget's web target carries that source beside its resolved address since the fixes for the file review's round 15, correctness-1) and dressFigureMark with FIGWEB_MARK (data-fv-figweb, the outbound mark on a web picture that wears no control, set from the verdict right after the title; since the fixes for the file review's round 12, fresh-1), figureFoldOf (a fold's own first summary, a sibling of FIGURE_LINK_SET that the click listener, dressFigureTitle and dressFigureMark read and linkAbove does not, so a picture there yields its click, plain or modified, to the fold, wears no title line and no mark and keeps its control, which opens it; since the fixes for the file review's round 14, fresh-1), addFigureControls in mdBlock's file arm, dropDimmingClasses there right after the sanitize, which takes every class of SHEET_DIM_CLASSES, the classes the sheets dim, off each img and svg image of a file document and every element above it, since the fixes for the file review's round 16, extra5-2, dropPressThrough beside it, which takes off every author element of a file document the classes of SHEET_PRESS_THROUGH_CLASSES, the classes the sheets set pointer-events on at any value but auto, the inert attribute and each pointer-events declaration of its style attribute, the name read by declaredProperty, so the hit test reads an author's element where a press would reach it, since the author's closing pass after the file review's round 16 completing its extra5-1, armFigureControls (the load and the error), watchFigureBoxes (one ResizeObserver per open over the figures of the Rendered box, armed at each text paint and not before the first, in place of a re-read from the width watch's repaint, which missed a text-size step, the PR's file review, round 2; a 0 by 0 report runs no decision, a rule over the report whatever produced it since the file review's round 4 (regression-3: before its round 3 the reason named the viewer's hide and a gated placeholder's img as the roads to such a report, reporting the real box at the show or the restore; narrowed in its round 3 and stated with its residual by the author's closing pass after the file review's round 3 (behaviour-4), the list still closed, and a loaded figure the author gave no box outside it), the residual a figure hidden after its load by any other road keeping a standing control until its next report with a box, which the product reaches by a `<details>` folded by the reader (an expanded callout renders as one), harmless there since the fold reports nothing and folds the control with the figure and the reopen's first report with a box decides the figure again (the file review's round 4, ui-1; the line before it said no road in the product reached it), and the arm before the first paint removed in the file review's round 3) and removeFigureControl with the keyboardTakers register (the keyboard handed to the viewer's body before a control's removal), the body's second click listener with openFigure and the figure's own door openFigureInViewer, openFigure's web arm reading webGestureShown since the fixes for the file review's round 16, extra5-1, the one gate every gesture meets: a web picture's tab opens only while its sign, the web control figureSign finds after figureAnchor or the mark on a picture that wears none, is in view by inViewPart and uncovered by signUncovered, a hit test at the centre and the four quarter points of the part in view of the element a press there would reach, read at the gesture's start by a press recorder at the window's capture phase (pointerdown, pointercancel and click, added per open and removed through the close hooks) and by the key gate at a press's first keydown, and otherwise the gesture opens nothing and revealSign scrolls the sign into view without moving the focus, a refused key closing the viewer's own text-size flyout as well, which no other key but Escape closes, so the next one opens, a sign partly in view counting as in view, a double click's second click, a held key's repeats and Space's release taking the refused verdict by their own detail and repeat fields and never by time, the body's mousedown listener cancelling a press of any mouse button on the web control, so no press focuses it, and keyOnHiddenWebControl with controlInView and clipToScrollports, Enter or Space opening the web control only while its box meets the viewport, the scrollport of every ancestor that clips on that axis and, walked up through the same-origin frames that host the viewer as the dashboard does, each parent's viewport and the scrollports of the frame's ancestors there, and last the visual viewport of the topmost window reached, so a control a pinch zoom leaves off the screen is out of view, a parent of another origin (VS Code's webview host) ending the walk with the reads made so far and any other failed read answering out, read at each key (since the fixes for the file review's round 14, ui-1 with extra9-1; the visual viewport and the frame walk since the file review's round 15, extra5-2; since the fixes for the file review's round 16 clipToScrollports passing over an ancestor on which overflow clips nothing, one whose display is contents, or inline, ruby, ruby-text or a table's row or column or one of their groups, to which overflow does not apply, regression-1, and every read in one coordinate space, cssScale scaling each ancestor's client metrics and the frame element's border by its zoom, a transform read by neither and stated as a limit, fresh-1), and figureControlAfter with figureLabelAfter's step past the control; since the author's closing pass after the file review's landing round mdBlock's chain block comment states the census's reach, the reached locals' imported callees' modules and their imports under every import form, and its judged sites with their reasons, in place of a no-write claim the widened census contradicted), ui/webview/icons.ts (ICON_BACK, ICON_FORWARD, ICON_EXPAND, and ICON_OUTBOUND since the fixes for the file review's round 11, ui-1 with extra8-1: a web picture's control wears the outbound glyph, names the host in its words and carries the class fv-figopen-web, and the picture's own title carries the address after an author's), ui/webview/styles.css and ui/webview/feed.css (the `.fileview-md .fv-figopen` rules, the same lines in both, the rest's margins logical since the fixes for the file review's round 15, ui-2, -28px at the line's start and 6px at its end, so the control stands over the picture's top-left corner in right-to-left text, and the float twins' physical, the dashed `.fv-figopen-web` rule among them since those fixes, and the two `.fileview-md img[data-fv-figweb]` rules under screen at-rules, the mark's outline on hover and at rest where hover is none or any pointer is coarse, since the fixes for the file review's round 12, fresh-1; both dresses in the outbound dress's own token, `--outbound-line`, declared in both theme blocks of both sheets and clearing 3 to 1 over the page in each theme, the control's rest colour a rule of its own, `.fv-figopen-web:not(:hover)`, and both at-rest at-rules keyed on hover none or any pointer coarse with screen on both queries, since the fixes for the file review's round 13, ui-1 with extra6-1 and extra7-2; and four more rules under screen at-rules since the painted-contrast ask of 2026-09-23, the web control resting at full opacity where the at-rest rule stands, a local one at 0.8, its focus ring standing 2px off its border, the button family's press cue off on it, since the release that opens the tab comes while it is held, and a dead link holding either dress dimming by colour and not by opacity, so every state a gesture opens the tab from, inside an href-less dead link too and the press held, paints the dress at 3:1 or better, the token's line at the token's own ratio (4.83:1 dark, 4.90:1 light), the mark's outline in each of its states and the web control's border at rest, revealed by the pointer over its picture, under any focus and under a Space press from it, and the family's accent border at 7.87:1 dark and 3.79:1 light against its hover wash, the web control's under the pointer and in a pointer press, as the file review's round 14, extra7-1, found this line had to split them; and since the fixes for the file review's round 14 both mark rules stand on a ring of var(--bg) 3px wide under the outline, with a margin rule at rest under screen, 3px up and down and 3px plus 0.3em across the line, each axis the least that keeps the ring off neighbouring ink at the picture's own text size, the sheets stating the bound for a picture in smaller text than an italic or bold italic f glued before it, correctness-2 with extra5-1 and extra5-2 and the two margin rulings of 2026-09-24, and below 0.7em of the f's the residual measured at the floor, where the margin falls to the ring's 3px, 30 pixels at a device scale of 1 and 102 device pixels at 2, sampled maxima, since the fixes for the file review's round 15, extra5-3, and with the f larger than the paragraph's text 40 pixels and 135 device pixels beside a picture 20 by 20, the count growing with the f without a bound beside a taller one, since a verifier of those fixes read a heading's f, and the reveal rule carries `.fileview-md .fv-figopen-web:focus`, so any focus on the web control is painted, ui-1 with extra9-1; and since the tap-highlight ruling of 2026-09-24 a rule under screen turns the browser's tap highlight off on the web control and on the mark, since in the pressed frames of a finger's tap a phone's default highlight took the line as low as 2.22:1 dark and 2.41:1 light in a screencast of every frame, 2.578:1 and 2.531:1 in the frames the leg samples; and since the fixes for the file review's round 16 the sheets' comment, the same lines in both, says every gesture on a picture from the web opens the tab only while its control or its mark is in view and uncovered, extra5-1, that an author's class on the figure or above it dims none of the dress, since the viewer takes those classes off, an author's own link opening its address undressed as before, extra5-2, and that an author's element laid over the figure is the one gate's to read, which refuses a gesture while an element a press would reach covers the sign, the viewer taking off it the classes these sheets set pointer-events on, the inert attribute and a pointer-events declaration of its style, extra5-1, and that with the picture in two nested small elements the ring covered at most 4 pixels at a device scale of 1 and 5 device pixels at 2 in the larger-f sweep and beside pictures up to 400 tall, extra8-4, no rule changed), ui/webview/real-viewer-leg.ts (inBrowser takes optional launch options handed to playwright's launch, so the open leg's hybrid twin runs Chromium under Blink's own device settings, every existing caller unchanged, in the file review's round 13, extra7-2; and openViewer an optional origin, ORIGIN unless named, so the error leg opens a page at https://notes-api.test, since the fixes for the file review's round 15, fresh-1), docs/guide.md (the two closing sentences of the Links in a file paragraph; since the author's closing pass after the file review's round 3 (behaviour-2, records-1, coverage-1) the trail sentence names the pictures opened from figures among what the trail keeps and states that the two arrow buttons appear once there is a file to step back or forward to, after a link is followed or a picture opened, and not before; its round 3 had conditioned them on a link followed, which is false on the picture road; and, since the fixes for the file review's round 14, the figure sentence's clauses on a web picture's button opening from Enter or Space only while it is on the screen, ui-1 with extra9-1, on the picture's tooltip showing the address without its sign-in part, query and fragment, correctness-1, and on a picture in a fold's title line, whose click opens or closes the fold, fresh-1, and the Figures paragraph's failed-figure sentence, where a web address shows without its sign-in part, query and fragment, extra5-3; and, since the fixes for the file review's round 15, those two clauses say that the picture's tooltip and a failed figure's label show the address's origin alone, extra9-2, and that an address with an @ that may be a sign-in is withheld, even for a harmless name such as a@2x.png, correctness-1, that the button shows under a focus reached from the keyboard, the web button under any focus, and a local button a Cmd-click or a dragged press left focused stays hidden until a key is pressed, ui-1, and that in right-to-left text the button stands at the picture's top-left corner, ui-2; since the author's closing pass after the file review's round 16 the failed-figure sentence quotes the label's withheld wording in bold where it had said a note; and since the fixes for the file review's round 16 the figure sentence says that a click, a tap, Enter or Space opens a picture from the web only while its button, or on a small picture its dashed border, is on the screen with nothing over it that would take a click, something the file itself lays over the picture named among the covers since the author's closing pass after the file review's round 16, and otherwise opens nothing and scrolls it into view, extra5-1, and that no key opens a picture under the floor, since the button is the keyboard's only way to a picture, and a picture a narrower pane shrinks below 48 pixels loses its button and that way, ui-2); tests: ui/webview/file-trail.test.ts, ui/webview/file-trail-browser.test.ts, ui/webview/file-figure-open.test.ts and ui/webview/file-figure-open-browser.test.ts (new; since the author's closing pass after the file review's round 8 the browser leg drives the reader's place on a top-level html-block figure wearing the production control, the Raw switch landing on the img's own row, the layout the node scene's fixture box stands in for; since the fixes for the file review's round 14 the browser leg reads the mark by its worst dash-to-ring ratio on the highlight, table header, even-row and note-callout grounds and holds that the ring covers no neighbouring ink at the picture's own text size and that the dash beside a neighbour reads 3:1 on both sides, runs of f glued to the picture among the neighbours and, in a case of its own on the Files pane, italic runs in a level-2 and a level-1 heading, a left float in a note callout and an italic run glued to a picture in two nested small elements, whose ink covered it holds to the bound the sheets state, at the default text size and at 150%, and in a line pin of its own at a device scale of 2 that the ring covers no paint of a code span or of keys on the line above and paints no pixel past the picture's margin box over a callout's, an even row's or a highlight's tint, correctness-2 with extra5-1 and extra5-2 and the two margin rulings of 2026-09-24, drives the web control's focus pins (a) to (f), pins (d) and (f) by Space, by Enter and by the numeric keypad's Enter, with a key-release pin, an other-button pin and four keep checks, ui-1 with extra9-1, and the fold cases with their two controls, fresh-1, and the node module holds the closed set with the margin rule, the fold's source pins and the Mouse census read by the compiler with its armed plants, controls and bound, tests-1 with extra6-3; since the fixes for the file review's round 15 the browser leg zooms the page through CDP and holds that no key opens a keyboard-focused web control off the zoomed screen, the viewer the top page and in a same-origin frame, with keep cells at a zoom that leaves the control on screen and a frame of another origin whose keys still open it, extra5-2, holds the body's scrollport and the table's, one pin per axis, extra6-1, reads the control unpressed after every Space, tests-2, the control over the picture's top-left corner in right-to-left text beside its float controls, ui-2, a picture in a details element's later summary, tests-4, and the floor's scene of the smaller-text residual at 100%, 150% and 200%, extra5-3, its titles the origin alone, extra9-2, and the node module holds the words line, the web return line and the sign-in rule's constants, correctness-1 with tests-1, the rest's logical margins and the left float's physical ones, ui-2, and the census's six indirect forms, extra6-3; and since the tap-highlight ruling of 2026-09-24 the browser leg reads each frame of a finger's tap on the web control and on the mark at 3:1 or better, on a phone, under CDP touch emulation and on the touchscreen laptop, and the node module holds the tap rule in both closed sets; and since the author's closing pass over those fixes the browser leg holds an italic run in a level-2 heading at the floor within 1 and 40 pixels and, in a case of its own at a device scale of 2, a bold italic run in a big element at the floor within 1 and 135 device pixels, counted by a reader of device pixels, the later summary's title equal to the origin's line, and its tap pin reads a frame about every 33 ms, and the node module's words-line pin states the rule's cost; and since the fixes for the file review's round 16 the browser leg holds the one gate on the chat modal and the Files pane, every pointer device and the keys on a sign out of view, a pinch zoom, one gesture's later events with the held Space's control unpressed, the Outline popover, the text-size menu and an author's overlay over the sign, the key's refusal closing that menu since the author's closing pass after that round, and an author's element a press would pass through by a page class, a style declaration or the inert attribute since that pass too, with keep, sliver, middle-click, local-picture, row, mark and anchor cells and a framed chat modal under a pinch zoom as a stated limit's witness, extra5-1, the predicate in the dashboard's pane wrapper, its rules lifted from kernel.py at run time, at 800 px and at 1024 px with touch, inside an author's inline or contents span, ruby, ruby text or table row around a cell that spans the row below, and under body zooms of 1.25, 0.8 and 14/13 with the floor's cells in CSS pixels and under the browser's own zoom of 90%, a forced device scale, regression-1 and fresh-1, and the dimming classes taken off an author's span and img with the dress's painted ratio, extra5-2, and the node module holds the web arm's gate, extra5-1, SHEET_DIM_CLASSES to the sheets both ways, extra5-2, and since the author's closing pass after that round SHEET_PRESS_THROUGH_CLASSES to them both ways, extra5-1), ui/webview/file-view-figure-shapes.test.ts and ui/webview/file-view-figure-shapes-browser.test.ts (new in the review's round 1: figureTarget reads the web address before the model's join, no control stands inside a link that holds more than the figure, the bare figure's click yields to an enclosing link, and the 48px floor, pinned at source and driven in Chromium; since the file review's round 3 the source pins derive the refused states and hold their literals absent from every reader, since its round 4 read as the compiler reads them through ui/webview/source-units.ts, the reader the attribution pin shares, with the pin's boundary (whole literal values and regular expressions; not an identifier key, an assembled word, a prefix or a case-folded test) stated in its message and pinned by execution since the author's closing pass after the file review's round 4 (guards-1 with records-11), and hold the arm before the first paint absent; the web target's pin carries the source the sign-in rule reads since the fixes for the file review's round 15, correctness-1, and figureBox's pin its CSS pixels, with no offsetWidth or offsetHeight read, since the fixes for the file review's round 16, fresh-1), ui/webview/file-view-figure-chosen.test.ts and ui/webview/file-view-figure-chosen-browser.test.ts (new in the review's round 2: figureTarget reads chosenSource, pinned at source and driven in Chromium over a picture element, a srcset img and a gated remote picture; the browser leg observes the remote figures' requests at a context-level route with the real window.open since the PR's file review, and since its round 2 from before the open, with a never-drained list of foreign window.fetch calls, the Cookie header of each request read under three seated classes, the loaded picture's Ctrl-click and the failed remote figure's clicks; since the fixes for the file review's round 15 its source pins carry the web target's source, correctness-1, and failedSource's read of the chat page's heal record, fresh-1, which leaves a record equal to the page's own base unread since the fixes for the file review's round 16, correctness-1), ui/webview/file-view-figure-floor-browser.test.ts, ui/webview/file-view-figure-state-browser.test.ts and ui/webview/file-view-figure-recent-browser.test.ts (new in the PR's file review, 2026-09-20: the floor re-read at each reflow of the figure, the body's width and a text-size step alike, the keyboard handed on at a control's removal, the hidden viewer's 0 by 0 report adding no control while hidden, and, its round 3, a loaded `<img hidden>` and `<img width="0">` beside prose getting no control with the click on the words before them opening nothing, the click point derived from the words themselves since the author's closing pass after the file review's round 3 (behaviour-3), and a picture re-fetched while the viewer is hidden losing its control at that load and regaining it at the show; the control decided from the figure's current state, a failed figure with a box opening nothing; and no Recent row for a picture opened from a figure while a Forward step onto it mints one, each driven in Chromium; the floor leg's titles the origin alone since the fixes for the file review's round 15, extra9-2), and re-aimed ui/webview/file-view.test.ts, ui/webview/file-view-links.test.ts (its listener pin follows the mousedown listener's cancel of a press on the web control since the fixes for the file review's round 14, ui-1 with extra9-1), ui/webview/file-view-text-size.test.ts (its bar case reads the group hidden on a fresh open), ui/webview/fileview-parity.test.ts (and, in the file review's round 10, correctness-5, its Outline button census on parsed rules over every sheet a page of either host loads, and its heads the mark's margin rule and the reveal's `:focus` member since the fixes for the file review's round 14, and the tap rule's since the tap-highlight ruling of 2026-09-24), ui/webview/file-view-outline.test.ts (the same census, and since the fixes for the file review's round 14, correctness-1, the title cases beside its credential case, a query token, a signature pair, a fragment token, a sign-in part with a query and addresses the parser refuses, each title the origin alone since the fixes for the file review's round 15, extra9-2, beside a case of a path parameter and an opaque token segment in the path; and since that round's correctness-1, one table of the sign-in spellings, painted over the stand-in and failed through the real error listener on an http, an https and a VS Code webview base and on a URL document, each reading the picture's title, the control's title and aria-label and the label, with the rule's cost rows and its controls, in place of the witness of two spellings whose disclosed residual the coordinator withdrew, beside direct calls of the rule on file:, DATA:, S3: and BLOB: sources, and since the author's closing pass over those fixes the table also holds two leading backslashes before a token with no colon, percent-encoded colons before a sign-in and a data: head past 40 characters to its comma, its controls a short inline svg and a refused address cut at a backslash, and the credential case states the rule's cost; and since that round's tests-3, the key gate's node unit, which CI runs, each key dispatched on the focused web control through the body's registrations, a cell for each read of the frame walk, extra5-2, and the twins of a parent of another origin, whose keys are not cancelled; and since the fixes for the file review's round 16 two guards of the one gate, which CI runs, a click on the figure and on its control out of view, covered or shown, and one gesture's later events, with elementFromPoint, a cover and the control's previous sibling in the key gate's scene, extra5-1, three guards of the predicate, an ancestor of the frame element computing display contents or inline and the control's own ancestor computing each display overflow does not apply to, a table cell still clipping, and the region under a body zoom through both of cssScale's roads, regression-1 and fresh-1, two sign-in rows with a malformed byte before an encoded at sign, tests-1, a frame whose wrapper's computed-style read throws in the region case, tests-2, case 20's fails collected over both texts, extra6-1, the bounded decode's answers and pass counts, regression-2, the dimming classes over the render path, extra5-2, and since the author's closing pass after that round what lets a press pass through an author element over the render path, extra5-1), ui/webview/theme-parity.test.ts (the outbound line's pair, --outbound-line over --bg at 3 to 1 in both themes of both sheets, and feed's evaluated count, in the file review's round 13, ui-1 with extra6-1; and the painted test beside it since the painted-contrast ask of 2026-09-23, every state a gesture opens the tab from composed from the declared opacities over the worst picture and on the VS Code editor grounds, whose executed twin is the open leg's paintedRatio; since the fixes for the file review's round 14 the mark's ring read off both sheets, correctness-2, and the reveal's `:focus` member among the painted states, ui-1), ui/webview/file-view-notice.test.ts (its Escape cases run every keydown handler an open registers in the document's order, the capture phase first, and its shim removes a listener only under the capture flag its add carried) and ui/webview/md-url-view.test.ts (the local-file mode's sibling link opens through openFromViewer with a push, and the door's shape: the tag set, the host's opener called, the tag cleared in a finally) and ui/webview/pdf-new-tab.test.ts (the sweep repair: its count of the gesture reads inside openFileView counts the links' two and the figure's four apart and the rest of the function as none, so openFileView's own open still reads none), and ui/test-code-only.ts (the comment stripper the source pins read through: its header's reader roster names ui/webview/file-figure-open.test.ts as its sixth reader since the file review's round 13, extra7-1 with extra6-2, whose count of figureLinkOf's calls and census of the arguments of the Playwright Mouse calls its compiler walk reads read the code alone, the walk a callee that is a member of a member named mouse, through parentheses, a non-null assertion and the type forms, each member a property access or a string-literal element access, a computed key, an alias and a destructured binding outside it, each witnessed by a plant, since the fixes for the file review's round 14, tests-1 with extra6-3, a Mouse method reached through an alias or a destructured binding outside it too, each witnessed by a plant, since a verifier of the fixes for the file review's round 15 planted them, and six forms of indirect invocation outside it too, call, apply, bind, a comma expression, Reflect.apply and a function parameter named mouse passed page.mouse, each witnessed by a plant, a limit stated as read since the fixes for the file review's round 15, extra6-3); the trail half (file-trail.ts, the bar's group, the chords, the two arrows) reads nothing upstream lacks, and the figure half reads fork-only modules (figurePath from file-comments-model.ts, panelMark from file-comments.ts, GATE_ACT from figure-gate.ts, the regions layer's wrap) and its browser legs boot the fork's real-viewer-leg.ts, files.ts page and md-config.ts wikilinks, so an offer adapts them rather than copies; FORK-ONLY: ui/webview/anchor-map.ts and ui/webview/reader-place.ts (CONTROL_CLASSES gains fv-figopen; anchor-map's isFigureCompanion; since the file review's round 8, correctness-1, reader-place's structural read leaves a figure's two companions out of the element lists it walks by the same pair predicate, blockElementsOf with isFigureCompanion, so a top-level figure wearing the control reads as the figure and not as the paragraph after it) with their suites ui/webview/anchor-map-fallback-markup.test.ts, ui/webview/file-view-figure-error.test.ts and ui/webview/file-view-figure-error-browser.test.ts (re-aimed: the label readers step past the control; since the fixes for the file review's round 14, extra5-3, the error suite's node unit, which CI runs, calls shownSource directly and through the error listener, and the error leg's Chromium label cases relay the synthetic hosts from a second local server, no label printing a sign-in part, a query or a fragment; since the fixes for the file review's round 15, extra9-2, those labels show the origin alone, and every row carrying a sign-in part shows the withheld address, and since that round's fresh-1 the node unit reads the label of an img the chat page's heal parked from the heal's record, and the error leg holds the chat modal with the page's heal on an http page and on an https page that real-viewer-leg.ts's openViewer opens at a second origin, and since the author's closing pass over those fixes the URL viewer label's message says a relative figure names the document's origin and an absolute one its own; and since the fixes for the file review's round 16, correctness-1, the node unit reads an empty destination in the heal's shape and its fragment twin, and the error leg's heal case adds four empty destinations and asserts both pages' labels at once) and tests/test_guide_files_failures.py (re-aimed: first to hold both walks' control lists' seventh entry, fv-figopen, then, the file review's round 8, correctness-1, to name the executed cases of the control's exclusion in that pin's place, a pin on a list's membership having stayed green while the structural read ignored both lists; its failed-figure sentence gains the web-address clause since the fixes for the file review's round 14, extra5-3, and says a web address shows its origin alone and an address that appears to carry a sign-in is withheld since the fixes for the file review's round 15, extra9-2 with correctness-1, its shared names holding FIGURE_ADDRESS_WITHHELD, and FIGURE_SAYS moves with the sentence, which quotes the withheld wording, since the author's closing pass after the file review's round 16), ui/webview/file-view-place-blocks.test.ts (re-aimed in the file review's round 8, correctness-1: readPlace over a top-level figure wearing the control, at the root's level and nested in a wrapper, and over a right-floated figure wearing the failed figure's label at its top, reads the figure; red before the fix at the paragraph after it), ui/webview/file-view-seam.test.ts (re-aimed at the merge of the fork's main and in the file review's landing round: its re-parse census's callee list gained addFigureControls, this follow-on's pass, with the local functions it reaches, and the walk follows a reached local's imported callees and every import form transitively, judging the sites it finds per module, with the whole file's count in its one home; since the author's closing pass after the file review's landing round its RE_PARSE matches a template argument under any spelling, the form its derivation command and the plan's paragraph spell, the same lines at that head, and its test's name counts the third judged site on the road, the glyph's holder after the adoption; since the author's closing pass after the file review's round 8 its method-call guard reads the compiler's tree under any access spelling, its binding reader follows a require through a cast, parentheses, a non-null mark or a member, and its three compiler readers refuse a module the parser reports a diagnostic on; REACHED_LOCALS names figureFoldOf since the fixes for the file review's round 14, fresh-1, and figureSourceCredentialed, decodeEscapes, decodeEscapesOnce and authorityCut since the fixes for the file review's round 15, correctness-1 with extra9-2, with decodeURIComponent and parseInt among the language's globals it judges; the calls on the sanitized body name dropDimmingClasses first since the fixes for the file review's round 16, extra5-2, and dropPressThrough after it since the author's closing pass after that round, extra5-1), ui/webview/file-view-decisions.test.ts (re-aimed since the fixes for the file review's round 16, extra5-2: its pin on the word the viewer no longer uses for the editor's decisions lets the one quoted page class inside SHEET_DIM_CLASSES's literal through by value, since that list holds another page's tree-node class as the sheets spell it), ui/webview/anchor-map.test.ts (re-aimed in the file review's landing round: the control at the box's top level is no block's node, executed beside the failed figure's label's case; and, extra9-1, the caption beside a control carrying a text node of its own maps, the executed case of anchor-map.ts's entry) and ui/webview/md-config-figure-gate-place.test.ts (re-aimed there too: readPlace over a loaded figure wearing the control, and, extra9-1, over one whose control carries a text node of its own, the executed case of reader-place.ts's entry), ui/webview/compact-seam-exec.test.ts and ui/webview/spacer-measure.test.ts (two of main's censuses, which the merge of the fork's main at fa3ef54b5 brought, re-aimed after it: the closed multiset of class mutators whose class is no constant of the source names decideFigureControl's classList.add, the control's float side read off the figure's align, and dressFigureControl's classList.toggle, the web class declared by a concatenation the census does not fold; the roster of the modules under ui/webview no page bundle loads names ui/webview/css-rules.mjs, ui/webview/host-sheets.mjs and ui/webview/source-units.ts, the readers this follow-on's tests import; and the closed multiset of the writes of a view property names the trail's two, trailSetView's and moveTrail's, kept in the page's trail state and never posted; the class mutators name dropDimmingClasses's classList.remove since the fixes for the file review's round 16, extra5-2, and dropPressThrough's since the author's closing pass after that round, extra5-1), and the fork's records with their pin: plans/markdown-viewer.md (the section Follow-on: Link navigation, L1 to L6, and Slice 7's failed-figure label formula with its stripping rule since the fixes for the file review's round 14, extra5-3, and with the sign-in rule since the fixes for the file review's round 15, correctness-1; the convention's enumeration and roster carry round 15 and the ids its fixlist filed; and the URL viewer's label shown as its origin alone since the author's closing pass over those fixes; and since the fixes for the file review's round 16 the convention's enumeration and roster carry round 16 and the ids its fixlist filed, L3's gesture and in-view clauses, L6 and open points 7 and 11 state the one gate with its cost, L3's clause on the hit test what a press would reach, and since the author's closing pass after that round the drop of what lets a press pass through an author's element and the key's refusal closing the text-size flyout, extra5-1, L3's in-view clause the display skip over every display overflow does not apply to, the one coordinate space and the transform's limit, and its floor sentence the CSS pixels with the layout's grid under the browser's own zoom, regression-1 and fresh-1, L3's failedSource clause the empty destination, correctness-1, its sign-in clause a pointer to the rule's one home, extra7-2, its floor paragraph the keyboard's limit, ui-2, its author-class clause and its record of the class-mutator census the drop, extra5-2, the residual the two-nested-small figure, extra8-4, open point 7 the sheets' own condition, ui-1, and L6 the delta's run at the head that carries those fixes), plans/file-browser.md (the pointer sentence in the navigation-stack section, the glyphs' condition added in the file review's round 3), tools/markdown-viewer-plan-linknav.test.mjs (new; since the fixes for the file review's round 14 its reveal constant carries the web control's `:focus`, ui-1, its helperStyle pins refuse any str.<method>( before the run and a constant-held or split opening tag, with pins recording the over-refusal and the bound, extra6-1 and extra6-2, and its copy of the guide's figure sentence moves with the guide; since the fixes for the file review's round 15 its REST and LEFT constants carry the rest's logical margins and the left float's physical ones, ui-2, and its words-line and web-return pins the sign-in rule, correctness-1; and since the tap-highlight ruling of 2026-09-24 its closed set carries the tap rule, TAP_OFF; and since the fixes for the file review's round 16 its web-return pin carries the one gate, extra5-1, its copy of the guide's figure sentence moves with the guide, extra5-1 and ui-2, again since the author's closing pass after that round, extra5-1, and its retired-sentence pins refuse open point 7's old phrase, ui-1), ui/webview/css-rules.mjs with ui/webview/css-rules.d.mts (new in the file review's round 9, correctness-1 with tests-1 and ui-1: the sheet's rules read as rules, each with the at-rules enclosing it, the one reader the closed set over the control's sheet rules stands on in ui/webview/file-figure-open.test.ts and in tools/markdown-viewer-plan-linknav.test.mjs, where the set had been keyed on lines at column zero; under tools/ at that round's fixes, and under ui/, the tree the bundles read, since the author's closing pass after the file review's round 10, because a ui test may not import from tools/, hooks/ or tests/, trees outside the kernel's bundle-staleness inputs, which tests/test_lab_dist.py's derivation pin holds), ui/webview/host-sheets.mjs with ui/webview/host-sheets.d.mts (new in the file review's round 10, correctness-1 with regression-5 and correctness-5: every sheet a page of either host loads, derived from the page assembly, the kernel's page functions and the extension's webview builders, the population the closed set and the Outline census read through the reader; under ui/ for the reader's reason; since the fixes for the file review's round 11, extra6-1 with extra7-1, kernel-1 and tests-1, the style blocks the helpers a page calls write into its HTML at serve time are followed one level, and the page read fails by name on a def in a shape it has no rule for and reads a body through a triple-quoted literal, the file review's round 11, correctness-1 with regression-1 and extra7-2; since the fixes for the file review's round 14 helperStyle refuses any str.<method>( standing before the run, extra6-1, and detects a helper by either tag, so a closing tag with no opening literal it finds fails by name, extra6-2), tools/markdown-viewer-plan-gate-adopt.test.mjs (main's pin of the gate-before-adoption section, re-aimed at the merge: its held copy of that section's re-parse paragraph, re-derived in the file review's landing round with the derived figures filled from the seam test's pins, and its held copy of the chain block's sentence and of that paragraph following the author's closing pass after the file review's landing round; since the author's closing pass after the file review's round 8 its no-typed-addend guard over that paragraph is keyed on the property, a number word within forty characters of a module token in either order), tools/markdown-viewer-plan-linknav-review.test.mjs (new in the review's round 2: the record's corrections held to the source; its condition pin follows L6's and open point 11's words since the fixes for the file review's round 14, extra8-1, and its figureTarget and failedSource pins the web target's source and the heal record's read since the fixes for the file review's round 15, correctness-1 and fresh-1, and since the fixes for the file review's round 16 its web-arm pin and its L6 sentence pin the one gate, extra5-1, and its failedSource pin the record left unread at the page's own base, correctness-1), ui/webview/linknav-records-attribution.test.ts with ui/webview/source-units.ts (new in the file review's round 4, rules-1 with tests-2: every round a record of the follow-on names belongs to the review named nearest before it and is one the plan's convention enumerates for that review, a pass of the author's has no rounds and an id of its family stands only where the review named nearest before it is the author's pass, and since the fixes for the file review's round 11, fresh-1, a finding cited under a round of the file review is one that round's fixlist filed, held to the roster the plan's convention carries beside its enumeration, read in every checkout as a rule over the tree, every file git lists at the repo root, tracked or untracked and not ignored, whose text names the file review or carries an id of the author's family, the files the branch created (an existence roster, judged in full) and the plan's section, the guide's paragraph and the browser plan's pointer, a phrase outside those judged only where the review named nearest before it is the file review or the author's pass (the file review's round 5, extra5-1 with tests-1, regression-2, correctness-5 with tests-6 and correctness-6 with tests-5 and extra8-4: the first road had read a roster of eighteen files, checked against the diff only by a second road that runs in no CI job; a new uncommitted file is in the population, which no diff lists; file-view.ts's count of this follow-on's units is held equal to the derived count; the reader refuses a suffix it has no rule for naming the file, correctness-7 with tests-4 and extra7-2), and, kept and disclosed as running in no CI job and in none after the merge, from every unit the branch added or touched since the merge-base behind a two-part gate, the base not origin/main and the diff adding the module, the gate a pure function pinned in all four cells and run against a temp repo shaped as the open PR branch, the same round's tests-7 (the file review's round 4, extra8-1, so main, every later branch after the landing and a batch head cut from main's tip stand the road down; a batch head main has moved under runs it over the batch's whole delta, disclosed and not closed by the author's closing pass after the file review's round 4, attribution-and-gates-2 with records-2, which also had the road diff the working tree against the merge-base, guards-3, read the plural and range forms of a round and state the forms outside the reader, guards-2 with attribution-and-gates-3, charge a fault in the plan's section to its plan line, attribution-and-gates-4, and read file-view.ts's units carrying an id of the family in every checkout, records-1), TS and JS read through the compiler's parser and Markdown and Python as paragraphs; the rule stood in the review module since the file review's round 3, keyed on one string and one apostrophe, and the author's closing pass after the file review's round 3, behaviour-6 with records-2 and coverage-2, gave it its two roads; since the author's closing pass after the file review's round 8 its exported count of checks keyed on the delta is held to its own call sites, read with the comments blanked; since the fixes for the file review's round 14 the plan's convention enumerates that round with the ids its fixlist filed, and the count of file-view.ts's units counts the units those fixes add; since the fixes for the file review's round 15 the count counts the units those fixes add too, figureSourceCredentialed's docstring, correctness-1, and failedSource's, fresh-1, and since the fixes for the file review's round 16 the seven more those fixes add, the gate's block comment and the docstrings of controlInView, figureSign and revealSign, extra5-1, FIGOPEN_MIN_PX's, fresh-1, DECODE_PASSES's, regression-2, and SHEET_DIM_CLASSES's, extra5-2), tests/test_guide_trail_chords_and_figure_button.py (new in the review's round 2: the guide's two sentences held to the shell script's lines in kernel.py and to the viewer's; re-aimed in the PR's file review with a census of figureWantsControl's refusal arms, the failed picture among the four kinds without the button, in its round 3 with the refused states' literals held absent by the property, that copy removed in its round 4, regression-2 with extra6-1, for the one pin in ui/webview/file-view-figure-shapes.test.ts, and since its round 4, extra8-3, asserting the hide line, the guide's condition clause and the browser plan's clause together in one test; its copy of the guide's figure sentence moves with the guide since the fixes for the file review's round 14, and again with the fixes for the file review's round 15, correctness-1 with extra9-2, ui-1 and ui-2, and with the fixes for the file review's round 16, extra5-1 and ui-2, and with the author's closing pass after it, extra5-1) and this entry; 54 files in all by `git diff --name-only $(git merge-base origin/main HEAD) HEAD`, run 2026-09-25 at the head that carries the fixes ruled in the file review's round 16 and the author's closing pass over them, where the merge-base is fa3ef54b5, the fork's main the branch merged after the painted-contrast ask of 2026-09-23, its tip the merge of the fork's batch PR 907, every one of them named in this line (the run at the head that carries the fixes ruled in the file review's round 16, from the merge-base fa3ef54b5, listed 53, before ui/webview/file-view-decisions.test.ts, a module of main whose pin that pass re-aimed, joined the delta; the run at the head that carries the fixes ruled in the file review's round 15, from the merge-base fa3ef54b5, listed the same 53; the run at the head that carries the fixes ruled in the file review's round 14, from the merge-base fa3ef54b5, listed the same 53; the run at the head that carries the entries two of main's censuses owe this follow-on's modules, after the merge of the fork's main, from the merge-base fa3ef54b5, listed the same 53; the run at the head that carries that ask and the merge of the fork's main after it, from the merge-base fa3ef54b5, listed 51, before ui/webview/compact-seam-exec.test.ts and ui/webview/spacer-measure.test.ts joined the delta in those entries; the run at the head that carries the fixes ruled in the file review's round 13 and the merge of the fork's main after them, from the merge-base 6cf6839ba, the fork's main the branch merged after those fixes, its tip the merge of the fork's batch PR 906, listed the same 51; the run at the head that carries the dress fixes alone among them, from the merge-base de06b8718, the fork's main the branch merged before the file review's round 13, listed 50, before ui/test-code-only.ts joined the delta in the case fixes; the run at the head that carries the fixes ruled in the file review's round 12, from the merge-base de06b8718, the fork's main the branch merged before the file review's round 13, listed 48, before ui/webview/theme-parity.test.ts and ui/webview/real-viewer-leg.ts joined the delta in those dress fixes; the run at the head that carries the fixes ruled in the file review's round 11, from the merge-base 4ae3b6659, the fork's main the branch merged for the file review's round 12, listed the same 48; the run at the head that carries the fixes ruled in the file review's round 10, from the merge-base 53f9f4aa3, the fork's main the branch merged for the file review's round 11, listed the same 48; the run at the head that carries the author's closing pass after the file review's round 10, from the merge-base 65f1895f6, the fork's main the branch merged for the file review's round 10, listed 45, before ui/webview/host-sheets.mjs, ui/webview/host-sheets.d.mts and ui/webview/file-view-outline.test.ts joined the delta in those fixes; the run at the head that carries the fixes for the file review's round 8, where the merge-base was 5e5ee79ed, the fork's main merged into the branch before landing, listed 43, before ui/webview/css-rules.mjs and ui/webview/css-rules.d.mts (under tools/ until the author's closing pass after the file review's round 10) joined the delta in the fixes for the file review's round 9; the run at the head that carries the landing round's first fixes, where the merge-base was 01434a45b, the fork's main the branch had merged for the file review's round 5, listed 42, before ui/webview/file-view-place-blocks.test.ts joined the delta in the fixes for the file review's round 8; the run at the head after that merge, for the file review's round 5, listed 40, before the landing round's executed test re-aimed two more standing suites, and this line then named 39 of them, file-view-seam.test.ts unnamed; the two earlier runs, at the commit that built the one reader for the file review's round 4 and at the head that closed the author's closing pass after the file review's round 4, listed 38 from the older merge-base they shared, before the merge made main's tools/markdown-viewer-plan-gate-adopt.test.mjs a file the branch edits; the branch point became an ancestor of main when main was merged in, so a diff from it no longer names the branch's files), this entry among them
added: 2026-09-19
pr: 862
tier: feature
offered:
closed:
---
A link followed inside a shown file replaced the card with no way back but the Files pane's Recent list. The viewer now keeps a trail of the files reached through its own links (a plain state with pure functions, file-trail.ts; the viewer's own opens take one door that tags the open, so an open from outside is one by construction) and shows Back and Forward as two glyph buttons at the left of its bar, with Cmd+[ and Cmd+] on a Mac, and Alt+Left and Alt+Right on a Files or chat page open in a browser tab of its own, as chords while the file is not being edited and no text box holds the keyboard (in the dashboard the shell takes Alt+Left and Alt+Right to move the keyboard between the panes; the file review's round 14, fresh-2). Back re-opens the previous file with no target, so the remembered place re-seats it, in the view it was left in, without touching the saved preference; an open from outside the viewer starts the trail over, and closing the viewer ends it, as does, under the chat's document-level opener, a plain click on a same-origin absolute .md or .markdown link inside the shown file (the chat's link rule, md-links.ts's isMarkdownUrl, case-insensitive), which replaces the card with the URL view (openUrlView) and ends the trail the same way (the file review's round 9, ui-2: this entry and the guide had named the close alone, the plan's L1 both; on the Files page, whose document has no such opener, the link opens a tab and the trail stands, driven in Chromium by file-trail-browser.test.ts; the replace is held in the tree by file-trail.test.ts's wiring pin). A picture a rendered file embeds from a file or a web address wears an Open the picture glyph button after it (a sibling, never a wrapper; decided from the figure's current state at the paint, the load, the error and each reflow of the figure's own box, the body's width and a text-size step alike: none while the picture is fetching or after a failed load, on a data: picture, on one under 48 px on either side, or inside a link a click can follow that holds more than the picture; a dead link, or an anchor that only marks a place, is not one, so a picture inside it keeps its control and its title, the file review's round 12, correctness-1 with ui-1), revealed by the pointer or a keyboard focus, the web control by any focus, and shown at rest where hover is none or any pointer is coarse, that opens the picture in the viewer through the same push, so Back returns to the file at the figure's place; the picture opened is the candidate the browser chose (a picture element's source, a srcset candidate), never the fallback src; a plain click on the picture does the same while the Comments panel is closed, a Cmd/Ctrl-click on the picture opens the /file URL in a tab while the panel is closed or the pointer is coarse (with the panel open on a fine pointer the layer's overlay takes the press, modified or not, and offers a comment; the control opens at any time), and neither click opens a picture inside a fold's own summary, whose click toggles the fold, while its control stays and opens it (the file review's round 14, fresh-1); a loaded remote picture opens its own address in a top-level tab (each gesture only while the picture's sign, its control or on a picture that wears none its mark, is in view and uncovered at the gesture's start, read at the press or at the first keydown, and otherwise it opens nothing and scrolls the sign into view, so the next one opens, the later events of one gesture, a double click's second click, a held key's repeats and Space's release, taking its verdict by their own fields and never by time, and a sign partly in view counting as in view, the file review's round 16, extra5-1, where a tap or a click on the picture had opened the tab with its control off the screen; the control by a click or by Enter or Space, the plain click on the picture with the Comments panel closed, and the Cmd/Ctrl-click where the press reaches it, the panel closed or the pointer coarse, neither click on a picture inside a fold's own summary, whose click toggles the fold; no mouse press leaves the keyboard on the web control, the file review's round 14, extra8-1 with ui-1 and extra9-1; a second, differently kinded, credentialed request: a document request to a host the page had requested the image from, carrying cookie classes the image request did not, the Lax class in Chromium's measurement, where before only an author's link opened one: the PR is a privacy surface and lands on the owner's word; the gate is not bypassed), a fetching or a failed figure opens nothing on any gesture, and a gated placeholder gets its control once loaded; the outbound case is shown before the click: a web picture's control names the host in its words and wears the outbound glyph and a dashed border, the picture's own title carries the address's origin alone (the file review's round 15, extra9-2), after an author's, and a source that appears to carry a sign-in, an @ anywhere after its scheme, shows a phrase saying its address is withheld in the title, the control's words and the failed label (the same round's correctness-1, whose stated cost is that a harmless address with an @ in its path is withheld too), and a web picture that wears no control, one under the floor, wears the dashed outline itself, on hover on a fine pointer and at rest where hover is none or any pointer is coarse, both dresses in a token of the outbound dress's own that clears 3 to 1 over the page in both themes (the file review's round 11, ui-1 with extra8-1, its round 12, fresh-1, and its round 13, ui-1 with extra6-1 and extra7-2); the picture opened from a figure takes no Recent row, and a Forward step onto it does. The Back and Forward group is hidden when neither direction has a target (T367). Browser history is not integrated (recorded as a follow-on with the trade-off). No kernel change, no new route. The trail half reads nothing upstream lacks; the figure half reads the fork's comments model, gate and panel, so an offer adapts it (the where line). Records in plans/markdown-viewer.md, Follow-on: Link navigation.
