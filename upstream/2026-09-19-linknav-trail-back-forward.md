---
title: The file viewer keeps a trail of the files reached through its links, with Back and Forward, and an Open the picture control on every figure
status: candidate
where: fork branch `filereview-linknav`, cut from the fork's main 34142c262: ui/webview/file-trail.ts (new: the trail's state and pure functions, trailRoot, trailPush, trailBack, trailForward, trailSetView, trailEnd, trailBackTarget, trailForwardTarget, fileNameOf, navTitle and navChord, and the live instance, liveTrail and setTrail), ui/webview/file-view.ts (openFromViewer and the trailNext tag; moveTrail after runLeave's write in the replace path; the bar's fileview-nav group with its two glyph buttons; onNavKey, one capture-phase document listener removed through the close hooks; closeFileView and openUrlView end the trail and the conflict Reload tags itself reload; the body delegate's path-link open through openFromViewer; the figure half: FIGURE_OPEN_TITLE, figureTarget, figureControlOf, figureOfControl, bareFigureOf, linkAbove, figureBox and the FIGOPEN_MIN_PX floor with dropFigureControl, ensureFigureControl, addFigureControls in mdBlock's file arm, armFigureControls, the body's second click listener with openFigure, and figureControlAfter with figureLabelAfter's step past the control), ui/webview/icons.ts (ICON_BACK, ICON_FORWARD, ICON_EXPAND), ui/webview/styles.css and ui/webview/feed.css (the `.fileview-md .fv-figopen` rules, the same lines in both), docs/guide.md (the two closing sentences of the Links in a file paragraph); tests: ui/webview/file-trail.test.ts, ui/webview/file-trail-browser.test.ts, ui/webview/file-figure-open.test.ts and ui/webview/file-figure-open-browser.test.ts (new), ui/webview/file-view-figure-shapes.test.ts and ui/webview/file-view-figure-shapes-browser.test.ts (new in the review's round 1: figureTarget reads the web address before the model's join, no control stands inside a link that holds more than the figure, the bare figure's click yields to an enclosing link, and the 48px floor, pinned at source and driven in Chromium), and re-aimed ui/webview/file-view.test.ts, ui/webview/file-view-links.test.ts, ui/webview/file-view-text-size.test.ts, ui/webview/fileview-parity.test.ts, ui/webview/file-view-notice.test.ts (its Escape cases run every keydown handler an open registers in the document's order, the capture phase first, and its shim removes a listener only under the capture flag its add carried) and ui/webview/md-url-view.test.ts (the local-file mode's sibling link opens through openFromViewer with a push, and the door's shape: the tag set, the host's opener called, the tag cleared in a finally); the trail half (file-trail.ts, the bar's group, the chords, the two arrows) reads nothing upstream lacks, and the figure half reads fork-only modules (figurePath from file-comments-model.ts, panelMark from file-comments.ts, GATE_ACT from figure-gate.ts, the regions layer's wrap) and its browser legs boot the fork's real-viewer-leg.ts, files.ts page and md-config.ts wikilinks, so an offer adapts them rather than copies; FORK-ONLY: ui/webview/anchor-map.ts and ui/webview/reader-place.ts (CONTROL_CLASSES gains fv-figopen; anchor-map's isFigureCompanion) with their suites ui/webview/anchor-map-fallback-markup.test.ts, ui/webview/file-view-figure-error.test.ts and ui/webview/file-view-figure-error-browser.test.ts (re-aimed: the label readers step past the control) and tests/test_guide_files_failures.py (re-aimed: both walks' control lists carry fv-figopen as the seventh entry), and the fork's records with their pin: plans/markdown-viewer.md (the section Follow-on: Link navigation, L1 to L6), plans/file-browser.md (the pointer sentence in the navigation-stack section), tools/markdown-viewer-plan-linknav.test.mjs (new) and this entry; 28 files in all by `git diff --name-only 34142c262 HEAD` run at the review's round 2 fix commit, the last commit touching this entry, this entry among the 28
added: 2026-09-19
pr:
tier: feature
offered:
closed:
---
A link followed inside a shown file replaced the card with no way back but the Files pane's Recent list. The viewer now keeps a trail of the files reached through its own links (a plain state with pure functions, file-trail.ts; the viewer's own opens take one door that tags the open, so an open from outside is one by construction) and shows Back and Forward as two glyph buttons at the left of its bar, with Alt+Left and Alt+Right (Cmd+[ and Cmd+] on a Mac) as chords while no text field holds the keyboard. Back re-opens the previous file with no target, so the remembered place re-seats it, in the view it was left in, without touching the saved preference; an open from outside the viewer starts the trail over, and closing the viewer ends it. Every picture a rendered file embeds wears an Open the picture glyph button after it (a sibling, never a wrapper), revealed by the pointer or a keyboard focus, that opens the picture in the viewer through the same push, so Back returns to the file at the figure's place; a plain click on the picture does the same while the Comments panel is closed, a Cmd/Ctrl-click opens the /file URL in a tab, a remote picture opens in a tab, and a gated placeholder gets its control once loaded. Browser history is not integrated (recorded as a follow-on with the trade-off). No kernel change, no new route. The trail half reads nothing upstream lacks; the figure half reads the fork's comments model, gate and panel, so an offer adapts it (the where line). Records in plans/markdown-viewer.md, Follow-on: Link navigation.
