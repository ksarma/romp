---
title: The file viewer keeps a trail of the files reached through its links, with Back and Forward, and an Open the picture control on its figures
status: candidate
where: fork branch `filereview-linknav`, cut from the fork's main 34142c262: ui/webview/file-trail.ts (new: the trail's state and pure functions, trailRoot, trailPush, trailBack, trailForward, trailSetView, trailEnd, trailBackTarget, trailForwardTarget, fileNameOf, navTitle and navChord, and the live instance, liveTrail and setTrail), ui/webview/file-view.ts (openFromViewer and the trailNext tag; moveTrail after runLeave's write in the replace path; the bar's fileview-nav group with its two glyph buttons, hidden when neither direction has a target (T367; the PR's file review, round 2); onNavKey, one capture-phase document listener removed through the close hooks; closeFileView and openUrlView end the trail and the conflict Reload tags itself reload; the body delegate's path-link open through openFromViewer; the figure half: FIGURE_OPEN_TITLE, figureTarget (refusing on figureHasPicture, one rule over FigureState with figureWantsControl, a target or a control only for a state with a picture to name, so a fetching or a failed figure opens nothing on any gesture and a state added later is refused with them; the PR's file review, round 2, the rule in place of a list of the two before its round 3) and chosenSource (the candidate the browser chose, as the author wrote it, which failedSource delegates to; the review's round 2), figureControlOf, figureOfControl, bareFigureOf, linkAbove (any anchor), figureState with figureHasPicture, figureBox (the laid-out box of a figure in the document as it is, 0 by 0 included, so a loaded figure the author gave no box is under the floor; the picture's own size only for a figure not yet in the document; the file review's round 3, correctness-1) and the FIGOPEN_MIN_PX floor with figureTooSmall, the one decision decideFigureControl over the verdict figureWantsControl, addFigureControls in mdBlock's file arm, armFigureControls (the load and the error), watchFigureBoxes (one ResizeObserver per open over the figures of the Rendered box, armed at each text paint and not before the first, in place of a re-read from the width watch's repaint, which missed a text-size step, the PR's file review, round 2; a 0 by 0 report runs no decision, a rule over the report whatever produced it since the file review's round 4 (regression-3: before its round 3 the reason named the viewer's hide and a gated placeholder's img as the roads to such a report, reporting the real box at the show or the restore; narrowed in its round 3 and stated with its residual by the author's closing pass after the file review's round 3 (behaviour-4), the list still closed, and a loaded figure the author gave no box outside it), the residual a figure hidden after its load by any other road keeping a standing control until its next report with a box, which the product reaches by a `<details>` folded by the reader (an expanded callout renders as one), harmless there since the fold reports nothing and folds the control with the figure and the reopen's first report with a box decides the figure again (the file review's round 4, ui-1; the line before it said no road in the product reached it), and the arm before the first paint removed in the file review's round 3) and removeFigureControl with the keyboardTakers register (the keyboard handed to the viewer's body before a control's removal), the body's second click listener with openFigure and the figure's own door openFigureInViewer, and figureControlAfter with figureLabelAfter's step past the control; since the author's closing pass after the file review's landing round mdBlock's chain block comment states the census's reach, the reached locals' imported callees' modules and their imports under every import form, and its judged sites with their reasons, in place of a no-write claim the widened census contradicted), ui/webview/icons.ts (ICON_BACK, ICON_FORWARD, ICON_EXPAND), ui/webview/styles.css and ui/webview/feed.css (the `.fileview-md .fv-figopen` rules, the same lines in both), docs/guide.md (the two closing sentences of the Links in a file paragraph; since the author's closing pass after the file review's round 3 (behaviour-2, records-1, coverage-1) the trail sentence names the pictures opened from figures among what the trail keeps and states that the two arrow buttons appear once there is a file to step back or forward to, after a link is followed or a picture opened, and not before; its round 3 had conditioned them on a link followed, which is false on the picture road); tests: ui/webview/file-trail.test.ts, ui/webview/file-trail-browser.test.ts, ui/webview/file-figure-open.test.ts and ui/webview/file-figure-open-browser.test.ts (new; since the author's closing pass after the file review's round 8 the browser leg drives the reader's place on a top-level html-block figure wearing the production control, the Raw switch landing on the img's own row, the layout the node scene's fixture box stands in for), ui/webview/file-view-figure-shapes.test.ts and ui/webview/file-view-figure-shapes-browser.test.ts (new in the review's round 1: figureTarget reads the web address before the model's join, no control stands inside a link that holds more than the figure, the bare figure's click yields to an enclosing link, and the 48px floor, pinned at source and driven in Chromium; since the file review's round 3 the source pins derive the refused states and hold their literals absent from every reader, since its round 4 read as the compiler reads them through ui/webview/source-units.ts, the reader the attribution pin shares, with the pin's boundary (whole literal values and regular expressions; not an identifier key, an assembled word, a prefix or a case-folded test) stated in its message and pinned by execution since the author's closing pass after the file review's round 4 (guards-1 with records-11), and hold the arm before the first paint absent), ui/webview/file-view-figure-chosen.test.ts and ui/webview/file-view-figure-chosen-browser.test.ts (new in the review's round 2: figureTarget reads chosenSource, pinned at source and driven in Chromium over a picture element, a srcset img and a gated remote picture; the browser leg observes the remote figures' requests at a context-level route with the real window.open since the PR's file review, and since its round 2 from before the open, with a never-drained list of foreign window.fetch calls, the Cookie header of each request read under three seated classes, the loaded picture's Ctrl-click and the failed remote figure's clicks), ui/webview/file-view-figure-floor-browser.test.ts, ui/webview/file-view-figure-state-browser.test.ts and ui/webview/file-view-figure-recent-browser.test.ts (new in the PR's file review, 2026-09-20: the floor re-read at each reflow of the figure, the body's width and a text-size step alike, the keyboard handed on at a control's removal, the hidden viewer's 0 by 0 report adding no control while hidden, and, its round 3, a loaded `<img hidden>` and `<img width="0">` beside prose getting no control with the click on the words before them opening nothing, the click point derived from the words themselves since the author's closing pass after the file review's round 3 (behaviour-3), and a picture re-fetched while the viewer is hidden losing its control at that load and regaining it at the show; the control decided from the figure's current state, a failed figure with a box opening nothing; and no Recent row for a picture opened from a figure while a Forward step onto it mints one, each driven in Chromium), and re-aimed ui/webview/file-view.test.ts, ui/webview/file-view-links.test.ts, ui/webview/file-view-text-size.test.ts (its bar case reads the group hidden on a fresh open), ui/webview/fileview-parity.test.ts (and, in the file review's round 10, correctness-5, its Outline button census on parsed rules over every sheet a page of either host loads), ui/webview/file-view-outline.test.ts (the same census), ui/webview/file-view-notice.test.ts (its Escape cases run every keydown handler an open registers in the document's order, the capture phase first, and its shim removes a listener only under the capture flag its add carried) and ui/webview/md-url-view.test.ts (the local-file mode's sibling link opens through openFromViewer with a push, and the door's shape: the tag set, the host's opener called, the tag cleared in a finally) and ui/webview/pdf-new-tab.test.ts (the sweep repair: its count of the gesture reads inside openFileView counts the links' two and the figure's four apart and the rest of the function as none, so openFileView's own open still reads none); the trail half (file-trail.ts, the bar's group, the chords, the two arrows) reads nothing upstream lacks, and the figure half reads fork-only modules (figurePath from file-comments-model.ts, panelMark from file-comments.ts, GATE_ACT from figure-gate.ts, the regions layer's wrap) and its browser legs boot the fork's real-viewer-leg.ts, files.ts page and md-config.ts wikilinks, so an offer adapts them rather than copies; FORK-ONLY: ui/webview/anchor-map.ts and ui/webview/reader-place.ts (CONTROL_CLASSES gains fv-figopen; anchor-map's isFigureCompanion; since the file review's round 8, correctness-1, reader-place's structural read leaves a figure's two companions out of the element lists it walks by the same pair predicate, blockElementsOf with isFigureCompanion, so a top-level figure wearing the control reads as the figure and not as the paragraph after it) with their suites ui/webview/anchor-map-fallback-markup.test.ts, ui/webview/file-view-figure-error.test.ts and ui/webview/file-view-figure-error-browser.test.ts (re-aimed: the label readers step past the control) and tests/test_guide_files_failures.py (re-aimed: first to hold both walks' control lists' seventh entry, fv-figopen, then, the file review's round 8, correctness-1, to name the executed cases of the control's exclusion in that pin's place, a pin on a list's membership having stayed green while the structural read ignored both lists), ui/webview/file-view-place-blocks.test.ts (re-aimed in the file review's round 8, correctness-1: readPlace over a top-level figure wearing the control, at the root's level and nested in a wrapper, and over a right-floated figure wearing the failed figure's label at its top, reads the figure; red before the fix at the paragraph after it), ui/webview/file-view-seam.test.ts (re-aimed at the merge of the fork's main and in the file review's landing round: its re-parse census's callee list gained addFigureControls, this follow-on's pass, with the local functions it reaches, and the walk follows a reached local's imported callees and every import form transitively, judging the sites it finds per module, with the whole file's count in its one home; since the author's closing pass after the file review's landing round its RE_PARSE matches a template argument under any spelling, the form its derivation command and the plan's paragraph spell, the same lines at that head, and its test's name counts the third judged site on the road, the glyph's holder after the adoption; since the author's closing pass after the file review's round 8 its method-call guard reads the compiler's tree under any access spelling, its binding reader follows a require through a cast, parentheses, a non-null mark or a member, and its three compiler readers refuse a module the parser reports a diagnostic on), ui/webview/anchor-map.test.ts (re-aimed in the file review's landing round: the control at the box's top level is no block's node, executed beside the failed figure's label's case; and, extra9-1, the caption beside a control carrying a text node of its own maps, the executed case of anchor-map.ts's entry) and ui/webview/md-config-figure-gate-place.test.ts (re-aimed there too: readPlace over a loaded figure wearing the control, and, extra9-1, over one whose control carries a text node of its own, the executed case of reader-place.ts's entry), and the fork's records with their pin: plans/markdown-viewer.md (the section Follow-on: Link navigation, L1 to L6), plans/file-browser.md (the pointer sentence in the navigation-stack section, the glyphs' condition added in the file review's round 3), tools/markdown-viewer-plan-linknav.test.mjs (new), ui/webview/css-rules.mjs with ui/webview/css-rules.d.mts (new in the file review's round 10, correctness-1 with tests-1 and ui-1: the sheet's rules read as rules, each with the at-rules enclosing it, the one reader the closed set over the control's sheet rules stands on in ui/webview/file-figure-open.test.ts and in tools/markdown-viewer-plan-linknav.test.mjs, where the set had been keyed on lines at column zero; under tools/ at that round's fixes, and under ui/, the tree the bundles read, since the author's closing pass after the file review's round 10, because a ui test may not import from tools/, hooks/ or tests/, trees outside the kernel's bundle-staleness inputs, which tests/test_lab_dist.py's derivation pin holds), ui/webview/host-sheets.mjs with ui/webview/host-sheets.d.mts (new in the file review's round 10, correctness-1 with regression-5 and correctness-5: every sheet a page of either host loads, derived from the page assembly, the kernel's page functions and the extension's webview builders, the population the closed set and the Outline census read through the reader; under ui/ for the reader's reason), tools/markdown-viewer-plan-gate-adopt.test.mjs (main's pin of the gate-before-adoption section, re-aimed at the merge: its held copy of that section's re-parse paragraph, re-derived in the file review's landing round with the derived figures filled from the seam test's pins, and its held copy of the chain block's sentence and of that paragraph following the author's closing pass after the file review's landing round; since the author's closing pass after the file review's round 8 its no-typed-addend guard over that paragraph is keyed on the property, a number word within forty characters of a module token in either order), tools/markdown-viewer-plan-linknav-review.test.mjs (new in the review's round 2: the record's corrections held to the source), ui/webview/linknav-records-attribution.test.ts with ui/webview/source-units.ts (new in the file review's round 4, rules-1 with tests-2: every round a record of the follow-on names belongs to the review named nearest before it and is one the plan's convention enumerates for that review, a pass of the author's has no rounds and an id of its family stands only where the review named nearest before it is the author's pass, read in every checkout as a rule over the tree, every file git lists at the repo root, tracked or untracked and not ignored, whose text names the file review or carries an id of the author's family, the files the branch created (an existence roster, judged in full) and the plan's section, the guide's paragraph and the browser plan's pointer, a phrase outside those judged only where the review named nearest before it is the file review or the author's pass (the file review's round 5, extra5-1 with tests-1, regression-2, correctness-5 with tests-6 and correctness-6 with tests-5 and extra8-4: the first road had read a roster of eighteen files, checked against the diff only by a second road that runs in no CI job; a new uncommitted file is in the population, which no diff lists; file-view.ts's count of this follow-on's units is held equal to the derived count; the reader refuses a suffix it has no rule for naming the file, correctness-7 with tests-4 and extra7-2), and, kept and disclosed as running in no CI job and in none after the merge, from every unit the branch added or touched since the merge-base behind a two-part gate, the base not origin/main and the diff adding the module, the gate a pure function pinned in all four cells and run against a temp repo shaped as the open PR branch, the same round's tests-7 (the file review's round 4, extra8-1, so main, every later branch after the landing and a batch head cut from main's tip stand the road down; a batch head main has moved under runs it over the batch's whole delta, disclosed and not closed by the author's closing pass after the file review's round 4, attribution-and-gates-2 with records-2, which also had the road diff the working tree against the merge-base, guards-3, read the plural and range forms of a round and state the forms outside the reader, guards-2 with attribution-and-gates-3, charge a fault in the plan's section to its plan line, attribution-and-gates-4, and read file-view.ts's units carrying an id of the family in every checkout, records-1), TS and JS read through the compiler's parser and Markdown and Python as paragraphs; the rule stood in the review module since the file review's round 3, keyed on one string and one apostrophe, and the author's closing pass after the file review's round 3, behaviour-6 with records-2 and coverage-2, gave it its two roads; since the author's closing pass after the file review's round 8 its exported count of checks keyed on the delta is held to its own call sites, read with the comments blanked), tests/test_guide_trail_chords_and_figure_button.py (new in the review's round 2: the guide's two sentences held to the shell script's lines in kernel.py and to the viewer's; re-aimed in the PR's file review with a census of figureWantsControl's refusal arms, the failed picture among the four kinds without the button, in its round 3 with the refused states' literals held absent by the property, that copy removed in its round 4, regression-2 with extra6-1, for the one pin in ui/webview/file-view-figure-shapes.test.ts, and since its round 4, extra8-3, asserting the hide line, the guide's condition clause and the browser plan's clause together in one test) and this entry; 48 files in all by `git diff --name-only $(git merge-base origin/main HEAD) HEAD`, run 2026-09-21 at the head that carries the author's closing pass after the file review's round 10, where the merge-base is the fork's main the branch merged after the fixes for the file review's round 10, every one of the 45 named in this line (the run at the head that carries the fixes for the file review's round 8, where the merge-base was 5e5ee79ed, the fork's main merged into the branch before landing, listed 43, before ui/webview/css-rules.mjs and ui/webview/css-rules.d.mts (under tools/ until the author's closing pass after the file review's round 10) joined the delta in the fixes for the file review's round 10; the run at the head that carries the landing round's first fixes, where the merge-base was 01434a45b, the fork's main the branch had merged for the file review's round 5, listed 42, before ui/webview/file-view-place-blocks.test.ts joined the delta in the fixes for the file review's round 8; the run at the head after that merge, for the file review's round 5, listed 40, before the landing round's executed test re-aimed two more standing suites, and this line then named 39 of them, file-view-seam.test.ts unnamed; the two earlier runs, at the commit that built the one reader for the file review's round 4 and at the head that closed the author's closing pass after the file review's round 4, listed 38 from the older merge-base they shared, before the merge made main's tools/markdown-viewer-plan-gate-adopt.test.mjs a file the branch edits; the branch point became an ancestor of main when main was merged in, so a diff from it no longer names the branch's files), this entry among them
added: 2026-09-19
pr: 862
tier: feature
offered:
closed:
---
A link followed inside a shown file replaced the card with no way back but the Files pane's Recent list. The viewer now keeps a trail of the files reached through its own links (a plain state with pure functions, file-trail.ts; the viewer's own opens take one door that tags the open, so an open from outside is one by construction) and shows Back and Forward as two glyph buttons at the left of its bar, with Alt+Left and Alt+Right (Cmd+[ and Cmd+] on a Mac) as chords while no text field holds the keyboard. Back re-opens the previous file with no target, so the remembered place re-seats it, in the view it was left in, without touching the saved preference; an open from outside the viewer starts the trail over, and closing the viewer ends it, as does, under the chat's document-level opener, a plain click on a same-origin absolute .md or .markdown link inside the shown file (the chat's link rule, md-links.ts's isMarkdownUrl, case-insensitive), which replaces the card with the URL view (openUrlView) and ends the trail the same way (the file review's round 10, ui-2: this entry and the guide had named the close alone, the plan's L1 both; on the Files page, whose document has no such opener, the link opens a tab and the trail stands, driven in Chromium by file-trail-browser.test.ts; the replace is held in the tree by file-trail.test.ts's wiring pin). A picture a rendered file embeds from a file or a web address wears an Open the picture glyph button after it (a sibling, never a wrapper; decided from the figure's current state at the paint, the load, the error and each reflow of the figure's own box, the body's width and a text-size step alike: none while the picture is fetching or after a failed load, on a data: picture, on one under 48 px on either side, or inside any link that holds more than the picture), revealed by the pointer or a keyboard focus, that opens the picture in the viewer through the same push, so Back returns to the file at the figure's place; the picture opened is the candidate the browser chose (a picture element's source, a srcset candidate), never the fallback src; a plain click on the picture does the same while the Comments panel is closed, a Cmd/Ctrl-click on the picture opens the /file URL in a tab while the panel is closed or the pointer is coarse (with the panel open on a fine pointer the layer's overlay takes the press, modified or not, and offers a comment; the control opens at any time); a loaded remote picture opens its own address in a top-level tab from the control at any time, and from the plain click and the Cmd/Ctrl-click where the press reaches the picture, the panel closed or the pointer coarse (a second, differently kinded, credentialed request: a document request to a host the page had requested the image from, carrying cookie classes the image request did not, the Lax class in Chromium's measurement, where before only an author's link opened one: the PR is a privacy surface and lands on the owner's word; the gate is not bypassed), a fetching or a failed figure opens nothing on any gesture, and a gated placeholder gets its control once loaded; the picture opened from a figure takes no Recent row, and a Forward step onto it does. The Back and Forward group is hidden when neither direction has a target (T367). Browser history is not integrated (recorded as a follow-on with the trade-off). No kernel change, no new route. The trail half reads nothing upstream lacks; the figure half reads the fork's comments model, gate and panel, so an offer adapts it (the where line). Records in plans/markdown-viewer.md, Follow-on: Link navigation.
