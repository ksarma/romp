---
title: The file viewer keeps a trail of the files reached through its links, with Back and Forward, and an Open the picture control on its figures
status: candidate
where: fork branch `filereview-linknav`, cut from the fork's main 34142c262: ui/webview/file-trail.ts (new: the trail's state and pure functions, trailRoot, trailPush, trailBack, trailForward, trailSetView, trailEnd, trailBackTarget, trailForwardTarget, fileNameOf, navTitle and navChord, and the live instance, liveTrail and setTrail), ui/webview/file-view.ts (openFromViewer and the trailNext tag; moveTrail after runLeave's write in the replace path; the bar's fileview-nav group with its two glyph buttons, hidden when neither direction has a target (T367; the PR's file review, round 2); onNavKey, one capture-phase document listener removed through the close hooks; closeFileView and openUrlView end the trail and the conflict Reload tags itself reload; the body delegate's path-link open through openFromViewer; the figure half: FIGURE_OPEN_TITLE, figureTarget (refusing on figureHasPicture, one rule over FigureState with figureWantsControl, a target or a control only for a state with a picture to name, so a fetching or a failed figure opens nothing on any gesture and a state added later is refused with them; the PR's file review, round 2, the rule in place of a list of the two before its round 3) and chosenSource (the candidate the browser chose, as the author wrote it, which failedSource delegates to; the review's round 2), figureControlOf, figureOfControl, bareFigureOf, linkAbove (any anchor), figureState with figureHasPicture, figureBox (the laid-out box of a figure in the document as it is, 0 by 0 included, so a loaded figure the author gave no box is under the floor; the picture's own size only for a figure not yet in the document; the file review's round 3, correctness-1) and the FIGOPEN_MIN_PX floor with figureTooSmall, the one decision decideFigureControl over the verdict figureWantsControl, addFigureControls in mdBlock's file arm, armFigureControls (the load and the error), watchFigureBoxes (one ResizeObserver per open over the figures of the Rendered box, armed at each text paint and not before the first, in place of a re-read from the width watch's repaint, which missed a text-size step, the PR's file review, round 2; a 0 by 0 report runs no decision, since the roads the product has to such a report, the viewer's hide and a gated placeholder's img, report the real box at the show or the restore, before its round 3, the reason narrowed in its round 3 and stated in its round 4 as a rule with its residual, a figure hidden after its load by any other road keeping a standing control, which no road in the product reaches, and the arm before the first paint removed in its round 3) and removeFigureControl with the keyboardTakers register (the keyboard handed to the viewer's body before a control's removal), the body's second click listener with openFigure and the figure's own door openFigureInViewer, and figureControlAfter with figureLabelAfter's step past the control), ui/webview/icons.ts (ICON_BACK, ICON_FORWARD, ICON_EXPAND), ui/webview/styles.css and ui/webview/feed.css (the `.fileview-md .fv-figopen` rules, the same lines in both), docs/guide.md (the two closing sentences of the Links in a file paragraph; since the file review's round 4 the trail sentence names the pictures opened from figures among what the trail keeps and states that the two arrow buttons appear once there is a file to step back or forward to, after a link is followed or a picture opened, and not before; its round 3 had conditioned them on a link followed, which is false on the picture road); tests: ui/webview/file-trail.test.ts, ui/webview/file-trail-browser.test.ts, ui/webview/file-figure-open.test.ts and ui/webview/file-figure-open-browser.test.ts (new), ui/webview/file-view-figure-shapes.test.ts and ui/webview/file-view-figure-shapes-browser.test.ts (new in the review's round 1: figureTarget reads the web address before the model's join, no control stands inside a link that holds more than the figure, the bare figure's click yields to an enclosing link, and the 48px floor, pinned at source and driven in Chromium; since the file review's round 3 the source pins derive the refused states and hold their literals absent from every reader, and hold the arm before the first paint absent), ui/webview/file-view-figure-chosen.test.ts and ui/webview/file-view-figure-chosen-browser.test.ts (new in the review's round 2: figureTarget reads chosenSource, pinned at source and driven in Chromium over a picture element, a srcset img and a gated remote picture; the browser leg observes the remote figures' requests at a context-level route with the real window.open since the PR's file review, and since its round 2 from before the open, with a never-drained list of foreign window.fetch calls, the Cookie header of each request read under three seated classes, the loaded picture's Ctrl-click and the failed remote figure's clicks), ui/webview/file-view-figure-floor-browser.test.ts, ui/webview/file-view-figure-state-browser.test.ts and ui/webview/file-view-figure-recent-browser.test.ts (new in the PR's file review, 2026-09-20: the floor re-read at each reflow of the figure, the body's width and a text-size step alike, the keyboard handed on at a control's removal, the hidden viewer's 0 by 0 report adding no control while hidden, and, its round 3, a loaded `<img hidden>` and `<img width="0">` beside prose getting no control with the click on the words before them opening nothing, the click point derived from the words themselves since its round 4, and a picture re-fetched while the viewer is hidden losing its control at that load and regaining it at the show; the control decided from the figure's current state, a failed figure with a box opening nothing; and no Recent row for a picture opened from a figure while a Forward step onto it mints one, each driven in Chromium), and re-aimed ui/webview/file-view.test.ts, ui/webview/file-view-links.test.ts, ui/webview/file-view-text-size.test.ts (its bar case reads the group hidden on a fresh open), ui/webview/fileview-parity.test.ts, ui/webview/file-view-notice.test.ts (its Escape cases run every keydown handler an open registers in the document's order, the capture phase first, and its shim removes a listener only under the capture flag its add carried) and ui/webview/md-url-view.test.ts (the local-file mode's sibling link opens through openFromViewer with a push, and the door's shape: the tag set, the host's opener called, the tag cleared in a finally) and ui/webview/pdf-new-tab.test.ts (the sweep repair: its count of the gesture reads inside openFileView counts the links' two and the figure's four apart and the rest of the function as none, so openFileView's own open still reads none); the trail half (file-trail.ts, the bar's group, the chords, the two arrows) reads nothing upstream lacks, and the figure half reads fork-only modules (figurePath from file-comments-model.ts, panelMark from file-comments.ts, GATE_ACT from figure-gate.ts, the regions layer's wrap) and its browser legs boot the fork's real-viewer-leg.ts, files.ts page and md-config.ts wikilinks, so an offer adapts them rather than copies; FORK-ONLY: ui/webview/anchor-map.ts and ui/webview/reader-place.ts (CONTROL_CLASSES gains fv-figopen; anchor-map's isFigureCompanion) with their suites ui/webview/anchor-map-fallback-markup.test.ts, ui/webview/file-view-figure-error.test.ts and ui/webview/file-view-figure-error-browser.test.ts (re-aimed: the label readers step past the control) and tests/test_guide_files_failures.py (re-aimed: both walks' control lists carry fv-figopen as the seventh entry), and the fork's records with their pin: plans/markdown-viewer.md (the section Follow-on: Link navigation, L1 to L6), plans/file-browser.md (the pointer sentence in the navigation-stack section, the glyphs' condition added in the file review's round 3), tools/markdown-viewer-plan-linknav.test.mjs (new), tools/markdown-viewer-plan-linknav-review.test.mjs (new in the review's round 2: the record's corrections held to the source; since the file review's round 3 a rule that no line the branch wrote names a third or later round of the branch's own review, read since its round 4 from the files the branch created and the plan's section in every checkout and from the added lines since the merge-base where origin/main is known, the roster of created files checked against the diff's there), tests/test_guide_trail_chords_and_figure_button.py (new in the review's round 2: the guide's two sentences held to the shell script's lines in kernel.py and to the viewer's; re-aimed in the PR's file review with a census of figureWantsControl's refusal arms, the failed picture among the four kinds without the button, and in its round 3 with the refused states' literals held absent by the property) and this entry; 36 files in all by `git diff --name-only $(git merge-base origin/main HEAD) HEAD` run at the file review's round-4 fixes' closing commit (2026-09-20, the merge-base 3711863c9 then; the branch point 34142c262 became an ancestor of main when main was merged in, so a diff from it no longer names the branch's files), the last commit touching this entry, this entry among the 36
added: 2026-09-19
pr: 862
tier: feature
offered:
closed:
---
A link followed inside a shown file replaced the card with no way back but the Files pane's Recent list. The viewer now keeps a trail of the files reached through its own links (a plain state with pure functions, file-trail.ts; the viewer's own opens take one door that tags the open, so an open from outside is one by construction) and shows Back and Forward as two glyph buttons at the left of its bar, with Alt+Left and Alt+Right (Cmd+[ and Cmd+] on a Mac) as chords while no text field holds the keyboard. Back re-opens the previous file with no target, so the remembered place re-seats it, in the view it was left in, without touching the saved preference; an open from outside the viewer starts the trail over, and closing the viewer ends it. A picture a rendered file embeds from a file or a web address wears an Open the picture glyph button after it (a sibling, never a wrapper; decided from the figure's current state at the paint, the load, the error and each reflow of the figure's own box, the body's width and a text-size step alike: none while the picture is fetching or after a failed load, on a data: picture, on one under 48 px on either side, or inside any link that holds more than the picture), revealed by the pointer or a keyboard focus, that opens the picture in the viewer through the same push, so Back returns to the file at the figure's place; the picture opened is the candidate the browser chose (a picture element's source, a srcset candidate), never the fallback src; a plain click on the picture does the same while the Comments panel is closed, a Cmd/Ctrl-click opens the /file URL in a tab; a loaded remote picture opens its own address in a top-level tab from the control, the plain click and the Cmd/Ctrl-click alike (a second, differently kinded, credentialed request: a document request to a host the page had requested the image from, carrying cookie classes the image request did not, the Lax class in Chromium's measurement, where before only an author's link opened one: the PR is a privacy surface and lands on the owner's word; the gate is not bypassed), a fetching or a failed figure opens nothing on any gesture, and a gated placeholder gets its control once loaded; the picture opened from a figure takes no Recent row, and a Forward step onto it does. The Back and Forward group is hidden when neither direction has a target (T367). Browser history is not integrated (recorded as a follow-on with the trade-off). No kernel change, no new route. The trail half reads nothing upstream lacks; the figure half reads the fork's comments model, gate and panel, so an offer adapts it (the where line). Records in plans/markdown-viewer.md, Follow-on: Link navigation.
