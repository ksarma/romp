---
title: tests: a real postal bus no longer outlives the test that revived it, and no test module writes an environment variable at module level outside a licensed set (an import-time environment leak reaching a spawned child; a run-end check that makes a run red when a process still holds a path under the run's temp roots through its environment, cwd, open files or argv)
status: candidate
where: tests/README.md (text); tests/__init__.py (module level, root_lineage, LINEAGE, write_owner_marker, remove_dead_children); tests/conftest.py (module level, pytest_sessionfinish, _report_leaked_run_processes, _leaked_run_processes, _processes_holding, _run_roots, _join_live_threads, LEAK_EXIT_BOUND_S, _dead_manager_port, DEAD_BUS_PORT, _dead_bus_port, _loaded_kernels, _loaded_postal_clients, _SEAM_ENV_NAMES, MODULE_WATCHED_ENV_NAMES, MODULE_ENV_FLOORS, _module_env_restored); tests/fixtures/module-level-env-writers/ROMP_KERNEL_NO_OPEN.txt (text); tests/fixtures/module-level-env-writers/ROMP_SERVE_TOKEN.txt (text); tests/fixtures/module-level-env-writers/floor-only.txt (text); tests/test_hermetic_kernel_postal.py (module level, LICENSED_MODULE_LEVEL_WRITES, FLOOR_MODULES, FLOOR_LEAK_WRITES, module_level_env_census, _Census, _EnvNames, _env_write_records, _Module, _OUTSIDE_THE_SCAN, _Licence, _placement_faults, _DIAL_SPY, BUS_DIALLING_MODULES, HermeticKernelPostal); tests/test_kernel.py (PostalPeerTunnels.test_notify_bus_peer_is_guarded); tests/test_kernel_env_floor.py (CliBinaryFloor.test_the_module_level_write_rule_for_this_name_is_the_repo_wide_pins); tests/test_kernel_known_hosts.py (KnownHostMemory.setUp); tests/test_kernel_tunnels.py (module level, _PostalTrio, TunnelConcierge, BootstrapRemoteKernel); tests/test_peer_reconnect.py (PersistedIntent.setUp); tests/test_postal_agents_render.py (module level, _Seam, AgentsRowsDisambiguate, ShortIdAddresses); tests/test_postal_bus_lifetime.py (KernelUp.setUp, KernelUpSeamRestore); tests/test_postal_heartbeat_fetches.py (module level, _Seam, Counting); tests/test_postal_peer_identity.py (module level, _Seam, PeerIdentityFold); tests/test_postal_peer_tier.py (module level, _Seam, TierDeclaration); tests/test_postal_quarantine.py (module level, _Seam, InboundTrustGate, LeastTrust, TokenProvenDialerGate, ExchangeHandleIsTokenProven, QuarantineDecide, PeerUpdateTrust); tests/test_postal_read_receipts.py (module level, _Seam, _Base); tests/test_postal_self_host.py (module level, MessageIdsNeverCollide.setUp); tests/test_postal_self_identity.py (module level, _Seam, ForkedSelfIdentity); tests/test_postal_token.py (module level, _Seam, BusTokenGate, PeerTokenPlumbing, _BusServer, _PostalTokenFile, _LiveBus); tests/test_relay_exec_dmid.py (module level, _Seam, ReceiptCarriesDeliveryMid); tests/test_run_end_leaked_processes.py (module level, Scanner, Guard, RunEnd, Leaker); tests/test_task_tracking_switch.py (module level); tests/test_tracked_delegation.py (module level, _Seam, PostalTrackedWire, CourierTracked, FeedPayloadPins); upstream/2026-09-18-tests-postal-peers-per-test.md (text); upstream/2026-09-22-tests-postal-bus-outlives-test.md (this entry)
added: 2026-09-22
pr: 894
tier: docs
offered:
closed:
---
A real postal bus started from tests/test_kernel.py's peer-notify guard test (an in-process kernel whose refused bus call revives the bus on a thread, running the postal service's ensure with the test process's environment) and outlived the run in one CI cell of fork PR #813, writing into a shared state root every 30 s and turning another module's snapshot test red. The child's environment was the sum of three import-time writes in other modules: tests/test_kernel_tunnels.py's module-level ROMP_POSTAL_PORT (the run's own, so the bus's fixed-port belt licensed the bind) and ROMP_POSTAL_CLIENT_ONLY (inert while peers are on), and the ROMP_SESSIONS_FILE seam ten postal modules set at module level (one live row, so the bus never autostopped). The fix is fitted to the class rather than the instance: the tunnels module sets all three postal legs per test and patches the kernel's import-time BUS_PORT beside them, restored by cleanups; the guard test holds the revive its refused notify kicks inside itself (the revive rebound to a wrapper that sets an Event in a finally, the restore of its trio waiting on that Event, and for the whole window a scoped fake of subprocess.run that records and answers any call whose argv names romp-postal-service and passes every other call, from any thread, to the real run), its one assertion on the fake being that no postal-service call reached the real run; conftest pops ROMP_POSTAL_PORT before every test; the seam and the postal host move per test in the ten modules; and the repo-wide pin in tests/test_hermetic_kernel_postal.py holds the set of environment names the test modules write at module level EQUAL to a licensed set, each licence checked per write (a value, a per-test re-assert in conftest, a date and the item a temporary licence waits on) and the writer modules of the two dated names whose population nothing mandates committed and compared as sets, with a census command for the counts by name and shape. Beside it, tests/conftest.py gains a run-end check in the controller: every live process whose environment, cwd, open files or argv hold a path under one of the run's temp roots (the controller's and every nested root, which each nested process records in the run's first root) is named (pid, command line, what it holds the root through, the test phase current at its spawn when it carried one) after a bounded wait for its exit, and the run is red; the classes it does not read are named beside it (a process holding no path under a root, an unreadable one, one started after the check reads /proc); it found the tunnels mocks' orphaned trailing sleep on its first run. What stays unknown is what put the first write inside the asserting test's 50 ms window on that cell. Reading rules out a kick, a restart road or a removal of postal/ in the 21 modules between the bus-restore module and the asserting module (server.pid and server.log were not among the files added, so no bus started inside the window), and it read only those modules. One source it did not rule out ran in every run at the round-1 head of fork PR #894, deterministic and seen in 4 of 5 CI cells: every kernel a test module loads in-process read BUS_PORT 25302, the machine's fixed bus port, so its bus calls reached the bus a developer's box runs there (the postal service loaded in-process built its client's BASE from the same popped name, and three set_working tests of tests/test_postal_relay_honesty.py beat the bus through it), and where nothing listened three tests' refused detach notify revived the bus with a real romp-postal-service ensure from the test process (tests/test_kernel_known_hosts.py's two KnownHostMemory detach tests and tests/test_peer_reconnect.py's test_detach_is_the_one_end_of_intent). This PR closes it: conftest gives every loaded kernel module a dead BUS_PORT and every loaded postal module a dead BASE for each test, the three detach tests stub the revive, and the pin in tests/test_hermetic_kernel_postal.py runs the modules whose in-process kernels or postal client dialled the fixed port together in both orders under a connect and spawn spy, with no connect to the fixed port and no postal-service process. That population was derived by a spy over one full serial run, which also recorded two classes of postal-service spawn it leaves out, each for its reason: tests/test_postal_fixed_port_belt.py starts the service as the subject of its tests, and the served labs' kernels run their boot ensure with a port of their own and client-only. What would settle fork PR #813's cell: that spawn spy over one serial run of its head, or PYTEST_CURRENT_TEST and the thread name logged beside the kernel's refusal line. The disclosure is conditional: a run carries the contaminant only when the revive wins the race AND a write lands in an asserting window, the witness being the leaked process the run's log names, with the test name when the spawn carried it. Tests and docs only.

Upstream (their main as the fork read it on 2026-09-25) carries the module-level writes this PR removes: the ROMP_SESSIONS_FILE seam in the same ten postal modules, and ROMP_POSTAL_PORT and ROMP_POSTAL_CLIENT_ONLY in tests/test_kernel_tunnels.py; and their PR #999 took the temp-root hygiene the run-end check reads. It differs on four counts. Their PR #1848 (3968bf4ec, 2026-09-18) fixed the guard test's race in the kernel: _revive_postal_bus returns before its lock when the kernel's environment carries ROMP_POSTAL_CLIENT_ONLY, their conftest sets that name to "1" for the run, and their guard test waits the revive out before its restore and asserts that no romp-postal-service run happened. Their tunnels module still writes ROMP_POSTAL_PEERS at module level, which the fork moved per test in upstream/2026-09-18-tests-postal-peers-per-test.md. They have no census walker: the licensed set, the census and the committed writer sets are this PR's. And their tests/__init__.py has no TEST_ROOT_CHILDREN, the list of nested roots the run-end check reads, so tests/conftest.py's _run_roots raises AttributeError against it; the fork added that list in upstream/2026-09-21-tests-temp-root-beside.md. This entry depends on those two fork candidates, neither offered yet, and supersedes neither: it renames the 2026-09-18 entry's _PeersOff to _PostalTrio and moves the port and client-only per test beside peers, and that entry's other files stay its own.

Fork PR #875 folds their PR #1848 into the fork: the revive's skip gated on the kernel having ensured no bus of its own (_BUS_ENSURED[0]), their guard test's assertion, and the client-only floor in tests/conftest.py, "1" at import after the hermetic marker and again before every test in _dead_manager_port. This PR takes the guard test's form as the reviewer ruled it (round 1's ruling, as corrected on 2026-09-23): the test holds the revive inside itself with the wait and the scoped fake above and asserts nothing about whether the revive runs the ensure, so it holds under the fork's kernel, upstream's and fork PR #875's. The census's per-name check skips the floor modules, and the tunnels import probe compares client-only with the value the floor left, so both hold under this PR's conftest and under fork PR #875's floor.

Whichever of the two PRs lands second resolves the merge in five places. The two change four of the same files (tests/conftest.py, tests/test_hermetic_kernel_postal.py, tests/test_kernel.py and tests/test_postal_bus_lifetime.py). A merge of the two heads read on 2026-09-25 marks one conflict, in _dead_manager_port, and each resolution below was shown by running that merge over 65 test modules: every module either PR changes, and every module that names client-only, the revive, _BUS_ENSURED or the bus port. With the conflict resolved by (1) alone, the merge leaves tests red in tests/test_hermetic_kernel_postal.py, in tests/test_postal_peers.py under the selections (4) names, and in modules whose tests run after the guard test in the same process and depend on subprocess.run working. The guard test as the merge writes it leaves this PR's fake in place of subprocess.run when it ends (item (2) below says why), so for the rest of that process a call whose argv names romp-postal-service gets the fake's refusal and every other call recurses into the fake until Python raises RecursionError. Which modules fail depends on which tests share the guard test's process and on what the code that calls subprocess.run does with the refusal or the error. Run after the guard test in one process per group of ten to twelve modules, 24 of the other 64 modules fail that pass without it: the 13 served and browser modules (the names that end in _served or _browser), tests/test_chat_build_sig_inputs.py, tests/test_chat_fixed_cost_memos.py, tests/test_chat_pages.py, tests/test_host_transport.py, tests/test_judge_serve.py, tests/test_kernel_delta_send.py, tests/test_kernel_tunnels.py, tests/test_perf_stats.py, tests/test_postal_fixed_port_belt.py, tests/test_run_end_leaked_processes.py and tests/test_stage0_log_readers.py. The hermetic module and tests/test_perf_bench.py fail more tests than they do without it. In the same runs, a spy wrapped around subprocess.run before each test recorded calls from 37 of the 64 modules and no call from the other 27. The 24, the hermetic module and tests/test_perf_bench.py are 26 of the 37. The other eleven pass although they call it: every call from them is one of the kernel's reads of git state (the checkout's HEAD, its sha or its version, or the toplevel of a session's directory), each of which catches the RecursionError and returns what it returns when git fails, and their tests pass with that value. They are tests/test_bus_seed_token.py, tests/test_card_boards.py, tests/test_feed_composition.py, tests/test_feed_session_memo.py, tests/test_feed_session_started.py, tests/test_fold_checkpoints.py, tests/test_kernel_goal_cache_wiring.py, tests/test_kernel_known_hosts.py, tests/test_skill_load_wrapper.py, tests/test_subagent_tree_cycle_scope.py and tests/test_task_tracking_switch.py. The spy records the calls that reach subprocess.run during a test's setup, call or teardown. It does not record a call through a reference bound before the guard test ran, a call made while a test's own stub replaces subprocess.run, or a call in a child process; none of those reaches the fake. In tests/test_kernel.py's own order, 444 of its tests run before the guard test and 19 after it; the spy recorded no call from the 19, and they pass. In one run of the 65 under -n 4, 9 of the 24 failed. (1) _dead_manager_port keeps both lines: this PR's pop of ROMP_POSTAL_PORT and fork PR #875's client-only "1". (2) The guard test is resolved by hand. The merge writes fork PR #875's lines into it with no conflict marker, after the line that installs this PR's fake. Their line real_run = km.subprocess.run then binds real_run to the fake, so the recorder they install over the fake and the fake's own pass-through to the real run both call the fake, and the restore in the finally puts the fake back. Three hermetic pins red on the merged text: test_the_peer_notify_guard_test_runs_the_trio_the_wrap_the_wait_and_the_scoped_fake_as_statements_that_run, test_the_guard_pin_reds_on_a_part_that_never_runs_and_on_a_put_back_in_any_form and test_the_guard_tests_fake_answers_only_a_postal_service_call_and_passes_every_other_call_to_the_real_run. The recorder and its polling loop go; fork PR #875's assertion stays, as an assertion that the fake's list of postal-service calls is empty, with _BUS_ENSURED[0] held False from before the revive is rebound until after the wait. (3) Client-only becomes a name conftest re-asserts before every test, and three hermetic tests state those names by equality: the count in test_conftest_re_asserts_the_names_the_re_asserted_licences_rest_on goes from 11 to 12, and client-only joins the names _dead_manager_port re-asserts in test_a_re_asserted_licence_holds_only_where_a_child_pytest_sees_the_fixtures_own_re_assert and test_the_real_conftests_proof_runs_its_child_in_one_scratch_copy_of_the_checkout. (4) For the same reason, conftest's _module_env_restored compares client-only with its floor, as it does ROMP_POSTAL_PORT: MODULE_ENV_FLOORS maps ROMP_POSTAL_CLIENT_ONLY to "1", test_the_module_env_fixture_watches_the_seams_and_the_trio_and_no_name_conftest_re_asserts_but_the_port expects both names, and the two client-only tests of tests/test_postal_peers.py put the prior value back instead of popping it. Without that last change, the module check names tests/test_postal_peers.py in any run whose last test of the module is one of those two, such as a selection of either. (5) Fork PR #875 brings three upstream modules, tests/test_card_boards.py, tests/test_postal_bus_revive_guard.py and tests/test_subagent_tree_cycle_scope.py, that write ROMP_KERNEL_NO_OPEN and ROMP_SERVE_TOKEN at module level. The committed writer sets admit no new writer, so the census reds; the resolution drops the six writes, and the three modules pass without them. Resolved this way, the merge ran over the 65 modules under -n 4 on Python 3.12 and on 3.10, with vscode-extension/node_modules in place. Without it, 91 of the 103 tests of the 13 served and browser modules skip with the reason extension deps absent, every test of 11 of those modules (nine that fork PR #875 changes and two that name client-only and the bus port), and the run reads green with none of those tests run. Every module ran tests, and every test passed, or skipped on a precondition it names, except two. The skips, 24 on 3.12 and 31 on 3.10, are ten tests of tests/test_perf_stats.py that need a git checkout, which the copy was not; eleven of tests/test_run_end_leaked_processes.py that run only in the child pytest the module's own tests start; one of tests/test_perf_stats.py that runs on a Mac only; one of tests/test_host_transport.py that skips when XDG_STATE_HOME holds no romp/session-hosts file, as it does on main; one of tests/test_feed_composition.py that reads the PR body, which lives outside the repository; and, on 3.10 alone, five of tests/test_host_transport.py and two of tests/test_session_host.py that need the agent SDK, which that interpreter's environment lacks. The first of the two failures is the trio's revive probe, test_the_trio_stubs_the_revive_road_and_its_cleanup_fails_on_a_revive_and_puts_the_road_back: it asserts that a revive under the trio's client-only reaches the road it stubs, and under fork PR #875's gate that revive returns before the road. How that probe should change is not yet decided. The second is a test that reads the checkout's git HEAD: it fails in any copy with no .git, a copy of either head alone included. The rest of tests/, the bats tests and the webview tests were not run on the merge.
