---
title: Tags-scale test: shim nodes inspect as a projection, so a failing assertion cannot allocate a 100 GB diff; the UI test runner caps the heap
status: candidate
where: ui/test-dom-shim.ts (the shared fake DOM: nodeFactory, hideEdges, the structural projection rule, and defineHidden for the shim's own later writes), ui/test-dom-shim.test.ts (the rule pinned, the detector's shapes on scratch sources, and a ratchet over ui/**/*.test.ts: a test file that initialises an edge-named property calls hideEdges( or nodeFactory( from the shared module or is on a named allowlist whose length a constant pins, lowered as files migrate), ui/timeline-tags-scale.test.ts (on the shared shim; the projection pin over the live dialog at scale plus a node of upstream's variant shape; the runner-cap pin), ui/timeline-hidden-hold.test.ts, ui/timeline-hidden-stub.test.ts, ui/timeline-kernel-post.test.ts, ui/timeline-live-tick.test.ts, ui/timeline-nan-window.test.ts, ui/timeline-open-interval.test.ts, ui/timeline-pending-hosts.test.ts, ui/timeline-render.test.ts, ui/timeline-tagbtn-click.test.ts, ui/timeline-tagorder-drag.test.ts, ui/timeline-theme-light.test.ts, ui/timeline-transform-tick.test.ts, ui/timeline-views-ack.test.ts, ui/timeline-zoom-anchor.test.ts (the fourteen other timeline tests that build the panel on a fake DOM) and ui/webview/tab-color-picker.test.ts (all fifteen switched from their near-copies of the shim to the shared module), CONTRIBUTING.md (the runner cap, its derivation and its limit), vscode-extension/package.json (the test script passes --max-old-space-size=2048). Upstream (main as fetched 2026-09-10, 9109afac): sixteen makeNode shims with a different membership from the fork's sixteen. Thirteen of the fork's fourteen timeline siblings are there with shim bodies byte-identical to the fork's (the surrounding tests differ in six of them), ui/webview/tab-color-picker.test.ts is identical, ui/timeline-tag-chips.test.ts carries a variant the fork lacks (textContent a plain data property, getAttribute null for an absent key, removeChild leaving parentNode set), and their tags-scale test (their PR #1198, merged 2026-09-09) carries an adopt()/_ownerDoc/ownerDocument shim and a beforeEach the fork does not carry yet. Their ui/timeline-live-tick.test.ts has no fake DOM (its tick tests live in transform-tick on both sides and its two other tests are source pins), so the fork's DOM section there has no upstream counterpart. Their test script has no --test-concurrency, so the cap lands there as node esbuild.js --tests && node --max-old-space-size=2048 --test 'out-tests/**/*.test.js'. The offer is a PORT onto their copies as they are: the shared module and the structural rule onto their sixteen (the adopt()/_ownerDoc/ownerDocument members move into the module, where the rule hides them; the ported SWITCHED list swaps live-tick for tag-chips or the ratchet goes red; the fork picks tag-chips up at the next fold), the recursive textOf in their kernel-post's rows, rowNamed and selected() helpers and at its All-row check-mark read (all four go through their concatenating getter), and the runner cap in their runner's shape
added: 2026-09-09
pr: 523
tier: docs
offered:
closed:
---
Root cause: assert/strict equal and deepEqual build a diff on failure even with a message, inspecting both sides at depth 1000 with getters on; the shim node enumerable parentNode and firstChild getter made that dump the whole dialog (2^depth), and node Myers diff then clones an Int32Array of 2(N+M)+1 per edit-distance level, 8N^2 bytes for a 71k-line dump against null, outside the V8 heap. Reproduced under a cgroup cap at 1.2 GB/s. Fix: the shim edges, listener tables and methods are non-enumerable; a node dumps in about 25 lines.

2026-09-10, review round 1 (fork PR 523): the edge hiding is STRUCTURAL and lives in one shared module, ui/test-dom-shim.ts, which replaced the fifteen near-copies of the shim (fourteen ui/timeline-*.test.ts siblings and ui/webview/tab-color-picker.test.ts): after construction every own property holding an object, an array, a function or null, and every accessor, is non-enumerable, and primitives stay. A key list had missed upstream's variant of the shim (their #1198 gives every node an enumerable _ownerDoc and an ownerDocument accessor; the projection pin failed there at 127 lines per dot); the rule covers it, and the pin now includes a node of that shape. Follow-up, named in ui/test-dom-shim.test.ts's allowlist (150 ui/webview test files after round 3): most are class-based fake DOMs (El and Txt, FakeNode, E, FakeEl, Elm) carrying parentNode, parent, childNodes or ownerDocument fields; some hold a window stand-in (a parent that is the stand-in itself, or a record) or a goal fixture with a children list, dumps of a few lines today; the fix is a hideEdges call from the shared module on the object (at the end of each constructor for a class), plus a projection test; until then a card in feed-keynav-covered dumps as 318,835 lines. Port notes: their test file's two conflicts are the header comment beside their adopt() comment and the new test's placement next to their beforeEach; their adopt() walks children and sets _ownerDoc, so the shared module there carries the ownerDocument members (hidden by the rule) and its appendChild and insertBefore adopt as theirs do; the fork's copy picks up their shim members at the next fold (upfold #479 does not reach 13443256).

2026-09-10, review round 2 (fork PR 523): the ratchet's detector implements the rule its header states (an edge name under any of ten names, initialised as a class field with any modifier anywhere on a line, plain, definite, optional or declared without an initializer, a constructor parameter property, an assignment through any name, or an object-literal key with null, undefined, [] or an identifier; a file importing nodeFactory or hideEdges from the shared module counted as switched, until round 3 required the call), with a positive and a negative scratch source per shape in its own tests; it found four more files (codex-meta-choices, file-view, pdf-new-tab, track-decorations), allowlisted at 141 under a ceiling constant lowered as files migrate (round 3 pins the length by equality). The shim's own write after construction (_sel) is defined non-enumerable, and the header says what the rule hides and when: at creation, so what product code hangs on a node later enumerates, with the projection pins and the cgroup cap as the backstop. The where line was recounted from upstream/main as fetched (9109afac).

2026-09-10, review round 3 (fork PR 523): one rule for every file under ui/. A test file that initialises an edge-named property in any shape the detector reads calls hideEdges( or nodeFactory( imported from the shared module (the import alone is not the credential, and either quote style on the specifier is read) or is on the allowlist, whose length ALLOWLIST_MAX pins by equality: a renamed file replaces its entry, a file that comes off lowers the constant in the same commit, a new file may not join. The DOM-vocabulary condition is gone, so a window stand-in's parent and a goal fixture's children count like a class's parentNode. The detector reads a capitalised identifier and a this.-rooted value; in the declared-only field form it leaves host out with the payload lines (a value that starts with a quote, a digit or a boolean, or carries a parenthesis); and the text a regex cannot tell from an edge init (a destructuring default, a type literal's member, a source pin quoting a typed parameter) is pinned as such, with a case per shape. Nine more files are listed, 150 in all, for PR 569 to migrate: card-subgoals, feed-absorb, perf-telemetry, setting-stale, shell-perf, tab-snapshot-view, thread-selection-scope, timeline-rehover and track-decorations-hover-cost. CONTRIBUTING.md names V8's 256 MB floor on the old generation and the flag behind its 4 GB step.
