---
title: Tags-scale test: shim nodes inspect as a projection, so a failing assertion cannot allocate a 100 GB diff; the UI test runner caps the heap
status: candidate
where: ui/test-dom-shim.ts (the shared fake DOM: nodeFactory, hideEdges, the structural projection rule, and defineHidden for the shim's own later writes), ui/test-dom-shim.test.ts (the rule pinned, the detector's shapes on scratch sources, and a ratchet over ui/**/*.test.ts: a test file that initialises an edge-named property calls hideEdges( or nodeFactory( from the shared module or is on a named allowlist whose length a constant pins, lowered as files migrate; on the fork that list is empty since fork PR 569, entry 2026-09-10-shim-migration, every ui/webview test file the detector finds either calling the module or named in a second list as a documented non-DOM use of an edge-named key, so the port computes upstream's lists from upstream's own tree), ui/timeline-tags-scale.test.ts (on the shared shim; the projection pin over the live dialog at scale plus a node of upstream's variant shape; the runner-cap pin), ui/timeline-hidden-hold.test.ts, ui/timeline-hidden-stub.test.ts, ui/timeline-kernel-post.test.ts, ui/timeline-live-tick.test.ts, ui/timeline-nan-window.test.ts, ui/timeline-open-interval.test.ts, ui/timeline-pending-hosts.test.ts, ui/timeline-render.test.ts, ui/timeline-tagbtn-click.test.ts, ui/timeline-tagorder-drag.test.ts, ui/timeline-theme-light.test.ts, ui/timeline-transform-tick.test.ts, ui/timeline-views-ack.test.ts, ui/timeline-zoom-anchor.test.ts (the fourteen other timeline tests that build the panel on a fake DOM) and ui/webview/tab-color-picker.test.ts (all fifteen switched from their near-copies of the shim to the shared module), CONTRIBUTING.md (the runner cap, its derivation and its limit), vscode-extension/package.json (the test script passes --max-old-space-size=2048). Upstream (main as fetched 2026-09-10, 9109afac): sixteen makeNode shims with a different membership from the fork's sixteen. Thirteen of the fork's fourteen timeline siblings are there with shim bodies byte-identical to the fork's (the surrounding tests differ in six of them), ui/webview/tab-color-picker.test.ts is identical, ui/timeline-tag-chips.test.ts carried a variant the fork then lacked (textContent a plain data property, getAttribute null for an absent key, removeChild leaving parentNode set; the fold has since brought the file to the fork and fork PR 569 switched that copy onto nodeFactory), and their tags-scale test (their PR #1198, merged 2026-09-09) carries an adopt()/_ownerDoc/ownerDocument shim and a beforeEach the fork does not carry yet. Their ui/timeline-live-tick.test.ts has no fake DOM (its tick tests live in transform-tick on both sides and its two other tests are source pins), so the fork's DOM section there has no upstream counterpart. Their test script has no --test-concurrency, so the cap lands there as node esbuild.js --tests && node --max-old-space-size=2048 --test 'out-tests/**/*.test.js'. The offer is a PORT onto their copies as they are: the shared module and the structural rule onto their sixteen (the adopt()/_ownerDoc/ownerDocument members move into the module, where the rule hides them; the ported SWITCHED list swaps live-tick for tag-chips or the ratchet goes red; on the fork, fork PR 569 made that swap on the copy the fold brought, nodeFactory replacing its makeNode, the same swap the port makes upstream), the recursive textOf in their kernel-post's rows, rowNamed and selected() helpers and at its All-row check-mark read (all four go through their concatenating getter), and the runner cap in their runner's shape
added: 2026-09-09
pr: 523
tier: docs
offered:
closed:
---
Root cause: assert/strict equal and deepEqual build a diff on failure even with a message, inspecting both sides at depth 1000 with getters on; the shim node enumerable parentNode and firstChild getter made that dump the whole dialog (2^depth), and node Myers diff then clones an Int32Array of 2(N+M)+1 per edit-distance level, 8N^2 bytes for a 71k-line dump against null, outside the V8 heap. Reproduced under a cgroup cap at 1.2 GB/s. Fix: the shim edges, listener tables and methods are non-enumerable; a node dumps in about 25 lines.

2026-09-10, review round 1 (fork PR 523): the edge hiding is STRUCTURAL and lives in one shared module, ui/test-dom-shim.ts, which replaced the fifteen near-copies of the shim (fourteen ui/timeline-*.test.ts siblings and ui/webview/tab-color-picker.test.ts): after construction every own property holding an object, an array, a function or null, and every accessor, is non-enumerable, and primitives stay. A key list had missed upstream's variant of the shim (their #1198 gives every node an enumerable _ownerDoc and an ownerDocument accessor; the projection pin failed there at 127 lines per dot); the rule covers it, and the pin now includes a node of that shape. Follow-up, named in ui/test-dom-shim.test.ts's allowlist at the time (150 ui/webview test files after round 3, 156 after round 4): most were class-based fake DOMs (El and Txt, FakeNode, E, FakeEl, Elm) carrying parentNode, parent, childNodes or ownerDocument fields; some held a window stand-in (a parent that is the stand-in itself, or a record) or a goal fixture with a children list, dumps of a few lines; the fix is a hideEdges call from the shared module on the object (at the end of each constructor for a class), plus a projection test, and fork PR 569 (entry 2026-09-10-shim-migration) landed it in every one the same day (see the 2026-09-10 follow-up line below); until then a card in feed-keynav-covered dumped as 318,835 lines. Port notes: their test file's two conflicts are the header comment beside their adopt() comment and the new test's placement next to their beforeEach; their adopt() walks children and sets _ownerDoc, so the shared module there carries the ownerDocument members (hidden by the rule) and its appendChild and insertBefore adopt as theirs do; the fold brought their ui/timeline-tag-chips.test.ts to the fork and fork PR 569 switched that copy onto the shared module, nodeFactory replacing its makeNode (their variant kept textContent as a data property, returned null from getAttribute for an absent key and left parentNode set on removeChild; tag-chips runs after the switch, 5 of 5), the same swap the port makes upstream.

2026-09-10, review round 2 (fork PR 523): the ratchet's detector implements the rule its header states (an edge name under any of ten names, initialised as a class field with any modifier anywhere on a line, plain, definite, optional or declared without an initializer, a constructor parameter property, an assignment through any name, or an object-literal key with null, undefined, [] or an identifier; a file importing nodeFactory or hideEdges from the shared module counted as switched, until round 3 required the call), with a positive and a negative scratch source per shape in its own tests; it found four more files (codex-meta-choices, file-view, pdf-new-tab, track-decorations), allowlisted at 141 under a ceiling constant lowered as files migrate (round 3 pins the length by equality). The shim's own write after construction (_sel) is defined non-enumerable, and the header says what the rule hides and when: at creation, so what product code hangs on a node later enumerates, with the projection pins and the cgroup cap as the backstop. The where line was recounted from upstream/main as fetched (9109afac).

2026-09-10, review round 3 (fork PR 523): one rule for every file under ui/. A test file that initialises an edge-named property in any shape the detector reads calls hideEdges( or nodeFactory( imported from the shared module (the import alone is not the credential, and either quote style on the specifier is read) or is on the allowlist, whose length ALLOWLIST_MAX pins by equality: a renamed file replaces its entry, a file that comes off lowers the constant in the same commit, a new file may not join. The DOM-vocabulary condition is gone, so a window stand-in's parent and a goal fixture's children count like a class's parentNode. The detector reads a capitalised identifier and a this.-rooted value; in the declared-only field form it leaves host out with the payload lines (a value that starts with a quote, a digit or a boolean, or carries a parenthesis); and the text a regex cannot tell from an edge init (a destructuring default, a type literal's member, a source pin quoting a typed parameter) is pinned as such, with a case per shape. Nine more files are listed, 150 in all, for PR 569 to migrate: card-subgoals, feed-absorb, perf-telemetry, setting-stale, shell-perf, tab-snapshot-view, thread-selection-scope, timeline-rehover and track-decorations-hover-cost. CONTRIBUTING.md names V8's 256 MB floor on the old generation and the flag behind its 4 GB step.

2026-09-10, review round 4 (fork PR 523): the detector and the credential read code only. A light lexer blanks every line and block comment and the contents of every string, template and regex literal to spaces of the same length (delimiters and newlines kept; a template's holes stay code; a slash opens a regex only where no operand precedes it, and one that does not close on its line is a division) before either regex runs, so a source pin quoting a shape, a shape in a comment and a hideEdges( token in a comment or a string count for nothing; feed-absorb and setting-stale, listed on quoted product text alone with no object to hide, came off (148), and a positive and a negative case per literal kind pin the lexer. The credential admits a namespace import (shim.hideEdges( under the import's own alias) and a .js-suffixed specifier. parentElement is the eleventh edge name, an object-literal accessor (get EDGE() {, the incident's own shape) is the sixth shape, a declared field's parenthesised type is read (childNodes!: (Elm | Txt | string)[];), and a brace after (, =, a comma, let, const or var opens no class body, so the one-line destructuring defaults and the one-line semicolon type literal no longer trip; what the detector still does not tell from a shape (a wrapped destructuring default, a type literal's member on its own line or comma-separated, a typed parameter after a comma in code, a local of an edge name reassigned at line start or after a semicolon, a class whose first member is an edge getter) is pinned under the shape it reads as, and non-empty array literals, Object.defineProperty and the shorthand key are named out of scope. The ratchet's first message gives a newcomer three paths: a fake DOM calls nodeFactory( or hideEdges(this) at the end of its constructor; a non-DOM use of an edge name is renamed or carried on the documented non-DOM list (which lands with PR 569); the allowlist itself is closed. ui/timeline-transform-tick.test.ts wraps its hover target, the one object literal with an enumerable parentNode among the switched files, in hideEdges and pins its projection. Rebased onto the fork's main (5917393e) as the last step and listed the eight class fakes main added between the base and the landing (actions, file-comments-markclick, file-comments-markclick-controls, file-comments-seen-fixes, file-comments-seen-review2, file-comments-seen-review3, file-comments-send-seen, file-view-notice), which predate the rule: 156 in all, for PR 569 to migrate.

2026-09-10, review round 5 (fork PR 523): the lexer's operand test reads the blanked text, so a comment before a line-opening regex is no operand (a regex pin on the line after a trailing comment, the pin-array layout in at-bottom, was read as a division and its closing slash blanked code to the next slash) and a slash after a postfix ++ or -- is a division; a regex right after a condition's closing paren stays a division to the lexer, pinned as indistinguishable, with the false negative a quote or a backtick inside it causes named and pinned as accepted (none in the tree: the lexer agrees with a TypeScript-AST blanking on all 764 UI test files). The namespace credential escapes the alias and anchors it with a lookbehind (an alias with a dollar sign reads; a longer name ending in the alias does not), and its import half requires a statement in code (an import line quoted inside a template or a backslash-continued string is not one). A semicolon inside a one-line brace group opened after (, =, a comma, let, const or var anchors no class member, so a one-line type literal's edge member after another member is told apart (a one-line interface or annotation still reads as a class field, pinned). The detected set is unchanged: 156 listed, 17 switched.

2026-09-10, review round 6 (fork PR 523), the last verify and the final rebase: the value grammar reads a conditional by its final operand after its last colon (the head and the middle operand free of parentheses), so a window stand-in whose parent is `opts.framed ? {} : win` is detected and federation-hidden-hold is listed; the lexer reads a slash after a closing quote, double quote or backtick as a division (a string or template literal is an operand), with a division after a closing brace pinned as the second accepted misread; the docstrings state the class-field anchors (a line start, a member semicolon, the class brace), the primitive skip on the declared type first token, the ASCII alias and the member-path call forms, the spread and the logical value as out of scope, and the fourteen tree files whose only edge writes are out-of-scope shapes are recorded in the PR body. The rebase onto main (c98759de) listed seven class fakes main added (file-comments-about and file-comments-resolve-answered, with their fixes and review2 siblings, and file-comments-arrivals-about); ALLOWLIST_MAX 164, 164 of 773 files detected, 17 switched, the lexer agreeing with the TypeScript-AST blanking on 773 of 773.

2026-09-10, follow-up landed (fork PR 569, entry 2026-09-10-shim-migration): every one of the 167 files the allowlist named at round 4 and at the final rebase (156 ui/webview files at round 4, then the eleven main and the fold added before the landing: the seven file-comments-about and file-comments-resolve-answered class fakes, tab-hide, preview-retry-pace, federation-hidden-hold and ui/timeline-tag-chips, the last on nodeFactory outright and in SWITCHED) either calls the shared module or is a documented non-DOM use of an edge-named key. The 141 from rounds 1 and 2, five of round 3's nine (perf-telemetry's and shell-perf's window stand-ins, thread-selection-scope's element literals, timeline-rehover's node literals and track-decorations-hover-cost's El) and the eight of round 4 (actions, file-comments-markclick, file-comments-markclick-controls, file-comments-seen-fixes, file-comments-seen-review2, file-comments-seen-review3, file-comments-send-seen, file-view-notice) hide their edges through hideEdges (a non-enumerable define of parentNode, parent, childNodes or ownerDocument in each node constructor and hideEdges at its end; on a window stand-in or a literal, hideEdges on the object; the file-comments and file-view Ev classes end in hideEdges too, so an event's target and currentTarget hide with the nodes), each with a projection test, which in the 67 files with an Ev class pins the event as well through assertHiddenEvent. Two of round 3's nine initialise no DOM edge (card-subgoals' goal fixture has a children array of ids, tab-snapshot-view's list model holds plain rows with no edge back) and are named in NON_DOM_EDGES, a second list pinned by its own constant with a one-line reason and the count of edge-initialising lines per entry (one each); the other two, feed-absorb and setting-stale, came off at round 4, whose lexer blanks the product text their source pins quote; the allowlist of unmigrated fakes is empty with its ceiling at 0, and a further pin requires every ui/webview file that calls hideEdges( to be in the detector's view. The same PR gives the shim a per-node serial (_nid, stamped by hideEdges) so two distinct nodes are never deepEqual, sameNodes for node-list assertions and assertHiddenEvent for the event pins; all live in ui/test-dom-shim.ts, so a port of this entry that lands first ships the shim without them and PR 569's port adds them. Port note for the ratchet: both lists are computed from upstream's tree, never carried over. By round 4's detector over upstream/main as fetched 2026-09-10 (f83a2ea6), 42 test files there initialise an edge-named property in code: the sixteen shims this entry ports, the twenty webview files PR 569's entry ports (see it), one file PR 569's entry lists as a non-DOM use (card-subgoals), and five files whose upstream copies alone fake edges (four of them exist on the fork as source-text pins with no fake DOM: code-block, reload-notices and staged-list-cap are the fork's own files, so a fold meets each as an add/add conflict, and skeleton-tabs-wiring is upstream's file as the fold brought it at 459e50d6, before upstream's copy grew its ChipEl fake, so a fold meets it as a modify/modify; md-img-park is absent from the fork; preview-retry-pace was a sixth until the fold brought it to the fork and PR 569 migrated it, so it is among the twenty), which this offer leaves on the ported allowlist.
