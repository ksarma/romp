---
title: Tags-scale test: shim nodes inspect as a projection, so a failing assertion cannot allocate a 100 GB diff; the UI test runner caps the heap
status: candidate
where: ui/test-dom-shim.ts (the shared fake DOM: nodeFactory, and hideEdges, the structural projection rule), ui/test-dom-shim.test.ts (the rule pinned, and a ratchet over ui/**/*.test.ts with a named allowlist of the remaining enumerable-edge shims), ui/timeline-tags-scale.test.ts (on the shared shim; the projection pin over the live dialog at scale plus a node of upstream's variant shape; the runner-cap pin), the fourteen other ui/timeline-*.test.ts and ui/webview/tab-color-picker.test.ts (switched from their near-copies of the shim to the shared module), CONTRIBUTING.md (the runner cap, its derivation and its limit), vscode-extension/package.json (the test script passes --max-old-space-size=2048). Upstream: the tags-scale test is on their main since their PR #1198 (merged 2026-09-09 23:11Z), with an adopt()/ownerDocument shim and a beforeEach the fork does not carry yet, and their fourteen sibling copies match the fork's; their test script has no --test-concurrency, so the cap lands there as node esbuild.js --tests && node --max-old-space-size=2048 --test 'out-tests/**/*.test.js'. The offer is a PORT: the shared module and the structural rule onto their fifteen copies (their adopt()/ownerDocument members move into the module, where the rule hides them), the runner cap in their runner's shape
added: 2026-09-09
pr: 523
tier: docs
offered:
closed:
---
Root cause: assert/strict equal and deepEqual build a diff on failure even with a message, inspecting both sides at depth 1000 with getters on; the shim node enumerable parentNode and firstChild getter made that dump the whole dialog (2^depth), and node Myers diff then clones an Int32Array of 2(N+M)+1 per edit-distance level, 8N^2 bytes for a 71k-line dump against null, outside the V8 heap. Reproduced under a cgroup cap at 1.2 GB/s. Fix: the shim edges, listener tables and methods are non-enumerable; a node dumps in about 25 lines.

2026-09-10, review round 1 (fork PR 523): the edge hiding is STRUCTURAL and lives in one shared module, ui/test-dom-shim.ts, which replaced the fifteen near-copies of the shim (fourteen ui/timeline-*.test.ts siblings and ui/webview/tab-color-picker.test.ts): after construction every own property holding an object, an array, a function or null, and every accessor, is non-enumerable, and primitives stay. A key list had missed upstream's variant of the shim (their #1198 gives every node an enumerable _ownerDoc and an ownerDocument accessor; the projection pin failed there at 127 lines per dot); the rule covers it, and the pin now includes a node of that shape. Follow-up, named in ui/test-dom-shim.test.ts's allowlist: 137 ui/webview test files with class-based fake DOMs (El and Txt, FakeNode, E, FakeEl, Elm) still carry enumerable parentNode or childNodes fields; the fix is a non-enumerable define in their constructors, or hideEdges from the shared module, plus a projection test; until then a card in feed-keynav-covered dumps as 318,835 lines. Port notes: their test file's two conflicts are the header comment beside their adopt() comment and the new test's placement next to their beforeEach; their adopt() walks children and sets _ownerDoc, so the shared module there carries the ownerDocument members (hidden by the rule) and its appendChild and insertBefore adopt as theirs do; the fork's copy picks up their shim members at the next fold (upfold #479 does not reach 13443256).
