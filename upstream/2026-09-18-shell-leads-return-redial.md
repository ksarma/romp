---
title: The shell leads the visible pane's return redial
status: candidate
where: kernel/kernel.py: _shim (parentLink, awaitLink, the link-up dial with linkUpMs, the panes message listener, the 25 s link-backstop, the returnDiag linkUpMs hook, the onclose re-await); _LANDING_MOBILE_JS shellWS (the shell socket gains the shim's liveness rules: one live attempt, lastRecv and freeze/resume stamps, a 5 s watchdog, the refused ladder and the 15 s connect cut, the return-probe row, window.__rompLink); _LANDING_COLLAPSE_JS panesMsg (the link field) and broadcastAll (the link word to every other shim-bearing iframe, with load hooks for the settings frame and a split column); ui/webview/render.ts (the chat frame handler returns on the shell's link word, before the preview heal); docs/read-side.md (the pane-shim rows paragraph: awaitLink on the return row, linkUpMs on the return-fresh, the link-backstop stale row, the return-probe row, its -1 sentinels and the two returns that file none, the shell's connT as its loop-alive stamp; and the amended resume sentence: a socket already 30 s overdue when the tab froze is redialed at the return, at once when the shell's link is up and else at the link-up word, and a browser without resume behaves as before); tests: tests/test_pane_shim_return.py ShellLedReturn (the await, the link-up word and the link word, the in-window cadences, the backstop bracket and its refused cadence), tests/test_kernel_mobile.py ShellLinkProbe (the shell socket under node: one live attempt, the ladder and its probe integers, one pending redial timer, the watchdog arms with the freeze and resume stamps, the abandon's API health hook, the loop-alive stamp's two halves with one pane's shim following the shell's real publication), tests/test_kernel_ws_heartbeat.py ShellLivenessMatchesTheShim (the two copies agree on the constants and the tick; the backstop bound derived from the shell's constants), tests/test_client_diag_allowlist.py (the awaitLink, linkUpMs and return-probe rows), tests/test_api_health_rail.py (the shell onclose and abandon tell the detail, then re-tell the panes), tests/test_files_pane.py (the panes word's link field), tests/test_pane_state_broadcast.py (the panes word with link; the link reaches every iframe), ui/webview/pane-shim-stale.test.ts (the harness runs every 5 s interval the shim arms; the gate from both sides: a standalone page keeps today's path, a function publication leads the return, a publication that is not a function is no shell), ui/webview/render-link-word.test.ts (the lifted chat frame handler over stubs: the link word and the panes word heal nothing, hostUp still heals once, the branch sits before the heal at source)
added: 2026-09-18
pr:
tier: fix
offered:
closed:
---
D3 of the phone reconnect plan. On the phone a return from the background found every pane socket dead and thirteen independent reconnect machines dialed a down path at once; the refused regime was a dial storm (236 dials per pane in 30 s, harness baseline). The shell socket becomes the page's one probe with the shim's liveness rules (one attempt in flight); it files one shell clientDiag return-probe row per return and publishes the page's link synchronously (window.__rompLink) and as a word re-told on its open, close and abandon: the link field of the panes word for the pane frames, a link word of its own for every other shim-bearing iframe (the settings frame, a split chat column). One redial timer pends at a time (a dial clears it), and an abandon tells the API health detail as a browser-reported close does. A pane returning with a dead socket puts it down for every socket state (the watchdog tick inert), files its return row, and dials at once when the link is up at the decision or on the link-up word otherwise (awaitLink), recording linkUpMs in its return-fresh; a loud link-backstop dials anyway when the shell's connT, its loop-alive stamp (the later of its last dial and its watchdog's last tick with a socket to watch), is 25 s stale. Independent of PR 3 (hidden panes park). No shared fragment inlined into the shim: the shell keeps its own copy of the liveness rules, pinned to agree with the shim by one test. Touches CLIENT_DIAG_KEYS (the shell's return-probe keys and the pane shim's awaitLink and linkUpMs). Stacked on PR 765 (relay redials wait for the local socket), itself on PR 762 (the beacon extension). Offer waits on the user's word.
