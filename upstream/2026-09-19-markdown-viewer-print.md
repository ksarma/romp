---
title: Markdown viewer follow-on: Print from the viewer with its pictures (a Print glyph button in the bar and Ctrl/Cmd+P in front of window.print; over placeholders for hosts outside the gear's list the bar arms and fetches from them only on Print with them, one restore per placeholder that reaches the paper and no grant for the page, where a click keeps its host-wide, page-life grant; every picture that reaches the paper awaited with one probe per URL per press and a lazy picture set eager, an ask at the 8 s deadline instead of a silent print and never a print on a repaint under the ask; a picture opened directly fitted to the page; a PDF printing itself through its frame or the /file tab; Print disabled while the loader, the plain fallback editor, a failure pane or a root the flow's closed lists do not name holds the body, a PDF's pages loader excepted, read off the body itself, by aria-disabled and never the property)
status: candidate
where: fork branch `filereview-print` (fork PR #853), merged with the fork's main 3110baf62 (fork PR #849), the review's base: ui/webview/file-print.ts (new: the machine `step` over PrintState and PrintEvent, the words and the titles, `isPrintKeys`, `isPrintChord`, `ownsEscape` with OWN_ESCAPE_SEL and OPEN_POPUP_SEL, the printable rule `printable`, `rendered`, `figureHidden` and `figurePrintable` over `offPaper`, `paintsOf` and `shows` (the browser's computed opacity and visibility, the author's display and the HTML hiding attributes, read over every painting element of the figure), `collectPictures` taking the probe factory, setting a lazy img eager and filtering an svg <image> by the container walk (`inRenderingSvg` over `svgContainerRenders`, the round-7 fixes: Firefox and WebKit report a client rect for an image inside a container that never renders, which the rect alone counted there), `settlePictures`, `pdfFrameWindow`, `bodyReady` with `rootKind` over the closed lists READY_ROOTS, NOT_READY_ROOTS and LINE_ROOTS and the PDF kind's PDF_LOADER_ROOT, and the DOM driver `installFilePrint` with the per-press probe Map and `dropDone` before a repaint's re-aim, the ask written into the wait's row, the observer of the body where the document has one, the keyboard hand-over through the host's takeKeyboard, and the deadline seam `setPrintSettleMs`/`printSettleMs`), ui/webview/figure-gate.ts (`loadGatedFigure`, the restore of one placeholder that grants its host nothing for the page; `loadGatedHost`, the click's road, unchanged), ui/webview/file-view.ts (openFileView installs the flow after Download with the file's kind, the /file tab opener and takeKeyboard; openUrlView installs it before Copy URL; `typingHere` beside isTypingTarget; no paint reports the body, the flow reads it), ui/webview/icons.ts (ICON_PRINT), ui/webview/styles.css and ui/webview/feed.css (in the print block, after the `.fileview-body` line, the `.fileview-imgbox` and `.fileview-img` rules; `.fileview-print-line .fileview-load`; byte-equal), ui/webview/real-viewer-leg.ts (the browser legs' bundle exports the seam; `inBrowser` takes the engine, Chromium by default, since the round-6 fixes), docs/guide.md (the printing sentence of the Opening a markdown document paragraph); tests upstream can take: ui/webview/file-print.test.ts (new), file-print-browser.test.ts (new), file-print-media-browser.test.ts (new), file-print-driver-browser.test.ts (new), file-print-armed-browser.test.ts (new), file-view-print-takings-browser.test.ts (new), file-print-egress-browser.test.ts (new: the requests leaving the page counted per road under a Chromium intercept, per host and per placeholder, with the page-life grant read, a 302 off a granted host counted on the print's road and the click's, and three figures whose own URLs are never asked, one of them on a host asked for a figure that prints), file-print-figure-browser.test.ts (new: the figure half of the printable rule held to the ink every gated shape's ungated twin puts on the page, in Chromium, Firefox and WebKit, each engine held to its own ink where they differ, with the twin oracle's and the flow's readings recorded on the row where they differ from the ink, since the round-7 fixes, and a collectPictures case per engine; the shape count is pinned in the plan's section, derived there from the rows the leg builds, and not restated here; and the restore-and-fetch oracle: every placeholder whose figure paints restored as Print with them restores it, and the URLs the page then asks for held per shape, per engine where the engines differ), figure-gate.test.ts (the one-placeholder restore's case) and fileview-parity.test.ts (the wait line's loader rule held byte-equal); FORK-ONLY, reading fork-only surfaces, so an offer adapts rather than copies: plans/markdown-viewer.md (the section "Follow-on: Print (2026-09-19)", decisions P1 to P7, its derivations paragraph, its Tests list and its open points), tools/markdown-viewer-plan-print.test.mjs, tools/markdown-viewer-plan-print-record.test.mjs and tools/markdown-viewer-plan-print-vocabulary.test.mjs (the section held to the tree), tests/test_guide_print_palette_chord.py (the guide's palette clause held to the palette's binding and the flow's stand-down), and this entry; 24 files in all by `git diff --name-only 3110baf62 HEAD` run at the round-6 commit that re-derived this line (the shape-count commit, after the round-5 review), this entry among the 24 (the count was 20 at the entry's first commit, with the egress leg named ahead of its own commit as the 21st; the round-2 fixes added ui/webview/figure-gate.ts and ui/webview/figure-gate.test.ts, 23; the round-4 fixes added ui/webview/file-print-figure-browser.test.ts, 24; the round-6 and round-7 fixes added none)
added: 2026-09-19
pr: 853
tier: feature
offered:
closed:
---
Upstream ships the same viewer, the same print sheet, the same figure gate and the same guide, so the capability applies there once the viewer slices before it have landed: a Print button and Ctrl/Cmd+P in front of window.print; a privacy choice before any fetch from a host outside the list, one restore per placeholder that reaches the paper and no grant for the page (a click keeps its host-wide, page-life grant); every picture that reaches the paper awaited, one probe per URL per press, a lazy picture set eager, an ask at the deadline instead of a silent print, and never a print on a repaint under the ask; a picture opened directly fitted to the page; a PDF printed through its frame or the /file tab; and the button disabled while the loader, the plain fallback editor, a failure pane or a root the flow's closed lists do not name holds the body, a PDF's pages loader excepted, read off the body itself, by aria-disabled and never the property. A privacy surface: it lands on the fork on the owner's word whatever its tier, and whether it is offered is the owner's call too. The fork-only half (the plan section with its pins, the guide's Python pin) stays here.
