---
title: Markdown viewer follow-on: Print from the viewer with its pictures (a Print glyph button in the bar and Ctrl/Cmd+P in front of window.print; over placeholders for hosts outside the gear's list the bar arms and fetches from them only on Print with them, and only for a placeholder that reaches the paper; every picture awaited with one probe per URL per press and a lazy picture set eager, an ask at the 8 s deadline instead of a silent print; a picture opened directly fitted to the page; a PDF printing itself through its frame or the /file tab; Print disabled while the loader, the plain fallback editor or a failure pane holds the body, read off the body itself, by aria-disabled and never the property)
status: candidate
where: fork branch `filereview-print` (fork PR #853), merged with the fork's main 3110baf62 (fork PR #849), the review's base: ui/webview/file-print.ts (new: the machine `step` over PrintState and PrintEvent, the words and the titles, `isPrintKeys`, `isPrintChord`, `ownsEscape` with OWN_ESCAPE_SEL and OPEN_POPUP_SEL, `printable`, `collectPictures` taking the probe factory and setting a lazy img eager, `settlePictures`, `pdfFrameWindow`, `bodyReady`, and the DOM driver `installFilePrint` with the per-press probe Map, the observer of the body, the keyboard hand-over through the host's takeKeyboard, and the deadline seam `setPrintSettleMs`/`printSettleMs`), ui/webview/file-view.ts (openFileView installs the flow after Download with the file's kind, the /file tab opener and takeKeyboard; openUrlView installs it before Copy URL; `typingHere` beside isTypingTarget; no paint reports the body, the flow reads it), ui/webview/icons.ts (ICON_PRINT), ui/webview/styles.css and ui/webview/feed.css (in the print block, after the `.fileview-body` line, the `.fileview-imgbox` and `.fileview-img` rules; `.fileview-print-line .fileview-load`; byte-equal), ui/webview/real-viewer-leg.ts (the browser legs' bundle exports the seam), docs/guide.md (the printing sentence of the Opening a markdown document paragraph); tests upstream can take: ui/webview/file-print.test.ts (new), file-print-browser.test.ts (new), file-print-media-browser.test.ts (new), file-print-driver-browser.test.ts (new), file-print-armed-browser.test.ts (new), file-view-print-takings-browser.test.ts (new), file-print-egress-browser.test.ts (new: the requests leaving the page counted per road under a Chromium intercept) and fileview-parity.test.ts (the wait line's loader rule held byte-equal); FORK-ONLY, reading fork-only surfaces, so an offer adapts rather than copies: plans/markdown-viewer.md (the section "Follow-on: Print (2026-09-19)", decisions P1 to P7, its Tests list and its open points), tools/markdown-viewer-plan-print.test.mjs, tools/markdown-viewer-plan-print-record.test.mjs and tools/markdown-viewer-plan-print-vocabulary.test.mjs (the section held to the tree), tests/test_guide_print_palette_chord.py (the guide's palette clause held to the palette's binding and the flow's stand-down), and this entry; 20 files in all by `git diff --name-only 3110baf62 HEAD` run at the records commit that adds this entry, this entry among the 20; ui/webview/file-print-egress-browser.test.ts is named here ahead of its own commit, which the round's egress work makes beside this one on the branch, so a run at a head that carries it lists 21
added: 2026-09-19
pr: 853
tier: feature
offered:
closed:
---
Upstream ships the same viewer, the same print sheet, the same figure gate and the same guide, so the capability applies there once the viewer slices before it have landed: a Print button and Ctrl/Cmd+P in front of window.print; a privacy choice before any fetch from a host outside the list, and only for a placeholder that reaches the paper; every picture awaited, one probe per URL per press, a lazy picture set eager, and an ask at the deadline instead of a silent print; a picture opened directly fitted to the page; a PDF printed through its frame or the /file tab; and the button disabled while the loader, the plain fallback editor or a failure pane holds the body, read off the body itself, by aria-disabled and never the property. A privacy surface: it lands on the fork on the owner's word whatever its tier, and whether it is offered is the owner's call too. The fork-only half (the plan section with its pins, the guide's Python pin) stays here.
