---
title: The setSessionFlag socket op refuses a flag name outside the lane toggles, as POST /flag does, through one shared predicate, so a dashboard client can no longer set threadMail or the legacy postalOff on a session through either flag door
status: candidate
where: fork branch `quickfix-ws-flag-whitelist`: `kernel/kernel.py` (`_refuse_setting`, `_lane_flag_refusal`, `_state_write_route`, `Handler._dispatch_ws`), `tests/test_obsidian_state_routes.py` (`_client_frame`, `_ping_frame`, `_read_frame`, `SocketFlagWhitelist`, `_KERNEL_DIR`, `_kernel_modules`, `_PARSES`, `_parse_kernel_module`, `_module_qual`, `_short`, `_source_functions`, `_callee`, `_DOTTED`, `_text_of`, `_mentioned`, `_mentions`, `_door_names`, `_door_walk`, `_doors_reaching`, `_doors_naming`, `_STORE_NEEDLE`, `_store_seeds`, `_NESTED`, `_holds`, `_carries`, `_root_name`, `_call_spelling`, `_callee_spelling`, `_PATH_EXIT`, `_PATH_STORED`, `_PATH_KEYED`, `_PATH_STOPS`, `_READ_BINDINGS`, `_NOT_BINDING`, `_BODIES`, `_import_binding`, `_merge_bindings`, `_scope_bindings`, `_module_bindings`, `_misbound`, `_module_of`, `_split`, `_bindings`, `_bound_names`, `_path_valued`, `_store_flow`, `_unwrap_str`, `_LOADERS`, `_NO_CHILD`, `_CHILD_FIELDS`, `_Facts`, `_WALKS`, `_facts`, `_unit_facts`, `_CENSUS_BUILDS`, `_Census`, `_flag_census`, `_FROZEN_BEFORE`, `_BUILT_BEFORE`, `_CENSUS_REF`, `setUpModule`, `tearDownModule`, `_unit_names_the_store`, `_derive_namers`, `_derive_flag_census`, `_derive_roads`, `FlagWriterPopulation`, `StoreFlowReach`), upstream/2026-09-23-ws-flag-whitelist.md (this entry)
added: 2026-09-23
pr: 909
tier: fix
offered:
closed:
---
The setSessionFlag socket op wrote any flag name an authenticated dashboard client sent, while POST /flag refused a name outside `_LANE_FLAGS` with a 400. So a client could set `threadMail` on a comment thread, the key that turns its mail on (fork PR #897 discloses what follows on the fork: the deadness mirror's roster then omits comment-thread sids that relay), or the legacy `postalOff`, which the kernel and the postal bus read as isolation. Upstream main carries the same arm and reads the same keys. Both flag doors now ask one predicate, `_lane_flag_refusal`, over the one list: the socket answers an unlisted name on the settingRefused frame with the route's sentence inside and `value` null, and writes nothing. It asks for every name a frame carries, a falsy one (null, "", 0, false, [], {}) included, since the arm keys on the flag key's presence; a frame with no flag key draws the terminal arm's unknownOp (the kernel's answer for a known op missing a field its arm requires), where POST /flag answers its 400, and neither writes. The saveFile socket op, under the file-editing consent, can still write session-flags.json whole, as it can any text file; it is not a flag door, and that consent already grants the write. No product surface sends a name outside the list (the lane gear's LANE_TOGGLES, the tab menu's typed union, the Obsidian panel's POST). Tests: the refusal executed through the real dispatcher and over a real socket to the served Handler, red before the fix (the store gained threadMail), the falsy names among the refused ones and the no-key frame a witness; the doors that write a session flag derived from the source of every module in the kernel's directory (which holds every module the kernel loads, pinned) and pinned as a set, an arm counting as a door when it mentions a setter at all (called by name, bound to a local, imported under an alias, picked from a table, named by a string), each asking the predicate, returning on its answer before any setter runs, handing `_set_session_flag` as its flag argument the expression it asked about, with nothing rebinding that expression's root name in between, and calling each setter with exactly its pinned parameters (`(sid, flag, value)`, `(sid, value)`); those source pins read only the names the source spells, so an alias of the frame, a name built from pieces, a star import and the setters' bodies are outside them, which the test module states, and the arm's behaviour is left to the executed tests; every road from a request to a setter (the route's POST arm, the socket's receive loop) derived upward from the setters and pinned, so a new way into either door function reds; the setters are found from every function in those modules that names the flags store in code (a str or bytes literal) or mentions a helper returning its path (a call, a reference bound to a local, an import of it), pinned by role (reader, writer, path helper), so a writer counts however its write is spelled, a read counting as one only where its name is bound as the census read it (a nested `def sorted`, `str = lambda ...`, `Path = shutil.copyfile`, or a module binding a read's name otherwise, makes the call a write), the path reaching it through an assignment, a parameter's default, such a helper or a def, lambda or class nested in it (each a value holding what it mentions, followed wherever it goes, its decorators, bases and metaclass included), a callable a call runs (a lambda, or a name the function binds only to undecorated defs or plain `name = lambda ...` assignments of its own scope, handed to a call is read with its parameters bound from the call's other arguments; any other keyword value handed to `sorted` or `json.loads` beside the path but a constant is refused as a call outside the reads unless it is a read itself, as `key=str` is, and a function of the module handed by name is refused too, even where a scope nested in the function binds that name to a def or lambda of its own, since the census does not read another function's body from a call, and so is a decorated def's name, which its decorator binds to what it returns), and whichever loaded module it lives in, where a module-level class's decorators, bases and keywords count as that module's code; what a function hands back counts as the path unless it passes through a read of the file or a fact about it (the rule lists where the path stops, not the ways it goes through), and a function that keeps the path, or an object that holds it, anywhere but a plain local name (a subscript or attribute, `self` included, a global, an `.append`, or a decorator or a keyword of `sorted` or `json.loads` that is one) is refused rather than traced. A path a function did not spell is outside those pins: a file name a client sent, a key of the readers' cache, what a call the census classes by reading it keeps, or a name or path read back by reflection (a frame's locals, a function's defaults or code, a docstring); a callback whose code the function swaps by reflection (`poke.__code__ = ...`, or setattr) is read as its own body, not the code the call runs, so a write there is outside them; and a method among the reads (`.get`, `.stat`, `.read_text`, `.append`, `.items`) is read by its name alone, so another object's `.get` that writes the file, or returns its key and so hides the path, is outside them too; and the census reads calls, so an expression that runs a method of another object with the path (an operator or comparison such as `p in REG`, a subscript `REG[p]`, an f-string's format spec, a match, or a comparison, conversion or type test a read makes) runs that object's method, and a write there is outside them as well, which the test module states with its witnesses. Porting the tests: SocketFlagWhitelist's end-to-end case needs `km.READY_GATE_CAP`, the fork's kernel ready hold, which no offer carries; and FlagWriterPopulation's LOADED set, with the NAMERS roles and the module figures, is derived from the fork's kernel/ and is derived again against upstream's tree (upstream's kernel/ also holds gc_freeze.py). The module imports no test helper upstream main lacks: its census parses the kernel's files itself and does not use tests/parse_cache.py, which upstream main lacks and only the parked-ops-liveness-boot-hold candidate carries.
