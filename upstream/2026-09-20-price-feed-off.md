---
title: The price feed has an off switch, and the cost view and /version say whether prices are the live feed or the built-in defaults
status: candidate
where: upstream-facing: kernel/kernel.py (_price_feed with its unrecognisedSaid, overrideFileSaid and overrideRowsSaid latches, its flights counter that mints the flight token and inflight as that token (0 when no flight is in the air; the dead rows field removed), _price_feed_lock, _price_feed_off, _price_feed_unrecognised, _price_feed_first (the member variant for the row latch), _price_feed_clip, _price_feed_error_class, _price_feed_verify_errors, _price_feed_status with the unrecognised, overrideFault and overrideRowsRejected keys, _price_feed_line (the day arm of the stderr tail's age), _PRICE_OVERRIDE_FAULT_LINES (the row head names three classes), _PRICE_RATE_STRING and _price_rate_value (the B re-ruling: the one strict read of a rate both parses share, a JSON number or a quoted plain decimal whole, held finite, its docstring carrying the premise's answer), _PriceTable (fault and rejected), _model_prices (the fault classes, the per-row skip and its count, the read of an override rate through _price_rate_value (a present key accepted as a JSON number or as a string in the strict decimal form, any other string rejected, an absent key inheriting the table's rate), the say-once fault lines: one latch per class, one entry per row), _refresh_remote_prices (the unrecognised arm, the TTL check and its stamp under _price_feed_lock with the attempt's fields as one transition, the per-flight token and its three clears under the lock, every feed rate read through _price_rate_value; round 3: the worker's local helper cache_rate, through which the two optional cache keys inherit the input rate when absent, null or a numeric zero and go through _price_rate_value for every other present value, and the docstring's substitution clause), _version_info priceFeed, _ANALYTICS_MEMO (the entry's prices), _ANALYTICS_REPRICING, _token_analytics both roads (the memo road's table check, the store after the block, the no-block serve); since the fifth round Handler._send, whose comment on its Referrer-Policy write names the one exception the Network access section states: an inline svg's paint reference, on a page whose own address carries ?token=), ui/webview/gear.js (raPriceNote with the unrecognised, skipped-rows and whole-file clauses, and since the fifth round the in-flight clause worded to stay true after the fetch lands; raAgo the binding of api-health-merge.ts agoWords at the top of the file, the local function deleted; raPriceLine, raRender and raFetch inside initGear, module.exports), ui/webview/gear.css (.ra-price), docs/reference.md (The price feed subsection: the unrecognised clause and the override count said by position, the not-a-number predicate, the stderr sentence conditioned on the cost view's road with the block carried whether or not the view was opened, the whole-file clause quoted in the last slot; the analytics modal paragraph), SECURITY.md (Network access, the whole section: the trigger keyed on the last attempt with the stamp-before-fetch sentence; the derivation passage naming scripts/network-inventory.py, scripts/network-inventory-expected.json, what the run refuses, tests/test_price_feed_census.py and the four counted classes the scan cannot derive, since round 3 widened to programs a shell script of romp's, the manager or the editor extension start and to a dynamic import of a computed module URL, with the fifth class named and not counted in the script's own sentence and the disclosure sentence that the shell and browser sides are matched by a named list with no completeness gate, NET and SUB glossed; the release and drift checks with their cadences and the drift verdict's three disjuncts; the browser's pictures road in the by-default group with the gear's shipped list; the set-up frame worded to the actor, the PR watch's two gh calls, the watch predicate's own item with the route, the shell, the cadence floor and default, the bound and the boot re-arm, the editor extension's update prompt, since round 3 naming vscode-extension/install.sh (npm install and npx) on the click and what it reaches (the npm registry, not PyPI), the release clause's two scripts (bin/romp-sdk-setup, vscode-extension/install.sh) and the condition sentence for vscode-extension/install.sh (npm install on every run; npx for vsce only with an editor CLI present or ROMP_EXT_PACKAGE_ONLY set, the script exiting after the build otherwise), and before the fourth round the clicked-link sentences in the set-up paragraph (a link you click, in the browser showing the dashboard or in one of the editor extension's views, what each kind of link carries and to which host, the sentence that romp adds no credential to a link's URL and that nothing sends until you click, and the road's residual in the script's words; since the fourth round the walk sentence naming the pages the kernel serves, the served-pages sentence in the script's words beside the walk's claim (the routes read from kernel.py's syntax tree, the SERVED gate, a file the page reads covered by the walk and a stylesheet named, not scanned), the echo rule right after the disclosure sentence, the binding sentence for the browser and editor side's connection family through its bindings (the docstring's clause), the chat-media sentence in the by-default group (a rendered message's media on the web dashboard, whatever cookies that browser sends to that host and no Referer to any other origin, the editor webviews' content security policy), and in the set-up paragraph the clicked-link trigger list naming the file viewer's GitHub button with the sent clause for the address romp composes on github.com, the sign-in link's opener per document, and the residual's three anchors with the sign-in link as the witness of its class, one text with the script's CLICK_RESIDUAL); bootstrap.pypa.io and ROMP_NO_GET_PIP; the census-lists sentence; the telemetry sentence scoped to the four kernel-request rows; the closing sentence's three destinations; since the fifth round the trust model's Referrer-Policy sentence with its one exception (an inline svg's paint reference on a page whose own address carries `?token=`), the served-pages sentence in the script's words (every `_send` call and every Content-Type header written outside `_send` a route candidate, the type read through the definition it reaches with FRAME_WRITERS, SCRIPT_TYPES, the receivers and containers the pass reads, SERVED_ALLOW with its place counts and its gates, every `fetch(` and `import(` read per match with no comment skip), the three binding shapes no pattern reads right after the disclosure sentence, the binding sentence narrowed to the shapes the patterns read, the sentence on the two kinds of browser request named in the table and not counted, the sentence naming every other text the chat renders as markdown, the paint sentence on three surfaces (the chat's rendered markdown, the chat's file preview and a notice card) with the paint clause in place of the fourth round's fill, mask and filter clause, and the .svg tab paragraph after the set-up paragraph), tests/test_price_feed_off.py (PriceFeedCase with _settled, OffSwitch (the first-statement pin re-anchored to the With block over the lock), SayOnceLatches (round 3: _race's holder-keyed Shim on km._price_feed_lock, two event edges, the park's return asserted before the liveness check; after the second merge of main, _race takes its product-function target from its callers test_two_attempts_under_off_arriving_together_write_the_off_line_once, test_two_attempts_reading_a_value_that_is_not_off_write_its_line_once and test_two_builds_meeting_a_skipped_row_together_name_it_once and registers join_started from tests/thread_ends.py as a cleanup before the start, the shapes main's thread-stop census reads), _PerCallGate and OneFlightPerWindow (the gate pin and the flight-token pin), GuardRoad, FailedFetch, LiveFeed (the day-arm case; round 3: the rejected-row case's eight cache-key subtests and the guard test_a_cache_rate_absent_null_or_a_numeric_zero_inherits_the_input_rate_and_true_rejects_the_row), TheOverrideFileIsSaid (the seven not-a-number shapes and the absent-key control), APartialOverrideRow, ReattemptKeepsTheEarlierResult, TheBlockCountsTheTable, TheRecorderCountsTheFeedAlone, TheFeedDictIsWhatTheHarnessResets; FEED_RESET without rows, with inflight 0 and flights 0; ROW_LINE naming three classes; CACHE_KEYS, the feed row's two optional rate keys), tests/test_price_feed_consistency.py (OneSession, TheBlockDescribesTheTableThatPricedTheDollars, OneSnapshot (LockShim, the lock stand-in that records the ask and the owner; both cases keyed on the observed ask, never a wall-clock second), TheLabelNeverCarriesTheHost, APartialLandingIsSaid, TheTailNamesTheShare), tests/test_price_feed_census.py (TheFeedHasOneFetchSite, TheCensusIsLive, text-only over kernel/kernel.py; TheCensusRunsFromTheSuite, TheWalkIsRecursiveOverTheDeclaredScope, AnExternalProgramIsNeverLocalByDefault, TheBrowserFetchIsClassifiedByItsUrl, ThePrimitiveListIsKeptHonest, ASecondSiteInsideARowedFunctionIsRed, TheTableIsTheLedgers (its rows case test_the_table_has_a_row_per_road_the_local_row_the_four_classes_and_the_unseen_row since round 3), and since round 3 TheShellSideHasAnInterpreterArm, TheBrowserSideHasTheChildProcessFamilyAndTheConnectionPrimitives, ThePythonImportGateReachesImportModule, TheNetListNamesSendtoAndTheLoopConnections, TheResidualClassIsStatedAndHeld, TheHeadsFigureNamesASwappedProgram and TheGitClassBoundaryIsPinned, with the constants RESIDUAL, DISCLOSURE and UNSEEN_LABEL and the helpers _inventory_docstring and _listed, before the fourth round TheClickedLinkRoadIsPinned (test_the_extensions_openExternal_is_a_site_on_the_clicked_link_road, test_the_openExternal_entry_removed_leaves_the_call_unlisted_and_the_run_refuses_its_stale_row, test_the_window_open_lines_are_sites_on_their_rows_and_none_is_a_browser_dom_load, test_the_roads_residual_is_the_docstrings_sentence_and_the_tables) with the constants OPEN_EXTERNAL_ENTRY, CLICKED_LABEL and CLICK_RESIDUAL and the docstring's clicked-link paragraph, and since the cost cut of 2026-09-22 the base _SharedRun (one run per named group: the classes naming a run plant their row-less mutations together through the module-level _plant, _append and _replace, read one output, and every mutation is undone before the cases run; the shell, browser, credentials and walk runs) with the planted texts as module constants (SERVICE_TEXT, WAKE_TEXT, WAKE_STARTS, NPX_LINE, NPX_GONE, EXEC_TEXT, SPAWN_TEXT, CONNECTIONS_TEXT, IMPORTS_TEXT, DYNAMIC_TEXT, UNKNOWN_IMPORTS_TEXT, KNOWN_IMPORTS_TEXT, SENDTO_TEXT, GIT_TEXT), and since the cost cut of 2026-09-23 before the fifth round every run of the script in this process (the state ratchet's preamble at the module's head; the two-road import of tests/parse_cache.py as PC; KERNEL and KERNEL_TREE from source_and_tree, census taking the tree and _read_kernel removed; _collector_off; _SCRIPTS and script_module; inventory as the script's own main over a root; _Run, _run_of, TREE_KEY, _tree and tree_run as one derivation serving the listing and --table; _replace_text and _line_in beside _replace and _line_of; SERVED_KEY, SERVED_PLANT_LINES, _served_build and served_pass, the served class's in-process pass; _script_binding reading the loaded module), the clean run (TheCensusRunsFromTheSuite as the control, planting nothing, and TheResidualClassIsStatedAndHeld with UNSEEN_TEXT), AnExternalProgramIsNeverLocalByDefault a member of the credentials run, ThePrimitiveListIsKeptHonest (SOCKETS_TEXT) and ASecondSiteInsideARowedFunctionIsRed (WARM_TEXT) members of the walk run, and TheServedPagesAreScanned reading the pass in setUp and no longer a run, and since the fourth round the classes TheEchoRuleScansTheLiveRemainder (a member of the shell run: test_the_pipe_and_the_substitution_forms_are_sites_and_the_rowed_keys_counts_move, test_a_remedy_that_names_a_tool_inside_its_quotes_is_no_site, test_the_trees_four_remedy_lines_have_no_site_line_and_the_run_is_clean, test_the_head_skip_restored_leaves_the_live_lines_silent), TheConnectionFamilyIsReadThroughItsBindings (a member of the browser run: test_the_three_binding_shapes_are_sites_keyed_by_the_familys_tool, test_a_call_the_literal_list_and_the_arm_both_match_is_one_site, test_the_dedupe_removed_counts_a_doubly_matched_call_twice, test_the_timeline_views_two_requests_sit_on_the_local_kernel_row, test_the_arm_removed_leaves_the_bindings_silent_and_the_row_stale), TheServedPagesAreScanned (a served run of its own until the cost cut before the fifth round, then the in-process served pass: test_a_third_party_fetch_in_the_timeline_boot_is_unclassified, test_a_beacon_in_a_page_template_is_unclassified_at_the_templates_lines, test_a_route_the_extraction_cannot_read_is_refused_by_name, test_an_f_string_page_is_read_and_a_format_slot_url_is_the_computed_class, test_a_second_socket_or_opener_in_the_served_text_moves_its_rowed_keys_count, test_the_import_gate_over_served_text_reads_import_led_lines_and_not_a_brace_led_one, test_the_served_texts_own_sites_are_rowed_or_computed_and_its_stylesheets_named, test_the_served_pass_removed_leaves_the_plants_silent_and_the_rows_stale), TheChatMediaRoadIsRowed (test_the_rows_two_sites_are_the_pipelines_post_pass_lines_and_the_table_has_the_row, test_the_row_dropped_and_the_lookbehind_removed_leave_the_sites_unclassified_and_the_definition_a_site), TheGitHubButtonsHrefWriteIsListedByContent (test_the_write_is_one_dom_load_line_at_its_own_line, test_the_write_deleted_moves_the_class_count, test_an_opener_beside_the_write_moves_the_roads_count_and_the_files_key) and TheKindOfReadsADottedHooksShebang (test_the_early_return_restored_skips_the_dotted_hook_by_kind), the cases TheResidualClassIsStatedAndHeld.test_the_fourth_rounds_sentences_stand_in_the_docstring_beside_the_disclosure and TheClickedLinkRoadIsPinned.test_the_residual_constant_is_the_scripts_and_names_the_third_anchors_condition, the hooks/probe.bash plant in TheWalkIsRecursiveOverTheDeclaredScope's walk run with its assertion in test_every_hook_is_scanned_whatever_its_extension_and_a_fixture_of_another_kind_is_not, the fifth key in test_the_window_open_lines_are_sites_on_their_rows_and_none_is_a_browser_dom_load, the constants ECHO_RULE, SERVED_PAGES, SERVED_REFUSED_SCAN, SERVED_IMPORT_GATE, SERVED_KEYED_RESIDUAL, SERVED_NAMED_CLASS, ROUND_FOUR_SENTENCES, CLICK_RESIDUAL (the fourth round's text) and CLICK_RESIDUAL_CONDITION, the planted texts and anchors BASH_HOOK_TEXT, ECHO_TEXT, ECHO_STARTS, REMEDY_LINES, ECHO_ARM, ECHO_SKIP, BINDINGS_TEXT, DEDUPE_TEXT, ARM_LINE, ARM_OFF, TIMELINE_CALL, BOOT_ANCHOR, BOOT_PLANTS, SETTINGS_ANCHOR, SETTINGS_PLANT, CHAT_BRANCH, ROUTE_PLANTS, PROBE_PAGE_DEF, SEND_ANCHOR, SEND_PLANT, SERVED_CALL, SERVED_OFF, SERVED_ROWED, SERVED_COMPUTED, PANE_CSS, CHAT_MEDIA_LABEL, CHAT_MEDIA_ROW, MD_IMG_ENTRY, MD_IMG_ENTRY_NO_LOOKBEHIND, GITHUB_WRITE, GITHUB_LISTING, SHEBANG_LINE and EARLY_RETURN, and the helpers _script_binding (the script's binding read by a child import until the cost cut before the fifth round, then from the loaded module) and _line_of, and in the fifth round _command_line and _Scope.assertCommandLine (the file run as the command line runs it, in a child), the tiny roots' child runs in TheCensusRunsFromTheSuite.test_no_sites_and_a_missing_root_are_each_refused_by_name, TheServedPagesAreScanned.test_the_command_line_over_a_copy_carrying_the_plants_prints_the_pass with _served_text and SERVED_PLANTS as the plants' one writer and _served_build splicing the allowlist hits, the served pass's F, G and H plants as four cases of TheServedPagesAreScanned, one subtest per plant (test_a_routes_type_is_read_through_a_constant_a_keyword_a_local_and_every_script_running_type, test_the_allowlist_names_an_entry_that_names_nothing_and_a_new_place_under_an_entrys_key, test_a_page_is_read_through_encode_format_map_its_containers_and_class_attributes, test_served_text_is_read_per_match_with_no_comment_skip_each_site_at_its_parts_line) with FGH_PAGE, FGH_ROUTES, FGH_DEFS, FGH_HANDLER, DRIFT_JS_ANCHOR, H1_PLANT, DRIFT_CSS_ANCHOR, H2_PLANT, SLICE_SEND, SLICE_LITERAL, SLICE_KEY, DIST_KEY, DIST_PLACES, FGH_TYPED, FGH_TYPE_REFUSED, FGH_READ, FGH_REFUSED, FGH_PLANT_LINES, SERVED_COMPUTED_PLANTS, _fgh_route, _fgh_mark and _plant_lines (the plants' lines by content in one pass), TheAddedBindingShapesAreRead (a browser run member, its RUN and mutate the shared run's: test_each_added_shape_is_a_site_keyed_by_the_familys_tool, test_the_unread_shapes_are_no_site_the_residual_witness, test_over_https_the_gate_reads_the_literal_shapes_and_not_the_aliased_require) with SHAPES_TEXT, SHAPE_LINES, SHAPE_COUNTS, UNREAD_TEXT and UNREAD_HTTPS, ECHO_TEXT and ECHO_STARTS gaining four lines through the echo arm's branches (the bin/romp:curl delta 6), TheClickedLinkRoadIsPinned.test_the_sent_cell_points_at_the_paint_reference_exception_and_no_longer_says_the_token_travels_only with CLICKED_NEW, CLICKED_TRAVELS_ONLY_WITHDRAWN and CLICKED_WITHDRAWN, TheChatMediaRoadIsRowed's paint constants PAINT_LIST, PAINT_CLAUSE, CHAT_MEDIA_PAINT and PAINT_TOKEN_CONDITION and its cookie constants CHAT_MEDIA_COOKIES and CHAT_MEDIA_COOKIES_WITHDRAWN, TheTableIsTheLedgers' rows case counting the .svg tab row and the paint row (SVG_TAB_LABEL, PAINT_ROW_LABEL), TheResidualClassIsStatedAndHeld.test_the_fifth_rounds_sentences_stand_in_the_docstring with ROUND_FIVE_SENTENCES (SERVED_LINE_RESIDUAL, PAINT_ROAD_SENTENCE, SVG_TAB_ROAD_SENTENCE, UNREAD_BINDINGS), SERVED_PAGES and SERVED_KEYED_RESIDUAL rewritten, and the docstrings of inventory, script_module and tree_run naming the child runs: the script run over the tree (by subprocess until the cost cut before the fifth round, then in this process), over a copy of the scanned scope with one mutation alone or with a group's row-less mutations in one shared run, as the command line runs it in a child since the fifth round, and --table against this entry's block between its marker lines; the module loads no romp code but tests/romp_load.py's exec of kernel/loadsource.py, imported above the preamble since the fifth round), tests/test_reference_price_feed.py (TheReferenceCarriesTheSubsection, TheSwitchValueRule, ThePartialRowRule (the not-a-number case; the omitted-rate case re-anchored to the merge's new expression), TheLineClausesSitWhereGearJsPutsThem (the clause order by index over raPriceNote; the trigger pin), TheAnalyticsParagraphPointsAtTheLine, TheNewProse; the helper _jsdef), tests/test_reference_price_feed_rows.py (TheOverrideCountIsOfRowsThatChange, TheCachedRowsLiveUntilARestartOrTheNextLandedFetch, AnUnreadableRowOrFileIsSaid (the fault lines under the refresh guard read by ast; the whole-file clause and the not-a-number predicate), TheNewProse), tests/test_security_price_feed.py (TheNetworkSectionNamesTheSwitch, TheSectionScopesItsClaimToWhatTheCodeDoes (the set-up case renamed test_it_names_the_roads_that_open_when_set_up_by_you_or_a_session_and_what_sets_each_up; the trigger case), TheSectionIsTheTables (the derivation sentence against the script's gates, every road of --table said in the section, the four kernel-request rows and the counted class rows held equal to the committed counts file's classes; since round 3 test_the_class_sentence_counts_the_counted_classes_and_names_the_uncounted_one_from_the_table, test_the_residual_and_the_disclosure_sentences_are_the_scripts_words and test_the_external_program_rows_label_is_the_sections_phrase_read_from_the_scripts_class_rows, the label pin, and before the fourth round test_the_clicked_link_road_is_said_its_residual_is_the_scripts_sentence_and_the_code_opens_as_the_section_says), TheEditorPromptRunsTheExtensionsOwnInstallScript (round 3: test_the_section_names_the_script_the_click_runs_and_the_code_resolves_to_it, test_the_click_reaches_no_pip_and_no_sdk_setup, test_the_install_scripts_condition_is_the_scripts_gate_and_the_tables; the set-up case's bare install.sh substring pin on extension.ts deleted), TheNewProse; the helpers _pydef, _table (every cell of a row since round 3; since the cost cut before the fifth round reading tests/test_price_feed_census.py's tree_run under --table through the two-road import census, no child of its own), _row, _class_label (the script's CLASS_ROWS label, read in a child interpreter until the fifth round, then through _script_binding), _code_lines and _ts_function; the constants EDITOR_PROMPT, EDITOR_NOT_ROOT, OLD_EDITOR_PROMPT, RELEASE_THROUGH, EXT_GATE, EXT_CONDITION, SELF_UPDATE_GATE, UPDATE_TARGET, UPDATE_TARGET_TEST, EXT_INSTALL, ROOT_INSTALL, RESIDUAL, DISCLOSURE, UNSEEN_LABEL, NOT_COUNTED, COUNTED_CLASSES, FIFTH, EXTERNAL_PROGRAM, OLD_EXTERNAL_PROGRAM, EXTERNAL_PROGRAM_LABEL, NUMBER_WORDS and ORDINALS, and before the fourth round CLICKED_LABEL, CLICKED_LINK, CLICKED_HOST, CLICKED_NO_TOKEN (with CLICKED_NO_TOKEN_WITHDRAWN since the fifth round), CLICK_RESIDUAL, RENDER, LINK_OPENER and VIEW_ROUTING, with ROAD_PHRASES' entry for the clicked-link label and the docstring's clicked-link paragraph, and since the fourth round TheChatMediaRoadIsWitnessed (test_the_witness_pins_the_send_headers_by_value_and_the_kernel_writes_them_on_chat: the witness pins the four headers by value from _send's lines, and the kernel's Handler, driven in a child interpreter through tests/test_kernel_auth_hardening.py's harness, answers GET /chat with exactly those four), the six cases added to TheSectionIsTheTables (test_the_residual_constant_is_the_scripts_and_names_the_third_anchors_condition, test_the_sign_in_link_opens_by_document_and_the_settings_page_installs_no_opener, test_the_github_button_is_said_and_its_mechanics_are_the_codes, test_the_github_buttons_sent_clause_names_what_the_executed_url_shapes_carry, test_the_chat_media_road_is_said_and_its_witness_is_the_browser_leg, test_the_echo_rule_and_the_served_pages_sentences_are_the_scripts_words), the clicked-link case's opener pins with messages naming the executed test that proves each leg or saying text-pin-only, the target pin's whole-statement needle over updateExtension and its once-bound count, ROAD_PHRASES' entries for the chat-media label and the sign-in sentence, the constants CLICKED_SIGNIN, CLICKED_SIGNIN_CELL, SIGNIN_WITNESS, GEAR_SIGNIN_BUILD, SETTINGS_PAGE_INIT, SETTINGS_PAGE, TARGET_BLANK, BLANK_ANCHOR_BUILDS, CHAT_MEDIA_LABEL, CHAT_MEDIA, CHAT_MEDIA_COOKIES, CHAT_MEDIA_COOKIES_WITHDRAWN (since the fifth round), CHAT_MEDIA_WITNESS, DASHBOARD_HEADERS, SEND_HEADERS, HEADER_LINE, CLICKED_GITHUB_SENT, CLICKED_GITHUB_TEMPLATE, CLICKED_GITHUB_PARTS, CLICKED_GITHUB_REFUSALS, CLICKED_GITHUB_TRIGGER, CLICKED_GITHUB_DEFAULT_OPEN, CLICKED_GITHUB_NO_EDITOR_LEG, SECURITY_GITHUB_TRIGGER_ITEM, SECURITY_GITHUB_SENT, FILE_GITHUB_TEST, GITHUB_BLOB, DETACHED_SHAPE, FILE_VIEW, ANCHOR_BUILD, OPENER_CLASS_SOURCES, SELECTOR_LINES, ECHO_RULE, SERVED_PAGES, BINDING_ARM (the section's binding sentence, one text with the docstring's clause), CLICK_RESIDUAL (the fourth round's text) and CLICK_RESIDUAL_CONDITION, and the helpers _script_binding, _road, _blank_anchor_builds and _executed_github_shapes, and in the fifth round TheStripAndMdCallersArePlaced (test_every_stripRemoteLoads_caller_is_placed: every caller of stripRemoteLoads under ui/webview and vscode-extension/src placed through _enclosing_callers, the provider slot through PREVIEW_CONTENT_CALL; test_the_md_callers_are_placed_in_the_chat_media_trigger_cell: MD_CALLERS and MD_CALL), TheChatFilePreviewAndNoticePaintRoadIsNamed (test_the_paint_road_is_said_and_is_a_row_named_and_not_counted), TheSvgTabRoadIsNamedAndItsPopulationBounded (test_the_svg_tab_road_is_said_and_is_a_row_named_and_not_counted, test_image_svg_xml_is_the_one_document_type_the_file_route_serves, test_the_witness_serves_the_svg_file_headers_and_the_kernel_writes_them_on_both_arms; _is_document, SVG_TYPE, ONE_DOCUMENT, DOCUMENT_ESSENCES, XML_FAMILY, SVG_FILE_HEADERS), TheTokenSentencesNameThePaintException (test_the_trust_model_sentence_names_the_paint_exception, test_the_kernel_comment_on_the_referrer_policy_write_names_the_exception, test_the_section_says_two_kinds_of_request_are_named_and_not_counted, test_the_section_names_every_other_text_the_chat_renders_as_markdown; TRUST, TRUST_EXCEPTION, KERNEL_COMMENT_EXCEPTION, REFERRER_WRITE, NAMED_NOT_COUNTED_KINDS, CHAT_MEDIA_KINDS), the constants PAINT_LIST, PAINT_CLAUSE, CHAT_MEDIA_PAINT and PAINT_TOKEN_CONDITION read from the census module, PAINT_ROW_LABEL, PAINT_FILE_PREVIEW, PAINT_NOTICE, PAINT_STRIPPED, PAINT_WITNESS, STRIP_CALLERS, STRIP_REMOTE_LOADS, SVG_TAB_LABEL, SVG_TAB, SVG_TAB_RELAY, SVG_TAB_PAINT, SVG_TAB_SENT and SVG_TAB_WITNESS, UNREAD_BINDINGS, SERVED_PAGES and BINDING_ARM rewritten, ROAD_PHRASES' entries for the two new labels, TARGET_BLANK widened to the attribute spelling with BLANK_ANCHOR_BUILDS' two setAttribute keys (nine statements), and _script_binding reading census.script_module(ROOT), the two child programs gone), tests/test_price_feed_vocabulary.py (TheTwoSidesAreFound, TheVocabularyIsOneSet with VIEW_EXEMPT and the two-way key pin (every key the status emits is read in raPriceNote or named there with its reason), TheExtractionIsLive with the liveness case for it), tests/conftest.py (the ROMP_PRICE_FEED floor and its comment), tests/test_price_feed_floor.py (PriceFeedFloor, one self-contained case that runs conftest's _no_price_feed_fetch itself through _conftest and _fixture_function; TheFloorsAreOnConftestsSource), tests/test_model_catalog_floor.py (the docstring's pointer to the source pins), tests/test_ship_reship_served.py (kernel_env ROMP_PRICE_FEED, RelaunchEnv, LabKernelEnv), tests/README.md (the kernel_env paragraph: the three network switches as the lab's own), ui/webview/analytics-price-source.test.ts (the wired case executes raRender over the DOM stand-in, the lifted render block with its closure names stubbed, and pins the call's place by index, never the footnote's copy; the live-feed case's ages in the shared helper's words; its executed node cases, the two raPriceLine cases and the wired raRender case, take nodeFactory from ui/test-dom-shim.ts, a fork module absent upstream: the offer ports after the tags-scale-memory and shim-migration entries, fork PRs 523 and 569, or adapts that one file, the body saying why and the entry's third paragraph listing every fork file the offer's tests need; since the fifth round its in-flight literals reworded), ui/webview/analytics-price-source-states.test.ts (the ignored-whole case, the row, absent and foreign guard, the age boundaries with the executed agoWords loop; imports ./api-health-merge, upstream's module; since the fifth round its in-flight literals reworded and a case that drives the modal's open, metric, group and period handlers lifted from initGear with fetch stubbed, ROOT and INFLIGHT beside it, importing ../test-dom-shim as analytics-price-source.test.ts does), ui/webview/gear.test.ts (the export pin and the age-helper source pin), ui/webview/chat-media-loads-browser.test.ts (since the fourth round: the chat-media road's executed witness, render.ts's md and userMd pipeline over two loopback servers under the dashboard's four headers, nine media shapes requested once each with no Referer and the cross-site cookie alone, none under the editor's content security policy, a Referrer-Policy control; skips at exit 0 without a browser, as the other browser legs do; since the fifth round the paint references in both scenes, the Chromium list read from SECURITY.md's paint clause and held equal to the requests that arrive, filter's absence pinned, mask's origin Referer and no cookie asserted, a file preview scene, by hover and by keyboard focus, and a notice card scene in which only the paint references load, and Firefox and WebKit legs that load at least mask; the definitions the fifth round added or changed: FIGURE_GATE, paintAttrs, PAINT, PAINT_LIST_RE, PAINT_MIN_RE, PAINT_NONE_RE, paintFromSecurity, PAINT_EL, paintPath, paintMd, MEDIA, mediaMd, feedHtml, FEED_STYLES, STYLES, noKatex, editorCsp, PNG, SVG_DOC, TOKEN and SID), ui/webview/svg-tab-loads-browser.test.ts (since the fifth round: the .svg tab road's executed witness, openFileTab over the kernel's `/file` headers and the `/remote/<host>/file` relay's, the tab's seven requests recorded on each arm with their headers, and a control scene without the sandbox whose mask request carries the page's origin as its Referer; skips the same way), ui/webview/settings-signin-link-browser.test.ts (since the fourth round: the sign-in link's executed pin, the settings page served as the kernel serves it, a real click on the anchor, no window.open, no openLink post, the popup at the href; skips the same way), scripts/network-inventory.py (the census of outbound connections behind SECURITY.md's Network access section and this entry's table; standard library only, no fork-only dependency, so it is offerable with the change, and SECURITY.md's derivation passage names it; the recursive walk (walk, kind_of, PY_ROOTS, JS_ROOTS, NAMED, SKIP_DIRS), the gates in problems (SCOPE, PARSE, IMPORT with KNOWN_IMPORTS, since round 3 over IMPORTERS too (importlib.import_module and __import__, a run-time name refused) and with KNOWN_JS_IMPORTS over every package the browser and editor code imports, NO SITES, UNCLASSIFIED, STALE ROW, TABLE, COUNTS (the heads group since round 3), PROGRAM with KNOWN_PROGRAM_REFS), the classes (CLASSES, INTERPRETERS, RUNTIME_ROADS, CLASS_ROWS: external-program, runtime-program, browser-computed-url, browser-dom-loads, the external-program sent cell and the browser-computed-url cells reworded in round 3, the browser-dom-loads trigger and sent cells before the fourth round (an anchor's href write loads nothing by itself; no window.open counted there); the fifth row named and not counted, UNSEEN_ROW; the DOM pattern list DOM), the browser fetch placed by its URL (_call_arg, _fetch_arg's name since round 3, _fetch_class, KERNEL_URL_HELPERS; a dynamic import() with a computed specifier in the same class), the wider NET (asyncio and anyio connections, urlretrieve, build_opener, a bound socket's connect; since round 3 a socket's sendto through SOCKET_METHODS, and the event loop's sock_connect, create_connection and create_datagram_endpoint on a loop the scan resolves through LOOP_GETTERS and LOOP_METHODS) beside SUB, the argv resolution of a list constant and sys.executable (Scan.argv), the row table T through _t with the rows added for kernel/kernel.py _release_remote and kernel/host_transport.py HostTransport.connect and the ui/webview:fetch row removed, and since round 3 the ten rows for the shell scripts' inline interpreter texts and for npx (bin/romp:python3 -c, bin/romp:python3 -, bin/romp-service:python3 -, hooks/romp-postal-context.sh:python3 -c and :python3 -, hooks/romp-postal-ensure.sh:python3 -c and hooks/romp-usertodo-context.sh:python3 - on local-kernel; bin/romp-uninstall:python3 - on local-program; vscode-extension/install.sh:node -e and :npx on install-ext; before the fourth round vscode-extension/src/extension.ts:openExternal and ui/webview/render.ts:window.open, ui/webview/link-opener.ts:window.open and ui/webview/file-view.ts:window.open on clicked-link, ui/webview/preview.ts:window.open on local-kernel), LOCAL_TOOLS without sh, node and perl, LOCAL_GIT without remote and checkout, LOCAL_ROADS, the line patterns SH (ssh with any argument, gh widened; since round 3 npx, scp, rsync, sftp and nc, and the interpreter arm SH_INTERPRETER: a head python, python3, node, perl, sh or bash, path-prefixed or not, followed by -c, -e or a bare -) and JS (since round 3 net.connect, net.createConnection, tls.connect, XMLHttpRequest, sendBeacon and import(), before the fourth round the URL openers openExternal and window.open, the latter leaving the DOM list DOM, the bare execFile and spawn patterns replaced by the child_process family CP_FAMILY qualified to its binding through _CP_MODULE, _cp_bindings and _cp_sites, each program site classed by its argv through _js_args and _js_argv, and the package gate through _js_specifiers and _js_package; since the speed cut of 2026-09-22 each list's patterns compiled once with one alternation per list, _compiled with SH_RX and SH_ANY, JS_RX and JS_ANY, DOM_RX and DOM_ANY, the single patterns compiled once, _HELPER_CALL, _SCHEME, _PROGRAM_PATH (Scan.visit_Constant matches a spelled program path through it), _CP_NAMES, _CP_SPACES, _CP_RENAME, _SHELL_TRUE, _JS_FROM, _JS_SIDE_EFFECT and _JS_REQUIRE, and _cp_bindings returning the file's compiled call patterns, None for a file that never names child_process), Site, Result, Scan (since round 3 bind's LOOP binding, bind_loop, visit_For, visit_AsyncFor, visit_comprehension, comp, modules_of, and visit_Call's socket methods, loop methods and IMPORTERS gate), line_scan (over a file's text since the speed cut), scan (since the speed cut each file's text read once, the one-liner pass over the text bootstrap.sh's line scan read), figures (the heads map since round 3), problems, render_sites, the table's prose columns (ROADS, whose self-update sent cell and install-ext trigger and sent cells state the editor-CLI condition since round 3, and before the fourth round the clicked-link row, a link you click (browser), with CLICK_RESIDUAL, the road's residual sentence shared with the docstring and SECURITY.md, the docstring's clicked-link paragraph and the KNOWN_JS_IMPORTS comment naming vscode's call a site; LOCAL_ROW, naming the browser's own-tab open of a kernel file URL before the fourth round), --table (render_table with the unseen row, _where, _programs), --write-expected and the committed counts (EXPECTED, figures, main); since the fourth round the shell scan's quote-aware echo arm in line_scan (_live_remainder: the live remainder of an echo- or print-led line, the text outside quotes and the body of every $(...) and backtick substitution by a paren-depth walk, a word-initial # ending it, read by the interpreter arm and the tool list), the connection family through a file's bindings (NET_FAMILY, _NET_PATTERNS, _binding_patterns generalising the child_process shapes to a module, whose tuple binds _CP_MODULE, _CP_NAMES, _CP_SPACES and _CP_NAME, _names_module, _module_bindings, _net_bindings, _net_sites, and the once-per-line dedupe in line_scan), the served pages (SERVED_CTYPES until the fifth round, _PY_SLOT, _TEXT_CALLS, _READ_CALLS, Scan.defs and Scan.routes filled by Scan.__init__, Scan.enter and Scan.visit_Call, from every _send whose content type was text/html or text/javascript until the fifth round, the class _Served with its _locals, _returns, _ctx, _path, resolve and run, served_texts, Result.served and Result.served_files, line_scan's base, dom and served parameters, _js_specifiers's and _fetch_class's served parameter, render_sites's served-file lines, render_table's dom-loads where cell), kind_of's early return for a dotted name removed (the shebang read decides), the JS entries clients.openWindow and mdImgPostPass, the rows ui/webview/render.ts:mdImgPostPass on chat-media and ui/romp-timeline-view.js:http.request, kernel/kernel.py:WebSocket, kernel/kernel.py:window.open and kernel/kernel.py:clients.openWindow on local-kernel, the ROADS entry chat-media, the clicked-link cells (the GitHub button's class in the sent cell, the trigger per document, the sign-in link by document), CLICK_RESIDUAL's three anchors and its page-built class, LOCAL_ROW's served-scripts clause, CLASS_ROWS' browser-computed-url trigger and browser-dom-loads sent cells, and the docstring's echo rule, served-pages, refused-scan, import-gate, keyed-residual and named-class sentences; and in the fifth round the route population read from every `_send` call and every Content-Type header write (routes_of, reached through Scan.visit_Module; each call typed through the `_send` definition it reaches, FRAME_WRITERS naming the two frame writers; _ctype_values, _dict_of, _scopes_of, _script_type with SCRIPT_TYPES and the +xml rule, and _is_ctype_write; SCRIPT_TYPES in place of SERVED_CTYPES, removed), SERVED_ALLOW with each entry's place count and reason and the stale, count and frame-writer gates in problems, Result.allow_hits and Result.writes (Result.__init__), Scan.visit_Global, Scan.visit_Name, Scan.visit_Subscript and Scan.visit_AugAssign with Scan's sends, ctype_writes, responds, globals and writes (Scan.__init__, Scan.enter and Scan.visit_Call changed: the candidates and the mutating-call writes, the literal route predicate removed), the served pass's receivers and containers (_Served._allowed, _Served._memo, _Served._base and _Served.receiver, _FOLLOW_ANY and _MUTATORS; _Served.__init__, _Served._locals, _Served._ctx, _Served._path, _Served.resolve and _Served.run changed, the held locals maps among them; the class docstring's value-slot sentence corrected), line_scan's served mode reading every fetch( and import( by finditer with no comment skip and _call_arg's opener index, each f-string part keyed at its own line and served_texts' part-keyed dedupe, _binding_patterns one alternative per line with the added shapes and _module_bindings reading every filled group (the _cp_bindings and _net_bindings docstrings), PAINT_LIST and PAINT_CLAUSE in the chat-media sent cell, PAINT_ROW and SVG_TAB_ROW printed by render_table, the clicked-link sent cell's parenthetical, CLASS_ROWS' browser-computed-url trigger naming route[h] and its browser-dom-loads sent cell naming the paint row, LOCAL_ROW's where text, the comments on preview.ts's window.open and on the clicked-link openers, and the docstring's served, gate, binding, residual, paint road and .svg tab road sentences), scripts/network-inventory-expected.json (the committed counts the census is compared against: sites, roads, local_roads, local_sites, kinds, classes, per_road, per_key and, since round 3, heads, the program head per Python command site by row key and primitive; written by --write-expected from a clean run, regenerated once at the fourth round's tree and once at the fifth round's), tools/ui-bench.mjs (startPageServer's kernel environment: ROMP_PRICE_FEED=off beside the catalog's floor), tests/ui-bench.test.mjs (the env case plants ROMP_PRICE_FEED and asserts off), tools/perf-bench.py (prepare_env: ROMP_PRICE_FEED=off), tests/test_perf_bench.py (InProcessChecks: its env case plants ROMP_PRICE_FEED and asserts off); fork-only: ui/webview/file-comments.test.ts (the kernel load's env: ROMP_PRICE_FEED=off beside the catalog's floor), tools/file-review-viewer-recipe.test.mjs (its header's census of the browser modules that render through the sanitizer pipeline names ui/webview/chat-media-loads-browser.test.ts among those whose pages declare no viewer box; a fork file upstream's main does not have, so fork-only since the fifth round's check, where the earlier line tagged it upstream-facing), tests/test_lab_dist.py (_TREE_COPIERS names tests/test_price_feed_census.py: the census's scope copy into a scratch root, never a build and never dist; since round 3 _ESBUILD_TEXT_READERS names tests/test_security_price_feed.py, whose condition pin reads vscode-extension/install.sh as text and locates its build line to hold the order npm install, the build, the gate, npx: a source pin, no build and no copy), upstream/2026-09-20-price-feed-off.md (this entry)
added: 2026-09-20
pr: 878
tier: fix
offered:
closed:
---
The kernel fetched a third party's price table (the public LiteLLM list on raw.githubusercontent.com, no credential) whenever the Token usage modal opened with a cache older than six hours, with no off switch and no trace: a table served from the built-in defaults read as live, and a failed fetch was swallowed by a bare return. ROMP_PRICE_FEED=off copies the model catalog's switch (ROMP_MODEL_CATALOG=off: the same stripped, case-folded read, placed as the first statement of _refresh_remote_prices, the one function a fetch can start in, before the TTL check and the stamp) and goes one step past the catalog's silent off: the state is said where the user looks. One function, _price_feed_status, computes the block the /analytics payload carries as priceFeed (outside the 15 s memo on both roads, and describing the table that priced the figures beside it: a landing that changes the rows clears the memo, so the click after it prices from the landed table instead of serving the old table's dollars under a block that reads live, and a build whose fetch landed during its sessions walk, after its pricing merge, prices once more from the table now) and /version carries beside modelCatalog, so the modal, /version and a curl cannot disagree. The worker publishes a landing (the cache and the fields that describe it) under _price_feed_lock, the lock the status reads its one snapshot under, so no read pairs one fetch's rows with another's counts.

What the user sees, on one muted line under the modal's footnote, worded by gear.js raPriceNote from the payload's block: with the feed live, `prices: live feed, fetched 4 minutes ago` (the age in words from the kernel's ageS through the webview's one age helper, agoWords in ui/webview/api-health-merge.ts, bound in gear.js as raAgo: now under 45 seconds, then rounded minutes, rounded hours under a day, then days); when the feed priced some of the table's models and not all (`rows` below `known`, the ids the built-in table holds), `prices: live feed for 1 of 6 models, fetched now; built-in defaults for the rest`, and when rows the feed had for known models could not be read (`matched` above `rows`) `; the feed's rows for 1 known model could not be read` follows the share; with the switch on, `prices: built-in defaults; live feed off (ROMP_PRICE_FEED=off)`; after a failed fetch, `prices: built-in defaults; the feed could not be fetched (HTTPError: HTTP 500)` (a reason class, never the response body or the URL: the exception type, an HTTP status, a wrapped socket error's type and errno, an SSL failure by OpenSSL's reason token and the verify code's entry in the kernel's copy of OpenSSL 3.0's verify-error table, such as `SSLCertVerificationError: CERTIFICATE_VERIFY_FAILED (self-signed certificate)`, `(hostname mismatch)` or `(format error in certificate's notBefore field)`, the verify message itself never read because CPython composes it with the server hostname for a hostname or IP address mismatch, and a code the table lacks, or any code under an OpenSSL before 3, reading `(verify code N)`, so the label never carries the URL or its host, a resolver failure by the EAI_ constant's name and libc's text such as `gaierror: EAI_NONAME (Name or service not known)`, never a system errno's text for a library code); on the modal's first open, `prices: built-in defaults; the feed was still being fetched when these figures were priced; pick a period to reprice` (the payload is built before the fetch it starts lands, and the next open or a period pick reads the feed); before any attempt, `prices: built-in defaults; nothing fetched from the feed yet`; after a fetch that landed and left no usable row, `prices: built-in defaults; the feed matched no known model` when no row signed to a built-in id (`matched` 0), or `prices: built-in defaults; the feed's rows for 2 known models could not be read` when rows did and none parsed. A landed or failed result outranks a fetch in flight, so the TTL re-attempt keeps saying failed or empty while its worker runs. Under off a cache already in process memory keeps serving with its age said, and the feed line then carries the refresh's own state, `prices: live feed, fetched 10 minutes ago; refresh off (ROMP_PRICE_FEED=off)`, or after a refresh that failed while the rows still serve, `; the last refresh failed (HTTPError: HTTP 500)`: the switch stops traffic, not data, and there is no cache file. Rows from the user's ~/.config/romp/model-prices.json price their models whichever table the line names, so the block counts them (`overrides`, from the merge that prices) and either line ends `; 1 row overridden by model-prices.json` when one is in effect. When ROMP_PRICE_FEED is set to a value other than off (only off turns the feed off), the feed stays on and either line ends with one more clause before the override count, `; ROMP_PRICE_FEED is set to a value that is not off, so the feed stays on (only off turns it off)`, from the block's boolean `unrecognised`, never the value. When rows of the file could not be read, either line ends, after the override count, `; 1 row of model-prices.json could not be read and was skipped (the rest of the file applies)` (`; 2 rows ... were skipped ...` above one), from the block's count `overrideRowsRejected`, never a key of the file. When the file could not be read as a JSON object at all, that last slot carries instead `; model-prices.json could not be read as a JSON object and was ignored (none of it applies)`, from the block's `overrideFault` reading `file`, never the path or the file's text; the kernel reports one class or the other, never both, so the two clauses never both render. A payload without the block (an older kernel) renders no line, and a block without a newer key (`matched`, `known`, `overrides`, `unrecognised`, `overrideFault`, `overrideRowsRejected`) is worded as the older shape. Stderr, in the catalog's idiom, each line's tail read from the same status and worded with the facts the modal's line words (the source, the share a partial feed priced, the age, the overrides), so the log and the modal cannot disagree: `price feed: off (ROMP_PRICE_FEED=off); the cost view prices tokens from the built-in defaults` once per kernel life at the first refused attempt (a kernel where nobody opens the modal says nothing; with rows already in memory the tail reads `the feed rows in memory, fetched 2 h ago` (`fetched 30 d ago` past a day, so the log counts days as the modal does), or `the feed rows in memory for 1 of 6 models, fetched 2 h ago, and the built-in defaults for the rest` when the feed priced some of the table's models, and an override in effect appends `, 1 row overridden by model-prices.json`); `price feed: fetch failed (<reason class>); ...` with the same tail, once per failed fetch, which the six-hour TTL bounds to one line per six hours; `price feed: fetch landed with no usable row (2 signed to a built-in id, none parsed); ...` once per such fetch; `price feed: fetch landed with rows for 1 known model unreadable (2 signed to a built-in id, 1 parsed); ...` once per fetch that parsed rows for some known models and not others; and `price feed: fetch failed (<reason class>), the worker thread did not start; ...` when the refresh's thread could not start, which clears the in-flight mark and records the failure instead of leaving the status reading inflight for the TTL. Three lines came with the fork manager's first review round (the round paragraph below): `price feed: ROMP_PRICE_FEED is set to '<value>', which is not off, so the feed stays on; ...` once per kernel life at the first attempt that reads such a value, the value by repr clipped to 40 characters (the kernel's own log is the one surface that carries it); and `price feed: the row 'claude-opus-4-8' in model-prices.json could not be read (not an object, a rate that is not a number, or a rate that is not finite), so that row is skipped and the rest of the file applies; ...`, once per kernel life for each skipped row, the key by repr clipped to 40 characters as the switch's value is (the manager's re-ruling of 2026-09-21; the round's line said the row and every row after it were ignored, and named no row), or `price feed: model-prices.json could not be read as a JSON object, so the file is ignored whole; ...`, once per kernel life, each fault class on a latch of its own, each with the same tail and never a value from the file or its path.

T350 kept: _model_prices(refresh=False), the spend guard's road on the pusher's path, is unchanged and never enters _refresh_remote_prices, so it cannot start a fetch with the switch off, on or unset (tests/test_spend_guard.py test_the_guard_never_starts_the_price_feed_fetch, tests/test_price_feed_off.py GuardRoad). The signature _refresh_remote_prices(now), the price-refresh thread kind and the inner def name work are unchanged, so the _THREAD_KINDS register, the stage-marks census, the perf-bench positional call and the test_token_usage.py harnesses that replace the refresh with `lambda now: None` all hold. The suite floors the switch for every test (tests/conftest.py, the model catalog floor's shape, set at import and re-asserted per test; tests/test_price_feed_floor.py pins the per-test re-assert by execution and both statements, the import-time assignment and the fixture, on conftest's source, for this switch and the catalog's; a read inside a test body cannot tell the two halves apart, and the fork manager's first review round found the import-time line unpinned), and a lab kernel built from names, which never inherits the runner's floor, is handed the switch by tests/test_ship_reship_served.py kernel_env beside the catalog's (LabKernelEnv pins it; two served labs open the Token usage view and fetched the feed on every run without it), so no test kernel reaches the feed's host; the feed's own tests pop it in setUp. The offer's tests need fork files upstream does not have, derived from each test file's imports and literal file reads at the branch head and checked against upstream's main (the text here named one until the fifth round): tests/test_price_feed_off.py imports tests/thread_ends.py; tests/test_price_feed_census.py imports tests/parse_cache.py, copies tools/file-comments-host.mjs into its scope copy (the script's declared scope names it as the one program the kernel starts from tools/) and appends a plant to hooks/romp-track-bash-guard.mjs in its walk run; tests/test_security_price_feed.py imports the census module, so it needs the same files, and reads ui/webview/link-opener.ts at import and ui/webview/url-links.ts in its new-tab anchor census; ui/webview/analytics-price-source.test.ts builds the nodes of its two executed cases, and since the fifth round ui/webview/analytics-price-source-states.test.ts those of its modal-flow case, with nodeFactory from ui/test-dom-shim.ts, so upstream's test bundle (every ui/webview/*.test.ts, through vscode-extension/esbuild.js) fails on the unresolved import; and ui/webview/chat-media-loads-browser.test.ts bundles ui/webview/md-config.ts and reads the paint attributes from ui/webview/figure-gate.ts. tests/thread_ends.py and tests/parse_cache.py ride the parked-ops liveness entry (a candidate, not yet offered); ui/test-dom-shim.ts the tags-scale-memory and shim-migration entries (fork PRs 523 and 569, both approved, neither offered yet); ui/webview/link-opener.ts and ui/webview/url-links.ts the user-todo-links entry (fork PR 405); ui/webview/md-config.ts and ui/webview/figure-gate.ts the markdown viewer's md-config entry (fork PR 707); tools/file-comments-host.mjs the file comments entry; and hooks/romp-track-bash-guard.mjs no entry. The census script needs no fork module (standard library only), but its declared scope names tools/file-comments-host.mjs and its committed counts and the table below are this tree's, so an offer regenerates scripts/network-inventory-expected.json and the table from upstream's tree. The offer therefore ports after those entries or with them; the alternative, adapting its tests for upstream (an inline join in place of thread_ends.py, a module-local parse and tree memo in place of parse_cache.py, an inline fake in place of nodeFactory, which upstream, with no ratchet, accepts, and the census's scope and plants fitted to upstream's tree), is the owner's call at offering; in the fork the ratchet in ui/test-dom-shim.test.ts requires a fake node that carries an edge (parentNode, children and the other edge names) to go through the shared module (nodeFactory, or hideEdges on the object). The settings sign-in witness, the .svg tab witness and gear.test.ts import and read nothing that upstream's tree lacks.

Design points recorded and left as they stand, each a separate decision: the share clause (`live feed for N of 6 models`) counts the table's rows the feed priced, not the models the payload's figures resolved to (sessions running one model, with a feed that priced the other five, read `live feed for 5 of 6 models` while every dollar is that model's default row), and the follow-up is a per-payload count taken where each response is priced; the catalog's own switch, ROMP_MODEL_CATALOG=off, is documented nowhere in docs/ and is silent under off (no off marker in its /version block, nothing on stderr), and giving it the same treatment is a separate change; ROMP_UPDATE_CHECK reads its switch as an exact match where the catalog strips and case-folds, the price feed copies the catalog, and the two older precedents still disagree (` Off ` counts for two switches and not the third); and the fast-mode probe and the reference's sentence that the kernel makes two API calls of its own are the same claim class as the guide's outbound-traffic correction in hand elsewhere, so whoever lands second covers that paragraph (the probe is a road in the census below; the sentence is the census's `Not this PR` note). Where the code departs from the design note this change was built from, with the reason: the line renders under both metrics, tokens and cost, not in cost mode only, because the view says where its prices came from whenever it is open (the presence rule), and whether it should be cost-only is the one question left open for the fork's owner; a separate `#ra-price` node, present with its text and absent without, never an empty node, in place of a clause appended to the footnote; the age from the kernel's ageS in words through the webview's one age helper (agoWords, bound as raAgo), not raWhen over fetchedAt; no `_async` keyword, because the tests join the price-refresh thread by name and the signature stays literally unchanged; lastError a reason class, not a `Type: message` clip, so nothing from the response or the URL reaches the label; six wordings under defaults where the note wrote four (inflight said apart from unfetched; empty worded two ways by `matched`); the five reason values are the note's; the kernel test module named tests/test_price_feed_off.py, not the note's tests/test_price_feed_switch.py; `built-in defaults` on every surface where the note wrote `baked-in table`; the off line a deliberate step past the catalog's silent off (the requirement that the log and the view say defaults), not a copy error; the webview pins executed through gear.js in ui/webview/analytics-price-source.test.ts and analytics-price-source-states.test.ts, where the note placed source pins in rail-spend.test.ts and marked the raPriceNote extraction optional, because requiring gear.js under node was unverified when the note was written and the build verified it (gear.js's top level is declarations, requires and the export), so rail-spend.test.ts is unchanged and the note's pin that kernel.py emits priceFeed in both _token_analytics and _version_info is executed in tests/test_price_feed_off.py rather than pinned as source from the webview side; the worker stamping fetchedAt with the attempt's `now` (the /analytics handler's clock, the one the TTL stamp uses) where the note wrote int(time.time()) at success, so ageS and the TTL are computed on one clock and the tests drive both with one synthetic time, the difference being the fetch's own duration; and `_price_feed_lock` where the note's risks section said no lock is needed (written for single-key writes), because round 1 made the view read the block as a compound (source with rows and known, reason with matched, the feed line with lastError), so a landing has to be seen whole or not at all. The fork manager's first review round added four more departures, each in the round paragraph below with its reason: the block carries a boolean, `unrecognised`, for a switch value that is set and not off, and never the value, where the design note's status shape had no such key (the block rides the auth-exempt /version); the finiteness check on rates covers the override file's parse as well as the feed's, where the note's population had the file as a read-only input; the census of outbound connections is a derived table with a re-runnable script, scripts/network-inventory.py, where the note's ask listed six roads by hand; and _price_feed_lock also serialises the say-once latches as test-and-sets, where the note's risks section said single-key writes needed no lock. The fork manager's second review round added two more, each with its reason in its round paragraph below: the TTL check and its stamp take _price_feed_lock with the attempt's fields as one transition, where the earlier review had refuted the check-then-stamp race by trial count, because a parked read of the stamp reproduces two requests for one stale cache on both interpreters; and the in-flight mark is a per-flight token cleared under the lock by its own flight, where the status shape had a boolean, because the first of two flights to end cleared the shared boolean for both. The additions the note did not have (the partial share, the override count, `matched`, the feed line carrying the switch or the last failed refresh, a landed or failed result outranking a fetch in flight, inflight worded as a fetch under way) are the second paragraph's, and tests/test_price_feed_vocabulary.py pins the reasons, sources and keys the two sides share.

The first review round by the fork's manager, 2026-09-20, landed on the same branch before its re-review, changed the following. The block describes the table that priced the figures beside it on both /analytics roads because the serve is guarded, not the store: the memo entry carries the merged table its figures were priced with (`prices` in _ANALYTICS_MEMO) and is served only while that is the table now (_price_feed_status(now, memo["prices"]) on the memo road, a rebuild when it returns None), the miss road memoizes only once a block describes its payload, and the one rebuild's table moving again serves the figures with no block (`priceFeed` null, no line in the modal) and memoizes nothing, the disclosed residual; before, the memo road read a fresh status with no table check, so an edit to model-prices.json inside the 15 s memo window paired memoized dollars with a block counting an override that never priced them, and the worker's memo clear covered a feed landing alone. Only `off`, whitespace and case ignored, turns the feed off (the catalog's spelling, unchanged and pinned once in the source); a value that is set and not off leaves the feed on and is now said: one stderr line per kernel life at the first attempt that reads it (the attempt is the event, boot is not), naming the variable and the value it read (repr, clipped to 40 characters), through _price_feed_unrecognised, read per call like its sibling and checked after the off arm and before the TTL check so a refused attempt never reads as unrecognised, and the block's boolean `unrecognised` on the modal's line and /version, never the value, because the block rides the auth-exempt /version and carries no environment text; the choice the manager may re-rule is that split, the log names the value and the block carries the boolean. Every say-once latch (offSaid, unrecognisedSaid, overrideFileSaid, and overrideRowsSaid, a frozenset of the row keys named, replaced under the lock) is a test-and-set under _price_feed_lock through _price_feed_first, with the line written outside the lock because _price_feed_line re-enters it through _price_feed_status; before, offSaid was a check-then-set outside the lock and two builds arriving together wrote the off line twice. A rate that is not a finite number (JSON NaN or Infinity, which json.loads accepts) is rejected at both parses, the feed's (counted in `matched`, never in `rows`, never cached, said as a landing with no usable row) and the override file's (a rejected row); before, such a feed row was cached as a live row and serialized as a bare NaN token the browser could not parse. A row of model-prices.json the kernel cannot read (not an object, or a rate that is not a finite number) is skipped alone and every other row applies, wherever in the file it sits, a file it cannot read or parse as a JSON object (a JSON list included) is ignored whole, a missing file is nothing, and the block and /version carry the class as `overrideFault` (null, `row` or `file`) and the count of skipped rows as `overrideRowsRejected`, said on stderr with the consequence (the second paragraph's lines); both travel on the merged table itself (_PriceTable, the dict _model_prices returns), so the status reports the fault and the count of the very merge it counts overrides from, exact under its lock. The round as first landed had two deltas, and the manager re-ruled both on 2026-09-21: a row the kernel could not read was rejected together with every row after it (one try around the whole loop, where the base had skipped a non-object row silently and kept the rest), rejected because the same bad row placed first voided the user's whole table and placed last voided nothing, a reach that was an artifact of the parse and not a rule, and ruled the shape the unrecognised switch value has: the row alone rejected, every other row kept, the stderr line naming the row and saying the rest applies, the block carrying the fact as a count; and the say-once latch was one across both fault classes, which let the first fault of either class silence the other class's first for the rest of the kernel's life, ruled one latch per class (`overrideFileSaid`; `overrideRowsSaid`, keyed by row so each bad row is named once and a row that goes bad later is named too). _price_feed_off's docstring names the update check's switch by its real name, _update_checks_off, and states the rule in place of a precedent inventory. tests/test_price_feed_census.py pins, by ast over kernel/kernel.py and loading no romp code, that PRICE_FEED_URL is read at the worker's urlopen alone, that _refresh_remote_prices is entered from _model_prices alone, and that _model_prices lets a fetch start from _token_analytics alone (the refresh=False callers being the guard's road and the status's own merge), classify-or-red: a site not in its table fails naming file, function path and line and the two resolutions, and a classified site that is gone fails as a stale row. The one-rebuild bound of _token_analytics (_ANALYTICS_REPRICING) has a test: a table that moves on every read is priced twice at most and served with no block. The three test assertions that read the block out of the build that started the fetch (the list below) read the landing after the join or from a second build inside the TTL, the rule now in the harness's docstring and its helper PriceFeedCase._settled; the control case was the free-threaded red in CI, and the two others were the same defect not yet fired. The minutes branch of the stderr tail's age and three legs of _price_feed_error_class (a resolver herror, an SSLError with no reason token, an errno past a C int) are executed by tests. tests/test_price_feed_floor.py pins the import-time half of the runner's floor on conftest's source (TheFloorsAreOnConftestsSource, an ast walk over tests/conftest.py for the module-level assignment and the autouse fixture's re-assert), for ROMP_PRICE_FEED and ROMP_MODEL_CATALOG under one helper, beside the per-test half it already pinned by execution; the catalog's cases sit beside the feed's there because an import into the catalog's module would load the kernel a second time. docs/reference.md's price feed subsection states the switch's condition as the code has it (only `off`, whitespace and case ignored; any other value, `0`, `false` and `no` included, leaves the feed on, and the kernel says so on stderr naming the value, on the line and in the block as the fact alone) where it read `with the variable set`; says the omitted-rate rule as _model_prices has it (a rate the row omits keeps the table's only for an id the table itself names, zero for any other id, matched by exact id and inheriting nothing), the prose branch, the base resolution unchanged and guarded by APartialOverrideRow; says what an unreadable row or file does; and says the cache keeps the rows it parsed, not matched. SECURITY.md's Network access section no longer says romp makes one outbound request by default; it is written from the table below: `By default the kernel opens one connection of its own to a host other than the model provider` (the price feed, with no credential), then the kernel's other default connections (the catalog refresh and the fast-mode probe, both to the model provider), the programs it runs by default that connect on their own (`git ls-remote` for the release and drift checks, `git ls-remote --heads origin` in a viewed file's checkout, the session and judge CLIs, one `npm install` at a failed boot rebuild), the connections that open once set up, the by-hand install scripts, a sentence that what those programs send is theirs, and the telemetry claim scoped to what the table supports (romp sends no telemetry; session text goes only to the model provider the session or the judge call is billed to and, encrypted end to end, to the push service of a phone you subscribed); the switch paragraph there also states the value rule. The round's tests, classified: of the 52 cases this round added or rewrote (50 Python across tests/test_price_feed_consistency.py, tests/test_price_feed_off.py, tests/test_price_feed_census.py, tests/test_reference_price_feed.py, tests/test_reference_price_feed_rows.py, tests/test_security_price_feed.py and tests/test_price_feed_floor.py, and 2 node in ui/webview/analytics-price-source-states.test.ts), 29 Python cases are red over the archive each names (the head the round reviewed, the ninth commit on the branch, for 28; the PR's base for LiveFeed's non-finite rate case, which is red at both) at the assertion each pins, identically on CPython 3.12.3 and free-threaded 3.14.6; 8 are mutation-proved at this head (TheTailNamesTheShare's minutes case; FailedFetch's TLS-and-resolver case, three legs; APartialOverrideRow, a guard green at both archives; the census module's three site pins, TheFeedHasOneFetchSite's url, refresh-caller and merge-caller cases; the floor module's two source pins), each mutation re-run in a scratch copy of the tree and red there, green at the tree; 12 are controls, guards or self-tests, green and saying so (the census module's anchors guard, its first-statement text twin and TheCensusIsLive's six; the three in-flight members, red as found under the recorder's 50 ms delay on both interpreters and green fixed with and without it; the narrowed override-count case in the reference module, whose moved tie is red in ThePartialRowRule); one, SayOnceLatches' second-latch case, is red before its assertion on the arm that did not exist at the archive, its docstring says so, and its lock proof is the unlocked mutant reading 2 != 1 on both interpreters; and none is left red on a symbol the fix introduced (the key-presence red of TheOverrideFileIsSaid.test_no_file_is_no_fault_and_no_line is an AssertionError at its own assertion, its subject the new key, and its docstring says so). The three in-flight members separately: the control (tests/test_price_feed_consistency.py:299 at that ninth commit) red by timing alone on 3.14t, 3 of 3 in module order, and green on 3.12; LiveFeed's override-row case (tests/test_price_feed_off.py:594) and FailedFetch's failed-refresh case (:482) green by timing in module order on both interpreters; all three red 3 of 3 on both interpreters with the 50 ms worker delay, and green fixed over the archive with and without the delay (36 of 36 member runs) and at the worktree. The two node cases are red over that ninth commit's archive at their first behavioural assertion and green at the tree, 8 of 8, in the view's own node runs. The re-ruling's tests, classified. It replaced one of those 52, TheOverrideFileIsSaid's case that a bad row voided the rows after it; rewrote four more of that class (the non-object row, the non-finite rate, the file class and the no-file case, the last two now asserting the count) and the two AnUnreadableRowOrFileIsSaid cases of tests/test_reference_price_feed_rows.py; and added six to TheOverrideFileIsSaid (the bad row first; the same row last or in the middle, three placements as subtests; two bad rows with a row that goes bad later; a long key clipped; the file class then a row; a row then the file class), one to SayOnceLatches (two builds meeting a skipped row together name it once) and two node cases to ui/webview/analytics-price-source-states.test.ts: 15 cases, 13 Python and 2 node. Every one is red for its stated reason over a git archive of the round's head with the harness fix (the eleventh commit on the branch) with the final test modules copied in, identically on CPython 3.12.3 and free-threaded CPython 3.14.6 with the GIL read off in-process: the three Python classes selected run 16 failed and 2 passed there (13 cases, the placement case's three subtests counted separately; the 2 passed are the older SayOnceLatches cases), the node file 8 pass and 2 fail. At the assertion each pins: the bad row first, `1e-05 != 1.2e-05 : a good row applies` (the archive's one try around the loop voided both good rows); the placements, last `None != 1 : the block counts the skipped row` (its table is green there by accident, a last row voiding nothing, and the archive has no overrideRowsRejected key, as its docstring says), middle `3e-06 != 4e-06 : and so does the other good row, wherever the bad row sits`, first `1e-05 != 1.2e-05 : a good row applies`, then `0 != 3 : three placements ran`; two bad rows, `1e-05 != 1.2e-05 : the good row between two bad ones applies`; the non-object row, `Tuples differ: (1.2e-05, 3e-06) != (1.2e-05, 4e-06) : both object rows apply`; the non-finite rate, `Tuples differ: (0, None) != (0, 1)`; the file class, `None != 0 : no row was read, so none was skipped: the count is 0`; the no-file case, `'overrideRowsRejected' not found in {...} : so is the count`, an AssertionError at its own assertion with the new key as its subject; the clipped key, `0 != 1` at the row line's count, the message quoting the archive's log with its line that names no row; the two class-latch cases, each at the line count of the class said second, `0 != 1 : the second class's first fault is said, on its own latch:` for the file class then a row and `0 != 1 : the file class's first fault is said, on its own latch:` for the reverse (the first class's check passes at both heads, the file line's text unchanged and the row check counting a phrase both heads' row lines carry, so each order is red at the class the archive's one latch silenced); SayOnceLatches' skipped-row case, `False is not true : the gate saw the first read of the latch` (the archive never reads the key), its lock proof the set latch's test-and-set moved outside the lock in a scratch copy of the tree, red `2 != 1 : one line per kernel life per row, whatever arrives together:` on both interpreters and green at the tree; and both reference cases, `docs/reference.md (The price feed) does not carry 'A row the kernel cannot read (not an object, or a rate that is not a finite number) is skipped and every other row applies, wherever in the file the bad row sits'`. The node cases: the count case expects the line to end `; 2 rows overridden by model-prices.json; 1 row of model-prices.json could not be read and was skipped (the rest of the file applies)` and the archive's line ends at the override count; the guards case is red at its one behavioural assertion, the line for a block with a count of 1 lacking the clause, after its zero, absent, string and boolean guards pass at both heads. At the tree (the worktree at that head with the re-ruling's edits): the seven price feed modules (tests/test_price_feed_off.py 46 cases in ten classes, tests/test_price_feed_consistency.py 17, tests/test_price_feed_census.py 11, tests/test_price_feed_floor.py 4, tests/test_price_feed_vocabulary.py 9, tests/test_reference_price_feed.py 15, tests/test_reference_price_feed_rows.py 6) run 108 passed and 29 subtests passed on each interpreter, the two harness modules 63 passed and 25 subtests in 5 of 5 runs each; under node, the four webview files that name raPriceNote: the states test 10 of 10, analytics-price-source 9 of 9, gear 20 of 20, rail-spend 18 of 18. The where: line names the 22 changed paths and the 18 top-level kernel/kernel.py definitions whose source differs from the base's, _price_feed_clip among them, 22 of 22 and 18 of 18, checked by ast over the base's file and the tree's.

**The in-flight block reads this round fixed**, a population derived by execution over an archive of the reviewed head on CPython 3.12.3 and free-threaded 3.14.6 before any fix, every read of the block in the two modules walked by ast; three members, at the archive's line numbers:
1. tests/test_price_feed_consistency.py APartialLandingIsSaid.test_control_a_landing_whose_signed_rows_all_parse_says_nothing, line 299: red-by-timing. It read (rows, matched) from the payload of the build that started the fetch and expected the landing (1, 1); with no delay added, green 16 of 16 on 3.12 and red 12 of 16 on 3.14t (3 of 3 in module order, 9 of 13 alone) as `Tuples differ: (0, 0) != (1, 1)`; a 50 ms sleep in the recorder before the worker answers reds it 3 of 3 on both interpreters with the same message.
2. tests/test_price_feed_off.py FailedFetch.test_a_failed_refresh_after_a_landed_fetch_says_the_cached_rows_still_serve, line 482: green-by-timing. It read lastError from the payload of the build that started the failing re-attempt; green 16 of 16 with no delay on each interpreter; the 50 ms delay reds it 3 of 3 on both as `AttributeError: 'NoneType' object has no attribute 'startswith'`.
3. tests/test_price_feed_off.py LiveFeed.test_an_override_row_is_counted_in_the_status_and_the_line, line 594: red-by-timing. It read (source, rows, overrides) from the payload of the build at NOW + 1 that started the case's first fetch (the earlier build ran under the switch and stamped nothing); green 16 of 16 on 3.12, on 3.14t green 3 of 3 in module order and red 9 of 13 alone as `Tuples differ: ('defaults', 0, 1) != ('feed', 1, 1)`; the 50 ms delay reds it 3 of 3 on both interpreters.
Ruled not members: the sibling reads at tests/test_price_feed_off.py line 481 and tests/test_price_feed_consistency.py line 315, on the same fetch-starting builds, read the earlier joined landing's fields (source, rows, fetchedAt, ageS, known), which a failing re-attempt cannot change, and are green under the delay 3 of 3 on both interpreters; every other block read in the two modules is from a second build inside the TTL after the join, a joined refresh, the status function after the join, a build under the switch, or a gated worker whose flight is the assertion's subject, all green under the delay in 12 of 12 module runs. Fixed (the control's counts from a second build inside the TTL after the join, the failed refresh's lastError from the status after the join, the override case from a second build at the same clock after the join), all three pass in every run over the archive with and without the delay and at the tree on both interpreters. The rule, now in the harness's docstring: any assertion about a landing or a failure reads it after the join or from a second build inside the TTL, never from the payload of the build that started the fetch.

The second review round by the fork's manager, 2026-09-21, over the whole change at the head the first round's fixes and the harness fix left, ruled 47 findings (3 high, 24 medium, 18 low, two refuted and not refiled) and changed the following, one cluster at a time. The instrument: the census is run by the suite and refuses loudly (the paragraph and the table below), where the round found it run by nothing, unable to fail on a short result (the kernel's main file moved one directory down in a scratch copy took it from 266 sites and 30 roads to 165 and 16 with exit 0 and 0 unclassified), filing a shell or node child and every browser fetch local by spelling, and folding a second site in a rowed function silently; the census's row key stays file plus enclosing function with a committed count per key, so a second site in a rowed function changes its key's count and fails the run naming the key and both counts (a line in the key would go stale on every edit above a site in kernel/kernel.py and be regenerated by habit, which accepts a new site as readily as a moved one). The override row's rate: a rate that is present and not a JSON number (null, a string, a numeric string too, a list, an object, false or true) is rejected by type into the per-row reject path, classed `row`, counted in `overrideRowsRejected` and named once on stderr, never coerced; until this round `float(x or 0)` priced such a row at zero per token, or at a dollar a token for `true`, while the block read a clean override and stderr said nothing; only an absent key inherits the table's rate; the row's stderr line names the three classes (not an object, a rate that is not a number, or a rate that is not finite) and docs/reference.md states the predicate beside the two it had (the string's arm is re-ruled in the B re-ruling, below: a quoted plain decimal is read as that number). The gate: the TTL check (the read of `_price_cache["t"]`) and its stamp take `_price_feed_lock` with the attempt's fields as one transition, so exactly one caller per TTL window proceeds and one request leaves for one stale cache; before, two cost-view builds arriving together each started a worker, two requests and two fetch lines, reproduced at the head and at the base on both interpreters by parking the first thread's read of the stamp (the earlier review's refutation of the same race, one worker in 4000 trials, was a timing observation and not a proof). The in-flight mark is a per-flight token, the attempt counter `flights` incremented under the lock, cleared under the lock only by its own flight, so the first of two flights to end leaves a later flight's mark standing; before, a shared boolean the worker's finally cleared outside the lock for every flight; the status reads the mark's truthiness, so the block's keys are unchanged. The whole-file clause: the modal's price line reads `overrideFault` and words the file class as `; model-prices.json could not be read as a JSON object and was ignored (none of it applies)` in the skipped-rows slot on both branches (the second paragraph), where a file discarded whole rendered the clean line; the age in the line comes from the webview's one age helper, agoWords, bound as raAgo (now under 45 seconds, then rounded minutes, rounded hours under a day, then days), the local copy with its own thresholds and no day arm deleted, and the kernel's stderr tail gains a day arm at 24 hours so the log and the modal agree on the unit. The section: SECURITY.md's Network access section is derived from the census's `--table` output sentence by sentence and every exhaustive quantifier the table cannot support is gone: the closing sentence names three destinations for session text (the model provider billed; a machine you attached, over your own ssh tunnels; a subscribed phone's push service, encrypted end to end) in place of an `only` over two; the set-up frame is worded to the actor, you or a session you are running, with the watch predicate as its own item (the route, the shell, the cadence floor and default, the bound, the boot re-arm); the browser's pictures road sits in the by-default group with the gear's shipped host list; the trigger is keyed on the last fetch attempt, stamped before the fetch runs, so a failed or empty fetch holds the six hours like a landed one and the first open after a start always fetches; bootstrap.pypa.io and ROMP_NO_GET_PIP are named; the release and drift checks carry their cadences and the drift verdict's three disjuncts; the derivation passage names the script, the committed counts, what the run refuses and the four classes the scan cannot derive, without counts, since the section is long-lived and this entry and the counts file carry the figures; and tests/test_security_price_feed.py TheSectionIsTheTables holds the section's road list equal to `--table` by execution, each sentence pinned to the code fact behind it. docs/reference.md says where each clause of the modal's line sits (the unrecognised clause after the source and before the override count, the count before the skipped-rows clause, only the last slot ending the line, pinned to raPriceNote's push order by index), states the rate predicate, conditions the stderr sentence on the cost view's road (the spend ceiling's check and /version price with the same file and write no line; the block carries the class whether or not the view was opened; pinned by ast on the refresh guard around both fault lines), and quotes the whole-file clause. The pins: the two OneSnapshot cases wait for the thread that asks for `_price_feed_lock` while the other holds it (a stand-in over the lock records the ask and the owner) in place of a fixed second, and prove themselves by mutation, the lock taken out of the status's snapshot or out of the landing failing them ten runs of ten on CPython 3.12 and free-threaded 3.14 with the tree green ten of ten; the floor pin is one case that pops the switch and runs conftest's own fixture function, so the re-assert executes in whatever worker the case lands in; the vocabulary pin checks the block's keys both ways, with the two keys the view leaves unworded (attemptedAt and fetchedAt) declared with their reasons, and is red over the head's archive naming overrideFault; the wiring pin executes raRender over the DOM stand-in and anchors the call's place on the call alone, so the footnote's copy can change without touching it; and the dead `rows` field of the status dict (written under the lock by every landing, read by nothing) is gone, the dict literal's keys held equal to the harness's reset keys. The population of kernel-launching harnesses that copy tests/conftest.py's network floors, derived by reading every ROMP_MODEL_CATALOG spelling outside the kernel, has three members without the price feed's floor: tools/ui-bench.mjs's bench kernel gains ROMP_PRICE_FEED=off with tests/ui-bench.test.mjs asserting it; tools/perf-bench.py (with tests/test_perf_bench.py asserting it) and ui/webview/file-comments.test.ts are the other two and take the same floor in this round. The record: the census paragraph and the table below are dated to the branch head in words and the table is the script's own output, held to it by execution; the browser row lost its two Copy image clauses because the script files that fetch local (it reads the kernel's own file URL), which is how a hand-written cell was told apart from a derived one; the Recorded, not fixed list is labelled from (a) (no item was dropped: the commit that added it recorded two follow-ons and labelled them (b) and (c)). The round's tests, classified: 55 of 67 cases this round added or rewrote across tests/test_price_feed_census.py, tests/test_price_feed_off.py, tests/test_price_feed_consistency.py, tests/test_price_feed_floor.py, tests/test_price_feed_vocabulary.py, tests/test_security_price_feed.py, tests/test_reference_price_feed.py, tests/test_reference_price_feed_rows.py, tests/ui-bench.test.mjs, ui/webview/analytics-price-source-states.test.ts, ui/webview/analytics-price-source.test.ts and ui/webview/gear.test.ts are red over the archive each names (the reviewed head's for behaviour the change introduced; the base's for the coercion, the check-then-stamp and the merge) at the assertion it pins, identically on CPython 3.12.3 and free-threaded 3.14.6 for the Python cases and under node for the rest, 6 are mutation-proved at this head, 5 are controls or guards, 1 is red at an anchor assertion whose subject is the new API (the census's ROADS literal, named as such), and none is left red on a symbol the fix introduced: the one error-shaped red, the flight token's AttributeError over the base archive, is at an archive that is not that case's archive of record, and at the reviewed head's archive it is red at its assertion. The where: line names every changed path (29 at this tree, the committed counts file, the two benches and their tests, and the dist ratchet's tree-copier allowlist naming the census module among them) and the 20 top-level kernel/kernel.py definitions whose source differs from the base's, checked by ast over the base's file and the tree's (18 after round 2; the B re-ruling adds _PRICE_RATE_STRING and _price_rate_value).

What the switch stops is one request, the price feed's; every other request romp makes goes out as before. The table below is the output of `python3 scripts/network-inventory.py --table` at the branch head, and tests/test_price_feed_census.py TheTableIsTheLedgers holds the block between its two marker lines equal to that output by execution, so no row in it is written by hand. The census is run by the suite, on every test run and so in CI's Python job on every pull request and push to main: tests/test_price_feed_census.py derives the tree once in its own process (the script's scan, figures, problems, listing and table composed in main's order from one scan, without calling main), runs its mutations over a copy of the scanned scope, alone or joined into shared runs (a site one directory down, a shell as argv[0], an absolute URL in a browser fetch, an import the census does not know, a second site inside a function that already has a row, a row naming no site, the kernel's main file moved one directory down, a shell script's inline interpreter text (python3 -c, the heredoc shape, node -e), a child_process call and a net connection in the browser and editor code, a package the browser code imports outside its allow-list, a module named to importlib.import_module outside the allow-list or at run time, a socket's sendto and the event loop's connections, a same-count swap of a local tool for curl inside a rowed function, a planted socket primitive on a receiver the scan cannot resolve that the run does not see, a row-less git checkout and a bare git with a runtime subcommand, the openExternal entry removed from the script's JavaScript list, and since the fourth round an echo-led shell line whose pipe or substitution runs a tool beside a remedy naming a tool inside its quotes, the connection family reached through a file's bindings (an inline require, a renamed destructured member, a ws default import, and a call both the literal list and the binding arm match), a dotted hook read for its shebang, the chat-media row and its pattern's lookbehind dropped, and the GitHub button's href write deleted or given an opener, and since the fifth round four echo-led lines through the substitution's backtick, subshell paren, quoted paren and backslash-escape arms, and the binding shapes that round added (a mixed default-plus-named import, a default-plus-namespace import, `import { default as X }`, TypeScript's `import X = require()` and a bound `await import()` whole and destructured, each a site) with the three shapes no pattern reads held at no site and, over `https`, the import gate reading the literal shapes and not the aliased require), applies the served pages' plants (a third-party fetch, a second socket, a second opener, a brace-led alert and an import statement in the timeline boot, a beacon in the settings page's template, a route serving a file the walk does not scan, an f-string page, a page whose fetch URL is a Python format slot and a method serving text/html from a parameter, and since the fifth round the content-type, receiver and joined-constant plants: a type read through a constant, a keyword, a mixed-case spelling or a local, an svg, XHTML or application/javascript type, a send_header route, an unreadable body, a type from a function's return, an allowlist entry left naming nothing and a new place under an entry's key; a page read through encode, a get with a default, a subscript joined with a literal, format_map, a class attribute, a mixed dict subscript, a two-deep chain, a function's or a method's return as receiver, a run-time memo and two same-named locals; a fetch after a local one in a joined constant, a constant that starts like a comment, a second import() on a line, an f-string page with a site in a later part, two identical parts on two lines and two identical constants on one line, each asserted at its own line, and the three shapes the line-keying residual names, each held at the line it is listed at) to kernel.py's text in memory and splices that pass into the tree's result in place of kernel.py's own contributions, and runs the file as the command line runs it in two cases, each in a child interpreter: over each of two tiny roots (no sites; the kernel root missing), exiting 1 with the in-process run's output byte for byte, and over a copy whose kernel.py carries exactly the served pass's plants, written by the one helper the pass reads, exiting 1 with the pass's output byte for byte, so the documented command's exit code, the tree derivation and the splice are each held against a real run. The script exits 1, naming the reason, on a site with no road, a row naming no site, no sites at all, a missing declared root, a Python file that does not parse, an import it does not know (a Python module outside its allow-list, whether imported or named to importlib.import_module or __import__, a module named there at run time, or a package the browser and editor code imports that the census does not know, KNOWN_JS_IMPORTS), a road with no table entry, a program referenced under tools/ or scripts/ that it does not name, a served route whose content type, receiver, container or text the served pass cannot read, a script-running type written outside `_send`, a function that answers outside `_send` more often than it writes a Content-Type header, or a `_send` definition that writes none and is not a named frame writer, each unless SERVED_ALLOW names it, and an allowlist entry that names nothing or covers a different number of places (SERVED), and any figure that differs from the counts committed beside it in scripts/network-inventory-expected.json (the totals and the counts by kind, by class, by road and by row key, and for the Python command sites the program head per row key), so a scan that finds fewer sites than the committed total, a file its walk stopped opening, a new HTTP client, a second site inside a function that already has a row, or a local tool swapped for a network tool inside a rowed function is a loud line and not a clean report; the moved-file case the second round reproduced by hand is a test case now, red at the archive and green at the tree, where the run exits 1 naming the stale rows, the changed per-key count and the moved file's unclassified sites. Its walk is recursive over kernel/, cli/, postal/, bin/, hooks/, ui/ and vscode-extension/src (test files, __pycache__, node_modules and symlinks skipped; the browser and editor trees read for their own kinds, so a Python fixture under ui/ is skipped by kind, while a dotted file under the Python roots outside the five extensions is read for its shebang, so a .bash or .zsh hook is scanned where until the fourth round it was skipped by kind), plus bootstrap.sh, install.sh, vscode-extension/install.sh and tools/file-comments-host.mjs, the one program the kernel starts from tools/ (the earlier walk was flat and took hooks/ as shell files only; the two files it never opened, a node hook and a CRLF fixture, have no site at this head). The pages the kernel serves and its service worker's script, from its own string constants (the dashboard shell, the seven pane pages, the token login page, the too-large page and /sw.js, with the shim, the timeline boot and the shell scripts they inline), are read from kernel.py's syntax tree and scanned as browser text keyed kernel/kernel.py plus tool, with the DOM loads counted. The routes are derived from every `_send` call and every Content-Type header written outside `_send`, in every scanned Python file. A `_send` call's content type is read through the definition it reaches: the kernel's Handler._send writes its `ctype` parameter, so the call's third argument or its `ctype=` keyword; the postal bus's writes application/json; the session host's and its transport's write a frame to a Unix socket and answer no HTTP request (FRAME_WRITERS), and any other definition that writes no Content-Type fails the run. The type is read through module constants, through a local whose every binding is read and through a dict literal's values; the part before any `;`, stripped and lower-cased, is compared with the types a browser runs script from (SCRIPT_TYPES: text/html; the XML types text/xml, application/xml, text/xsl and any type with a `+xml` suffix, image/svg+xml and application/xhtml+xml among them; and text/javascript under each name a browser takes for JavaScript, application/javascript among them). The page function of each script-running route is followed to the text it returns or inlines. In that text the served pass reads a BoolOp's operands, a method call's receiver and a subscript's container when they name a module constant or a local, the receiver of `.encode` or `.format_map` whatever it is, a class attribute the class body binds, a loop, unpacking or with target from its source, and a local container's appended or stored values; it passes over a base that carries no page text (an import, a builtin, a parameter, an except name, or a name the function binds from one of those). The run fails by name (SERVED) on a content type the pass cannot read, a script-running type written outside `_send`, a function that answers outside `_send` more often than it writes a Content-Type header, a container the module writes at run time, any other receiver or container, and a route whose text the pass cannot read, unless the served allowlist, SERVED_ALLOW, names the place by its function and expression, with the number of places the entry covers and the reason (the two answers with no body, the CORS preflight's 204 and the websocket upgrade's 101, are named there); an entry that names nothing in the run, or covers a different number of places, fails the run too. In served text every `fetch(` and `import(` on a line is read by its own argument, and no comment skip applies, since a joined constant is one line whatever it starts with. A file the page reads at run time is covered by the walk when it is a scanned kind, and a stylesheet is named, not scanned. The whole of kernel.py is not scanned as text, since a text scan misreads Python and JS concatenations (a Python method spelled like a client, a `from` inside a script split across Python literals). Over served text the import gate's statement form applies only to a line that starts with import or export (a line led by a closing brace is a multi-line import's last line in a module and any block's in a page's script), while a literal require() or import() is gated wherever it stands. Its primitives are every socket and HTTP client construction (urllib's urlopen, Request, urlretrieve and build_opener, http.client, socket.create_connection and a connect on a socket bound in the function, a socket's sendto, the event loop's sock_connect, create_connection and create_datagram_endpoint on a loop the scan resolves, ssl.wrap_socket, smtplib, asyncio's and anyio's connections, and the SDK clients whose packages spawn a CLI) and every command the kernel runs (subprocess, the os.exec and spawn families, asyncio's subprocesses, os.system and os.popen, webbrowser.open), resolved through import aliases, module constants (a constant holding a list is unwrapped, so ps, systemctl, journalctl and systemd-run read as themselves) and names bound to a primitive; every top-level module the scoped Python files import is on an allow-list, so a network library imported later is a loud line before any of its calls is, and importlib.import_module and __import__ go through the same allow-list (a literal, a module constant, or a loop or comprehension variable over one; a name spelled at run time is refused); for the shell scripts and the browser and editor code, a line scan for curl, wget, git's clone, fetch, pull, push and ls-remote, npm install, pip install, gh, ssh with any argument, npx, scp, rsync, sftp and nc, an interpreter head followed by -c, -e or a bare - (the heredoc shape), fetch, WebSocket, EventSource, Image, http.get and http.request, the child_process family through its binding (a bare exec( with none is RegExp exec and no site), net.connect, net.createConnection, tls.connect, and since the fourth round the connection family the same way as child_process: a `get` or `request` of `http` or `https`, a `connect` or `createConnection` of `net` and a `connect` of `tls` through an inline require, a name the file keeps the module under (a require or an `await import()` assigned whole in its declaration, TypeScript's `import X = require()`, a namespace or default import, alone or the two in one statement, a default beside a brace list, `import { default as X }`), or a bare name the file binds from the module by a brace list, alone or beside a default, in an import, a destructured require or a destructured `await import()` (renamed or not), and `ws` by its constructor shape (`new <binding>(`, `new <namespace>.WebSocket(`, `new (require('ws'))(`), each an added arm beside the literal spellings (`http.get(`, `net.connect(`, the bare global `new WebSocket(`), a call the literal list already names on a line counted once under the same tool; XMLHttpRequest, sendBeacon, a dynamic import() with a computed specifier, the three URL openers (`vscode.env.openExternal`, `window.open` and the service worker's `clients.openWindow`, each a site that takes a row), the chat pipeline's one line on a message's pictures before the browser fetches them (`mdImgPostPass`, the chat-media road's site, the definition in preview.ts refused by the pattern), and the one read of the figure-host setting. A program started whose far end its argv does not derive (a shell or an interpreter as argv[0], shell=True or child_process's exec, git with a subcommand the code does not spell out, an argv the code does not spell out), whether the kernel starts it, a shell script of romp's runs its text inline, or the manager or the editor extension starts it, takes an explicit row with its reason and is never set aside as local; a browser fetch is placed by its URL (a relative literal or a kernel-URL helper call is local, an absolute literal needs a row, a computed argument is its own class); and the residuals of the rules are stated: a new site whose argv is a fixed literal of a local tool (ps, scutil, the systemd tools, the notifiers and openers) or a read-only git subcommand is placed local by rule and caught as a new row key by the committed counts; the committed counts carry the program head per Python command site (row key, primitive, head), so a same-count swap of a local tool for a network tool inside a rowed function is a COUNTS line naming the key and the head, while a shell or JavaScript site is a line keyed by tool with no head figure; a head held in a shell variable ("$PY" -c) is not matched; a shell text inside a Python string literal travels as the argument of a rowed ssh or bash site, whose row's prose carries it; the served pages' rows are keyed by tool (kernel/kernel.py plus WebSocket, window.open or clients.openWindow) and counted once per line, only fetch and import() being read per match, so a second socket or opener in the served text is caught by the count per key when it changes the tool or stands on a line without its tool, and not when it joins a line or a joined constant that already carries its tool, as any rowed shell or JavaScript line is; a site in served text is listed at the first line of the string part that carries it (text joined across implicitly concatenated literals is one part, and on Python 3.10 and 3.11 an f-string part that starts on a later line is listed at the line where the expression before it ends), the count the same on every interpreter; named and not counted in the served text: a stylesheet's `url()` loads (THEME_CSS's fonts, _LOADER_CSS's face, _RDRIFT_CSS's and the dashboard shell's own rules, and the pane stylesheets under ui/webview read at run time), every one a `/media` path on the kernel's own origin, and the same-origin navigations no list names (`location.replace` on the token login page, `location.reload` in the shim and the shell, `navigator.serviceWorker.register('/sw.js')`, `history.replaceState`); and, said once in the script's docstring and in SECURITY.md and held equal there by execution: the shell and browser sides are matched by a named list with no completeness gate: a tool or a client the lists do not name is no site and no line; the Python side's gate is module-granular: an import outside the allow-list fails the run, and a primitive of a known module outside NET and SUB is not a site. Three binding shapes no pattern reads fall under that rule: a require assigned after its declaration (`let h; h = require("http")`), a `.then()` callback parameter of `import()`, and a call through an alias of `require` (`const r = require; const h = r("http")`); a call through any of them is no site and no line. The import gate reads the literal `require()` and `import()` of the first two, so in those shapes an `https`, `net` or `tls` module fails the run and the residual reaches `http`, `ws` and `child_process`, the packages the list knows; an aliased require spells no literal `require()`, so the gate does not read its module either, and that shape reaches every module. An echo- or print-led shell line is skipped as a printed remedy only when nothing live follows the printed text: the text outside quotes and the body of every `$(...)` and backtick substitution, wherever it stands, are scanned by the interpreter arm and the tool list, so `echo "$body" | curl ...` and `echo "rate: $(curl ...)"` are sites and a remedy that names a tool inside quotes is not. Four classes of outbound activity the scan cannot derive are named in the table with their counts at the branch head, and SECURITY.md's Network access section names the same four without counts: an external program started whose far end its argv does not derive (108 sites: `python3 -c` 63, an argv the code does not spell out 21, `python3 -` 12, `sys.executable` 4, `git` with a subcommand the code does not spell out 3, `bash -c` 1, `bash` 1, `node -e` 1, `sh -c` 1, `sys.executable -m` 1, each on a road by an explicit row), a program supplied at run time (2: the watch predicate, and the operator's helper or a login's token command), a browser request whose URL is computed at run time (10: four fetches in the browser code reading a kernel URL by their binding, four in the dashboard shell's scripts the kernel serves whose route literal the caller passes (fetchDoc, vact, readSwitch and post), the drift banner's per-host route (`route[h]` in _RDRIFT_JS), and the PDF worker's dynamic import of a URL derived from its own chunk's script src), and the browser DOM's own loads (103 lines carrying an attribute write, a setAttribute call or a template, in 20 files under ui and vscode-extension/src and in the pages kernel/kernel.py serves, one count per line that carries one, so a page template inlined as one string is one line however many it carries; an anchor's href write loads nothing by itself and a `window.open` is a site of its own; the viewer's and the chat's rendered-markdown insertions have no line of their own and are not counted, so the browser-figures road is named from the gate's host-list read and the retry probe rather than from a request, and the chat-media road from the pipeline's post-pass on a message's pictures (`mdImgPostPass`), for the same reason; the paint references of the chat's file preview and a notice card are a road named and not counted, below); a fifth class is named in the table and not counted, since the scan cannot see it: a socket primitive called on a receiver the census cannot resolve (an attribute-held or parameter socket) is not a site here, and the census module plants one and holds the sentence to the behaviour. Two kinds of browser request are also named in the table and not counted, since the content shown, not a line of this tree, names what they load: an inline svg's paint references in the chat's file preview and in a notice card, and the loads of an .svg opened in its own tab. Two browser roads carry prose the code alone does not derive, held to it by execution since the fourth round: the chat-media road (a rendered message's media) says that the render of a message on the web dashboard (a session's reply, your own message, a postal body) loads an image, video, audio, srcset or picture media, a video's poster and an inline svg's image from the host its URL names, with no click and no setting, with whatever cookies that browser sends to that host and no Referer to any other origin, while an inline svg's paint references load at render too, with no click: in Chromium a `fill`, `stroke`, `clip-path`, `mask`, `marker-start`, `marker-mid` or `marker-end` whose `url()` names another host loads from that host, in Firefox and WebKit at least a `mask` does, and a `filter` does in no engine; each such request carries no cookie and carries the page's origin (the dashboard's scheme, host and port) in its Origin header; in Chromium a `mask` request can also carry that origin as its Referer, from any page, framed or bare; and a paint request can carry the full page address with the serve token in its Referer, but only when the page's own address carries `?token=` (a pane page opened bare, such as `/chat?token=`; the shell drops the token from its address before it frames its panes, and frames them without it); these paint requests are the one exception to the trust model's sentence on `Referrer-Policy: same-origin` (the response is blocked as cross-origin; the request, with those headers, has reached the host), the editor extension's webviews blocking the same loads by their content security policy, the row named from the pipeline's post-pass and held by an executed witness (ui/webview/chat-media-loads-browser.test.ts: render.ts's own pipeline over a page served under the four headers the kernel's _send writes, pinned by value and tied to the wire by a case driving the kernel's Handler in a child interpreter; since the fifth round its paint scenes hold the attributes whose requests arrive equal to the list the section's clause names, filter's absence pinned, with Firefox and WebKit legs, and a file preview scene and a notice card scene in which only the paint references load); the clicked-link road's cells name the file viewer's GitHub button (an address romp composes from the checkout's owner and repository name, its branch or the commit sha when HEAD is detached, and the file's path, to github.com, held to the executed URL shapes of tests/test_file_github.py) and the gear's sign-in link by document, and its residual is a stated class, three anchors the chat page's click delegate leaves to the default action and a page-built anchor with a scheme in a document that installs no opener, one text in the docstring, the trigger cell, SECURITY.md and both test modules' constants, held by an executed pin on the settings page (ui/webview/settings-signin-link-browser.test.ts: a real click on the sign-in anchor, no window.open, no openLink post, the browser's own new tab). Since the fifth round three more sentences of the section stand here in its words. Every other text the chat page renders as markdown loads its media the same way: the text a Continue press sends, a message romp or another program sent to the session on your behalf, a note or notice the harness injected (a command's output, a scheduled task's firing), a notice from romp, a compaction summary, a background task's report, a subagent's skill text, prompt and report, a peer agent's message and an agent's reply in a file comment thread. An inline svg's paint references load with no click on three surfaces of the web dashboard: the chat's rendered markdown, when it renders; the chat's file preview, the card that opens on a pointer dwell of 350 ms or a keyboard focus on a link in the chat to a markdown file the kernel allows to preview (one in the session's folder or your home; a glossary term links to its section the same way); and a notice card's body on the feed page, which a session writes through POST `/notice` (`romp card`), when the feed paints the card. The file preview and the notice card strip an image, video, audio, a poster and an svg image before the nodes join the page, so only the paint references load there. On all three, in Chromium a `fill`, `stroke`, `clip-path`, `mask`, `marker-start`, `marker-mid` or `marker-end` whose `url()` names another host loads from that host, in Firefox and WebKit at least a `mask` does, and a `filter` does in no engine; each such request carries no cookie and carries the page's origin (the dashboard's scheme, host and port) in its Origin header; in Chromium a `mask` request can also carry that origin as its Referer, from any page, framed or bare; and a paint request can carry the full page address with the serve token in its Referer, but only when the page's own address carries `?token=` (a pane page opened bare, such as `/chat?token=`; the shell drops the token from its address before it frames its panes, and frames them without it); these paint requests are the one exception to the trust model's sentence on `Referrer-Policy: same-origin` (the response is blocked as cross-origin; the request, with those headers, has reached the host); the editor extension's webviews block these loads by their content security policy, so this is the web dashboard's alone. On the web dashboard, a Cmd, Ctrl or middle click on a path link to an .svg in a viewed file opens the file in the browser's own tab from the kernel's `/file` route (Cmd or Ctrl with Enter or Space on a focused link does the same; for a file of a session on an attached machine the tab opens from the `/remote/<host>/file` relay, which sends the same headers), and the tab is an svg document, sandboxed so no script runs in it, that loads each resource its markup names from that resource's host, whether or not the host is on the gear's Pictures from the web in files list: an image, a CSS `@import`, an `xml-stylesheet` instruction, an `feImage`, HTML inside a `foreignObject` (an `img`, a stylesheet, a frame, a video, audio, an object or an embed), a cursor image, a web font, and paint references: in Chromium a `fill`, `stroke`, `clip-path`, `mask`, `marker-start`, `marker-mid` or `marker-end` whose `url()` names another host loads from that host, in Firefox and WebKit at least a `mask` does, and a `filter` does in no engine; a load the browser makes without CORS (an image, a stylesheet, a frame, a media element or an embedded object) carries whatever cookies that browser sends cross-site to that host, a paint reference or a web font (a CORS request) carries none, and none carries a Referer or a serve token. At the branch head the run's summary line, measured from the tree, reads `--- 399 sites, 32 roads (5 of them local), 202 local sites set aside, 0 unclassified; 108 external-program, 2 runtime-program, 10 browser-computed-url, 103 browser-dom-loads; 239 files scanned, 1112 skipped by kind`; a run takes about 4 s on the box (3.6 s at this tree against 3.5 s for the reviewed head's script over its own tree, medians of three paired runs under a load average of about 16; the fourth round measured 3.3 s at its tree, 2.6 s before its served-page pass and binding arm). `python3 scripts/network-inventory.py` from the repository root (an optional argument names another root) prints every site as one line (file and line, primitive, program or target, enclosing function, road, and the class where one applies), `--table` prints the table below, and `--write-expected` rewrites the committed counts from a clean run and is refused while any other gate fails, so a count that moves is a one-line diff a reviewer reads. The roads, by class (kernel-request: the kernel's own connection; kernel-runs-a-command: a program the kernel starts; session-or-judge-cli; bus-request; browser: a viewed file's pictures, a rendered message's media and a link you click; install-time-by-hand; the .svg tab row, named and not counted; local; and, last, the four counted classes the scan cannot derive, the row for the paint references of the chat's file preview and a notice card, named and not counted, and the fifth class it names and does not count):

<!-- network-inventory: table begin -->

| road | where | trigger and cadence | what is sent and to where | off switch |
|---|---|---|---|---|
| price feed (kernel-request) | kernel/kernel.py `_refresh_remote_prices.work` | an open of the Token usage view (`_token_analytics` with refresh) when the last fetch attempt is older than `PRICE_TTL`, six hours, or there has been none this kernel life; never at boot; nothing re-arms it | GET of the public LiteLLM price table on raw.githubusercontent.com, no credential | `ROMP_PRICE_FEED=off` |
| model catalog refresh (kernel-request) | kernel/kernel.py `_fetch_models_api` | through `_refresh_model_catalog`, from `_model_catalog_boot` and `_note_unknown_model`: the first boot with no catalog cache file; after that once per unknown `claude-*` model id per kernel life (a set path or a pick names an id the merged list lacks); single-flight, up to ten pages | GET `/v1/models` on api.anthropic.com (`ROMP_MODELS_URL` overrides) with the apiKeyHelper's key, else the `ANTHROPIC_AUTH_TOKEN` bearer claimed at startup; a box with neither sends nothing | `ROMP_MODEL_CATALOG=off` |
| fast-mode organisation probe (kernel-request) | kernel/sdk_backend.py `_fetch_key_fast_org` | through `key_fast_org_env` and `helper_fast_org_env`, from `SdkSession._options` (every connect compose) and kernel/judge.py `_fast_org_env`: every key-billed connect of a session (a launch, a resume, a reconnect, the reconnect a fast toggle makes) when the operator's apiKeyHelper is configured and the project's own settings name no other helper: the fetch runs first and the memo spares only a failed one; the judges ask once per judge process (the kernel, or the `romp-judge --serve` child when `STATE/judges-process` reads on) for a key-billed call whose tier's Fast-mode box is on with a fast-capable (Opus family) model, and ask again after any CLI fast refusal drops the memo | GET `/api/claude_code_penguin_mode` on api.anthropic.com (`ANTHROPIC_BASE_URL`) with the helper's key, three-second bound | none of its own: no helper, no probe; a login-billed session, no probe |
| web push (kernel-request) | kernel/kernel.py `_push_post` | through `_push_send_one` and `_push_notify` (a card needing you, a completion, a turn end, a relayed peer event) and the `/push/test` route: every notification event while `push-subscriptions.json` holds a row, one POST per subscription; a 404 or 410 removes the row | an aes128gcm-encrypted payload (title, body, routing) to each subscription's endpoint, the push service of the subscribed phone or browser (Apple, Google, Mozilla), signed with the kernel's own VAPID key; the service sees ciphertext | none in the environment; unsubscribe on the device (the bell), which removes the row |
| release check (kernel-runs-a-command) | kernel/kernel.py `_latest_release_tag` | through `_update_check` and `_update_check_loop`: at boot and every six hours (`_UPDATE_CHECK_EVERY_S`) on a clone with a VERSION file, in update modes ask (the default) and auto; re-armed at every boot | `git ls-remote --tags <release remote>`: `upstream` when the clone has one, else `origin`, GitHub for a bootstrap install; git's own credential if the remote needs one | `ROMP_UPDATE_CHECK=off`, or the gear's update mode off |
| main drift check (kernel-runs-a-command) | kernel/kernel.py `_origin_main_sha` | through `_main_drift_check` and `_update_check_loop`, behind the audience gate `_main_channel_verdict`: every 300 s (`_MAIN_CHECK_EVERY_S`) when the clone is main-tracking: on branch main, or update mode auto, or with an attached or remembered machine; re-armed at boot | `git ls-remote <release remote> refs/heads/main` | `ROMP_UPDATE_CHECK=off`, or update mode off |
| self-update to a release (kernel-runs-a-command) | kernel/kernel.py `_run_update` | a bash script: the update banner's Update click, or update mode auto when a newer release tag is found, once per tag | `git fetch <release remote> refs/tags/<tag>`, then `./install.sh`, which runs bin/romp-sdk-setup (pip against PyPI or pip's configured index; get-pip.py from bootstrap.pypa.io when the python lacks ensurepip) and vscode-extension/install.sh (`npm install` against the npm registry on every run, and `npx --yes @vscode/vsce package`, a fetch of vsce from the same registry even when it is cached, only when an editor CLI is present or `ROMP_EXT_PACKAGE_ONLY` is set) unless `ROMP_NO_SDK` or `ROMP_NO_EXT` is set; then a restart request to the manager on the loopback | update mode ask (the default) runs it only on a click; `ROMP_UPDATE_CHECK=off` stops the discovery |
| main converge (kernel-runs-a-command) | kernel/kernel.py `_run_main_update` | kind pull: the drift banner's Update click, or update mode auto, on a main-tracking clone whose main moved | `git fetch <release remote> main`; the rest is local (status, merge-base, checkout, `node esbuild.js`, a restart request to the manager) | update mode off, or ask (a click only); `ROMP_UPDATE_CHECK=off` |
| PR watch (kernel-runs-a-command) | kernel/kernel.py `_pr_watch_read` | through `_pr_watch_tick` in the tunnel supervisor pass: per registered row (`romp watch-pr`, POST `/watch-pr`) every 60 s (`PR_WATCH_EVERY`; 90 s while checks run), re-armed at boot from `pr-watches.json`, retired on merge, close or three consecutive gh failures; a failed check holds the watch and keeps polling | `gh pr view <n> --repo <owner/repo> --json state,statusCheckRollup` to GitHub's API on gh's own token | none: no registration, no poll; a cancel retires the row |
| watch-pr registration (a romp CLI verb a session or the user runs, not the kernel) | bin/romp (`gh`) | the `watch-pr` verb: `romp watch-pr` given no `--repo`, once per registration | `gh repo view --json nameWithOwner` to GitHub's API on gh's own token | none; `--repo` skips the call |
| file viewer origin check (kernel-runs-a-command) | kernel/kernel.py `_git_net_out` | through `_origin_has_branch` and `_file_github_link`: a viewer open of a file in a git checkout whose current branch has no local tracking ref under origin; a no is remembered while the clone's refspec tracks the branch, until the ref appears; anything else is asked again per open; three-second bound; every credential prompt closed | `git ls-remote --heads origin refs/heads/<branch>` to the checkout's origin (GitHub for a GitHub-hosted file); a configured credential helper may answer, askpass is refused | none |
| predicate watch (kernel-runs-a-command) | kernel/kernel.py `_watch_run` | through `_watch_tick` in the supervisor pass: per registered row (`romp watch`, POST `/watch`) every `every` seconds (floor 15, default 60) until exit 0, the timeout (24 h by default; a longer caller bound stands, a shorter one re-arms through the default) or a cancel; re-armed at boot from `watches.json`; each run bounded to 45 s | whatever the registered command sends: it runs as `/bin/sh <scratch file holding the text>`, so the program is RUNTIME-SUPPLIED and this scan sees the shell and nothing else | none |
| ssh tunnels and everything over them (kernel-runs-a-command, and kernel-request over the forward) | kernel/kernel.py `Handler._relay_download`, `Handler._remote_api_health`, `Handler._remote_control`, `Handler._remote_file`, `Handler._remote_sessions`, `Handler._remote_ws`, `_checkin_handshake`, `_checkin_stop_hub`, `_peer_call`, `_peer_spend_call`, `_poll_remote_api_health`, `_poll_remote_sessions`, `_poll_remote_usage`, `_poll_remote_version`, `_poll_remote_views`, `_remote_forward_answer`, `_remote_kernel_call`, `_spawn_tunnel` | `_spawn_tunnel` (`_tunnel_argv`) on an attach (the dashboard or `romp`), re-attach at boot from `remotes.json`, a redial of a dead ssh on a backoff ladder, every tunnel dropped and redialed on a route change; over the forward the supervisor polls each attached host's kernel every 15 s (`SUPERVISOR_PASS_S`; 0.25 s while a row is in transition), and the browser's `/remote/<host>/` requests and websocket are relayed over it on demand | `ssh -N -T` (BatchMode, no multiplexing) with `-L` forwards to the remote kernel and bus, plus `-R` forwards for a check-in; over it HTTP to the remote kernel with that machine's own serve token, and the postal peering | none in the environment; detach the host |
| one-shot ssh commands on an attached host (kernel-runs-a-command) | kernel/kernel.py `_discover_remote_clone`, `_fetch_remote_token`, `_host_reachable`, `_open_folder_remote`, `_remote_kernel_up`, `_restart_remote_kernel`, `_start_remote_kernel`, `_update_remote` | an attach and the boot re-attach (the serve-token read; the kernel-port probe and the remote start when that kernel is down), a dial's death (the reachability probe), the Update, Pull and Restart actions on a remote row (clone discovery, the push, the guarded apply, the guarded restart), an automatic push of this machine's build when the gear's auto-update remotes is on (off by default), and a remote session's folder click (a terminal running `ssh -t`) | one ssh command per call on the attached host (a `cat` of its serve-token file, a `/dev/tcp` port probe, `nohup romp-serve`, `git rev-parse` and `git status` in its clone, a reset-and-restart script), and `git push --force` of the committed HEAD to a scratch ref in the remote clone over `GIT_SSH_COMMAND` | none; the automatic push is a gear setting, off by default |
| git fetch from an attached checkout (kernel-runs-a-command) | kernel/kernel.py `_pull_remote` | the Pull action on a remote row (`/tunnels/pull`, the sync-pull action) | `git fetch <host>:<remote clone dir> HEAD` over ssh, then a local fast-forward | none |
| npm install retry at boot (kernel-runs-a-command) | kernel/kernel.py `_ensure_bundles` | at boot when the served bundles are older than their sources, `node_modules` exists and the `node esbuild.js` build fails: one `npm install --no-audit --no-fund`, then one rebuild | package downloads from npm's configured registry | none (`ROMP_EXT_DEV_BUILD` changes the build profile only; no `node_modules`, no build) |
| the operator's own commands: the apiKeyHelper and a stored login's token command (kernel-runs-a-command) | kernel/credentials.py `run_helper` | through `helper_key` (callers kernel/kernel.py `_models_api_credential`, kernel/sdk_backend.py `helper_fast_org_env`, kernel/judge.py `_fast_org_env`) and through kernel/logins.py `token_value` (callers kernel/sdk_backend.py `SdkSession._options`, kernel/judge.py `_judge_env`): the helper, from Claude Code's managed or user settings (never a project's), runs through `/bin/sh` whenever the kernel needs the key and its memo is past the TTL (`CLAUDE_CODE_API_KEY_HELPER_TTL_MS`, five minutes by default): the catalog refresh and the fast-mode probe, so at key-billed connects and the judges' once-per-process ask; a stored login's token command runs with no memo at every launch, resume or reconnect of a session billed to that login and at every judge call billed to it, on the login-billed side | whatever those programs send (a password manager's or a vault's read), on their own credentials, from a whitelisted environment, stdin closed, stderr discarded | none of romp's: no helper configured and no stored login, nothing runs |
| the session CLIs (session-or-judge-cli) | kernel/codex_backend.py `CodexBackend._get_client`; kernel/kernel.py `_aprobe_commands`, `_login_start`; kernel/sdk_backend.py `SdkBackend._spawn_host`, `SdkSession._amain`; kernel/session_host.py `PipeCliTransport.connect`, `SessionHost._spawn` | `SessionHost._spawn` (the SDK's `SubprocessCLITransport`, a class outside this tree) and `PipeCliTransport.connect` (the SDK-less test transport); `SdkBackend._spawn_host` (bin/romp-session-host) and `SdkSession._amain` (`ClaudeSDKClient`; with hosts off the SDK spawns the CLI in the kernel process); `_aprobe_commands` (the slash-command probe) and `_login_start` (the login flow); `CodexBackend._get_client` (`CodexClient`, the `codex app-server`): a session's launch, resume and reconnect (hosts on by default: one host process per session starts the CLI); the composer's slash-command list for a cwd not probed in 300 s starts a CLI with no prompt; the Billing flyout's login action starts the CLI's OAuth flow; a Codex session's first use starts one app-server per backend | the sessions' own model calls to Anthropic (or `ANTHROPIC_BASE_URL`) on the session's billing (the machine's login, a stored login's token, or the key the CLI resolves through the apiKeyHelper), plus whatever the CLI does on its own; a Codex session's calls to OpenAI on its login | none: running them is what romp is for |
| the judges' CLI (session-or-judge-cli) | kernel/judge.py `_judge_run_impl`; kernel/kernel.py `_JudgeChild._start` | `_judge_run_impl` (`perl` alarm around `claude -p`, or `codex exec`); `_JudgeChild._start` (`bin/romp-judge --serve`, the judges' process when `STATE/judges-process` reads on): every judge call (triage, index, distill and the rest) on session activity, one CLI run per call | the judge prompt (session excerpts) to Anthropic on the call's billing (the machine's login tokens, a stored login's token, or the key the CLI resolves through the apiKeyHelper), or to OpenAI through `codex exec` when `STATE/judge-engine` reads codex | none in the environment; the engine setting picks the CLI |
| the postal bus to peer buses (bus-request) | postal/postal_service.py `_peer_http` | through `_peer_exchange_once` and `_peer_loop`: one long-poll exchange per up peer, re-issued as each returns (backoff to 30 s on errors), while the kernel reports the peer's tunnel up | POST `/peer-exchange` (mail relays, presence) to the peer's bus through the tunnel's local forward, with the peer machine's serve token | `ROMP_POSTAL_PEERS=0` (the legacy singleton mode, where the bus is reached over an `-R` forward instead) |
| a viewed file's pictures from the web (browser) | ui/webview/figure-gate.ts (`figureHosts`); ui/webview/preview.ts (`Image`) | a viewed markdown file's pictures and clips whose host is on the gear's Pictures from the web in files list load when the file opens (the default list, ui/webview/settings.ts `FIGURE_HOSTS_DEFAULT`: github.com, raw.githubusercontent.com and the other GitHub image and asset hosts, localhost, 127.0.0.1); a figure from another host loads on one click; the viewer's retry (`probeMdImgUrl`) probes a failed figure's URL | GET of the figure's URL from the browser showing the dashboard, with that browser's own cookies for the host; the loads themselves are DOM insertions this scan cannot see (the browser-dom-loads row), so this road is named from the gate's host-list read and the retry probe, not from a request | the setting (remove the hosts) |
| a rendered message's media (browser) | ui/webview/render.ts (`mdImgPostPass`) | the render of a message on the web dashboard, and nothing else: no click, no gate, no setting; every text the chat page renders as markdown through `md` or `userMd` (ui/webview/render.ts): a session's reply (`md`, from the assistant branch of `renderEventInner`), your own message (`userMd`, from its user branch and from `renderQueued`'s echo of a pending send), the other texts that branch and that echo show (a Continue press, a message romp or another program sent to the session on your behalf, a note the harness injected such as a command's output, a notice from romp: `md` in `renderEventInner` and `renderQueued`), a compaction summary (`renderCompact`), an injected notice and a background task's report (`renderInjected`, `renderAgentNotif`), a subagent's skill text, prompt and report (`renderTool`), a postal body (`md` in `renderPostalService`), a peer agent's message (`renderTeammate`) and an agent's reply in a file comment thread (`commentMsgEl`) go through marked and the shared sanitizer (ui/webview/md-sanitize.ts), whose html profile keeps `img` (src, srcset), `video` (src, poster), `audio`, `source` and `picture` (and an inline svg's `image`), and are written into the page with `innerHTML`, at which point the browser requests every one; `mdImgPostPass`, the row's site, is the pipeline's one line on a message's pictures before the browser fetches them (a URL that failed this page life is parked, and re-probed by the browser-figures row's `Image` site on a reconnect); the editor extension's webviews block these loads by their CSP (`img-src` the webview's own resource origin and `data:`, no `media-src` under `default-src 'none'`), so this road is the web dashboard's alone | GET of each media URL as the message's author wrote it (a session, you or a peer, a class this scan cannot bound), from the browser showing the dashboard to the host the URL names, with whatever cookies that browser sends to that host and no Referer to any other origin (every page the kernel serves carries `Referrer-Policy: same-origin`); an inline svg's paint references load at render too, with no click: in Chromium a `fill`, `stroke`, `clip-path`, `mask`, `marker-start`, `marker-mid` or `marker-end` whose `url()` names another host loads from that host, in Firefox and WebKit at least a `mask` does, and a `filter` does in no engine; each such request carries no cookie and carries the page's origin (the dashboard's scheme, host and port) in its Origin header; in Chromium a `mask` request can also carry that origin as its Referer, from any page, framed or bare; and a paint request can carry the full page address with the serve token in its Referer, but only when the page's own address carries `?token=` (a pane page opened bare, such as `/chat?token=`; the shell drops the token from its address before it frames its panes, and frames them without it); these paint requests are the one exception to the trust model's sentence on `Referrer-Policy: same-origin` (the response is blocked as cross-origin; the request, with those headers, has reached the host) | none: no setting gates a message's media (the gear's Pictures from the web in files list gates a viewed file's figures, not the chat's) |
| a link you click (browser) | ui/webview/file-view.ts (`window.open`); ui/webview/link-opener.ts (`window.open`); ui/webview/render.ts (`window.open`); vscode-extension/src/extension.ts (`openExternal`) | your click, and nothing else: in the browser showing the dashboard, the chat page's click delegate (`window.open` in ui/webview/render.ts) opens every anchor with a scheme, a link in a session's reply or in your own message, a whole-backtick URL, a URL in a todo's text or a pinned note, the address a todo carries, a pull-request reference; the shared opener (ui/webview/link-opener.ts, installed by the feed, the outline and the Waiting-on-you panes) opens a pull-request or URL link on a pointer release or an Enter; the file viewer (`openUrlTab` in ui/webview/file-view.ts) opens a URL in a viewed file's text on a modified click (Ctrl, Cmd or the middle button; a plain click is left to the browser's own open of the anchor); the file viewer's GitHub button (`GitHub ↗`, an anchor of class `fileview-btn` in the title bar's `fileview-gh` span, rowed once the owning kernel's `fileGitLink` reply carries a URL) opens as the document it stands in decides: on the chat page the click delegate takes it as it takes every anchor with a scheme (`window.open`, the anchor's default action cancelled); on the Files pane, the feed page and the Waiting-on-you page the anchor's own default open (target `_blank`, rel `noopener`: the browser's new tab), since no opener in those documents matches it (the viewer's own `linkOf` returns only `[data-act="openpath"]`, `a.fv-url` and `a.fv-frag` inside the body, and the button stands in the title bar; the shared opener serves `a.pr-link` and `a.url-link`), so no modified-click path reaches it (`openUrlTab` runs only for a link `linkOf` returns) and a middle click is the browser's own on every page; the viewer mounts in no editor webview (a file click there opens the file in the editor), so the button has no editor leg; the gear's sign-in link, an anchor the gear builds to open in a new tab (`a.target = '_blank'` in ui/webview/gear.js), opens by document: on the dashboard's settings page (/settings), which installs no opener, the browser's own open in a new tab, no site of this tree running on the click; in the editor extension's chat panel, which mounts the gear, the chat delegate's `openLink` post; in the editor's feed panel, which mounts the gear too and installs only the pull-request opener, the webview host's own link handling, outside this tree; in the editor extension the chat delegate, the shared opener and the file viewer post the href to the host, whose `openLink` (vscode-extension/src/extension.ts, from the chat panel's handler and from `routeViewMessage`'s `openLinkLocally` for the feed panel, the outline panel and view and the timeline view) hands it to `vscode.env.openExternal`, the extension's own `vscode://romp.romp-chat-view` deep link handled in the extension instead and never reaching a browser; every opener sits in a click or pointer-release handler, no automatic step; three anchors the chat page's click delegate leaves to the default action (one with no scheme that the page built; one with no scheme in a message that does not resolve to an http or https address; a message's own download anchor, one with no scheme carrying a `download` attribute whose href resolves to an http or https address on this page's origin, which the browser saves from this origin), and a page-built anchor with a scheme in a document that installs no opener (the gear's sign-in link on the dashboard's settings page and in the editor's feed panel is one), are gestures this tree does not route: on the dashboard the browser's own open or download, and what the editor's own webview host does with them is outside this tree | the clicked URL, as the content carries it, to the host that URL names, requested by the browser showing the dashboard (a new tab, `noopener,noreferrer`) or, from the editor extension, by the operating system's default browser, with that browser's own cookies for the host: a pull-request link carries the session's repository name (from the checkout's origin remote, or the text's own owner/repo) and the number, to github.com; the file viewer's GitHub button carries an address romp composes (`_file_github_link` in kernel/kernel.py, run by the kernel that owns the file, once per viewer open): `https://github.com/<owner>/<repository>/blob/<branch or sha>/<path>`, the checkout's owner and repository name read from its origin remote, its current branch, or the commit sha when HEAD is detached, and the file's path inside the checkout, each segment percent-encoded and slashes kept, to github.com, with that browser's own cookies for github.com and no Referer (the chat page's opener passes `noreferrer`; every page the kernel serves carries `Referrer-Policy: same-origin`); the button is not rowed, and no address composed, for an untracked, staged-only or uncommitted file, a path outside a git checkout, a checkout with no origin remote or with one not on github.com, or a relative path with no session directory to place it; no query string is ever added; a link in a message, a todo, a pinned note or a viewed file is whatever its author wrote, a session, you or a peer, a class this scan cannot bound; the gear's sign-in link is the CLI's own OAuth request to claude.com or claude.ai; romp adds no serve token, key or login token to a link's URL (a link is built from content or the checkout, never from the page's address; a paint request's Referer, in the chat-media row and the row for the chat's file preview and a notice card, can carry the full page address with the serve token, but only when the page's own address carries `?token=`) | none; nothing sends until you click |
| bootstrap.sh (install-time-by-hand) | bootstrap.sh (`curl`, `git clone`, `git fetch`, `git pull`) | by hand: the documented one-liner, and re-runs | `curl` of bootstrap.sh from raw.githubusercontent.com (the one-liner itself), `git clone` of the repository (`ROMP_REPO`; github.com by default), on an existing clone `git fetch --tags origin` then `git pull --ff-only`, then `./install.sh` | none |
| bin/romp-sdk-setup (install-time-by-hand; also run by the kernel's self-update through install.sh) | bin/romp-sdk-setup (`curl`, `pip install`, `wget`) | the `fetch` helper and the pip lines; install.sh runs it unless `ROMP_NO_SDK=1`: by hand at install, and at the kernel's self-update | get-pip.py from bootstrap.pypa.io only when the python lacks ensurepip (`ROMP_GET_PIP_URL` overrides); `pip install --upgrade pip`, `pip install claude-agent-sdk==<pin>`, `pip install --upgrade cryptography` from pip's configured index, PyPI by default | `ROMP_NO_SDK=1` skips the script; `ROMP_NO_GET_PIP=1` skips the bootstrap fetch |
| vscode-extension/install.sh (install-time-by-hand; also run by the kernel's self-update and by the editor extension's update prompt) | vscode-extension/install.sh (`node -e`, `npm install`, `npx`); vscode-extension/src/extension.ts (`install.sh`) | `npm install` on every run; `node -e` and `npx` only when an editor CLI is present (`code`, `code-insiders`, `cursor` or `codium` on PATH, or an editor bundle under `ROMP_EDITOR_APPS`) or `ROMP_EXT_PACKAGE_ONLY` is set, the script exiting after the build otherwise; install.sh runs it unless `ROMP_NO_EXT=1`; the editor extension's `runInstall` (`execFile bash`): by hand at install; the kernel's self-update; the editor extension's update prompt, on the user's click | `npm install` against npm's configured registry, then a local `node esbuild.js`; behind the editor-CLI gate, a local `node -e` that stamps package.json's version, `npx --yes @vscode/vsce package`, which asks npm's configured registry for vsce even when it is cached, and a local `code --install-extension`; with no editor CLI and `ROMP_EXT_PACKAGE_ONLY` unset nothing after `npm install` is sent | `ROMP_NO_EXT=1` for install.sh |
| bin/romp-codex-setup (install-time-by-hand) | bin/romp-codex-setup (`pip install`); kernel/codex_runtime.py `install_runtime` | the setup script, and kernel/codex_runtime.py `install_runtime`, which the setup runs as a script (the kernel only looks the runtime up: kernel/codex_backend.py `runtime_path`; `ensure_codex_sdk` installs nothing): by hand | `pip --isolated install openai-codex==<pin>` from PyPI (no get-pip fetch: the script refuses an unverified bootstrap); the pinned Codex CLI wheel from github.com's release downloads, hash-checked, with `--no-index` | none |
| an .svg opened in its own tab (browser) | not counted: the loads are the opened document's own, named by the file's markup, and have no line in this tree; the tab is opened by `openFileTab` in ui/webview/preview.ts (a `window.open` of `fileUrl(path, sid)`), a site counted on the local-kernel road because the URL it opens is the kernel's own; image/svg+xml is the one type /file serves a file's bytes under that a browser tab opens as a document (the `_media_policy_headers` docstring in kernel/kernel.py), so an .svg is the whole population | on the web dashboard only: a Cmd, Ctrl or middle click on a path link to an .svg in a viewed file (`data-act="openpath"` in ui/webview/file-view.ts: a markdown link such as `[diagram](diagram.svg)`, or a bare path in a viewed text file), or Cmd or Ctrl with Enter or Space on a focused one (`pathLinkKey` in ui/webview/path-links.ts), reaches `openFileTab`, which opens the kernel's `/file` URL in the browser's own tab with no check of the file's kind; for a file of a session on an attached machine the URL is the `/remote/<host>/file` relay, which sends the same headers (read on the wire from a second kernel on this machine standing in for the attached one; the ssh tunnel itself was not exercised); a plain click shows the svg in an image at an object URL, which loads nothing from another host, and the editor extension has no such tab (`canPreview` is false in its webviews) | the tab is an svg document, sandboxed by the kernel's `Content-Security-Policy: sandbox` so no script runs in it, that loads every resource its own markup names from the host each names, outside the gear's Pictures from the web in files list: an `image` element's `href` or `xlink:href`, a CSS `@import`, an `xml-stylesheet` instruction, an `feImage`, HTML inside a `foreignObject` (an `img`, a stylesheet, a frame, a video, audio, an object or an embed), a cursor image, a web font, and paint references: in Chromium a `fill`, `stroke`, `clip-path`, `mask`, `marker-start`, `marker-mid` or `marker-end` whose `url()` names another host loads from that host, in Firefox and WebKit at least a `mask` does, and a `filter` does in no engine; a `use` that names another host loads in no engine; a load the browser makes without CORS (an image, a stylesheet, a frame, a media element or an embedded object) carries whatever cookies that browser sends cross-site to that host, a paint reference or a web font (a CORS request) carries none, and none carries a Referer or a serve token (a `SameSite=None` cookie, not a Lax or Strict one; the sandbox gives the document an opaque origin, and it is the sandbox, not the page's `Referrer-Policy`, that withholds the Referer: with the sandbox removed a `mask` request carried the page's origin; the tab's URL carries the path and the session id, never the token); a frame the markup embeds is a page from that host, which loads what that page names in turn | none: no setting gates it (the gear's Pictures from the web in files list gates a viewed markdown file's figures in the viewer, not an opened tab) |
| local, set aside and counted (local) | 202 sites on the 5 local roads (local-bus 9, local-git 21, local-kernel 143, local-manager 7, local-program 22), each a connection to this machine or a fixed program with no network use of its own: the kernel to the manager, the postal bus and itself; the bus, bin/romp's curls, cli/*, the installed hooks, the VS Code extension and the manager to the kernel on 127.0.0.1; the browser's fetch and websocket to the kernel's own origin (a relative URL or a kernel-URL helper), the served pages' own scripts included (the shim's and the shell's sockets to location.host, the shell's fetches of its own routes, the service worker's open of the kernel's own push URL, and the timeline boot's window.open, which no caller reaches at this head), and its own-tab open of a file the kernel serves (`fileUrl`; the loads an .svg opened that way makes are the .svg tab row's); the SDK transport's connection to a session host's Unix socket; git read-only queries, ps, scutil and the systemd tools spelled out in the argv; and `_primary_addr`'s UDP connect to TEST-NET-1, which sends no packet. A program on a local road whose far end this scan cannot derive is counted in the external-program row, never here | as the kernel, the CLIs and the browser run | nothing is sent to another host | not applicable |
| an external program started whose far end its arguments do not show (not derivable by this scan) | 108 sites, by program: `python3 -c` 63, an argv the code does not spell out 21, `python3 -` 12, `sys.executable` 4, `git` with a subcommand the code does not spell out 3, `bash -c` 1, `bash` 1, `node -e` 1, `sh -c` 1, `sys.executable -m` 1 | as the roads above run: every site of this class sits on a road by an explicit row with its reason, and is never set aside as local | whatever the program sends: a shell, node, perl or python runs a program text this scan does not read, started by the kernel, by a shell script inline (`python3 -c`, `python3 -` with the text on stdin, `node -e`) or by the manager or the editor extension (child_process); `shell=True` and child_process's exec run the configured command; git with a subcommand the code does not spell out and an argv the code does not spell out (RUNTIME-SUPPLIED) name their program at run time | the road's own switch; none for the class |
| a program supplied at run time (not derivable by this scan) | kernel/credentials.py `run_helper`; kernel/kernel.py `_watch_run` (2 sites) | as the predicate watch and the operator's own commands rows run | whatever the registered text sends: the predicate as `/bin/sh <scratch file holding the text>`, the helper or token command through the shell | none |
| a browser request whose URL is computed at run time (not derivable by this scan) | 10 sites: kernel/kernel.py `fetch(u)`; kernel/kernel.py `fetch(path)`; kernel/kernel.py `fetch(url)`; kernel/kernel.py `fetch(path)`; kernel/kernel.py `fetch(route[h]||'/tunnels/update')`; ui/webview/file-view.ts `fetch(href)`; ui/webview/pdf-chunk.ts `import(${JSON.stringify(target.href)})`; ui/webview/preview.ts `fetch(src)`; ui/webview/preview.ts `fetch(url)`; ui/webview/render.ts `fetch(url)` | as the dashboard runs: a fetch whose first argument is a variable, or a dynamic import of a computed module URL; at this head each reads a kernel URL by its binding (a `same-origin` mode, a `fileUrl` or `kernelUrl` result, the PDF worker's URL derived from its own chunk's script src), and in the dashboard shell's scripts a route literal its caller passes (fetchDoc, vact, readSwitch and post in kernel/kernel.py) and the drift banner's per-host route (`route[h]` in _RDRIFT_JS, '/tunnels/askpull' or '/tunnels/update' by the host's row), which the scan cannot derive from the line | to the URL the variable holds at run time; the kernel's own origin at this head by reading the bindings (the editor's webview reaches the same served bundles by its resource URL) | not applicable |
| the browser DOM's own loads (not derivable by this scan) | 103 lines in 20 files under ui and vscode-extension/src and in the pages kernel/kernel.py serves (attribute write 42, setAttribute 11, template 50) | as the dashboard and the editor views render: an element that loads (an image, a frame, a script, a stylesheet) loads its URL when the attribute lands, and an anchor's href waits for a click; the viewer's and the chat's rendered-markdown insertions load their figures with no attribute line at all and are not counted | to the URL written: kernel URLs (`fileUrl`, `mediaSrc`, `/media`), object URLs and editor webview URIs, and for a viewed file's figures the hosts the browser-figures road names, and for a rendered message's media the host its URL names (the chat-media road), and for an inline svg's paint references in the chat's file preview and a notice card the hosts they name (that row, named and not counted); an anchor's href write loads nothing by itself, the click that opens it is the clicked-link road, and a `window.open` is a site of its own (that road, or a local row), not a load counted here | the figure-host setting for figures; not applicable otherwise |
| an inline svg's paint references in the chat's file preview and a notice card (browser) | not counted: two surfaces render markdown through the shared sanitizer (ui/webview/md-sanitize.ts) and then `stripRemoteLoads` (ui/webview/file-preview.ts), which removes an element whose src, srcset, poster or data, or an svg image's, use's or feimage's href, names another origin, and reads no paint attribute: the chat's file preview (`previewMdClean` in ui/webview/render.ts) and a notice card's body (`noticeBodyNodes` in ui/webview/feed.ts); their loads are rendered-markdown insertions with no attribute line, so this scan cannot count them, and tests/test_security_price_feed.py places every caller of `stripRemoteLoads` by grep (these two, and the provider slot in `renderFilePreview`, which no producer fills at this head), so a new surface on the strip is red by name | the chat's file preview of a markdown file: a pointer dwell of 350 ms (`PREVIEW_DWELL_MS`) or a keyboard focus on a link in the chat to a markdown file the kernel allows to preview (one in the session's folder or your home; a glossary term links to its section the same way); a notice card's body: the feed page painting the card, its body written by a session through POST `/notice` (`romp card`), a session on an attached machine included; no click, no gate, no setting; the editor extension's webviews block these loads by their CSP (`img-src` the webview's own resource origin and `data:`), so this road is the web dashboard's alone | GET of each paint reference's URL as the file or the notice carries it (a file in the session's folder or your home, whoever wrote it; a notice a session posted), a class this scan cannot bound, from the browser showing the dashboard to the host the URL names; only an inline svg's paint references load there (an image, video, audio, a poster and an svg image are stripped before the nodes join the page): in Chromium a `fill`, `stroke`, `clip-path`, `mask`, `marker-start`, `marker-mid` or `marker-end` whose `url()` names another host loads from that host, in Firefox and WebKit at least a `mask` does, and a `filter` does in no engine; each such request carries no cookie and carries the page's origin (the dashboard's scheme, host and port) in its Origin header; in Chromium a `mask` request can also carry that origin as its Referer, from any page, framed or bare; and a paint request can carry the full page address with the serve token in its Referer, but only when the page's own address carries `?token=` (a pane page opened bare, such as `/chat?token=`; the shell drops the token from its address before it frames its panes, and frames them without it); these paint requests are the one exception to the trust model's sentence on `Referrer-Policy: same-origin` (the response is blocked as cross-origin; the request, with those headers, has reached the host) | none: no setting gates them |
| a socket primitive on a receiver this scan cannot resolve (not derivable by this scan) | not counted: a socket primitive called on a receiver the census cannot resolve (an attribute-held or parameter socket) is not a site here; the scan resolves a socket receiver as a name bound to `socket.socket()` in the same function or by a tuple-literal address, and a rule on the method name alone would tag the backends' and transports' own connect methods, which are not sockets | wherever such a call would run; no site of this class can be listed by this scan | to the address the socket is given at run time | not applicable |

<!-- network-inventory: table end -->

Of the 32 roads, 5 are local; of the rest, the kernel's own connections by default are the price feed (the one to a host that is neither the model provider nor a release remote, with no credential) and the catalog refresh and the fast-mode probe to the model provider (the probe only with an apiKeyHelper configured), with web push the fourth kind of the kernel's own connection once a device subscribes; the programs it runs by default that connect are the release and drift checks, the file viewer's origin check, the session and judge CLIs and the npm retry; everything else opens once set up, by hand, or in the browser (a viewed file's pictures behind the gear's host list, a link you click, and, since the fourth round, a rendered message's media on the web dashboard, which loads on render with no click and no setting; whether the chat's media should be gated as the viewer's pictures are is a decision on a privacy surface for the fork's owner, outside this change; and, since the fifth round, two kinds of browser request named in the table and not counted: an inline svg's paint references in the chat's file preview and in a notice card, which load with no click, and the loads of an .svg opened in its own tab). The rows carry the five corrections the round asked for: the predicate watch with its cadence (floor 15 s, default 60 s), its persistence across a session's end and a kernel restart until met, timed out or cancelled, and the method's limit stated in its row (the shell and a scratch file are all the scan sees, so the primitive finds it and the program name never could); the eight one-shot ssh roads on an attached host (the reachability probe, the serve-token read, the kernel-port probe, the remote start, clone discovery, the guarded apply and its git push, the guarded restart) and the terminal a remote folder click opens; the fast-mode probe's occasions (every key-billed connect, a launch, a resume or a reconnect; the judges once per judge process and again after a fast refusal drops the memo) and its gate (a configured helper for the sessions; a key-billed call whose tier's Fast-mode box is on with a fast-capable model for the judges); bootstrap.sh's curl of itself, clone and update path (fetch --tags, pull --ff-only) and install.sh's pip and npm through the two setup scripts, with the directory-scope cause above in place of the old reason; and the operator's own commands among the roads, the apiKeyHelper on the key-billed side with its five-minute TTL and a stored login's token command on the login-billed side with no memo, never as an excluded kind. SECURITY.md's Network access section is written from the same table, and tests/test_security_price_feed.py TheSectionIsTheTables holds its road list equal to the script's `--table` output by execution, so the two lists cannot drift apart silently. Gaps the derivation records: the session CLI's spawn primitive lives in the claude_agent_sdk package and the Codex app-server's in openai_codex, so the census finds the constructions of those clients in this tree, not the spawn itself; the judges have no environment off switch (the per-tier gear settings and STATE/judge-engine are the controls); `romp watch-pr`'s `gh repo view` is run by a session or the user, not the kernel, and is classed beside the CLIs because the classes have no slot for a romp CLI verb; and the default rules leave one residual: a new site whose argv is a fixed literal of a local tool (LOCAL_TOOLS) or a read-only git subcommand (LOCAL_GIT) is placed local by rule and caught as a new row key by the committed counts rather than as an unclassified site; an import the census does not know fails the run before any of its calls could, so a network library imported later (requests, httpx, websockets and the like) is a loud line (at this head no such import exists in kernel/, cli/, postal/ or bin/); since the third round the same gate reads a module named to importlib.import_module or __import__ and refuses one named at run time, a package the browser and editor code imports outside its own allow-list (KNOWN_JS_IMPORTS) fails the run the same way, and the shell and browser sides' tool and client lists stay named lists with no completeness gate, said as such in the paragraph above, in the script's docstring and in SECURITY.md. Not this PR: docs/reference.md's Service environment and credentials paragraph beginning `The kernel makes two API calls of its own` is the same claim class as the guide's outbound-traffic correction in hand elsewhere; by the table the kernel's own connections are four kinds (the two it names, both on the helper credential; the price feed, with no credential; and web push, to a subscribed device's push service), and the kernel also runs git, gh, ssh, npm and the CLIs, so whoever lands second covers that paragraph (a narrowing that keeps it true: two API calls of its own on the helper credential). Also not this PR: the models list, web push.

The CI red at the round's head, 2026-09-21. The serial CPython 3.12 cell (the suite in one process, in alphabetical order) failed one case of 17135, tests/test_price_feed_off.py SayOnceLatches.test_two_attempts_under_off_arriving_together_write_the_off_line_once, at `self.assertEqual(self.calls, [])`: 677 urllib Request objects in the recorder, every one a POST to a `/peer-exchange` path on 127.0.0.1, inside the case's 0.5 s park (a fixed park then; an event edge since the third round, whose paragraph is below). The mechanism: the harness patches urllib.request.urlopen process-wide and its recorder counted every caller and answered it with the feed body, and the postal service's peer dialer threads, started by three other test modules and never stopped, dial urlopen whenever their backoff returns, so a dial that landed inside a window was counted and answered with bytes that parse as JSON, which the dialer's loop reads as a completed exchange and follows with the next request at once, hundreds of requests per second per thread for as long as the window stays open. The round added the 0.5 s park in SayOnceLatches and two gate cases in tests/test_price_feed_consistency.py, and that is the new exposure: a gate case catches a backoff-timed dial, releases it as a burst when the gate opens, the window closes, the real dial to 127.0.0.1 is refused, and the 2 s backoff returns every dialer together inside the park of the module that runs next (the earlier heads had the same recorder and no window wider than milliseconds). The fix, in this PR: PriceFeedCase._urlopen compares the request's url (a str, or a Request's full_url) to PRICE_FEED_URL and records and answers the feed alone; any other request is not recorded and is refused with a canned urllib.error.URLError whose reason (FOREIGN_REFUSED) names the url, nothing of the real urlopen kept for delegation, so a foreign caller inside a test never reaches the network and never sees the feed's bytes, and the dialer reads the refusal as a failed dial and backs off. The comparison target is km.PRICE_FEED_URL read at call time, the product's own constant, which the worker of _refresh_remote_prices passes to urlopen as a plain str (no Request object, and no composition of a host and a path anywhere in the product), so the recorder compares against exactly what the product sends, and a change to the constant moves the recorder with it; a literal copy of its text would, after such a change, refuse every feed request as foreign and record nothing while every count assertion still read 0 or 1 as if the feed were the subject. The harness's docstring says so, added with the re-ruling's fix on 2026-09-21. `self.calls` counts the feed alone in both modules (tests/test_price_feed_consistency.py inherits the harness); all 37 assertions on it across the two were each exposed to a single foreign call, every one green in CI by timing until this window. The pin, TheRecorderCountsTheFeedAlone.test_a_request_that_is_not_the_feed_is_refused_and_not_counted, builds once under the switch, dials a synthetic non-feed url from a helper thread inside the case's window, and asserts the URLError with the harness's reason and the url, an empty count, and that the feed's own request in the same case still counts. Over an archive of the round's head with the harness as it was, the case is red at its first assertion on CPython 3.12.3 and free-threaded 3.14.6 alike (`None is not an instance of <class 'urllib.error.URLError'>`, the message showing the thread holding the feed body and the recorder holding the Request), and a mutant harness that counts before it refuses is red at the second (`Lists differ: [<urllib.request.Request object at ...>] != []`). At the tree the two modules pass 57 of 57 in 5 of 5 runs on each interpreter, the GIL read off inside every 3.14t run; the 91-file alphabetical prefix that reproduced the CI red in one process (tests/test_nudge_walk_parse_gate.py through tests/test_price_feed_floor.py) plus the two modules, over a git archive of the round's head with the fixed module, runs 1880 passed and 1 failed on each interpreter, the one red a perf-bench artifact of a non-git archive (it runs git rev-parse on the tree), SayOnceLatches green and no red in either price feed module.

After the second round the fork's manager re-ruled B, the rate written as a string, on 2026-09-21 (a correction inside that round's follow-up, not a numbered round): a rate in quotes that is a plain decimal whole is read as that number, on the override file's road and the feed's, through one strict parse, and every other string stays refused. Round 2 had refused every string by type, a regression on the quoted "0.5" that both earlier heads accepted; restoring the bare float() was refused too, because the hazard is the coercion, not the string: float() on a string reads "inf", "-inf", "nan" and "1e999" (inf), " 0.5" and "0.5 " (whitespace either side), "+0.5" (a leading plus) and "1_0" (an underscore separator, 10.0). The rule: a JSON number is accepted always; a string only when the whole of it matches the strict decimal form (an optional minus, digits, an optional single dot with digits after it, an optional exponent, nothing else), then float() and the same finite check a JSON number gets; everything else takes the row road's report. One helper, kernel/kernel.py _price_rate_value with its regex _PRICE_RATE_STRING (fullmatch, so a trailing newline is refused where `$` would admit it), reads every rate on both parses, the feed worker's and the override file's, and holds the finite check each parse had. On the feed's road that claim held for the input and output keys alone until the third round: the two optional cache keys (cache_creation_input_token_cost and cache_read_input_token_cost) inherit the input rate when absent, null or a numeric zero (0 or 0.0) and go through the strict read for every other present value, so false, an empty string, a list, an object and true reject the row as they do on the input and output keys; the read had been `v.get(key) or inp`, older than the change, which took every falsy value to the input rate and cached the row with nothing said (the third round's paragraph below). The property the regex encodes, ruled in this re-ruling and stated in the helper's docstring so neither edge is re-decided on its own: the accepted set is the plain decimal forms a feed or a person plausibly writes, and everything that could be a misread or a special value is refused (inf, nan, 1e999, an underscore separator, surrounding whitespace, the empty string). Both edges follow from that one rule: "007" is accepted (a padded figure, the same value as 7, no misread possible) and "+0.5" is refused (the same value as 0.5, so refusing it costs no real input, and the strict set is the smallest covering the inputs that occur); a negative rate passes as the number -0.5 does, finite being the one range check either arm has. The premise, derived before the fix and recorded in the helper's docstring so it is not re-litigated: the live feed sends numbers only. The document at the feed's URL, fetched once at 2026-09-21T16:46:51Z, carried a JSON number at all 9,438 values present under the four keys the worker reads and 0 strings among the 14,185 values under every cost- or price-named key, and no on-box cache of an earlier response exists. That fetch was made once, from a scratch directory outside the tree, by nothing the tree carries: the committed census counts (the table above and scripts/network-inventory-expected.json) are unchanged by it, and this re-ruling adds no network site. So today the string arm changes nothing on the feed's road (it would matter only if the document's types changed) and restores the quoted "0.5" on the file's. docs/reference.md's override paragraph says a rate written as a plain decimal number in quotes is read as the number it spells and anything else in quotes makes a skipped row, said like any other; tests/test_reference_price_feed.py, tests/test_reference_price_feed_rows.py and tests/test_security_price_feed.py pin the sentences, the helper and both callers' reads. The re-ruling's tests, classified over a git archive of round 2's head (the thirteenth commit on the branch), identically on CPython 3.12.3 and free-threaded 3.14.6: 14 cases are red at the assertion they pin, each a rate or a cache read. In tests/test_price_feed_off.py TheOverrideFileIsSaid, the six accepted quoted shapes ("0.5", "3e-06", "1E5", "10", "-0.5" and "007") are red at the merged rate, where the head rejected the row. In LiveFeed, the seven feed shapes the head's float() read and cached are red at the cache: "1_0" as ten dollars a token, the padded, trailing and plus-signed forms as 11e-6, true as a dollar, false as zero, and "1_0" in the optional cache key. In tests/test_price_feed_consistency.py APartialLandingIsSaid, the two arms in one landing are red at the partial line's count. 22 guards are green at that archive: the fourteen refused shapes on the file's road, and the eight on the feed's that the head's finite check or its float() already refused. A mutant with the strict-form check cut out of _price_rate_value reds the seven guards only that check refuses ("1_0", the three padded forms, "+0.5", ".5" and "5." on the file's road), five of the red-at-head feed shapes and the consistency case; the other fifteen guards stay green as the finite check's or float()'s own refusals. One control, the feed row whose quoted rates are all plain decimals, is cached at those numbers at every head. Over the base's archive, twelve of the fourteen file-road guards are red at the coercion itself and two, "abc" and "0x10", are red off their subject, the base fetching the feed so its table is the feed row's.

The third review round by the fork's manager, 2026-09-22, the landing round over the whole change at the head the B re-ruling's rationale commit left (the fifteenth commit on the branch), ruled 20 findings, 8 medium and 12 low, every one confirmed by an executed attack on its fix (plants in scratch copies; five fixes corrected and one marked wrong on the way), in seven groups, and changed the following. The census: the completeness claim held for the Python half alone, the shell and browser halves being closed tool lists with no interpreter arm, no import gate and no class for a program site, so the shell scan gains an interpreter arm (a head python, python3, node, perl, sh or bash, path-prefixed or not, followed by -c, -e or a bare - for the heredoc shape; every such site is external-program by class and takes a row that says what the text does: nine rows for bin/romp, bin/romp-service, bin/romp-uninstall, three hooks and vscode-extension/install.sh, whose texts parse the kernel's replies, render JSON, quote a query string or stamp an audit row or a package version and open no connection of their own) and the tools npx, scp, rsync, sftp and nc; the browser and editor scan gains the child_process family qualified to its binding (a bare exec( with no binding is RegExp exec and no site), net.connect, net.createConnection, tls.connect, XMLHttpRequest and sendBeacon, classes each program site by its argv as the Python side does (the manager's two spawns and the editor extension's two execFiles carry the class now, three of them leaving the local set-aside count; the timeline view's literal open stays local), lists a dynamic import() of a computed specifier in the computed-URL class (the PDF worker's, the one import no gate can decide) and gates every package the files import or require against KNOWN_JS_IMPORTS; vscode-extension/install.sh's npx, a fetch of vsce from the npm registry even when it is cached, is rowed on install-ext and the install-ext and self-update cells state the condition (an editor CLI present, or ROMP_EXT_PACKAGE_ONLY set; the script exits after the build otherwise) instead of claiming every install fetches; the Python side's NET gains a socket's sendto and the event loop's sock_connect, create_connection and create_datagram_endpoint (zero sites at this head), importlib.import_module and __import__ go through the import allow-list (a literal, a module constant, or a loop or comprehension variable over one; a name spelled at run time is refused), a rule tagging every .connect whatever its receiver was refused because it would tag nine backend and transport connects that are not sockets, and the residual, a socket primitive on a receiver the census cannot resolve, is named as a fifth class row of the table, in the docstring and in SECURITY.md, held by a witness case that plants one and asserts the census does not see it; the committed counts carry the program head per Python command site, so a same-count swap of a local tool for curl inside a rowed function is a COUNTS line naming the key and the head; the disclosure that the shell and browser sides are matched by a named list with no completeness gate stands in the docstring and the section; and one commit regenerates the counts, the table, this entry's block, SECURITY.md's text and the body's count line together, 267 sites to 345, 28 external-program to 108, 162 local sites set aside to 159, 4 browser-computed-url to 5, the class row's by-program breakdown now `python3 -c` 63, an argv the code does not spell out 21, `python3 -` 12, `sys.executable` 4, `git` with a subcommand the code does not spell out 3, `bash -c` 1, `bash` 1, `node -e` 1, `sh -c` 1 and `sys.executable -m` 1, and the class row's label takes the section's wording, an external program started whose far end its arguments do not show, in place of a program the kernel starts, since the class holds what romp's shell scripts, the manager and the editor extension start too, with the table pin, the road map and this entry's block moved together and a pin reading the label from the script to hold it to the section's phrase. The git boundary: the decision page outside the repository said git checkout was a member of the external-program class; the code is right (the class is a shell or interpreter, shell=True, an argv the code does not spell out, and git with no spelled subcommand; checkout and remote left LOCAL_GIT and need a row, a row-less one refused UNCLASSIFIED with no class tag), the page is corrected outside the repository, and the boundary is pinned by two plants, a row-less git checkout printing UNCLASSIFIED with no class tag and a bare git with a runtime subcommand carrying it, since the page's sentence survived two rounds with nothing executed holding it. The feed's cache keys: on the feed's road the two optional cache keys (cache_creation_input_token_cost and cache_read_input_token_cost) inherit the input rate when absent, null or a numeric zero (0 or 0.0) and go through the strict read for every other present value, so false, an empty string, a list, an object and true reject the row as they do on the input and output keys; until this round the read was `v.get(key) or inp`, older than the change, which took every falsy value to the input rate and cached the row with nothing said, while the comment beside it named absent, null or zero and the re-ruling's paragraph above claimed the strict read for every rate; the worker's docstring, the comment and a local helper, cache_rate, now state and hold the substituted set. The say-once pin: SayOnceLatches proves each latch's mutual exclusion on two event edges in place of a half-second park whose return was never asserted (with the lock in place the pin passed by timeout, and without it its red needed the second thread inside the half second): a shim on _price_feed_lock keyed on holder identity fires when the other of the case's two threads asks for the lock the parked one holds, observed from whichever side comes second under the shim's own lock; the second read of the latch is the other edge; the park's five-second bound must return True or the case is red, asserted before the liveness check because the threads' own join races the park; keyed on identity and never on a count of asks, since on the cost view's road the parked thread itself asks for the lock twice before the row latch. The table sentence: the census paragraph above dates the table to the branch head alone, with no appositive and no commit named, since a sentence naming a commit is stale at the commit that writes it, and the PR body's twin sentence takes the same edit. The sweep: the body's Sweep paragraph named one commit while its trailer named another and carried a bats figure from the earlier sweep, so the full sweep (pytest, bats, npm, typecheck, the dashboard build) runs once at the head this round's commit leaves, the trailer's four figures come from that one run, the earlier sweeps stay as history naming their own commits, and the build leg's exit is stated in prose at the head it ran at. The section: SECURITY.md said the editor extension's update prompt ran the same install.sh as the release update, the root script with pip and npm; the click runs vscode-extension/install.sh (npm install and npx) and never the root script or pip, so the sentence names that script and what the click reaches (the npm registry, not PyPI), the release clause stands and names its two scripts, and the pin reads the code (update-target.ts resolving the vscode-extension candidate's install.sh, extension.ts's runInstall taking the resolved target's script, read with its comments stripped, and the executed update-target.test.ts named in the message; a negative pin that extension.ts and vscode-extension/install.sh carry no pip and no romp-sdk-setup) in place of a bare install.sh substring the file's comments satisfied whichever script the code ran. The round's tests, classified: 35 cases added or rewritten, all Python, across tests/test_price_feed_census.py (20 added in seven classes and the table's rows case rewritten for the fifth row), tests/test_price_feed_off.py (the rejected-row case's eight cache-key subtests, one guard case, the three SayOnceLatches cases rewritten on the two edges) and tests/test_security_price_feed.py (six added in two classes, three rewritten). 30 are red for their stated reason over the archive each names, at the assertion it pins, identically on CPython 3.12.3 and free-threaded 3.14.6: the census module's 20 and the security module's 9 over a git archive of the reviewed head (the fifteenth commit) with the module copied in (the census module's at `0 == 0 : the run must exit non-zero` for a planted interpreter text, connection, package or import the archive's script passed in silence, at a listed line the archive's script did not print, at `30 != 31` for the table's rows, or at a sentence the archive's docstring lacked; the security module's seven at a SECURITY.md sentence the archive's section lacked (the label pin among them, at the class phrase, which the archive's section spelled with the kernel alone), one at the table's road set and one at the class label the archive's table printed in the old wording, which the road map no longer carries (the kernel-request rows case), which the archive's census printed without the fifth row), and the eight cache-key subtests over the base's archive and the reviewed head's at the cache assertion (`'claude-fable-5-1' unexpectedly found in {...'cache_w': 1.1e-05, 'cache_r': 1.1e-05}`, the row cached at the input rate). 4 are proved by mutation at the tree with no archive red of their own: the three SayOnceLatches cases (the lock taken out of _price_feed_first reads `2 != 1` in every case, with the second thread on time or 0.6 s late, where the old shape passed the late arrival; a shim with both lock edges removed reads `[False] != [True]` at the park's return after 5.00 s) and the git boundary pin (checkout added to LOCAL_GIT, or the bare-git clause removed from the scan, reds it). 1 is a guard, the inheriting set: green at both archives for its seven inheriting shapes (absent; null, 0 and 0.0 on each key), its two true arms red over the base's archive at the cache assertion for the base's own reason (a bare float() read true as a dollar a token) and green over the reviewed head's. Every census case is also red with its arm taken out of the script in a scratch copy of the tree (thirteen mutations, both interpreters), and every security case that reads a file is red with that file mutated the way its message names (the label pin with the old label restored in the script, naming both texts). No red is an error on a symbol the fix introduced. The where: line names every changed path (29 at this tree) and the definitions this round touched: the script's constants, helpers and visitors, the worker's cache_rate, the test classes and constants, and the section's sentences. Before the fourth round, on the reviewer's ruling over the derivation of the editor extension's `vscode.env.openExternal`, one commit adds the clicked-link road, a link you click, on both hosts: the extension's one call is a site through a JS entry, and `window.open` moves from the DOM list to the JS list so each of its four lines is a site with a row (the chat page's click delegate, the shared opener and the file viewer on the road; the file preview's own-tab open of the kernel's own file URL local), the browser-dom-loads cells say that an anchor's href write loads nothing by itself and that no `window.open` is counted there, the road's residual (an anchor with no scheme the chat page's delegate leaves to the default action is a gesture the tree does not route, and what the editor's webview host does with it is outside the tree) stands in the script's docstring, the road's trigger cell and SECURITY.md as one text, and the summary moves from 345 sites, 30 roads, 159 local sites set aside and 80 browser-dom-loads to 350, 31, 160 and 76.

The fourth review round by the fork's manager, 2026-09-23, the landing round again over the whole change at the head the walk-figures commit left (the twenty-third commit on the branch, the merge of main counted once), with the completeness check run, ruled 19 findings, 2 high, 5 medium and 12 low, every one confirmed by an executed check on its fix, in eight groups, and changed the following. The media road: on the web dashboard the chat pane loads a rendered message's media (an image, video, audio, srcset or picture media, and a video's poster and an inline svg's image, the sanitizer's whole kept set) from the host its URL names the moment the message renders, a session's reply, your own message or a postal body alike, with no click and no setting, with whatever cookies that browser sends to that host and no Referer to any other origin, while the editor extension's webviews block the same loads by their content security policy; the census had no row and the section no sentence for it, so the table gains the road a rendered message's media (browser), named from the pipeline's one line on a message's pictures before the browser fetches them (`mdImgPostPass` in ui/webview/render.ts, two sites, the pattern refusing the definition in preview.ts), the section gains the sentence beside the viewer's gated one, the security module's road map gains the entry, and the row is held to an executed witness (ui/webview/chat-media-loads-browser.test.ts: render.ts's own md and userMd pipeline over two loopback servers under the four headers the kernel's _send writes, pinned by value and tied to the wire by a case that drives the kernel's Handler in a child interpreter and reads the same four on GET /chat; nine media shapes, one request each with no Referer and the cross-site cookie alone, zero requests under the editor's policy, a Referrer-Policy control), which skips at exit 0 without a browser as the other browser legs do; whether the chat's media should be gated as the viewer's pictures are is a decision on a privacy surface for the fork's owner, outside this change. The GitHub button: the clicked-link row's what-is-sent cell bounded the private strings a click carries to three classes and missed the file viewer's GitHub button, an anchor romp composes (`_file_github_link`: the checkout's owner and repository name from its origin remote, its current branch or the commit sha when HEAD is detached, and the file's path, each segment percent-encoded with slashes kept, to github.com, no query string and no Referer; not offered for an untracked, staged-only or uncommitted file, a path outside a checkout, or a checkout with no origin remote or one not on github.com); the sent cell names the class, the trigger cell names the button with its mechanics per document (on the chat page the click delegate; on the Files pane, the feed page and the Waiting-on-you page the anchor's own default open, since the viewer's `linkOf` matches none of the button's classes and no modified-click path reaches it; the viewer mounts in no editor webview, where the ruling had named an editor feed panel, so the button has no editor leg), the section's trigger list and sent clause say the same, the pin reads the executed URL shapes of tests/test_file_github.py in place of a kernel.py spelling, the href write's listing is pinned by content and not by line number, and the anchor class census is derived by grep. The trigger cell and the residual: the cell had the chat delegate opening the gear's sign-in link everywhere and the editor posting openLink for it; it now states the link by document (on the dashboard's settings page, which installs no opener, the browser's own open in a new tab, no site of this tree running; in the editor's chat panel the delegate's openLink post; in the editor's feed panel the webview host's own link handling, outside this tree), and the residual is a stated class: three anchors the chat page's click delegate leaves to the default action (a page-built one with no scheme; one with no scheme in a message that does not resolve to an http or https address; and, the anchor this round added, a message's own download anchor, one with no scheme carrying a `download` attribute whose href resolves to an http or https address on this page's origin, which the browser saves from this origin) and a page-built anchor with a scheme in a document that installs no opener, the sign-in link on the settings page and in the editor's feed panel its witness, one text in the docstring, CLICK_RESIDUAL, the trigger cell, SECURITY.md and both test modules' constants (each read the script's binding by import), the six new-tab anchor builds under ui/webview classified by file and statement, and an executed pin (ui/webview/settings-signin-link-browser.test.ts: the settings page served as the kernel serves it, a real click on the sign-in anchor, zero window.open calls, zero openLink posts, the popup at the href and the settings document unmoved). The shell echo skip and the JS binding gap: line_scan skipped every echo- or print-led shell line whole, so `echo "$body" | curl ...` and `echo "rate: $(curl ...)"` ran a named tool with no site and exit 0; the skip now holds only when nothing live follows the printed text, the live remainder (the text outside quotes and the body of every $(...) and backtick substitution, by a paren-depth walk with quote states, a word-initial # ending it) is what the interpreter arm and the tool list read, the four remedy lines that carry a pipe or a substitution inside quotes stay clean, and the echo rule stands beside the disclosure sentence in the docstring and SECURITY.md, no committed figure moving for it alone; the JS connection family (the get and request of http and https, the connect and createConnection of net, the connect of tls, and ws) was matched by its qualified spelling alone, so an inline require or a destructured member was no site and the timeline view's two `require('http').request` calls to 127.0.0.1 were uncounted; the family now goes through the file's bindings as child_process does (an inline require, a namespace or default import, a bare name the file binds, renamed or not; ws by its constructor shape), an added arm beside the literal patterns with a call both match counted once, the two timeline sites rowed on local-kernel, and the binding sentence in the docstring and the section. The served pages: the HTML and JavaScript the kernel serves from its own string constants sat outside both scans, so a third-party fetch, opener, socket or beacon planted in the timeline boot or a page body passed at the committed counts while the section claimed the walk found every connection-opening site; the population is derived from kernel.py's syntax tree (every `_send` whose content type is text/html or text/javascript, so the service worker's script is in, each route's page function followed to the constants it returns or inlines), scanned as browser text keyed kernel/kernel.py plus tool with the DOM loads counted, a route serving a page from text the extraction cannot read refused by name (SERVED; the fifth round widened the population to every `_send` call and every Content-Type header written outside `_send` and the refusal to what the served pass cannot read, as its paragraph below says), a file a page reads at run time covered by the walk when it is a scanned kind and a stylesheet named and not scanned, the whole-file text scan refused because it misreads Python and JS concatenations, the import gate's statement form narrowed over served text to import- or export-led lines, the shim's and the shell's sockets to location.host, the boot's window.open (reached by no caller at this head) and the service worker's clients.openWindow (a new JS entry) rowed on local-kernel, a served literal whose URL carries a Python format slot placed in the computed class, and the residuals named (the served rows are keyed by tool, so a second socket or opener that keeps the tool is caught by the count per key alone; a stylesheet's url() loads and the same-origin navigations no list names are named and not counted; the DOM class counts one line per scanned line, so a page template inlined as one string is one line however many loads it carries); the population measured 36 fetch lines, two sockets, one opener and the worker's opener against the round's estimate of about 47 sites and a computed class of 11, an estimate that came from a whole-file text scan. The pins: each opener pin of the clicked-link case names the executed test that proves its leg (the chat delegate's window.open by the md-sanitize chat-links browser leg, which skips without a browser; the shared opener's post leg by the url-links and pr-links tests; the extension's routing by view-routing's test) or says text-pin-only where none exists (the delegate's postMessage leg, the shared opener's browserEnv.open, the extension's openExternal and its routes beside the open-link-wiring regexes), with file-view-links-browser.test.ts named as a stand-in over another document and not this leg's witness; the target pin needles the whole statement `const script = target.script;` over updateExtension's own text and asserts `script` is bound there exactly once, since the prefix needle alone passed a re-pointing before the assignment; and kind_of's early return for a dotted name is gone, so a .bash or .zsh hook falls through to the shebang read, the walk run planting hooks/probe.bash with a curl and reading it unclassified (no committed figure moves: the five dotted files under the Python roots outside the extensions are README files with no shebang). The cost, a landing fact and not a gate (the fourth round's ruling, kept here as history and superseded before the fifth round, as the end of this paragraph records): this change's eight test modules cost about 80 s serial on the box (Python 3.10: 80.89 s and 82.54 s of pytest wall at the reviewed head in two runs, the census module 72.8 s and 74.6 s of that; the round's checks measured 78.6 s to 86 s) and about 100 to 110 s on the CI runner, an estimate by dot-line alignment of the head run's log (the line holding the census module's cases spans 96 s); the merge base's own serial Python 3.10 cell (fork main as merged, run 35780636842) ran pytest in 1193.52 s (19:53) and its job in 1231 s (20:31); the reviewed head's (run 35800398701) ran pytest in 1463.43 s (24:23, pytest's own wall; the Run pytest step 1481 s) and its job in 1492 s (24:52): 270 s of pytest growth, of which this change's tests are about a third and runner variance the rest, main's last eight push runs spanning 20:31 to 24:09 of job time (1231 to 1449 s) and its last fourteen 17:21 to 24:09; two commits of this branch were cancelled at the 25-minute wall with pytest green (the seventeenth commit, the entry's where: line naming the label pin, run 35772527942, the job at 1528 s with the pytest step green at 1512 s; the twentieth, the line naming Scan.visit_Constant, run 35787574970 first attempt, pytest's summary at 1476.39 s and the job cancelled at 1512 s, the second attempt passing in 1307 s); the earlier cost record measured a different head and module set and is cited for the per-run cost of the script alone (about 3.5 s a run then, 3.3 s at this round's tree against 2.6 s before the served-page pass and the binding arm; the census module's cases 58 to 83 and its runs of the script nine more, one shared run for the served pages and eight mutation cases with a run each, so its serial wall on the box is about 110 s at this tree, 110.07 s on CPython 3.12.3 and 93.68 s on free-threaded 3.14.6); the cap under .github/ is the owner's open decision, and the batch that lands this change watches the 3.10 cell for a cancellation with pytest green and reruns it. The decision page outside the repository, whose figures were true at the head it named and stale at the reviewed head, is the reviewer's and takes his dated correction; nothing is owed in the tree. The census, regenerated once after every change of the round: 350 sites to 394, 31 roads to 32, 160 local sites set aside to 198, 5 browser-computed-url to 9, 76 browser-dom-loads to 103, the js kind 63 to 107, external-program 108 and runtime-program 2 unchanged, 237 files scanned unchanged and 1098 skipped by kind to 1100 (the two browser witness files, skipped by kind as every test file is), the row keys 170 to 176 (kernel/kernel.py:fetch 36, kernel/kernel.py:WebSocket 2, kernel/kernel.py:window.open 1, kernel/kernel.py:clients.openWindow 1, ui/romp-timeline-view.js:http.request 2, ui/webview/render.ts:mdImgPostPass 2), the local row local-kernel 101 to 139, and the table 34 lines to 35 with the chat-media row between the browser-figures and clicked-link rows; the round's estimates were 352 for the media road alone, 352 for the shell and binding fixes alone and about 47 more for the served pages, and the measured sum is 44 (2, 2 and 40, the fortieth the service worker's opener the round ruled in). The round's tests, classified: 36 cases added or changed, 34 Python (27 in tests/test_price_feed_census.py: 23 in six new classes, two in existing classes, the hooks case and the window.open case changed; 7 added in tests/test_security_price_feed.py, six in TheSectionIsTheTables and one in a new class, with the clicked-link case's messages and the target pin changed) and 2 node (the two browser witnesses, one case each, run through the box's browser slot with their request lines recorded, 1 test and 0 skipped each, and red under their mutations: the sanitizer forbidding img, the editor's img-src widened, an opener installed on the settings page). Over a git archive of the reviewed head with the module copied in, on CPython 3.12.3: the census module 22 failed and 61 passed, every plant and tree case red at its behavioural assertion (`0 == 0 : the run must exit non-zero` for a planted third-party fetch, an unreadable route or an import statement in served text the head's script passed in silence; a listed line the head's script did not print for the pipe form, the binding shapes, the timeline sites, the served sockets and the media road's two lines; the third anchor absent from the docstring; the fifth key absent from the window.open list; the .bash hook absent from the unclassified set) and every mutation case stopping at its anchor there, its evidence the mutation at the tree (the head's echo skip restored, the binding arm emptied, the served pass removed, the chat-media row and its lookbehind dropped, the GitHub href write deleted or given an opener, kind_of's early return restored, the arm's once-per-line guard dropped); the security module 9 failed and 19 passed, every red at a behavioural assertion (a SECURITY.md sentence the head's section lacked, the road map's set equality, the sent cell's GitHub clause against the executed URL shapes, the residual constant against the script's binding, the witness file's absence, whose subject is the witness); the target pin red by mutation at the tree (the statement re-pointed by a .replace; a `target.script =` inserted before the assignment, which the whole-statement needle alone passed). At the tree tests/test_price_feed_census.py runs 83 passed on CPython 3.12.3 and free-threaded 3.14.6 with the GIL off once this block was regenerated (82 passed and 1 failed, the block equality, until then) and tests/test_security_price_feed.py 28 passed on both once the section's seven edits landed. No red is an error on a symbol the fix introduced. The where: line names every changed path (31 at this tree, the two witness files new) and the definitions this round touched: the script's echo arm, binding family, served-page extraction and rows, the six census classes and two cases, the security module's class, six cases and constants, the two witness files, and the section's sentences. Before the fifth round, the chat-media sentence was corrected from the reviewer's executed fact check of the media road on a lab kernel at the fork's main: the sanitizer keeps an inline svg's paint references (a fill, mask or filter whose url() names another host), which load at render with no click like the media the sentence names, and their request does not reliably follow the page's same-origin referrer policy, carrying the page's origin in one run and the full chat URL with the serve token in another where every image, video, audio, srcset and picture request carried none; so the section's sentence and the table's what-is-sent cell state that class as the one case whose request can carry the dashboard's address with the serve token in its Referer (the response blocked as cross-origin, the request gone to that host), the claim that no serve token, key or login token rides is withdrawn from both, the pins in both test modules follow with the clause held as one text, and the block above is regenerated with every count unchanged; the mechanism fix, the sanitizer and the hover preview's strip dropping remote paint references with a witness, is another PR's, in flight, not this one's (the fifth round corrected this clause's attribute list, conditioned its token half and rowed the chat's file preview and a notice card, as its paragraph below says). Before the fifth round too, on the reviewer's ruling of 2026-09-23 at 09:2x UTC that a change's own test cost is a landing blocker once its own CI cancels at the 25-minute wall, the census module moved every run of the script into the test process and applied the served pages' plants to kernel.py's text in memory, which on the box took the census, security and off modules, in one pytest invocation, from 134.49 s to 68.77 s on 3.10 and from 115.41 s to 73.63 s on 3.12 (each after figure the median of three runs); that supersedes the cost clause above, and the fifth round found that on the free-threaded 3.14 runner the cut saved about 0 to 15 s, so the landing waits on the owner's decision on CI headroom (its paragraph below).

The fifth review round by the fork's manager, 2026-09-23, the landing round again over the whole change at the head the census-paragraph commit left (the thirty-first commit on the branch, each merge of main counted once, main merged a second time before the round), with the completeness check run, ruled 34 findings, 3 high, 12 medium and 19 low, none refuted and several fix lines corrected by the checks, in seventeen groups, did not land the change, and changed the following. The file preview and the notice card: on the web dashboard the chat's file preview of a markdown file (a pointer dwell of 350 ms or a keyboard focus on a link in the chat to a file the kernel allows to preview, a glossary term's link included) and a notice card's body on the feed page (which a session writes through POST `/notice`) render markdown through the shared sanitizer and then stripRemoteLoads, which strips an image, video, audio, a poster and an svg image and reads no paint attribute, so an inline svg's paint references load from the hosts they name with no click; the table gains a row for the two surfaces, named and not counted beside the DOM class since the scan cannot see those loads, the section and the security module's road map name it, a caller census places every caller of stripRemoteLoads by grep (the provider preview slot named as filled by no producer, since nothing fills its HTML body at this head), and the chat-media witness gains a file preview scene and a notice card scene in which the paint references arrive and the stripped media do not; a fix of the strip in another change inherits these rows and scenes if it lands second. The .svg tab: on the web dashboard a Cmd, Ctrl or middle click on a path link to an .svg in a viewed file, or Cmd or Ctrl with Enter or Space on a focused one, opens the kernel's `/file` URL, or the `/remote/<host>/file` relay's for a file of a session on an attached machine, in the browser's own tab, a sandboxed svg document that loads each resource its markup names from that resource's host, outside the gear's Pictures from the web in files list; the table gains a row named and not counted before the local row (preview.ts's `window.open` stays on the local-kernel road, its comment corrected), the section a paragraph after the set-up paragraph, a case holds image/svg+xml as the one document type `/file` serves a file's bytes under, and a browser witness records the tab's requests on both arms with a control scene without the sandbox; the loads the tree shows are more than the ruling named (an `xml-stylesheet` instruction, an `feImage`, HTML inside a `foreignObject`, a cursor image, a web font and the Chromium paint list besides the image, the CSS `@import` and the `foreignObject` image it named), and a content security policy that blocks them belongs to another change. The command line: the cost cut before this round had left the documented command held by no real run, so a script whose entry dropped its exit code passed both modules; the census module now runs the file as the command line runs it, in a child, over each of two tiny roots and over a copy whose kernel.py carries exactly the served pass's plants, each exiting 1 and printing byte for byte what the in-process run or the served pass prints, and SECURITY.md's sentence that the suite runs the command is true again. The cookie and Referer clauses: the chat-media cell and sentence named the cookies a browser sends there as the cross-site set, and the clicked-link cell and sentence said that no request carries a serve token; both now state only what holds at every host, with whatever cookies that browser sends to that host, and that romp adds no serve token, key or login token to a link's URL; the chat-media clause's no Referer now reads no Referer to any other origin, since a media URL at the dashboard's own origin is a same-origin request whose Referer goes to the kernel itself; and the local row's sent cell reads nothing is sent to another host. The paint clause: it named fill, mask and filter; in Chromium seven attributes load from another host (fill, stroke, clip-path, mask, marker-start, marker-mid and marker-end), in Firefox and WebKit at least mask, and filter in no engine, one text in the section, the rows that carry it, both test modules and this entry, and the chat-media witness now renders all eight and holds the set whose requests arrive equal to the list the section's clause names, filter's absence pinned, with Firefox and WebKit legs. The token half: the clause said a paint request could carry the full chat URL with the serve token and stated no condition; it now says each paint request carries the page's origin in its Origin header (a step past the ruling's wording, on the safe side), a mask request in Chromium can carry that origin as its Referer from any page, and a paint request can carry the full page address with the serve token only when the page's own address carries `?token=`, as a pane page opened bare does and a framed pane does not, the one exception to the trust model's Referrer-Policy sentence, which now points at it, as the kernel's comment beside the header does; the clicked-link sent cell no longer says the serve token travels only on the bundles' own URLs, and an absence pin holds the old words out. The served routes: the population came from `_send` calls whose content type was a text/html or text/javascript literal; every `_send` call and every Content-Type header written outside `_send` is now a candidate, its type read through the `_send` definition it reaches (the session host's and its transport's frame writers named as answering no HTTP request) and through constants, locals and dict literals, normalized and compared with the named list of types a browser runs script from, and a type the pass cannot read is a SERVED line unless the served allowlist names the place with its count of places and its reason, a stale entry or a changed count failing the run; the direct Content-Type writes are six, not the four the ruling named, and the reason for the `/dist` and `/media` branch states what the tree serves there. Receivers and containers: the served pass had passed every attribute, every subscript and most method receivers as a slot with no text, so a page served as a constant's `.encode()`, a `.get()` with a default, a subscript joined to a literal or a class attribute went unscanned; it now reads the receiver of `.encode` and `.format_map` whatever it is, a method's receiver and a subscript's container when they name a module constant or a local, a class attribute, a BoolOp's operands, a loop or unpacking target from its source and a local list's appended values, refuses by name a container the module writes at run time (the two memos whose values come from walked files are allowlisted with that file) and any other receiver, and leaves the tree's listing byte-identical under this change alone; the class docstring's value-slot sentence is corrected, and a defect the round's derivation found beyond the rulings is fixed: the once-per-route read of a local was keyed on the id of a map freed on return, so a same-named local in a second function was skipped on 3.10 and free-threaded 3.14. Joined constants: the served pass read a joined constant as one line, so a fetch after the first on it, or any fetch in a constant that starts like a comment, went unread, which hid five live fetches (four local and the drift banner's per-host route, a computed site); every `fetch(` and `import(` on a served line is now read by its own argument with no comment skip, each f-string part is listed at its own line and deduplicated by its place, and the lines Python 3.10 and 3.11 still list early are a stated residual. Binding shapes: the binding patterns the connection family and child_process share missed a mixed default-plus-named import, a default-plus-namespace import, `import { default as X }`, TypeScript's `import X = require()` and a bound `await import()`; each is read now, and the three shapes no pattern reads are named beside the disclosure sentence with a plant held at no site, stating what the tree showed against the ruling's wording: an aliased require of `https`, `net` or `tls` escapes the import gate too. The echo rule: four lines in the shared shell run go through the substitution's backtick, subshell paren, quoted paren and backslash-escape arms, each red under a mutant that removes its arm. The cost, the fourth round's clause above superseded: the landing waits on the owner's decision on CI headroom, since on the free-threaded 3.14 runner the in-process cut before this round saved about 0 to 15 s, this change's share of that cell there is about 117 to 137 s, and that cell was cancelled at the 25-minute cap on every attempt at the reviewed head; no further cut is asked of this change unless that decision keeps the cell serial under the cap, and then one measured on that runner. The new-tab anchors: the anchor census matched only the property spelling of a new-tab target, and now also matches `setAttribute("target", "_blank")` in both whitespace forms, the viewer's two such builds classified as document anchors rowed by the viewed-file clause, nine statements where it counted six. The cost view's first open: `fetching the feed now` stayed on the line after the fetch landed, since only a period pick or a reopen fetches again, and the line now reads `the feed was still being fetched when these figures were priced; pick a period to reprice`, quoted in the second paragraph above. The modules' prose: the census module imports tests/romp_load.py above its preamble, so a direct run leaves no directory in the system temp dir, and says that helper execs kernel/loadsource.py; the security module reads the script's CLASS_ROWS, CLICK_RESIDUAL and ROADS from the module the census has already loaded, its two child programs gone and their docstrings corrected. This entry: line 15 lists every fork file the offer's tests need where it named one, the fourth round's cost clause is kept as history and marked superseded, the census paragraph states the mechanism as it runs (it described one run per mutation over a copy, false since the shared runs began), the table is regenerated once, and the where: line is derived again over the final diff. The decision page outside the repository is the reviewer's and takes his correction; nothing is owed in the tree. The PR's body: the reviewed head's CI paragraph is not corrected in place, and the two earlier heads' CI paragraphs move to the review record outside the repository with a pointer left. The census, regenerated once after every change of the round: 394 sites to 399, 198 local sites set aside to 202, 9 browser-computed-url to 10 (the drift banner's route), the js kind 107 to 112, the local-kernel road 211 to 215 (its share of the local row 139 to 143), kernel/kernel.py:fetch 36 to 41, 1100 skipped by kind to 1112 (the .svg tab witness, a test file, skipped by kind as every test file is, and the eleven test files main added before the fixes, merged into the branch a third time), 237 files scanned to 239 (main's two new modules, ui/webview/chat-compact-tail.ts and ui/webview/turn-estimate.ts), 32 roads, 5 of them local, 108 external-program, 2 runtime-program, 103 browser-dom-loads and the 176 row keys unchanged, and the table 35 lines to 37 with the .svg tab row before the local row and the paint row after the DOM class row. The round's tests: the census module gains ten cases (the three binding-shape cases, the command line's own case, the clicked-link absence case, the fifth round's docstring sentences, and four cases of the served class that carry the content-type, receiver and joined-constant plants and the line-keying residual's three shapes as thirty-five subtests inside the one served pass, which adds about half a second to that pass and no scan) and the security module ten (the stripRemoteLoads and md callers' two, the paint road's one, the .svg tab road's three, and four text pins: the paint exception in the trust model's Referrer-Policy sentence and in the kernel's comment beside the header, the two kinds of request named and not counted, and the chat's other markdown texts), with the table's rows, echo, served, chat-media and anchor cases rewritten; the analytics states file gains a case that drives the modal from a first open with the feed in flight through a Cost ($) and a group click to a period pick; the chat-media witness runs three tests (Chromium's scenes, Firefox and WebKit) and the .svg tab witness one; the mutations run from M32 to M87 (the command line's entry, splice and scan mutants M32 to M39, the binding alternatives M40 to M46, the echo arms M47 to M50, then the paint strip, sanitizer and caller mutants, the document-type and policy mutants, the four text pins' deletions, the served pass's twenty arms, the new-tab anchor plant, the Referer clause in its two homes and two planted row changes, M51 to M87); each served plant is silent or at the wrong line at the reviewed head's script (the same-named locals on 3.10 and free-threaded 3.14, where that defect reproduces) and red for its reason when its arm is removed from this one, in runs of the served pass alone; and every pin the round added or rewrote is red for its stated reason over an archive of the reviewed head with its module copied in, or under a named mutation at the tree, and green at the tree, the record outside the repository carrying each red. The where: line names every changed path (33 at this tree, the .svg tab witness new) and the definitions the round touched: the script's route population, allowlist and gates, its served pass's receivers, containers and part lines, its binding patterns and its two rows named and not counted, Handler._send's comment, raPriceNote's in-flight clause, the census module's six cases, command-line helpers and constants, the security module's three classes and their constants, the two witnesses, and the section's sentences.

## Recorded, not fixed (owners to be placed)

(a) tests/test_postal_peer_identity.py (PeerIdentityFold), tests/test_postal_peer_tier.py (TierDeclaration) and tests/test_postal_read_receipts.py (the classes on its _Base fixture) each mark synthetic peers up through ps.peer_update with `up` true, which starts one `peer:<host>` dialer thread per host, and their tearDown pops ROMP_POSTAL_PEERS and nothing else: peers_on() defaults on and no peer is marked down, so every dialer outlives its module and keeps dialing 127.0.0.1 on its backoff for the rest of the run. Pre-existing on main and untouched by this PR. The shape of the fix, its own PR: a tearDown that marks each host down (ps.peer_update with `up` false, which pokes the wake so _peer_loop exits) and joins the threads in ps._peer_threads, with a pin that no `peer:*` thread outlives the module.

(b) postal/postal_service.py _peer_loop (bin/romp-postal-service is its symlink), through _peer_exchange_once and _peer_http: any parsed JSON answer is the outcome `ok`, after which the loop continues with no wait, the pacing left to the far bus's long-poll, so a far end that answers at once with any JSON is dialed at hundreds of requests per second. A product defect class outside this PR. The shape of the fix: a floor on the loop's cadence when an answer returns faster than the exchange's long-poll can, or a check that the answer has an exchange response's shape before it counts as `ok`, with a test that a far end answering instantly is dialed no faster than the floor.
