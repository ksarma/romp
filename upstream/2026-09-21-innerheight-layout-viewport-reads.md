---
title: The shell's seven layout-viewport reads take the root's clientHeight through one helper (layoutH), not window.innerHeight, which WebKit shrinks to the visual viewport under a standing pinch: the keyboard-open test, the bell popover's bottom, the spend list cap and tooltip flip, the timeline band cap, the drift banner clamp, the fine-pointer fit
status: candidate
where: `kernel/kernel.py` (`_LAYOUT_H_JS`, the helper spliced at the top of `_LANDING_JS`, `_LANDING_USAGE_JS`, `_LANDING_MOBILE_JS`, `_LANDING_PUSH_JS` and `_STALE_JS`; the reads in `cap`, `spSizePane`, `spTipPlace`, `fit`, `kbOpen`, `place` and `clampXY`); `tests/test_layout_viewport_height.py` (new: every reader under WebKit-under-pinch models and a Chromium model, and the census of every innerHeight in kernel.py's text and in every script the shell serves); `tests/test_layout_height_served.py` with `tests/layout_height_browser.js` (new: the premise in real engines at scale 1); `tests/test_kernel_mobile.py` and `tests/test_shell_viewport_fit.py` (the two exact-text pins on `kbOpen` and the fine road's `var h=` respelled)
added: 2026-09-21
pr: 885
tier: fix
offered:
closed:
---
The device limit first: the WebKit half rests on WebKit's source and one Chromium run; playwright's WebKit on Linux holds no page scale above 1 (no CDP, a tap-only touchscreen); no on-device read under a pinch exists, and whether a pinch is reachable under the page's user-scalable=no meta is unconfirmed. Offered as a fix instead of a report (the user 2026-09-21, who chose an offer over a report) because the swap is a no-op wherever innerHeight was right (standards mode, the root overflow:hidden), so it is safe whether or not the pinch is reachable. The population is seven reads, derived by classifying every innerHeight in kernel.py's text (the pane shim's zero test excluded), which corrects the two-read count and the re-bind road named in 2026-09-20-innerheight-under-pinch-upstream-reads.md; that entry's status is left to the user. Left, named, for a follow-up: the shell's five window.innerWidth reads, three in the functions changed here (the spend tooltip's left flip, the bell popover's right edge, the drift banner's x clamp) and two in tooltip placers that read no height (the rail usage tooltip's clamp in _LANDING_USAGE_JS, the API-health tooltip's clamp in _LANDING_APIH_JS), which WebKit takes from the same rect (LocalDOMWindow::innerWidth), and one height read in a shell bundle, the band cap during the docking drag in ui/webview/panedock-main.ts (window.innerHeight times 0.7; the engine stops under the phone layout).

The review road (2026-09-21), the user's call to offer a fix and the manager's ruling on how: the offer's code lives on the fork branch `iosperf-innerheight-layout-viewport`, two commits over the project's main at e4e73d14b (the fix; the census over kernel.py's whole text with the second WebKit reading and the helper's comment), and carries no fork-only file: this entry travels to fork main in its own docs-tier PR (branch `iosperf-innerheight-ledger`) because a file under `upstream/` on the offer branch would ride into the project's PR, and an entry that lived only on a never-merged branch would never reach fork main, where the ledger is read. The review is the review-only fork PR #885 (labels `fix` and `hold`, never merged, closed after the review), whose base is the fork branch `offer-base-innerheight-e4e73d14b`, pushed at e4e73d14b: a PR needs a base in the same repository, and with fork main as the base the diff would carry every project commit the fork has not folded, so the base is the project's main itself, and the PR shows the offer alone. Merging it would put unfolded project commits into fork main, which is what the hold label and the title say. The offer to the project is opened by the upstream session from the same head branch; `offered:` is left empty for it.

2026-09-21: the Chromium page-scale-2 figure re-derived at the offer head (Chromium 151 under Playwright's iPhone 14 descriptor, `Emulation.setPageScaleFactor` 2): on the landing as served the meta's `maximum-scale=1,user-scalable=no` holds and the scale stays 1; with the clamp lifted on the same served bytes, and on a synthetic control page, `visualViewport.height` 422, `innerHeight` 844, `documentElement.clientHeight` 844. The 2026-09-20 figure was read on a synthetic page without the clamp; the offer body says so.
