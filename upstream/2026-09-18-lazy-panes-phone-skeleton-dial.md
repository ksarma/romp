---
title: Lazy off-screen panes and the skeleton first dial on the phone
status: candidate
where: kernel/kernel.py: _LANDING_MOBILE_JS (promote with its per-promotion token, committed reading the pane's own document by the shim's window marker so an HTTP error body fails, failed and the re-park of a pane whose document did not load, paneDiv, paintLoading with body.pane-failed and the #pane-load-msg copy, loaded, the #pane-load tap-to-retry, the data-lazy-src parking at boot, the promotion inserted in show() before the re-tell and the shown pane's synchronous __rompPaneShown hook inserted after the m-on toggle, the lazyFlip media-query listener with its flip-back parking), _LANDING_DESKTOP_PANES_JS (the desktop promotion of the Waiting and Files panes, its own script spliced before the mobile script), _pane_spin (two inserted listener lines: romp:firstpaintheld clears the sheet's failsafe, romp:firstpaintreleased re-arms it), CLIENT_DIAG_KEYS['shell'] (pane, n for the pane-load-failed row), _shim (the fork line after parentMobile setting RESTART_DIET for the phone chat pane first dial), _LANDING_SETTINGS_JS (the two inserted about:blank waits in the viewFile and browseFiles arms), _landing (the head layout probe before any iframe, the f-waiting and f-files data-src, the #pane-load element with its #pane-load-msg child and its CSS inside and outside the phone media block, the failed state's rules); ui/webview/skeleton-tabs.ts (gate and returnHold on SkeletonState, gateOnFrame, gateOnStrip keyed on the want's host, gateOnShow, all three refusing while the hold stands, onSocketUp closing the gate and holding on the phone, nextPrefetch null while closed); ui/webview/render.ts (gateWant read at the head of upsert and gateOnFrame before its arm; gateOnStrip on the local strip in applyTabOrder; gateOnShow in setActive and silentActivate; the chat-visibility install's schedulePrebuild hook; the panes handler's chat-shown re-arm; phoneShell read once at the wsup arm); ui/webview/chat-visibility.ts (the onShown hook); ui/webview/paint-gate.ts (firstPaintHeld, viewportHiddenSinceLoad, revealDecision with willPaint); ui/webview/feed.ts (paintPlan and paintedKeyOf, the render's one derivation; feedShellOn, parentMobile, revealShown and seenNow, pendingRevealKey with its retirement on the pane's next visibility change, firstPaintHoldTold and the two loader events, cardByKey, jumpToCard, the render() gate line, the observer's callback, the panes handler, the revealCard handler's plan-decided park written or cleared before its arms so a second reveal replaces or drops it, window.__rompPaneShown, releasePaint's pending reveal); docs/read-side.md (the lazy panes file no rows; the pane-load-failed row with the HTTP error body; the phone first dial skeleton row; a return on the phone reloads the visible tab alone, the hold's two rules); tests: ui/webview/skeleton-tabs.test.ts (T2, T3, T5, T6 flipped to the return hold, the remote want, gateOnShow), skeleton-tabs-wiring.test.ts, chat-visibility.test.ts (the show hook), chat-split-exec.test.ts (the lifted strip world's gate and arm), paint-gate.test.ts, feed-hidden-paint.test.ts (T4, the held reveal decided from feed.ts's lifted paint plan, D3, D4 with the observer speaking before the show and the re-show through the override, D5 with the lifted revealCard handler), federation-hidden-hold.test.ts (the fifth release site, the observer's callback, the install pin), feed-age.test.ts (the observer's callback), feed-freeze.test.ts and feed-session-filter.test.ts (render reads the view through paintPlan), feed-followup-move.test.ts (the gate pin reads the paint's measure, seenNow()), tests/test_pane_state_broadcast.py (LazyPanes with the failed-load, http-error-body, promotion-token, show-hook and desktop-script-abort cases, RelayArms' lazy Files case, the markup pin, the script count), tests/test_kernel_mobile.py (LazyPaneLinked and the landing pins, the loader content pin, the script counts), tests/test_pane_loader_reconnect.py (the executed _pane_spin harness for the first-paint hold), tests/test_client_diag_allowlist.py (the pane-load-failed row), tests/test_pane_shim_return.py (_run's before seam), tests/test_waiting_pane.py, tests/test_files_pane.py and tests/test_kernel_pane_rail.py (the f-files data-src pins), tests/test_return_from_background_served.py and return_from_background_browser.mjs (the eager set, the lazy counts, the tab-tap leg and the tapped Firefox and WebKit legs with the aborted first fetch, the http-error-body leg, the boot-tab leg and its engine twins, the skeleton term, the feed's first-paint witness by the loader's measure, the loader-paint witness, the return chain count, the derived-set guards), ui/webview/file-view.test.ts (the panes-handler pin follows the chat-shown re-arm), ui/webview/pane-focus.test.ts (the setActive pin admits gateOnShow), ui/webview/notify-click-reveal.test.ts and ui/webview/feed-focus-local-switch.test.ts (the revealCard fallback pins follow revealDecision's open arm)
added: 2026-09-18
pr: 821
tier: feature
offered:
closed:
---
Stage 0 of the reconnect design (client half). On the phone layout only the chat, the feed (exempt: the bell) and the stored tab load at boot; the Outline, the Sessions band, the Waiting pane and the Files pane load on their first tap, with the src set before the re-tell and a shell-owned loader until the iframe load event. The phone chat pane first dial declares skeleton=1, so a cold open is served one full plus statuses; the idle prefetch chain starts from the stored tab first full applied (or from the strip when it lists no such tab) instead of the strip first paint; an off-screen feed on the phone applies its first frame without painting the board. The desktop's panes and dial are unchanged; the prefetch start gate runs on every layout (it orders the chain behind the active tab's first full). The Files pane is lazy too: its markup line is the one upstream token this fork changes (src to data-src), taken on the coordinator's decision of 2026-09-18; the shell's viewFile and browseFiles relays wait for a just-promoted pane's page load (two inserted lines). Fails-before tests T1 to T6 per the stage-0 design. Offer waits on the user word.
