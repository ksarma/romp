---
title: Lazy off-screen panes and the skeleton first dial on the phone
status: candidate
where: kernel/kernel.py: _LANDING_MOBILE_JS (promote with its per-promotion token minted on every promotion and clearing a standing failed class, docState classifying the frame's document in five answers (none, blank, the pane's own by the shim's window marker, a 200 the kernel served by the stamp Handler._send writes on every text/html 200 whose body has an <html> tag (_stamp_served_html: data-romp-served=200 on that tag; a rootless body, the token page, is served unstamped), shown as served and said by the pane-load-unmarked row, or a document with neither, a failure), failed reading the layout at fire time and docState's answer (the phone's re-park under data-lazy-src and failed state; the desktop's table: a fetch still in flight at the backstop held with its src, one re-promotion per episode, and at the bound a document the kernel sent dropped from the frame with its url under data-lazy-src, the attribute the controller's gear-save reconcile does not read, while the browser's own error page keeps its src, the hold and the bound recorded in DEAD), every promotion arming the load listener and the 30 s backstop with PEND as the backstop's guard, the EPI episode counter beside FAILS, paneDiv, paintLoading with body.pane-failed, the #pane-load-msg copy by episode and the #pane-load-retry button's hidden, loaded resetting the episode, unmarked filing the pane-load-unmarked row, _stamp_served_html and its one call at the top of Handler._send, the #pane-load tap and the #pane-load-retry click retrying, the data-lazy-src parking at boot, the promotion inserted in show() before the re-tell and the shown pane's synchronous __rompPaneShown hook inserted after the m-on toggle, the lazyFlip media-query listener with its flip-back parking, a DEAD pane included and checked first, with the failed state), _LANDING_DESKTOP_PANES_JS (the desktop promotion of the Waiting and Files panes, its own script spliced before the mobile script), _pane_spin (the fork's latch: held set by romp:firstpaintheld, which also re-shows the sheet, cleared by romp:firstpaintreleased, which re-arms, and two fork listeners after upstream's wsdown and wsup standing the socket's arms down while held), CLIENT_DIAG_KEYS['shell'] (pane, n for the pane-load-failed row; pane, via for pane-load-unmarked) and CLIENT_DIAG_KEYS['feed'] (itemId, sid, why, key, painted for reveal-dropped), _LANDING_ERRS_JS (the Log row's __rompMobileTab('feed') before its revealCard post), _LANDING_COLLAPSE_JS panesMsg (the mob layout word), _shim (the fork line after parentMobile setting RESTART_DIET for the phone chat pane first dial and its comment naming both readers of the flag), _LANDING_SETTINGS_JS (the two inserted about:blank waits in the viewFile and browseFiles arms; the gear opener's tap-time dead-document check reading the url and the shim's marker, its one load listener, and the fork's sArmed, sOpen and sDeferred declarations on their own lines after the project's `var sPend=false;` line (PR 821 pass 6, 2026-09-20; the pass ids in this entry are the author's build labels, not the reviewer's review rounds)), _landing (the head layout probe before any iframe, the head's notified-session seed after the probe (?push-reveal= into the chat blob's activeId before the chat iframe parses, pass 3), the f-waiting and f-files data-src, the #pane-load element with its #pane-load-msg (role=alert) and #pane-load-retry button children and their CSS inside and outside the phone media block, the failed state's rules), _resolve_reconnect (the parked-reveal preference, pass 4b: a reveal parked for the client's window names the one full when the tab list carries it; a parked sid the list lacks leaves the hint as before; pass 5: for a window with one chat column, keyed on the col each column declares, and on a pop that consumes; the pass-5 verify: the declaration first, the shell's chat column count posted with the tap, _LANDING_REVEAL_JS cols on the /reveal body, admitted by the route and kept on the park by _reveal_request, the sockets read the belt; a park with no declaration keeps the sockets read alone, the disclosed residual; pass 7, the reviewer's round-5 finding kernel-1: the session the preference serves whole is recorded on the client (`preferred`, written under the slot lock) and `_watched_tab` reads it in the hint's place for _push's active-first set, build_order, _all_active and the cold-tab gate, on fork lines after the project's two set lines; the page's next activeTab drops the record); ui/webview/skeleton-tabs.ts (gate, returnHold and redialed on SkeletonState, gateOnFrame and gateOnShow refusing while the hold stands, gateOnStrip keyed on the want's host and opening under the hold since pass 4b, nextPrefetch's hold term keeping the open gate silent, onSocketUp closing the gate, holding on the phone and recording the redial, onLayoutWord re-deciding the hold on the shell's layout word, nextPrefetch null while closed or held); ui/webview/render.ts (gateWant read at the head of upsert and gateOnFrame before its arm; gateOnStrip on the local strip in applyTabOrder; gateOnShow in setActive and silentActivate; the chat-visibility install's schedulePrebuild hook; the panes handler's chat-shown re-arm and its layout-word arm (onLayoutWord, gateOnShow); phoneShell read at the wsup arm); ui/webview/chat-visibility.ts (the onShown hook); ui/webview/paint-gate.ts (firstPaintHeld, viewportHiddenSinceLoad, revealDecision with willPaint and the shell's word, its drop arm); ui/webview/feed.ts (predictFollowMoves, the pure transform applyFollowMove applies in place and paintedKeyOf plans over; paintPlan and paintedKeyOf, the render's one derivation; feedShellOn, parentMobile, revealShown and seenNow, pendingRevealKey with its retirement on the pane's next visibility change and the first hidden word, pendingReveal beside it, revealDropped, firstPaintHoldTold and the two loader events, cardByKey, jumpToCard, the render() gate line, the observer's callback, the panes handler, the revealCard handler's plan-decided park bounded by the shell's word, written or cleared before its arms so a second reveal replaces or drops it, its drop arm, window.__rompPaneShown, releasePaint's pending reveal with its losing arm); docs/read-side.md (the lazy panes file no rows; the pane-load-failed and pane-load-unmarked rows; the phone first dial skeleton row; a return on the phone reloads the visible tab alone, the hold's two rules, the hold following the layout, the true reason for the one rule); tests: ui/webview/skeleton-tabs.test.ts (T2, T3, T5, T6 flipped to the return hold, T6b the layout word, the remote want, gateOnShow), skeleton-tabs-wiring.test.ts, render-link-word.test.ts (the two-reads pin, the hold term, the lifted panes handler with the layout word), chat-visibility.test.ts (the show hook), chat-split-exec.test.ts (the lifted strip world's gate and arm), paint-gate.test.ts, feed-hidden-paint.test.ts (T4, the held reveal decided from feed.ts's lifted paint plan, HIGH-1 case g through the lifted predictFollowMoves and applyFollowMove, D3, D4 with the observer speaking before the show and the re-show through the override, D5 with the bound and the losing arm on the phoneFeed copies and on feed.ts's own lines through the feedWiring park accessors, the revealWiring companion and the panesWiring lift), paint-gate.test.ts (the decision table's shellOn column), federation-hidden-hold.test.ts (the fifth release site, the observer's callback, the install pin), feed-age.test.ts (the observer's callback), feed-freeze.test.ts and feed-session-filter.test.ts (render reads the view through paintPlan), feed-followup-move.test.ts (the gate pin reads the paint's measure, seenNow()), tests/test_pane_state_broadcast.py (LazyPanes with the failed-load, unmarked-document, promotion-token, layout-flip, episode-counter and retry-button, show-hook and desktop-script-abort cases, RelayArms' lazy Files case and the gear's dead-document case, the boot word's mob, the markup pin, the script count), tests/test_kernel_mobile.py (LazyPaneLinked with the real shim's marker read by the shell's listener, the landing pins with the retry button, the loader content pin, the script counts), tests/test_error_center.py (the Log row's tab switch in order), tests/test_pane_loader_reconnect.py (the executed _pane_spin harness for the first-paint hold and its latch under a socket blip), tests/test_settings_page.py (the gear opener's byte pins: the once-armed load listener and the tap-time dead-document read), tests/test_client_diag_allowlist.py (the pane-load-failed, pane-load-unmarked and reveal-dropped rows), tests/test_lazy_pane_layout_flip_served.py and lazy_pane_layout_flip_browser.mjs (a failure judged across an actual viewport flip in Chromium, both roads, the desktop's second failure recovered back on the phone by each of the tab tap, the overlay tap and the Try again button, the kernel's own 403 at the desktop's bound dropped from the frame, and a slow desktop load held through the backstop), tests/test_pane_shim_return.py (_run's before seam), tests/test_waiting_pane.py, tests/test_files_pane.py and tests/test_kernel_pane_rail.py (the f-files data-src pins), tests/test_return_from_background_served.py and return_from_background_browser.mjs (the eager set, the lazy counts, the tab-tap leg and the tapped Firefox and WebKit legs with the aborted first fetch and the retry button's keyboard reads, the denied-document leg and its Firefox and WebKit twins (the real kernel's 403 by a credential-less route.fetch, the status and the page's state read, never the body), the unmarked-document leg (the kernel's real 200 with the shim's marker stripped, the stamp read in the engine) and the kernel's stamp census with the landing and the token page, the WebKit first-tap leg with no abort and the document-load, retirement-stamp and painted reads on every lazy-pane tap leg (gated on the tapped pane's boot state: an eager pane's tap is a show), the boot-tab leg and its engine twins, the skeleton term, the feed's first-paint witness by the loader's measure, the loader-paint witness, the return chain count, the derived-set guards, the held active full leg and the driver's hold, the Waiting and Files tap legs with the loader's retirement waited for, the transcript-less docs session and the strip read on the boot-on-Feed legs), ui/webview/file-view.test.ts (the panes-handler pin follows the chat-shown re-arm), ui/webview/follow-move-cache-echo.test.ts (the copy-on-write pins follow the transform, predictFollowMoves, and the case runs the two functions lifted from feed.ts), ui/webview/pane-focus.test.ts (the setActive pin admits gateOnShow), ui/webview/notify-click-reveal.test.ts and ui/webview/feed-focus-local-switch.test.ts (the revealCard fallback pins follow revealDecision's open arm), tests/test_notification_tap_resume_browser.py (the phone iteration of the link road: the first chat dial names the notified session and its full is the one pushed before the page's first ask), tests/test_federated_dial_terms_served.py (the phone shell's cold open with a remote attached: the relay dial after the local open, without a skeleton term, the remote's whole board), ui/webview/federation-remote-dial.test.ts (the pre-open shape's URL), tests/test_files_pane_toggle_served.py (the phone pass's Files pane content), tests/test_pane_hidden_word_browser.py and tests/pane_hidden_word_browser.js (the show hook's counter per engine), ui/webview/skeleton-tabs-wiring.test.ts (the executed panes-word belt case), tests/test_chat_skeleton_reconnect.py (the parked-reveal preference: the parked session's one full with the stored tab a skeleton, the ended and host-prefixed fallbacks, the split window, the stale twin and the fresh pop, the declared split and one-column window, the declaration beside a registered second column and the undeclared park as executed), tests/test_kernel_webpush.py (the reveal script's chat column count executed on each road and the /reveal route's shape check, every body pin carrying a one-column page's cols), tests/test_shell_reveal_unhide.py (the fork's gated userSwitch declaration after the project's line, so a desktop reveal un-hides and switches no tab); the ten upstream-present kernel.py lines the branch touched (four in _reveal_request and the /reveal route, the gear opener's `var sPend=false;` and its load listener pair, the reveal script's body line, the browse arm's gated tab swap, the f-files token; eight modified at the head, the sPend and reveal body lines restored byte-identical with a fork line after each; derived by per-commit removal over the branch's kernel.py commits, the method stated there) are the offer entry 2026-09-20-lazy-panes-upstream-line-offers
added: 2026-09-18
pr: 821
tier: feature
offered:
closed:
---
Stage 0 of the reconnect design (client half). On the phone layout only the chat, the feed (exempt: the bell) and the stored tab load at boot; the Outline, the Sessions band, the Waiting pane and the Files pane load on their first tap, with the src set before the re-tell and a shell-owned loader until the iframe load event. The phone chat pane first dial declares skeleton=1, so a cold open is served one full plus statuses; the idle prefetch chain starts from the stored tab first full applied (or from the strip when it lists no such tab) instead of the strip first paint; an off-screen feed on the phone applies its first frame without painting the board. The desktop's panes and dial are unchanged; the prefetch start gate runs on every layout (it orders the chain behind the active tab's first full). The Files pane is lazy too: its markup line is the one upstream markup token this fork changes (src to data-src), taken on the coordinator's decision of 2026-09-18, and the other upstream-present kernel.py lines the branch touched (ten with this one, by the offer entry's per-commit derivation; two of them restored byte-identical with a fork line after each) are the offer entry 2026-09-20-lazy-panes-upstream-line-offers; the shell's viewFile and browseFiles relays wait for a just-promoted pane's page load (two inserted lines). Fails-before tests T1 to T6 per the stage-0 design. Queued as its own change (pass 3, the author's label, taking the reviewer's round-2 finding ui-2): a pointer term in _MOBILE_MQ, so a narrowed mouse window keeps the desktop grid; it shares a root with the layout-flip family (it would shrink the flip window) and is a change to the phone layout's trigger, bigger than this PR. Offer waits on the user word.
