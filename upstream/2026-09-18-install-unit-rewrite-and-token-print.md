---
title: install.sh writes the unit under a running manager and prints no token for an unserved dashboard
status: candidate
where: install.sh, bin/romp-service (the rewrite verb and its --check mode; the round-3 addendum's unit and plist readers, _unit_scan and _plist_readable, and the one writer, _unit_word), bin/README.md, kernel/kernel.py (_NO_RESTART_ACTIONS, _is_primary_kernel, _run_update, the /update route), bin/romp-manager (specEnv's ROMP_KERNEL_ID, ensureKernel's stale consult), tests/conftest.py, tests/install-sh.bats, tests/install-optional-deps.bats, tests/romp-service.bats, tests/test_restart_cuts.py, tests/test_kernel_update.py, tests/test_main_drift_notice.py, tests/test_update_banner_confirm_served.py, tests/__init__.py, tests/test_state_isolation_order.py, tests/manager-registry.test.js, tests/manager-stale-consult.test.js, tests/romp-service-differential.py, tests/README.md, docs/reference.md
added: 2026-09-18
pr: 778
tier: fix
offered:
closed:
---
Two findings of the box administrator's hazard review of the merged pull-in (2026-09-16). (1) install.sh skipped its whole service step when romp-service status said running (its 2026-07-21 guard against the macOS bootout), so a unit change a release carried, the MALLOC_ARENA_MAX=2 line the memory fix needs, never reached a box that installed while its manager ran, and the administrator added a drop-in by hand. A running manager now takes the new romp-service rewrite: the unit (the plist on macOS) is written whole, by rename, systemd is reloaded on Linux, nothing is restarted, and one line says the manager keeps its old unit until its next restart and names the command; a failed rewrite fails the run. (2) Under ROMP_NO_SERVICE the closing lines printed the dashboard URL with the serve token read from disk, a credential into the scrollback and every scripted install's log for a dashboard this run is not serving; that road now prints the bare URL and says romp url prints the token. Upstream ships both the skip and the print; an offer waits on the user's word. Round 1 of the review (2026-09-18): the rewrite keeps the unit's own identity (ExecStart, ROMP_DIR, PATH, the service.env path, the instance lines) and refuses on a differing compared value (ExecStart and ROMP_DIR against the clone, the instance variables and a set service.env path against the environment; PATH is kept, never compared); its audit row is written after the checks and is in the kernel's no-restart set, since the row read as the restart request behind any SIGTERM within ninety seconds of a deploy; the self-update's child carries no instance port or Claude config dir and runs from the primary kernel only (the manager hands each kernel its id); after the reload the rewrite reads NeedDaemonReload back. Round 2 (2026-09-18): the kernel's self-update preflights the rewrite's identity check (rewrite --check) before it fetches the release and moves the checkout, and its report says when the tree moved without a restart (the auto marker withheld then); the identity refusal has its own exit code, 5, and install.sh points at its lines instead of a retry command; absence is read as the service reads it (no PATH line stays absent, the service.env path is compared only when the deploying shell set it, the state root is the file's); the read-back after the reload covers FragmentPath and both writing roads; the marked update child keeps an installed unit's identity on the install road; a non-primary's drift click is no longer refused over a known release; the manager's /ensure consults the stale-manager check. Round 3 (2026-09-19): the plist reader asserts the file's form before it trusts any absence (plutil when present, else romp's one-line layout, refused with exit 5 otherwise, since a plist any macOS tool had re-saved read as one with no entries and the rewrite dropped PATH, the instance block and the log paths at exit 0), unescapes once on read and escapes once on write; the update banner's click carries the offer it showed, and the route does that or refuses (a non-primary kernel's release banner ran the main-drift converge on its click); install.sh's install road branches on the identity refusal as the rewrite road does, with no retry line and no false end state; a quoted instance line goes back as written and an install quotes a value systemd would otherwise cut; the update script reads whether the tree moved from the tree, so a failed install over an unmoved checkout keeps the auto marker; the closing dashboard link reads the state root's serve-port record; both advance-without-restart marker legs are pinned. The addendum to round 3 (2026-09-19, two pre-run lenses) installs one rule in both readers: a reader that cannot read a form whole refuses it (exit 5, nothing written, the form and the remedy named), and a value it accepts is written back in a form systemd or launchd reads identically, verified by parsing the written file the way the daemon does. The plist fallback refuses any entry it reads that is split across lines and a ProgramArguments array not laid out one string a line (a one-line plist with one hand-split entry had passed the two-marker check and read that entry as absent); the unit is parsed once as systemd parses it (blanks around =, sections, word splitting and unquoting, %% undone, a later assignment replacing an earlier one) and refuses a continuation, a specifier, an unbalanced quote or unknown escape, two assignments on a kept line, a second ExecStart or EnvironmentFile line, a prefixed or non-`up` ExecStart and a kept line outside [Service]; ROMP_DIR, ExecStart's command, every instance variable and the service.env override go through one writer on both roads, quoting whitespace, either quote, a backslash and a percent; the fallback plist reader decodes numeric character references. The second pass over the addendum (2026-09-19): an empty assignment (Environment=PATH=, an empty <string></string> entry) is a value the daemon reads, the variable set to the empty string, kept as written and compared as a value, where the value alone had read as absence and the line was dropped at exit 0; a manager path systemd refuses as an executable name (a quote, a backslash, a control character) is refused on install and on rewrite so no unit is written that cannot start, and the tests' unit oracle models that check; the plist reads the sweep found outside the form check (a key on two lines, a ProgramArguments array with up anywhere but once and last, or with none through plutil) are routed through it and refused. Round 4 (2026-09-19, thirty-eight findings, none refuted, two high): the unit reader's line ending is read as systemd reads it (one trailing CR off before the comment skip and the continuation count, which ran first and on the raw line, so a comment ending in a backslash refused every deploy while a CRLF continuation was read as live; a bare CR or a NUL is refused, since systemd ends a line there and bash's read does not, and a CR-only unit had read as one line and re-pointed ExecStart at exit 0); a section header systemd refuses whole (not ending in ], or a quote, backslash or control character in the name) and a line that is not UTF-8 are refused, since systemd loads nothing from such a file where the reader read on; blanks are systemd's set; every escape systemd decodes is decoded (\a \b \f \v \xNN \NNN \uNNNN \UNNNNNNNN) and the refusal texts say what systemd does (drops the item and the rest of the line, the items before it standing); a specifier in a PATH line or a dropped variable is no refusal; the EnvironmentFile prefix goes back as it came; a kept value ending in a newline is refused on the unit road, the plist road and at install, and a newline inside a value round-trips (&#10; on the plist); drop-ins are not read and the rewrite says so at exit 0 (reading or refusing them would refuse a live deploy whose drop-in resets ExecStart through a shell wrapper; the residual, a fragment with no ExecStart beside a non-resetting drop-in, is named in the reader); the tests' oracle models systemd 255 and was checked against systemd-analyze verify, raising where it does not model; every refusal that shipped untested has its case in both directions; the once-only marker's write no longer stops the launch; the dead second 409 is gone; the ROMP_KERNEL_ID floor is in both test entry points. The fold after round 4's oracle lens (2026-09-19): the lens ran the tests' oracle and systemd-analyze verify (systemd 255.4) over 684 synthetic units and found 125 disagreements in 11 classes; classified by running the reader on each class beside systemd, four were the reader's too and are fixed (a % before a non-alphanumerical character is a % to systemd and reads as one; a \u noncharacter, a \U surrogate or noncharacter and a raw noncharacter are refused as systemd drops or refuses them, where the reader had carried them and written them raw, a line systemd loads nothing from; a continuation backslash on the file's last line is read as systemd parses it), the other seven were the oracle's alone (the deprecated %c %r %R, the @ prefix's argv, repeated prefixes, a quoted ; argument, executable-name validity, the simplified exec path, the - prefix's downgrade) and the oracle now reads them as systemd does; the differential is checked in as tests/romp-service-differential.py with its systemd version and expected counts, and zero disagreements remain on 255.4. The fold's addendum (2026-09-19, after the mutation pass on the fold): a continuation backslash that only comment lines separate from a blank line or the file's end is read as systemd parses it (the fold had read the file's last line alone, and a comment line after the backslash was still refused where systemd reads the value); the remedy for a \u surrogate or noncharacter escape is to remove it, where it said to write the character, which is the whole-file refusal; the accepted side of each refused Unicode range, the last plane's noncharacters, the 255-byte name and component, the argument text of an escaped ; and the census of refusal call sites are pinned, and the recipe's fold batch carries the new shapes.
