---
title: install.sh writes the unit under a running manager and prints no token for an unserved dashboard
status: candidate
where: install.sh, bin/romp-service (the rewrite verb and its --check mode; the round-3 addendum's unit and plist readers, _unit_scan and _plist_readable, and the one writer, _unit_word), bin/README.md, kernel/kernel.py (_NO_RESTART_ACTIONS, _is_primary_kernel, _run_update, the /update route), bin/romp-manager (specEnv's ROMP_KERNEL_ID, ensureKernel's stale consult), tests/conftest.py, tests/install-sh.bats, tests/install-optional-deps.bats, tests/romp-service.bats, tests/test_restart_cuts.py, tests/test_kernel_update.py, tests/test_main_drift_notice.py, tests/test_update_banner_confirm_served.py, tests/__init__.py, tests/test_state_isolation_order.py, tests/manager-registry.test.js, tests/manager-stale-consult.test.js, tests/romp-service-differential.py, tests/test_romp_service_differential_mark.py (the differential's dangerous mark and its self-check, pinned in the suite), tests/README.md, docs/reference.md, tests/test_bats_bare_negation.py (main's version again since round 9, this PR's diff of it empty; the scan's growth is its own fork PR, the bare-negation scanner), tests/test_upstream_ledger.py (this entry's where line and round currency, derived per entry from the commits that touched it), tests/test_romp_service_docs.py (the two exit-5 documents' plutil and file-form sentences), tests/test_docs_anchors.py (every in-page link of the documentation resolves to a heading of its file, round 10; its slugger held to the renderer over the derived corpus, round 12), tests/docs-anchors-oracle.py and tests/fixtures/docs_anchor_slugs.json (the renderer's slug for every heading of the corpus and the battery, by execution, and the command that rewrites it; round 12), .github/workflows/ci.yml (the extension job's docs-anchors-oracle --check step)
added: 2026-09-18
pr: 778
tier: fix
offered:
closed:
---
Two findings of the box administrator's hazard review of the merged pull-in (2026-09-16). (1) install.sh skipped its whole service step when romp-service status said running (its 2026-07-21 guard against the macOS bootout), so a unit change a release carried, the MALLOC_ARENA_MAX=2 line the memory fix needs, never reached a box that installed while its manager ran, and the administrator added a drop-in by hand. A running manager now takes the new romp-service rewrite: the unit (the plist on macOS) is written whole, by rename, systemd is reloaded on Linux, nothing is restarted, and one line says the manager keeps its old unit until its next restart and names the command; a failed rewrite fails the run. (2) Under ROMP_NO_SERVICE the closing lines printed the dashboard URL with the serve token read from disk, a credential into the scrollback and every scripted install's log for a dashboard this run is not serving; that road now prints the bare URL and says romp url prints the token. Upstream ships both the skip and the print; an offer waits on the user's word. Round 1 of the review (2026-09-18): the rewrite keeps the unit's own identity (ExecStart, ROMP_DIR, PATH, the service.env path, the instance lines) and refuses on a differing compared value (ExecStart and ROMP_DIR against the clone, the instance variables and a set service.env path against the environment; PATH is kept, never compared); its audit row is written after the checks and is in the kernel's no-restart set, since the row read as the restart request behind any SIGTERM within ninety seconds of a deploy; the self-update's child carries no instance port or Claude config dir and runs from the primary kernel only (the manager hands each kernel its id); after the reload the rewrite reads NeedDaemonReload back. Round 2 (2026-09-18): the kernel's self-update preflights the rewrite's identity check (rewrite --check) before it fetches the release and moves the checkout, and its report says when the tree moved without a restart (the auto marker withheld then); the identity refusal has its own exit code, 5, and install.sh points at its lines instead of a retry command; absence is read as the service reads it (no PATH line stays absent, the service.env path is compared only when the deploying shell set it, the state root is the file's); the read-back after the reload covers FragmentPath and both writing roads; the marked update child keeps an installed unit's identity on the install road; a non-primary's drift click is no longer refused over a known release; the manager's /ensure consults the stale-manager check. Round 3 (2026-09-19): the plist reader asserts the file's form before it trusts any absence (plutil when present, else romp's one-line layout, refused with exit 5 otherwise, since a plist any macOS tool had re-saved read as one with no entries and the rewrite dropped PATH, the instance block and the log paths at exit 0), unescapes once on read and escapes once on write; the update banner's click carries the offer it showed, and the route does that or refuses (a non-primary kernel's release banner ran the main-drift converge on its click); install.sh's install road branches on the identity refusal as the rewrite road does, with no retry line and no false end state; a quoted instance line goes back as written and an install quotes a value systemd would otherwise cut; the update script reads whether the tree moved from the tree, so a failed install over an unmoved checkout keeps the auto marker; the closing dashboard link reads the state root's serve-port record; both advance-without-restart marker legs are pinned. The addendum to round 3 (2026-09-19, two pre-run lenses) installs one rule in both readers: a reader that cannot read a form whole refuses it (exit 5, nothing written, the form and the remedy named), and a value it accepts is written back in a form systemd or launchd reads identically, verified by parsing the written file the way the daemon does. The plist fallback refuses any entry it reads that is split across lines and a ProgramArguments array not laid out one string a line (a one-line plist with one hand-split entry had passed the two-marker check and read that entry as absent); the unit is parsed once as systemd parses it (blanks around =, sections, word splitting and unquoting, %% undone, a later assignment replacing an earlier one) and refuses a continuation, a specifier, an unbalanced quote or unknown escape, two assignments on a kept line, a second ExecStart or EnvironmentFile line, a prefixed or non-`up` ExecStart and a kept line outside [Service]; ROMP_DIR, ExecStart's command, every instance variable and the service.env override go through one writer on both roads, quoting whitespace, either quote, a backslash and a percent; the fallback plist reader decodes numeric character references. The second pass over the addendum (2026-09-19): an empty assignment (Environment=PATH=, an empty <string></string> entry) is a value the daemon reads, the variable set to the empty string, kept as written and compared as a value, where the value alone had read as absence and the line was dropped at exit 0; a manager path systemd refuses as an executable name (a quote, a backslash, a control character) is refused on install and on rewrite so no unit is written that cannot start, and the tests' unit oracle models that check; the plist reads the sweep found outside the form check (a key on two lines, a ProgramArguments array with up anywhere but once and last, or with none through plutil) are routed through it and refused. Round 4 (2026-09-19, thirty-eight findings, none refuted, two high): the unit reader's line ending is read as systemd reads it (one trailing CR off before the comment skip and the continuation count, which ran first and on the raw line, so a comment ending in a backslash refused every deploy while a CRLF continuation was read as live; a bare CR or a NUL is refused, since systemd ends a line there and bash's read does not, and a CR-only unit had read as one line and re-pointed ExecStart at exit 0); a section header systemd refuses whole (not ending in ], or a quote, backslash or control character in the name) and a line that is not UTF-8 are refused, since systemd loads nothing from such a file where the reader read on; blanks are systemd's set; every escape systemd decodes is decoded (\a \b \f \v \xNN \NNN \uNNNN \UNNNNNNNN) and the refusal texts say what systemd does (drops the item and the rest of the line, the items before it standing); a specifier in a PATH line or a dropped variable is no refusal; the EnvironmentFile prefix goes back as it came; a kept value ending in a newline is refused on the unit road, the plist road and at install, and a newline inside a value round-trips (&#10; on the plist); drop-ins are not read and the rewrite says so at exit 0 (reading or refusing them would refuse a live deploy whose drop-in resets ExecStart through a shell wrapper; the residual, a fragment with no ExecStart beside a non-resetting drop-in, is named in the reader); the tests' oracle models systemd 255 and was checked against systemd-analyze verify, raising where it does not model; every refusal that shipped untested has its case in both directions; the once-only marker's write no longer stops the launch; the dead second 409 is gone; the ROMP_KERNEL_ID floor is in both test entry points. The fold after round 4's oracle lens (2026-09-19): the lens ran the tests' oracle and systemd-analyze verify (systemd 255.4) over 684 synthetic units and found 125 disagreements in 11 classes; classified by running the reader on each class beside systemd, four were the reader's too and are fixed (a % before a non-alphanumerical character is a % to systemd and reads as one; a \u noncharacter, a \U surrogate or noncharacter and a raw noncharacter are refused as systemd drops or refuses them, where the reader had carried them and written them raw, a line systemd loads nothing from; a continuation backslash on the file's last line is read as systemd parses it), the other seven were the oracle's alone (the deprecated %c %r %R, the @ prefix's argv, repeated prefixes, a quoted ; argument, executable-name validity, the simplified exec path, the - prefix's downgrade) and the oracle now reads them as systemd does; the differential is checked in as tests/romp-service-differential.py with its systemd version and expected counts, and zero disagreements remain on 255.4. The fold's addendum (2026-09-19, after the mutation pass on the fold): a continuation backslash that only comment lines separate from a blank line or the file's end is read as systemd parses it (the fold had read the file's last line alone, and a comment line after the backslash was still refused where systemd reads the value); the remedy for a \u surrogate or noncharacter escape is to remove it, where it said to write the character, which is the whole-file refusal; the accepted side of each refused Unicode range, the last plane's noncharacters, the 255-byte name and component, the argument text of an escaped ; and the census of refusal call sites are pinned, and the recipe's fold batch carries the new shapes.

Round 5's preface (2026-09-19, four commits on the fold's addendum, with one addendum of their own): D3 is pinned over %c %r %R beside %h; the dangerous mark counts a both-set mismatch and a shorter list carrying a command or a file systemd does not run or read; the fold case's locale leg fails with a remedy instead of falling back; the ExecStart escape refusal says why and how, and no longer says the install restarts the manager; install.sh's exit-3 message says what the install does on each platform, the restart clause's population derived over the tree; the reference names both escape surfaces; the EnvironmentFile spelling residual is disclosed and left.

Round 6 (2026-09-19 to 2026-09-20, a commit and two addenda): the unit reader takes a UTF-8 byte order mark once per file, after the comment test, as systemd does, and the mark's remaining shapes are pinned on the three roads; the plist reader learns plutil's line end on a scratch plist it writes, never on the file's Label; a comparison systemd-analyze cannot supply (the exec path of a first command under the - prefix) is a REFUSES row with its reason, printed under its own heading; the shared refusal tail is pinned where it renders; the dangerous mark has a table of synthetic pairs on both sides of its shapes, run before the sweep and pinned in the suite; a UTF-8 checker that cannot check says so and names what to install; the control-character arm of the executable-name check is spelled as systemd's byte set; a bare negation that asserted nothing under bats is a checked form.

Round 7 (2026-09-20): the plutil calibration measures a value with no line end of its own and three that end in one (a top-level string, a nested entry, an array element), sets the flag only when the four agree on one behaviour, and refuses, naming the probe and the bytes it read, a plutil under which a value's own trailing newline cannot be told from the tool's (round 6 had measured the one class of value where the flag changes nothing, so a plutil writing a line end only when the value lacks one read a Label ending in a newline a newline short and the rewrite wrote the values and the program's path short at exit 0; the refusal is the tool's, every plist included, and says so with the two ways out); the shapes case's helpers live at file scope (the bare-negation scan's own block-end fix left this PR in round 9 for its own fork PR, the bare-negation scanner); the mark's self-check reads its clause population from compare()'s source and requires a row that tells each clause's mutant apart, three rows added for the clauses no row isolated; the printed legend carries the qualifier the docstrings and tests/README.md carry, the four pinned alike; this entry's where line is derived from the branch's diff and pinned against it. The round's addendum (2026-09-20, after four lenses over the round-7 commit): the calibration's transfer from the scratch to the file is checked on the file, since the four probes span the value's own line end and the key shape and by construction nothing else (a scratch is not the file): under a plutil the scratch showed writing a line end, every raw extract of the file must end in one, and where the tool honours -n on the scratch every value is read with and without the switch and the two must differ by exactly the tool's line end, so a plutil writing no line end on the file, or one only when the file's value lacks one, is refused on the file at the key that shows it, where round 7 wrote the value a newline short at exit 0; a plutil that ignores or rejects -n is read one way, as round 7 read it, and the residual under such a tool is said in the reader; the mark's self-check refuses a mark set in a form its census does not read, naming the line, labels an and-tested clause whole, and carries a row where the oracle reports no command; the second-arm, the scratch's removal, the nested and array probes and the README's counts are pinned; the plutil stand-in's shapes are modes of the stand-in, not sed edits of it (BSD sed took the script as a backup suffix).

Round 8 (2026-09-20, the round-7 ruling in its order): the round's work on the bats bare-negation scan (its coverage pin against bash's parse, its block-end rule and, in the addenda, its register of shapes run under bats) left this PR in round 9 for its own fork PR, the bare-negation scanner, and the module in this PR is main's. The plutil road reads each entry's type through a second extract (xml1) beside the raw one and refuses an entry that is not a string or that extracts one way only, where an array had been written back as its element count at exit 0 and a container a plutil will not render raw had been dropped as absent; every value it reads is echoed through the scratch plist under the same key, so a line end keyed on the value's bytes, the key's name or the array index is told on the file's own values; the reader's comment states the rule (these reads tell the tool's line end from the value's own only for a tool that reports the value's bytes and treats the scratch as the file) and the residual at its width (a tool that alters the bytes, and a file-keyed tool that also ignores -n, both untested by construction), and the evidence clause says what was read (plutil(1)'s -n sentence, PLUContext.swift line 1035, no plutil binary run here); every calibration arm names the way to proceed (the Label arm had printed neither the totality sentence nor a route; the scratch arms name a writable TMPDIR or the install); the reference and bin/README.md document the plutil refusal at exit 5, pinned by their text; this entry's where line is derived per entry from the commits that touched it, so a batch or a stacked branch is not blamed for another PR's files, and its body must name the latest review round those commits' subjects carry; what that scoping drops is disclosed in round 9 and not widened (correctness-3): a file changed only by commits of this branch that touch no entry is owed by no where line, so the round-7 omission this pin was made for (rules-2 and regression-4, a test module the chain created in an entry-less commit) passes green, the shape it misses a module-only commit beside an entry-only commit in either order, and the proposed widening was rejected since it reds the batch and the stacked branch the pin's own tests hold green. The round's addendum (2026-09-20, after four lenses over the round-8 commit): the plutil echo writes each value twice, as it would be written back and as the raw bytes read on the file, since a line end withheld only for a value that ends in a newline and is in a class the probes do not carry never fired on the first echo's stripped value and the value went back a newline short at exit 0 (the lens's 22 rows, the Label and the manager's path among them); the residual is restated at its width (a byte-altering tool; a tool keyed on what no scratch shares with the file, the document's other entries included, where -n is not honoured; the type read resting on the tool's own xml1 rendering), and the two exit-5 documents put the non-string entry in the file-form disjunct with the install as its way out. The ledger pins gain the whole-token direction that matters, the highest round of several and an entry deleted at head.

Round 9 (2026-09-20, the round-8 ruling): the bats bare-negation scanner leaves this PR for its own fork PR (the bare-negation scanner) and tests/test_bats_bare_negation.py is main's version again, since nothing here needs its growth (main's scanner over the 45 bats files at the round-8 head reports zero sites; that zero never covered 22 lines of this PR's own class-E addendum case in tests/romp-service.bats, since main's scanner ends a test's block at the first line holding a lone brace and the case defines a helper, `_ok()`, inside its `@test` body, whose indented closing brace ends the block 23 lines before the test's real close: the lines after it, 5338-5359 at the round-10 head, were read by hand, and closing that blind region is fork PR #871's, the bare-negation scanner), every commit that grew it was a review-round commit and none the change under review, its subject is the whole tree, its rounds were not converging (fourteen new findings in round 8 after nine in round 7, twenty-two of the round's twenty-four in that file), and a deploy-machinery diff the user must read should carry the change the user is asked to approve alone; the `! cmp` fix the scan found in tests/romp-service.bats stays, a product-test fix and not the scan. The where pin's dropped case is disclosed above (correctness-3) and the pin is not widened. The file-keyed residual of the plutil reader is stated once, in clause (b) of the RULE in _plutil_calibrate's comment, and the two bats cases and the PR body point at it (extra6-1: a four-item copy of the five-item list had drifted in the echo case's comment and was attributed to the reader).

Round 10 (2026-09-20, the round-9 ruling): the update banner's failed ending adopts the answer's standing offer in a window that made none (a page loaded while the update ran, or one the running push flipped into the wait before it offered anything), so the re-shown Update's confirm names the release or the main sha and the route starts it, where since round 3 that click had posted an empty offer and been refused (400, the click named no offer) until the page was reloaded, a regression against main, whose route derived the action from its own slots; a non-primary kernel in auto mode is gated before the discovery latch, with a slot of its own for the once-per-version notice, so it holds no offer it never makes and /update-check reads no counts and dials no registry on a page load against it (the round-6 pin passed on the re-entered code because it fills the slot by hand and never runs the check pass; a twin that runs it is added), at the cost that a gear flipped from auto to ask offers the release at the next check pass rather than the next page load; the reference's dead in-page link is dropped and tests/test_docs_anchors.py resolves every in-page link of the documentation against its file's headings; the echo case's pointer in tests/romp-service.bats lists nothing; this entry's round-9 sentence says what main's scanner scanned and names the blind region.

Round 11 (2026-09-20, the round-10 ruling): the PR body's line numbers and diff figures are derived by a script outside the repository, run against the commit the body names as the last act before each push, so a later commit cannot leave them stale unnoticed (rounds 9 and 10 each found citations one commit behind the head); tests/test_docs_anchors.py's slugger leaves an intraword underscore pair alone, as GitHub's slug does, and pins a heading with two underscores in both directions (its correct link green, a dead link to the round-10 module's wrong slug red); the banner's adoption comment says what the code does, that any updated ending the adopting window reads, its own retry's or a later push's, dismisses the adopted identifier durably, with that road pinned; and the property the confirm step exists for is named by a test: the /update route acts only on the offer it owns, and a confirm naming a kind outside release and main, or an identifier that is not the release tag or the main-drift commit this kernel currently holds, is refused and starts nothing, red at this PR's base, whose route derived the action from its own slots.

Round 12 (2026-09-20, the round-11 ruling; the PR body's derivation script is outside the repository and its round-12 changes are its own): the /update route's ownership is stated as the route has it and observed, not inferred (extra7-1, regression-3): a confirm is refused unless its kind is release or main and its identifier equals what the kernel's own slot holds now, the release tag or the main-drift commit, whether or not any banner showed it (round 11 had written the check as keyed on what the banner showed, which is wider than the route; its sentence above reads the route's property since the round-12 fix-up); the launchers are recorded, not patched to raise, since the route starts the converge on a daemon thread that swallowed the raise and left the pin green under a converge on a handed offer, the record is written by the launcher and by the thread's construction, which the route does before it answers, the thread is joined, and the record is shown reachable by a confirm naming the slot's main commit and one naming its release, each launching; the same record replaces the raising patches at the route's two sibling refusals. tests/test_docs_anchors.py's slugger reads emphasis as CommonMark does (tests-2, extra8-1, extra8-2): left- and right-flanking delimiter runs for asterisk and underscore, the process-emphasis pairing with the rule of three, the runs between a pair literal, an intraword underscore run never a delimiter, an unpaired run kept, a code span's text opaque, a tag dropped, an entity decoded, an escape's character kept, where round 11's underscore arm stripped unbalanced runs, admitted one intraword underscore in a span and read no whitespace flanking; the population is derived, every ATX heading of the anchor pin's files and of plans/ (597 headings in 59 files, 530 distinct at the round-12 commit, before the merge of main; the table's corpus field carries the counts at the head) plus a battery enumerated from a grammar of runs and the shapes CommonMark's examples name (457), and the renderer's slug for each (marked 12.0.2, the extension's, rendering the heading and github-slugger's rule over the h element's text) is written by tests/docs-anchors-oracle.py into tests/fixtures/docs_anchor_slugs.json, which the test holds the slugger to, failing on a heading with no row, a row with no heading and any disagreement; the table is the oracle in the suite because CI's Python job has no node and no marked, and the recipe refuses to run without them rather than skipping, and CI's extension job, which has both, runs the recipe's --check, so a table edited to agree with a wrong slugger is caught there; over the derived corpus round 11's arm agreed on every real heading and disagreed on 123 of the battery's 457 shapes (111 underscore runs, 12 whitespace-flanked asterisk runs), the fixed slugger on none of the 987 rows at that commit. The adoption comment's reason is corrected (kernel-1): the Not now after an updated ending posts the identifier the window holds, adopted or its own, never read against the identifier that landed, so on another door's landing a standing offer that did not land is retired durably for every window; round 11 had called the held offer moot once any update lands, false on that road; the write is pinned as it stands for both windows, and the narrowing to the landed identifier is left as a behaviour decision for its own change, the smaller true change being the sentence. The second round-12 fix-up, after the merge of main (which added two corpus headings and two rows): the counts above and the module docstring's name their vintage, the adoption comment says that only the dismissal store's twenty-entry cap ever drops a dismissed identifier (the previous wording said nothing in the store clears it; _dismiss_update keeps the last twenty), and tests/README.md's recipe paragraph names the extension job's --check step.
