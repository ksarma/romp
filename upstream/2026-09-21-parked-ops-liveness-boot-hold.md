---
title: The parked-ops liveness test releases the boot judge hold it predates and ends its producer on every exit path; a census over tests/ holds every loop or waiting thread a test starts to a stop that runs on every exit path
status: candidate
where: tests/test_kernel_parked_ops_liveness.py (module docstring, DeliveryRidesTheSettle.setUp, DeliveryRidesTheSettle.tearDown, DeliveryRidesTheSettle._census_check, DeliveryRidesTheSettle._pass_stubs, DeliveryRidesTheSettle._pass_patches, DeliveryRidesTheSettle._end_producer, DeliveryRidesTheSettle._pass_in_flight, DeliveryRidesTheSettle.test_parked_op_delivers_on_settle_while_a_judge_pass_is_stuck, DeliveryRidesTheSettle.test_a_body_that_fails_before_its_tail_leaves_no_producer_behind); tests/test_thread_stop_census.py (new module); tests/test_view_deltas.py (_py_maps.put); tests/test_kernel_parked_ops_lock.py (OneLockAroundEveryMutation.test_a_cancel_mid_call_cannot_shift_the_list_under_the_drains_pop, OneLockAroundEveryMutation.test_a_cancel_during_an_in_flight_send_hits_only_what_is_still_queued, OneLockAroundEveryMutation.test_a_handlers_park_completes_while_the_drain_is_inside_a_slow_send); tests/test_ws_send_bounded.py (WedgedClientCannotStallTheSendLoop._wire, WedgedClientCannotStallTheSendLoop.test_a_first_frame_larger_than_the_budget_is_delivered_whole, WedgedClientCannotStallTheSendLoop.test_a_large_frame_round_trips_byte_exact); tests/test_ledger_unproved_reads.py (CorruptBytes.test_a_second_reader_of_the_same_corrupt_bytes_cannot_move_a_fresh_ledger_aside); tests/test_host_transport.py (AttachStandDown._sweep); tests/test_sdk_stream_survives_handler_errors.py (TheLiveTailLock._hammer); tests/test_kernel_names.py (ConcurrentCreates.test_two_concurrent_new_requests_mint_exactly_one_session); tests/test_observability_routes.py (ObservabilityRoutes.test_concurrent_feed_json_reads_share_one_build); tests/test_postal_peers.py (RecallAfterTheCarry.test_a_flight_that_ends_unsent_releases_nothing_another_open_flight_holds); tests/test_post_push_coalescing.py (ControlRouteLatency.test_end_answers_fast_while_builds_run); tests/test_codex_backend.py (Lifecycle.test_same_process_metadata_mutations_commit_in_session_order, Lifecycle.test_concurrent_local_sends_keep_memory_and_registry_order); tests/test_sdk_boot_stagger.py (StaggerBoundsConcurrency.test_fourth_spawn_waits_for_a_released_slot, BootAttachesOffTheStagger.test_attaches_run_first_all_at_once_and_cold_launches_keep_the_stagger)
added: 2026-09-21
pr:
tier: fix
offered:
closed:
---
tests/test_kernel_parked_ops_liveness.py now releases the kernel's boot judge hold in setUp (the stub tests/test_judges_process.py uses, a lambda returning True, here as one of setUp's patchers so tearDown restores it) and ends the real km._producer it starts on every exit path: the producer's end (the stop seam, the gate, a bounded join), the pass's stubs and the T282 census are cleanups registered before the start and run LIFO after tearDown, and an executed leak case runs both stop shapes as nested cases. The new tests/test_thread_stop_census.py derives, by AST over tests/*.py, every thread a test starts, its kind (loop, waits, bounded) and how its stop is reached, and holds that no loop or waiting thread has its stop behind an assertion; the other sites it named, in three more files, are fixed the same way, one commit per file, and the three the second pass's stricter reading named (AttachStandDown's lease beater in tests/test_host_transport.py, the live-tail hammer's session thread in tests/test_sdk_stream_survives_handler_errors.py, the byte-exact drain in tests/test_ws_send_bounded.py) likewise, one commit per file. The third pass (romp-manager's rule, 2026-09-22) made the census's unknown case fall to the restricted side: a target the walk cannot read is kind unreadable and listed, never the excused bounded kind; bounded comes only from a body the walk read (the census now reads functions under kernel/, postal/, cli/ and bin/, resolving module aliases by value) or from a stdlib table with a reason per entry; a synthesis the parser rejects is caught and counted, never an error across the module; asserted structurally over a table of unknown shapes and over the tree. Re-deriving the 68 bounded tail-only starts the earlier rule had excused: 68 before, 61 after; the twelve movers were be.set_model (twice) and be.send (twice) in tests/test_codex_backend.py, km._push_all in tests/test_post_push_coalescing.py, be._boot_reconcile (twice) in tests/test_sdk_boot_stagger.py, a parked create in tests/test_kernel_names.py, a parked feed build in tests/test_observability_routes.py and a held peer response in tests/test_postal_peers.py (nine sites, fixed the same way, one commit per file), and s._mirror_auth_pending (tests/test_sdk_backend.py), be._lease_close (tests/test_sdk_lifecycle_hardening.py) and km._suppress_session_retry (tests/test_session_retry_suppress.py), read back as bounded from their product bodies and excused again. Every target text the census displays, keys or compares is the author's source segment, never ast.unparse's rendering (Python 3.10 renders a lambda as `lambda : x`, 3.12 as `lambda: x`; an allow entry keyed on the rendering read stale on 3.10), asserted over the module's own source; the table is identical under 3.10 and 3.12, a check rather than the proof. Why: kernel 3421c94d0 (2026-09-11, upstream-authored) put an 8 s boot hold (BOOT_JUDGE_HOLD_S, _wait_boot_attached) in front of the producer's first pass, and this test (2026-09-03) polls 5 s for that pass, so alone it failed on "a judge pass is in flight" (1 failed, 1 error in 10.9 s). The defect is a condition, not a frequency: _BOOT_ATTACHED (kernel/kernel.py:30789, set at :30860) is a one-way, process-wide latch, never cleared, and the test patches km._sdk to lambda: None, so it can never latch it itself; the test passes if and only if something earlier in the same interpreter already latched it, and fails otherwise after the 8 s hold against its 5 s deadline. Alone serially: always red. The module serially: green (its alphabetically first test's _pusher_cycle() reaches _sdk() and builds an SdkBackend whose boot reconcile latches it as a side effect). The module under -n 9: red. A full -n 10 sweep: a prior latcher in the target's worker is likely but not guaranteed: five full sweeps checked on 2026-09-21 did not fire it (those for 853, 862 twice and 887, and 781's at c7e51ae47), one did (box 2's control at 65f1895f6). An isolated-level certainty and a sweep-level flake at the same time, decided by which tests ran before it in that worker's process; never in serial CI. The red-before measurement is the single test alone, serially. The contamination disclosure is conditional: a sweep carried the leaked loop only if this test's failure line is in its pytest log, which is checkable per sweep. The failed assertion skipped the stop on the body's tail and tearDown only cleared the flag, so an unpatched judge loop ran for the rest of the worker's life; the T282 census named it and could not stop it. Upstream carries the same test and the same hold; this fix was NOT run upstream (unverified there). The fifth pass (romp-manager's seven rulings of 2026-09-22 03:14Z) made the census collect the shapes it had passed over and emptied ALLOW. A bound `.start` handed on as a value (tests/test_postal_token.py:436's `before=writer.start`) is a start row classed at the handing statement, and one whose receiver the walk cannot resolve is listed; the threads the PRODUCT starts on a test's call are counted as INFORMATIONAL rows (kind product-start, never pinned: a product object's own start(), a call of a product spawn-helper derived from the product index, a start under `if <parameter>:` counted when the argument or its default is a true literal, loop.run_in_executor, a ThreadPoolExecutor), 227 on the tree; the stdlib table excuses a name only with a reason that says it returns whatever its arguments and on any stdlib receiver, and time.sleep, server_close and a Timer's interval are conditioned on the call (a literal at or under 5 s; a server with no handler threads to join); the untimed spellings q.get(True), q.get(block=True), ev.wait(None), ev.wait(timeout=None), lk.acquire(blocking=True) and t.join(None) are untimed wherever a blocking call is judged; and ALLOW is EMPTY BY CONSTRUCTION: tests/test_view_deltas.py's _py_maps positional-key search became a bounded `for n in range(n, n + len(items) + 1)` with the same break (old and new agree on 20000 random payloads), its row reads bounded, and the entry that excused it (whose reason overstated: a plain Lock can be released by any thread, so a cleanup could have unwedged it) is deleted. The census's population is the non-recursive listing of tests/test_*.py, 936 modules at this head, a figure the table prints; the runtime oracle (thread_census / wait_for_census) is called by 5 of them, a figure the census derives and holds its docstring to.

The blast radius, proven by romp-manager's probe (2026-09-21), has two figures with different meanings. Reach: the leaked loop runs _compact_goal_stores() on its 3 s backstop (_producer_wake.wait(3)), globbing jd.GOALDIR and rewriting any store whose mtime moved; km.jd is the one process-wide judge module (kernel/kernel.py:52, jd = load_source("romp_judge", ...): one object per interpreter), and 141 test modules call jd._rebind_state(), so the leaked loop follows jd.GOALDIR to wherever the latest rebind put it: any of those 141 scheduled after the failure in the same worker (a store saved under a private sid with a cleared root was archived 7.5 s later by the leaked thread). Visible set: 13 modules save stores with cleared roots and 18 assert on the archive; that is where a wrong result would surface, a spurious pass as well as a spurious failure in a module that did nothing wrong, and the spurious pass is the dangerous direction. 13 is not the exposure; 141 is the reach. Not determined, carried: whether the leaked loop produced flakes in earlier sweeps. The SdkBackend side-effect build is benign on its own (its orphan reap is gated on its own empty registry; sweep_dead_test_roots removes only romp-tests-* roots whose marker names a dead pid).

Measured in hermetic scopes with a 17-byte TMPDIR. At the fourth pass's head 6afe49254 (65f1895f6 for the befores): the target alone 1 failed, 1 error in 10.94 s before, 1 passed in 0.19 s after; the module serially 9 passed in 0.28 s before, 10 passed in 0.88 s after; the module under -n 2 --dist load 10 passed in 1.10 s; the leak case alone 1 passed; the leak case with a planted self.fail after tail.run(res), in a copy, 1 failed and 0 errors (before the cleanup registered ahead of the nested run: 1 failed and 1 error, the producer and its two tiers left behind); the six modules fixed in the third pass 55, 11, 97, 4, 121 and 30 passed (3.12). At the fifth pass's final code head (5fc6c35a2 and the review commit after it, which closed the adversarial review's findings on the census and moved no tree row): tests/test_thread_stop_census.py 43 passed under Python 3.12 (55 s) and 3.10 (160 s), the tree table identical under both: 936 modules read (the printed figure; an earlier revision of this entry said 948, a miscount), 411 start rows (204 loop, 31 waits, 174 bounded, 2 of unreadable kind whose target is a helper's parameter, both with guaranteed stops), 0 tail-only among the pinned kinds, 0 unreadable listed, 0 stale allow entries, 61 bounded tail-only excused, ALLOW empty, 227 informational product-start rows (191 spawn-helper calls over 31 heads, 15 SdkSession.start() in 5 modules, 5 run_in_executor, 16 ThreadPoolExecutor, every one under a with), the runtime oracle called by 5 of the 936; tests/test_view_deltas.py 59 passed (the _py_maps rewrite); tests/test_postal_token.py 33 passed; the liveness module 10 passed serially (0.97 s) and under -n 2 --dist load (1.02 s). Full sweeps through the box's slot runner at -n 10: the first pass's head 324b7c667 17789 passed, 0 failed; the second pass's final head 366f6393a 17800 passed, 42 skipped, 1 xfailed, 0 failed, 0 errors; the control at main 65f1895f6 17768 passed, 1 failed, 1 error (this test); the fourth pass's final head 6afe49254 17806 passed, 42 skipped, 1 xfailed, 0 failed, 0 errors in 9:39 (TMPDIR /tmp/sweep-Ho6Qx0, 17 bytes); each code head of the fifth pass, which also carries the merge of main 53f9f4aa3 (PR 889), is swept after its commit and the totals are appended in a dated line below.

Tier: fix for the upstream offer, on the user's word (a test always red alone and a sweep-level flake since 2026-09-11, and a leaked kernel loop whose reach is every rebinding module in the worker), recorded here as `tier: fix`; the fork's PR carries the docs label (tests-only), as romp-manager asked: both are stated wherever the tier is. The change is inside tests/ only. Upstream (romp-on/romp main 31b87e45f) carries the same test and hold; the fix was NOT run there.

2026-09-22 04:20Z: the fifth pass's final head 5fc6c35a2 swept through the box's slot runner at -n 10 (unit parked-ops-pass5-20260922T041045Z, slot 1, waited 0 s; TMPDIR /tmp/sweep-WGcI3s, 17 bytes; log parked-ops-notes/pass5/logs/full-sweep-5fc6c35a2-20260922T041045Z.log): 17825 passed, 42 skipped, 1 xfailed, 6761 subtests passed, 0 failed, 0 errors, no killed worker, in 548 s (9:08). Against the fourth pass's 17806 at 6afe49254: +7 are this pass's census tests (36 to 43) and +12 (with +6 subtests) came with the merge of main 53f9f4aa3 (PR 889 and batch 892), which this head carries; the skips are the same 42.
