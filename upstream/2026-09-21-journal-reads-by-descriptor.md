---
title: The journal reads under hosts/ descend by descriptor, the write opens refuse by shape, and the two directory rows file once per observed state of their subject
status: candidate
where: kernel/host_transport.py (module level, HostDirRefused, _open_dir_nofollow, HostDirs, _descend, open_host_dirs_if_present, _open_file_nofollow, host_stderr_open, remove_host_dir, write_spawn_spec, HostTransport.__init__, HostTransport.from_journal, HostTransport._read_journal); kernel/sdk_backend.py (SdkSession._amain, SdkBackend.__init__, SdkBackend._host_lease_applies, SdkBackend._host_transport_for, SdkBackend._refuse_host_directory, SdkBackend, SdkBackend._refused_directory_row, SdkBackend._loose_rows_new_episode, SdkBackend._file_loose_directory_rows, SdkBackend._spawn_road_failed, SdkBackend._host_orphan_recover, SdkBackend._refused_launch_log, SdkBackend._file_host_log_rows); kernel/session_host.py (module level); tests/test_host_transport.py (module level, foreign_uid, foreign_uid._Stub._swap, SpawnSpec.test_an_unwritable_file_of_ours_at_spawn_json_or_host_stderr_is_the_opens_own_error_with_eacces_and_not_a_refusal, TransportOverJournal.test_the_replay_answers_the_initialize_and_yields_the_journal_then_ends, TransportOverJournal.test_the_replay_answers_the_initialize_and_yields_the_journal_then_ends.go, BackendHostRules.test_a_loose_directory_is_one_row_per_connect_episode_per_mode_observed_however_many_descents, BackendHostRules, BackendHostRules.test_a_directory_of_ours_with_no_search_bit_is_the_fault_it_is_out_of_the_lease_applies_road_and_the_other_roads_arms_answer_it, EndToEnd, EndToEnd.test_a_turn_survives_a_drain_and_finishes_under_the_next_backend); tests/test_hosts_path_census.py (module level, HostsPathCensus.test_no_by_path_syscall_on_a_hosts_path_remains_except_the_listed_sites); tests/test_session_host.py (orphan_journal, JournalRules, JournalRules.test_offsets_are_ordinals_and_reads_start_anywhere, JournalRules.test_segments_rotate_at_a_turn_boundary_and_acked_ones_are_dropped, JournalRules.test_a_failed_raw_write_leaves_nothing_behind_and_the_numbering_holds, JournalRules.test_a_write_that_will_not_truncate_leaves_the_segment_behind_for_a_fresh_one, PreludeRefusalRead.test_a_filesystem_failure_at_the_launchers_descent_or_its_host_stderr_open_is_the_launch_error_with_its_errno_and_no_stale_tail, PreludeRefusalRead.test_a_filesystem_failure_at_the_launchers_descent_or_its_host_stderr_open_is_the_launch_error_with_its_errno_and_no_stale_tail.launch, PreludeRefusalRead, HostProcess._journal_landed, HostProcess.test_the_journal_lands_every_record_behind_the_socket_when_the_writer_lags, HostProcess.test_a_token_in_the_hosts_environment_reaches_the_cli_and_is_nowhere_the_host_writes); upstream/2026-09-18-host-socket-mode.md (text); upstream/2026-09-21-journal-reads-by-descriptor.md (text)
added: 2026-09-21
pr: 884
tier: fix
offered:
closed:
---
The kernel's five remaining by-path reads under hosts/<sid>/ after fork PR #814 (the orphan journal's segment glob, its gaps.json read and each segment's open in the host module's read_journal_dir, and the two journal globs in kernel/sdk_backend.py, _host_lease_applies and _host_orphan_recover) take the read descent's held <sid> descriptor: the listing is a scandir off it with a name match (host_transport.journal_segments) and every file is put the owner question by name under it and opened by name through the one reader (_open_host_file), so a foreign file is one host.directory-refused row and skipped as absent, a link of ours is the file-shape row and the road refused, and a <sid>/ renamed away and relinked after the descent lists the directory the descent verified. That closes the weaker precondition #814's record states for these reads, a group-writable hosts/ or a loose hosts/<sid>/ of ours under a state root the peer can traverse, on the lease-applies and orphan roads and in the replay transport (HostTransport.from_journal takes the HostDirs, borrowed for the replay's life, and a transport built with no row to file gets a refusal raised out of its read loop, never a silent end of the stream); the census (tests/test_hosts_path_census.py) holds its follow-up role empty. What remains by path under hosts/ is the connect to the published socket (HostTransport.connect), permanent while the transport is a Unix socket, under the stronger precondition of a peer-writable root.

The two write opens of the spawn road (host.stderr in host_stderr_open, spawn.json in write_spawn_spec) go through one opener, host_transport._open_host_file_for_write: the readers' shape question by name under the held descriptor before any open (a peer's entry of any kind is the owner row and the launch refused; a directory, a FIFO or a socket of ours is a refusal naming the kind, nothing opened), then the open with O_NONBLOCK, the fstat of the descriptor it returned, the caller's O_TRUNC applied after the checks and O_NONBLOCK cleared before the descriptor is handed back. Through #814 both opens went straight to the O_NOFOLLOW open: a FIFO a peer planted at either name under a <sid>/ of ours while it was loose blocked the open inside the kernel's event loop until a reader appeared (the drives verifier of #814's final body pass: blocked 2 s, released by a reader end), and a directory was EISDIR out of it with no row.

The host.directory-refused row and the host.directory-loose row file once per observed state of their SUBJECT, not once per road or session that observes it (the reviewer's ruling of 2026-09-21 07:19Z, correcting the 03:54Z property this PR's first commit built the refused row's latch on and the 2026-09-20 20:40Z ruling #814 built the loose row's on: both keyed the latch on the sid and its connect episode, which is right where the subject is one session's and wrong where it is shared, since N sessions are N episodes; at one restart nine sessions connected over one hosts/ at 0775 and the loose row fired nine times for one directory). The subject and its owner are decided by path, in one place for both rows (sdk_backend._row_owner): a directory at or under hosts/<sid>/, or a file under it, is that session's and is forgotten at its next connect episode; hosts/ and the road above it (on the spawn road, the helpers' state root and the directories above it) are shared, stand for the kernel's life, and are filed again only on a changed observation, the loose latch holding the mode last observed and the refused latch the refusal text last observed, an admitted directory forgetting its refusal and a refused directory forgetting its loose mode, so a refusal that returns after a repair, or a directory seen loose again once a plant at its path is gone, is filed again (the reviewer's ruling of 2026-09-21 09:32Z: through this PR's fifth commit the refusal left the loose latch standing, and the return read as the same mode). THE RULE is stated once, at sdk_backend._file_loose_directory_rows; sdk_backend._refused_directory_row and _loose_rows_new_episode point at it. Through #814 one hosts-off connect over a foreign identity.json with a journal beside it filed the owner row twice, from the lease-applies read and again from the orphan road; through this PR's fourth commit N connecting sessions filed a refused or loose hosts/ N times.

Tier: fix, on the user's word (a session-to-session security boundary). Cut from fork PR #814's head, after #814 landed on main. Pinned by execution in tests/test_host_transport.py (JournalReads, WriteOpens, BackendHostRules) and tests/test_session_host.py; the census pin (tests/test_hosts_path_census.py) refuses a by-path read written back under hosts/.
