---
title: The file viewer under a big reviewed note: a mark is painted from the blocks its range touches, a reflow re-places the Comments panel cards instead of running its paint pass, the pane divider drag moves a ghost line and resizes the panes once at release, and the pane-wide table width property is non-inherited and written on the tables
status: candidate
where: fork PR #420, branch viewer-freeze-fixes, cut from the working branch perf-viewer-resize (ui/webview/anchor-map.ts (wrapBetween: the blocks between the two holders, not a walk of the whole document), ui/webview/file-view.ts (fireRendered carries a why of reflow or paint; stampBodyWidth writes --fv-body-w on each top-level table), ui/webview/file-comments.ts (the onRendered hook answers a reflow with scheduleLayout), ui/webview/styles.css and ui/webview/feed.css (@property --fv-body-w, inherits: false), kernel/kernel.py (_LANDING_JS: the vertical gutter drags #gv-ghost and writes the two grows at mouseup); tests: anchor-map.test.ts, file-view-reflow-browser.test.ts, file-view-body-width-browser.test.ts, shell-gutter-drag-browser.test.ts over ui/webview/shell-drag-leg.ts, file-view-text-size.test.ts, tests/test_kernel_pane_rail.py; the bench under tools/viewer-resize-* is fork infrastructure, fork PR #421 stacked on #420)
added: 2026-09-09
pr: 420
tier: fix
offered:
closed:
---
Four mitigations from the 2026-09-09 incident (a big reviewed note in the Files pane stalled the shared main thread for about 20 s during a pane divider drag), each kept on the measured rule in tools/viewer-resize-bench.ts and each with a test that fails at the commit before it. Two touch code upstream ships: the divider drag in kernel/kernel.py wrote both flex-grows on every mousemove and each write re-laid out every same-origin pane document, now a ghost line follows the pointer and the grows are written once at mouseup (tests/test_kernel_pane_rail.py, shell-gutter-drag-browser.test.ts); and --fv-body-w, the pane-wide table cap (2026-09-08-markdown-viewer-typescale), was written on the body as an inherited custom property, so a width change restyled every node under it, now registered non-inherited in both sheets and written on each top-level table by file-view.ts (file-view-body-width-browser.test.ts). Two are fixes to fork-only files that ride with their candidates: the anchor map (2026-09-08-markdown-viewer-place) painted each mark after a text-node walk of the whole document, now only of the blocks the range touches; and the Comments panel (2026-09-07-file-comments-tracked-changes, 2026-09-07-markdown-viewer-text-size) answered every onRendered call with its whole paint pass, now file-view.ts passes a why and a reflow (a width change, a text-size step) re-places the cards only, while a paint (new nodes) still runs the pass. Those three entries describe the pre-fix shape; an offer built from them takes these hunks with them. A fifth (content-visibility: auto on the rendered blocks) was measured, then reverted on the branch: the reader landing after a jump into a never-shown region drifted.
