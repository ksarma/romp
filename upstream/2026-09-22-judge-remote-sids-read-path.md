---
title: The judge's dead-session ladder reads the remote-sids mirror where the postal bus writes it (STATE/postal/remote-sids), and the mirror says which hosts are reachable, so the revived rule 5 presumes a sid closed only when a host whose link the kernel holds up (or, under the legacy singleton scheme, a live legacy heartbeat) fails to name it; rule 5 had read a path nothing wrote since its birth and never fired
status: candidate
where: kernel/judge.py (_presumed_closed_verdict, Deadness, _source_causes, _remote_sids_mirror, _presumed_closed), postal/postal_service.py (_write_remote_sids, _remote_sids_document, _remote_sids_via_key, _direct_row_speaks, _via_held, _drop_peer_name_dupes, _remote_sids_previous, _remote_sids_carry, _remote_sids_lost, _remote_sids_lost_cleared, _remote_sids_say, _seed_peers_from_kernel, _link_down, _link_up, _source_link_down, _source_link_up, _via_duplicate, _record_heartbeat, local_agents_checked, _local_listing_owned, _monitor_tick, peer_update, peer_exchange_handle, build_exchange_request, peer_exchange_apply, _local_presence_checked, _local_presence, presence_payload, fleet_presence), tests/test_dead_session_staleness.py (ReaderFollowsTheWriter, PresumedClosed, CARRY_LOST, UNANSWERED, CLEARED_RULE_5, ROAD_SIDS, _mirror, _row, _bus_wrote), tests/test_judge_propagate_loads.py (SettleUnderTheUnansweredArm, SettleWhenTheCachedHostIsHeldDown, LIVE_ON_CACHED, _real_bus, _dials_us, _mirror, _bus_hears_nobody_remote), tests/test_postal_bus_lifetime.py (MonitorTick, _forget_presence, _mirror_rows), tests/test_postal_heartbeat_fetches.py (test_after_the_latch_a_local_peer_is_unreachable_by_name_during_a_blink), tests/test_postal_isolation.py (ThreadOwnSendRefused), tests/test_postal_relay_honesty.py (PresenceBlinkHonesty), tests/test_postal_remote_sids_mirror.py, upstream/2026-09-22-judge-remote-sids-read-path.md (this entry)
added: 2026-09-22
pr: 897
tier: fix
offered:
closed:
---
Upstream carries the same two bindings (the judge's STATE is the romp root, the bus's is the root plus postal) and the same read in _presumed_closed, so rule 5 of its dead-session ladder is dead there too: every sid reaching it answers cannot-determine, and a dead sender's card settles only by rules 1 to 3. Re-checked at upstream/main for this round: its _write_remote_sids still writes the whitespace list, pruned by HEARTBEAT_TTL at every write and folded from every PEER_STATE row, from memory alone, and its judge still reads STATE/remote-sids. Found as a side finding of the fork's postal-bus tests PR (fork PR #894), where the reviewer's ruling asked for both STATE bindings to be read and the read to be run; the probe there confirmed it by execution and filed it for its own fix-tier PR.

Moving the reader to the writer's path alone would arm a rule that settles falsely, which the round-1 review of the fork's PR showed by two roads: a bus restarted from empty memory rewrote the mirror at its first poll naming nobody, so until its first exchange every sid live on another host was absent from the file and its local mirror store settled; and, under the legacy singleton scheme, the bus pruned an expired heartbeat's sid at every write, so a tunnel drop or a stalled peer longer than the 90 s TTL settled a live session on no new information. The mirror therefore changed shape: a JSON document with one row per presence source, the roster it last reported, whether the bus heard it in its current process, and whether its presence expired. The writer carries every host the previous file named and this process has not heard forward, unreachable, until its heartbeat or exchange arrives (one bad byte in the previous file costs one sid, never the document, and a previous file the bus cannot read whole marks the mirror: the twenty-second commit's paragraph below); an expired beat's row stays, marked. The reader presumes a sid closed only when a host vouches for absence and none names it; a sid only an unreachable host names, a mirror in which no host vouches for absence, or a mirror of another shape (the old whitespace list, said once in the judge's log) answers cannot-determine, as the dead rule did. The kernel's link state gates reachability too (round 2's ruling by the reviewer): a host is reachable only when heard and the kernel does not hold its link down, so the down notify makes it unreachable at once with its roster kept (a host that is down cannot vouch for absence, and a session started there since its last roster is in no roster) and its next heartbeat or exchange heard with the link up makes it reachable again; the writer computes the flags per row and the reader reads them, and the two cannot-determine reasons that turn on a source's state name the sources and why each cannot vouch. The rule is two-sided (the reviewer's ruling, the round's fifth commit): a heard host that is not held down vouches for the PRESENCE of the sids it names (`reachable`, rule 4), and a host vouches for the ABSENCE of a sid it does not name (`vouchesAbsence`, rule 5's precondition) only when its link is known up, a dialable PEERS row the kernel holds up and the host heard since the link last dropped (a legacy heartbeat within its TTL vouches as before, having no link, under the legacy singleton scheme alone since the sixteenth commit, below), so a heard host with no link state (a host the kernel never notified, or a far bus filed under the hostname it declares before this bus's own dial has folded it under the alias the kernel dials) answers cannot-determine for a sid it does not name, as a down host does. The events: the up notify plus the host heard since make a host vouch for absence, the down notify ends it at once, and for a far bus under its declared name the fold files its row where the alias's link reaches it. Event-keyed, no grace period. One test runs the real writer and the real reader over one root under both root shapes through the first write, a restart, an expiry and the legacy shape. Fix tier: product code with tests red before it.

Round 3 (the reviewer's ruling, the seventh commit): a hub's word about a host this bus also holds directly is never discarded. Until this round the writer folded a hub's gossip about a far host away whenever that host had a dialable PEERS row or a heard row, whatever that row's state (the display fold, _via_duplicate), so with the direct row carried after a restart, or held down by the kernel, a session the hub freshly named on that host was in no row, and the hub, vouching for absence, let rule 5 presume it closed: a false settle, and a regression from round 1's head, which named every gossiped sid. Now the gossip folds into the far host's own row only when that row is heard in this process and the kernel does not hold its link down, matched by the bus id the hub stamps on the gossip (the row may sit under the alias the kernel dials) or by the name the hub uses for the host when the row carries no different known bus id (the eighth commit, the reviewer's verifier's finding by execution: a row under that name whose bus id is not the gossip's is another bus, another machine the hub calls by the name or this one restarted under a new id, and a name match that folded whatever the ids left a session on the other machine in no row while two sources vouched for absence, rule 5 for as long as the direct row was heard and up); otherwise the hub's word stands as a via row under via:<hub>/<far>, a key no host name can collide with, beside the direct row, whose last roster still protects the sids it named; a via row whose hub is not heard is carried like any key, and it is dropped by the carry once the direct host speaks again (its exchange arriving with its link not held down, the event), since its own row then names what runs there, or once the hub's current word about that host folds at a write (a hub knows one machine by a name, the premise of rule 5's reach through a hub that the forty-seventh commit's paragraph below states, so its earlier word under the key is superseded; without this a far bus restarted under a new id and heard here before the hub heard it left a via row stamped with the old id for the file's life, and a session that then ended there was cannot-determine by that stale row where rule 5 was due). A via key carries two names, the hub's and the hub's name for the far host, and either changes with no restart on either side (the ninth commit, the reviewer's verifier's finding by execution through the real handler and the real fold): this bus's own dial folds a hub heard first through its dial to us from the hostname it declared under the alias the kernel dials, and a hub's own fold of the far host under the alias it dials changes the name it stamps; the hub's word is then written under the new key, and the row under the old key, carried by key alone, named a session that had since ended for the file's life, cannot-determine where rule 5 was due. So the carry drops a carried via row by identity as well: when the hub's bus is heard in this process under another name (the same test that drops the hub's own stale row, so the two leave together), and when a hub's current gossip names the far host's bus, by its stamped bus id, under any name. A heard hub gossiping nothing about a far host under the same name still leaves its via row carried (a restarted hub has not heard it yet). What no identity in the file reaches stays disclosed, residual (2): (2a) a hub restarted under a new bus id and heard under another name leaves its old row and that row's via rows carried, a session that ended on the far host since named by an unreachable host, until this bus's own dial reaches the hub under the old name and its fold files the hub's current word there, and for the file's life when the old name is never heard again (a departed host's rows); (2b) a hub that stamps no bus id renaming a far host leaves its old-name row carried the same way, until it names the host under the old name again, and for such a hub too a restarted hub's silence is no word. Each has its named witness in tests/test_postal_remote_sids_mirror.py since the thirty-sixth commit, asserting the carried rows beside a host that vouches: test_residual_2a_a_hub_restarted_under_a_new_bus_id_and_heard_under_another_name_leaves_its_old_rows_carried and test_residual_2b_a_hub_stamping_no_viabus_that_renames_a_far_host_leaves_the_old_names_row_carried_and_its_restarts_silence_is_no_word. The drop once the direct host speaks again is pinned on its own, with the hub not heard, since every earlier pin of it had the hub heard and the hub's gossip folding first. The two identity drops are pinned with hubs that carry a bus id, as every real bus does (the reviewer's verifier: every carried-hub fixture until then had none, so a carry reading a carried row's bus id alone as the hub heard under another name, and one dropping a carried via row on any hub's current word about the far bus, passed every pin): a carried hub's word about a host nobody holds stands while its bus is heard under no other name, whatever another host vouches, and a second hub's current word about the far bus drops no other hub's carried row. The gossip is never added to the direct row as a naming source: that would credit a source that did not vouch. The display and routing consumers keep their own fold. The one residual of the fold, residual (1), is disclosed as a bound, not closed by a timer: a direct row heard, not held down and answered whose roster is older than the hub's gossip folds it, so a session started on that host since its last exchange is in no row for a window of one exchange interval of that host, closed by its next exchange (its named witness, whose THE RESIDUAL assertion holds that window: tests/test_postal_remote_sids_mirror.py test_a_hubs_word_about_a_directly_held_host_folds_only_while_that_host_is_heard_and_not_held_down); the population is every heard row the kernel does not hold down whose last exchange carried an answered listing, a heard row with no link state included (a far bus under the hostname it declares before this bus's own dial has folded it, closed by that bus's next dial), and a heard row whose last exchange served a cached roster is not in it: that row speaks for nothing, and the hub's word stands beside it (the fifteenth commit, below). The reader needed no new arm: the via key is a key, and the cannot-determine reasons name it like any source.

Round 3, the eleventh commit (the reviewer's ruling; the road found by its refuters through the real handler and writer): a heard host whose exchange served a cached roster vouches for presence alone. While a host's kernel listing does not answer (mid-restart), its presence producer serves the last answered rows into the exchange (the blink honesty of 2026-08-31, which upstream carries too), and until this commit that cache rode the exchange with no sign of it, so a session started on the host after that listing was in no roster while its mail rode the same exchange, and the host, heard with its link known up, vouched for absence: rule 5 presumed a live session closed. Now the answered bit the sender already computes rides the exchange payload as `presenceAnswered` beside `presence`, in the request builder and the response builder alike; both recorders keep it on the host's row; every mirror row carries a seventh flag, `answered` (the reader requires the seven), and a source vouches for absence only when its roster is an answered listing besides its link being known up: a row whose last exchange served a cache is reachable (the sessions it names were live at the last answered listing, rule 4) and does not vouch for absence, with a fifth closed-set cause in the reader's reason, "listing unanswered", after "no link state"; the next exchange that carries an answered listing releases it, the event, no grace period. A hub stamps the far host's bit on every gossiped row (`viaAnswered`), so a via row carries the far host's word about its own roster, not the hub's; a payload lacking the field, an older peer's, or carrying a non-boolean, reads unanswered, the restricted side, so an older peer cannot reopen the road; a legacy heartbeat is its own answer (True); the legacy list and a row carried from a file that predates the field read False. Red before the change on a scratch copy of the tenth commit with the changed test modules overlaid: the real request builder under an unanswered listing, the real handler, the writer and the reader in one interpreter over one root answered rule 5 for a session started during the blink where cannot-determine with the cause is due.

Round 3, the fifteenth commit (the reviewer's verifier at the eleventh, by execution through the real builder, handler, writer and reader): the fold's gate reads the bit too. The eleventh commit's gate on a hub's word about a directly held host read heard and not held down alone, so a hub's answered word about a session started on a host during that host's blink folded into the host's cached row, the session was in no row, and the hub, vouching for absence, let rule 5 presume it closed until the host's next dial: one exchange interval of the host, the cached row having joined the fold residual's population above. Now a heard row whose last exchange served a cache is the third state, beside carried and held down, in which a direct row speaks for nothing about a session started on its host since: the hub's word stands as a via row beside the cached row, carrying the far host's bit as the hub stamped it, so the session is rule 4's while the hub vouches; with the hub not heard, the carried via row stands beside the cached row instead of being dropped, so the session is cannot-determine by the hub's last word while another host vouches for absence, never rule 5. The host's exchange that carries an answered listing is the event, the same one that restores its absence vouch: its row speaks, the via row is dropped by the carry, and the hub's next gossip folds. The cached row still vouches for the presence of the sids it names. The gate stops the cached host's own fold and vouch. The presumption from another host's word, a session started on the cached host during its blink that no hub names presumed closed while another host vouched for absence, is closed by the reader's listing-unanswered arm since the twenty-ninth commit, whatever the cached host's link since the thirtieth (both below), and what that arm leaves open is residual (3), two far-host sub-cases, one restart sub-case and, since the thirty-third commit, one of order, each with its witness in tests/test_dead_session_staleness.py ReaderFollowsTheWriter: (3a) a far host whose cached roster is empty, behind a heard hub, whose session still answers rule 5 (test_residual_3a_a_far_host_whose_cached_roster_is_empty_behind_a_heard_hub_still_answers_rule_5; its second face since the thirty-second commit, the far host's bus restarted with no twin after a cached exchange that carried the session's mail, whose empty cache its hub's silence cannot be told from an empty answer: test_residual_3a_a_far_hosts_empty_cache_after_its_bus_restarts_releases_its_held_word_and_its_session_answers_rule_5); (3b) a far host gone after a cached exchange with its hub, which holds every sid at cannot-determine while the hub gossips it (test_residual_3b_a_far_host_gone_after_a_cached_exchange_with_its_hub_holds_every_sid_while_the_hub_gossips_it); and (3c) a session whose mail landed on a host's cached exchange before this bus restarted, which answers rule 5 until that host's first exchange in the new process, since a carried row holds nothing (test_residual_3c_a_session_whose_mail_landed_on_a_cached_exchange_before_this_bus_restarted_answers_rule_5); and (3d) a roster that reaches this bus after a newer one from the same source, which stands, so a session whose mail rode the newer, cached roster answers rule 5 until the source's next exchange (test_residual_3d_an_older_roster_recorded_after_a_newer_one_stands_and_its_session_answers_rule_5); (3a)'s second face and (3d)'s same-road face are caused by the thirty-second commit's release by the same hub process's omission, which the reviewer ruled to keep, and with a third race, a second machine a hub files under a far host's name answering it with an empty listing, a window at each of its exchanges while the host's word at the hub is a cache (test_the_releases_third_race_a_second_machine_answering_the_hub_under_a_far_hosts_name_with_an_empty_listing_answers_rule_5), are its cost (the forty-fifth and forty-seventh commits' paragraphs below); a second machine naming a session of its own there is outside the model, since a hub knowing one machine by a name is the premise of rule 5's reach through a hub, its violation witnessed by test_the_premise_violated_a_second_machine_naming_its_own_session_under_a_far_hosts_name_presumes_the_far_hosts_sessions_closed (the reviewer's ruling, the forty-seventh commit's paragraph below; the forty-sixth disclosed both as residual (3e)). The contrast is where the session's mail can have landed, not round 2's down host and not the host's link: a cached host's session reaches the judge through its own mail, on an exchange that omits it, and every such exchange in this bus process leaves the host's row heard with its bit False whatever the kernel says of its link before or after, while a host held down after an ANSWERED exchange can carry such a session's mail only on its next exchange, which replaces the bit. Pinned at the writer (the far host's cached dial through the real builder and handler, the hub's answered word beside it, the carry's half with the hub not heard, and the release) and in the composition with the reader's verdicts (the hub heard through the real fold, then an eleventh restart with a second hub vouching, where the eleventh commit's gate answered rule 5 for the session the hub named).

Round 3, the sixteenth commit (the reviewer's ruling on its refuters' finding, by execution): a heartbeat row vouches for absence under the legacy singleton scheme alone. In peer mode, the default, a local session stops beating once its bus calls it local and remote presence arrives through the exchange, so the beats that reach the bus's heartbeat table there are local sessions' beats filed as remote presence while the kernel's listing did not answer (a blink); such a row names a session of this machine and says nothing about any other host. Every heartbeat row had vouched for absence by its TTL whatever the scheme, so a restarted bus in peer mode that had heard no host presumed every sid nothing named closed within 90 s of such a beat, and a blink's phantom vouched while a peer's link was down. Now the writer reads the scheme at each write (peers_on, the bus's own switch, ROMP_POSTAL_PEERS) and a heartbeat row vouches for absence only when it is not on: in peer mode the row is reachable, its own answer, and vouches for presence alone, so its sid is rule 4's and a sid it does not name is cannot-determine by it, the reader's reason naming the beat with no link state, the literal fact; under the legacy scheme, where the beats are a remote session's only presence, the row vouches by its TTL as ruled in round 1. Nothing is stored on the row and the reader is unchanged. A retirement that popped the heartbeat entry once the listing called the sid local was refused: the writer's carry re-files a key missing from memory from the previous file as a row heard by nobody, a phantom for the file's life; the recorder keeps the key (superseded at the seventeenth commit, below: the release is the writer's, and the entry leaves with the row once the file without it is in place). The existing heartbeat pins that asserted the TTL vouch under the default peer mode either moved under ROMP_POSTAL_PEERS=0 (the composition's five heartbeat phases, the switch set in the child before the first write and read back) or were re-pinned to peer mode's presence alone (four pins of the mirror module), each saying which. Pinned at the writer (a beat through the real recorder under a listing that does not answer, beside a peer the kernel holds down, the same rows under the legacy scheme, and the key kept once the listing answers) and in the composition with the reader's verdicts (a twelfth restart in peer mode, the beat through the real recorder: cannot-determine where rule 5 answered, rule 5 once a host with its link up is heard, cannot-determine again with that host held down, rule 5 under the legacy scheme, and the row kept once the listing calls the session local), and at the reader's ladder with the fixture rows of both schemes.

Round 3, the seventeenth commit (the reviewer's rulings on the retention findings and on the comment-thread finding as its refuters narrowed it). Rows that nothing retires: no timer, by the design rule, and the one event-keyed release that exists lands. A heartbeat row whose sid the local kernel's ANSWERED listing owns is dropped by the writer, heard or carried, and the HEARTBEATS entry is forgotten with it once the file without the row is in place, so the carry has nothing to re-file (the sixteenth commit had refused a pop in the recorder for that reason and kept the row): rules 1 and 2 of the judge's ladder own a sid the local kernel lists, its transcript being local, and in peer mode every beat that reaches the table is such a session's, filed during a listing blink. The listing is the one the bus last read through local_agents_checked, which now records what it read (the recorder at every beat, the presence producer at every exchange, the autostop gate at every poll, whose tick reads before it writes so the release lands within the tick); a listing that did not answer releases nothing, and the last answered rows the presence producer serves through a blink are a cache the release never reads, so a beat filed during the blink stays until a listing answers; a read without thread rows owns no comment thread's sid, so a thread's blink beat waits for the recorder's read, which asks for them; nothing asks the kernel for the release, which rides the reads the bus already makes. Every other row still has no release, and the cost is stated plainly, here and in the writer's docstring: a heartbeat row of a session that stopped beating (under the legacy scheme a remote session that ended, or whose tunnel never came back) stays, expired, for the life of the state root, and so do the peer row and the via rows of a host the kernel no longer dials; the mirror grows by one row per such beat or host (the reviewer's refuters measured 204 rows and 56 KB after 200 expired beats; re-measured at this commit through the real recorder under the legacy scheme, 200 rows and 58361 bytes after 200 expired beats, 200 rows and 58561 bytes after a restart's carry); every write (each beat, exchange, notify and poll) reads, parses and rewrites the whole file; and rule 5 stays dead for those sids, each cannot-determine by an unreachable row's last word. This is residual (4), and its named witnesses are tests/test_postal_bus_lifetime.py MonitorTick test_an_expired_heartbeat_is_marked_unreachable_by_the_polls_write_and_stays for the expired beat and tests/test_postal_remote_sids_mirror.py test_a_restart_carries_every_host_it_has_not_heard_as_unreachable_and_releases_each_on_its_event for a host's carried rows. The release event for a departed host's rows would be the kernel's word that the host is gone, which no notify carries yet: the follow-up, for the kernel's owner and outside this fix-tier change, is a forget notify on the bus's /peer route naming the host, on which the writer drops the host's own row and its via rows (the same shape as the down notify, which already writes the mirror). The earlier framing of an expired beat as event-closed by "the next beat" is corrected in the writer's docstring, the reader's and here: a beat from the session makes its row reachable again, and a session that has ended beats no more. Comment threads are outside every roster: the presence producer reads the default listing (the 2026-08-22 rule that hides threads from every remote consumer), so a live thread's sid is in no row on any peer, and a host vouching for absence would let rule 5 presume it closed there. No roster change (listing threads to every remote consumer through _local_presence is refused: it would make them addressable and listed everywhere with no filter); the disclosure states why the road is closed today, and a witness runs it: a thread's mail is off by default since T356 (_mail_off_why answers "thread" for a session whose durable record carries threadOf, unless session-flags.json carries `threadMail` at the literal True; this paragraph said no route of the kernel or the bus writes that key, which the eighteenth commit's paragraph below corrects), and the /send handler (Handler.do_POST) refuses a thread's own send with 403 before resolve_recipient and any relay, so its sid never reaches a far host as a sender. tests/test_postal_isolation.py ThreadOwnSendRefused, the gate's home, runs the road through the real handler: the listing with thread rows has the thread and the roster omits it, a send to a session on a peer host is refused and nothing is parked in the peer's outbox, and with the flag hand-set the same send is parked (the disclosed road; the pin reds when the gate is opened, by its flag or in the code). If thread mail is ever re-enabled, the shape is a separate exchange field carrying the mirror-relevant thread sids, read by the mirror's writer alone. Pinned at the writer (a beat filed while the listing did not answer kept, by the recorder's write and by a bare one; the release at the next write after a consumer's read of an answered listing that owns the sid, the entry forgotten, the row not returning at a blink's write; a carried heartbeat row released the same way; a row the listing does not own kept, an answered empty listing releasing nothing; a thread's beat kept by a read without thread rows and released by the recorder's), at the poll (within one tick, an unanswered or a non-owning listing releasing nothing) and in the composition with the reader's verdicts on both root shapes (the listing answering owning nothing keeps the row; the listing owning the beating session drops it and forgets the entry, the sid in no row and no reason, and a blink's write after it does not bring the row back). Red before the change on a scratch copy of the sixteenth commit with the changed test modules overlaid: the writer kept the row and the entry once the listing owned the sid, at the writer, at the poll and in the composition on both shapes.

Round 3, the eighteenth commit (the reviewer's verifier at the seventeenth commit, by execution through the real routes, writer and reader). Three corrections. (1) The thread-mail key has a kernel writer. ONE road writes `threadMail` today: the kernel's WebSocket setSessionFlag arm (Handler._dispatch_ws), which accepts ANY flag name from an authenticated dashboard client, while POST /flag (_state_write_route), the kernel's one other route that writes a session flag, applies the _LANE_FLAGS whitelist and refuses it (no control of the UI sends the key: the lane gear's toggles and the tab menu's typed union are the three names _LANE_FLAGS lists; a hand edit of the file writes the same bytes). The consequence, in one sentence: a client that sets threadMail on a comment thread lets that thread's mail relay while every roster still omits its sid, so a far host's mirror can presume the live thread closed, a false rule 5 that a user reaches only by hand-crafting a WebSocket message (or by editing the file). The fix is a separate fix-tier PR, fork PR #909, that makes the WebSocket arm apply the same whitelist as POST /flag (a kernel change, so it owes the webview tests); a hand edit of the file stays open after it. The writer's docstring says so, and tests/test_postal_isolation.py ThreadOwnSendRefused, the gate's home, drives both kernel routes over the file the bus reads: POST /flag refuses the key and the thread's send to a peer host is still refused before the relay; the key written through the real WebSocket arm lets the same send be parked. The fix flips exactly that WebSocket half: a mutant applying the whitelist in the arm reds the witness at its WebSocket pin, so the witness moves with the fix. (This item reads as the nineteenth commit left it, with the thirty-sixth commit's two additions: fork PR #909's number, and the clause that a hand edit of the file stays open after it. The eighteenth recorded the arm's whitelist as a follow-up for the kernel's owner, and the reviewer's ruling made it the separate PR.) (2) The release reaches heartbeat rows alone. A carried legacy list, peer row or via row naming an owned sid beside another is carried whole, since its other sids are that source's last word. A carry with its kind test deleted passed every pin, and it drops a whitespace list naming a local session beside a session on a host this process has not heard yet, which by execution through the real writer and reader is a false rule 5 while another host vouches. Pinned now at the writer (the legacy list, a peer row and a via row, each carried whole while the owned sid's heartbeat row is released at the same write) and in the composition on both root shapes (the unheard host's session named-by-unreachable-host by the list, while a heard host with its link up vouches and a sid nothing names is rule 5's). (3) The order of the release is pinned by a failing write (the temporary path a directory): the entry is kept, the next write under a listing that does not answer re-emits the row heard, and the next write under an answered listing that owns the sid releases it. The writer's docstring states what the order buys: a pop before the replace would carry the row from the previous file heard=false after a failed write, its sid cannot-determine rather than rule 4's, never presumed closed, until an answered write released it. The test fixtures that read the listing record through getattr, for the seventeenth commit's red-before over the product before the record, read it directly, so a renamed record fails loudly.

Round 3, the nineteenth commit (the reviewer's ruling on section D of 2026-09-23, and its verifier at the eighteenth commit). Docstrings and tests alone; no product behaviour changes. (1) Section D's three texts are made true as ruled: the writer's docstring, item (1) of the paragraph above and the witness's docstring name the one road that writes the thread-mail key (the WebSocket arm, while POST /flag refuses it by the whitelist), state the consequence in one sentence, and name the fix as a separate fix-tier PR. The witness pins the current truth as a set: a census of kernel/kernel.py (the functions that write session-flags.json through a write door, and every mention of those functions, each located by its enclosing function and the selector of the if-branch that holds it) derives the routes that write a session flag, and they are exactly POST /flag and the WebSocket setSessionFlag arm, each driven through the real handler. A mutant adding a third WebSocket arm that writes any flag passed every pin at the eighteenth commit and reds the set pin now; the census is run against planted sources of each shape it must catch (a third arm; a writer through a module-level path, a path function or a local name, reached through a helper; a write at module level). (2) The release's reach in memory. A writer dropping a heard via row that names a sid the answered listing owns beside another passed every pin, and by execution through the real writer and reader that is a false rule 5 for the other sid while the hub vouches. Pinned at the writer (a heard peer row and a heard via row naming the owned sid beside another are written whole under an answered listing owning it) and in the composition on both root shapes (B, heard with its link up, names the local session beside a session live on the spoke through the real handler: the spoke's session is rule 4's and a sid nothing names is rule 5's). (3) The release's order is pinned at both steps of the write: a write that fails at the temporary file and one that fails at the replace each keep the entry, so a pop between the temporary write and the replace, which passed the eighteenth commit's pin, now fails it.

Round 3, the twentieth commit (the reviewer's rulings on the mirror's bytes and version, with its refuters' corrections, and on one pin of the reader). The bus's previous-read decodes the file as UTF-8, strictly, inside a catch of OSError and ValueError, and returns an empty table for bytes that are not UTF-8: until this commit the UnicodeDecodeError passed the OSError catch, so every later write failed at that read and the file was never replaced. A replacing decode was refused: it would hand the whitespace parser the garbage's safe-id-shaped runs to carry as legacy sids (the twenty-second commit, below, amends this for the JSON parse alone on the reviewer's ruling; the whitespace list keeps the strict decode). A row carried from the file has its values coerced, never dropped: each of the seven flags is bool() of its value (a non-bool flag was carried verbatim, and the reader, which requires seven booleans, answered cannot-determine for every sid until that source was heard again; dropping the row would leave the sids it named in no row, and a vouching host would let rule 5 presume them closed), each sid is str() of its value (since the twenty-second commit also dropped when it fails the session-id shape), and a busId that is not a str is ignored (an unhashable busId or sid failed every write at the carry's set membership tests, and an int busId matched a heard id through str() in the identity drop). The rows of a document of any version are carried, since a carried row vouches for nothing. The judge's reader reads the file as bytes and decodes inside its parse try, so a UnicodeDecodeError lands in the said-once unparsable arm naming the file (until this commit it raised out of the ladder for every sid), and it gates on the document's version: `v` must be 2, the version the writer stamps, or the file is unparsable whatever its rows carry (until this commit a document of any version, or of none, whose rows spelled the seven booleans answered as a v 2 one does). Both modules' docstrings state the gate. The same class had one more member, found by this commit's builder: a document nested past the JSON parser's depth raises RecursionError, which is not a ValueError, so the reader raised it out of the ladder and the writer's previous-read failed every write; both parses now catch it. The ladder gains a step in which a sid named by a reachable row and by a carried row answers rule 4, pinning the reader's any-row predicate. Pinned at the writer, in the ladder (two bytes that are not UTF-8, one stray byte inside the sid a vouching host names, the nesting, v 1, no v, v 3 and the string 2) and through the real writer and reader in one interpreter over one root under both root shapes (a fourteenth restart over bytes that are not UTF-8, the stray byte, the nesting, a hand-written document whose rows carry a non-bool flag, an unhashable busId and an unhashable sid, and documents at v 1 and with no v). Red before the change on a scratch copy of the nineteenth commit with the two changed test modules overlaid.

Round 3, the twenty-first commit (the reviewer's verifier at the twentieth commit, by execution through the real writer and reader: one stray byte inside the sid host B names in the bus's own document, a restart and B heard first, and the strict previous-read carried nothing, so rule 5 presumed the live session host A named closed until A was heard). That commit disclosed the road and named such a file once in the bus log; the reviewer refused the disclosure-only shape, since the judge still answered "no reachable host names it" for a sid whose row was lost, a false rule 5 under a loud log line. The twenty-second commit replaces it.

Round 3, the twenty-second commit (the reviewer's ruling of 14:57Z on that finding). One bad byte costs one sid, never the document: the bus's previous-read decodes the bytes as UTF-8 with a byte-order mark accepted, and when that decode is not clean it decodes again with replacement for the JSON parse alone; a document at v 2 read that way is carried row by row, every sid validated against the session-id shape (a replacement character fails it) and dropped when it fails, a row that is not a roster row dropped whole, every other row carried with its flags coerced, and one line in the bus log names the file and the counts. The twentieth commit's "never errors=replace" is amended for that parse alone: its reason, garbage bytes becoming a legacy sid, belongs to the whitespace list, which keeps the strict decode, so bytes that are not UTF-8 never reach its parser. The judge's reader stays strict and accepts a byte-order mark. A file the bus cannot read whole (bytes that are not UTF-8 in a file that is not a document at v 2 after the replacing decode; text that is neither JSON nor the whitespace list, a text that opens with a brace being a document and never the list; a text naming no session id; an empty file, which no bus since 2026-09-22 writes and a crash can leave; a JSON value of neither shape; an OSError other than no file) carries no row, and the write stamps the document with a lost-carry mark, `carryLost`, its cause and the write's second, said once in the bus log. While the mark stands the judge answers carry-lost, cannot-determine (closed False, no rule, the reason naming the lost carry, its cause and its second), where rule 5 would fire; rule 4 and the carried rows still answer. Every write carries the mark, across a restart too, until it clears: in a bus process that read the kernel's list of links at its start (the seed from /tunnels), holding at least one dialable link, once every dialable PEERS host has been heard in that process since the mark. The seed is required because a bus whose seed failed fills PEERS one host per notify, and a host heard before a later host's notify landed would clear the mark over that later host's lost rows. The bound: a linked host that stays down keeps the mark for as long as it is down; under the legacy singleton scheme, which runs no seed, no bus clears the mark, so it stands and rule 5 answers cannot-determine while buses on that scheme write the file, as before 2026-09-22, and a bus in peer mode over the same root clears it by the rule (corrected in round 4, the thirty-fifth commit: this sentence said the mark stood for the life of the state root, which the reviewer's verifier showed false by execution); what the mirror knows once the mark clears is stated under the twenty-third and twenty-fourth commits below (this commit named one kind of lost row the clear does not rebuild, the rows of a host the lost file named that the kernel no longer links, and rested the rest on a false premise, that a hub re-gossips its via rows on its next exchange). Also found by this commit's builder in the same class: a standing mark survives a mark read back in another shape (kept, re-stamped at the write's second; a second outside 1970 through the year 9999 among them, which the judge's reader refuses, since the verdict prints it) and a stray byte inside a top-level key when the parse read a replacing decode (that key may have been the mark's). Pinned at the writer (each case above, the mark's carry, its clearing, a down host, a failed seed, an empty link table), in the ladder (the byte-order mark, the mark's verdicts, a mark of another shape) and through the real writer and reader in one interpreter over one root under both root shapes (one bad byte, the byte-order mark, and the lost carry across three restarts: a seeded restart with one link heard, a restart whose seed fails with every host it holds heard, and a seeded restart whose last linked host heard clears the mark). Red before the change on a scratch copy of the twenty-first commit with the two changed test modules overlaid, adapted only for the attribute and the call signature that commit lacks, so each pin reds at its assertion.

Round 3, the twenty-third commit (the reviewer's verifier at the twenty-second commit, three findings by execution). (1) The clearing's premise that a hub re-gossips its via rows on its next exchange is false for a hub that has restarted: a bus gossips every far host it has heard in its current process and forgets none until it restarts, so a lost via row is rebuilt only when its hub has not restarted since it last heard the far host. The lost rows no heard linked host rebuilds are now stated at the clearing rule: a hub's word about a far host when the hub has restarted since it last heard that host and has not heard it again; the rows of a host the kernel no longer links (a departed host, directly held or a hub, its via rows included); and the whitespace list of a bus from before 2026-09-22, for its sids on such hosts. A session one of those rows named is named again by any linked host that hears its host now; a session on a host that no linked host hears now is outside every source after the clear, and the judge presumes it closed by rule 5 while a host vouches for absence, where the intact file's carried row answers named-by-unreachable-host. No event the bus receives names a far host whose row was lost, and a rule that matched the intact carry would never clear, which the ruling's clearing event excludes. This commit disclosed that bound for the reviewer's ruling, and the reviewer ruled it at 15:45Z as the design's first-start answer (the twenty-fourth commit, below). The bus log's clearing line said what the clearing does not reach; the twenty-fourth commit restates it. The witness runs through the real writer and reader in one interpreter under both root shapes: a linked hub names a session on a far host that is never linked here; the hub restarts and names nothing there; with the file intact the carried via row answers named-by-unreachable-host, and with the file made not JSON the mark clears and the session answers rule 5. A writer-level twin pins the same contrast in the file. (2) A host counts toward the clearing only when it has been heard: its PEER_STATE row must carry a seenAt at or after the mark's second. The clearing had read a host with no seenAt as second 0, so a mark at second 0, which the carry and the judge's reader both accept, cleared with no host heard. No bus stamps 0, so this needed a hand-written file or a clock in the epoch's first second. The fix is pinned at the writer and through the real writer and reader, a drift note filed for the unheard host in both. (3) An origin-only PEERS row is no link, and now a pin reds when one is counted: every seed in the composition, and one writer test, read a remembered unattached host beside the links, so counting it (the verifier's mutant X9) keeps the mark standing where these tests clear it. The verifier argued its mutants X6 and X2 equivalent under the current code, and neither is counted as open. Red before the change, on a scratch copy of the twenty-second commit with the two changed test modules overlaid: 7 failed on 3.12 and on 3.10, and every first failure is an AssertionError.

Round 3, the twenty-fourth commit (the reviewer's ruling of 15:45Z on the clearing rule, clause 5, and its verifier at the twenty-third commit). The bound is settled as the design's first-start answer: the mark clears once every host the kernel holds a link to has been heard since it was set, after the clear the mirror knows what a fresh bus knows, and a session on a host that no linked host hears now is outside every source after the clear, as on a first start. The twenty-third commit's wording overstated the consequence: it said a live session a lost row named is in no row once the mark clears, which is false when another linked hub hears the far host now (that hub's via row names the session, rule 4), and its clearing line said the same of a host the kernel no longer links. The writer's docstrings, the judge's _presumed_closed_verdict docstring, the bus log's clearing line, the test docstrings and this entry now state the ruled sentence, and the line's pins read it. The named witness now runs the ruled road through the real writer and reader in one interpreter under both root shapes: a hub, the only link, names a session on a far host that is never linked here; the file is made not JSON; the bus restarts seeded with the hub alone and marks the mirror; the hub, restarted and no longer hearing the far host, is heard; the mark clears, the hub's row is the one row and vouches for absence, and the session answers rule 5. A first start over the same link, with no previous file, writes the same rows and gives the same verdicts. The twenty-third commit's witness seeded two links, so its rule 5 held on the other link's vouch alone. No product behaviour changes beyond the clearing line's text. Red before the change, on a scratch copy of the twenty-third commit with the two changed test modules overlaid: 6 failed on 3.12 and on 3.10, each at the clearing line's new pin with an AssertionError. The one-link phase's verdicts pass there, since the behaviour is unchanged. They red under three mutants that leave the twenty-third commit's two-link witness green: a clearing that needs two links, a hub row that does not vouch while the other link does, and a reader whose rule 5 needs two vouching rows.

Round 4, the twenty-ninth commit (the reviewer's ruling on its round-3 refuters' finding, section A; the road found by both refuters on four roads through the real builders, handler, writer and reader). A session started on a host during its kernel's blink is in no roster, and its mail rides that host's cached exchange. The fifteenth commit stopped the cached row's own fold and vouch, but rule 5 fired whenever ANY row vouched, so such a session was presumed closed while another reachable row, answered with its link up, vouched: another peer, or a via row the cached host itself gossiped. A settle is sticky (the first settle records a settle event), so the sender's card stayed Completed while the session was live. Now the reader has a sixth cannot-determine arm, after the no-vouching arm and before the carry-lost arm: while any REACHABLE row is unanswered (heard in this bus process, not expired, not held down, its last exchange a cache: the writer's `reachable` True with `answered` False; since the thirtieth commit, below, every row heard in this process whose last exchange served a cache, held down or not), the answer is cannot-determine, `listing-unanswered: <each such row with why it cannot vouch>`, its own token in the Deadness comment's list, which states the order too: where the arm and the lost-carry mark both hold, listing-unanswered answers, naming the source whose next answering exchange releases it. Rule 5 fires only when a row vouches for absence, no row names the sid, no heard row is unanswered (no reachable row, at the twenty-ninth commit) and no lost-carry mark stands. The release is that source's next answering exchange, which both recorders already record (for a via row, the far host's answering exchange with the hub and then the hub's next exchange here): no new event, no new writer state, no timer. The arm only adds a cannot-determine answer, so it creates no rule 5, and a heartbeat row carries answered True and never trips it. A carried unanswered row beside a vouching host keeps rule 5 (residual (3c) since the thirtieth commit); at the twenty-ninth a held-down one kept it too, which the thirtieth reverses. The witnesses, under both root shapes, in a second child of tests/test_dead_session_staleness.py ReaderFollowsTheWriter (each road its own buses over state roots of their own, exchanging through the real builders, handlers and folds, our bus writing and the judge reading): the four roads, each rule 5 before the arm, test_a_session_started_on_a_hub_during_its_blink_is_cannot_determine_while_its_word_about_another_host_vouches, test_a_cached_hosts_session_is_cannot_determine_while_another_host_vouches_until_its_listing_answers, test_a_far_hosts_cached_word_through_a_heard_hub_holds_its_session_until_the_hub_relays_its_answer and test_a_sessions_own_mail_rides_its_hosts_cached_exchange_while_its_sid_is_cannot_determine; the release, test_the_release_is_the_sources_next_answering_exchange; the controls, test_the_down_host_and_the_lone_cached_host_answer_as_before_the_arm. In the reader's ladder (PresumedClosed), the rung that held residual (3) as a design property (a cached row beside a vouching host, rule 5) is re-pinned to the arm and names the residual, a rung pins the order against the mark, and a carried unanswered row beside a vouching host keeps rule 5 (and, at the twenty-ninth commit, a held-down one); the composition's two asserts in test_a_hubs_answered_word_about_a_directly_held_host_stands_beside_that_hosts_cached_row_until_it_answers move from rule 5 to the arm the same way. Residual (3) is what the arm leaves open, two far-host sub-cases that no exchange field in this fix-tier PR closes and, since the thirtieth commit, one restart sub-case, and since the thirty-third one of order (the fifteenth commit's paragraph above names the witnesses). (3a) A far host whose cached roster is empty, behind a heard hub: its kernel has not answered and its bus has no twin, or its last answered listing named nobody, so its exchange with the hub carries presenceAnswered False and no session row; presence_payload stamps a far host's bit only on that host's session rows, so no via row carries it, and the hub's answered row vouches; a session started there reaches the judge through its own mail relayed by the hub and answers rule 5, a false rule 5 left open until the far host's answering exchange with the hub and the hub's next exchange here. Its witness asserts that rule-5 answer, so it turns red when the residual closes. (3b) A far host whose last exchange with the hub served a cache and that then stops exchanging with the hub, whether or not the hub holds it down: a hub gossips every far host's PEER_STATE row whatever its link state and forgets none (the only pop is _drop_peer_name_dupes), so the via row here stays reachable and unanswered, and the arm holds every sid on this machine at cannot-determine until that host answers the hub again, the hub restarts (no release since the thirty-first commit, below), or this bus holds that host directly and its own answering exchange lands here; the restricted side, no false settle. The costs, all on the restricted side, each with its named witness (heard means heard in this bus process: a peer that stops exchanging stays heard, held down or not, until this bus restarts, whose carry makes its row not heard, and a hub's word about a far host whose last word was unanswered stays heard the same way across the hub's rosters that omit that host, the hub's restart included; at this commit the arm read reachable rows, so the kernel's down notify released it, which the thirtieth commit reverses, and the hub's restart released a hub's word, which the thirty-first reverses): (a) a peer that lacks the field (an older bus, the project's own included), heard here or gossiped by a heard hub, holds rule 5 at cannot-determine for every sid (test_cost_a_a_peer_lacking_the_field_holds_every_sid_beside_a_vouching_host); (b) so does a heard peer whose kernel never answers (the second road's witness, its host heard again over its cache); (c) so does a heard row with no link state (an origin-only row, a host the kernel never notified, a far bus under its declared name before the fold) whose last exchange served a cache and that then stops exchanging, since a peer's presence has no TTL and such a row has no down event: the arm holds every sid until that host's next answering exchange or this bus's restart, whose carry makes the row not heard, the only release for a host that never returns (test_cost_c_a_host_with_no_link_state_that_dials_once_over_a_cache_and_falls_silent_holds_every_sid_until_a_restart); (d) residual (3b); (e) the settle: the release reaches the reader's answer, not a settle already decided, because the judge reads _presumed_closed only at a courier write, a reply or a user resolve and a dead sender has no pass of its own, so a dead sender whose recipient completes while any heard row anywhere is unanswered (one peer's kernel restart is enough) stays unsettled after the release until the next completion, reply or resolve for it (tests/test_judge_propagate_loads.py SettleUnderTheUnansweredArm test_cost_e_a_dead_sender_rolled_up_while_a_reachable_row_is_unanswered_stays_unsettled_after_the_release, the mirror written by the real handler and writer). Costs (c) and (e) are accepted as restricted-side costs by the reviewer's ruling: at the branch's base the judge never reached rule 5 at all, so every state the arm holds at cannot-determine was unsettled there too, and the arm trades a false rule 5 for a card that stays unsettled until the user resolves it or the next courier write. Two follow-ups, outside this fix-tier PR: the carrier fix, each heard far host's answered bit carried independent of session rows (an exchange-field change; the two residual witnesses are the tests that move), and settle-on-release, which re-runs the settle of each dead sender a courier write rolled up unsettled under the arm, keyed on the release event, with no timer. Red before the change, on a scratch copy of the twenty-eighth commit with the three changed test modules overlaid: 18 failed on 3.12 and on 3.10, each at its ruled assertion (the four roads, the costs (a) and (c), residual (3b), the ladder's re-pinned rung, the composition's first re-pinned assert and cost (e)'s settle, which reads Completed there); the ladder's order rung (carry-lost there), its cached rung with no link state and the composition's second assert each red there once the assertions before them in the same test accept the old answer. Residual (3a), the release and the controls pass there as at this head.

Round 4, the thirtieth commit (the reviewer's verifier at the twenty-ninth, by execution through the real builders, handler, fold, notify handler, writer and reader, under both root shapes). The twenty-ninth commit's arm read the writer's `reachable`, so a row whose last exchange here served a cache stopped holding once the kernel held its host down: a session started on that host during its kernel's blink, whose own mail had landed here on that exchange, answered rule 5 as soon as the kernel's supervisor reported no kernel answering through the host's tunnel (the down notify a kernel restart there produces), and run_propagate settled the live sender's card, which is sticky; the same held for a host held down first that dialed this bus over its cache carrying the mail, and for a far host's cached word through a hub the kernel then held down. The round's pin that a held-down unanswered row beside a vouching host keeps rule 5 rested on the contrast that such a host's session reaches the judge only through a roster that names it, which those roads refute; this commit reverses that pin. The arm's population is now every row HEARD in this bus process whose last exchange served a cache, whatever its link state (the writer's `heard` True with `answered` False), so rule 5 fires only when a row vouches for absence, no row names the sid, no heard row is unanswered and no lost-carry mark stands, and the bus log's clearing line now says so too ("while a host vouches for absence and no host heard since this bus started has an unanswered roster", pinned by value at both clearing tests). The release is unchanged, that source's next answering exchange; the up notify alone does not release a held-down row, whose link stays down until the host is heard with it up. _source_causes names "listing unanswered" for every row heard in this process, held down or not, so a held-down cached row reads "(link down, listing unanswered)" in every arm that names it, where it read "(link down)" alone. A carried unanswered row still holds nothing (a hold on it would hold every sid for the file's life for a host never heard again), and the session whose mail landed on a cached exchange before this bus restarted is residual (3c), a false rule 5 left open until that host's first exchange in the new process. One more cost on the restricted side, (f): a host held down after a cached exchange that never returns holds every sid until this bus's restart, the shape of cost (c). The witnesses, under both root shapes in tests/test_dead_session_staleness.py ReaderFollowsTheWriter: test_a_session_whose_mail_rode_its_hosts_cached_exchange_stays_cannot_determine_once_the_kernel_holds_that_host_down (the mail on the cached host's request, or on its response to our dial, then the down notify, beside another peer or beside the hub's answered word about that host; the up notify alone; the release), test_a_host_held_down_that_dials_us_over_its_cache_holds_its_sessions_sid_at_cannot_determine, test_a_far_hosts_cached_word_stays_held_once_the_kernel_holds_its_hub_down, test_residual_3c_a_session_whose_mail_landed_on_a_cached_exchange_before_this_bus_restarted_answers_rule_5 (rule 5 before and after this commit; it turns red when the residual closes) and test_cost_f_a_host_held_down_after_a_cached_exchange_that_never_returns_holds_every_sid_until_a_restart; through run_propagate, tests/test_judge_propagate_loads.py SettleWhenTheCachedHostIsHeldDown (the live sender unsettled where the twenty-ninth commit settled it Completed); in the reader's ladder (PresumedClosed), the held-down rung re-pinned to the arm with both causes, and the carried rung keeping rule 5 with its message naming residual (3c). Red before the change, on a scratch copy of the twenty-ninth commit with the three changed test modules overlaid: 15 failed on 3.12 and on 3.10, the same set, each at its ruled assertion (the ladder's held-down rung, the held-down roads and cost (f) under both shapes, the two clearing-line pins under both shapes, and the two run_propagate witnesses, which read Completed there); with every assertion made soft, 33 assertion failures there across the two shapes, among them the halves with the mail on the cached host's response and beside the hub's word, the up-notify step and the cause text of the rungs after the first red, and none at this head. Mutations on a scratch copy of this head: the twenty-ninth commit's arm (reachable rows alone) reds 11; the arm without its heard conjunct reds 13 (the carried rung, residual (3c), the restart releases of costs (c) and (f), and three phases with a carried legacy list); the cause keyed as before reds the 11 held-down reasons; carry-lost first reds the order rung; the clearing line without the arm's conjunct reds the two clearing pins under both shapes. One mutant stays green, and it is equivalent: an `expired` conjunct in the arm, which reaches only a heartbeat row, and the writer writes every heartbeat row answered.

Round 4, the thirty-first commit (the reviewer's verifier at the thirtieth, by execution through the real builders, handlers, folds, this bus's writer and the reader, under both root shapes). The reviewer's ruling of 00:27Z puts the arm on every row heard in this bus process whose last exchange served a cache and allows two releases, that host's next answering exchange and this bus's restart. A via row broke it when its hub's bus restarted: the restarted hub has not heard the far host yet, so its first roster here omitted the host, the writer carried the via row (heard false, out of the arm), and a session started on the far host during its kernel's blink, whose mail the hub had relayed here on the host's cached exchange, answered rule 5 while another row vouched, a second hub's older answered word about the host among them. Now a far host's unanswered word stays on the hub's PEER_STATE row (`viaHeld`, postal/postal_service.py _via_held): both recorders keep it across the hub's rosters that omit the host, _drop_peer_name_dupes hands it to the row that stays when the same bus is folded under another name, and the writer files it as the hub's word, heard, its bit False, until the hub's word names the host again (by the hub's name for it or by its bus id), the host's own row speaks for it here (while that row speaks), or this bus restarts. An answered word the hub omits is carried as before, heard false. The ruling asked for no new writer state; the population needs this one piece, in memory and gone with the process, and the release is unchanged. Residual (3b) loses the hub's restart as a release; residual (3c) gains its far-host face, since a held word does not survive this bus's restart. The costs paragraph's header now says what heard means (the verifier's low at the thirtieth: it said a peer stays heard until the kernel holds its link down, against cost (f), where a held-down row holds until its host answers or this bus restarts): a peer that stops exchanging stays heard, held down or not, until this bus restarts, and a hub's held word the same way; this entry's round-4 paragraph above says so too. The witnesses, under both root shapes in tests/test_dead_session_staleness.py ReaderFollowsTheWriter: test_a_far_hosts_cached_word_stays_held_when_its_hub_restarts_until_the_far_host_answers (the hub's dial and our dial to the restarted hub, F's cached exchange with it, the release), test_a_second_hubs_older_answered_word_does_not_release_the_first_hubs_held_word_and_our_restart_does and test_a_hub_known_by_its_declared_name_that_restarts_and_then_folds_keeps_the_held_word; and tests/test_postal_remote_sids_mirror.py test_a_hubs_held_word_about_a_far_host_stays_heard_until_the_hub_names_that_host_again (through the real handler: the held word and its second, the releases by name and by bus id, an answered word not held, rosters in shapes no bus sends). Red before the change, on a scratch copy of the thirtieth commit with the two changed test modules overlaid: 7 failed on 3.12 and on 3.10, each at its ruled assertion (the three roads under both shapes at the hub's restart, rule 5 there, and the mirror test at the held word, carried there). Mutations on a scratch copy of this head, each red: the recorders holding nothing (7), the handler's recorder alone (7), the dialer's fold alone (2), the writer ignoring held words (7), no hand-over at the fold (2, the declared-name road at the fold), answered words held too (2), release by name alone or by bus id alone (1 each), the via row's second read from the hub's later exchange (1), held words not kept across exchanges (5), and each of the three shape guards (1 each).

Round 4, the thirty-second commit (the reviewer's verifier at the thirty-first, by execution through the real builders, handlers, folds, this bus's writer and the reader, under both root shapes). A held word was released only when the hub named the far host again, and a hub names a far host only through that host's session rows (presence_payload), so a far host that answered its hub with an EMPTY listing, its sessions all ended, never released its word: every sid on this machine stayed cannot-determine for this bus process's life (the verifier's Z2, re-derived here on its probe at the thirty-first commit: the arm at the hub's next exchanges, rule 5 only after our bus restarted). Now each held word carries the bus id of the hub process whose roster last named it (`hubBus`, postal/postal_service.py _via_held), and the SAME hub process omitting the host releases the word: a hub gossips a far host only through its session rows and forgets a far host's PEER_STATE only at its own fold, whose rows then name the host's bus under the other name, so a process that named the host and now omits it has recorded the host's next exchange, carrying no session row, which is the ruled release, the host's answering exchange, when its listing answered empty. A restarted hub (a new bus id, since BUS_ID is minted per process) omitting the host has not heard it yet, so the word stays held as the thirty-first commit made it, and a hub that sends no bus id holds its omitted words; both recorders and _drop_peer_name_dupes's hand-over pass the hub's current bus id. What this leaves, each disclosed with its witness: residual (3a) gains a second face, a far host whose bus restarts during its kernel's blink with no twin after a cached exchange that carried a new session's mail, whose empty cache the same hub's silence cannot be told from an empty answer, so the session answers rule 5 (held at the thirty-first commit); and cost (g), a far host that answers a RESTARTED hub with an empty listing leaves its word held, every sid at cannot-determine, until the hub names it again or this bus restarts; and residual (3d)'s same-road face, two of a hub process's dials landing here out of order by one road, the older omitting the host (a restarted hub's in the first witness), which the release turns from cannot-determine into rule 5 (found by the reviewer's verifier at the thirty-third commit and disclosed at the forty-fifth, whose paragraph below states the reviewer's ruling to keep the release). The carrier fix closes the first two, and the same-road face wherever the older roster carries no answered word of the host (this sentence as the forty-seventh commit corrected it; it had said the carrier fix closes all three, and at the forty-sixth, the same-road face where the hub built the older roster before it heard the host: one hub process's older roster built after the host answered it with an empty listing carries that answered word, and only the source's own order closes that shape, while one built while the hub's word about the host was an empty cache carries no answered word, and the carrier closes it too). The verifier's two mutants that stayed green now red: a release whenever the hub's roster names any far host (its Z1) and held words that never fold into the far host's own row (its Z3). extra5-2's last clause: the X_mail_rides_cached witness's docstring called its road the reachability contrast, and now names THE CONTRAST as the writer and the judge state it, where the session's mail can have landed, not the host's link. The witnesses, under both root shapes in tests/test_dead_session_staleness.py ReaderFollowsTheWriter: test_a_far_hosts_empty_answer_through_the_same_hub_process_releases_its_held_word (the hub's dial), test_a_far_hosts_empty_answer_releases_its_held_word_through_our_dial_to_the_same_hub_process, test_a_far_hosts_empty_answer_releases_its_held_word_through_the_fold_of_the_same_hub_process, test_residual_3a_a_far_hosts_empty_cache_after_its_bus_restarts_releases_its_held_word_and_its_session_answers_rule_5 (rule 5; it turns red when the residual closes), test_cost_g_a_far_host_that_answers_a_restarted_hub_with_an_empty_listing_holds_every_sid_until_a_restart, test_a_restarted_hub_naming_another_far_host_first_releases_nothing (Z1) and test_the_far_hosts_own_answering_row_here_consumes_its_held_word_through_the_fold (Z3); and tests/test_postal_remote_sids_mirror.py test_a_hubs_held_word_is_released_by_the_same_hub_process_omitting_the_host_and_held_by_a_hub_that_cannot_say (through the real handler: the release, the hold across a restart and across the restarted process's later exchange, the hold by a hub with no bus id). Red before the change, on a scratch copy of the thirty-first commit with the two changed test modules overlaid: 9 failed on 3.12 and on 3.10, the same set, each at its ruled assertion (the three release roads under both shapes, listing-unanswered there; the (3a) face under both shapes, the arm there; the mirror test at its release). Mutations on a scratch copy of this change, each red on 3.12: no release by the same process's omission (9), empty bus ids counted as one process (1), any omission releasing (14), the stamp refreshed at each exchange (10), the stamp read from the current exchange (14), the handler's recorder, our fold of a response and the fold's hand-over each passing no bus id (5, 2 and 2), the verifier's two (2 each), and the thirty-first commit's heldAt refreshed at each exchange (1).

Round 4, the thirty-third commit (the reviewer's verifier at the thirty-second, by execution through the real builders, handlers, folds, this bus's writer and the reader, under both root shapes). Three findings. (1) The release by the same hub process's omission assumed that this bus records one hub process's rosters in the order the hub took them. It records them by two roads, the hub's dial (peer_exchange_handle) and the hub's answer to this bus's dial (peer_exchange_apply), and the hub takes that answer when our dial reaches it, so an answer taken before the hub's later dial can be folded here after that dial is recorded: the verifier's R1, a restarted hub's answer omitting a far host folded after its dial naming the host's cached word and carrying a new session's mail, released the word, and the session answered rule 5 while another row vouched. Each road keeps the hub's order (one dialing loop each way, each exchange finished before the next), so each held word now carries the road of the roster that last named it (`hubRoad`, postal/postal_service.py _via_held; each recorder stamps its road on the PEER_STATE row it files, `road`), and only an omission on that road releases it. _drop_peer_name_dupes's hand-over passes the staying row's road: our dial's fold of a hub's declared-name row holds the word until the hub's next dial (at the thirty-second commit it was released at the fold), and the hub's own dial under a new name, the same road, releases it. Cost (h) is new, on the restricted side: a word one road named and the other omits stays held, every sid at cannot-determine, until the naming road omits it too, or, if that road carries no further exchange, until the hub names the host again, the host's own row speaks for it here or this bus restarts. Residual (3d) is new, a false rule 5 left open: the latest roster recorded stands, so a roster that reaches this bus after a newer one from the same source presumes closed a session whose mail rode the newer, cached one, until the source's next exchange; by the same road (a hub that links this bus under two names dials it from two loops, and a request the hub gave up on can arrive after its next one) it releases a word the newer roster named, and by either road an older answered word, or a peer's older answered roster, replaces a newer cached one (this clause as the forty-seventh commit corrected it; it had said across the roads). All three faces read rule 5 at the round's base, where no arm existed, and the thirty-second commit's release by the same hub process's omission causes the same-road face, which held the word at the thirty-first commit (this sentence as the forty-fifth commit corrected it; it had said so of the cross-road faces alone). Closing it needs the source's own order on its rosters, a follow-up beside the carrier fix, which closes the same-road face too wherever the older roster carries no answered word of the host (the forty-seventh commit's wording; where one hub process built it after the host's answer with an empty listing, only the source's own order closes it). (2) The verifier's mutant N9, a release only for a far host whose rows carry a viaBus, passed every module, since every release road gossiped a far host with a bus id; a far bus from before busId and presenceAnswered, whose rows the hub stamps with no viaBus, is now released on the same road, through the reader and at the writer. (3) _via_held's list of what stays open said a hub from before viaBus that renames a far host leaves the old name's word held until this bus restarts. A hub that sends busId but no viaBus renaming a far host within one process releases the old name's word, the new name's standing, and only a rename across the hub's restart holds it, until this bus restarts; such a hub predates presenceAnswered, so cost (a) already holds every sid while it is heard. The list now says so, cost (g) gains the rename across the restart, and a writer-level witness pins both. The witnesses, under both root shapes in tests/test_dead_session_staleness.py ReaderFollowsTheWriter: test_an_older_answer_folded_after_the_hubs_newer_dial_keeps_the_far_hosts_word_held (the verifier's R1), test_cost_h_a_far_hosts_word_named_on_the_hubs_dial_stays_held_across_our_dials_until_the_hubs_next_dial_omits_it, test_a_far_hosts_empty_answer_holds_its_word_through_the_fold_and_the_same_hub_processs_next_dial_releases_it (named test_a_far_hosts_empty_answer_releases_its_held_word_through_the_fold_of_the_same_hub_process until this commit), test_a_far_hosts_empty_answer_releases_its_word_through_the_fold_by_the_hubs_own_dial_under_a_new_name, test_a_far_hosts_empty_answer_releases_its_held_word_through_our_dial_to_the_same_hub_process (our dial now names the host before it omits it), test_a_far_host_with_no_bus_id_is_released_by_the_same_hub_processs_omission_on_the_road_that_named_it and test_residual_3d_an_older_roster_recorded_after_a_newer_one_stands_and_its_session_answers_rule_5 (rule 5 on three faces; it turns red when the residual closes); and tests/test_postal_remote_sids_mirror.py test_a_hubs_held_word_is_released_on_the_road_that_named_it_whatever_bus_id_the_far_host_carries and test_a_hub_from_before_viabus_renaming_a_far_host_releases_the_old_names_word_within_one_process_and_holds_it_across_its_restart. Red before the change, on a scratch copy of the thirty-second commit with the two changed test modules overlaid: 8 failed on 3.12 and on 3.10, the same set, each at its ruled assertion (the R1 road, cost (h) and the declared-name fold under both shapes, rule 5 there; the mirror road test under both far hosts, released there). Mutations on a scratch copy of this change, each red on 3.12: the road ignored (8), the road stamped from the current exchange (8), held words restamped with each exchange's road (4), the handler comparing the answer's road (16), our fold comparing the dial's road (8), the handler recording no road (18), our fold recording no road (4), both recorders one road (8), the hand-over on the dial's road (2), releasing nothing (2) or passing no bus id (2), the verifier's N9 (4), and the thirty-second commit's three re-applied: empty bus ids counted as one process (1), no release by the same process's omission (20), any omission releasing (23).

Round 4, the thirty-sixth commit (the reviewer's ruling on its round-3 refuters' findings, sections G and H). Tests and docstrings alone; no product behaviour changes. (1) The flag-route census behind ThreadOwnSendRefused's set pin is derived to a fixpoint: path functions and module-level names grow together, a path function being one whose own return carries the flags path (the constant, a name bound to it in that function or outside every function, or a call of another path function), and open called as a method is a door whatever its receiver. Until this commit a path function was one whose return held the constant itself and open was a door by bare name alone, so a writer through a function returning a module-level name bound to the constant (the kernel's own _X_FILE and _x_path() idiom), through a chain of path functions, or through p.open, os.open or io.open was missed. The refuter drove two of these through the real WebSocket arm, a path function returning a module-level name and a write through p.open, and each arm wrote the thread-mail key while the witness stayed green; the others are shown missed by their plants in the census's test, red under the census as it stood. On kernel/kernel.py the derived census returns the same two writers and four references, so the set pin stays green. Each new shape is planted in the census's own test, and the census's docstring lists the shapes it covers and discloses what it does not see (a path built by string concatenation, shutil.copy, a subprocess, a path handed to a helper that writes its parameter), one plant of each asserted not found, so the text moves if the census ever reaches one. (2) Each residual names its witness beside it, here and in the writer's docstring: (1) and (4) in the paragraphs above, (2) through two new writer tests, (3) as the twenty-ninth to thirty-third commits named them, (5) ThreadOwnSendRefused, (6) the twenty-fourth commit's witness. Red before the change, on scratch copies of the thirty-fifth commit and of the round-3 head with the changed test modules overlaid: each new census plant is red under the census as it stood, and a WebSocket arm planted in kernel/kernel.py with a writer of either missed shape writes the key through the real handler while the witness passes under the old census and fails under the new. The two residual witnesses pass on the product, as a disclosed residual's witness does, and each is red under a carry that closes its residual: one dropping the old via row once a heard via row from a hub with no link here names the same far bus, and one dropping a carried via row with no bus id while its hub gossips any via row, both of which passed every module at the round-3 head (the second also reds, at the thirty-fifth commit, the thirty-third commit's test of a hub from before viaBus). A carry dropping such a hub's carried rows once its current gossip carries any word with no bus id reds the (2b) witness at its rename phase, the first assertion it reaches, since it drops the old name's row there too; with that assertion removed it reds the restart phase, where it leaves a session live on a far host the restarted hub has not heard yet in no row while another host vouches, a false rule 5. The restart phase alone is red under a carry dropping a carried via row with no bus id once its hub is heard under a bus id other than the one its previous row carried.

Round 4, the thirty-seventh commit (the reviewer's verifier at the thirty-sixth commit, sections G and H). A test, its docstrings and this entry alone; no product behaviour changes. (1) Ten shapes and narrowing rules the flag-route census's docstring lists gained a plant of its own in the census's test, each in its own subtest (this paragraph said every shape and narrowing rule the docstring lists then had one, and six did not; the thirty-eighth commit's paragraph below names them): a path function called as a method, a module-level name bound by an annotated assignment, the path in a door's keyword argument, a path function returning a name bound in it, the rename and write_bytes doors, a module-level name bound inside an if, a mention of a writer that is not a call, a selector as a string constant compared for equality alone, and a nested function's return being its own. Until this commit a census dropping any one of them passed every plant; now each such census reds that plant, and on kernel/kernel.py the set pin is unchanged. (2) The census's docstring and the thirty-sixth commit's paragraph above credit the refuter's runs through the real WebSocket arm with the two shapes it drove, a path function returning a module-level name and a write through p.open, and show the others missed by their plants; both had credited the refuter with every missed shape. (3) The thirty-sixth commit's paragraph names the phase at which the (2b) witness turns red under a carry dropping a no-viaBus hub's carried rows once its gossip carries any word with no bus id, the rename phase (it had said the restart phase), and names a carry under which the restart phase alone is red. (4) The eighteenth commit's item (1) says in its closing note what the thirty-sixth commit added to it.

Round 4, the thirty-eighth commit (the reviewer's verifier at the thirty-seventh commit, section G). A test, its docstrings and this entry alone; no product behaviour changes. The thirty-seventh commit said, in the census's docstring, its test's docstring and its paragraph above, that every shape and narrowing rule the flag-route census's docstring lists had a plant of its own, so a census dropping one turns that plant red. Six had none in the census's test. A census whose path functions need every return to carry the path, whose references are bare names alone, whose selectors come from the right of a comparison alone, or which binds a name through any assignment target, passed the census's test and the set pin; one that takes the selectors of nested ifs innermost first or of the innermost if alone, or that counts a name bound in any function everywhere, turned only the set pin on kernel/kernel.py red. Each now has a plant that such a census turns red: a path function with a return that does not carry the path beside one that does, a mention of a writer through an attribute, a selector on the left of its comparison, the selectors of nested ifs outermost first, a name bound in another function not counting, and a name bound by unpacking into a tuple, asserted not found. The doors' split, two door names by bare name alone and four as methods alone (open both ways), was a seventh rule with no plant, and it could only miss writes (a census counting _write_state_json and _atomic_write as methods too passed the census's test and the set pin), so each door name is now a door by bare name and as a method alike, the rule the thirty-sixth commit applied to open, and a method named _atomic_write and a replace imported from os are each planted. On kernel/kernel.py the census returns the same two writers and four references, so the set pin is unchanged. The census's docstring now names the narrowing rules its claim covers and states the claim as a plant that a census dropping or changing a shape, or widening a rule, turns red, one plant carrying several shapes where the test's first two do; it adds to what the census does not see, each planted and asserted not found, a name bound other than by an assignment to a plain name (unpacking into a tuple, a for loop's target, an assignment expression, a parameter's default) and a name bound in another function through a global statement. Red before the change, on a scratch copy of the thirty-seventh commit: under eight of the verifier's census mutants, the six rules above and the door split, its test module passes five and reds only the set pin under three; the new test module with the thirty-seventh commit's census code swapped in reds the two door plants and nothing else. Mutations of the census on a scratch copy of this change, 48, each red on 3.12 at a plant in the census's test: each of the seven door names dropped; each door form dropped (the two name doors as methods, the four method doors by bare name, open either way, open on os or io); the thirty-seventh commit's door rule; the receiver, the keywords or the module-level writer unread; the constant read at the top of an expression alone, or matched as a substring; the bindings narrowed five ways (no annotated assignment, no function-local names, module-level names from the top statements alone, none inside an if, none inside a try) or read in one pass; a tuple, a for loop's target, an assignment expression, a parameter's default, a global statement or any function's binding counted; a path function called by bare name alone or as a method alone, its returns read without its own names or without module-level names, a nested function's return counted for the outer, the fixpoint cut to one round or its module-level names derived once before it, and every return required to carry; references as calls alone or bare names alone; selectors from any comparison, of any constant type, from an else branch too, innermost first, of the innermost if alone, from the left or the right of a comparison alone; and the class dropped from the dotted name. Each of the six rules' mutants reds at its new plant.

Round 4, the thirty-ninth commit (the reviewer's verifier at the thirty-eighth commit, section G). A test, its docstrings and this entry alone; no product behaviour changes. (1) The doors. The thirty-eighth commit made each door name a door by bare name and as a method, and planted two of the six cells its change added: four of the fourteen cells (_write_state_json as a method, and rename, write_text and write_bytes by bare name) had no plant, and a census dropping any one of them passed. Each of the fourteen cells now has a plant of its own, and the census docstring's list of doors is asserted equal to the list planted. (2) References. The census docstring defines a reference as every other mention of a writer's name in the file, and no plant had one outside every function, so a census skipping mentions at module level or in a class body passed. A mention at module level, one in a class body, and the selectors of a mention at module level are planted. (3) Selectors. Five narrowing rules had no plant: a census taking the first comparison of a test alone, taking selectors from != or from is, from a while, or from an if outside the function around the mention passed. Each is planted, with each of the nine comparison operators other than ==, a comparison mixing == with another operator, a conditional expression and a match's case, and the docstring states the rule in full. (4) Where a binding counts. The docstring said a name counts in the function that binds it and not in another function, while the census counted a name a nested function binds in the function around it and read no binding of an enclosing function in a nested one, so a closure writing through its enclosing function's name was missed. A binding in a function now counts throughout the outermost function around it, every function nested in it at any depth included, and nowhere outside it; the rule errs wide as the doors do (a nested function's binding counts in the function around it, which a nonlocal statement makes real), and the docstring states it. Planted: a closure's write, a write through a name a nested function binds through a nonlocal statement, a write in a function beside the binding in one outermost function, a path function returning its enclosing function's name, and a name bound in a function not counting outside every function. (5) Kinds of binding. An augmented assignment (p = jd.STATE; p /= "session-flags.json") was on neither of the docstring's lists and was missed; it now binds, and is planted. A with statement's as, a comprehension's target and an assignment to an attribute join what the census does not see, with a path held in an attribute and written in another method, each planted and asserted not found. On kernel/kernel.py the census returns the same two writers and four references, so the set pin is unchanged. Red before the change, on a scratch copy of the thirty-eighth commit with this commit's test module overlaid and the thirty-eighth commit's census function swapped back in: the plants of an augmented assignment, a closure's write, a write beside the binding and a path function returning its enclosing function's name are red, and every other plant passes, since each pins what that census already did. Mutations of the census on a scratch copy of this change, 59, each red on 3.12 at a plant in the census's test: each of the fourteen door cells dropped (each changing its own cell's plant alone), and the thirty-seventh commit's door split; references with no mention outside every function, none in a class body, or no selectors outside every function; selectors from each of the nine other operators, from a comparison with any ==, past the function around the mention, from a while, a conditional expression or a match's case, from the first comparison of a test alone, or through and and or alone; no augmented assignment; a binding counting in its own function alone, in its own function and those nested in it (the thirty-eighth commit's rule), in its own function and those around it, in all of those but a function beside it, or in the whole file; path functions reading their own function's bindings alone; the binding read through a for loop's target, an assignment expression, a parameter's default, a global statement, a with statement's as, a comprehension's target, an attribute, or any assignment target; a binding in a function counting outside every function; and the earlier rounds' rules (every return required to carry, references as bare names alone, selectors from the right of a comparison alone, innermost first or from the innermost if alone, names bound anywhere counting everywhere, the class dropped from the dotted name, positional arguments after the first unread, a nested function's return counted for the outer). A docstring whose list of doors drops rename reds the assertion that the list equals the planted list.

Round 4, the fortieth commit (the reviewer's verifier at the thirty-ninth commit, section G). A test, its docstrings and this entry alone; no product behaviour changes, and the census's code is unchanged. Six claims of the flag-route census's docstring had no plant, and a census breaking any one of them passed every plant in the census's test. (1) Selectors come from every operand of a chain of ==: a census reading a chain's left operand and its first comparator alone passed. (2) Selectors come from a comparison anywhere in the test: the docstring named the operands of an and, an or and a not, while the census reads every comparison the test holds, one inside a call included, so a census following the and, the or and the not alone passed, and the thirty-ninth paragraph's statement that the docstring states the rule in full did not hold; the docstring now states the rule the census follows. (3) A binding counts at any depth: no plant was more than two functions deep, so a census counting a binding only in the function one or two levels up from the write passed. (4) Two methods of one class are two functions: a census treating the outermost class or function as the unit, so that the methods of one class share bindings, passed the census's test and turned only the set pin on kernel/kernel.py red, with four extra writers. (5) A reference's dotted name holds every class and function around the mention: a census naming the innermost function and its classes alone passed. (6) A door call in a class body is the writer "<module>": a census naming it by its class passed. Planted, each in its own subtest: a chain of three operands with the constants first and last, and one of four with the constants second and fourth; two comparisons inside a call's argument in the test; a write four functions deep through a name the outermost function binds, and a write in the outermost function through a name bound four functions deep; a name bound in another method of the same class, asserted not to count; a mention of a writer inside a nested function, and one in a method of a class defined in a function; and a door call in a class body. The docstring states each of the six as the census follows it. On kernel/kernel.py the census returns the same two writers and four references, so the set pin is unchanged. Before the change, on a scratch copy of the thirty-ninth commit, ThreadOwnSendRefused passes under the verifier's census mutants for (1), (2), (3) (two of them), (5) and (6), and under three more (a chain read at its left operand and last comparator alone, bindings read from the outermost function and the functions directly nested in it alone, and a dotted name with the classes before the functions); the mutant for (4) reds only the set pin. Mutations of the census on a scratch copy of this change, 84, each red on 3.12 at a plant in the census's test: the verifier's 82 at the thirty-ninth commit but one (a census counting every function's bindings in every function, uncached, which did not finish in the verifier's run; its cached form is among the 81), each of the six above at its new plant, and the three more, each at a new plant of its own.

Round 4, the forty-first commit (the reviewer's ruling of round 4 on section G, which re-plans it as a net and supersedes the thirty-eighth to fortieth commits' closing of the verifier's findings one plant at a time; those commits stay as written). A test, its docstrings and this entry alone; no product behaviour changes. (1) The net. The flag-route census behind ThreadOwnSendRefused's set pin gains a pre-scan of every name and every string of the source it reads (every identifier field of every node of its tree, checked against Python's tokenizer so that a name token it does not read raises), keyed on the derivation's own sets, which refuses by name, with the token and its line, a function that mentions one of them and that the derivation leaves unclassified. Five clauses: the store's name (a string holding session-flags.json); a module-level name and a path function of the fixpoint's, each as a name or as a string equal to it (a name reached through globals() or getattr); a door's name, as a name alone, since the kernel uses open, replace and rename as words; and a writer's name, as a name or a string, so that a route the references miss is refused too, a clause beyond the four the ruling lists. The derivation classifies a writer's name in a function holding a reference, and the path's tokens in a writer, in a function holding no door call it reads that is a path function or holds a binding whose outermost function holds a writer or a path function, and outside every function where a binding is; a door's name it does not read as a call it classifies nowhere. Folded, and counted for no clause: the name of a def or a class, a keyword's label, a door call the derivation reads, a string that is a whole statement (a docstring), and a path function's call or a module-level name whose truth alone decides the test of an if, a while or a conditional expression. On a scratch copy with one clause alone and the derivation's arm behind it removed (the constant, a name or a path function's call in carries; a door by bare name or as a method; a reference through an attribute), that clause refuses its plant by name, and with the arm in place the plant is found and not refused. (2) The folds. On kernel/kernel.py the net refuses seventeen functions the derivation leaves unclassified, each named in NET_EXEMPT with its reason: twelve hold no door call, eleven of them handing a flag reader's value on (the path-function rule counts the readers, a widening the set pin already carries) and one statting the store; four hold a door call that sets no flag (moving an unparseable state file aside, writing notify-cards.json, a string's replace, two appended logs); and one takes a parameter named rename. A row naming a function the net does not refuse raises, so the rows stay exact. The census returns the same two writers and four references, so the set pin is unchanged. (3) The augmented assignment the ruling names (p = jd.STATE; p /= "session-flags.json"; p.write_text(cur)) is bound by the derivation since the thirty-ninth commit; its plant is red under the census of the thirty-eighth commit and of the round-3 head, and with the derivation's arm for it removed on a scratch copy, the net alone refuses it. (4) Widenings are not planted: the docstring says the census's precision is pinned only by the exact set pin on kernel/kernel.py, and the plants that pinned a widening alone (a nested function's return, a comparison not all ==, each of the nine other comparison operators, an if outside the function around the mention, and a while, a conditional expression and a match's case giving no selector) are removed; the three that pinned where a binding does not count now pin the net's refusal of that binding. Every rule sentence of the census's docstring names its plant, and its account of the thirty-seventh to fortieth commits is cut. (5) Each shape the derivation does not see that is planted here is refused by the net, with its refused row (this paragraph said every shape the derivation does not see was refused; the verifier of this commit drove four writers through the real kernel that passed both, three of them closed by the forty-second commit and the fourth stated there as the net's limit): shutil.copy, a subprocess, a path handed to a helper that writes its parameter, a door reached through getattr and a string, and a name bound by unpacking into a tuple, a for loop's target, an assignment expression, a parameter's default, a comprehension's target, a with statement's as, an assignment to an attribute, in an attribute and written in another method, or through a global statement. The net's limits are stated, each with a witness asserted neither found nor refused: a path built at run time (string concatenation), and, the net being function-level, a path a function the derivation classifies writes through a door reached by a string, or hands on through a channel the derivation does not follow to a function whose door call the derivation reads (a path function writing through getattr; a path function handing its path to a helper that writes its parameter). Mutations of the census on a scratch copy of this change, 38, run on 3.12 over ThreadOwnSendRefused: each derivation arm behind a clause removed, each clause removed, each fold removed or widened, each accounting rule dropped or widened, the exemption rows unapplied, stale, short a row or long a row, the string mentions dropped, and the scan narrowed (parameters; global and nonlocal names, red at the tokenizer check alone) are each red; removing the tokenizer check alone changes no result, since the scan misses no token of any source the tests read, and the narrowed scan is its pin.

Round 4, the forty-second commit (the reviewer's verifier at the forty-first commit, section G, under the reviewer's ruling that only a writer passing both the derivation and the net is a finding). A test, its docstrings and this entry alone; no product behaviour changes. The verifier drove four writers through the real kernel that passed both, each writing threadMail into the file the bus reads while ThreadOwnSendRefused stayed green: a new WebSocket arm inside Handler._dispatch_ws, whose exemption row lifted every refusal in the function; a writer reached through globals() and its name as a string, in a route that already held a reference; a new POST branch in _state_write_route that hands the store's path to a helper, since a function returning a flag reader's value was a path function and the net let every mention in it by; and a writer in kernel/judge.py that a new arm calls. (1) Each row lifts the pairs it lists. A row of NET_EXEMPT names the function, the reason its refusal is false, and each (clause, token) pair the net refuses there with its count. A pair beyond its count, or one the row does not list, is refused at every line of it; a pair the net refuses fewer times than its row lists raises; and when the row's function has become a writer, the message says so and names the writers, so the red points at the set pin and not at the row. (2) A writer's name is let by only where the derivation records a reference, a name or an attribute. As a string or an import it is refused, whatever else the function holds. (3) The net judges each mention. A mention the derivation reads (the constant, a bound name, a path function's call) is let by in a writer, or where its value reaches, through divisions alone, one of its function's own return values or an assignment all of whose targets are names the derivation binds; anywhere else it is refused. Two clauses join the five: a name bound to the path, in its outermost function, so that a bound name handed on is refused; and a method the language calls without naming it, a division's (the derivation follows a division) or a path function's named with two underscores on each side. The derivation's rule for carrying the path narrows to match: an expression carries it when it is the constant, a bound name or a path function's call, or a division either of whose operands carries, and the net refuses a path inside any other expression. Under the wider rule every flag reader was a path function (thirty on kernel/kernel.py), and this net refused 417 mentions in 48 functions there. Under the narrow rule no function of kernel/kernel.py is a path function, and the net refuses 27 mentions in nine functions, each lifted by a row with its reason: five that stat the store or look its path up, the two flag readers that hand their path to _read_state_json, the display cache and the quarantine checks, the state quarantine, and _edit_tag's parameter named rename. The dispatchers need no row. The census returns the same two writers and four references, so the set pin is unchanged. New folds: an operand of a comparison whose operators are all is or is not, a bound name whose truth alone decides a test, a name being bound or deleted, and a nonlocal statement's name that the derivation counts in the function around it. The net reads the words of a string, so a derived name inside a string handed to eval is refused. (4) The store's name inside a longer piece of an f-string, joined to a string, and in bytes is planted three times, each refused; a census matching the whole string alone, or reading no bytes, passed before. (5) The limit is stated as what the net reads: kernel/kernel.py alone, so a writer passes when it mentions none of the names the net keys where it stands. A path built at run time, a caller's local read through its frame, and a path or a writer in a module the kernel loads each pass, each with a witness asserted neither found nor refused. The forty-first commit's second limit, a path that a function the net let by hands on, is gone: its two witnesses are now refused. Red before, on a scratch copy of the forty-first commit: with this commit's test module and the forty-first commit's census and rows swapped in, 19 subtests are red, among them the shape of each finding. Over the verifier's planted kernels, the forty-first commit's test module passes ThreadOwnSendRefused for the dispatcher arm, the writer named by a string and the POST branch, and this commit's module reds on each, naming the function, the clause, the token and its line, while the arm still writes threadMail and the bus reads the thread's mail as on. With the derivation's arm behind each clause removed, the clause refuses its plant, and the census as is finds it. Mutations of the census, 37, run on 3.12 over ThreadOwnSendRefused: 32 red (this paragraph said 39 and 34; its record holds 37 and 32), and five that restate what the derivation computes change no result alone. Under a narrowed derivation, removing any of four of them lets the plant pass both; the fifth, the door fold's check, decides whether the door clause reports beside the path token.

Round 4, the forty-third commit (the reviewer's verifier at the forty-second commit, section G, under the reviewer's ruling that only a writer passing both the derivation and the net is a finding). A test, its docstrings and this entry alone; no product behaviour changes. The verifier drove seven writers through the real kernel that passed both, each writing threadMail into the file the bus reads while ThreadOwnSendRefused stayed green; six are closed below, and the seventh, a local read back through locals() without its name, is stated in the limit (5). (1) The folds. The net folded the name of every def and class, every keyword's label and every string that is a whole statement, and Python hands each of these back as text at run time: the store's name in a docstring, read through __doc__ or inspect.getdoc and joined to the state directory; a writer's name as a keyword's label, read as a dict's key; and a writer's name as the name of an unrelated method, read through __name__. The fold now keeps a door's name as the name of a def or a class or as a keyword's label, and a writer's or a path function's name as that function's own def when the def has no decorator, since a decorator is handed the function. Every other def's or class's name and every other keyword's label is keyed, and every string is read, docstrings included. Six plants, each refused: the docstring read back, the keyword's label, the method's name, a writer's own def decorated, a def named like a path function that is not one, and a path function's own def decorated. (The forty-fourth commit restores these folds, the decorated def's name excepted, under the reviewer's ruling that reflection is a stated limit.) (2) The clause for a name bound to the path read it only as a variable or in a global or nonlocal statement, so a binding in a class body inside a function, read as the class's attribute, passed. It now reads every name the scan reads, as the other clauses do: the attribute, and a keyword's label read back through eval, are refused (two plants). (3) The rows. A row lifted a (clause, token) pair at its count, so a lifted mention edited to hand the path to a helper that writes it kept its row. A row now lifts (clause, token, what the value is handed to), at its count: through divisions, and from a keyword argument to its call, the function a call names (.name for a method, Call for a function with no name), the attribute read on it, or else the kind of node holding it, and for the name of a def, a parameter, an import or a global statement, its own kind. The edited mention leaves its row unmatched, which raises, and the same clause and token handed elsewhere beside the lifted mention is refused. On kernel/kernel.py the net refuses 34 mentions in twelve functions, each lifted by a row with its reason; the seven mentions the forty-second commit did not refuse are docstrings, four of them in three functions that had no row (_retire_flags_quarantine, the writer _set_notify_session naming the other writer, and _state_write_route). The census returns the same two writers and four references, so the set pin is unchanged. (4) Two plants pin where the fold for a truth that decides a test stops: a bound name as a conditional expression's body, and one under an and, each handed to a helper, are refused; a census folding either passed before. A further plant, a division defined by assigning __truediv__ in a class body, pins that a method the language calls unnamed is folded nowhere, a name being bound included. (5) The limit is stated as the two members the reviewer ruled: a path or a name reached at run time rather than written, built by string operations or a local read back by reflection without its name (a caller's through its frame, a function's own through locals()), and a path or a writer defined in a module the kernel loads. A witness for locals() joins the frame's; each is asserted neither found nor refused. (The forty-fourth commit restates the limit: see there.) Red before, on a scratch copy of the forty-second commit: with this commit's test module and the forty-second commit's census and rows swapped in, 14 subtests are red. Nine are the plants of (1) and (2), where the old census refuses nothing (or, for the name and the attribute assigned at once, not the attribute); five are the row subtests, which the old census cannot read. The row's red for its own reason is shown in each census's own format: a row lifting the store's name handed to a stat keeps lifting the mention edited to hand it to a helper under the forty-second commit's census, and raises under this one. Over the verifier's planted kernels (the docstring read through __doc__ and through inspect.getdoc, the keyword's label, the method's name, the class body's attribute, and the stat edited into a helper that writes the store), the forty-second commit's test module passes ThreadOwnSendRefused while each arm writes threadMail and the bus reads the thread's mail as on; this commit's module reds on each, naming the function, the clause, the token, its line and what it is handed to, or, for the edited stat, the row it leaves unmatched. The locals() plant passes both modules, as the limit states. With the derivation's arm behind each clause removed, the clause refuses its plant, and the census as is finds it. Mutations of the census, 67, run on 3.12 over ThreadOwnSendRefused: 62 red, among them each fold narrowed or widened, the bound clause restricted, each part of the rows' key dropped or changed, and each arm of the test fold. The five green are the forty-second commit's five that restate what the derivation computes and change no result alone.

Round 4, the forty-fourth commit (the reviewer's verifier at the forty-third commit, section G, and the reviewer's rulings after it: pass-through closes one hop, the one-hop stopping rule, the class body, and reflection as a stated limit). A test, its docstrings and this entry alone; no product behaviour changes. (1) The one hop. A row lifted a mention at its consumer, but when the consumer passed the path on, into a name through str() or a tuple a loop unpacks, new code wrote the store through that name while the row still matched. The verifier drove three such writers through the real kernel, each writing threadMail while ThreadOwnSendRefused stayed green: a write through p in _flags_unknown_cold, p handed there to a helper that writes it, and a write through the loop's p in the view-cache signature. The net now follows a keyed value exactly ONE hop, to its consumer or to the name it lands in. A value that leaves the function through a return, a container, an attribute or a store, and is read back elsewhere, is inside the stated limit, with witnesses. The hop climbs each step that hands the value on as it is: a division, a keyword argument, a conversion (a call named str, bytes, fspath, fsencode, fsdecode, Path, PurePath, abspath, realpath, normpath, expanduser, resolve, absolute or as_posix), a container display, a starred item, a comprehension's element, an operand of and or or, a conditional expression's branch, and an assignment expression. The name the value lands in (a target of an assignment, a for loop or a comprehension, unpacked, or the parameter whose default it is) is keyed as a name bound to the path throughout its outermost function, to a fixpoint. A row's consumer is read at the end of the hop, so a lifted str() handed on to a helper no longer matches its row. On kernel/kernel.py the net refuses 31 mentions in nine functions, each lifted by a row with its reason. The two pass-through names cost four entries: p compared and looked up in _flags_unknown_cold, and p statted and k used as the signature's key in the view-cache signature. Five entries move to the consumer after the hop: Assign and For where they read str and Tuple, and .get and Subscript where the display readers hand str(p) to the cache. The set pin is unchanged. (2) The class body. A binding in a class body inside a function, the class returned and its attribute read in another function, passed both. The derivation no longer follows a binding in a class body, at any depth of it, so the mention is refused where it stands; kernel.py has none, so no row changes. Plants: the returned class, the binding inside an if in the class body, and a method reading self.p (the verifier's second plant for the attribute clause). (3) Reflection is a stated limit, as the reviewer ruled. The folds of a def's or a class's name, a keyword's label and a string that is a whole statement, as a docstring is, are restored, and the seven docstring rows the forty-third commit added are dropped. The name of a def with a decorator stays keyed, since the decorator is handed the function, a consumer one hop away. The limit states a name read back at run time through reflection (a docstring through __doc__ or inspect, a def's or class's __name__, a keyword label made a key). Its witnesses, each asserted neither found nor refused, are the verifier's D1, D2, DX and GD (the store's name as a docstring read through __doc__ and inspect.getdoc, a writer's name as a keyword's label made a key, the store's name cut from a docstring's words, and a writer picked by its parameters' names through __code__), the forty-third commit's plants of the same positions, and a class named like a writer. The one-hop limit's witnesses are a path returned as a string, one appended to a module-level list, one held in an attribute, and one stored as a cache's key and read back elsewhere (the verifier's E1). (4) Controls, each green: the lifted stat split across lines with a keyword argument (the reviewer's item 10), and p's uses re-spelled with the consumers its row lists (item 12), each as a plant and on the real kernel. Red before: over the verifier's planted kernels, C1, C1h, C2 and the returned class write threadMail through the real Handler._dispatch_ws, and the bus reads the thread's mail as on. The forty-third commit's module passes ThreadOwnSendRefused on each; this commit's reds on each, naming the function, the clause, the token, its line and what it is handed to. The same plants in the committed test's form pass the forty-third commit's census with rows in its own key and raise under this one. With the forty-third commit's census body and rows swapped into this commit's module, 96 subtests are red. E1, DX, GD, D1 and D2 write through the real kernel and pass this commit's module, as the limit states. With the derivation's arm behind each clause removed, each clause still refuses its plant alone, and the census as is finds it. Mutations of the census, 99, run on 3.12 over ThreadOwnSendRefused: 92 red, among them each step, each target, the fixpoint, where a landed name is keyed, the consumer read before the hop, the class body, and each restored fold. The seven green are two widenings (a decorated class's name keyed, and a landing followed from a classified mention) and the forty-second commit's five that restate what the derivation computes.

Round 4, the forty-fifth commit (the reviewer's ruling of round 4 on the release by the same hub process's omission, and its verifier at the thirty-third commit, by execution through the real builders, handlers, folds, this bus's writer and the reader, under both root shapes). Docstrings, two witnesses' pins with their harness steps, and this entry; no product behaviour changes. RULED: the release stays, the thirty-second commit's, on the road that last named the host as the thirty-third narrowed it. Holding the word on that omission instead leaves every sid on this machine at cannot-determine in a common state, a far host whose word is held answering its hub with an empty listing once its sessions all end, until the host has a session to name again or this bus restarts (unless its own row speaks here), and nothing tells the reader it is happening. The release's cost is three races, each a false rule 5 whose window ends at the next word about the host, while a completion, reply or resolve for the sender inside a window settles the live session's card wrong for good (this sentence as the forty-seventh commit restated it under the reviewer's ruling of 11:08Z; at this commit it said the ruling weighed two narrow races, each a false rule 5 the reader answers until the next word about the host reaches here): residual (3a)'s second face (a far host's bus restarting during its kernel's blink with no twin after a cached exchange that carried a new session's mail, whose empty cache the same hub's silence cannot be told from an empty answer), until the far host answers the hub and the hub's next exchange reaches here; residual (3d)'s face (1) (two of a hub process's dials landing here out of order by one road, the older omitting the host; a restarted hub's in this commit's witness), until the hub's next roster, which names the host again; and the third race, a second machine answering the hub under the host's name with an empty listing (the forty-seventh commit's paragraph below, which states the ruling's trade and its withdrawn reason), whose window opens again at each of the second machine's exchanges until the host's kernel answers (the forty-eighth commit's paragraph below). The verifier found that face (1) belongs to that population: at the thirty-first commit the word stayed held, cannot-determine once the older dial landed, so the release by the same hub process's omission causes the face; at the round's base it answered rule 5 already at the newer roster, since no arm existed, as the other two faces of (3d) do. The homes now say so: the writer's docstring (the (3d) passage, which states the ruling, the (3a) and release passages, the residual list's opening, both follow-up lists and _via_held's closing sentence), this entry (the fifteenth commit's paragraph, the thirty-second's list of what the release leaves, and the thirty-third's sentence on the round's base, corrected where it said so of the cross-road faces alone), and the witnesses in tests/test_dead_session_staleness.py ReaderFollowsTheWriter, test_residual_3a_a_far_hosts_empty_cache_after_its_bus_restarts_releases_its_held_word_and_its_session_answers_rule_5 (whose docstring named the release as the previous test, which is cost (g)'s, and now names the release's own test) and test_residual_3d_an_older_roster_recorded_after_a_newer_one_stands_and_its_session_answers_rule_5, with the class docstring. Each residual names its closure: the carrier fix, each heard far host's answered bit carried independent of session rows, closes (3a)'s second face and face (1) wherever the older roster carries no answered word of the host, since the release then keys on the far host's own answered word and such a roster carries none, and the source's own order on its rosters closes face (1) too; face (1)'s message names both (the forty-sixth commit corrected "closes both faces", and the forty-seventh the scope: where one hub process's older roster carries the host's answered empty listing only the source's own order closes it, and where it was built over the host's empty cache the carrier closes it as well). The (3d) witness now pins face (1)'s cause beside its answer, the release's three conditions (the newer roster, recorded by the hub's dial, named the host's cached word, and the older dial is the same hub process's), and each witness pins the next word ending the reader's false answer (the hub's next roster naming the host again, cannot-determine; the host answering the hub, rule 4). Red before, on scratch copies with the changed test module overlaid, the same on 3.12 and 3.10: at the thirty-first commit 4 failed, face (1) and (3a)'s second face under both shapes, each at its ruled rule-5 assertion (listing-unanswered there, the word held); at the round's base 4 failed, each at its first cannot-determine step (rule 5 there, no arm); at the forty-fourth commit both pass, the new pins restating its behaviour. Mutations on a scratch copy of this change, each red on 3.12: no release by the same process's omission, the ruling's HOLD (20 across the seven modules, the release's own pins and the two faces alone; 4 on the two witnesses, each at its rule-5 assertion); the handler recording no road beside a release that ignores the road (2, face (1)'s cause pin alone, its rule-5 answer standing); and three on the harness, the older dial another hub process's (2, face (1) held), no next roster from the hub (2) and the far host's kernel still not answering (2), each at its pin.

Round 4, the forty-sixth commit (the reviewer's verifier at the forty-fifth, by execution through the real builders, handlers, folds, this bus's writer, the reader and run_propagate, under both root shapes). Docstrings, three witnesses with their harness roads, and this entry; no product behaviour changes. Three findings. (1) A false rule 5 that no residual named: a hub files every exchange under the name its sender declares, so a second machine, its own bus, exchanging with the hub under a far host's name replaces the host's word there, and this bus cannot tell the two apart. When the second machine answers with an empty listing, the same hub process's roster omits the host on the road that named it, the release frees the held word, and a session live on the host whose mail rode its cached exchange answers rule 5 until the host's next exchange with the hub and the hub's next exchange here, and again after each of the second machine's later exchanges while the host's word at the hub is a cache (the forty-eighth commit's paragraph below); at the thirty-first commit the word stayed held, so the release causes this shape. When the second machine names a session of its own, the hub's roster names the host answered with that session, and every session the host last named answers rule 5, at the thirty-first commit and the round's base as well; once the host's kernel answers, each exchange by either machine presumes the other's sessions closed, for as long as both exchange with the hub. It was disclosed as residual (3e) with its witness, no follow-up named here closing it (the carrier fix would carry the second machine's answered bit as the host's, and the source's own order does not tell two machines apart), and whether two machines under one name at one hub are inside the model was put to the reviewer, who has ruled (the forty-seventh commit's paragraph below applies the ruling): the second machine naming its own session is outside the model, a hub knowing one machine by a name being the premise of rule 5's reach through a hub, and its empty listing joins the release's cost as a third race; residual (3e) is gone. (2) The carrier fix's closure of residual (3d)'s face (1) was overstated. This commit scoped it to where the hub built the older roster before it heard the host, a restarted hub's first dial, the witness's shape (the forty-seventh commit's paragraph below states the scope by its predicate, wherever the older roster carries no answered word of the host, an older roster built over the host's empty cache included). Face (1) also occurs in one hub process: the hub's dial built after the host answered it with an empty listing, delivered after the dial that named the host's cached word and carried a new session's mail. That older dial carries the host's answered word, so a release keyed on it still fires, and only the source's own order closes this shape; at the thirty-first commit the word stayed held. The writer's docstring (the (3d) passage, the residual list's opening and both follow-up lists), this entry (the thirty-second, thirty-third and forty-fifth commits' paragraphs, corrected in place) and the witness docstrings now say so, and a new witness pins the shape with the host's answered word on the older dial. (3) The homes called each race a false rule 5 left open until the next word about the host, and the witnesses said the next word ends the race. The next word corrects the reader's answer, not a settle decided on it (the reviewer's ruling that followed withdrew the release's reason that the next event corrects each wrong answer and restated its cost as a rare permanent wrong settle: the forty-seventh commit's paragraph below): a courier pass (run_propagate) in the window settles the live session's card Completed, and the card stays Completed after the next word, through a second pass and through a rollup of the store with the reader's current answer, since run_propagate is forward-only and never reopens a sender's node, and rollup_status keeps a settled card completed until a reopen event. The writer's docstring states this for every residual's false rule 5 and under the ruling, and a witness pins it in each of the release's four windows ((3a)'s second face, face (1) in both shapes, and (3e)'s first shape; five since the forty-seventh commit, which adds face (1) over an empty cache and names (3e)'s first shape the third race). The witnesses, under both root shapes in tests/test_dead_session_staleness.py ReaderFollowsTheWriter: test_residual_3e_a_second_machine_the_hub_knows_by_a_far_hosts_name_presumes_the_far_hosts_sessions_closed (each pin its own subtest), test_residual_3d_face_1_in_one_hub_process_an_older_dial_carrying_the_far_hosts_empty_answer_answers_rule_5 and test_a_settle_decided_in_a_window_of_the_releases_false_rule_5_stands_after_the_next_word; the (3a) second-face and (3d) witnesses' docstrings and messages say the next word ends the reader's false answer and point to the settle's witness. Red before, on scratch copies with the changed test module overlaid, the same on 3.12 and 3.10: at the thirty-first commit 16 failed, 12 of them the new pins, each at its ruled assertion (the settle in each of the four windows under both shapes, cannot-determine and 'working' there; face (1) in one hub process and (3e)'s first shape under both shapes, listing-unanswered there with the word held), and the forty-fifth commit's two faces under both shapes; at the round's base 14 failed, each at its first cannot-determine step or, for the settle, at the next word (rule 5 there, no arm; the settle's pins in the (3a) window pass there, where the session answered rule 5 before the window as well); at the forty-fifth commit all pass, the new pins restating its behaviour. Mutations on a scratch copy of this change, 3.12, each red at its pin: no release by the same process's omission (32 across the seven modules, 16 on the witnesses); no courier pass in the window (8); no second pass (8); rollup_status without its sticky completion (8, the rollup pin); the second machine under its own name, in the first shape (6), in the second shape (8) and in its later exchange alone (2); the forty-fifth commit's M1, the handler recording no road beside a release that ignores the road (4, the two cause pins); the one-process shape's older dial built by a restarted hub (2); and no next word (8). Green: the seven postal and judge modules, 291 passed on 3.12 and on 3.10; every tests/test_postal*.py and tests/test_judge*.py, 2357 passed and 2 skipped on 3.12 (the served legs, extension deps absent); state isolation, the upstream ledger and tempdir hygiene, 141 passed; the ledger check ok.

Round 4, the forty-seventh commit (the reviewer's ruling of round 4 at 11:08Z on the release by the same hub process's omission, and its re-verifier at the forty-sixth commit, by execution through the real builders, handlers, folds, this bus's writer, the reader and run_propagate, under both root shapes). Docstrings, witnesses with their harness roads, and this entry; no product behaviour changes. Three findings. (1) The reviewer ruled on the two points the forty-sixth commit left open. A second machine that names a session of its own to a hub under a far host's name is outside the model: bus ids are unique by construction, so two machines exchanging with one hub under one host name is a misconfiguration. It is now stated as the PREMISE of rule 5's reach through a hub, a hub knows one machine by a name, where the writer's docstring states the reader's rules (postal/postal_service.py _remote_sids_document, THE PREMISE; the judge's rule 5 in kernel/judge.py _presumed_closed_verdict points to it), with that shape's road as the witness of what a violation does: every session the host last named answers rule 5, at the thirty-first commit and the round's base as well, and once the host's kernel answers each exchange by either machine presumes the other's sessions closed. A second machine that answers the hub under the host's name with an EMPTY listing is caused by the release, since at the thirty-first commit the word stayed held, so it joins the release's cost as its THIRD RACE, beside residual (3a)'s second face and residual (3d)'s face (1): its window ends at the real host's next exchange with the hub and the hub's next exchange here, a settle inside it standing, and opens again at each of the second machine's later exchanges until the host's kernel answers (the forty-eighth commit's paragraph below), and neither the carrier fix nor the source's own order closes it. Residual (3e) is gone. The writer's docstring (_via_held's closing sentence, the premise, the residual list's opening, the release passage, the (3d) passage and the closing follow-up list), this entry (the fifteenth commit's residual list, the forty-fifth's cost sentence and the forty-sixth's paragraph, corrected in place) and the witness and class docstrings say so. (2) The ruling withdrew the release's first reason, that each race is a wrong answer the next event corrects. A completion, reply or resolve for the sender inside a race's window settles the live session's card wrong for good: the judge reads _presumed_closed for a sid absent from the live registry at a courier write and at a reply (run_propagate) and at a user's resolve (kernel/kernel.py's resolve handler), run_propagate is forward-only, and rollup_status keeps a settled card completed until a reopen event. So the release's cost is a RARE PERMANENT WRONG SETTLE, weighed against HOLD's COMMON FROZEN MACHINE: holding the word on the same process's omission leaves every sid on this machine at cannot-determine once a far host whose word is held answers its hub with an empty listing, cleared only by this bus's restart while the host stays empty. The trade keeps the release: a permanent wrong settle needs a narrow race AND a completion, reply or resolve for the sender inside its window, while HOLD's freeze is the common empty-peer state, cleared only by a restart. The writer's (3d) passage and the release's cost lists, this entry and the witness docstrings state it that way, and the next-word wording is gone from them: a race's window ends at the next word, a settle inside it does not. The settle's witness drives the completion; the reply and the resolve roll the store up by the same read, as the writer's docstring cites. (3) Residual (3d)'s face (1) has a third shape, which the release causes too: in one hub process, the hub's dial built while its word about the host was an EMPTY CACHE (the host's bus restarted during its kernel's blink with no twin), delivered after the dial that carried the host's cached word and a new session's mail; the session reads rule 5 at this head, listing-unanswered with the word held at the thirty-first commit, and rule 5 at the round's base. So 'face (1) has two shapes' was false as a closed set, and the carrier's scope was misstated: the carrier fix closes face (1) wherever the older roster carries no answered word of the host (the hub had not heard it, or its word there was a cache), since a release keyed on the host's own answered word does not act on an unanswered one, the reason it closes (3a)'s second face; only where the older roster carries the host's answered empty listing does the source's own order alone close it. And face (2), worded 'across the two roads', left out the same road: the hub's older dial naming the host's answered word, another session, delivered after its newer dial, which reads rule 5 at this head, at the thirty-first commit and at the round's base, so the release does not cause it. Face (2) is now stated by either road. The writer's (3d) passage and follow-up lists, this entry (the thirty-second, thirty-third, forty-fifth and forty-sixth commits' paragraphs, corrected in place) and the (3d) witnesses' docstrings say so. The witnesses, under both root shapes in tests/test_dead_session_staleness.py ReaderFollowsTheWriter: test_residual_3d_face_1_in_one_hub_process_an_older_dial_over_the_far_hosts_empty_cache_answers_rule_5 (new, its road hubDialsOutOfOrderOverEmptyCache, the hub's word about the host a cache, empty, when it built the older dial, among its pins); the same-road face (2) in test_residual_3d_an_older_roster_recorded_after_a_newer_one_stands_and_its_session_answers_rule_5 (its road olderAnsweredDialSameRoad); test_a_settle_decided_in_a_window_of_the_releases_false_rule_5_stands_after_the_next_word, now in five windows; and test_the_releases_third_race_a_second_machine_answering_the_hub_under_a_far_hosts_name_with_an_empty_listing_answers_rule_5 and test_the_premise_violated_a_second_machine_naming_its_own_session_under_a_far_hosts_name_presumes_the_far_hosts_sessions_closed, the forty-sixth commit's test_residual_3e_a_second_machine_the_hub_knows_by_a_far_hosts_name_presumes_the_far_hosts_sessions_closed split in two. Red before, on scratch copies with the changed test module overlaid, the same set on 3.12 and 3.10: at the thirty-first commit 34 failed, each new pin that the release causes at its ruled assertion (face (1) over an empty cache and its window's settle under both shapes, listing-unanswered there with the word held and the tracker 'working'; the third race's rule-5 pin), while the premise's witness passes there, and the same-road face (2), not reached there past face (1)'s pin, reads rule 5 in the roads' record at that commit; at the round's base 81 failed, each new pin at its first cannot-determine step (rule 5 there, no arm); at the forty-sixth commit all pass. Mutations on a scratch copy of this change, 3.12, each red at its pin: no release by the same process's omission (36 across the seven modules, 20 on the witnesses, the premise's witness and the same-road face (2) passing, since the release causes neither); the courier's settle not reading the reader (10) and rollup_status without its sticky completion (10), each at the settle's pins in the five windows; the re-verifier's stand-in for the hub keying a colliding exchange by bus id (26, the premise's and the third race's collision pins among them); the second machine under its own name in the third race's road (6) and in the premise's (8); the empty-cache road's host answering with an empty listing instead (2, at the pin on the hub's word about the host); the same-road face (2) with the hub's dials in order (2, at its rule-5 pin); and the re-verifier's T1 on the answered-empty shape (2). Green: the seven postal and judge modules, 293 passed on 3.12 and on 3.10; every tests/test_postal*.py and tests/test_judge*.py, 2357 passed and 2 skipped on 3.12 (the served legs, extension deps absent); state isolation, the upstream ledger and tempdir hygiene, 141 passed; the ledger check ok.

Round 4, the forty-eighth commit (the reviewer's re-verifier at the forty-seventh commit, by execution through the real builders, handlers, folds, this bus's writer, the reader and run_propagate, under both root shapes). Docstrings, witnesses with their harness road, and this entry; no product behaviour changes. One finding. The homes described the release's third race as one window, and it recurs. After the host's cached word and a new session's mail are relayed here, each exchange by the second machine, an empty listing under the host's name, opens a window: the same hub process's roster omits the host on the road that named it, and the release frees the word. Each of the host's exchanges, still cached, ends it. So while both machines exchange with the hub and the host's kernel does not answer, the session reads rule 5 after every exchange of the second machine that reaches here, and cannot-determine after every exchange of the host; the re-verifier drove three windows, with a courier settle in the second that stood after the next word. The windows end at the host's kernel's answer: once the host names the session (rule 4), the second machine's next exchange replaces an ANSWERED word, which is not held and is carried, not heard, still naming the session, so the reader answers cannot-determine, as at the thirty-first commit and the round's base. At the thirty-first commit the word stayed held at each of the second machine's exchanges, so the release causes every window. The ruling's population: the reviewer's ruling of 11:08Z weighed the release's cost as a narrow race and a settle landing inside its window. The third race is narrow in its condition, a second machine exchanging with the hub under the host's name while the host's kernel does not answer, but not in its windows: under that condition every exchange of the second machine opens one, each another chance for a settle to land. The homes now state the recurrence as part of the cost the ruling weighs; whether it changes the trade is put to the reviewer. The writer's docstring (_via_held's closing sentence, THE PREMISE, the residual list's opening, the release passage's cost, the (3d) passage's third race, the ruling's cost list and trade, and the witness list), this entry (the fifteenth, forty-fifth, forty-sixth and forty-seventh commits' paragraphs, corrected in place) and the third race's, the settle's and the class docstrings say so. The witnesses, under both root shapes in tests/test_dead_session_staleness.py ReaderFollowsTheWriter: the third race's road (hubNameCollisionEmpty) now drives, after the first window, the second machine's next exchange and the hub's dial, the host's next cached exchange and the hub's dial, the host's kernel answering naming the session, and the second machine's exchange after that; test_the_releases_third_race_a_second_machine_answering_the_hub_under_a_far_hosts_name_with_an_empty_listing_answers_rule_5 gains four pins, each its own subtest (the second window, rule 5 with the word released and the hub's row for the host's name carrying the second machine's bus id; the host's next word ending it, cannot-determine; the host's answer, rule 4; and the second machine's exchange after it, cannot-determine, named by the carried answered word); and test_a_settle_decided_in_a_window_of_the_releases_false_rule_5_stands_after_the_next_word pins a settle in the second window too, now six windows. Red before, on scratch copies with the changed test module overlaid, the same set on 3.12 and 3.10: at the forty-seventh commit all pass (the new pins restate its behaviour); at the thirty-first commit 16 failed, the new ones the second window's rule-5 pin and its settle under both shapes, each at its ruled assertion (listing-unanswered there with the word held, the tracker 'working'), while the host's next word, its answer and the cannot-determine after it pass there; at the round's base 16 failed, the new ones the host's next word ending the second window and the second window's settle, at the next word, rule 5 there (no arm), while the second window's rule 5 and the two pins at and after the host's answer pass there. Mutations on a scratch copy of this change, 3.12, on the two witnesses, each red at its pin: no release by the same process's omission (16, the new ones the second window's pin and its settle under both shapes, the pins at and after the host's answer passing, since the release causes neither); a single-window writer, a release that fires once per hub bus id and far host in a bus process (4, exactly the second window's pin and its settle under both shapes; 7 across the seven postal and judge modules, the other three the writer-level release tests in tests/test_postal_remote_sids_mirror.py, which release twice in one module object); a hub's omitted word about a far host dropped rather than carried (2, the pin after the host's answer, rule 5 there, both shapes); the courier's settle not reading the reader and rollup_status without its sticky completion (12 each, every window's settle, the second window's among them); in the harness, the second window's exchange made by the host instead of the second machine (4, the second window's pin and settle) and the host's kernel never answering (4, the host's answer and the pin after it, rule 5 there as the windows go on). Green: the seven postal and judge modules, 293 passed on 3.12 and on 3.10; every tests/test_postal*.py and tests/test_judge*.py, 2357 passed and 2 skipped on 3.12 (the served legs, extension deps absent); state isolation, the upstream ledger and tempdir hygiene, 141 passed; the ledger check ok.
