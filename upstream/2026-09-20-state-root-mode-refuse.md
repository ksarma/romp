---
title: The state root's trust boundary: the process exits (code 2) when the root is writable by another local user (an other write bit, or a group write bit under a group that is not the owner's private group; a group database that cannot be read counts as writable, with its own remedy), when its mode cannot be read, when its path is not a directory (ENOTDIR), or when a pre-existing root that held entries read so at the judge module's import before its own chmod, refusing at the kernel's import (before the first read under the root), at boot, from whichever thread finds it at runtime, in the postal bus at import in every mode and on every poll, and in the session host at start and on every beat; every read of a path under the root goes through one guarded reader whose population an AST census derives and pins, and an entry that fails the guard (a symlink, another uid's, writable by another) is quarantined and read as absent, never adopted; a root not 0700 but writable by nobody else files an error-centre row; the check reads the mode before it repairs; one implementation in kernel/state_root_mode.py serves the kernel, the event model, the bus, the session host and the kernel-side modules the kernel hands its root to
status: candidate
where: kernel/state_root_mode.py (new, stdlib only: errno_text, errno_name, private_group with the bounded, memoized lookup, writable_by_another, check (the ENOTDIR verdict; list_entries), import_read_refusal (the empty-root exemption), trusted_path, relative_under, quarantine, Reader (read_text, read_bytes, open, os_open, gzip_open, stat, exists, isdir, dir, listdir, scandir, iterdir, glob, sys_path_dir), prime_private_group, the `trusted` command line); kernel/judge.py (srm reused or loaded by path under romp_state_root_mode; READER_REFUSED_HOOKS, _gr, _ckpt_dir_guarded and em.set_checkpoint_dir, em.set_state_root; _STATE_ROOT_MODE_AT_IMPORT, _STATE_ROOT_IDS_AT_IMPORT, _STATE_ROOT_CREATED_AT_IMPORT, _STATE_ROOT_EMPTY_AT_IMPORT, _STATE_ROOT_NOT_A_DIRECTORY_AT_IMPORT, _STATE_ROOT_REPAIR_STEP, _STATE_ROOT_REPAIR_ERROR, STATE_ROOT_WRITE_BITS, state_root_mode_check folding importModeRead, importIds, importCreated, importEmpty, importRepairError and importRefusal; _rebind_state's 0700 floor and its checkpoint-directory reset; every read of a path under STATE through _gr); kernel/kernel.py (_READER_REFUSED_PENDING, _reader_refused_row, _flush_reader_refusals, _gr, _state_root_reader_row; _state_root_refusal, _state_root_import_gate before _load_downtime() and TOKEN = _load_token(); _serve_token_read_or_mint's O_NOFOLLOW lock, its bounded wait (SERVE_TOKEN_LOCK_WAIT_S) and its mkdir(mode=0o700); _REPO_ROOT_WRITE_ERROR and _persist_repo_root; STATE_ROOT_CHECK_S, _STATE_ROOT_MODE, _STATE_ROOT_KEY, _STATE_ROOT_LOCK, _STATE_ROOT_REFUSED, _state_root_exit, _state_root_check_interval, _state_root_key, _state_root_row, _state_root_unknown_check, _StateRootExiting, _state_root_exit_now, _state_root_read, _state_root_verdict, _state_root_import_mode_row (importEmpty), _repo_root_write_row, _state_root_boot_check (the parked refusals filed), _state_root_public_status; main() after check_boot_environment; the `stateRootMode` block of _version_info; the `stateRootMode` stage of _jobs_pass and _PerfStats.PASS_JOBS; Handler._state_root_recheck and its call in do_GET, do_POST, do_HEAD and do_OPTIONS; the _ws loop's _STATE_ROOT_REFUSED guard before _dispatch_ws; _sdk_venv_site_packages, _ensure_sdk_on_path and _push_crypto through _gr.glob and _gr.sys_path_dir; every read of a path under jd.STATE through _gr); kernel/event_model.py (_load_state_root_mode, _srm, set_state_root, _STATE_ROOT_FN, READER_REFUSED_HOOKS, _gr; checkpoint_sweep and every checkpoint document and sidecar read through _gr; the round-3 _ckpt_dir_real and _CKPT_SWEEP_REFUSED removed); postal/postal_service.py (_load_state_root_mode, _srm, _gr; _state_root_gate at import in EVERY mode, with list_entries and the empty-root exemption at start, in serve() after STATE.parent.mkdir(mode=0o700), and in _monitor every POLL; the loader's O_NOFOLLOW lock, bounded wait and mkdir(mode=0o700); server.pid written by an O_EXCL temp and os.replace after the guard; _tl_append's guard before the append; every read of a path under the root through _gr; _STATE_ROOT_MODE_AT_START removed); kernel/session_host.py (_load_state_root_mode, _srm, state_root_gate; Journal's and read_journal_dir's guarded reads; SessionHost's guarded spec read; main() gates the root before the spec read and the CLI, and the beat gates it every LEASE_HEARTBEAT_S); bin/romp-session-host (the sdkvenv glob through the shared module's Reader); bin/romp-serve, bin/romp-sdk-setup, bin/romp-codex-setup (pick_python runs the module over the venv's pyvenv.cfg before reading it, byte-identical in the three; romp-codex-setup gains ROMP_DIR); tools/perf-bench.py (StateShadow.target's record flag; load_kernel's import-time diversion covers os.open with a write or create flag and os.replace beside Path.write_text; the builder thread counter excludes the guard's one bounded group lookup); docs/reference.md (Where things live, the state-root paragraphs); plans/state-root-mode.md (the method: the census, the guard's property, the quarantine contract, the boundary); tests/test_state_root_mode.py (new); tests/test_state_root_readers.py (new: the AST census and its allowlist, over the five root binders and the six handed-root modules); kernel/logins.py, kernel/palette.py, kernel/host_transport.py, kernel/sdk_backend.py, kernel/codex_backend.py, kernel/codex_runtime.py (each loads the shared module by path under romp_state_root_mode and reads under the root it is handed through a per-call Reader, `_reader(state_dir)`, or the backends' lazily built `self._gr`; ensure_codex_sdk's codexvenv discovery through the reader's glob and sys_path_dir; the shared module's REFUSED_HOOKS carries their rows to the kernel's error centre); kernel/state_root_mode.py (REFUSED_HOOKS; a root that itself fails the guard is said once per process per root and files no row); kernel/judge.py (_state_root_mode_line names ENOTDIR for a file at the root's path; _rebind_state(path, make=True) makes and floors a test's root); tests/conftest.py (an autouse fixture doubles the kernel's runtime exit with a raising BaseException for every test's length); tests/guarded_reads.py (new: the shared Reader seams for tests that fault, count or interpose on reads under the root); tests/test_jobs_thread_split.py (HOUSEKEEPING gains the stage; two count comments drop the numeral); tests/test_perf_stats.py (one count docstring drops the numeral); tests/test_kernel_serve_token_mode.py, tests/test_postal_token.py and tests/test_pinned_notes.py (one clause each: the bus imports nothing else from kernel/); tests/test_kernel_headless_ops.py (one test waits for the backend's boot threads before its open guard); the eleven test modules whose setUp rebinds the kernel onto a root they make under the umask lose their chmod 0700 and bind the root through jd._rebind_state, which floors it; upstream/2026-09-18-stage-attribution.md (the pass-job count, unchanged this round)
added: 2026-09-20
pr: 874
tier: fix
offered:
closed:
---
kernel/judge.py makes the state root 0700 at import, best-effort, swallowing the OSError, and since 2026-09-19 reads the mode back and says one stderr sentence when it is not 0700, refusing nothing. So a root this uid does not own stays loose with one line nobody reads, a root loosened after boot is never read again, and the premise of _atomic_write's interim-mode argument (a file written at a looser mode is not exposed because the root is owner-only) rested on a diagnostic. The check now has a consequence, and after round 2 the consequence is ONE shape: the process exits with code 2. A root with a group or other write bit (mode & 0o022) is the cross-session code-execution road (another local user can plant or replace entries under it); a root whose mode cannot be read is unverified (a stat on a local directory does not fail transiently, so an unreadable root was removed, renamed or made untraversable, and continuing would re-create it under a parent someone else may own). Either verdict exits.

The exit is the round-2 decision (the 2026-09-20 review's section A). Round 1 answered a runtime refusal with a 503 latch that closed the kernel's two doors (the HTTP handler and the WebSocket read loop) and left every internal writer running under the hostile root, the housekeeping pass whose own first stage had set the latch included, so the write primitive the code-execution road depends on carried on for the life of the latch. An exit is the only shape that stops every writer by construction: the housekeeping pass, the pusher, the judge passes, the timers, the SDK backend's threads. So the latch, its 503 body, the WebSocket refusal frame and refusingSince are gone.

The refusal fires at three places. FIRST at the kernel's module import (_state_root_import_gate), right after the judge module loads and before TOKEN = _load_token() reads the serve token and _persist_repo_root() writes under the root, so the kernel never adopts an entry a writable root already held; the remedy says entries planted while the root was writable are not to be trusted (remove serve-token and repo-root, then chmod 700 the root). This closes the ordering hole (fresh-2): before it, the boot check ran only in main(), after the import had already read or minted the token, and a planted symlink at serve-token killed the import with the token's RuntimeError so the state-root refusal never printed. The verdict stands on the mode as READ AT THAT CHECK, which for the import gate is after the judge module's own repair chmod (the settled rule, round 2b): a pre-existing root this uid owns that read 0777 before that chmod and reads 0700 at the gate is NOT refused, since the same rule would refuse the first boot after every creation of the root by another tool under a group-writable umask (bin/romp-manager's mkdirSync, `romp engine`'s mkdir -p and every test harness's makedirs make the root at the umask's mode, 0775 under 002, and the judge module's import is what tightens it), and the ruling's arm ONE is stated on the current mode. What the import read is REPORTED at boot instead, loud and not fatal: the judge module records the mode it read before its chmod (_STATE_ROOT_MODE_AT_IMPORT) and whether its own mkdir created the root (_STATE_ROOT_CREATED_AT_IMPORT: the mkdir runs without exist_ok), and the boot files one stderr line and one error-centre row for a PRE-EXISTING root that read anything but 0700 and was tightened (_state_root_import_mode_row), with the distrust remedy when the read carried a write bit: entries planted while it was writable are not to be trusted, so remove serve-token and repo-root under the root and restart, and the next boot re-mints them. A root the import CREATED is exempt from the report: a fresh directory carries the umask's mode until the chmod that follows, a creation default and not a loosening. The adoption risk of a root that was writable before this process tightened it is closed by the serve-token loader (it reads or tightens no token through a symlink and faults on a foreign-owned or unchmodable one, its own RuntimeError and exit 1) and by repo-root's atomic replace below; the import gate itself refuses a root whose chmod could not tighten it (it still reads writable) and one whose mode cannot be read. repo-root is written by a temp and os.replace, the token mint's shape (fresh-1's second half): a planted entry at the path, a foreign-owned file write_text could not overwrite or a symlink it wrote through, is replaced, and a write that fails is said on stderr with its errno and filed as an error-centre row at boot (_REPO_ROOT_WRITE_ERROR, _repo_root_write_row) rather than swallowed. SECOND at boot in main(), before any thread starts (SystemExit(2) in the main thread, on the mode as read there), which is where the warn surface lives and where the import's own pre-chmod mode, when it differed on a pre-existing root, is reported as a warn-class transition (loud, not fatal: "read 0755 at import, re-tightened to 0700", or "read 0775 at import (writable by other local users), re-tightened to 0700" with the distrust remedy). THIRD on a cadence while serving: the housekeeping pass re-reads the mode as its first job and every HTTP request re-reads it before routing, each read standing for at most STATE_ROOT_CHECK_S seconds (15; ROMP_STATE_ROOT_CHECK_S overrides). A refuse or unknown verdict found there calls os._exit(2) from whichever thread found it (_state_root_exit_now: the full line with the path to stderr, a flush, no drain and no further write), idempotent under a flag; a second finder raises _StateRootExiting and never returns a verdict to its caller, so no request or stage runs on under the hostile root while the first finder's line is in flight. os._exit, not SystemExit, because SystemExit raised from a non-main thread is swallowed by the thread bootstrap and would kill that thread alone. The manager sees exit 2 and restarts; the import check refuses again while the root reads so, so the manager crash-loops one line per attempt into its log until the root is repaired, and a deploy parked on the quiet window applies into a kernel that refuses at its next boot the same way (the /busy poll misses and the parked apply lands on a kernel that will not stay up).

The re-check READS the mode before it REPAIRS (the 2026-09-20 review's extra6-1). The verdict is taken from the mode as read; the best-effort chmod 0700 runs after and is reported (repaired, modeAfter), so a root this uid owns that was loosened after boot is re-tightened AND leaves a trace, rather than the silent re-tighten round 1 shipped. The import records the mode it read before its own chmod (_STATE_ROOT_MODE_AT_IMPORT) and which call failed (_STATE_ROOT_REPAIR_STEP: mkdir or chmod, so a mkdir failure is not mislabelled as a chmod's) with its errno; state_root_mode_check folds the import's failed call into err when it checks its own root, labelled as the import's (correctness-3). A root that is not 0700 but not writable by others (0750, 0755, 0711) is a privacy fault, not a code-execution one: one error-centre row per transition and one stderr line, no exit. The rows ride _sync_notice under the bell's "refused" kind (not the mutable "sdk" kind of _sdk_problem, whose one mute would hide a security row with a Claude-backend error; extra7-1), are keyed on the verdict AND its cause so an errno that changes under a constant mode refiles (extra6-2), and are built to fit SYNC_NOTICE_FIT whole with the point and the remedy first and the root's path named at most once (extra7-2); the full line with the path goes to stderr. _sdk_problem's seq collision (seq = len(ring)+1 over a ring trimmed to 20, so a long-lived kernel's rows share one signature and the bell's seen-set drops them) is pre-existing and not fixed here; disclosed because these rows no longer ride _sdk_problem.

A failed mkdir or chmod is recorded as "ENAME: strerror" (errno.errorcode and os.strerror, never the exception's text, which carries the path) and reaches the same line, row and /version field, a root that reads 0700 but whose chmod this uid cannot run included (verdict ok with a line, one row per transition). /version carries stateRootMode (verdict, modeRead, modeAfter, repaired, err, importRepairError, remedy, checkedAt) with no filesystem path (the route answers with no token, and the root's default sits under $HOME): the approved deviation, still standing. A served kernel only ever shows verdict ok or warn there, since refuse and unknown exit the process. The check reads the mode of the path the root resolves to and nothing else (not the parents, the owner, or whether it is a directory).

The postal bus is a second serving daemon on the same state root, kept alive by the kernel (its _ensure_postal_bus and every session's message server run `ensure`), and it writes under the root (mailboxes, markers, the timeline log, the pid file). It had no mode check at all (fresh-3), so a hostile root that stopped the kernel left the bus accepting and writing mail. It now loads THE SAME implementation (kernel/state_root_mode.py, by path under the fixed module name; round 2's reduced copy under a KEEP IN SYNC comment is gone, and OneText pins that no loader defines a copy) and takes the same exit: the gate runs at the module's import in EVERY mode, not only under argv `serve` (round 3's extra6-2: the module reads or mints the serve token under the root at import whatever it was invoked for, so `romp mail`, `romp-postal-service sweep` and each session's MCP process were writing under an unchecked root), before the serve token is read and before the bind, on the mode as read at its start whatever the gate's own chmod then did: a pre-existing 0777 root that held entries is refused with the distrust remedy even though the bus tightened it; an empty one is a creation default and starts silently; a pre-existing 0755 root, or 0775 under the owner's private group, is re-tightened and said once; a root the bus makes itself is made 0700 at the mkdir (the token loader's and serve()'s carry mode=0o700, so there is no looser instant: round 3's extra6-3); and again from its monitor loop every POLL while serving, on the mode as READ before that poll's repair; a warn root logs one line per transition and serves. The kernel's _ensure_postal_bus respawns a bus that exits on a hostile root until the kernel itself refuses at its import gate. The session host (bin/romp-session-host), on by default, writing its lease every 3 s and built to outlive the kernel, carried no check at all until round 4 (extra8-3, kernel-3, extra6-1: the exit's justification was false in the default configuration); it now loads the same file and runs the check at its start, before the spawn spec is read and before the CLI is spawned, and on every beat, exiting 2 with one state-root-refused row in host.log (never a path). The docs' sentence that the command-line tools and each session's message server write nothing under the root (correctness-6) is corrected: the tools that only talk to the bus write nothing, and the bus module's own in-process invocations are gated at its import.

ROUND 4 (romp-manager's round-3 ruling, 2026-09-21, after the artifact enumeration refuted round 2b's settlement and the round-3 review found the roster of two against a population of at least eight; the method note is plans/state-root-mode.md). THE DISCRIMINATOR: "writable by another local user" is an OTHER write bit, or a GROUP write bit under a group that is not the owner's private group (the group database lists no member but the owner and no other account has the gid as its primary group); a group write bit under the owner's private group is warn-class like 0755, which is what every directory made under umask 0002 on a user-private-group box reads until the judge import tightens it, and is why the suite stays collectable; the lookup runs in a daemon thread joined with a bound of a few seconds and its definite answer is memoized per (gid, uid) for five minutes, and a database that cannot be read or does not answer counts as writable, the restricted side, with its OWN remedy (the group database could not be read (ENAME or timeout): check getent group <gid> and the name service; romp refuses until it can tell who else can write here), never the distrust remedy. THE IMPORT-READ RULE AND THE CREATION EXEMPTION: a PRE-EXISTING root whose mode as read at the judge module's import, before its own chmod, was writable by another local user AND WHICH HELD ENTRIES at that read refuses at the kernel's import (exit 2) whatever the chmod did afterwards, with the distrust remedy (entries planted while it was writable are not to be trusted: remove serve-token, repo-root and every entry you did not make, or recreate the root, then chmod 700; what the next boot's readers refuse lands in <root>/quarantine); a root that was EMPTY at that read is a creation default whoever made it (correctness-3, tests-7: the exemption keyed on THIS process's mkdir until round 4, so the ordinary first boot after the manager made the root filed the loud distrust row), and boots silently; the same at boot (the two doors agree: _state_root_refusal), in the bus's start gate and in the session host's. ENOTDIR: a state root path that exists and is not a directory is verdict refuse with ENOTDIR and its own remedy at every check, the judge import recording the mkdir's EEXIST and never chmod'ing the file (regression-1). ONE GUARDED READER OVER A DERIVED POPULATION (the ruling's A, B and C): kernel/state_root_mode.py's Reader guards every read of a path under the root in the kernel, the judge, the event model, the bus and the session host (trusted_path: every component from the resolved root down lstat'ed, not a symlink, this uid's, not writable by another by the discriminator; a 0775 venv or a 0664 parked-ops mirror under the private group passes; a path outside the root passes through), and QUARANTINES a failing entry to <root>/quarantine/<utc-stamp>.<path below the root with slashes as dots> (created 0700), says one stderr line, files one refused-kind row naming the entry and the reason (a symlink, another uid's, writable by another, a group the database could not describe), and reads the path as absent, so a planted sdkvenv site-packages never reaches sys.path (kernel-1), a planted pending-ops.json is never replayed (correctness-1, kernel-2, extra5-2, extra8-1), a planted checkpoints link is never swept through (extra5-1; the guard sits at _ckpt_dir(), once and cached), the memo files read empty (extra5-5), planted mail is never listed and a symlinked messages.jsonl or server.pid is never read or written through (the enumeration's HIGH 3 and MEDIUMs), and nothing is merely skipped to be re-adopted next boot (B). The population is derived by an AST census, tests/test_state_root_readers.py, over eleven modules: the five that bind the root at import and, after the review of round 4's tree, the six kernel-side modules the kernel hands its root to per call (logins, palette, host_transport, sdk_backend, codex_backend, codex_runtime: a planted codexvenv reached sys.path[0] through codex_backend's bare glob exactly as kernel-1's sdkvenv had, and a planted logins record reached the login-billing road, both outside the first census's view); seeds from the judge's rebind globals, and for the handed-root modules every state_dir parameter and the backends' self.state_dir; the derivation through names, joins, returns, parameters, locals keyed on the nearest preceding binding, loop targets, a listing entry's .path and guarded listings' entries; the bytes a read answers are data, not a path. It fails for any read that neither goes through a Reader (a module's _gr, a per-call _reader(state_dir), a gr parameter) nor sits on its nine-entry allowlist with a reason (the serve-token loader's own reads and its temp sweep in the kernel and the bus, the kernel's os.listdir of the root for the judge-child records, the judge import's own two reads of the root, the bus's and the Codex registry's fsync descriptors), fails for a stale allowlist entry, and proves it can red by planting an unguarded reader in a copy of the kernel and of logins.py; `python3 tests/test_state_root_readers.py --list` prints the population (424 reads at this head: 415 guarded, 9 allowlisted). The serve-token lock is opened O_NOFOLLOW and its wait for a holder is bounded (SERVE_TOKEN_LOCK_WAIT_S, 30 s, a fault naming the lock and ETIMEDOUT), in both loader copies (correctness-2, extra5-4, extra8-2). THE PINS THAT CAN RED (G): the request road's second finder answers nothing (tests-1: send_response.assert_not_called(), red under the one-token mutation of _StateRootExiting's base to Exception); the no-writer-past-the-exit pin samples the exit instant from the child, a shim that stamps os._exit (correctness-4); the created exemption and the repaired=True road reach the real check (tests-3, tests-4); the socket road's contract is stated in docs and the _ws loop honours an exit already taken, pinned by a frame sent on an upgraded socket after the flag is set (tests-6; red under the guard's removal); a privately minted state root is floored at 0700 in ONE place, jd._rebind_state, and the eleven per-module chmods are gone, those modules binding the root through the seam; the thirty-one sites in twenty-two more test modules that made their root with a bare mkdir now bind it through jd._rebind_state(root, make=True), and TheFloorReachesEveryMintedRoot holds over every tests/*.py that no test makes its root bare (tests-5, extra8-6, and the review of round 4's tree). The one-text gate also holds the SHAPE of the predicate: no loader ands a mode against a write bit under any name. A root that itself fails the guard is said once per process per root and files no row (the gate's check is the root's surface); the judge's import line names ENOTDIR for a file at the root's path. _STATE_ROOT_MODE_AT_START, written and never read, is gone (correctness-5). The shell picker's guard: bin/romp-serve, bin/romp-sdk-setup and bin/romp-codex-setup run the module over the SDK venv's pyvenv.cfg before pick_python reads it, since that file names an interpreter they then run. THE BOUNDARY no check can close, stated in the note, the code and docs/reference.md: a descriptor a peer opened inside the root while it was writable survives the tightening (POSIX checks permission at open), so remove-and-recreate is the boundary of what any of this promises.

The /perf vocabulary gains one key with this PR, `stages_ms.jobs.stateRootMode` (the housekeeping stage's flat row, seeded at zero on every export like the other pass jobs); the receiver's vocabulary regeneration that already covers PR 797 and 784 must add it before uploads from a kernel carrying this PR are accepted, or the export is refused 422 at that key. Recorded (regression-3 of round 3) where the receiver's regeneration reads it: the accumulating deploy note under the perf collector's own state on box 2, in its "Keys added after this note" section, as the dotted job token jobs.stateRootMode; that note is outside this tree and was appended to by hand on 2026-09-21. Not changed: the 0700 target, who owns the root, and the serve socket's mode. Each refuse arm is proved behaviourally in tests/test_state_root_mode.py through child processes that exist at 84b27dd39 too (a kernel and a bus spawned with a hermetic root serve there and never exit when the root is loosened; the import-gate child exits with the token's error there, not the state-root refusal); the in-process tests pin the check dict, the read-before-repair, the transitions, the fit (with the remedy-first property and the rows nearest the cut), the concurrency (the second finder gets no verdict), the writers-stop, the request road (a live Handler, one request per method: the exit, no answer built), the repo-root rename and its failure surface, and the boot-import ordering; the served children pin the import's report on a self-owned 0777 root (serves, one loud line with the distrust remedy) and on a 0755 one (serves, the re-tightened line), and the import child pins the chmod-refused 0777 root's exit 2 before the token work and the token loader's own fault on a planted symlink under a root the import tightened. tools/perf-bench.py's loader diverts, for the length of the import, every os.open with a write or create flag and every os.replace aimed under the copy into the shadow beside Path.write_text, so the bench's census still sees the marker as a shadowed write and the copy unchanged; a temp's open (a name ending in .tmp) is unrecorded and its rename's destination is what the census names, and any other os.open write is recorded under its own name, so a new import-time door shows in the shadowed list rather than being absorbed unnamed.
