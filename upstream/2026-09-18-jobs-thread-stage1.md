---
title: Jobs stage 1: the wake-only nudge look records and skips on the ten-file key; the awaiting wake's unkeyed exits note their legs
status: candidate
where: kernel/kernel.py (`_nudge_look_gated`, `_nudge_look_check`, `_nudge_look_done`, `_nudge_look_mode`, `_tick_miss_named`, `_wake_goal`, the goal walk in `_auto_nudge_session`, `_NUDGE_FILE_KEYED_ROADS`, `_NUDGE_WALK_STATS`); docs/reference.md; tests in `tests/test_nudge_walk_gate.py`, `tests/test_nudge_walk_parse_gate.py`, `tests/test_dead_wait_block.py`
added: 2026-09-18
pr: 784
tier: fix
offered:
closed:
---
Upstream ships the same walk: the wake-only mode is its #936 and the parse gate is its T401, which the fork took in the 2026-09-15 pull-in. With the auto-nudge toggle off the walk runs wake-only and a wake-only look neither skipped nor recorded (the toggle was read as not a file of the session), so every alive session paid the state gates, a parse-cache lookup, the awaiting probe, a shared store view and a goal walk on every housekeeping pass: looks equal to parses, 7353 per 120 s on a 39-session box, 42 to 47 ms of an 85 ms pass. The toggle is the nudge ledger, the tenth keyed file, so the wake-only look now records a memo row under its own mode tag (full, wake, or wake plus reminders; a row serves a look of the same mode only) and skips while the key stands and no noted instant has come. Before that, the two blanket None notes that made every stamped and every delegated top unbounded (stampedWait, allDelegated) are retired, since every ending of a stamped wait is a keyed file or the instant the wake notes and the delegated check is pure over the store, and the wake's exits that read no file name their own legs (freshFault, refusedWrite, peerAlive, dormantOwner), a design-review finding; the code review then found two more ledger writes whose refusal went unnoted once stampedWait was gone, the answered wake's record and the moot stamp, and both note refusedWrite now. A skipped look repeats its verdict and writes nothing; the dead-man fires on the first pass at or after its instant; a skipping pass and a full pass over the same world leave the stores, the ledger and the memo rows byte-identical (a test proves it). Counters: skippedParses rises toward looks with the gear off, and memos.nudgeWalk.wakeOnlyRecorded counts the wake-mode rows. Fold ruling A condition 7 (exactly one shared load per alive session with the toggle off) becomes at most one, zero on a skip. Review round 1 found one high on the toggle-on road and round 2 closed it: retiring the two blanket notes exposed the full walk's user-todo stand-down exit in `_auto_nudge_session`'s goal loop, which reads `STATE/user-todos.json` through `_open_user_todos`, a file outside the ten, and noted no leg, so with the gear on a session holding a stamped or delegated top beside a plain working top was memoised as skippable while an open todo stood, and the plain top's status nudge was held past the todo's dismissal until the next box-wide keyed event or the wake's dead-man instant. The fix notes a `todoStandDown` clock leg before the stand-down exit (the row stays unbounded and the nudge fires on the pass after the dismissal), names the reader `_open_user_todos` in the `walk-completed` road of `_NUDGE_FILE_KEYED_ROADS` so the road census pairs the read with its note, adds the leg to the `unboundedBy` list in docs/reference.md, and pins it with the gear on: the leg test and the end-to-end dismissal pair in `tests/test_nudge_walk_gate.py`, and in `tests/test_nudge_walk_parse_gate.py` the census's noted-readers entry and its must-note check on `_todo_standdown`. The one correction outside the two walk modules is the routing pin in `tests/test_dead_wait_block.py`, which now names the corroboration-first condition of `_wake_goal`'s dormant branch. Filed 2026-09-18 by the fork's performance session.

The stand-down fix is FORK-ONLY, determined against upstream's kernel rather than assumed: at upstream/main 2301f38af8efd78894bda81b58fa29bb79941ec5 (fetched 2026-09-18), `kernel/kernel.py` has no `_open_user_todos`, `_todo_standdown`, `todoStandDown`, `user-todos` or `userTodo` (a grep for all five returns nothing), and its `walk-completed` road at line 14849 lists `_debt_asks`, `_asker_row_alive` and `_nudge_asks_by_target` alone, so upstream has neither the user-todos feature nor a stand-down exit in its goal loop; the one mention in its tree is a `userTodos: {}` fixture key at `tests/test_wire_once_per_build.py:52` that its kernel never reads. Upstream does ship the walk the rest of this entry changes (`_nudge_clock` at 11629, `_wake_goal` at 14067, `_NUDGE_FILE_KEYED_ROADS` at 14843, `_nudge_look_gated` at 14866 in that sha), so the wake-mode memo row, the retired notes, the four `_wake_goal` legs and the `refusedWrite` notes are upstream-relevant. An offer cut from this entry must not carry the `todoStandDown` note, the `_open_user_todos` road entry, the reference leg's `todoStandDown` text, the two gear-on todo tests, or the parse-gate census's noted-readers entry and `_todo_standdown` must-note.
