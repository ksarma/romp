---
title: Kernel restarts no longer re-run a mid-turn send: the boot duplicate guard (`_text_landed`) now recognises the `queued_command` attachment an absorbed message leaves, so a landed send is neither re-queued nor flagged as undelivered. Before, the guard read user records only (its raw-line prefilter required the literal `"user"`, which an attachment line lacks), so a restart between the splice and the next merged build reseeded the echo, read the send as never landed, re-queued the text, and the resumed CLI ran the same instruction twice (2026-09-06 review). `_text_landed` answers True (found: neither re-delivered nor flagged, the next build's by-text prune retires the echo), False (readable, not there: re-deliver) or None (unreadable: flag, never re-deliver on doubt); a record stamped before the send is not this send's landing, the match is the whole collapsed text (joined or any one block), and the prefilter keys on the two record-type literals, so a multi-line send is found. Round 3 of the 2026-09-06 review: the guard read a fixed 2 MB transcript tail, so a landed mid-turn send whose `queued_command` attachment sat further back was re-queued at the next restart and ran twice; and it matched whitespace-collapsed text while `prune_live` compared raw text against the kernel's stripped keys, so a trailing-newline send was found but never pruned or dismissable. Fixed in `86d50a18`: `send()` records the transcript's byte size and fsid on the echo (`_echo_off` / `_echo_fsid`, mirrored as `off` / `fsid`; `_transcript_mark`) and the scan reads from that mark to EOF when the fsid still matches, from byte 0 otherwise (another file, a mark past EOF, no mark); one key function (`session_backend.echo_text_key`, strip only) on every side (`_landed_texts`, `_text_landed`, `prune_live._by_text`, the kernel's `_atom_user_text(s)`, the queued-count fold, `_merge_live_atoms`, the tmux-route fold, and since round 4 the tmux echo's prune `_tmux_echo_prune` and the user-todo answer's landed check `_paste_landed_texts`, read by `_user_todo_answer_lost`; those two compared raw text against the stripped keys, so a delivered tmux send with a trailing newline was never pruned by text and the settle later marked it never-delivered, and a delivered todo answer with edge whitespace reopened at every boot); the found verdict is recorded on the echo (`_landed`) and honoured by `prune_live` and the merge, so a found echo always has an exit
status: candidate
where: fork branch `senddur` commits `6fa03f95` (`kernel/sdk_backend.py` `_text_landed`), `86d50a18` (`_transcript_mark`, the mark on the echo and in the mirror, the mark-to-EOF scan, the recorded verdict; LandingScanStartsAtTheSend, FoundImpliesPrunable) and `3f976a48` (round 4: `kernel/kernel.py` `_tmux_echo_prune` and `_paste_landed_texts` under the key; `tests/test_kernel_fed_echo_absorbed.py` TmuxEchoPrunesUnderTheOneKey and the forms-are-keys pin, `tests/test_user_todos.py` LostAnswerReopens' edge-whitespace answer); red-first in `tests/test_sdk_echo_durability.py` (AbsorbedSendsCountAsLandedAtBoot, private synthetic sid) and `tests/test_restart_redelivery.py`
added: 2026-09-06
pr:
tier:
offered:
closed:
---
Upstream ships both defects (verified 2026-09-06 against `upstream/main`: its `_text_landed` seeks `getsize(path) - 2_000_000` and matches `" ".join(text.split())`, sdk_backend.py ~6521-6545). Its `_reseed_echoes` → `_mark_dropped_echoes(refeed=True)` arm is still to check before offering. Offer with the echo-durability row above: the same restart path.

Status detail (migrated from the table): candidate
