---
title: The chat's mid-turn send story: the kernel's `kind:"user"` event for an absorbed atom carries `absorbed` + `landedAt` (event_model stamps `landedT` at emit time from the repaired timestamp of the queued_command attachment's file-order predecessor, fold-safe, CLAMPED to the send time when it would be earlier: `landedT >= t` always, no tolerance window, since real transcripts invert only by clock granularity and anything larger is a shape the CLI does not write; each clamp is counted as `landedT-clamp` in the assembly stats beside `ts-repair`, served in the version route's `parse` dict; metadata only, PLACEMENTS_V untouched; the two absorbed goldens carry the field and were regenerated to the CLI's real shape, a tool_result before the attachment; the chat shows the time to the minute); the client's pending bubble has no 20 s lifetime (it ends on a landing, the kernel's never-delivered verdict, or ✕, and reads "not confirmed" after a connection drop), an absorbed bubble wears a "joined mid-turn" header, a cue stays where the bubble sat when the message landed higher up ("delivered into the running turn at HH:MM", jump / ✕), and every composer send posts a clientDiag breadcrumb (sid, ts, length, route; never the text) (2026-09-05 audit). The bubble's decision is read from the events AFTER the send, never from a 30-event tail count (2026-09-06 review): at the press each entry is anchored to the last stable kernel event plus the user events already carrying its text; an exact-text landing after the anchor ends it (an absorbed atom a hundred events above the tail included), one landing ends one entry (two identical sends end in send order; "test" is not ended by "test the continue button"), the never-delivered verdict shares the anchor (a resend of a never-delivered message is not ended by the old bubble's verdict, and that verdict never suppresses), the kernel's own echo or queued copy seen after the press clears "not confirmed" and keeps a later drop from re-marking it, the cue anchors on an event the chat draws in its current mode (compact mode hides thinking), the down edge repaints only when an entry changed (the shim fires it per redial), and the bare group's label is per bubble ("not confirmed · sending…") with the ✕'s recount reading the surviving bubbles' own state. Round 3 of that review: the kernel's echo or queued copy is attributed per send the way a landing is (the k-th copy after the anchor hides and confirms the k-th same-text bubble; a claimed echo is background for every later bubble with the text; one echo used to confirm every same-text bubble and clear every "not confirmed", so a lost twin read "sending…" for good), the ✕ removes the bubble's own entry (its press time rides the button as `data-qts`; `dropPending`) instead of the first entry with the text (a "not confirmed · sending…" pair dropped the wrong one and the dismissed bubble came back), and a send pressed against a placeholder tab (no resident frame) is stamped `late` at its first frame, reading the events' kernel stamps so that only what predates the press is anchor or background: the frame's own echo or landed atom is the send's, where it used to be read as an older message (a double bubble, or a bubble that never ended; `stampBase` states the clock assumption: client and kernel host agree to the second). Round 4: the kernel's queued bubble carries no stamp, so a late stamp presumes the frame's newest queued copy of its text is this send's own (one copy per identical send pressed against the same placeholder frame: `stampBase`'s `own`, counted by `reconcilePending`), where it used to count every copy as background, so a send into a busy or held queue whose first frame already listed it sat as a second bubble beside the kernel's copy for the whole wait, with a ✕ that would have cancelled the real send by body; the one misread (an older identical copy already queued, this send not yet received when the frame was built) is stated in `stampBase`, and a stamp on each queued text would remove the presumption
status: candidate
where: fork branch `senddur` commits `5314581a` (`kernel/event_model.py` `_landing_t` + `_seq_ts`, `kernel/kernel.py` chat builder; `tests/test_kernel_fed_echo_absorbed.py`), `6f277cb1` (`kernel/event_model.py` the clamp + `landedT-clamp`; the two absorbed goldens regenerated; `tests/test_event_model_golden.py` AbsorbedLandingNeverPrecedesTheSend), `df572857` (`ui/webview/send-pending.ts` + `send-pending.test.ts`, `render.ts`, `styles.css`; `optimistic-send.test.ts` runs the module), `cc52afd9` (the review's seven fixes: `send-pending.ts` `stampBase` / `scanFrom` / `bareGroupLabel`, `render.ts` `cueRendered` / `markPendingLost` / `fillBareLabel`; `send-pending.test.ts` executes every rule) `7f761578` (round 3: `send-pending.ts` per-send receipt in `reconcilePending`, `dropPending`, the `late` stamp in `stampBase`; `render.ts` `qts` on the ✕ and `late` in `registerOptimistic`; `send-pending.test.ts` (11)–(13), `optimistic-send.test.ts` presses before the kernel's copy) and `3f976a48` (round 4: `send-pending.ts` `stampBase`'s `own` and the count in `reconcilePending`; `send-pending.test.ts` (14))
added: 2026-09-06
pr:
tier:
offered:
closed:
---
Upstream ships the same `OPT_TTL_MS` bubble (render.ts ~284/354) and the same absorbed-atom placement with no chat marking (`git grep absorbed upstream/main -- ui/webview/render.ts` is empty), verified 2026-09-06. No fork-only dependency. Offer WITH or AFTER the echo-durability row above (no floor on the SDK route): the cue is worth little while the kernel echo can still be pruned early. Three widened pins ride along (`queued-indicator.test.ts`, `followup-render.test.ts`, `pipe-durability.test.ts`).

Status detail (migrated from the table): candidate
