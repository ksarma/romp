---
title: PR references in rendered text link to GitHub: `#123`, `PR #123` and `owner/repo#123` in the chat (every md() surface plus the user-todo rows), on feed cards (titles, distiller lines, checklists, held-mail gists, the modal) and in the outline's goal rows link to the PR page of the repository the session works in. The kernel ships `githubRepo` (owner/repo, or null) per session, from the tree's origin remote through the file viewer's `_GITHUB_REMOTE` parser, memoized on the config file's mtime. One helper (`_session_cwd`: the registry's dir, else the transcript's cwd stamp) names the session's directory for both the chat frame and the feed's session rows, so a never-registered session links the same repo on both; and `_tree_of` files every verdict with the EVIDENCE it was reached under — the first `.git` on the directory chain (its path; for a not-a-repo verdict also its mtime and, for a worktree pointer, whether its gitdir exists), a stat walk and no fork — so a session whose directory becomes a repo after its first build (`git init`, `gh repo create`) links without a kernel restart, a `.git` git rejects (a worktree whose clone was removed) is asked about once, a tree deleted or reshaped under a live session re-resolves its HEAD and config pointers once instead of forking per build, and a re-ask that finds a tree's own `.git` changed shape (a worktree rm -rf'd and re-made as a plain clone, or another clone's worktree at the same path) forgets that tree's branch and repo memos instead of serving the dead worktree's for the kernel's life — and a tree whose shape record the store's own 512 bound cleared is treated as unvouched on its next re-ask, since the hit path refills a live tree's memos with no record behind them; a worktree pointer is keyed on its inode beside its mtime, so another clone's worktree re-made at the same path within one mtime tick (a coarse filesystem) is noticed, and a toplevel's own `.git` directory on its path and inode, so a symlinked cwd re-pointed at another repository is noticed too; an empty `.git` git walks past keeps its mtime in the key, so `git init` filling it is found; a worktree cwd costs three stats and no read per memoized call (the pointer's target is memoized on the pointer's mtime and inode), a plain toplevel two; a cwd that is itself a bare repository keeps the branch its HEAD names; a slash-separated run (`#12/#13`) links each reference as GitHub does, a repo name ending in `.` is refused; text inside code, pre, kbd or an existing link never links, nor does a color literal or a path fragment; the opener follows the anchor's href across a rebuild, so a push between press and release still opens the link, and spends only the click that lands on the released link or its ancestors
status: candidate
where: branch `prlinks` (kernel/kernel.py `_github_repo_of`, `_session_cwd`, `_tree_of`'s evidence-keyed verdicts with `_own_dotgit`/`_vouch_tree`/`_pointer_gitdir`/`_is_bare_gitdir`, `_pointer_mtime` + `githubRepo` and `selfHost` on the session frame, `selfHost` on the tabOrder frame (`_tab_order_frame`, the one spelling for its three senders) and `peerHost` on the inbound postal card, and the feed's session rows; postal/postal_service.py `deliver()` stamps `from_host` on every log row ("" = local); tools/perf-bench.py's tripwire admits the read-only `git remote get-url` pair; ui/webview/pr-links.ts with call sites in render.ts (`adoptSelfHost`), feed.ts, fleet.ts; federation.ts carries the local kernel's `selfHost` on the merged tabOrder frame; vscode-extension/src/view-routing.ts routes `openLink` host-side and extension.ts consumes it for every panel (chat, feed, outline); tests/test_github_repo.py, tests/test_postal_from_host_row.py, tests/test_perf_bench.py, tests/test_kernel_tabs_first.py, ui/webview/pr-links.test.ts, ui/webview/multi-kernel-merge.test.ts)
added: 2026-09-06
pr:
tier:
offered:
closed:
---
Upstream ships the same file-viewer parser (their #543), so the kernel half drops in. Two judgment calls to state in the offer: the cross-repo form links only when the session itself is on GitHub (a non-GitHub session links nothing, by design), and a postal body links only against the SENDER's repo, resolved from the frame by the sender's host AND name (the card carries `peerHost` from the message log, RELATIVE TO THE CARD'S KERNEL, and the chat reads it against the host of the session the card sits in: "" → that kernel's own session, the dashboard's own name → a local session, another name → that host's federated session; a sender on a host the dashboard has not attached stays text; a card with no field — a kernel that predates it, or a log row from before `deliver()` stamped `from_host` on every row — falls back to the name, when exactly one session carries it; the chat learns its own kernel's name from every tabOrder frame's `selfHost` — the frame every chat receives first, and the only one a dashboard whose kernel runs no local session gets — as well as from every local session frame and the picker's reply, through one adopter that re-renders built views when the name arrives late); no match, or no repo, leaves the reference as text, and the reader's repo is never a fallback. One limit to state: `peerHost` is the card's kernel's OWN name for the sender's host (the bus files a peer under the local ssh alias when one exists, else the name it declared at check-in, and no host id crosses the wire to match on instead), so the own-name fold and the cross-host row match hold when the two kernels agree on the name — the common topology, since the dashboard's kernel declares its name at check-in; when they disagree the stamp matches no row and the reference stays text, never a wrong link short of the user naming two different machines alike.

Status detail (migrated from the table): candidate
