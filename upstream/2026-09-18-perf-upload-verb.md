---
title: romp perf upload: send one paste-safe export to a configured receiver, with a yes on a terminal or --yes
status: candidate
where: cli/perf_upload.py (the verb: receiver_setting over a regular file, read_export with the depth bound and the top-level allowlist, returning the checked document re-serialised, strict_loads refusing a repeated key and a non-finite parse, an integer literal a double cannot hold among them through its parse_int hook _bounded_int, the deadline connection, post), bin/romp-perf-upload, bin/romp (perf block: dispatch, usage line, help row), cli/perf_export.py (check_document takes the refusal tail; ENVELOPE_KEYS; document_text, the one writer both verbs use; usage_block's uptime through finite_number, and its absence leaf PARSED_UNAVAILABLE in place of a parsed count the snapshot cannot give, with one of two fixed strings keyed on the snapshot's shape, PARSED_UNAVAILABLE_REASON for a snapshot with no parses.perSession block and PARSED_MALFORMED_REASON for one whose block is there but carries no number under sessions), cli/perf_public.py (STAMP_WINDOWS and its comment, key_tokens, duration_key, denylist_problems, private_entries and private_strings and private_strings_path, the private-string probes in machine_probes with their list lines, Probe, probe_in's union match, the numeric floor NUMERIC_PROBE_MIN_DIGITS with its measured derivation, number_matchable and LIST_UNDER_NUMERIC_FLOOR, the expansion bound EXPANSION_EXPONENT_MAX with expansion_bounded and LIST_EXPANSION_SKIPPED, the two fold rules, finite_number, the fold's rule for a number a double cannot hold, with _finite kept as an alias, held_number over the coarsenings' and the merged sums' results, public_uptime and public_bound total over ints, and MEMORY_FRACTION_BOUNDS and CONSTANT_BOUNDS, the classification of every bound-keyed leaf as data), cli/restart_metrics.py (the three-check comments), bin/README.md, cli/README.md, docs/reference.md (the upload section and its receiver contract; the /perf http bullet, whose remote family keeps its route with the host a star, one key per remote operation; the export paragraph's fold parenthetical pointing at that bullet; and the subsection Requests Romp makes on its own, the list of what the outbound census finds today, marked as derived by that test), docs/guide.md (the upload's clause and the kernel paragraph's traffic sentence, a rule naming what the upload sends, that other parts of Romp make requests of their own, and the census and the reference subsection where the derived list lives); tests in tests/test_perf_upload.py, tests/test_perf_export.py, tests/test_perf_stats.py (Disclosed, the live pin over the disclosure paragraph, since the closing check at the re-run's head pinning each conditional clause by a document without its condition, the pin named beside each clause and held to exist, and since the reviewer's re-check the sentence coverage table classifying every span of the paragraph, the word grammar kept as its second layer, and the pins over the two http collapse sentences), tests/test_outbound_requests_census.py (the outbound census: every request site under kernel/, cli/, bin/ and postal/ enumerated by AST, its allowlist naming each site's purpose and gate, the form-space test over planted forms, the shell layer, and the Docs pin holding the reference's list equal to the allowlist), tests/test_restart_metrics.py, tests/romp-perf.bats
added: 2026-09-18
pr: 782
tier: feature
offered:
closed:
---
Stage two of the usage-data plan (2026-09-18), stacked on the export (fork PR 764): one verb sends one file written by romp perf export --public to a receiver and nothing else. The receiver address is configuration and is EMPTY upstream: --receiver URL, else ROMP_PERF_RECEIVER, else ~/.config/romp/perf-receiver (the default-dir pattern; a readable regular file, else refused as an address naming the file, a socket or an unreadable file among them and never reported as no receiver set, so a fifo there does not hang the verb; only an absent or blank file is no receiver); with none set the verb refuses and names the three settings, so an installation nobody configured sends nowhere, and no code carries an address. The receiver is unauthenticated, so the verb reads no token and sends none. The file is checked again as it stands (at most 1 MiB, strict JSON with no repeated key and the romp-perf-export/1 schema line, nested at most 32 levels, about four times a fresh export's depth, refused naming the bound before any check walks it, since the checks recurse one frame per level and this is the one road that reads a file a person names, the export's own scan, walk and denylist check through check_document, a finding reported by kind and key path only, and for a listed private string by the one-based line of the list its entry is on with the remedy, editing that line or the value, never the entry (since 2026-09-19); the scan reads the hostname, login, home, the registry's ids and directories and the machine-local private-strings list when one exists, in every key, string value and number, a number by the spelling the export writes and the upload sends and, when that spelling carries an exponent, by its plain decimal expansion too, so a listed digit run in a numeric leaf is refused however the file spelled it, the expansion applied in keys and string values as well since 2026-09-19, so a listed 1.5e-05 refuses the key and the string 0.000015 too, and an entry whose exponent is beyond 324, further than any number an export carries, kept by its own text and not expanded, since the expansion's length is the exponent, said once on stderr by its list line and never its text, with a numeric floor since 2026-09-19: a listed entry is applied to a number only when it, or the plain decimal spelling of an entry written with an exponent, carries seven or more digits counted across the spelling (the alphabet is not asked: an entry carrying other characters is applied by its run of whole digit groups, as the base did), so such an entry matching a number refuses the export and the upload, an entry that could match a number, by its spelling or by its digit groups, with fewer digits in every spelling is checked in keys and string values and not in numbers, and one stderr line says how many entries that is, which by the line of the list each is on (never the entry's text, never the list's path), and what does and does not protect a number (measured on a real export of 3,770 numbers, a listed run of k digits collided by substring with some number with probability about 0.26 at four digits, 0.023 at five, 0.0019 at six, 0.00015 at seven and 0.000012 at eight, and over nine real exports 0.81, 0.13, 0.011, 0.00095 and 0.000076; below seven a match is coincidence too often to be evidence, so such an entry is not checked in numbers at all rather than checked with a known error rate; the closing check found the first cut computed the floor as the longest digit run, which dropped the base's protection of a listed 1234.5678 and fired the advisory for entries carrying a letter, both corrected in the closing delta, whose own first cut gated the arm on the alphabet and dropped the base's token-run match, corrected before landing; the machine's list held eleven entries, none carrying seven digits, so none is applied to numbers, and none that could match a number, so none is counted) (~/.config/romp/private-strings.txt, the list the pre-push hook reads, ROMP_PRIVATE_STRINGS and XDG_CONFIG_HOME honoured, plainly absent on a clone that never set one up, silently, and no list when the path is not a regular file, so a fifo there does not hang any caller, or when it cannot be read, or when it is a symbolic link whose target is gone, or when ROMP_PRIVATE_STRINGS names a file that is not there, each of those said in one stderr line naming the path and the reason at the moment it happens, since a check that turns itself off must say so (the fourth review round); read as far as 64 KiB with a partial last line dropped so a fragment never becomes a probe; every entry a probe whatever its length, matched as a run of whole tokens or as a substring, whichever finds it, where the hostname and the login stay whole-token matches; one stderr line says when the bound was hit and the entries past it are not checked, when lines that are not UTF-8 were dropped, or when listed entries did not become probes; the hook matches each entry as a plain substring, a known divergence a comment at the read names, one shared matcher being a later change), so romp perf export --public and romp restart-metrics --json --public refuse such a string too; the denylist walk is the fold's fixed point, holding the file to the export's own fold, paste-safe, not unlinkable, with six findings: a key the export drops, a string value the fold would have folded to other, a key the grammar's anchored match admits but the fold would have replaced (a trailing newline among them), an uptime off whole minutes, a bound off a power of two or a float inside a clock stamp's epoch window (1.5e9 to 2.0e9 seconds, 1.5e12 to 2.0e12 milliseconds) under any key but a duration key is refused, and, whatever those find, the document's top-level keys are held to the ones the export writes (schema, exported_at and perf, with kernel_commit and usage optional), any other refused naming the key alone, exported_at and kernel_commit to the export's own shapes, and perf and usage each to its own fold, the verb refusing naming the block, the whole root never folded because the fold's rules are anchored at the block root; and the measurements the export keeps pass: under a counter key an integer of any size a double can hold, about 1.8e308, a byte total or a count (one past it is refused at the parse with the overflowing float, since a double reader makes either an infinity; the closing check at the re-run's head, 2026-09-19, drove math.isfinite raising OverflowError on such an integer under uptime_s and the bound keys, a traceback on the road that sends, and the fix bounds every integer at the parse and makes the fold null one it cannot hold, as it nulls a NaN, the results of its own coarsenings and merged sums held to the same rule, since a bound at the largest double rounded up to a number no double holds and the upload refused the export's own file), under uptime_s one on whole minutes and under a bound key a power of two, the export's own coarsenings, a float outside both windows, the allocator's figures on a long-lived kernel among them, and a float inside a window under a duration key, a name carrying the token ms, a millisecond total, all of which a long-lived kernel's figures reach); what is sent is what was checked: the body is the parsed document re-serialised by the export's own writer (perf_export.document_text), never the file's raw bytes, so a spelling no check read never travels and a file as the export wrote it goes out byte for byte (the fourth review round closed the divergence between the parsed document the checks read and the raw bytes the verb sent); the verb prints the path, the byte size of the body and the URL it will dial (a path in the address included) and asks for a yes on a terminal, refusing off a terminal unless --yes is passed (the agent's form; no setting or variable stands in for it, so nothing uploads from a cron). One POST to <receiver>/v1/upload (stdlib urllib, redirects refused with the Location never parsed, no proxy variable read, no cookies, six headers: Content-Type, Content-Length and User-Agent romp-perf-upload/1 from the verb, Host, Accept-Encoding: identity and Connection: close from the client; 30 s as one deadline over the whole exchange, connect, send, status line, headers and body, held by a timer armed on the connection's socket that shuts it down when the budget runs out, the connect and handshake bounded by the connection's own timeout cut to the time left, not a per-read timeout, so a receiver answering in pieces cannot hold an unattended --yes run open, and the refusal is the same fixed line naming TimeoutError); the only answer accepted is 201 with exactly {receipt: uuid4, retention_days: int, av: ok|skipped}; every other status, body or error is refused with a fixed line carrying only the status code or the error class. docs/reference.md states the request and response contract so any operator can write a receiver, what the request carries (nothing that names the machine beyond the file, and the file's content, which is paste-safe, not unlinkable: the per-process and per-machine measurements the export keeps go with it (rss_peak_kb on macOS among them), so two uploads from one kernel remain linkable by design; the third round found the section promising that nothing about the machine travels, which the body it describes made false, and the fourth rewrote it; the closing check at the re-run's head found the paragraph conditioning the 53 action counts and 15 pane counts on --usage while a plain export's http table carried every one, identical; the paragraph now names the http table's per-route count and millisecond total as travelling in every export, says the usage block adds packaging and no number (true of every snapshot shape the export reads since the block stopped counting an older snapshot's per-sid table, one the plain export drops; for such a snapshot the block writes, in the count's place, sessions.parsedUnavailable where the snapshot gives no number for the count, so an absent count is told from a kernel that parsed nothing, the ruling of the second closing check, 2026-09-19, with one of two fixed strings, each true of the snapshot that carries it: predates-parses.perSession for a snapshot with no parses.perSession block, and perSession.sessions-not-a-number for a snapshot whose block is there but carries no number under sessions, a hand-made shape the leaf's first cut also called old, so a malformed snapshot is told from an old one, the ruling of the same day on the leaf; the paragraph names the leaf and both values with the condition of each; and where the snapshot's count is a number no double can hold, parsed is null in the count's place with no leaf beside it, the export's output for every such number, so sessions carries exactly one of three, a count, parsed null or the leaf, never two and never none, the ruling of 2026-09-19 restating the two outcomes as three, pinned by a fourth document in Disclosed and the corners in the export module's Usage tests, with no fourth reason on the leaf, which would have put one signal over three causes), drops the two provenance claims the code contradicts and names no maker, and Disclosed pins each conditional clause by a document without its condition beside the name populations, the per-generation tables among them, the bounds count derived from the module's classification of every bound-keyed leaf), the 180-day retention, the screening (vocabulary, antivirus) and deletion by receipt id; docs/guide.md says what the upload sends and to whom: a paste-safe copy of the counters, to a receiver the user configures, none shipping, and only after a yes; it ranks the upload against no other traffic. The kernel paragraph's traffic sentence, once an exhaustive list of what leaves the machine, states a rule since the reviewer's re-check of the ruled neighbours (2026-09-19): what the upload sends and when, that other parts of Romp make requests of their own, and where the derived list lives, tests/test_outbound_requests_census.py and the reference's subsection Requests Romp makes on its own; the exhaustive form named claude and the upload while the kernel also fetched the Models API and a price table, posted web push notifications, probed fast-mode availability and ran git, gh and ssh for its features, so a longer list would have been false the same way at the next feature. The same re-check found the /perf http bullet saying the remote family collapsed to one key each while the kernel keeps the route with the host a star, one key per remote operation, as the disclosure paragraph already said; the bullet and the export paragraph's fold parenthetical now say so, pinned in Disclosed beside a sentence coverage table that classifies every span of the disclosure paragraph, so a reworded or added clause reds whatever its wording, the word grammar kept as the second layer. The tests include an executed enumeration of the request a recording receiver saw (line, six headers, body bytes equal to the file for a fresh export and to the parsed document's re-serialisation for an edited one). The stage-three trigger (an unattended sender) is the owner's later word and is not built. Before landing, the branch's history was rewritten so the accepted-address fixture in tests/test_perf_upload.py carries the reserved example domain from the commit that introduced it (the tip had already been redacted; the introducing commit and the ones after it had carried a real service hostname and were published on the fork); the rewrite keeps the string out of main's history, which the fork's merge-commit-only landing would otherwise have carried it into, and it does not unpublish the earlier shas, which stay reachable through the PR's timeline.
