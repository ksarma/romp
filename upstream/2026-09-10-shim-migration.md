---
title: Webview fake-DOM tests: every fake the ratchet's detector finds hides its edges through the shared shim, whose nodes carry a serial, and node lists are asserted by identity
status: candidate
where: ui/test-dom-shim.ts (the serial every node gets from hideEdges, so two distinct nodes are never deepEqual; sameNodes and describeNode for node-list assertions; the header's scope and bounded-dump statements), ui/test-dom-shim.test.ts (the serial and sameNodes pinned; hideEdges on an event-shaped object pinned; the allowlist of unmigrated fakes emptied with its pin at 0, and NON_DOM_EDGES, a second list of documented non-DOM uses of an edge-named key with a reason per entry and its own pin, both a fork state: upstream's lists are computed from upstream's tree), and the eighteen ui/webview test files upstream also ships whose fakes there carry the same enumerable edges: chat-exact-tail-exec, codex-meta-choices, feed-render-incremental, file-view-links, file-view-text-size, file-view, fileview-chip, the live-clock test (webview/*-live-clock.test.ts), github-link, path-links, perf-telemetry, pr-links, setting-stale-fold, strip, tab-strip-skip-exec, thread-selection-scope, timeline-boot, timeline-rehover (each: a non-enumerable define of its edges in the node constructors and hideEdges at their end, or hideEdges on a window stand-in or a node literal, a projection test, and where a deepEqual compared node lists, sameNodes), plus three files upstream also ships that a ported NON_DOM_EDGES lists with the fork's reasons: card-subgoals (a goal fixture's children), feed-absorb (a source pin), setting-stale (a source pin). On the fork the same change covers 146 files (141 from PR 523's first two detector rounds and five of its third); the other 127 are fork-only tests (91 file-comments and file-view, 10 pdf-chunk, 7 anchor-map, 4 feed-keynav, shell-perf, track-decorations-hover-cost and a few more) inside candidate offers (file-comments-tracked-changes, user-todo-file, markdown-viewer-place, viewer-resize-mitigations among them), and pdf-new-tab, present upstream, fakes its window stand-ins only in the fork's openPath test; the fourth non-DOM entry, tab-snapshot-view, is fork-only
added: 2026-09-10
pr: 569
tier: docs
offered:
closed:
---
Depends on the tags-scale-memory entry (fork PR 523): ui/test-dom-shim.ts is absent upstream, so this ports after or with it, and the candidate offers whose tests now import ../test-dom-shim depend on the shim landing first. Why: a failing assert over a fake node with an enumerable parentNode dumps the tree (a feed card in feed-keynav-covered dumped as 318,835 lines; the 40 GB case) and the projection stops it, but a projection alone let a deepEqual pass for any node of the same tag (nine sites in seven files had stopped saying which node came back), hence the serial and sameNodes. Drift, upstream/main as fetched 2026-09-10 (f83a2ea6) against the fork base ed5f4938, for the eighteen: identical in setting-stale-fold, timeline-boot, thread-selection-scope and timeline-rehover; close in fileview-chip +1/-1, strip +7/-5, chat-exact-tail-exec +13/-1, github-link +13/-26, tab-strip-skip-exec +23/-21 and the live-clock test +8/-31; diverged in perf-telemetry +11/-98, file-view-text-size +721/-1308, path-links +387/-189, pr-links +138/-123, file-view-links +104/-180, file-view +163/-418, codex-meta-choices +179/-45 and feed-render-incremental +120/-3, so the port re-applies the pattern to upstream's node classes rather than copying files; of the non-DOM three, card-subgoals and feed-absorb are identical and setting-stale is +1/-1. Review round 2 (fork PR 569): the 40 file-comments Ev classes, fork-only files, end in hideEdges too, so an event's target and currentTarget hide with the nodes, and the shim test pins that on an event-shaped object. The ported ratchet's allowlist holds the seven upstream-only edge-faking files (code-block, file-view-notice, md-img-park, preview-retry-pace, reload-notices, skeleton-tabs-wiring, staged-list-cap) until they migrate, and its NON_DOM_EDGES the three above; by round 3's detector over f83a2ea6 those 44 files (the sixteen shims, the eighteen, the three, the seven) are every file there that initialises an edge-named property.
