---
title: Webview fake-DOM tests: every fake the ratchet's detector finds hides its edges through the shared shim, whose nodes carry a serial, and node lists are asserted by identity
status: candidate
where: ui/test-dom-shim.ts (the serial every node gets from hideEdges, so two distinct nodes are never deepEqual; sameNodes and describeNode for node-list assertions; assertHiddenEvent for the pin on an event class; the header's scope and bounded-dump statements), ui/test-dom-shim.test.ts (the serial and sameNodes pinned; hideEdges on an event-shaped object pinned; the allowlist of unmigrated fakes emptied with its pin at 0, and NON_DOM_EDGES, a second list of documented non-DOM uses of an edge-named key with a reason and the count of edge-initialising lines per entry and its own pin, both a fork state: upstream's lists are computed from upstream's tree; a pin that every ui/webview hideEdges caller is in the detector's view), and the twenty ui/webview test files upstream also ships whose fakes there carry the same enumerable edges: chat-exact-tail-exec, codex-meta-choices, feed-render-incremental, file-view-links, file-view-notice, file-view-text-size, file-view, fileview-chip, the live-clock test (webview/*-live-clock.test.ts), github-link, path-links, perf-telemetry, pr-links, preview-retry-pace, setting-stale-fold, strip, tab-strip-skip-exec, thread-selection-scope, timeline-boot, timeline-rehover (each: a non-enumerable define of its edges in the node constructors and hideEdges at their end, or hideEdges on a window stand-in or a node literal, a projection test, and where a deepEqual compared node lists, sameNodes), plus one file upstream also ships that a ported NON_DOM_EDGES lists with the fork's reason and count: card-subgoals (a goal fixture's children; one line), ui/timeline-transform-tick.test.ts (PR 523's file; its hover-target projection pin expects the serial as the one enumerable key, the one line this PR changes there) and ui/timeline-tag-chips.test.ts (upstream's file, which the fold brought to the fork with its own makeNode; on nodeFactory outright and in SWITCHED since this PR, the swap PR 523's port makes there). On the fork the same change covers 165 files (141 from PR 523's first two detector rounds, five of its third, the eight of its fourth and the eleven of its final rebase onto main, the fakes the fork's main and the upstream fold added before that PR landed): the twenty above, pdf-new-tab (present upstream; it fakes its window stand-ins only in the fork's openPath test), ui/timeline-tag-chips.test.ts, and the other 143, fork-only against upstream/main as fetched 2026-09-10 (f83a2ea6): 104 file-comments and file-view, 10 pdf-chunk and pdf-lazy-render, 7 anchor-map, 4 feed-keynav and 18 more (actions, federation-hidden-hold, path-links-pointer-focus, pinned-notes, render-todo-file-chip, tab-hide, tab-row-keep, track-decorations, track-decorations-guards, track-decorations-hover-cost, track-decorations-kept-embed, url-links, user-todo-links, user-todo-title-links, waiting-detail-link, waiting-file-chip, waiting-file-chip-unframed, and shell-perf, which upstream's tip carries since 8ca94fead, so 142 against the tip), inside candidate offers (file-comments-tracked-changes, user-todo-file, markdown-viewer-place, viewer-resize-mitigations among them); the second non-DOM entry, tab-snapshot-view, is fork-only
added: 2026-09-10
pr: 569
tier: docs
offered:
closed:
---
Depends on the tags-scale-memory entry (fork PR 523): ui/test-dom-shim.ts is absent upstream, so this ports after or with it, and the candidate offers whose tests now import ../test-dom-shim depend on the shim landing first. Why: a failing assert over a fake node with an enumerable parentNode dumps the tree (a feed card in feed-keynav-covered dumped as 318,835 lines; the 40 GB case) and the projection stops it, but a projection alone let a deepEqual pass for any node of the same tag (nine sites in seven files had stopped saying which node came back), hence the serial and sameNodes. Drift, upstream/main as fetched 2026-09-10 (f83a2ea6) against this PR's base, main 409c5367 (the merge of PR 523; git diff --numstat 409c5367 f83a2ea6 per file), for the twenty: identical in setting-stale-fold, timeline-boot, thread-selection-scope and timeline-rehover; close in chat-exact-tail-exec +0/-1, fileview-chip +1/-1, preview-retry-pace +2/-2, strip +7/-5, tab-strip-skip-exec +10/-20, github-link +13/-26, file-view-notice +24/-42 and the live-clock test +8/-31; diverged in perf-telemetry +11/-98, pr-links +13/-99, file-view-text-size +721/-1308, path-links +387/-189, file-view-links +104/-180, file-view +161/-417, codex-meta-choices +178/-79 and feed-render-incremental +120/-3 (fourteen figures are as round 4 took them against ad51cfe7, PR 523's round-4 head; five moved with main between ad51cfe7 and 409c5367: chat-exact-tail-exec, tab-strip-skip-exec, pr-links, file-view and codex-meta-choices; preview-retry-pace, absent at ad51cfe7, is measured here first), so the port re-applies the pattern to upstream's node classes rather than copying files; the non-DOM entry present upstream, card-subgoals, is identical. Review round 2 (fork PR 569): the 40 file-comments Ev classes, fork-only files, end in hideEdges too, so an event's target and currentTarget hide with the nodes, and the shim test pins that on an event-shaped object. Review round 3 (fork PR 569): rebased onto PR 523's fourth round, which lists the eight class fakes the fork's main added before that PR landed (actions, file-comments-markclick, file-comments-markclick-controls, file-comments-seen-fixes, file-comments-seen-review2, file-comments-seen-review3, file-comments-send-seen, file-view-notice); each is migrated the same way, and file-view-notice, present upstream, joins the ported files. NON_DOM_EDGES pins per entry the count of lines on which the detector reads an edge init (card-subgoals 1, tab-snapshot-view 1), so a fake that grows in a listed file trips; feed-absorb and setting-stale came off it at that rebase, since round 4's lexer blanks the product text their source pins quote. Every one of the 67 file-comments and file-view files with an Ev class pins its own event in its projection test through assertHiddenEvent, exported from the shim (target and currentTarget own and non-enumerable, the dump naming neither), so a removed hideEdges call is one red test; a new ratchet pin requires every ui/webview file that calls hideEdges( to be in the detector's view, and pdf-chunk-refused-open's literal writes its children key out for that. The ported ratchet's allowlist holds the five upstream-only edge-faking files (code-block, md-img-park, reload-notices, skeleton-tabs-wiring, staged-list-cap; preview-retry-pace was the sixth until the fold brought it to the fork and this PR migrated it) until they migrate, and its NON_DOM_EDGES the one above; by round 4's detector over f83a2ea6 those 42 files (the sixteen shims, the twenty, the one, the five) are every file there whose code initialises an edge-named property (round 3's detector counted 44: it read feed-absorb's and setting-stale's quoted product text as shapes). Review round 4 (fork PR 569): ui/timeline-transform-tick.test.ts's hover-target projection pin expects the serial as the one enumerable key (the one line this PR changes there; a port of PR 523 without this one keeps that pin at []); the shim test's out-of-scope list names every shape the detector is known to miss (a computed key, a spread of an object built elsewhere, a bracket-notation write, a compound assignment, a class field whose type starts on the line after the colon) and records the tree check that found no fake in any of them, and the callers-pin message points at the same list. Rebased onto main 409c5367, where PR 523 landed (its final head 205ca81b merged onto e0d7d81f); that head listed eleven more files at its own final rebase: the seven file-comments-about and file-comments-resolve-answered class fakes (six already hid parentNode and childNodes through a local helper; the shared module's call replaces it and their Ev classes end in it), the tab-hide and preview-retry-pace class fakes, federation-hidden-hold's window stand-in (hideEdges on the literal after its conditional-valued parent, the line the sixth-round detector reads) and the fold's ui/timeline-tag-chips.test.ts, which takes nodeFactory outright as its tagbtn-click sibling did and joins SWITCHED (seventeen); each with a projection test, the allowlist empty again at 0, 163 ui/webview files calling hideEdges, and the shim test's tree check re-measured on the merged tree (786 files, the lexer agreeing with the TypeScript-AST blanking on all of them). Of the eleven, ui/timeline-tag-chips.test.ts and preview-retry-pace are present upstream (the former is PR 523's port's to switch there as here; the latter comes off the ported ratchet's upstream-only list and joins the ported webview files, twenty now); the nine others are fork-only.
