---
title: Webview fake-DOM tests: every node class hides its edges through the shared shim, whose nodes carry a serial, and node lists are asserted by identity
status: candidate
where: ui/test-dom-shim.ts (the serial every node gets from hideEdges, so two distinct nodes are never deepEqual; sameNodes and describeNode for node-list assertions; the header's scope and bounded-dump statements), ui/test-dom-shim.test.ts (the serial and sameNodes pinned; the allowlist emptied with its ceiling at 0, a fork state: upstream's list is computed from upstream's tree), and the fifteen ui/webview test files upstream also ships whose fake DOMs there carry the same enumerable edges: chat-exact-tail-exec, codex-meta-choices, feed-render-incremental, file-view-links, file-view-text-size, file-view, fileview-chip, the live-clock test (webview/*-live-clock.test.ts), github-link, path-links, pr-links, setting-stale-fold, strip, tab-strip-skip-exec, timeline-boot (each: a non-enumerable define of its edges in the node constructors and hideEdges at their end, a projection test, and where a deepEqual compared node lists, sameNodes). On the fork the same change covers 141 files; the other 125 are fork-only tests (91 file-comments and file-view, 10 pdf-chunk, 7 anchor-map, 4 feed-keynav, and a few more) inside candidate offers (file-comments-tracked-changes, user-todo-file, markdown-viewer-place, viewer-resize-mitigations among them), and pdf-new-tab, present upstream, fakes its window stand-ins only in the fork's openPath test
added: 2026-09-10
pr: 569
tier: docs
offered:
closed:
---
Depends on the tags-scale-memory entry (fork PR 523): ui/test-dom-shim.ts is absent upstream, so this ports after or with it, and the candidate offers whose tests now import ../test-dom-shim depend on the shim landing first. Why: a failing assert over a fake node with an enumerable parentNode dumps the tree (a feed card in feed-keynav-covered dumped as 318,835 lines; the 40 GB case) and the projection stops it, but a projection alone let a deepEqual pass for any node of the same tag (nine sites in seven files had stopped saying which node came back), hence the serial and sameNodes. Drift, upstream/main as fetched 2026-09-10 (f83a2ea6) against the fork base ed5f4938: setting-stale-fold and timeline-boot identical; fileview-chip +1/-1, strip +7/-5, chat-exact-tail-exec +13/-1 close; file-view-text-size +721/-1308, path-links +387/-189, pr-links +138/-123, file-view-links +104/-180, file-view +163/-418, codex-meta-choices +179/-45, feed-render-incremental +120/-3 diverged, so the port re-applies the pattern to upstream's node classes rather than copying files. The ported ratchet's allowlist holds the seven upstream-only edge-faking files (code-block, file-view-notice, md-img-park, preview-retry-pace, reload-notices, skeleton-tabs-wiring, staged-list-cap) until they migrate.
