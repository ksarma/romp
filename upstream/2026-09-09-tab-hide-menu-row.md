---
title: Hide tab from the tab menu: a toggle row in the tab menu's behaviour section (after Notify me) hides the right-clicked copy inside its group, or shows it again, writing the same per-browser, per-(tab, section) entry the section snapshot's Hide and Show write through the one prune site; present only while the strip is sectioned and the copy resolves to a non-pending home section (absent on the flat strip and the phone layout, where hides do not apply); the copy's section is a pure function of the right-clicked copy and the current views (its own group, else the session's one remaining holder, else none; the copy a section ref resolved by its tag's id first and by its name when no held tag carries that id), re-aimed only by the user's own move or add in the Tags flyout, and the row follows it while the menu stays open, re-dressed by one notifier on every change to what the strip reads (a views push, an edit's ack, a reconnect, another pane's hide, pin or grouping flip, a phone-layout flip), the open Tags flyout rebuilt when any input its rows read changed and the menu and flyout placed again after every refresh, with no rebuild under a pressed pointer, a refused click whose re-dress changed no word acknowledged by the sheet's press pulse and the row's tooltip, and the flyout's New tag input one node through a rebuild; the flyout's rows carry their text as tooltips; the two menu rows whose sub-line carries a tag name (Hide tab, Tags) wear a modifier under which the line contributes nothing to the menu's intrinsic width and elides, so a tag name never widens the main menu in any face, while the Tags flyout's rows keep their natural width up to a per-row cap; no kernel round trip
status: candidate
where: fork PR #435 (`tab-hide-menu`): ui/webview/render.ts (showTabMenu: the copy's section copyNow, a SectionRef, and homeNow, a pure function of it and the views: the copy's own group, else the one remaining holder, else nothing, the trail's empty copy reading as none, nothing latched; heldCopy's two passes, the copy's tag id over every held union first and its name only when no union carries that id, and sameSection, the click's guard's comparison of the row's section and the resolution by their local ids when both have one, else by their names; the Hide tab row as one node with a refresh in two steps, the words (returning before any rebuild when unchanged, setting the row's title) and the placement (the menu and the open flyout seated again after every build and rebuild), the refresh and the click reading the phone gate through rowHome, its click checking its guard before the dismissal so a refused click re-dresses the row on the open menu, and a refusal whose re-dress changed no word giving the sheet's .romp-acted pulse (off on animationend) and a tooltip note; the open menu's hook tabMenuViewsHook, set by showTabMenu, cleared by dismissTabMenu and run by the one notifier viewsChanged, which the tabOrder frame handler, onViewsAck, onKernelCaps, the two tab-groups store listeners and the phone media rule's change listener call, as one run through the menu's pressHold so nothing is rebuilt under a pressed pointer; flySig, the flyout's signature listing every input its rows read (the unions, the home resolution, the pin bit), so a store event rebuilds an open flyout; the flyout's held, Move to, + and Show when folded rows carrying their text as titles; the Tags flyout's New tag input built once and never moved, the rows rebuilt in front of it; moveUnion aiming the copy at the destination and aimAdd aiming it at the copy the menu speaks for at an add, or at the add's tag from no group; seatMenu and the flyout's place, run again by the refresh; dressToggle; the ctxIcon tab glyph), ui/webview/styles.css (the .ctx-sub-capped modifier's structural rule: the row's body takes the spare width, its label and sub-line contribute nothing to the menu's intrinsic width and elide; worn by the Hide tab and Tags rows of the main menu alone; the Tags flyout's per-row caps on its labels and sub-lines; .ctx-item-sub unchanged and byte-equal to feed.css's), docs/guide.md, docs/reference.md; tests ui/webview/tab-hide.test.ts (the menu door executed over a transpiled showTabMenu: the live click, its refusal on the open menu, the row following the copy through a move, two tags in both orders and drag orders, the copy's tag removed with the one-holder return and the adds, a views arrival re-dressing the open menu and the tag coming back, a rename pushed while it is open in both drag orders, two remote-only sections, the store's own events and the phone flip through render.ts's own listeners and the caps frame through the notifier, a press held across a push and across a same-named tag's new id, the flyout placed again as it grows, the flyout rows' tooltips, the trail's copy, the flyout following the row over a rect model, the phone gate, the cap and the feed.css twin pinned), ui/webview/tab-hide-browser.test.ts (the menu's width measured with and without the tag-bearing rows in Chromium with Inter and Space Grotesk loaded at 450px, 400px and 383px panes, and in the fallback face; the Tags flyout measured nested in the menu in Chromium and Firefox, its 5-, 19- and 40-character destinations and thirty tags; render.ts's own showTabMenu with real pointer input in both engines: a push mid-press on the Hide tab, Move to and Show when folded rows, and the New tag input's caret, selection and focus through a rebuild), ui/webview/tab-groups.test.ts, ui/webview/tab-tags.test.ts, ui/webview/views-writes.test.ts, ui/webview/tab-tags-flyout.test.ts (the Tags row's source anchor re-aimed to the ctx-sub-capped class), ui/webview/peek-tab.test.ts, ui/webview/session-views.test.ts, ui/webview/tab-meta.test.ts, ui/webview/click-safe.test.ts, ui/webview/tab-emoji-picker.test.ts (the actions import pin re-aimed), vscode-extension/src/tab-menu-flags.test.ts, vscode-extension/src/tabs-first.test.ts
added: 2026-09-09
pr: 435
tier: feature
offered:
closed:
---
Rides the tab-hide-sessions entry (fork PR #380): the store and the strip plan are unchanged, this is a second door to the same entry. It reverses that entry's ruling that the snapshot pane was the one door to a hide: the user asked for the menu door (2026-09-09).
