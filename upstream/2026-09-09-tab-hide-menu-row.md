---
title: Hide tab from the tab menu: a toggle row in the tab menu's behaviour section (after Notify me) hides the right-clicked copy inside its group, or shows it again, writing the same per-browser, per-(tab, section) entry the section snapshot's Hide and Show write through the one prune site; present only while the strip is sectioned and the copy resolves to a non-pending home section (absent on the flat strip and the phone layout, where hides do not apply); the copy's section is computed once in showTabMenu, tracked through the Tags flyout's Move to and shared with the flyout, and the row follows it while the menu stays open, through the flyout's edits and through views arrivals (a pushed rename included: the copy is a section ref, matched by its tag's id first); the two menu rows whose sub-line carries a tag name (Hide tab, Tags) wear a modifier under which the line contributes nothing to the menu's intrinsic width and elides, so a tag name never widens the main menu in any face, while the Tags flyout's rows keep their natural width up to a per-row cap; no kernel round trip
status: candidate
where: fork PR #435 (`tab-hide-menu`): ui/webview/render.ts (showTabMenu: the tracked copy section copyNow, a SectionRef matched by its tag's id first and its name second so a pushed rename keeps the copy, and homeNow, the copy's own group, else the one remaining holder, else nothing, the trail's empty copy reading as none; the Hide tab row as one node with a refresh the Tags flyout's build calls after each of its writes, its click resolving the section live and writing only for the copy the row named; the open menu's views hook tabMenuViewsHook, set by showTabMenu, cleared by dismissTabMenu and run by the tabOrder frame handler and onViewsAck, re-dressing the row, the Tags row's sub-line and the flyout's rows when an arrival changed what they show; moveUnion writing the destination and claimIfLoose naming the copy an add from no group made; seatMenu and the flyout's place, run again by the refresh; dressToggle; the ctxIcon tab glyph), ui/webview/styles.css (the .ctx-sub-capped modifier's structural rule: the row's body takes the spare width, its label and sub-line contribute nothing to the menu's intrinsic width and elide; worn by the Hide tab and Tags rows of the main menu alone; the Tags flyout's per-row caps on its labels and sub-lines; .ctx-item-sub unchanged and byte-equal to feed.css's), docs/guide.md, docs/reference.md; tests ui/webview/tab-hide.test.ts (the menu door executed over a transpiled showTabMenu: the live click, the row following the copy through a move, two tags in both orders and drag orders, the copy's tag removed with the one-holder return and the adds, a views arrival re-dressing the open menu, a rename pushed while it is open, the trail's copy, the flyout following the row over a rect model, the phone gate, the cap and the feed.css twin pinned), ui/webview/tab-hide-browser.test.ts (the menu's width measured with and without the tag-bearing rows in Chromium with Inter and Space Grotesk loaded at 450px, 400px and 383px panes, and in the fallback face; the Tags flyout measured nested in the menu in Chromium and Firefox, its 5-, 19- and 40-character destinations and thirty tags), ui/webview/tab-groups.test.ts, ui/webview/tab-tags.test.ts, ui/webview/views-writes.test.ts, vscode-extension/src/tab-menu-flags.test.ts
added: 2026-09-09
pr: 435
tier: feature
offered:
closed:
---
Rides the tab-hide-sessions entry (fork PR #380): the store and the strip plan are unchanged, this is a second door to the same entry. It reverses that entry's ruling that the snapshot pane was the one door to a hide: the user asked for the menu door (2026-09-09).
