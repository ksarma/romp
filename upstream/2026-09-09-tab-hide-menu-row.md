---
title: Hide tab from the tab menu: a toggle row in the tab menu's behaviour section (after Notify me) hides the right-clicked copy inside its group, or shows it again, writing the same per-browser, per-(tab, section) entry the section snapshot's Hide and Show write through the one prune site; present only while the strip is sectioned and the copy resolves to a non-pending home section (absent on the flat strip and the phone layout, where hides do not apply); the copy's section is a pure function of the right-clicked copy and the current views (its own group, else the session's one remaining holder, else none; the copy a section ref resolved by its tag's id first and by its name when no held tag carries that id), re-aimed only by the user's own move or add in the Tags flyout, and the row follows it while the menu stays open, re-dressed by one notifier on every change to what the strip reads (a views push, an edit's ack, a reconnect, another pane's hide, pin or grouping flip, a phone-layout flip), the open Tags flyout rebuilt when any input its rows read changed and the menu and flyout placed again after every refresh, with no rebuild under a pressed pointer, a refused click whose re-dress changed no word acknowledged by the sheet's press pulse and the row's tooltip, and the flyout's New tag input one node through a rebuild; the flyout's rows carry their text as tooltips; the two menu rows whose sub-line carries a tag name (Hide tab, Tags) wear a modifier under which the line contributes nothing to the menu's intrinsic width and elides, so a tag name never widens the main menu in any face, while the Tags flyout's rows keep their natural width up to a per-row cap, the flyout capped at the visible height and scrolling within itself with its own scroll leaving the menu standing, its rows acting at the click on the union their ref names by its id, else by its name, Move to moving from the copy's home at the click and Show when folded pinning it, each refused once with a cue carried to the rebuilt row when that home is not the row's section, a row whose union is gone rebuilt away with no cue, and the click that opens the flyout focusing its New tag input without scrolling the flyout; no kernel round trip
status: candidate
where: fork PR #435 (`tab-hide-menu`): ui/webview/render.ts (showTabMenu: the copy's section copyNow, a SectionRef, and homeNow, a pure function of it and the views: the copy's own group, else the one remaining holder, else nothing, the trail's empty copy reading as none, nothing latched; heldCopy's two passes, the copy's tag id over every held union first and its name only when no union carries that id, and sameSection, the comparison of two sections by their local ids when both have one, else by their names, used by the Hide tab row's click guard, by Move to's source guard, by the pin row's home guard and by the carried cue's match; the Hide tab row as one node with a refresh in two steps, the words (returning before any rebuild when unchanged, setting the row's title) and the placement (the menu and the open flyout seated again after every build and rebuild), the refresh and the click reading the phone gate through rowHome, its click checking its guard before the dismissal so a refused click re-dresses the row on the open menu, and a refusal whose re-dress changed no word giving the sheet's .romp-acted pulse (off on animationend) and a tooltip note; the open menu's hook tabMenuViewsHook, set by showTabMenu, cleared by dismissTabMenu and run by the one notifier viewsChanged, which the tabOrder frame handler, onViewsAck, onKernelCaps, the two tab-groups store listeners and the phone media rule's change listener call, as one run through the menu's pressHold so nothing is rebuilt under a pressed pointer; flySig, the flyout's signature listing every input its rows read (each union's name, id, colour, pending state and hold on the session, each constituent tag's id and whether it holds the session, the home resolution, the pin bit), so a store event, or a constituent tag joining or taking the session, rebuilds an open flyout; liveUnion, by which the x, Move to's destination, the + beside Move to and the + <name> row resolve their union at the click from the ref they were built from, the ref's local id over every union first and its name only when no union carries that id, heldCopy's rule; Move to's source and the pin row's section, the copy's home at the click (homeNow), refused unless it is the section the row was built for (sameSection); refuse and cue, a refused click (Move to's source or the pin row's section no longer the copy's home) recorded by its row's kind and section, the rows rebuilt at once and build() putting the flyout-local .romp-acted pulse (off on animationend) and a tooltip note in the new row's own words (the pin row's sentence for the group the tab is in now, a Move to or + row's label) on the new row of the same kind and section (the one pin row by its kind alone) and on its +, nothing written, then clearing the record; a click whose union is gone (the x, the +, the + <name> row) rebuilding the rows at once with no record, the row's removal its answer; the window's scroll listener dismissing the menu only for a scroll whose target the menu does not contain, so the capped flyout's own scroll (a wheel, a click's scroll into view) leaves the menu; the click that opens the flyout focusing the New tag input with preventScroll, so the flyout opens at its top with the session's own rows in view; the flyout's held, Move to, + and Show when folded rows carrying their text as titles; the Tags flyout's New tag input built once and never moved, the rows rebuilt in front of it; moveUnion aiming the copy at the destination and aimAdd aiming it at the copy the menu speaks for at an add, or at the add's tag from no group; seatMenu and the flyout's place, run again by the refresh; dressToggle; the ctxIcon tab glyph), ui/webview/styles.css (the .ctx-sub-capped modifier's structural rule: the row's body takes the spare width, its label and sub-line contribute nothing to the menu's intrinsic width and elide; worn by the Hide tab and Tags rows of the main menu alone; the Tags flyout's per-row caps on its labels and sub-lines; the .ctx-sub-tags cap, max-height calc(100dvh - 8px), overflow-y auto and overscroll-behavior contain, so the flyout scrolls within itself past the visible height, with the scroll listener's exemption named beside it; .ctx-item-sub unchanged and byte-equal to feed.css's), docs/guide.md, docs/reference.md; tests ui/webview/tab-hide.test.ts (the menu door executed over a transpiled showTabMenu: the live click, its refusal on the open menu, the row following the copy through a move, two tags in both orders and drag orders, the copy's tag removed with the one-holder return and the adds, a views arrival re-dressing the open menu and the tag coming back, a rename pushed while it is open in both drag orders, two remote-only sections, the store's own events and the phone flip through render.ts's own listeners and the caps frame through the notifier, a press held across a push and across a same-named tag's new id, the flyout placed again as it grows, the flyout rows' tooltips, the trail's copy, the flyout following the row over a rect model, the phone gate, THE FLYOUT'S ROWS ACT ON THE LIVE UNION A, B, C, C2 and D and round 8's E to H (the id pass under a rename, the name pass on a remote-only union and on a union whose local half went, a tag made again under a new id, Move to's source guard with the carried cue on the rebuilt row and its +, the pin row's rename and its carried cue, the + beside Move to and the + <name> row), round 9's G2 (the pin row's home guard under a one-holder re-home, a two-holder re-home and a same-named tag under a new id), the record cleared by the build that carried the cue and a gone union leaving none, THE MENU'S OWN SCROLL through render.ts's window listener lifted onto the harness window, N6b, the pressHold import pin, the three-rule count on .ctx-sub-tags, the cap and the feed.css twin pinned), ui/webview/tab-hide-browser.test.ts (the menu's width measured with and without the tag-bearing rows in Chromium with Inter and Space Grotesk loaded at 450px, 400px and 383px panes, and in the fallback face; the Tags flyout measured nested in the menu in Chromium and Firefox, its 5-, 19- and 40-character destinations and thirty tags, the thirty-tag flyout's bottom inside the pane, scrolling within itself and its foot reachable once scrolled; render.ts's own showTabMenu and its window listeners with real pointer input in both engines: a push mid-press on the Hide tab, Move to and Show when folded rows, the New tag input's caret, selection and focus through a rebuild, and at thirty tags the click-open leaving the flyout at its top with the session's own row in view and the input focused, the flyout's own scroll leaving the menu standing while a scroll outside it dismisses), ui/webview/tab-groups.test.ts, ui/webview/tab-tags.test.ts, ui/webview/views-writes.test.ts, ui/webview/tab-tags-flyout.test.ts (the Tags row's source anchor re-aimed to the ctx-sub-capped class), ui/webview/peek-tab.test.ts, ui/webview/session-views.test.ts, ui/webview/tab-meta.test.ts, vscode-extension/src/tab-menu-flags.test.ts, vscode-extension/src/tabs-first.test.ts
added: 2026-09-09
pr: 435
tier: feature
offered:
closed:
---
Rides the tab-hide-sessions entry (fork PR #380): the store and the strip plan are unchanged, this is a second door to the same entry. It reverses that entry's ruling that the snapshot pane was the one door to a hide: the user asked for the menu door (2026-09-09).
