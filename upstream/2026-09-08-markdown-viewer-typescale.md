---
title: The file viewer renders a note in a document type scale: GitHub heading sizes with rules under h1 and h2, a centred 80ch column at 1.15 times the page size in a sans face in both themes at a 1.5 leading, task boxes without bullets, table header fill, striping and column alignment, kbd, tab-size 4, per-line rows and a Copy button on every fence, six more grammars (rust, go, c, java, sql, toml), a readable code-comment colour, a pane-wide table under the centred column capped at the body's observed width, an anchor map that paints across wrapped code rows, a reload held while a fence's Copy is pressed, and an @media print block that prints a note black on white across pages
status: candidate
where: fork branch mdviewer-s3 (ui/webview/styles.css, ui/webview/feed.css, ui/webview/files-pane.css, ui/webview/file-view.ts (mdBlock, the fence pass and its copy sources, the fetch landing under pressHold when its answer stands, the clamped seat in both viewers, the width observer's --fv-body-w), ui/webview/code-block.ts (new; wrapCodeLines writes --ln-digits, Copy acts on the Space keydown and acknowledges on the button on screen at the fence), ui/webview/viewer-grammars.ts (new), ui/webview/fence-source.ts (new), ui/webview/actions.ts (pressHold), ui/webview/highlight-cache.ts, ui/webview/render.ts, ui/webview/anchor-map.ts (the code runs, the `.code-copy` skip), ui/webview/reader-place.ts (seatPlaceOutcome, the code rows read, the 1px top-block threshold), ui/webview/file-comments.ts (styleFrame's comment), ui/webview/file-view-links.ts (LINE_UNITS gains .cl; fork-only, from #347), ui/CLAUDE.md, docs/guide.md, plans/markdown-viewer.md; tests: code-block.test.ts, file-view-fence-source.test.ts, file-view-copy-held.test.ts, anchor-map-wrapped-code.test.ts, anchor-map-wrapped-code-browser.test.ts (over the new fixture ui/webview/anchor-map-fixtures/fenced.md), tests/test_guide_files_column_width.py, the legs file-view-typescale-browser, file-view-print-browser, file-view-print-marks-browser, file-view-print-inks-browser, file-view-scrollbar-browser, file-view-fence-rows-browser, file-view-fence-blank-rows-browser, file-view-prose-leading-browser, file-view-copy-source-browser, file-view-copy-map-browser, file-view-copy-held-browser, file-view-copy-space-browser, file-view-copy-ack-browser, file-view-landing-throw-browser, file-view-landing-order-browser and file-view-url-place-bottom-browser over ui/webview/real-viewer-leg.ts, and the re-pinned suites)
added: 2026-09-08
pr: 407
tier: feature
offered:
closed:
---
Slice 3 of plans/markdown-viewer.md (decisions 3, 4 and 5 ruled 2026-09-07); the build note under the slice records where the code departs from the plan text. Depends on Slices 1 and 2 (fork PR #390, 2026-09-07-markdown-viewer-sanitizer, and fork PR #396, 2026-09-08-markdown-viewer-place) and on fork PR #348 (2026-09-07-markdown-viewer-text-size, a candidate, not offered): the prose size is `calc(var(--fs) * 1.15 * var(--fv-scale, 1))`, so the column decision 4 rules scales with #348's text-size control; the typescale leg presses that control (`button[aria-label="Larger text"]`) and waits for `--fv-scale` to read 1.15 before it measures the headings and the column at 115 percent; and the slice removes #348's 860px block cap and the media cap that scaled with it and rewrites the pins in #348's own file-view-text-size.test.ts. A Slice 2 landing does not carry #348 with it (that entry allows a port that drops its two #348 seats), so this one names it. The sheets alone fall back to 100 percent without it (`var(--fv-scale, 1)`), and a port over upstream's pre-#348 `.fileview-md` writes the new rule in place of upstream's and has no 860px cap to remove; the leg's two stepUp calls have no button to press there.

Fork-only pieces, each in a file upstream does not ship: `.cl` in file-view-links.ts's LINE_UNITS (fork PR #347, 2026-09-07-file-viewer-links; the hunk is dropped, or travels with #347); the code-run helpers and the `.code-copy` skip in anchor-map.ts (the Copy button's label is not the note's text) and the code-line reads in reader-place.ts, with anchor-map-wrapped-code.test.ts, its browser leg, file-view-copy-map-browser.test.ts and anchor-map-fixtures/fenced.md (the file-comments candidate, 2026-09-07-file-comments-tracked-changes, and Slice 2; a tree without the anchor map needs none of them); AUTO_LANGUAGES in highlight-cache.ts (fork PR #271, 2026-09-07-browser-round2-cuts; upstream's render.ts calls `hljs.highlightAuto(raw)` itself, and since it imports file-view.ts the six grammars reach its core too, so the port passes the ten names to that call to keep the chat's auto-detection where it is); the print block in files-pane.css and the guide's Files section (the Files pane, 2026-09-03-files-pane, approved, not offered; upstream's guide covers the viewer under its markdown-document paragraph). The rest ports as it is: the sheet rules in styles.css and feed.css (every token they read that is not new here, `--overlay-05`, `--kbd-bg`, `--kbd-border`, `--hl-cmt`, is defined in upstream's sheets; `--font-doc` and `--color-scheme` are new, and `--fv-body-w`, the body's content width the pane-wide table's cap reads, is no sheet's token: file-view.ts's width observer writes it on the body, so it travels with file-view.ts), code-block.ts, viewer-grammars.ts and fence-source.ts (new; fence-source.ts imports nothing and reads each fence's text back from marked's view of the note, so Copy pastes tabs as tabs; code-block.ts writes the Copied acknowledgement to the button on screen of the fence with the pressed fence's source, the text its Copy copies, since a rebuild can swap the pressed one out before the clipboard answers, and a write that rewrote or removed the fence acknowledges nothing), mdBlock's fence pass with its copy sources and its task stamp, render.ts importing the shared module in place of its own copies, reader-place.ts's `seatPlaceOutcome` and the seat openFileView and openUrlView keep while the body stands where a clamp left it (Slice 2's module, so a tree with Slice 2 takes them), and the press hold: actions.ts, upstream's file, gains `pressHold` (a rebuild held while the primary button is pressed over a surface and run at the release, on the press's own release events; its `defer` returns a promise that settles with the run, so a parked landing's throw reaches the fetch's catch), and file-view.ts's fetch landing and its failure path run through it when the answer stands (its viewer connected, no newer fetch out), so a reload that lands while a fence's Copy is pressed waits for the release instead of dropping the click (a pressed node removed before the mouseup dispatches no click at all) and an overtaken answer never displaces a newer parked landing. The landing that arrives under no gesture of the reader's is fork-only (the Comments panel's poll after a session's write), so upstream the hold waits on nothing until such a landing exists; the helper and the two wraps travel with file-view.ts as one text, or a port drops both hunks together. Of the tests, file-view.test.ts, fileview-parity.test.ts, theme-parity.test.ts, codeblock-copy.test.ts and codeblock-wrap.test.ts are upstream's and carry the pins; code-block.test.ts is new and travels once its highlight-cache import and its file-view-links and anchor-map pins are trimmed; file-view-fence-source.test.ts (the real marked and fence-source.ts alone) and file-view-copy-held.test.ts (the helper alone, with a source pin on actions.ts) are new and travel with their modules; the sixteen legs over Slice 2's real-viewer-leg.ts and its Comments aside (file-view-typescale-browser, file-view-print-browser, file-view-print-marks-browser, file-view-print-inks-browser, file-view-scrollbar-browser, file-view-fence-rows-browser, file-view-fence-blank-rows-browser, file-view-prose-leading-browser, file-view-copy-source-browser, file-view-copy-map-browser, file-view-copy-held-browser, file-view-copy-space-browser, file-view-copy-ack-browser, file-view-landing-throw-browser, file-view-landing-order-browser and file-view-url-place-bottom-browser), tests/test_guide_files_column_width.py (the guide's Files section), file-view-text-size.test.ts, styles-fc-region-layer.test.ts, file-comments.test.ts and the other re-pinned fork suites stay behind.
