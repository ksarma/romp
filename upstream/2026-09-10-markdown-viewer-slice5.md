---
title: Markdown viewer Slice 5: comments anchor on real notes (the flattened walk over an unclosed html wrapper and the reader's place inside it, code quotes read raw and table quotes across cells in the Rendered paint, the Raw offer at a formula, refusals in the person's terms, the first obstacle named, overlapping highlights as one wash with a click opening every covering card, the sequential hint for positionless comments, the Comment offer on a keyboard selection, and the unpaint normalized once per parent)
status: candidate
where: fork branch `mdviewer-s5`, on Slice 4's merge (5bae9a67, fork PR #707) and merged with origin/main 213fde5fa (the file-review tie-break slice, fork PR #748, whose confirmed copy leads the paint's hint): ui/webview/anchor-map.ts (topTags, the tag scan on the Walked row; analyzeRendered, fits and runFits, the flattened pairing with the two-block confirmation and the tag test; locate and formulaExtra descending to the nested block; shapeOf over the spliced nodes; renderedBlockWrappers, new; renderedBlocks with TextEmitter, lenientBlocks, lenientInline, lenientHtml and lenientRow, the paint fallback's reading of the scope through marked's tokens, and hayRuns; refusalNoun and NOT_HANDLED; the one-pass obstacle loop in mapRenderedSelection; from the review's round 1: runFits reading past a wrapper's leftover to the next html block's element, inlineEnds and the step over the parser's minted `<p>`, formulaEnd in the obstacle pass, blockTopOf with precedes and nextAfter in unitsUnder and wrapBetween; from the review's round 2: blockEnds and the wrapper chain on the Walked row with Block.minted and Block.htext, htmlText, topTags' stray end tags and the blank closing-tag block, pastMinted shared by runFits and the pairing loop, tagNameOf reading the regions layer's imgwrap span as its picture, the paint fallback widening to the whole text when the scope holds no occurrence, formulaHole and formulaSpan, and formulaBeside with the covered formulas in mapRenderedSelection; from the review's round 3: renderedBlocks (the pipeline as the fallback's needle, the strip retired), DROPPED_CONTENT, mappedSlice, buildSourceTable, renderedQuote (exported for the corpus test), the fallback scope less the block's wrappers and the widening on a count disagreement, P_CLOSERS and FORMATTING for Block.minted, Block.pOpen and Block.nested with topTags' pOpen, TopTag.kids and pastKids, pastHoisted, nextAnchor and handedTo, locate placing an end boundary by its last selected character, and formulaBeside stopping at the other boundary; from the review's round 4: CLOSES_P as the parser's whole p-in-button-scope set with P_SCOPE_BARRIERS and the button scope in walkedBlocks, the stray closer closing the open `<p>`, TopTag.kids at every depth with kidsAt and the depth-1 wrapper's scan, pastKids' text run, holdsContent and blankMark, nextAnchor's bound at a block of closing tags alone, FOREIGN, the character references (NAMED_ENTITIES as HTML 4.01's names, LEGACY_ENTITY_NAMES, C1_TO_UNICODE, numericRefText, namedRefText, charRefAt, decodeHtmlText), lenientInline's RCDATA read through marked's Parser.parseInline, lenientHtml's blank at a table part's end, and DROPPED_CONTENT's foreignObject; from the review's round 5: the depth-1 wrapper taken as the node at k, runFits' toEnd and nextAnchor reading past an html block whose element the content lacks, TopTag.kids as Kid entries carrying a text run's text with pastKids bounded by it, NAMED_ENTITIES on a null prototype read through hasOwn, domRefText with HTML5_LEGACY_HEADED in namedRefText, ForeignRoot and closeForeign shared by lenientInline and lenientHtml, dropsContent with DROPPED_CONTENT's TITLE, fileKindWalk over an inline RCDATA element's tokens, and nextIsTablePart for lenientHtml's blank; from the review's round 6: topTags flattening an unwrapped element's kids into its parent's with a textarea's content as a text kid, nextAnchor's candidate confirmed by the blocks after it through runFits with toEnd past scanTake, runFits' toEnd run pairing a mismatch in the confirmed tail with its node, nextIsTablePart looking past a raw-text element, an input, a form and any foster-parented element between two parts, and ForeignRoot.open with openInForeign, openInForeignObject and closeInForeign for an HTML element left open inside a foreignObject; from the review's round 7: startTagCloses with IMPLIED_END, SCOPE_BARRIERS and LIST_ITEM_STOPS, the parser's implied end tags at a start tag read by topTags and inside a foreign root's integration point, topTags' closeInner and closeDownTo listing every element still open above an end tag's element with the `</form>` rule, runFits' spent lookahead reading one unconfirmed mismatch before two confirmations and confirming nextAnchor's candidate, ForeignRoot.ip with integrationPoint and MATHML_TEXT for every integration point of a root, leafTag for a `/>` read as closing an element in foreign content alone with inlineTag's selfClosing, openInIntegrationPoint in place of openInForeignObject with openInsideForeign and closeInsideForeign, nextIsTablePart's drop stack and foreign counter past a dropped element's text, and IMPLIED_TABLE_CLOSES with lenientHtml's table frames for the blank where a part's start tag closes an open part), ui/webview/reader-place.ts (readRendered, the descent into a wrapper; from the review's round 1: boxOf by checkVisibility, closesAlone and the Raw read of a closing tag's row as the block after it; from the review's round 2: closesAlone over a run of closing tags, isCommentBlock over the pairing's commentsOnly (exported from anchor-map.ts), opensWrapper, readsAsNext and nextShown (a wrapper's own rows and a comment's read as the block after them), foldDepths and foldStands with Place.after (a Raw row inside a details carrying the blocks after the fold, the seat standing on the first shown); from the review's round 3: shownFoldOf and shutFoldBefore (a seat from Raw no lower than a shut fold's summary at the edge), Place.pic with picturesOf, pictureOf, imgsIn, imgLine, imgIndexBefore and picBox (a wrapper's own picture seated at the same fraction of its height in both directions); from the review's round 4: Place.row (a reflow of the same Raw text keeps the top row), Place.pic carried from a row of the block's own before the tag row and seated below the edge at its distance, and shutFoldBefore leaving an open fold's summary alone; from the review's round 5: Place.lead (the shown row of a wrapper's block's own at the edge, put back on a same-view Rendered reflow), the Raw pic carry through the run of blocks the row reads past with the second opensWrapper call gone, atEdge for the picture and the row at the edge in readRendered, ownRows and picturesOf as the one definition of a block's pictures for both reads (pictureOf retired), IMG_TAG skipping html comments in imgLine and imgIndexBefore, and the picture seated from a shut fold's closing row capped at the fold's summary; from the review's round 6: pictureLines and linePictures with picBox as the line's pictures' union, one box to both reads, picOutranksRow, the picture over the row on a same-view Rendered reflow, and foldStands' empty list with the seat's summary fallback for a fold with nothing standing after it; from the review's round 7: rowOfLine, lineBoxes and lineBoxesAt in place of picBox, the row holding the line's pictures beside text of the note's as the box one Raw row inverts, Pic.imgs as the pictures' own box beside it, rowPartsTop seating such a row by its parts on a same-view reflow, picOutranksRow gated to a picture at or above the row's own top, and ROW_SHOWN, the seat into Raw leaving the picture's row two pixels below the edge), ui/webview/file-comments.ts (openCovering in the fcopen handler; anchorKey, lastLocated and nextCopyHint in paintAll; onSelectionChange, pointerHeld and offeredFor with the selectionchange listener installed after the seam's hook and removed at dispose, offeredFor re-read after every paint by afterPaint, and hintedCopies for the words of a hinted copy; unwrapMarks, new; from the review's round 2: passageGone, read by onSelectionChange and by afterPaint, which hides a passage's float the writes left beside no selection, and pressBegan for the primary button alone with pressEnded on the document's contextmenu and the window's blur; from the review's round 4: the passage-gone hide in onSelectionChange dropping offeredFor; from the review's round 5: afterPaint hiding a passage's float the writes left beside a remnant with no box (floatSubjectRect); from the review's round 6: subjectHeld, the scroll listener's pixel test as one method, read by afterPaint too, which hides a passage's float the writes left beside a remnant with a box that moved from under the button; from the review's round 7: afterPaint offering the float again at the box of a selection the writes moved whole, the same text between two ends in the body as the seam reads a selection its reflow left standing, where the remnant's rule had hidden it), ui/webview/md-sanitize.ts (dropCommentChildren on the uponSanitizeElement hook, the review's round 4; dropBodyTitle beside it on the same hook, a body `<title>` dropped with its content and an svg's kept, the review's round 5), ui/webview/code-block.ts (fallbackCopy putting the selection's ranges and the focus back once the textarea's copy has run, the review's round 6, pre-existing on main; its round 7: the selection's two ends put back with setBaseAndExtent, restoreSelection, the direction kept, the Ranges the route for an engine without the method, a multi-range selection or a refused put-back), ui/webview/file-view.ts (the seam comment at onSelection), ui/webview/styles.css and ui/webview/feed.css (the `.fc-hl .fc-hl` rule, byte-equal); fixtures ui/webview/anchor-map-fixtures/wrappers-plain.md, wrappers-2block.md, wrappers-unclosed.md (new) and blank-scenes.json (its note); tests ui/webview/anchor-map-wrappers.test.ts and anchor-map-wrappers-browser.test.ts (new), anchor-map-html-text.test.ts (new, the review's round 4), anchor-map-html-rules.test.ts, anchor-map-dom-rules-browser.test.ts and anchor-map-html-text-browser.test.ts (new, the review's round 5), anchor-map-pairing-r6.test.ts (new, the review's round 6; the same round's two cases in anchor-map-html-rules.test.ts, three shapes and the normalized needle compare in anchor-map-html-text-browser.test.ts, the `<p>` block scene in anchor-map-wrappers.test.ts and two scenes in anchor-map-wrappers-browser.test.ts; the review's round 7: its tests 5 and 6, anchor-map-html-rules.test.ts's tests 9 to 13, sixteen shapes and two recorded entries in anchor-map-html-text-browser.test.ts and five scenes in anchor-map-wrappers-browser.test.ts), anchor-map-fallback-markup.test.ts, anchor-map-code-table-paint-browser.test.ts (new), anchor-map-obsidian.test.ts, anchor-map-change-marks.test.ts (its count-guard scene moved off an attribute's text in the review's round 2 and onto a rendering that gained a copy of the passage in its round 3, the entity scene becoming the positive pin), md-config-math-map-browser.test.ts, md-config-paint-trim-browser.test.ts (the REFUSED confinement lifted), file-view-place-blocks.test.ts, file-view-place-html-browser.test.ts, file-view-place-wrapper-end-browser.test.ts, file-view-place-edits-browser.test.ts, file-view-place-closed-details.test.ts and file-view-place-closed-details-browser.test.ts (new, the review's round 1), file-view-place-picture-line-browser.test.ts and file-view-place-last-fold-browser.test.ts (new, the review's round 6, which re-aimed file-view-place-closed-details.test.ts's tests 4 and 11; the review's round 7: picture-line tests 3 to 5, file-view-place-closed-details.test.ts's test 12 re-pinned and test 13 new, and file-view-place-html-browser.test.ts's test 13 re-pinned), file-comments-overlap.test.ts and file-comments-overlap-browser.test.ts (new), file-comments-hint-order.test.ts (new), file-comments-keyboard-offer.test.ts and file-comments-keyboard-offer-browser.test.ts (new), file-comments-paint-offer.test.ts and file-comments-paint-offer-browser.test.ts (new, the review's round 1; its round 7: node test 7 and browser leg 5, the selection the paint moved whole), code-block-copy-keeps-selection.test.ts and file-comments-copy-fallback-browser.test.ts (new, the review's round 6; its round 7: three node tests and a second leg over a selection made right to left), file-view-place.test.ts (its hideFloatOnScroll pin moved, the review's round 6), file-comments-behavior.test.ts, file-comments-regions.test.ts and file-comments-reveal-landing.test.ts (pins on paintAll's shape moved), file-comments-about-review2.test.ts and file-comments-markclick-controls.test.ts (their selection fakes read once), file-comments-unpaint-normalize.test.ts (new), file-comments-anchors.test.ts, fileview-parity.test.ts, md-config-paint-whitespace-browser.test.ts, md-sanitize.test.ts (the hook count and the comment drop's unit test, the review's round 4; the body title's, its round 5) and md-sanitize-comments-browser.test.ts (new, the review's round 4), tools/file-review-plan.test.mjs, tools/file-review-plan-anchors-states.test.mjs, tools/file-review-plan-markclick.test.mjs (pins moved), tools/file-review-plan-tiebreak-review-2.test.mjs, tools/file-review-plan-tiebreak-review-4.test.mjs and ui/webview/file-comments-tiebreak-touch.test.ts (their PanelCard and WordedCard pins re-aimed in that merge to carry hintedCopy), tools/file-comments-host-anchors.test.mjs and tests/test_file_comments_e2e.py (two guards each on the stored quote of a code line and a table row), tests/test_guide_files_keyboard_overlap_fold.py (new); docs/guide.md (its Comments paragraph's three clauses: the button following a keyboard selection, two comments over one text as one highlight with a click on the overlap opening both cards, and a closed fold opened on the way to a comment inside it; from the review's round 4: its HTML paragraph's sentence, an HTML comment dropped and the text around it kept; from its round 5: the same paragraph's sentence, a `<title>` dropped with its text; from its round 6: that sentence's svg clause, an inline svg's `<title>` kept), plans/markdown-viewer.md, plans/file-review.md (decision 51's sentence on the hint order, from that merge)
added: 2026-09-10
pr:
tier: feature
offered:
closed:
---
Upstream ships the same viewer, the same Comments panel and the same anchor map, so every item applies there once the slices before it have landed: a note whose markdown a browser nests inside an unclosed `<div align="center">` or `<details>` (a README's centred header, an Obsidian fold) becomes commentable after and inside the wrapper and the reader keeps their place through it; a comment on a code line or across two table cells saved from Raw paints in Rendered; a formula touched from Rendered hands the Raw view the formula itself; no refusal names a lexer token; overlapping comments read as one highlight and a click reaches every card under it; comments written by the CLI on a repeated passage take the copies in turn; a keyboard selection offers Comment; and unwrapping the marks of a long paragraph no longer costs the square of its links. Built as Slice 5 of plans/markdown-viewer.md on Slice 4's merge (fork PR #707, 2026-09-09-markdown-viewer-mdconfig), so the offer stacks on the four slices before it, each with its own entry (Slice 1 #390, Slice 2 #396, Slice 3 #407, Slice 4 #707). The files it changes are the ones those entries already name as fork-only candidates or as ported with them: anchor-map.ts and file-comments.ts travel with the file-comments candidate (2026-09-07-file-comments-tracked-changes) and its host tools, reader-place.ts with Slice 2, the sheets' `.fc-hl` region with the panel's CSS block, and the fixtures and tests with their modules; the guide and the plan port as they are. Nothing here reaches the kernel, the store format or the vendored CLIs: the stored quote stays the exact source slice (the paint-time reading of the plan's "strip cell delimiters" was the ruling of 2026-09-09), so the two host guards and the two e2e cases are regression guards on a contract upstream's host already keeps.
