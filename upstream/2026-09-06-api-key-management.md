---
title: API-key management as one model: romp never WRITES a key to a file and its docs keep `service.env` key-free; the kernel obtains credentials at launch (the manager's environment, the configured command of command mode, or Claude Code's `apiKeyHelper` inside the CLI), holds them in one private in-memory set, injects per child only where the Billing pick is the key, rotates by NAME (`romp keyswap <name>` writes a one-token selector) and reconnects by fingerprint; file mode still reads a `service.env` key line where an installation keeps one (`kernel/keysource.py` byte-identical), and the boot verdict/warning ring on such a line under a declaration. The pieces: the key-free command mode (`kernel/envsource.py`, the boot `key_source_verdict`, `romp-service status`), the catalog fetch's `ANTHROPIC_LP_API_KEY` rung, the every-`ANTHROPIC_*` strip on Codex judge children, the declaration-gated env-file credential warning, the file-mode refusal of the named swap, the report redaction in the test suite, and docs that keep `service.env` key-free
status: approved
where: fork PR #229 (branch `keyfree`, merge `988d010f`, 2026-09-06), fold commits `78a05f07` / `0c621352` (refusal, warning, runtime strings), `19dd4168` (catalog rung), PR #192 (docs), PR #145 (`apihealth`, the auth-label side); the per-piece rows (the key-free row: candidate; the keyswap-refusal row: divergence; the API-health row: candidate) hang off this one
added: 2026-09-06
pr: 229
tier: major-feature
offered:
closed:
---
(the user 2026-09-06: the third bigger bucket, named for a maintainer conversation.) Upstream's model is the other one: the key is a `service.env` line the kernel reads live and `romp keyswap <name>` rewrites from a sibling file (their `b4ca13e7` / `d177a8a8`, PR #921, and the maintainer's review fold `60051794`, PR #925, all merged 2026-09-04), optimised for no external tooling, a login service that runs without a shell, and a restart-free swap verified by fingerprint. Their open PR #932 (another contributor, 2026-09-05; unreviewed, and CONFLICTING against their main as of 2026-09-07) adds a runtime key source bound to one secrets-manager CLI through a reference string, fail-closed with no login fallback, `keyswap` selecting the reference through a `select_source` selector (`write_key` kept for compatibility), the secrets manager's own service-account token kept IN `service.env` and claimed out of the kernel's environment at boot (scrubbed from the tmux server and judge children), and a Codex bypass so a Codex judge call resolves no Anthropic credential; it and #229 fill the same slot and rewire the same functions (`work_api_key`, `cycle_key`, `_judge_env`, `_models_api_credential`, `cli/keyswap.py`), so they cannot both land as-is. The conversation settles: which second source upstream wants (a generic command or a product reference); keep-last-good versus fail-closed; whether the second source's own bootstrap credential may sit in `service.env` (#932) or must come from outside any romp-read file (#229), since the fork's boot verdict rings on a `*_TOKEN` line there; whether a Codex-engine judge call resolves the second source at all (#932 bypasses it, the fork's command mode does not); how a helper-billed session converges after a rotation (upstream's `cycle_key` skips it by design, an always-reconnect arm — `78a05f07`, removed by #229 — broke `60051794`'s convergence rule, and a helper fingerprint exists only in envsource); the `ROMP_CREDENTIAL_*` / selector / `ANTHROPIC_LP_API_KEY` names; the `/keycycle` and `romp-service status` contract additions; and whether the kernel may read the unit, drop-ins and plist for the boot verdict. The `ANTHROPIC_LP_API_KEY` rung is listed here although the model-pickers row's notes call the catalog's credential path FORK POLICY (not for the offer): #932 already deletes the ambient `ANTHROPIC_API_KEY` fallback in `_models_api_credential` on the same hunk, so only the rung's name is new, it is sequenced behind #932's fate rather than excluded, and whenever it travels it carries the widened Codex strip (or an LP key in `service.env` would reach another vendor's judge child). Not offerable: the file-mode refusal and the no-keys docs stance (policy; an offer keeps upstream's file write path). Offerable now without the discussion: the report redaction as an extraction against upstream's 142-line conftest, its length after their #944 (tier 0), the API-health signal (tier 2), the declaration-gated warning with the `=login` arm as the sure part and `=key` as a body design point (tier 2), and the every-`ANTHROPIC_*` Codex strip (tier 1).

Status detail (migrated from the table): candidate — **major-feature (tier 3)**, discuss with the maintainer first

APPROVED 2026-09-07 as an RFC: the fork owner approved offering the API-key model upstream as an RFC with a user-facing reference section, stacked on its tier 0-2 pieces, each now its own approved entry (`test-report-env-redaction`, `codex-judge-anthropic-env-strip`, `env-file-credential-warning`, `api-health-signal`). The keyswap refusal itself stays home (`keyswap-refused-file-mode`, divergence). Pointer fixes today: upstream #932 reads CONFLICTING (still open); upstream's `tests/conftest.py` is 142 lines after #944, not 113.
