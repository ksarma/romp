---
title: Readers that cannot parse skip the row and say so: the notices rev, the quarantine directory, the light-theme held-mail border
status: candidate
where: kernel/kernel.py (upstream: _note_read_fault_once, _FaultFold, _fault_fold_bell, _sync_notice, _sync_notice_standing, _end_notice_file_episodes, _HDR_BREAK_RE, _notice_sid_text, _notice_fault_text, _bell_fit, _UTF8_BOM, _NOTICE_ROW_FIELDS_INT, _NOTICE_BAD_ROW_SAID, _NOTICE_ROWS_FOLD_HEAD, _notice_row_bad_field, _notice_parse_rows, _notice_rows, _notice_rows_or_fault, _notice_rows_unlocked, _notice_projection, _notice_cards, _notice_action_run, post_notice, expire_notice, _compact_notices, _note_notice_dir_fault, _note_notice_file_fault, _stat_key, _stat_key_from, _LANDING_REMOTES_JS; fork-only: _held_records, _HoldUndecidable, _corrupt_aside_name, _hold_aside_name, _say_hold_unreadable_once, _end_hold_unreadable_episodes, _HOLD_UNREADABLE_SAID, _say_hold_asides, _end_hold_aside_episodes, _HOLD_ASIDE_SAID, _HOLD_ASIDE_LOCK, _HOLD_ASIDE_FOLD_HEAD, _HOLD_ASIDE_FOLD_HEAD_ID, _HOLD_ASIDE_TAIL, _HOLD_ASIDE_TAIL_ID, _hold_bell_text, _HOLD_NAME_FIT, _hold_name_text, _hold_text, _note_hold_dir_fault, _quarantine_cards, _CLEARED_NO_LEDGER, _CLEARED_NO_SESSION, _clear_all, _cleared_undoable, _undo_clear, _subtree_item_ids, build_feed, _feed_off_frame, Handler._dispatch_ws), postal/postal_service.py (upstream: _json_files, _listed_name, _say_unlistable_once, _listable_again, _UNLISTABLE_SAID, _say_unreadable_once, _UNREADABLE_SAID, _unread_fold, _say_unread_records, _end_unreadable_episodes, _list_json_records, _mail_unreadable, _recall, _hold_text, _hold_rows, _hold_fault_row, _hold_wire, _WIRE_SCALARS, _hold_sort_at, _held_records_bus, _held_unread_text, _HOLD_SKIPPED_SAID, _say_hold_skipped_once, QuarantineUnreadable, HeldRecordUnreadable, quarantine_list, quarantine_get, quarantine_decide, Handler), ui/webview/feed.css (upstream: .fdismiss.fq-ok, revealPulse, #feed-foot, #feed-search, #feed-clearall; fork-only: .feed-toast), ui/webview/styles.css (upstream: .staged-chip), ui/webview/button-vocab.test.ts (upstream: WEBVIEW, SHEETS, ANCHORS, rulesOf, darkAccentLiterals, withoutFallbacks, mixedAccentRules, DARK_ACCENT, ACCENT_TOKEN), ui/webview/feed.ts (fork-only: clearable, clearAllOffered, boardCardsUnknown, feedOff, clearAllFold, makeClearAllBtn, renderBody, applyFeedPayload, clearAllResult), ui/webview/federation.ts (fork-only: prefixInbound), ui/webview/render.ts (fork-only: dropCitationsAll), ui/webview/composer-citation.test.ts (fork-only: dropCitationsAll), ui/webview/feed-clear-all-offer.test.ts (fork-only: clearAllOffered, clearAllResult, feedOff, #feed-search), ui/webview/feed-render-incremental.test.ts (fork-only: clearAllResult, feedOff), ui/webview/setting-stale.test.ts (fork-only: clearAllResult), ui/webview/feed-card-prefs.test.ts (upstream: #feed-clearall), ui/webview/feed-css-footer.test.ts (upstream: #feed-foot), docs/reference.md (upstream: the mail paragraph, the state-files paragraph; both: the held-mail clause), tests/test_held_mail_readers.py (upstream: NoticeRowTypeFault, _writer_rows; fork-only: ClearAllLeavesHolds, HeldMailReader, _forget_said_once, _qcards; both: _Root), tests/test_held_mail_reader_guards.py (upstream: NoticeKeyNotText, SweepArchivesSkippedLines, _writer_rows; fork-only: HoldIdsNeverJournaled, UnreadableHoldRingsTheBell, BellRowFits, OffFrameCountsPassOverHoldRows, TheHarnessLeavesNoEpisodeBehind; both: NonFiniteNumbers, _Case, _Root), tests/test_held_mail_reader_guards_r2.py (upstream: NoticeLineNotUtf8, NoticeFileWithABom, NoticeRowsSplitOnEveryLineBoundary, NoticeStoreUnreadableIsSaid, WriterSideUnreadableFileIsSaidOnce, TypeWrongRowKeyNamedOnlyWhenValid; fork-only: HeldRecordFaultsWidened, HoldFieldsOfAnotherTypeAreNamedNeverFormatted, SaidOncePruneReadsASnapshot, UnreadableEpisodeEndsWithTheDirectory, OlderLedgerHoldRowsAreNoBatch; both: _R2Case), tests/test_held_mail_manager_r1.py (upstream: NoticeRowFaultsReachTheBell, WriterRefusesOverAnUnreadableFile, TheActionDoorTellsAFaultFromAnAbsence, EpisodesEndOnASuccessfulRead, AHoldersFaultRowReachesTheViewingPanel, RowFaultsFoldForTheListing; fork-only: ReadFaultLeavesTheFileInPlace, ANonTextBodyKeepsItsCard, AnIdTheBusCannotDecideNeverBecomesACard, ClearAllAnswersTruthfully, ATypeWrongAtKeepsItsCard, TheUnmovableArmNamesTheClassItRefusedFor; both: ADirectoryFaultIsSaidOnceForTheDirectory, AFileNameForgesNoLogOrBellLine, _MCase), tests/test_held_mail_manager_r1b.py (upstream: FourReadersRingAlike; fork-only: AMovedAsideRecordOutlivesTheRing, PlaceholdersTakeNoLedger, OverlappingListingsSayTheAsideOnce, _minted_families, LEDGER_HONOURING; both: _MCase), tests/test_kernel_trust.py (fork-only: QuarantineCards, _qcards), tests/test_kernel_citation.py (fork-only: ClearDropsCitation), tests/test_task_tracking_switch.py (fork-only: TheClearAllOp), tests/test_postal_quarantine.py (upstream: _HeldStore, HeldMailStoreUnlistable, TheStoresAreListedNeverGlobbed, HeldMailRecordsThatCannotBeParsed, HeldRecordOnDiskIsNeverReportedAbsent, HeldBodyThatIsNotTextKeepsItsHold, HoldSummaryFieldsAreNamedNeverFormatted, HeldRecordsUnderAFalseIdAreNeverServed, ListedNamesForgeNoLogLine, ADirectoryFaultIsSaidOnceForTheDirectory, AHolderWhoseStoreCannotBeListedSaysSoOnTheWire, RecallOfAParkedRecordInAStoreThatCannotBeListed, SkippedSaidPruneReadsASnapshot, ALoneUnreadableRecordRefusesTheStoreNeverNothingHeld, AnApproveOfAToIdThatIsNotTextIsRefused, ADenyWithANoteOverASenderThatIsNotTextIsRefused, TheRouteNamesEveryNonScalarFieldByType, TheBusCitesTheStatementAndRestatesNothing), tests/test_postal_peers.py (upstream: StoreFaultsAreLoud, StoreDirectoriesThatCannotBeListedAreLoud, AHoldersUnlistableStoreReachesThePeerAsAFaultRow, ARootRunSkipsBeforeTheFixturePatches), tests/test_reference_held_mail.py (both: TheHeldMailClauseStatesTheRuleAndCitesIt), upstream/2026-09-19-heldmail-readers.md (fork-only: this entry)
added: 2026-09-19
pr: 864
tier: fix
offered:
closed:
---
One rule at every site: a reader that cannot parse a row must not report the store absent and must not take down callers that had nothing to do with the bad row; it skips the row, names the session and the key in the log, and keeps the board. Upstream-facing part: one type-wrong rev, t or expiresAt in one row of one session's notices/<sid>.jsonl raised ValueError out of every feed build (the push cycle and GET /feed.json alike); the shared parse now skips that row and says it once per episode, naming the session, key, field and type, never the text, on stderr and as one bell row per file per episode. Also upstream: the held-mail Approve button's border reads the accent token through color-mix instead of a dark-accent literal, so the light theme's text and edge share a hue. Fork-only (upstream deleted the kernel's quarantine cards; the fork keeps them): a hold's card stands through Clear-all because the reader takes no cleared ledger and _clear_all declines a hold's id at the one write every door reaches, and the directory reader is the bus's _list_json_records shape ported (a record it read and cannot parse moves aside once and is said; a record it cannot read is skipped, said once and left in place for the build that can read it; a directory it cannot list is said once per episode; the board is kept). The bus's own readers of its stores take the same rule, and those apply upstream (upstream keeps the bus's held-mail store, its readers and the GET /quarantine route): a directory the bus cannot list is one bell row per episode for an outbox or readbox with the parked mail left standing, a 503 with the reason from GET /quarantine instead of an empty list, and the listing is os.listdir, never Path.glob, which on Python 3.12 swallowed the PermissionError (the recall's outbox arm included). A held record the bus cannot read or parse, or that is not an object, is skipped and said once in the log with the file left in place for the kernel's reader, a type-wrong `at` sorts as the oldest instead of raising out of the sort, and a decide on a record that is not an object is refused in plain words. The notices reader also reads bytes and skips a line that is not UTF-8 (a whole-file decode raised UnicodeDecodeError out of every build), keeps the first row of a file saved with a BOM, and says a notices directory or file it cannot list, stat or read once per episode instead of drawing a clean board.

The review of 2026-09-20 changed the following, under the one statement of the states a reader of a store meets, the kernel's _note_read_fault_once, which every reader cites and this entry does not restate. Fork-only, the kernel's held-mail reader and the clear door: a hold file the kernel cannot read is skipped and left in place for the next build, said once per file and fault on stderr and the bell, and the move aside is reserved for bytes read and not parsed. A hold whose only fault is a body that is not text keeps its card; its gist names the body's type on the kernel's card and in the bus's summary, Deny and an Approve that supplies edited text work on it, and a bare Approve is refused in plain words with the message still held. A hold whose id the bus would refuse to decide never becomes a card: it is moved aside and named by file, never by body. A held-mail file name is rendered as it is in a stderr line or a bell row only when it clears the bus's id grammar; any other has its line boundaries replaced and is shortened to the bell's width, so a name forges no second line. Clear all's answer is true: the chat drops only the chips of the ids the clear wrote, a press that cleared fewer cards than it asked is answered on the pressing page with the count and how many held messages stayed, and a press that cleared nothing is refused there in plain words. The footer offers Clear all only while a card on the board would take the clear, so a board of held messages alone loses the button, and so does a board of placeholders alone (provisional, awaiting, blocked and user-todo stand-ins), a wider change than the review named and a deliberate one; while a host's cards are unknown on a merged board the button stays offered on a board that has a card, since an absence proves nothing then and a press is answered truthfully; the view menu, the tag lens and the session box keep the card-count gate. The button's tooltip reads: clear the open cards (inbox-zero); Undo restores them. A held message stays until you approve or deny it. Upstream, the notices reader and its sweep: a notice row the parse skips is said on the bell as well as stderr, one row per file per episode naming the session and the keys, never a title or a value's text; every line the parse leaves out, whatever its shape (not JSON, not an object, no op, no key, an op that is not text), is said the same way before the sweep archives it as a skipped row. The writer's reader refuses a notice file it cannot read instead of answering nothing: a post over such a file assigns no revision, an expire retires nothing and says the fault rather than an absence, and the sweep leaves that session's rows live and moves on to the next session. The notice card's action door answers a read fault as a fault (nothing was done, try again) and not as the notice being gone, at both of its reads, so an action never runs twice. A fault shared by a directory's files is one bell row for the directory, with the count and the names that fit, and one stderr line per file; a fault one file has keeps its own row. A said-once episode ends on any successful read of its subject, wherever that read happens: a clean listing, a served memo entry, a store removed and recreated. Upstream, the bus's store readers: quarantine_get and quarantine_decide refuse a held record the bus cannot read or parse (a link with nothing behind it and a document that is null included) instead of answering that no such message is held, and the file is left for the kernel's reader. The bus never serves or gossips a held record under an id that is not its file's name or that it would refuse to decide, and a decide under such a name acts on nothing. A held record's body that is not text is served by GET /quarantine as its type name, with bodyType naming the substitution. An outbox or readbox record the bus cannot read is no longer moved aside with the sender's receipt closed as refused, as it had been since 2026-09-08: it is skipped, said once per file and fault as a bell row and a log line, left in place, and the receipt keeps reading pending; a store of two or more records none of which read is said once for the store with the count, and for the held-mail store that fault refuses quarantine_list, so GET /quarantine answers 503 for it. A recipient's inbox file the bus cannot read is still moved aside as before; that reader was not part of this change. A held-mail store a holding machine cannot list rides the peer exchange as one fault row naming the fault's kind and errno, never a path or a record's text, so the viewing machine's Held for approval elsewhere section names the holder's fault and counts it as no message instead of dropping the holder silently; the viewing kernel files no bell row for another machine's store fault (the holder's own kernel and bus ring the holder's bell, and the exchange payload cannot tell a clean listing from a pruned peer), and a viewing panel older than this build renders the fault row as one message held for approval until that machine updates.

The follow-on to that review, landed on the same branch before its re-review, changed the following. Fork-only, the kernel's held-mail reader and the clear door: a held record the kernel moved aside is said again by the error center each time the row carrying it has left the ring, after a kernel restart or once forty later rows have pushed it out, one row for however many asides stand (the count and the names that fit), until the file is renamed back without its .corrupt- suffix or deleted; the row names the file and what can be done with it, never its contents, and nothing is renamed to say it, so the fact that a record was moved aside outlives the ring. Clear all declines every re-listed placeholder id at the write and Undo passes over their rows at the read, both from one constant, _CLEARED_NO_LEDGER, which names the hold family beside the provisional, awaiting, blocked and user-todo stand-ins (the user-todo family had been missing from the no-session list too, and is a member now), so a press over stand-ins alone journals nothing and the first Undo after it restores the earlier deliberate clear, and a press that cleared cards beside stand-ins is answered on the pressing page naming the stand-ins for what they are, saying approve or deny only of held messages. Upstream, the bus's store readers: the hold summary a holder gossips carries a held record's from, to and origin as text (a container named by its type, a number spelled out) and its at as an integer or 0, so a type-wrong field on the holder reaches every peer's popover as a type name or an integer and never as an object the popover would format as its placeholder; the record stays listed and decidable. Upstream, the sheets: the two remaining rules that named the accent token beside the dark accent written out, the revealPulse glow in feed.css and the .staged-chip dashed edge in styles.css, read the accent through color-mix at their alphas, and the button-vocab census is rule-scoped over every rule of styles.css, feed.css and gear.css (a keyframe taken whole; a literal at alpha 0 or inside a var() fallback is no member), which widens this fix-tier PR by two rules that predate it. The reference's state-files paragraph lists quarantine/ among the postal stores and, since the bus's own reader of that store moves nothing, names the kernel's reader as the mover of a held record it read and could not parse or take, under the same naming, with no receipt changed since the relay was acknowledged when the hold landed, and states that the aside is named again until it is dealt with. The parity of the four readers is pinned: a row fault in a notice file, a notice file fault, a notices directory fault and a held record read and not parsed each file one refused row in one build and none on the next.

The second review round of 2026-09-20, landed on the same branch before its re-review, changed the following. Upstream, the bus's store readers: the held-mail store is refused whenever nothing in it could be served while a listed record could not be read, whatever the count and one record alone included, so GET /quarantine answers 503 with the reason and every viewing machine sees the holder's fault row in place of a vanished section; the refusal names the file, the errno and its text, never the record's contents (until this a lone unreadable record answered nothing held while two answered 503). An Approve of a held record whose toId is not text is refused in plain words naming the type alone, on the bare road and the edited-text road alike, the record untouched and still held and never coerced into a re-match by name. A Deny with a note of a record whose origin or frm is not text is refused in plain words before the note is built, the bare Deny still working; before, a container origin dropped the connection and a container frm parked a malformed note addressed to a container while the deny answered ok, and the note now names a container recipient by its type. GET /quarantine names every non-scalar field of a held record by its type name, recording the substitution as <field>Type beside it (a dict userAsk is served by its type name on that route alone; the decide replays the record whole), so a nested container in any field no longer drops the connection, and the record stays listed and decidable. Upstream, the notices reader and its sweep: the row-level bell row for the lines a notice file's parse skips rides the listing's fold, so one cause spread over many session files is one row with the count and the names that fit instead of a row per file evicting the ring, and a file whose bad rows are folded is not counted as unread; the sweep's memo skip keeps a standing read fault's episode open, so a fault that stands across sweep and build cycles is said once and not once per cycle; and every notices bell row is cut to the bell's width, with a session file's name bounded to 64 characters in every branch. Both daemons: the states a reader of a store meets are stated once, in the kernel's _note_read_fault_once, which says per state which reader owns which disposition; every reader on the kernel cites it and restates nothing (pinned by a case that reds on a restatement, not on the absence of a citation), the bus's walk and its outbox and readbox lister cite it with the bus's own clauses (a test reads the bus's text and reds on a restatement, a numbered state label or a run of the statement's own words), the .json link with nothing behind it is named in the statement as the one labelled exception to the never-rename rule (the kernel moves the link aside by its own name, moving no message bytes; the bus skips it), and an at that int() refuses is a field of the third state: the kernel's card takes the build's clock as it does for an absent at, nothing is moved and nothing said, where the first shape moved the record aside while the bus kept serving it, so a hold the bus served became undecidable after one feed build; the bus sorts such a record as the oldest, the one place the two readers' handling of a field differs. Fork-only, the kernel's held-mail reader and the clear door: the row for a file that could not be moved aside says the class it was refused for (could not be taken for a record that parsed and the bus cannot decide, could not be parsed for bytes the parser or the shape check refused, could not be read for the link), two classes in one listing filing two rows; the standing-aside row keeps the aside's UTC stamp whole, trying the name whole with the advice dropped before shortening the name, so the row names a file the user can find; that advice is worded by class, an aside from a name the bus cannot decide told to give it a decidable name and a matching message id or delete it, a torn record told to rename it back without the suffix; two listings overlapping on a standing aside say it once, the registry's check, the say and the write running under one lock; and the chip drop after a Clear all reads each session's goal store once per press, not once per written id. Fork-only, the feed pane: a Clear all's answer names the machine whose kernel gave it, since on a merged board the press reaches every attached kernel and each answers about its own board; a remote kernel's answer arrives host-stamped through federation as a settings stand-down does and is toasted and kept on the bell as that host's, the local kernel's as this machine's, and one press's answers fold into one toast, a line per machine, keyed on the press and on the toast on screen and never on a clock, so an unqualified nothing-was-cleared is never said for a press that cleared cards on another machine. While Task tracking is off the footer hides Clear all (a press reached a kernel that builds no feed while off, clears nothing and answers nothing) and holds it hidden through a settings change; Undo stays, since the off frame carries canUndoClear and the undo door has no tracking gate, and a built frame offers the button again (the off arm predates this PR, and the kernel's off-frame door still answers such a press with nothing where the pane cannot reach). The footer's right-hand dock rides the session box's margin instead of Clear all's, so Undo keeps its right edge whether Clear all is shown or hidden and the empty board's Undo stays at the left edge, measured in a browser test; the rule predates the PR and stands upstream, so that sheet change is marked upstream. Upstream, the sheets' census: the accent census reads every stylesheet under ui/webview by directory listing (eight today, with a floor so an empty read fails) instead of three named sheets, so a sheet added later joins the pin by construction; it covers CSS sheets only. Fork-only, the tests of the clear door: the no-ledger families Clear all declines and Undo passes over are derived from the kernel's own mint sites at test time (the placeholder form, the hold card's form and the notice helper, seven families today); every derived family must be classified into _CLEARED_NO_LEDGER or the explicit ledger-honouring pair, so a new family reds until it is classified, a mint shape the census does not read reds too, and feed.ts's clearable predicate is evaluated over the same derived set. The reference's held-mail clause states the readers' rule and cites the statement instead of listing the shapes the kernel moves aside, qualifies the read-fault state as a fault on bytes that exist, names the link with nothing behind it as the exception, states the refusal of a store none of whose records could be served, and gives a type-wrong at the build's time, pinned by tests/test_reference_held_mail.py. On the tests' own record: Of the 12 test methods the delta 35fad278c..0a589d1e4 rewrote across five kernel-side modules (tests/test_held_mail_readers.py 1, tests/test_held_mail_reader_guards.py 3, tests/test_held_mail_reader_guards_r2.py 6, tests/test_kernel_citation.py 1, tests/test_task_tracking_switch.py 1), 5 are red over the 35fad278c archive on Python 3.12 at the assertion each pins: UnreadableHoldRingsTheBell.test_a_directory_that_lists_but_cannot_be_searched_rings_the_bell (two rows where one is due), NoticeRowsSplitOnEveryLineBoundary.test_crlf_endings_read_every_row_and_a_skipped_line_carries_no_ending and .test_a_row_that_is_not_utf8_among_cr_rows_still_skips_and_is_said_once (a silent skip, one line where two are due), HeldRecordFaultsWidened.test_a_deep_document_the_parser_returns_is_moved_aside_as_a_list_and_kept_as_a_body_named_by_type (the record moved aside, no card) and ClearDropsCitation.test_clear_handlers_push_drop_to_the_chat (a source pin: dropCitationsAll still sent). 5 were rewritten only to take the writer's reader's (rows, error) pair (regression-2) and pin the rows, not the pair: NoticeRowTypeFault.test_the_writer_side_reader_skips_the_row_too, NoticeKeyNotText.test_the_writer_and_the_sweep_read_past_it_too, NonFiniteNumbers.test_an_infinite_rev_t_or_expiresat_skips_the_row, NoticeLineNotUtf8.test_one_latin1_byte_in_one_row_skips_that_row_and_keeps_the_board and NoticeFileWithABom.test_the_first_row_of_a_bom_prefixed_file_stands; as found each errored over 35fad278c on the tuple unpack of that head's bare list, an error on the new signature, and read shape-agnostically now (_writer_rows) each is green over 35fad278c and records nothing about the old behaviour there (each is red over its own class's archive at its own assertion or on the defect's own exception). HeldRecordFaultsWidened.test_a_document_nested_past_the_limit_moves_aside pins only the arm a parser that returns the deep value takes, so it is green over 35fad278c on 3.12, the interpreter every local figure was measured on, and its red-before is CI's 3.14t cell alone, unverified locally (the local free-threaded 3.14.6 build raises RecursionError at this depth). TheClearAllOp.test_clear_all_builds_no_feed_while_off_and_clears_nothing's rewrite is the _clear_all stub's written=None keyword, an adaptation, green over the archive. So 5 of 12: not 11 of 11, and not an earlier count's 10 of 12, which counted the five unpack errors as reds. Beside the 12, the two harness pins new in 085e08deb (HeldMailReader.test_the_next_case_inherits_no_aside_episode, TheHarnessLeavesNoEpisodeBehind.test_a_closed_root_takes_its_aside_episode_with_it) error over 0a589d1e4 on the registry's name, an error before their assertion; the registry is their subject and their docstrings say so, and the mutation is their evidence.

The where: line above is a locator: it names every file the PR's diff touches and, for kernel/kernel.py and postal/postal_service.py, every top-level function, class or constant a hunk of the diff touches (added, changed or removed), each as a whole word inside its file's parenthetical, with a tag per group and no other prose; a check derives both populations from the diff and holds the line to them. The tags: upstream marks the notices reader and its sweep and the remote-holds panel in kernel/kernel.py (the fault fold, the bell fit and the notices constants with them), every changed definition of postal/postal_service.py (the bus's store readers), the accent rules of the two sheets and the footer dock in feed.css, the accent census and the footer pins among the webview tests, the reference's mail paragraph and state-files paragraph, and the notices and bus halves of the Python tests; fork-only marks the kernel's quarantine road and the clear door (the held-mail reader with its asides and say-so, the hold constants, the cleared ledger's constants and readers, the chip-drop cache and the clearAll handler), the feed pane's Clear all gate, answer and off latch with the federation stamp and the toast rule, the two dropCitationsAll comments, the hold halves of the Python tests and this entry; both marks a class or a clause with a notices half and a hold half, the shared test harnesses among them.
