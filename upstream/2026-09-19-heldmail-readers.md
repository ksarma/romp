---
title: Readers that cannot parse skip the row and say so: the notices rev, the quarantine directory, the light-theme held-mail border
status: candidate
where: kernel/kernel.py (_notice_row_bad_field, _notice_parse_rows, _notice_rows, _notice_rows_unlocked, _compact_notices, _note_read_fault_once, _UTF8_BOM, _note_notice_dir_fault, _note_notice_file_fault, _stat_key_from and the listing arms of _notice_cards and _compact_notices: upstream, the notices reader and its sweep; _held_records and its widened record arms, _corrupt_aside_name, _say_hold_unreadable_once, _end_hold_unreadable_episodes, _hold_bell_text, _note_hold_dir_fault, _quarantine_cards, _clear_all and its build_feed and clearAll call sites, _cleared_undoable, _undo_clear and build_feed's dismissedCount and canUndoClear: fork-only, the kernel's quarantine road F1 F3 F4 F5), postal/postal_service.py (_json_files, _say_unlistable_once, _listable_again, _list_json_records, _hold_rows, QuarantineUnreadable, quarantine_list and the GET /quarantine route, _held_records_bus, _hold_sort_at, _HOLD_SKIPPED_SAID, _say_hold_skipped_once, the _recall outbox arm's listing, quarantine_get and quarantine_decide's not-an-object refusal: upstream, the bus's store readers), ui/webview/feed.css (.fdismiss.fq-ok border: upstream), ui/webview/button-vocab.test.ts (the fq-ok case: upstream), ui/webview/feed.ts (clearable comment: fork-only), docs/reference.md (the mail paragraph: upstream), tests/test_held_mail_readers.py (NoticeRowTypeFault: upstream; ClearAllLeavesHolds and HeldMailReader: fork-only), tests/test_held_mail_reader_guards.py (NoticeKeyNotText and the notice half of NonFiniteNumbers and SweepArchivesSkippedLines: upstream; the hold half, HoldIdsNeverJournaled, UnreadableHoldRingsTheBell, BellRowFits: fork-only), tests/test_held_mail_reader_guards_r2.py (NoticeLineNotUtf8, NoticeFileWithABom, NoticeStoreUnreadableIsSaid, TypeWrongRowKeyNamedOnlyWhenValid: upstream; HeldRecordFaultsWidened, SaidOncePruneReadsASnapshot, UnreadableEpisodeEndsWithTheDirectory, OlderLedgerHoldRowsAreNoBatch: fork-only), tests/test_kernel_trust.py (QuarantineCards: fork-only), tests/test_postal_quarantine.py (HeldMailStoreUnlistable, HeldMailRecordsThatCannotBeParsed, RecallOfAParkedRecordInAStoreThatCannotBeListed, SkippedSaidPruneReadsASnapshot: upstream), tests/test_postal_peers.py (StoreDirectoriesThatCannotBeListedAreLoud and ARootRunSkipsBeforeTheFixturePatches: upstream)
added: 2026-09-19
pr:
tier: fix
offered:
closed:
---
One rule at every site: a reader that cannot parse a row must not report the store absent and must not take down callers that had nothing to do with the bad row; it skips the row, names the session and the key in the log, and keeps the board. Upstream-facing part: one type-wrong rev, t or expiresAt in one row of one session's notices/<sid>.jsonl raised ValueError out of every feed build (the push cycle and GET /feed.json alike); the shared parse now skips that row and says it once per episode, naming the session, key, field and type, never the text. Also upstream: the held-mail Approve button's border reads the accent token through color-mix instead of a dark-accent literal, so the light theme's text and edge share a hue. Fork-only (upstream deleted the kernel's quarantine cards; the fork keeps them): a hold's card stands through Clear-all because the reader takes no cleared ledger and _clear_all declines a hold's id at the one write every door reaches, and the directory reader is the bus's _list_json_records shape ported (a record it cannot read moves aside once and is said; a directory it cannot list is said once per episode; the board is kept). The bus's own readers of its stores take the same rule, and those apply upstream (upstream keeps the bus's held-mail store, its readers and the GET /quarantine route): a directory the bus cannot list is one bell row per episode for an outbox or readbox with the parked mail left standing, a 503 with the reason from GET /quarantine instead of an empty list, and the listing is os.listdir, never Path.glob, which on Python 3.12 swallowed the PermissionError (the recall's outbox arm included). A held record the bus cannot read or parse, or that is not an object, is skipped and said once in the log with the file left in place for the kernel's reader, a type-wrong `at` sorts as the oldest instead of raising out of the sort, and a decide on a record that is not an object is refused in plain words. The notices reader also reads bytes and skips a line that is not UTF-8 (a whole-file decode raised UnicodeDecodeError out of every build), keeps the first row of a file saved with a BOM, and says a notices directory or file it cannot list, stat or read once per episode instead of drawing a clean board.
