---
title: CI runs the browser legs in the vscode-extension job after its Chromium install: a roster, an exclusions file with a reason per line, a parsed census of the tree, a script that holds both to the census before node --test and reads its own reporter after it, and ROMP_BROWSER_LEGS_REQUIRE=1 so a leg that cannot launch fails naming the switch
status: candidate
where: fork PR #887 (branch ci-browser-legs): `.github/workflows/ci.yml` (the step "Browser legs (node --test over ci-browser-legs.txt)" in the vscode-extension job, directly after "Install the pinned Playwright Chromium", env ROMP_BROWSER_LEGS_REQUIRE=1, run `bash scripts/ci-browser-legs.sh`, timeout-minutes 6); `vscode-extension/scripts/ci-browser-legs.sh` (the pre-run roster and exclusions checks against the census, `--list-legs`, `--check`, the post-run read of the reporter's record); `vscode-extension/scripts/browser-legs-census.mjs` (the census: what a browser leg is, read from each test module's tree with the TypeScript compiler; the roster gate; the engines); `vscode-extension/scripts/ci-browser-legs-reporter.mjs` (the node --test reporter whose record attributes every result to its bundle); `vscode-extension/ci-browser-legs.txt` (the roster) and `vscode-extension/ci-browser-legs-excluded.txt` (bundle path TAB reason; the pending class and the grandfather bound in its header); `tools/ci-browser-legs.test.mjs` (the tree test; CI's shell job runs tools/*.test.mjs with no npm ci); `ui/webview/ci-browser-legs-census.test.ts` (the census test in the extension's npm test, with the planted forms under `tests/fixtures/browser-legs-plants/`); `ui/webview/real-viewer-leg-switch.test.ts` (PR #860's executed test of the switch, copied); `CONTRIBUTING.md` (the browser-legs paragraph and the Running the tests block); `docs/batching.md` (the browser-leg item); `vscode-extension/.vscodeignore` (the CI-only files kept out of the VSIX); `tests/shell-portability.bats` (the extension's scripts directory in the portability guard's path list). The hunk in `ui/webview/real-viewer-leg.ts` (inBrowser's read of ROMP_BROWSER_LEGS_REQUIRE) and the `ui/webview/file-view-leg-tree.test.ts` change are PR #860's, carried byte-identical, and stay on that PR's entry (2026-09-19-relay-dial-page-caps-ws-bytes-by-host)
added: 2026-09-21
pr: 887
tier: docs
offered:
closed:
---
Upstream's vscode-extension job has the same ordering (npm test before the Chromium install), so its browser legs skip in its gating job too. An offer carries the step, the script, the census module, the reporter, the tree test, the census test and the CONTRIBUTING paragraph; the roster and the exclusions are re-derived against upstream's own browser legs (13 test sources import playwright there; upstream has no ui/webview/real-viewer-leg.ts and no vscode-extension/scripts/ directory), and the launcher rule needs the shared helper (PR #860's entry) landed there first.
