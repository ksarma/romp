---
title: CI runs the browser legs named in a roster in the vscode-extension job after its Chromium install, with ROMP_BROWSER_LEGS_REQUIRE=1, under which inBrowser fails a launch it cannot make, naming the switch and the reason, instead of skipping, and a script that checks the roster before node --test and reads its own reporter after it
status: candidate
where: fork PR #887 (branch ci-browser-legs): `.github/workflows/ci.yml` (the step "Browser legs (node --test over ci-browser-legs.txt)" in the vscode-extension job, directly after "Install the pinned Playwright Chromium", env ROMP_BROWSER_LEGS_REQUIRE=1, run `bash scripts/ci-browser-legs.sh`, and the job's cap comment); `vscode-extension/scripts/ci-browser-legs.sh` (the pre-run roster checks, `--check`, the post-run read of the reporter's record); `vscode-extension/scripts/ci-browser-legs-reporter.mjs` (the node --test reporter, whose record gives each result the file node's own data.file names for it, or - when it names none); `vscode-extension/ci-browser-legs.txt` (the roster, and the roster rule a reviewer checks); `tools/ci-browser-legs.test.mjs` (the tree test; CI's shell job runs tools/*.test.mjs with no npm ci); `ui/webview/real-viewer-leg-switch.test.ts` (began as PR #860's executed test of the switch, with this branch's third arm, narrowed title and assertion on the script's phrase); `CONTRIBUTING.md` (the browser-legs paragraphs and the Running the tests block); `vscode-extension/.vscodeignore` (the CI-only files kept out of the VSIX); `tests/shell-portability.bats` (the extension's scripts directory in the portability guard's path list); upstream/2026-09-21-ci-browser-legs.md (this entry). The hunk in `ui/webview/real-viewer-leg.ts` (inBrowser's read of ROMP_BROWSER_LEGS_REQUIRE) and the `ui/webview/file-view-leg-tree.test.ts` change are PR #860's, carried byte-identical, and stay on that PR's entry (2026-09-19-relay-dial-page-caps-ws-bytes-by-host)
added: 2026-09-21
pr: 887
tier: docs
offered:
closed:
---
At upstream's main b7bc5818d, read on 2026-09-24, the vscode-extension job has the same ordering (npm test before the Chromium install), so its browser legs skip in its gating job too. That commit has no ui/webview/real-viewer-leg.ts, no vscode-extension/scripts/ directory, no plans/markdown-viewer.md and no tools/*.test.mjs file (the fork's gate-adopt pin is one), and no workflow step there runs one. An offer carries every change the where line lists as this entry's, except the entry itself. The two changes the where line names as PR #860's land first, with that PR's entry, since the roster rule needs the shared helper there. The offer also adds to upstream's Shell job a step that runs tools/*.test.mjs, as the fork's Shell job does in its step "Vendored tooling and host-script tests (node --test)", and re-derives against upstream the roster, from upstream's own browser legs, and the step comment's fork-only references, among them the markdown-viewer plan and its gate-adopt pin and the heads the step was measured at. The roster rule: under the switch, a rostered leg passes only when inBrowser has launched Chromium, and the leg does nothing that lets it pass otherwise (for example: it launches no browser of its own; nothing catches or settles inBrowser's rejection, so the rejection fails its test; it does not change ROMP_BROWSER_LEGS_REQUIRE, and hands inBrowser no test context but the one node gave it; it does not end its own process, from a test, a hook or a timer; no condition the runner can leave unmet stands between a browser test and its inBrowser call; it skips and marks todo nothing). The reviewer of any PR that adds a roster line or changes a rostered leg's source or inBrowser checks the rule; the step does not. Nothing in the tree reads a leg's source for the rule, so the step can read green a rostered leg that breaks it. Examples, not the whole set: a rostered leg that launches its own browser and swallows a failed launch without skipping; a rostered module that launches nothing; a leg that drives a browser from a child process and tolerates the child's failure; a todo test that passes beside a real pass; a leg that catches inBrowser's rejection and passes (a try and catch around the awaited call, .catch(), .then's second argument or Promise's allSettled). A leg built to pass without a browser is outside what the step can detect. tools/ci-browser-legs.test.mjs runs a synthetic leg of each example and reads it green. Nothing checks that every browser leg in the tree is rostered, and main has no such check. The PR no longer claims that every browser leg in the tree is rostered or excluded. The census that made that claim (a parse of every test module with the TypeScript compiler, an exclusions file with a reason per line, and planted forms with their tests) left the PR by the review's round 7 ruling and is kept on the fork's branch ci-browser-legs-census, the corpus for a follow-up that starts with a design note.
