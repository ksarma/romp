---
title: The session host publishes its control socket owner-only
status: candidate
where: docs/reference.md (text); kernel/host_transport.py (module level, HostDirRefused, _open_dir_nofollow, HostDirs, open_host_dirs, _open_file_nofollow, host_stderr_open, host_stderr_size, _rmtree_at, remove_host_dir, _open_host_log, host_log_mark, host_log_rows, host_exit_reason, helper_shape_refusal, write_spawn_spec, HostDirAbsent, _descend, open_host_dirs_if_present, host_file_exists, read_host_file, host_sock_present, HostTransport.connect, HostTransport._read_journal, HostFileForeign, _foreign, _stat_name, HostDirs.__init__, _open_host_file); kernel/judge.py (_ensure_judge_scratch); kernel/sdk_backend.py (SdkBackend._host_transport_for, SdkBackend, SdkBackend._refuse_host_directory, SdkBackend._spawn_road_failed, SdkBackend._spawn_host, SdkBackend._host_orphan_recover, SdkBackend._host_ended, SdkBackend._file_refused_launch_context, SdkBackend._record_refused_launch_position, SdkBackend._host_lease_applies, SdkBackend._refused_directory_row, SdkBackend._file_host_log_rows, SdkSession._amain, SdkBackend.__init__, SdkBackend._file_loose_directory_rows, SdkBackend._loose_rows_new_episode, SdkBackend._refused_launch_log); kernel/session_host.py (module level, Journal.__init__, _b32, sock_names, temp_owner_pid, owner_only_dir, hosts_dir, SessionHost.__init__, SessionHost, SessionHost.run, main, read_journal_dir); tests/README.md (text); tests/test_host_transport.py (module level, SpawnSpec, SpawnSpec.test_a_raising_fchmod_closes_the_descriptor, BackendHostRules, ReadDescent, Pins.test_host_log_rows_are_filed_once_per_line, foreign_uid, BackendHostRules.test_a_file_another_uid_owns_under_a_loose_directory_of_ours_is_a_row_and_the_answer_absent_gets_on_each_read_road, BackendHostRules.test_a_loose_directory_of_ours_on_each_read_road_is_one_row_per_component_and_the_read_proceeds_with_the_mode_unchanged, ReadDescent.test_the_readers_take_names_under_the_descriptor_and_a_link_at_the_name_is_refused_naming_the_file, ReadDescent.test_the_readers_check_the_owner_of_the_object_they_hold_and_a_foreign_file_is_the_answer_absent_gets, ReadDescent.test_a_fifo_at_the_name_does_not_block_the_read_and_is_not_the_file, bind_unix_socket, plant_shape, BackendHostRules._refused, BackendHostRules.test_every_shape_a_peer_can_put_at_the_read_file_is_answered_from_the_table_on_each_read_road, BackendHostRules.test_a_loose_directory_is_one_row_per_connect_episode_per_mode_observed_however_many_descents, BackendHostRules.test_the_connect_loop_opens_the_loose_row_episode_at_its_top_before_the_first_descent, ReadDescent.test_every_shape_a_peer_can_put_at_the_name_is_answered_from_the_table_by_both_readers, ReadDescent.test_an_entry_swapped_in_between_the_stat_and_the_open_is_decided_by_the_fstat_of_the_descriptor, BackendHostRules.test_a_directory_of_ours_with_no_search_bit_is_the_fault_it_is_out_of_the_lease_applies_road_and_the_other_roads_arms_answer_it, ReadDescent.test_a_directory_of_ours_with_no_search_bit_is_the_fault_it_is_from_both_readers_with_nothing_opened, BackendHostRules._loose_sid, BackendHostRules._rows, BackendHostRules._modes, BackendHostRules.test_a_symlink_at_the_read_file_is_the_file_remedy_row_on_all_three_read_roads, BackendHostRules.test_a_foreign_host_log_under_the_spawn_roads_directory_refuses_the_launch_at_the_mark_and_a_refused_arm_reads_none_of_it, ReadDescent.test_the_read_descent_records_a_loose_component_from_its_own_fstat_and_changes_no_mode, ReadDescent.test_every_shape_a_peer_can_put_at_host_log_is_answered_from_the_table_by_the_spawn_roads_readers); tests/test_hosts_path_census.py (Tag, _pseudo_def, _dotted, Fn, Terminal, Census, derive, product_modules, wide_census, plant, HostsPathCensus, main, module level, Census.roads, HostsPathCensus.test_no_by_path_syscall_on_a_hosts_path_remains_except_the_listed_sites, HostsPathCensus.test_no_reader_has_a_by_path_fallback_arm_and_the_forwarding_analysis_would_report_one, Census.run, HostsPathCensus.test_the_views_are_idempotent_and_a_holder_typed_down_a_chain_of_untainted_functions_reaches_its_last_reader); tests/test_judge_scratch_private.py (module level, OwnerOnlyParity); tests/test_session_host.py (module level, system_tmp, padded_root, require_utf8_names, SpawnSecrets._spawn, SocketMode, SocketBudget, StateRootByHostsDir, HostsDir, PaddedRoots, KeptLease, PreludeRefusalRead, SpawnWaitMessageArms, SocketFchmod, HostProcess.test_the_journal_wait_fails_naming_what_it_saw_when_the_records_never_land, HostProcess, PreludeRefusalRead._connect_outcome, PreludeRefusalRead.test_a_peer_planted_host_log_behind_a_swap_after_both_descents_reaches_no_message_no_row_and_no_registry_position, SpawnWaitMessageArms.test_the_spawn_watermark_is_read_through_the_descriptor_so_a_hosts_swapped_at_the_mark_hides_no_reason_of_this_host); tests/test_session_host_sdk_pin.py (VersionCheck.test_the_mismatch_text_carries_the_causes_type_and_message_bounded_like_the_generic_road, HostProcess, HostProcess.test_a_wedged_host_that_wrote_nothing_inherits_no_previous_hosts_reason_or_drift_row_at_the_deadline, host_dir_0700, _with_dirs, _mark, _rows, _reason, HostProcess.test_a_mismatched_sdk_ends_the_host_through_the_crash_record_before_any_cli_or_socket, HostProcess.test_host_exit_reason_reads_the_last_crash_or_spawn_row_and_is_empty_otherwise, HostProcess.test_a_failing_row_with_no_error_an_empty_one_or_a_null_one_says_nothing_and_is_stepped_over, HostProcess.test_an_untested_but_working_sdk_is_filed_as_a_problem_row_naming_the_versions_and_the_remedy, HostProcess.test_an_untested_sdk_is_filed_once_per_kernel_life_not_once_per_launch, HostProcess.test_a_spawn_failure_on_an_untested_sdk_reaches_the_launch_error_with_the_version_it_ran_beside_the_type, HostProcess.test_a_spawn_failure_with_no_sdk_at_all_keeps_the_bare_type_name, HostProcess.test_host_exit_reason_states_the_version_from_the_untested_row_of_this_run_only, HostProcess.test_an_untested_row_written_after_the_failing_row_is_not_that_failures_fact, HostProcess.test_host_exit_reason_says_other_than_for_a_relation_that_is_not_newer_or_older_or_is_absent, HostProcess.test_host_exit_reason_states_the_version_beside_every_spawn_failure_alike_and_never_the_remedy, HostProcess.test_a_missing_binary_on_an_untested_sdk_is_the_bare_type_with_the_version_beside_it_through_the_real_host, HostProcess.test_a_drift_the_sdk_wrapped_as_a_connection_error_carries_the_chain_on_its_row, HostProcess.test_host_exit_reason_reads_past_the_spawn_watermark_never_a_previous_hosts_last_word, HostProcess.test_a_crash_row_after_an_untested_row_is_carried_whole_with_no_version_fact_composed_onto_it, HostProcess.test_the_pinned_residual_a_previous_hosts_unfiled_row_vanishes_after_a_refused_launch, HostProcess.test_the_untested_row_is_once_per_kernel_life_across_sessions_not_once_per_session); tests/test_tempdir_hygiene.py (PrivateTempRoot.test_the_handed_temp_dir_is_recorded_once_and_the_roots_nest_under_it, PrivateTempRoot.test_no_test_pins_a_temp_path_to_a_literal_directory); tests/test_upstream_ledger.py (HostSocketModeWhereLine); upstream/2026-09-18-host-socket-mode.md (text)
added: 2026-09-18
pr: 814
tier: fix
offered:
closed:
---
asyncio.start_unix_server binds and listens at the umask mode, and the chmod to 0600 landed one line later, so hosts/<sid8>.sock stood at the umask mode for the gap: the last member of the create-then-tighten class PR 789 closed for the kernel credential files, found in its round 1 (fresh-5) and split out as its own fix. The only guard then was the state root's mode, which kernel/judge.py chmods to 0700 at import, best-effort (the OSError swallowed) and never re-checked, so the window was out of reach only where that held: a window, not a live hole, on a root that read 0700; the umask is not a guard, since the live session host runs at 002, which leaves the group write bit an AF_UNIX connect needs (an earlier draft of this entry named it as one). The fix checks the published path's length against the socket path budget first (sun_path less its NUL, 107 on Linux: a published path over it is one the kernel can never connect to, and the bind's own check moved onto the temp's name once the temp had a name of its own, so at exactly 108 bytes a first cut published an unreachable socket and logged socket-ready), binds a writer-unique temp name beside the published one (the pid and random digits, the published name's length: the published path is the budget on a deep test root, and a fixed temp name was a shared name two hosts for one sid could cross), tightens it by path (fchmod is a no-op on a bound AF_UNIX socket on Linux), and renames it onto the published path, which is therefore born 0600. During its life at the umask mode the temp has one guard, the mode of hosts/, which both creators (the kernel's write_spawn_spec and the host's bind) make and keep 0700 by code through one helper, owner_only_dir, refusing a symlink or a foreign owner and reading a tighten back; each session's hosts/<sid>/ goes through the same helper on both of its creators (the spawn spec and the host's journal), where round 1 had left it on a bare mkdir. The helper closes the static shape; a re-point between its read-back and the path-taking open that follows needs descriptor-relative calls, and the reachable cases need a state root that is not 0700 while a session starts (ours reads 0700 because kernel/judge.py chmods it at import, best-effort, the OSError swallowed, read back once and never re-checked; nothing guards it afterwards). Every failure on the road is loud and leaves nothing it can vouch for: a socket-bind-failed row with the step, the error class, errno and the path length (never the text, which carries the path), the CLI ended, the lease removed once that CLI is confirmed gone by pid and start-time identity (kept, with a row, while it is not: the kernel's orphan road waits on it, and the transport's word is no proof), exit 1. The kernel connects to the same documented path and no process-wide umask moves. Upstream main carries both halves: its host binds the published path the same way (kernel/session_host.py, start_unix_server then chmod), and both of its roads create hosts/ at the umask's mode (the spawn spec's mkdir with parents=True and the host's own). Round 3 (the reviewer's ruling, 2026-09-19) moved the road's prelude ahead of the CLI: the lease-to-socket interval the kernel's lease-keyed attach roads race had grown on the round-2 head, because the directory work (hosts/ made ours, the budget check, the dead socket's unlink, the stale-temp sweep, whose cost is proportional to the entries in hosts/) ran between the lease write and the bind; it now runs before the CLI is spawned, so after the lease only one lstat of hosts/ (round 4), the bind, the chmod and the rename run, and a prelude refusal starts no CLI and writes no lease. The pair that measures the growth, both figures from the round-3 harness under one condition (cold through the real launcher, a fresh state root per launch with hosts/ holding one entry, 40 launches per tree per pass, two passes): the interval's median is 80.7 and 73.1 us on the base, 267.3 and 261.1 us on the round-2 head, 107.5 and 100.7 us after the reorder, below the 130 to 250 us a lease reader needs to reach its connect, and a caller timed at that band's floor landed in the interval 0 of 80 times on the base, 78 of 80 on the round-2 head and 0 of 80 after the reorder (an earlier version of this entry put the round-2 head's interval at about 292 us beside that pair; that figure was the gap investigation's cold read at the live root's 18 entries, a different population, and is not the pair's). The trade is that the lease appears later by the prelude's cost, about 160 us at a fresh root (the round-2 head's median less the reorder's, 267.3 less 107.5), lengthening the interval from the host's start to its lease, the one a kernel reading a foreign host's state mid-start falls into (it recovers an orphan and spawns a second host). That interval was read at about 670 ms on the production road by a separate investigation (about 80 ms kernel-side from the spawn specification's directory to the host's start, then the SDK import and the CLI's spawn); this PR's harness runs the pipe transport, where it reads about 1.2 ms, so the figure is the investigation's, not this PR's measurement, and 160 us is about two hundredths of one percent of it. That interval is queued as its own change and not addressed here. The same round guarded hosts/ in the host's constructor, above the journal (a symlinked hosts/ is refused before host.log, identity.json or a journal segment exist and before any CLI starts, where through round 2 all three were written through the link and a CLI spawned), made the kernel's spawn-wait message name host.stderr beside host.log for that refusal class, put the errno back on the host-crashed row as an integer only, and gave _cli_gone the identity predicate _write_lease uses.

Round 4 of the review (2026-09-20) found the kernel's spawn road writing through a swapped link: the launcher opened hosts/<sid>/host.stderr by path before Popen, so with hosts/ swapped for a symlink after the spec was written the link's target received that file, carrying the host's traceback with the absolute state root in it, and the round-3 pin that said nothing was written through the link held only because its own launcher redirected stderr outside the state root. The fix, a claim about KERNEL-SIDE WRITES and nothing wider: every kernel-side write under hosts/ on the spawn road, and the two roads that clear a host's directory, go through a descriptor descent (kernel/host_transport.py open_host_dirs: hosts/ opened O_DIRECTORY|O_NOFOLLOW off the state root and verified as a directory this uid owns with no group or other bits, then <sid> the same way relative to it; spawn.json, host.stderr, the published socket's unlink, the two watermarks and the refused roads' reads of host.log take names relative to those descriptors, and remove_host_dir empties hosts/<sid>/ by descriptors, refusing a link at any level), with one window left in the kernel's own process (round 5 of the review, 2026-09-20, correctness-2 and extra5-1; restated to the code's window at round 7, correctness-2 and extra6-2): write_spawn_spec's two directory helpers, sh.hosts_dir for hosts/ and sh.owner_only_dir for hosts/<sid>/, still make and check their directories BY PATH, each in up to five syscalls (its mkdir; on an existing directory the stat pathlib's mkdir(exist_ok=True) makes after the failed os.mkdir to decide whether EEXIST re-raises, which follows a link standing there; its lstat; and, when that lstat read a loose directory of ours, its chmod and the read-back lstat: recorded at round 7's addendum as two on a fresh directory, three on an existing 0700 one, five on an existing loose one), so the window runs from the first helper's first syscall to the second's read-back, inside each helper as much as between them, and a re-point landing anywhere in it is followed by the syscalls after it. By execution at round 7 and its addendum: a link swapped in at hosts/ or at the <sid> leaf between its helper's lstat (which read a loose directory of ours; hosts/ is loose on every install from before 2026-09-19, so the first spawn after this lands takes that chmod on hosts/ for real) and its chmod is followed by that chmod onto whatever the link names, any object this uid owns, a regular file included, where 0700 on a file is a loosening (a 0400 file outside the state root read 0700 after, driven at both components), and the read-back then refuses under the words "stays group/world-accessible" (an lstat of the link itself); the same chmod onto an object this uid does not own ends with EPERM, the launch error with errno 1 and the target's mode unchanged, and onto a target that dangles with ENOENT, refused under the class; a hosts/ re-pointed between the helpers to a directory gets an empty 0700 <sid>/ of ours made in it, or a loose one of ours already there tightened, no content, and the descent then refuses, and when that directory already holds <sid> as a non-directory (a file, a dangling link, a link to a file) the second helper's mkdir ends with EEXIST, refused under the class, nothing made or chmodded; a hosts/ re-pointed between them to a dangling link, a regular file, a link to itself or a directory this uid cannot write ends inside the second helper's mkdir with ENOENT, ENOTDIR, ELOOP or EACCES before any descent runs (the first three refused under the class since round 7, EACCES the launch error with its errno); a refusal is filed as a host.directory-refused problem row with the reason and the remedy and persisted as the launch error with no process started, and the pin drives the production launcher over a real host at four plant points and reads the peer's directory empty at each. The same round closed the create road's own TOCTOU (hosts_dir tightens the root it made by fchmod on a descriptor opened O_DIRECTORY|O_NOFOLLOW right after the lstat, with the read-back an fstat on it, so a link swapped in between reaches no chmod), restated the create road's window truthfully (under umask 000 and 002 the root carries the peer class's write bit in the stretch before the fchmod, which is create, rename and unlink of any name in it, so the exposure is a swap of hosts/, and the create-only scope does not protect a world-writable non-sticky root), put a guard back beside the bind (an lstat of hosts/ immediately before start_unix_server, step bind-hosts, about 3 us; the round-3 reorder had put the CLI's whole start between the last check and the bind), split the spawn-wait message on a watermark of host.stderr so a launch that wrote nothing there is not sent to a previous launch's traceback, and pinned the root's foreign-uid refusal. What remains, by execution: the reads the connect road makes before the spawn on the orphan and leftover arms (the directory's existence, identity.json, the journal's tail) still take paths, so over a state root that is not 0700 a hosts/ swapped for a link is read from the link's target there; the served road's read of hosts/<sid>/host.log (kernel/sdk_backend.py _file_host_log_rows, by path with no descent, at the host's hello and at its exit, after the spawn road's descriptors are closed) takes a path too, so a swapped hosts/ feeds the kernel a peer-authored host.log whose rows it files as this session's problem rows (round 5 of the review found it by hand after the grep the derivation published could not match its call shape; round 7 retired that grep and derives the residual by origin, tests/test_hosts_path_census.py, the round-7 paragraph below); the host's own writes by path (host.log, identity.json, the journal, the socket's bind) are the host's road, guarded by its constructor, its prelude and the lstat at the bind, with the bind's own microseconds after that lstat unguarded; the host also RE-OPENS its own spec by path in its constructor (kernel/session_host.py open(self.spec_path)) and adopts the state_dir and cli_path it names, BEFORE the constructor's hosts_dir guard, which checks only the state_dir the spec gives, so a hosts/ swapped for a link to an attacker's spawn.json in the window between the kernel's descent and the host's spec-open makes the host adopt the attacker's cli_path and state_dir, code execution as the launching uid, present on main as on this head and not closed by this fix (the round-5 review's link lens, 2026-09-20; confirmed by reading and by driving at the residual-conditions commit: the spec is opened by path with no O_NOFOLLOW before any guard, state_dir is taken from it, and the hosts_dir guard afterwards checks the root the spec named, so a host started by hand over a spec naming another root made that root 0700 with hosts/ 0700, bound its socket and wrote its lease under it, and wrote host.log, identity.json and the journal under the spec's own directory; with the named root's hosts/ a symlink the host exited 1 naming that root's hosts/ and never the spec's; and with hosts/ a link to a directory holding a spawn.json naming a marker script as cli_path, the host ran the marker as the launching uid. The spec names cli_path, cwd and the env overlay the CLI runs with, laid over the host's environment, which carries the launch's credential overlay). The precondition is a state root that is not 0700 while a session starts, with a peer holding write on it. Where that holds, by a lab kernel per root origin with the root read by lstat at the host's start: a root the kernel makes, a pre-made 0755 root and a pre-made 0700 root each read 0700, because kernel/judge.py's import mkdirs and chmods the root 0700 by path in the kernel's process before the backend exists (hosts_dir's create-road fchmod is not what repairs them: the kernel's spawn road never takes that road, since the root is on disk before it); a root loosened after the kernel booted (chmod 755 on the root of a running kernel, then a session start) read 0755 at the host's start and the host served, nothing on the spawn road re-reading the root's mode; a root the import's chmod could not tighten (another uid's, a filesystem that ignores modes) gets one stderr line at import and no refusal (by reading). Queued as its own item, owner romp-quickfix-1, in the general notes' small-asks queue (host-side reads before the guard under a non-0700 state root, 2026-09-20), with the derivation of every path-taking read of a spec or under hosts/ before its guard and the fix shape: the host given its state root and its spec by descriptor across exec, or the spec opened O_NOFOLLOW through a descent from a root the host is given and verified first, never from a field of the file being trusted; and a re-point after the publish is followed by the kernel's next connect, and the spawn wait's own poll of the published path and that first connect take that path too, so a re-point during the wait is read there, on every tree, the reach owner_only_dir's and HostDirs' docstrings state, under the same precondition.

Round 5 of the review (2026-09-20) narrowed and widened the refusal class and pinned what the descent's docstrings had enumerated: the two file-level O_NOFOLLOW opens (spawn.json in write_spawn_spec, host.stderr in host_stderr_open) refuse a symlink at the name under HostDirRefused, filed as a problem row with the file's remedy and no process started, where through round 4 the ELOOP was a bare OSError outside the class (no row, no remedy, a launch error composed over a stale stderr tail); write_spawn_spec's wrap of its two directory helpers keys on the helpers' decided refusals (a single-argument OSError, errno None) and a filesystem failure of theirs (a full disk, a read-only filesystem) is the launch error under its own text with the errno on it, no directory remedy and no refusal row, where round 4 folded every OSError into the class; host_stderr_open sets the mode on the descriptor after its open, so a host.stderr a previous launch left at 0664 reads 0600 after the next (every launch before round 4 opened it at the umask's mode, and each of the live deployment's per-session files flips at its next spawn); remove_host_dir's walk skips an entry that vanished since the listing, the semantics of the rmtree it replaced, and reports the directory cleared only when a stat off the hosts/ descriptor says it is gone, where round 4's already-absent arm also caught a FileNotFoundError from inside the walk and reported cleared with the directory standing; and the descent's foreign-uid and group/world refusals, the published socket's unlink by name, host_log_mark's and _open_host_log's descriptors and host.stderr's O_APPEND are each pinned by a case that reds when its arm is reverted (tests/test_host_transport.py SpawnSpec and BackendHostRules, tests/test_session_host.py PreludeRefusalRead and SpawnWaitMessageArms).

Round 7 of the review (2026-09-20; the round-6 rulings) replaced the residual's instrument, restated its window, re-keyed the refusal class and wired the launcher's road. The residual under hosts/ is derived BY ORIGIN, not matched by pattern: tests/test_hosts_path_census.py is a data-flow census over the three modules that mint such a path (kernel/host_transport.py, kernel/session_host.py, kernel/sdk_backend.py), text-only over their sources, seeded on every expression where the literal segment enters a path and on one declared seed (the host's spec_path parameter, the path the kernel hands over in argv, since the walk stops at exec), grown to a fixpoint through assignments, containers, conversions, call arguments and returns (the call recorded as an origin) and a descriptor kind seeded at every os.open on a tainted path or under a tainted dir_fd, and classifying each terminal (a call that gives the value to the operating system) as by-descriptor, by-path, exec-arg, mixed (the descent's own open: hosts/ by path with O_NOFOLLOW, <sid> by name under it) or escape (a value the walk cannot follow, reported, never dropped), with every carrier and mint recorded. The derived set at that commit (54 terminals, 30 carriers, 29 mints, 0 escapes) is pinned against a published table with each member's road; four more pins hold that no other product module makes a syscall on such a path, that every road into a dir_fd=None fallback arm passes a descriptor (the argument for "unreachable today"), that the eleven reader shapes round 6's verifiers planted are all found (the retired grep found 0 of 11), and that every RESIDUAL is a by-path use. The list, kernel side, at that commit: RESIDUAL by path, the queued item's, the connect road before the spawn (kernel/sdk_backend.py:13737, identity.json's existence and the journal glob; :13769, the leftover arm's trigger), the connects (kernel/host_transport.py:740, HostTransport.connect, reached from the attach by lease at kernel/sdk_backend.py:13793, the first connect after the spawn wait at :13987 and the end by lease at :18056), the spawn wait's poll (:13883), the orphan road (:14127 identity.json, :14132 the journal glob and read_journal_dir, whose reads are kernel/session_host.py:479, :481 and :488, and :14153 the replay transport, the same reads from kernel/host_transport.py:993) and the served road (:14384, _file_host_log_rows); THE HELPERS, condition 1's window, by path, kernel/session_host.py:759, :760, :766 and :767 (owner_only_dir's mkdir, lstat, chmod and read-back, reached from hosts_dir at :840, from write_spawn_spec at kernel/host_transport.py:646 and :647, and from the host's Journal at kernel/session_host.py:321); THE HANDOFF, kernel/sdk_backend.py:14090 (subprocess.Popen, the spec path in argv, which the host re-opens by path at kernel/session_host.py:894 before its guard, the deferred road); UNREACHABLE TODAY, kernel/host_transport.py:374 and :408 (the dir_fd=None arms); GUARDS by path, kernel/host_transport.py:144 and :150 and, host side, kernel/session_host.py:1470; BY DESCRIPTOR, kernel/host_transport.py:158, :240, :263, :278, :295, :297, :300, :307, :312, :348, :375, :376, :407, :658 and :662, and kernel/sdk_backend.py:13850; and the host's own by-path road under its constructor's and prelude's guards (the journal, host.log, the prelude, the socket, identity.json). What the census cannot see, from its own follow rules: a segment not spelled as the literal, a value leaving the process by any road but the argv it stops at, a syscall made outside the three files, a receiver it cannot type, a path reassembled from a leaf name, and control flow (it is flow-insensitive within a function, which is why the dir_fd=None arms are listed and held unreachable by the forwarding pin rather than omitted). The list adds no road to the residual and removes none; the basis changed. Condition 1's window, restated to the code above (the round-4 paragraph, which counts the syscalls): each helper's path-taking syscalls, up to five, the chmod following a swapped link onto any object this uid owns (a 0400 file read 0700), and a dangling, file or unwritable target ending inside the helper with a bare errno before any descent. The refusal class is an errno set: write_spawn_spec's wrap files under HostDirRefused a refusal a helper decided and any errno of HELPER_SHAPE_ERRNOS (EEXIST, ENOTDIR, ELOOP, ENOENT) from either helper, worded by helper_shape_refusal, and lets every other errno through as the launch error with its errno; round 5's `errno is None` key had thrown a regular file, a FIFO, a symlink to a file and a dangling symlink at hosts/ or hosts/<sid>/ out of the class (pathlib's mkdir(exist_ok=True) re-raises EEXIST before the helper's lstat), so a peer's one-syscall plant read "[Errno 17] File exists" with no row and no remedy. ENOENT joins the class (between the helpers it is hosts/ re-pointed to a dangling link, a peer's plant, and the descent's own open already words it "does not exist"); ENOTDIR joins it (hosts/ re-pointed to a file between the helpers is a peer's plant; a state root that is itself a plain file, the operator's, gets the same remedy); EACCES and EPERM stay the launch error's errno by the ruling, though a peer can cause them too. Nothing is converted inside the helpers (the judge twin's parity pin). The launcher's call site catches OSError beside HostDirRefused and hands it to _spawn_road_failed (the renamed handler, one sentence per arm: the spec write, the road's descent, the launcher), so an errno from the launcher's descent, its host.stderr open, the fchmod after it or Popen itself is the launch error with its errno where through round 6 it propagated bare and the card read the previous CLI's stale stderr tail. Pinned at function level (tests/test_host_transport.py SpawnSpec) and on the production road (tests/test_session_host.py PreludeRefusalRead), red at the round-6 head on every arm the change touches.

Round 7's addendum (2026-09-20; the round's two verifiers, who planted ten reader shapes the census found four of) widened the census's follow rules by BINDING FORM rather than to the shapes: the walk now indexes class bodies (a class-level constant reads through self, the class, type(self) and self.__class__), binds parameter defaults in their defining scope, resolves a property read to its return and a setter store to its parameter, indexes nested functions and lambdas as scopes of their own, stores an attribute on a receiver it cannot type by name and reads it back by name, follows a dynamic getattr on a typed receiver, an exception's filename, a match capture, a global or nonlocal name, and decides its pure, store and sink lists by one question (can a hosts path flow through the call); and every form it does not follow is an ESCAPE printed with file, line and the form named (an unresolved call with a tainted argument, an unknown method on a tainted receiver, a callable applied outside the walk beside a path, a store under a dynamic name, an unknown decorator on a function carrying a path, a dynamic read on an untyped receiver while a path is stored that way), pinned empty over the tree, so what the census cannot see is what it prints, plus two kinds by construction: a path across exec (followed to the exec boundary, re-seeded on the host's side) and a syscall inside a C extension or a library given a value it classed. (Retracted in the third addendum, below: the escape rule covers six forms and no others, and a value the walk drops at any other form is a silence; the second addendum's verifiers planted twenty-four forms at the addendum's commit and twenty-one were silent, none of which carries a hosts path in the three files at that head, by their survey.) Twenty-seven planted shapes are found or printed: the round-6 eleven, the verifier's ten and five extras (map(open, paths) the one escape among them), and three the builder wrote down before the rules. The derived set at the addendum's commit: 54 terminals, 36 carriers, 29 mints, 0 escapes; the six added carriers are HostDirs holders handed to functions of the files, which the tag rule could not see (host_stderr_size(dirs) at three sites among them). ELOOP in the shape class is pinned by hosts/ re-pointed to a link naming itself between the helpers (a self-link standing at hosts/ before the first helper is EEXIST, pathlib's is_dir() swallowing the loop, and that arm now says so); helper_shape_refusal names the component the errno's filename is, so a dangling-link state root reads `state root ... is not a directory` where it read `host directory`.

Round 7's second and third addenda (2026-09-20; the reviewer's ruling on the addendum: close the road, and do not build the census inversion). The kernel's reads under hosts/ that still took a path on the connect, orphan and served roads take the descent: hosts/ opened O_DIRECTORY|O_NOFOLLOW off the state root and <sid> the same way relative to it, each fstat-verified a directory of this uid, the file or the name then taken relative to the verified descriptor (host_transport.open_host_dirs_if_present, host_file_exists, read_host_file, host_sock_present), a refusal filed as one host.directory-refused row with the remedy through the shape the spawn road files (sdk_backend._refused_directory_row). Converted: identity.json's existence in _host_lease_applies (a refusal answers False with a row, and with hosts off the session runs as a kernel child); the leftover trigger in _host_transport_for (a refusal is the launch's, one row and the launch error, before the spawn road's helpers); the spawn wait's poll, now a stat of the published name under the held hosts/ descriptor; identity.json on the orphan road (a refusal files a row and replays nothing from under the directory); host.log on the served road at the hello and the exit (a refusal files a row and none of the file's rows). Two departures from the spawn road's descent, both stated in open_host_dirs_if_present: the mode is not a condition on a read (the removal road's position, and the reads run before the helpers that repair a loose hosts/, which reads 0775 on every install from before 2026-09-19 until the first spawn after this lands), and an absent component is an answer (HostDirAbsent, a subclass of HostDirRefused, turned into None) rather than a refusal. Unconverted, each named at its line and its own change by the ruling (superseded by the fourth addendum's paragraph below: the connect is a permanent residual, the five journal sites one follow-up item): the connect (HostTransport.connect, asyncio.open_unix_connection by path from the attach by lease, the first connect after the wait and the end by lease; connect has no descriptor-relative form), the two journal globs (_host_lease_applies, _host_orphan_recover) and sh.read_journal_dir's three reads (the glob, gaps.json, each segment; a directory-path interface whose descriptor form is a scandir under the <sid> descriptor); on the refused orphan road they are not reached, elsewhere they run after the descent has verified the directory, so a re-point landing between the descent and them is read through the link. Pinned by execution, one case per road (tests/test_host_transport.py BackendHostRules, ReadDescent; tests/test_session_host.py PreludeRefusalRead), each red at the addendum's commit with the peer's content read through the link. The census (tests/test_hosts_path_census.py) changes job: from a published road and provenance for every member to the pin that no by-path syscall on a hosts path exists except the listed sites (RESIDUAL, each by role with its reason: unconverted 6, helper 4, guard 3, unreachable 2, handoff 1, host 17 at this head), with the converted reads held by-descriptor; its printer line at this head, `terminals: 52, by-descriptor: 19, residual: 33 (unconverted 6, helper 4, guard 3, unreachable 2, handoff 1, host 17), escapes: 0, carriers: 41, mints: 34, seeds: 8`. The never-a-silence promise is retracted, not repaired: the escape rule covers six forms, a value dropped at any other form is a silence, and what a silent miss costs now is a regression guard, since the road is closed by the code; the exception rule reads a class's bases beside its name (HostDirAbsent was an escape at the second addendum's commit). This touches kernel/sdk_backend.py and kernel/host_transport.py.

Round 7's fourth addendum (2026-09-20; the reviewer's ruling of 19:12Z on the third addendum's stated departure, the read descent's unchecked mode). The ruling: neither refuse a loose directory on a read road (a denial of service on every install whose hosts/ was made at the umask before 2026-09-19) nor move the repair ahead of the read (the chmod is the spawn road's helpers', and a repair does nothing about a file already planted); instead the readers check the OWNER of the file itself, and the mode is read, filed and left alone. The owner check: read_host_file fstats the descriptor its O_NOFOLLOW open returned and host_file_exists fstatats the name under the verified <sid> descriptor with no link followed (never a path stat, which would reopen the re-point window the descent closes); a file whose st_uid is not this euid raises HostFileForeign (a subclass of HostDirRefused parallel to HostDirAbsent, `file` and `uid` set, the directory in its text), which each read road files as one host.directory-refused row naming the file, its directory and the owning uid (sdk_backend._refused_directory_row: an owner remedy, the `file` and `uid` fields) and then answers as it does for an absent file: False on the lease-applies road, whose journal listing then runs; raw None on the orphan road (no identity vouches for the registry's ack, the offset is -1, and the journal reads that follow are by path until the follow-up lands, so a journal file the same peer planted is replayed); return on the served road (none of the file's rows, no position). The mode row: the read descent records each component with group or other bits from the fstat it already makes (HostDirs.loose: what, path, mode) and the road files one host.directory-loose row per loose component per descent (SdkBackend._file_loose_directory_rows: the path, the mode in octal, the remedy naming the spawn road's helpers), then proceeds (the fifth addendum, on the reviewer's ruling of 20:40Z: once per connect episode per mode observed, not per descent); no read road refuses on the mode or chmods, pinned by the mode read before and after each road. The symlink row: a link at identity.json is refused naming the file on the lease-applies road too (host_file_exists raises the file-shape refusal instead of answering False), so all three read roads file the file remedy for the same plant. Beside these, read_host_file's open carries O_NONBLOCK and a non-regular file at the name (a directory, a FIFO, a socket) answers None, so a FIFO a peer planted no longer blocks the connect until a writer appears. (The fifth addendum: FALSE for a socket at this addendum's commit. The fstat that answered None was of the descriptor the open returned, and open(2) answers ENXIO for a UNIX socket before it returns one; the orphan and served roads catch OSError around their read, so a peer's socket at identity.json or host.log was answered absent there with no owner row while the lease-applies road filed one for the same plant. The fifth addendum's paragraph below has the correction: the owner question is asked of the entry at the name before anything is opened.) THE CONNECT IS A PERMANENT RESIDUAL, closed as an open item: HostTransport.connect's asyncio.open_unix_connection takes the socket's path because connect(2) has no dir_fd form, and a connect through /proc/self/fd or a chdir on the descriptor is a different mechanism, none queued; its precondition is a state root a peer can WRITE, since the swap of hosts/ is a rename in the root and the spawn road's helpers tighten hosts/ itself to 0700 before the unlink, the spawn and the poll. THE JOURNAL READS ARE ONE FOLLOW-UP: the two globs (_host_lease_applies, _host_orphan_recover) and sh.read_journal_dir's glob, gaps.json read and segment open are the five sites of one queued item, "the journal reads descend by descriptor" (the general notes' small-asks file), the comment at each site pointing at it, read_journal_dir's docstring included. The census (tests/test_hosts_path_census.py) replaces the UNCONVERTED role with PERMANENT (asserted to be exactly the connect) and FOLLOW-UP (asserted to be exactly the five sites, each reason naming the item, non-empty while it is open), and holds the owner check's fstat by-descriptor; its printer line at this head, `terminals: 53, by-descriptor: 20, residual: 33 (permanent 1, follow-up 5, helper 4, guard 3, unreachable 2, handoff 1, host 17), escapes: 0, carriers: 47, mints: 34, seeds: 8`. THE PRECONDITION, stated precisely (the escalation check the ruling asked for): the connect residual needs a peer who can write the state root; the journal residual is reachable by a peer who can write hosts/ and traverse the root WITHOUT writing it (a root that is not 0700, which the kernel repairs at import best-effort and never re-checks, under a hosts/ at 0775 with a shared group, the mode every install from before 2026-09-19 carries until its first spawn after this lands), by re-pointing <sid> between a read road's descent and its glob, and under a weaker precondition still (the fifth addendum): a group-writable hosts/, or a loose hosts/<sid>/ of ours, under a root the peer can traverse. Stated precisely (the fifth addendum, replacing this sentence's "under a 0700 root neither is reachable"): under a root that is 0700 before any peer held a descriptor inside it, neither is reachable; a peer who obtained a directory descriptor on a loose <sid>/ while the root was traversable keeps creating entries there until a spawn tightens that directory, since a create through a held descriptor is checked against that directory's mode and not an ancestor's. Pinned by execution in tests/test_host_transport.py (ReadDescent: the owner check at function level, the loose record and the unchanged mode, the FIFO; BackendHostRules: the owner check on the three read roads before and after, the mode row on the four read descents, the symlink row on the three roads), each red at the third addendum's commit at its own assertion; tests/test_session_host_sdk_pin.py's host.log fixtures make their directories 0700 (host_dir_0700), the shape the spawn road makes, since at the umask the served road now files a loose row in front of the rows those pins read. This touches kernel/sdk_backend.py, kernel/host_transport.py and kernel/session_host.py (read_journal_dir's docstring).

Round 7's fifth addendum (2026-09-20; the two verifiers of the fourth addendum, and the reviewer's ruling of 20:40Z on the loose row). THE SOCKET, AND THE TABLE IT EXPOSED. The fourth addendum's sentence that a non-regular file at the name, a socket among them, answers None from read_host_file was false for a socket: the owner check was the fstat of the descriptor the open returned, open(2) answers ENXIO for a UNIX socket before it returns one, and the orphan and served roads catch OSError around their read, so a peer's socket at identity.json or host.log was answered absent on those two roads with no owner row while the lease-applies road (host_file_exists, a stat by name) filed one for the same plant. Now the owner question is asked of the ENTRY at the name before anything is opened (host_transport._stat_name: a fstatat under the held <sid> descriptor with no link followed, shared by host_file_exists and read_host_file): an entry another uid owns, of ANY kind (a regular file, a symlink, a directory, a FIFO, a socket), is HostFileForeign; a symlink of ours is the file-shape refusal; a directory, a FIFO or a socket of ours is None or False with nothing opened, so the FIFO is no longer opened either; then read_host_file opens by name with O_NOFOLLOW|O_NONBLOCK and fstats the descriptor, the authoritative check when the entry changed between the two, deciding the same way; and what the open itself can answer after the stat said a regular file of ours is mapped to the same table: ENOENT None, ELOOP the file-shape refusal, ENXIO the owner question asked once more by name and then None, every other errno (EACCES, EMFILE, EIO) propagated as the fault it is, never swallowed here. The population, derived: the kinds mknod-free code can create (absent, a regular file, a symlink to a file, a dangling symlink, a directory, a FIFO, a UNIX socket) by owner (ours, another uid's; absent has none), 13 rows; times the two readers, 26 cells; times the three read roads, 39; a device node excluded, since mknod of one needs CAP_MKNOD, which no peer here has. Pinned cell by cell from one table (tests/test_host_transport.py SHAPE_TABLE: ReadDescent at function level with every open spied, BackendHostRules on the three production roads, and a swap of the entry inside the open for the second check), the foreign-socket cells on the orphan and served roads and the foreign-symlink cells on all three roads red at the fourth addendum's commit, and read_host_file's OSError ENXIO at function level. THE STUB. foreign_uid swaps the owner on os.fstat of a descriptor and on os.stat or os.lstat with dir_fd given ONLY, counts every path-form stat of the planted entry (os.stat, os.lstat and through them Path.stat, Path.exists, Path.is_file), and every behavioural pin asserts the count 0, so an owner check moved onto a path stat reds the module and not only the census (the fourth addendum's stub swapped the path form too, and the verifier's path-stat mutation left the module green with the census alone red). THE LOOSE ROW. Once per connect episode per distinct mode observed (the ruling of 20:40Z), not once per descent: SdkBackend._file_loose_directory_rows keeps, per sid, the mode last observed at each component (HostDirs.modes, every component the descent admitted, tight ones too) and files a row when a loose mode differs from it, so a transition is filed and a stable directory is not refiled; SdkBackend._loose_rows_new_episode, called at the top of each iteration of SdkSession._amain's connect loop before the iteration's first descent, forgets the previous episode. One hosts-off connect with a leftover tail makes three descents (the lease-applies read, the leftover trigger, the orphan road) and files one row per component where the fourth addendum filed three; a mode planted between two descents files a row naming it; the next connect files a still-loose directory again. The refusal rows carry no latch and need none: a refusal ends its road, so one per call is one per episode. THE PRECISION of the precondition, stated in HostDirs' docstring, in the fourth addendum's paragraph above and in the PR: under a root that is 0700 before any peer held a descriptor inside it, neither residual is reachable; a peer who obtained a directory descriptor on a loose <sid>/ while the root was traversable keeps creating entries there until a spawn tightens that directory. The journal residual is reachable under the weaker precondition (a group-writable hosts/, or a loose hosts/<sid>/ of ours, under a root the peer can traverse), and its follow-up, "the journal reads descend by descriptor", is the PR that follows this one, on the reviewer's word of 20:40Z. THE DESIGN SENTENCE (the half of the reviewer's deferral condition that IS met, and why the design works): the owner check is available on a held descriptor at EVERY read site under hosts/<sid>/, as the fstat of the opened descriptor for a read and as the fstatat of the name under the held <sid> descriptor for an existence question or a question asked before an open, so a sixth read site written later has the check at hand in both forms and needs no path stat. The census printer line at this head: `terminals: 53, by-descriptor: 20, residual: 33 (permanent 1, follow-up 5, helper 4, guard 3, unreachable 2, handoff 1, host 17), escapes: 0, carriers: 50, mints: 34, seeds: 8` (carriers 47 to 50, the calls into _stat_name; CONVERTED holds _stat_name's stat where it held host_file_exists's; every other figure as at the fourth addendum's commit). This touches kernel/host_transport.py, kernel/sdk_backend.py, tests/test_host_transport.py and tests/test_hosts_path_census.py.

Round 7's sixth addendum (2026-09-20; the two verifiers of the fifth addendum). THE FAULT CELL, DISCLOSED. The fifth addendum changed one answer without saying so: a hosts/<sid>/ of OURS with no search bit (0600, a mode no romp code path makes; the helpers make 0700) on the lease-applies road. The descent admits such a directory (its O_RDONLY|O_DIRECTORY open needs the read bit) and the fstatat of identity.json under it refuses (the search bit), a PermissionError with errno EACCES, a fault of the directory and not a shape at the name. Through the second, third and fourth addenda host_file_exists answered False to it through an `except OSError` arm around its stat, a silent answer to a fault where the repo's rule is to surface one, and none of those addenda said so; the fifth addendum's _stat_name dropped the arm, so the fault propagates out of host_file_exists and out of _host_lease_applies, which has no OSError arm, to the connect loop's handler (SdkSession._amain's except records it as the session's launch error and ends the connect; the kernel log carries the traceback), and the fifth did not say so either. That is the answer this PR's base gave: there the existence read was `(hdir / "identity.json").exists()` by path, and pathlib re-raises every errno but ENOENT, ENOTDIR, EBADF and ELOOP, so the same directory raised the same PermissionError out of the same method (verified on Python 3.12.3 with a 0600 directory). The population, derived: the fault times the two readers times the three read roads. Both readers raise it with nothing opened. The lease-applies road raises it out, the base's answer restored. The orphan road's OSError arm around its identity read swallows it (raw None, as the base's `except Exception` around read_text did), no identity vouches for the ack, and the road then raises the same PermissionError from its journal reads by path (the glob lists a segment, listing needing the read bit only; read_journal_dir's open of it needs the search bit), the queued follow-up's sites, which this PR does not touch. The served road's OSError arm, the base's, returns: none of the file's rows, no position. No code changes: the disclosure is in host_file_exists's docstring, read_host_file's (whose sentence that the roads' own OSError arms answer a fault is made precise: the orphan and served roads have one, the lease-applies road does not), _stat_name's and the comment at _host_lease_applies. Pinned by execution in tests/test_host_transport.py: ReadDescent, the fault from both readers with nothing opened (os.open spied), not a refusal of any shape; BackendHostRules, the three roads as above, the fault's frame read from its traceback (the owner question's stat on the lease-applies road, read_journal_dir on the orphan road), no refusal row and no loose row on any road, the mode unchanged. Red at the fourth addendum's commit on the lease-applies arm and on host_file_exists, `PermissionError not raised`; green at the fifth addendum's commit, the code being the same; red again under the mutation that puts the arm back into host_file_exists and under one that gives the lease-applies road an OSError arm, at the same assertions. The census printer line at this head is the fifth addendum's, no syscall having been added or moved. This touches kernel/host_transport.py and kernel/sdk_backend.py (docstrings and a comment) and tests/test_host_transport.py.

Round 7's seventh addendum (2026-09-20; the record-body verifier's second pass on the sixth addendum, two findings). THE LEAD MADE TRUE. The PR record's lead sentence, that every kernel read of identity.json and host.log under hosts/ asks the owner question before opening, was false for the SPAWN road's host.log readers: host_log_mark stated host.log by name under the held <sid> descriptor and read the size with no owner compare, and _open_host_log opened the name O_RDONLY|O_NOFOLLOW under the same descriptor and fdopen'd it with no fstat and no compare, reached through host_log_rows and host_exit_reason on the two refused arms of sdk_backend._host_transport_for and directly from _record_refused_launch_position. The population, derived before the fix: every kernel read (an open, a stat, a fdopen, a scandir) of a file under hosts/<sid>/ by name under a held descriptor in kernel/host_transport.py and kernel/sdk_backend.py, by road. Read roads (lease-applies, orphan, served): _stat_name's fstatat of identity.json or host.log, read_host_file's open and fstat and fdopen; all asked. Spawn road: host_log_mark's stat of host.log before the spawn (from _host_transport_for), _open_host_log's open and fdopen on the refused arms (host_exit_reason, _file_refused_launch_context through host_log_rows, _record_refused_launch_position); none asked. Also under the descriptor and outside the lead's population (host.stderr, not host.log): host_stderr_size's stat, a size and never content, taken before the launcher and compared only after a launcher that opened host.stderr itself and fchmod'd it 0600, which on a file another uid owns ends with EPERM (or EACCES at the open) as the launch error before any compare; left as it is and named here. The removal road's scandir and unlinks under the verified <sid> descriptor (_rmtree_at) delete and read no content. The poll (host_sock_present) and the socket's unlink take the published NAME under hosts/, not a file under <sid>/. THE FIX: the spawn road's readers take the HostDirs the road holds across its wait (host_log_mark(dirs), _open_host_log(dirs), host_log_rows(dirs, since), host_exit_reason(dirs, since); sdk_backend's two refused-arm helpers take it too) and ask through the read roads' two forms: host_log_mark reads the size off _stat_name's answer (a regular file of ours its size; no entry, or a directory, a FIFO or a socket of ours, 0; an entry another uid owns HostFileForeign and a link of ours the file-shape refusal, both raised); _open_host_log opens through _open_host_file, the one reader read_host_file's body became (the owner question of the entry at the name, the O_NOFOLLOW|O_NONBLOCK open by name, the fstat of the descriptor it returned, the open's ENOENT, ELOOP and ENXIO mapped to the same table), so a FIFO of ours at host.log, whose open blocked the refused arm through the sixth addendum (no O_NONBLOCK, no stat before it), is None with nothing opened. The readers' OSError arms for a fault (0, [], return) are the base's, and each re-raises the refusal class ahead of its arm, since HostDirRefused is an OSError. The two by-path arms the readers kept for a call with no descriptor (dir_fd None, UNREACHABLE in the census, held so by the forwarding pin) are gone with the signature: a reader handed no HostDirs is a TypeError, not a path. THE CALLERS' ANSWERS, the shape table's fourth road: at the mark a foreign host.log (or a link) refuses the launch the way every refusal of that road is refused (_refuse_host_directory: one host.directory-refused row with the owner's remedy, the launch error was not started, no process started); on a refused arm the three reads run in one place (_refused_launch_log: the reason, the untested row, the position) and a refusal at the first is one row with the arm's clause and the absent file's answers for all three (no reason, no untested row, no position, none of the file read), the served road's cell, the reads after the refused one not made, which keeps the row at one per refusal met. WHY THE SPAWN ROAD CAN MEET A FOREIGN FILE under a directory its descent verified 0700 and ours: sh.owner_only_dir tightens a loose directory of ours and keeps every entry a peer planted while it was loose, and a spawn meets a standing directory on two roads that clear nothing, a stale kernel-held lease (host_lease_state "none" with a lease, so the leftover trigger does not run and remove_host_dir is not reached) and a leftover the orphan road's remove_host_dir reported not cleared (a foreign directory inside it); the orphan and leftover roads clear the directory before their spawn, so remove_host_dir does not run before every spawn. No race remains after the mark: once the helpers have tightened <sid>/ to 0700 a create, a rename or an unlink in it is checked against the directory's own mode, a held descriptor included, so the entry at host.log changes after the mark only by this uid (the host creates it by path when none stands); a peer holding a descriptor on a file it planted can still write to it, which the owner question refuses whatever the bytes. The refused arms' handler is therefore the design's (every read asks) and not a road a peer has. PINNED by execution in tests/test_host_transport.py: ReadDescent, the shape table's 13 rows at host.log under a 0700 <sid>/ of ours put to the three readers through the spawn road's own descent, 39 cells, os.open spied (nothing opened but a regular file of ours, once per opener; the mark opens nothing), the stub's path-stat count 0; BackendHostRules, the production road through _host_transport_for with the launcher replaced: at the mark, on the stale-lease road, the launch refused with the owner row and no process started; on the refused arm, the file planted by the launcher's stand-in after the mark, one row with the arm's clause, no reason of the peer's, no host.sdk-untested row, no hostLogPos. The road case reaches its assertions at the sixth addendum's commit (the launcher was called; the peer's reason was the card's); the function-level case errors there at the call (the readers took state_dir, sid and dir_fd), a red before its assertion and no evidence, so the behaviour at that commit is a pasted probe through the old signature (the mark returned the size, the rows and the reason were read) and the pin's own red is the mutation's: the spawn road's readers put back to the sixth addendum's form red 14, every foreign and link cell and every nothing-opened assertion; the shared owner compare dropped from _stat_name red 32, both roads. tests/test_session_host_sdk_pin.py drives the readers through the read descent (helpers at module level), tests/test_session_host.py's mark wrapper takes the HostDirs. THE CENSUS (tests/test_hosts_path_census.py) at this commit: terminals 48, by-descriptor 17, residual 31 (permanent 1, follow-up 5, helper 4, guard 3, unreachable 0, handoff 1, host 17), escapes 0, carriers 52, mints 32, seeds 8; the two UNREACHABLE by-path arms and the three by-descriptor terminals of the old readers are gone from the derived set, CONVERTED names _open_host_file's fstat and fdopen, the forwarding pin now holds that no reader has a dir_fd=None fallback arm and shows the analysis live on a planted one. The census met an engine defect of its own here and fixed it: a scope was re-walked after round 0 only when something tainted could move it, so a HostDirs handed down a chain of untainted functions was typed only as far as round 0's (non-source) order reached and the views typed the rest lazily, members() not idempotent, two plant cases red at this addendum's first cut; a holder-typed name now makes its scope live and a pin holds the view idempotent over holder types. THE WHERE LINE is regenerated by the rule the ledger test pins, applied to the -U0 hunks of the four addenda since the pushed head over every definition a hunk falls in (each method a hunk lies in as Class.method, the class when a hunk touches a class-level line, module level outside any def), added to the names the earlier passes derived: HostDirs.__init__ (the fifth addendum's hunk) and every definition this commit touches are named (two of them, Census.run and the census's idempotence pin, in a second commit: the line had been derived from the working tree before the census's fixpoint fix and that pin were written).
