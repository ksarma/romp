---
title: Session tab emoji: one emoji before the name on a session's tab, set from the tab menu (`setSessionEmoji` WS op, `emojiSet` confirm), by the session itself (postal `set_emoji`), or by `romp emoji` (both via `POST /emoji`); one kernel validator (exactly one emoji, nothing textual, no non-string or unpaired surrogate, UTS #51 where the property tables reach: skin tones only on Emoji_Modifier_Base and directly after it, tags only on the black flag, at most four ZWJ parts; RGI membership itself is not checked; Unicode 16.0 tables, 48-byte cap) and one store write (the names registry's fifth field, carried by every writer of the record, the tmux resume/revive path included; a colorless record is colored before it gets a fifth field, and bin/romp's readers split on single tabs, so an empty color run can never shift the emoji into the color slot; the kernel's four names writers share `_NAMES_LOCK`; the SDK/Codex module writers and the shell rename hook stay outside it); rides the tab_meta push, federation included, and a remote kernel without the route is reported as version skew, not a dead tunnel
status: approved
where: fork PR #246 (branch `emoji`, merged 2026-09-06): kernel/kernel.py `_emoji_check` / `_set_session_emoji` / `_NAMES_LOCK` / `_remote_forward_status` / `/emoji`, postal/postal_service.py `set_emoji`, bin/romp `emoji` + `_romp_split_record`, ui/webview/render.ts + tab-meta.ts
added: 2026-09-06
pr: 246
tier: feature
offered:
closed:
---
Pure feature, no fork-specific content; every door pinned by tests (tests/test_session_emoji.py, tests/test_postal_emoji.py, tests/romp.bats, ui/webview/tab-meta.test.ts, vscode-extension/src/tab-emoji.test.ts); review round 1 (2026-09-06) fixed the resume-path field drop, the tab-collapse misread, the surrogate 500 and the non-string clear. Upstream ships the same names registry, tab menu and postal tools, so the shape ports as-is; the SDK/Codex `write_name` fifth-field carry is the one hunk outside the feature's own files. Round 2 (2026-09-06): the validator's invisibility test is table-first (Python 3.12/3.13's Unicode database predates the 16.0 tables and had it call a 16.0 emoji an invisible character), `romp emoji <id>` reads a dormant session's emoji from the names registry with the kernel stopped, and the bats suites' bare-`!` negations (vacuous mid-test) are `run` + status, with tests/test_bats_bare_negation.py guarding the form. Round 3 (2026-09-06): `GET /emoji?target=` is the read half of the POST, forwarded to an attached host the same way (an older remote's 404 is version skew), so `romp emoji <id>` reads a session another machine owns; the flag and keycap paths name a stray invisible character as the generic path does, and an unassigned code point is named as outside the tables rather than called invisible; bin/romp's names-record writer is atomic (temp + `mv`) and the kernel's four names writers never publish over a record that reads with no name (one re-read, then leave it and report the sid); the Emoji… dialog closes on its own late confirm after the backstop (a pure, tested decision function); the tab menu's glyph box clips an unjoined chain instead of pushing the row text; the `.tab-emoji` sheet comment states the exposed-glyph design.

Status detail (migrated from the table): **candidate**

APPROVED 2026-09-07: the fork owner said offer it (batch 13, tier feature — the SDK/Codex `write_name` fifth-field carry is the one hunk outside the feature's files; fork PR #278 (`emojipicker`, draft, 2026-09-07) builds a picker on top and should ride behind the offer or be folded in before it).
